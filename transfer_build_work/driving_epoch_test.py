# DECISIVE: where does the radiation-driving decay happen per mode (CAMB Weyl potential)?
# high-l (l>=1400, k>=0.1): 77-98% PRE-seam = collapse L2 (radiation-included) = matched to LCDM by the
#   time-reversal equality; L1 (radiation-free) is POST-seam and does not touch it -> high-l peaks MATCH
#   LCDM -> settled 9% r_D -> REAL high-l deficit. Corpus "L1 removes the high-l boost" misattributed.
# mid-l (l~700, k=0.05): only 32% pre-seam -> the CR-specific (post-seam L1) driving lives at MID-l.
# So the corpus low-l-matches/high-l-open split is INVERTED.
