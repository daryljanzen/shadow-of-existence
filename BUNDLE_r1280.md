# Bundle r1280 — the two-sided structure migrated open→closed, and the unification made explicit

Daryl-directed, across a few turns. A **major "open" item fully and completely moved to the closed
section**, its owed resolution built richly into P7's synthesis, and the scope of the unification the
synthesis had never stated made explicit. Builds on r1279. **P7: 49pp, zero errors, zero undefined refs,
every citation resolving.**

## The find (Daryl)
> "This is the corpus six ways from Sunday telling you it isn't what it is… a giant tangle the corpus
> resolved all over the place but there's still a knot bound at the centre that says all the loose organised
> strands are knotted together by the knot that it ties and won't let go of."

P7's frontiers item 2 held the **sheet-to-ruling assignment** as "a genuine open problem, held
do-not-assert." But P13 (`boundary_paper`) had **already resolved it** — the CPT factorisation
`C=(Q↦−Q)_field ∘ (R∘K)_geometric`, with `R` (mass-reflection, r-axis, linear) and `K` (reality involution
τ̃↦τ̃̄, τ̃-axis, antilinear) the **two axis-symmetries of the one Plate ℂ_r×ℂ_τ̃** (`prop:conjugation-closure`;
The Plate caption's own words, *"Daryl's P13/P14 conjecture"*). The rulings and the wings are not two
candidates for a bijection but the **linear and antilinear faces of one object**. A downstream paper had
untied the knot; the flagship never updated. The staleness the open-problems axis exists to catch.

## The move (three parts, all in `CR_framework.tex`)

**1. Built the closure — `\subsection{The two-sided closure…}` (`sec:two-sided-closure`), in the synthesis
where §1061 already set up R.** Two rich paragraphs: R/K as the two Plate axis-symmetries (rulings on R's
r-axis, wings on K's τ̃-axis — *not* a bijection to derive); the antimatter progenitor as R's r<0 branch; the
C-factorisation; the r=0 crossing as C's kinematic vertex; the residual named as the matter sector's, not the
framework's. Reciprocal to `JanzenBoundary`/`JanzenMatter`.

**2. Migrated the open off.** DELETED frontiers item 2 (SEVEN→SIX; all its content was recap of §849–871).
Inherited-data promoted to item 2 — which **auto-corrected** two stale `item~2` radiation-datum refs in item 1.
Redirected §860/§867/§871 from "held do-not-assert (§frontiers)" to the closure.

**3. Made the unification explicit — `\subsection{The scope of the unification}` (`sec:unification-scope`),
the synthesis capstone.** The point the synthesis never stated: three structures standard physics files under
separate theories — **GR's vacuum solution space, QFT's discrete CPT/charge-conjugation structure, and the
Standard Model's su(3) with ℏ** — are "not three unifications owed but one maximally symmetric object read
three ways: on its cuts, on its discrete residue, and on its two real forms." su(3) promoted from a buried
caveat to a pillar. And the "not inherited" half, carried into the whole-corpus synthesis for the first time:
the black-hole family, closed timelike curves, the problem of time, the hole argument are "not inherited but
dissolved… the unification reached not by adding structure to repair the troubles but by the reading under
which they do not arise." Weighed at coherence, handing off to the existing discipline.

## Logged
- `THE_OPEN_PROBLEMS_LEDGER`: item 2 (sheet-to-ruling) **KILLED r1280** → SIX live.
- `THE_CLOSURE_LEDGER` Part 2: first worked corpus-trace (WORKED-SHUT, migrated).
- No depmatrix/HTML sync: new cites all to papers P7 already depends on; no new cross-paper edges.
