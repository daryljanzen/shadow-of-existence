# BUNDLE — `r2376+c54.151`

**Two parts, each under 30 MB, no file split across parts. Together they are the whole programme.**

| part | file | size | contains |
|---|---|---|---|
| 1 | `CR_bundle_r2376+c54.151_part1_corpus.tar.xz` | 24 MB | every top-level document, `corpus/`, `figures/`, `receipts/`, `verification/` |
| 2 | `CR_bundle_r2376+c54.151_part2_working.tar.xz` | 14 MB | `computations/`, `capstones/`, `scripts/`, `forks/`, `kills/`, `resources/`, `retired/`, and the build/work directories |

Both extract to a single top directory `cr_r2376/`; extract part 1 and part 2 into the same place and
the tree is complete. **Verified this revision by extracting both parts and running a recursive diff
against the working tree — zero differences.** Checksums in `BUNDLE_r2376+c54.151.sha256`.

*The split is now a script rather than a hand procedure: `scripts/cut_bundle_two_part.sh`, written
this revision because the hand version had once cut a file across parts.*

---

## What is new since `r2376+c54.150`

**`THE_ASSUMPTIONS_RETREATED_UPWARD.md`** — the capstone on the `c54.114`–`c54.151` arc. What stands,
what was withdrawn, and the pattern the withdrawals make. It sits at top level rather than in
`capstones/`, which is the six-document why-layer read last in the spin-up and is not the place for a
sector synthesis.

**And cutting it found the c54.149 withdrawal had never propagated.** P16 was still asserting the
whole full-lap Floquet account in its published prose, and `THE_WORK` carried three of four affected
closure rows as standing results. All struck; the paper now states the withdrawal with both its
grounds; the phrases are in `check_withdrawn`'s registry; the receipt is banner-marked and its
`INDEX` row struck. `THE_BASE_RATE`'s fourteenth entry records the mechanism.

**Ten gates pass. All 17 papers compile at 0 errors, 0 undefined citations, 0 undefined refs, 0 dead
receipt links.**
