# BUNDLE — r2376+c54.93
*The c54 fork of the Cosmological Relativity programme, cut after the fold and the discharge of all three standing leads.*

**HEAD:** `cd91723 r2376+c54.93 — L-135 struck and the landing debt paid: P3 gains sec:winding, where closure on both horn-headed arrangements FORCES the thirds with no Standard Model input and the Z_3 sheet index supplies the closure rule rather than receiving it; P14 gains the affine isospin exchange w -> 1/3 - w, the SM's own Q -> Y - Q with the constant unfitted; no uncited receipt from this fork remains`
**Cut:** 2026-08-09 · node c54

---

## ⌗ WHY THIS CUT, AND HOW IT DIFFERS FROM c54.85

The c54.85 bundle was cut when the fork's own lead register reached its floor. **This one is cut
after that register was widened to hold what the whole corpus owes, and after the three leads it
left standing were discharged.**

```
LEAD REGISTER: 155 registered · 128 struck · 27 open · 0 HOT
  struck fraction: 82.6%   (reported so it cannot quietly fall)
  HOT fraction of open: 0.0%  (ceiling 34%)
```

Nine gates, all green at the cut, and 17/17 papers compiling at 0 errors, 0 undefined citations,
0 undefined refs, 0 dead receipt links.

| gate | rc |
|---|---|
| `check_receipts` | 0 |
| `check_kills` | 0 |
| `check_burndown` | 0 |
| `check_currency` | 0 |
| `check_queues` | 0 |
| `check_grains` | 0 |
| `check_supersession` | 0 |
| `check_withdrawn` | 0 |
| `check_compile` | 0 |

---

## ⌗ WHAT THIS CUT CARRIES THAT c54.85 DID NOT

**THE LANDING AUDIT (c54.86–c54.89).** P7's frontier had shrunk in substance and not on the page:
seven items, now six, with item 7 resolved in its own title and still listed. Worse, the audit
found the c54.31 withdrawal — generations are not the hinges — still live in the places that
*summarise*. **P14's own abstract asserted the identification P14's own section is titled after
denying**, and had also predated its own central result: the entire colour arc appeared nowhere in
it. P7's frontier item 5 stated the revision and then restated the withdrawn reading three
sentences later. P7's synthesis named only the compact-face route to $\su(3)$. All corrected.

**THE NINTH GATE, `check_withdrawn.py`**, carrying a registry of withdrawn claims plus the markers
that count as an adjacent correction — a paper may *quote* what the corpus withdrew, it may not
*assert* it. Verified by running it against the pre-fix tree (rc=1, all four named). Pointed at the
standing registers it found the same claim in five more forward-facing documents; nine are exempt
**by name** as historical records.

**THE UNCITED-RECEIPT DEBT (c54.87), measured for the first time.** Twelve receipts registered in
this fork and cited by no paper — banked results that reached nothing. Nine landed immediately;
four batch receipts were marked as process records; the rest became `L-135`.

**THE FOLD (c54.90, Daryl-directed).** `OPEN_PROBLEMS_MAP`'s 27 live items entered the lead
register as **`L-136`–`L-162`**, each carrying its map code as an alias. One ID space, one
burndown, one supersession scan. **The map is now a *view* of the register rather than a second
source of truth**, keeping the work-clusters, the readiness/priority sort and the runway. Cluster J
is registered at germ weight and stays off-runway by construction. *The map needed exactly the
infrastructure this fork built, and had been rotting quietly without it.*

**THE THREE STANDING LEADS, DISCHARGED (c54.91–c54.93).**

- **`L-110`** — the sector's discharge condition, every clause disposed. Its last live clause is
  answered **no**: $T$ acts the same on both $R$-eigenspaces, so the construction supplies a species
  *label* and not $SU(2)_L$'s chiral gauging. The mismatch with the Standard Model is exactly one
  pair wide — the left doublet's $\{+1,-1\}$ matches, the right singlets' $\{+1,+1\}$ does not.
  Recorded as a mismatch rather than smoothed.
- **`L-91`** — the flavour literature's own criterion says a cyclic group cannot organise
  generations into a multiplet, so **the corpus's threeness is a *deck* symmetry and not a
  horizontal one**. The same one-dimensionality bounds both sectors: *the discrete opening supplies
  characters, and characters label rather than multiplet.* Landed in P14 §scope as the uniform
  ceiling.
- **`L-135`** — the winding/charge sector, built. **P3 gains `sec:winding`**, where closure on both
  horn-headed arrangements forces the thirds with no Standard Model input, and the $\mathbb{Z}_3$
  sheet index supplies the closure rule rather than receiving it. **P14 gains the affine isospin
  exchange** $w\mapsto\tfrac13-w$, the Standard Model's own $Q\mapsto Y-Q$ with the constant
  unfitted.

---

## ⌗ COUNTS AT THE CUT

| | |
|---|---|
| papers | 17 `.tex`, all compiling clean |
| registered receipts | 244 |
| lead register | 155 registered · 128 struck · 27 open · 0 HOT |
| uncited receipts from this fork | **0** |
| gates | 9 |
| fork commits | 57 |

---

## ⌗ THE TWO PARTS

Unpack both in the same parent directory; they reassemble into one `cr_r2376/`. `part1_corpus`
carries the paper sources, PDFs, generated appendices, figures and the nine gates; `part2_rest`
carries the registers, ledgers, the whole `receipts/` tree, `computations/`, and
`FORK_HISTORY_c54.txt` — the fork's commit log in plain text, since `.git` (424 MB) is not
included.

---

## ⌗ WHAT IS OWED NEXT

The supersession scan, re-run over the widened register, is pointing at the newly-folded items —
`L-143`, `L-160`, `L-136`, `L-161` against the newest receipt, plus `L-142` (the map's `C·7`,
the ellipse-foci bridge) and three cluster-J items against the turnaround figure. **Those are the
cheapest strikes on the board and are the next front.**
