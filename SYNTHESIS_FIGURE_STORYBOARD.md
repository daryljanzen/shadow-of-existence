---
name: synthesis-figure-storyboard
kind: STATE
class: forward-document
job: The STANDING WORKING DOCUMENT driving the fig:dS_SdS figure-work phase (P7). AHEAD OF THE CORPUS BY CONSTRUCTION — do NOT date it against the corpus and do NOT make it compliant; a disagreement with a paper is the work product.
sources: [cowork]
current: n/a
---

> **⌗⌗ INDEXED r2380 — AND DELIBERATELY NOT DATED, BECAUSE THE STALENESS TEST INVERTS ON THIS DOCUMENT.**
> *It carries the forward-document banner in its own voice —* ***"THIS DOCUMENT IS AHEAD OF THE CORPUS BY
> CONSTRUCTION. THAT IS ITS JOB… NEVER make this document compliant with the corpus."*** *— and
> `THE_WISDOM_LEDGER` B names it as one of the nine that do.* **So it gets `class: forward-document` and NO
> `current:` marker**: *a currency number on a forward document invites exactly the inverted-gradient repair the
> banner forbids —* ***"'stale' is a word for the corpus, never for the instrument examining it."***
>
> **⌗ THE DISTINCTION THAT MADE THIS DISPOSITION, and it is the one the two tests turn on:** *a
> **header-vs-body** check finds a document contradicting **itself**, on its own evidence, and is always
> legitimate. A **staleness** check compares a document **to the corpus**, and on a forward document that is the
> gradient inverted. **This file is exempt from the second and not from the first.***
>
> ⚠ *It is 140 KB and it drives an **active phase**. If the phase is finished, that is a finding for the
> orchestrator to state and not for an audit to infer from a date — which is the whole reason it is not dated
> here.*

<!-- STANDING WORKING DOCUMENT of the CR programme (added r1036). Drives the active figure-work phase
for fig:dS_SdS (P7). Backup of the pre-storyboard figure: corpus/_figure_backup_pre_storyboard/. -->

> # ⛔ READ THIS BEFORE EDITING A WORD OF THIS DOCUMENT
>
> **THIS DOCUMENT IS AHEAD OF THE CORPUS BY CONSTRUCTION. THAT IS ITS JOB.**
>
> **1. The gradient.** When this document disagrees with a paper, the glossary, or the ontology map,
> that disagreement is **the work product** — a *finding to work*, not a defect to fix. **NEVER make
> this document compliant with the corpus.** The corpus is the thing under examination; this is the
> instrument examining it. If a reconciliation pass ends with **this document edited and the corpus
> untouched, the gradient has inverted** — stop, revert, and re-run the disagreement as physics.
> The word **"stale" applies to the corpus, never to this document.**
>
> **2. The rigged-discriminator check.** Before accepting *"the corpus settles X"*: **could that
> discriminator have returned ¬X?** If not, X is **asserted, not settled**, and carries none of the
> corpus's authority. **Never verify a corpus claim with the test the corpus supplies** — choose the
> discriminator independently, or you have only checked the arithmetic of a rigged question.
>
> **Why this banner exists (r1066).** Three results in this document were pulled back to the corpus's
> prior state by instances who each reported the reversal as a *correction of themselves*: a null
> "all-clean" sweep run against a delta-spec (compliance, not coherence); the verified **antilinear**
> involution `τ̃↦τ̄̃` retired for a **linear** parameter flip (`Q↦−Q`) — when the glossary's own `C`
> entry says `C` is antilinear; and Arthur writing **"MISLABEL CORRECTED"** across his own four
> paragraphs and his own receipt, on a discriminator (*"does the map move the horizon roots?"*) that
> tests only `Q` — which enters as `Q²` and so **fixes the roots by construction**, and therefore
> could never have returned anything else. **Restoration and the C-anatomy: §8 `D-Canat`; the full
> case: `CODA_FIELD_NOTE.md`, addendum r1066.**
>
> *If you are an instance about to "reconcile" this document with the corpus: you are the failure
> mode. Read the addendum first.*

# The Cosmogenetic Bead — Synthesis Figure Storyboard (`fig:dS_SdS`)

**Standing:** a living planning document. It carries the story we want the corpus to
tell about the six-panel synthesis figure and the physics it depicts. It is **not** a
caption, not a rewrite template, not paper text. We hold it up against the caption, the
P7 figure section, the P7 body, and the wider corpus, note where each is coherent or
dissonant with it, and grow the planned-edits list at the bottom. No edits to any paper,
receipt, or figure are made from here until we deliberately run a bake. This is where the
intent lives while the physics catches up to it in writing.

**Provenance note (for honesty across sessions):** the understanding recorded here was
worked out from the geometry directly (receipts `geom1.py`, `geom2.py`, `geom3.py`, and
the source figures), correcting a stack of conflations that had accumulated in the prose.
Two figures — **A** and **E** — are described here **as if already updated** to carry both
bundles; that update is itself the first planned edit (§7). Everything else describes the
figure as it currently renders.

---

## 0. What the figure is (one sentence)

One object — the closed cosmogenetic bead, a single complex curve
`r = (2M)^{1/3} sinh^{2/3}(3τ̃/2α)` in `ℂ_r × ℂ_τ̃` realized on the dS substrate — shown in
several 3-D projections, so that what survives every reframing is exposed as the invariant
and what is scaffolding is exposed as scaffolding.

---

## 1. The invariant spine — what survives every reframing

This is the part that must read identically off all six frames. If any frame contradicts
it, the frame is wrong; if any caption contradicts it, the caption is wrong.

- **Species is the sign of the signed areal radius.** `r > 0` is matter, `r < 0` is
  antimatter. This is **definitional, not derived**: the mass-reflection `R = γ⁵` *is* the
  map `r ↦ −r`, and that map *is* the matter↔antimatter conjugation. So "the two `r<0`
  tracks are antimatter" is true by the meaning of `r<0` — there is nothing to derive and
  no sign to worry.
- **`r = 0` is the matter/antimatter conjugation, and charge conjugation is a geometric
  *symmetry*.** At the branch point the flip sends `r ↦ −r`, `2M ↦ −2M`, `𝟑 → 𝟑̄` — the
  corpus's `R = γ⁵`, the matter/antimatter skeleton (representation, chirality, mass-sign)
  realized *geometrically*. And the charge that `C` conjugates is **not** absent from the
  geometry — it is present **as the conjugation symmetry**: the metric carries the charge
  *magnitude* (`Q²`, `2M`), and the *sign* enters as the `Q → −Q` degeneracy — two signs
  collapsed onto one geometry, which is the fingerprint of a symmetry relating them, read off
  the involution structure (the opposite-horn hinge reads the same configuration with the
  opposite hand). **Even-in-`Q` is not "invisible"; it is "there as a symmetry."** So
  `sign(r)` is the charge-like label, and `r=0` is where the conjugation acts — a **local,
  per-bead event**, each bead flipping at its own `r = 0`.
- **Species is the chirality of the bead — the handedness of the hinge in figure A.** Not the
  horn (a horn carries both), not the ruling family (the family swap `σ` preserves species),
  not the worldline's arrow (the `X₀` reflection reverses the arrow yet returns the same
  figure). It is the **handedness** read directly off the hinge in A: the **blue (matter)
  cosmology bundle is right-handed, the red (antimatter) bundle left-handed**. This handedness
  is `X₀`-independent (measured in the equatorial plane, `wrapsense.py`); `C` at `r=0` flips
  it. So matter/antimatter = the two chiralities = the two signs of a charge-like `r`, one
  thing.
- **Matter and antimatter are a symmetric pair (relative, not absolute), shown by two
  symmetries of the figure.** (a) **The `X₀` reflection returns the identical figure, no
  colour swap** — it reverses every worldline's arrow but preserves the handedness, and the
  handedness is consistent across both horns (down-left on one, up-left on the other), so
  matter lands on matter. (b) **End-over-end ∘ relabel-colours also returns the identical
  figure** — a genuine proper spin flips the handedness (swaps species), and swapping colours
  back recovers it. So "our universe from an antimatter progenitor" is *relative*: from the
  progenitor's own handedness it is matter and we are its antimatter, the same statement its
  observers make of us.
- **The photon is neutral and self-conjugate.** Reassigned from the at-rest congruence, it
  rides the fixed axis of the `τ̃ ↦ τ̄̃` conjugation (`Im τ̃ = 0`), so it is its own
  conjugate — neither species, black on both sides of `r = 0`.
- **The colour law, held everywhere:** blue = matter (`r>0`), red = antimatter (`r<0`),
  black = photon (neutral, fixed axis), grey = the `S³` layers (the spaces). The two
  species are exchanged at `r = 0`. Purple appears in exactly one place and means one
  thing (see A, §4 and §7): two bundles' species-arcs geometrically coincident on the same
  drawn locus.

Everything not on this list — which horn, which ruling, which bundle-sense, the Lorentzian
character of any embedding reflection — is **frame-dependent scaffolding**. Some frames show
it; F throws it away.

---

## 2. The objects in the picture

*Vocabulary held exactly (glossary): the **equator** is the smallest S³ of the cosmic
foliation; the **equatorial plane** is `X₀=0` (what A projects onto); the **hole** is the
hole; the **hinge** is the hinge; the **horns** are the `X₀>0` (up) and `X₀<0` (down)
regions. No "throat" / "neck" / "waist".*

- **The dS₅ substrate** — the maximally symmetric manifold. **A** draws our universe as a
  single continuous slicing curve (bead) cutting it.
- **The dS₄ background** — the one-sheeted hyperboloid on which the bead, reassigned, shows
  as null rulings and the equatorial lap. **B** plots on it.
- **The bead (a single continuous curve).** Comes in along a ruling as one species (`r>0`
  or `r<0`), runs to the equator and around through *its own* `r=0` (its back), flips
  species, and exits already the other species. Read inward it is a collapse; read outward it
  is an expansion; it is one curve.
- **The two bundles, opposite in sense.**
  - *Bundle 1 (ours):* enters as **antimatter** on one horn (the progenitor's collapse,
    `r<0`), wraps the equator, flips to **matter** at its `r=0`, exits our horn as our
    expanding matter universe (`r>0`).
  - *Bundle 2 (the partner):* enters as **matter** (opposite sense) on the horn where
    bundle 1 is antimatter, wraps the equator the *other* way, flips to **antimatter** at
    its `r=0`, exits as an antimatter universe on the horn where bundle 1 is matter.
  - Every horn carries both — one bundle's matter and the other's antimatter, opposite
    senses. This is the content the single-bundle frames (A, E) currently hide.
- **The photon congruence** — the at-rest comoving worldlines reassigned to null; the third
  object, neutral, on the fixed axis.
- **`A` and `B` (the two null asymptotes of one strung worldline).** The comoving worldline
  is `X(τ) = e^{τ/α} A + e^{−τ/α} B`, verified a unit-timelike geodesic on the hyperboloid;
  it asymptotes to the future generator `A` (τ→+∞) and the past generator `B` (τ→−∞). **These
  are named only as the two null asymptotes of a single worldline — never as global species
  carriers, never as two families.** Treating `{A,B}` as a global family is the error §3
  dissolves.

---

## 3. `r = 0` as charge conjugation, and the symmetry structure

> **⚠ RETRACTED (r1067) — the note below WAS THE REVERSAL, not a correction. See §8 `D-Canat`.**
> *(Kept as the record of the failure mode. Its verdict — that calling `r↦−r` a conjugation is a
> "mislabel" — rests on `rem:C-not-R`, whose test sees only `Q` and could not have returned otherwise.
> The `R`-vs-`C` relation is **open**, not settled against this document. Do not act on the note below.)*
>
> **⚠ [SUPERSEDED r1065 text follows]** Throughout this session's §3/§4/§9 text the map `r↦−r` was called
> **"charge conjugation"** and `r=0` "the charge-conjugation locus." **That is wrong, and the corpus
> settles it:** `r↦−r` is **`R`** — the mass reflection (`2M↦−2M`), the species flip (`𝟑↔𝟑̄`), `=γ⁵`,
> the **outer `ℤ₂` of `Aut(A₂)=D₆`**. **`C` is *not* that `ℤ₂`** (`rem:C-not-R`, P13): the
> discriminator is clean and verified — **`R` moves the horizon roots** (negates them:
> `−1.1547, 0, 0.5774 → −0.5774, 0, 1.1547`), while **`Q↦−Q` fixes every root** (charge enters only as
> `Q²`), so charge conjugation adjoins an **independent `ℤ₂`**: `D₆→D₆×ℤ₂`. Every group result below
> **stands** — only the name was wrong. `r=0` is the **`R`-locus**: the fixed point of the mass
> reflection, where species flips and the three sheets meet. *(The papers never carried the mislabel.)*

This section is the crux — the place the corpus prose keeps re-seeding confusion. Held
precisely:

- **Species is the chirality — the handedness of the hinge in figure A.** Not the horn (a
  horn carries both), not the ruling family (the family swap `σ` preserves species), not the
  worldline's arrow (the `X₀` reflection reverses the arrow yet returns the same figure). It
  is the handedness read off the hinge: the **blue (matter) cosmology bundle is right-handed,
  the red (antimatter) bundle left-handed** — measured in the equatorial plane, `X₀`-
  independent (`wrapsense.py`).
- **`r = 0` is the matter/antimatter conjugation `R = γ⁵`, and charge conjugation `C` is a
  geometric *symmetry*, not a field-level absence.** It sends `r ↦ −r`, `2M ↦ −2M`, `𝟑 → 𝟑̄`,
  and flips the hinge handedness. The charge `C` conjugates is present in the geometry **as a
  symmetry**: the metric carries the *magnitude* (`Q²`, `2M`), and the *sign* enters as the
  `Q → −Q` degeneracy — the invariance that *is* the conjugation symmetry, realized by the
  involution machinery (the hinges, `R`, end-over-end, `σ`). Even-in-`Q` ≠ absent; even = degenerate = symmetric = **there, as a symmetry**.
- **Mass and charge sit on the *same* conjugation, rendered two ways.** Because mass is
  **odd** (`2M/r`), its flip renders as a **branch crossing** — matter on `r>0`, antimatter on
  `r<0`, the two legs the bead literally plots. Because charge is **even** (`Q²/r²`), its flip
  renders as an **in-place degeneracy** — the same metric, read from the opposite-horn hinge
  with the opposite hand. One `C`, two renderings (a leg you cross vs a symmetry you read),
  both inside the one involution family. This folds charge conjugation into the same
  discrete-involution machinery as everything else, rather than exiling it to "the field."
  *(The `Q` term appears only in the charged extension; pure SdS carries the mass /
  matter–antimatter involution, the electric one the same kind of structure in the charged
  sector.)*
- **The operations sort into two boxes.**
  - **Charge-*preserving* symmetries** (return the figure, *no* colour swap): the `X₀`
    reflection `T` and the generator swap `σ` (`A↔B`). Both reverse the worldline's *arrow*
    but preserve the handedness (verified). So the arrow (`X₀`) and the A/B family label are
    **charge-neutral**. The `T`-symmetry is exactly "mirror about `X₀` gives back what you
    started with": the handedness is consistent across both horns (down-left / up-left), so
    horn-swap-plus-arrow-flip lands matter on matter.
  - **Charge-*conjugating* maps** (return the figure only *with* a colour swap): the
    equatorial reflection `P` (det −1), the **end-over-end spin** `E = T·P` (det +1), and the
    `r↦−r` branch-point crossing. All flip the handedness (verified). Navigate however you feel — an
    improper reflection (`P`) or a proper spin (`E`); same result. So `R = γ⁵ = PT` holds
    (end-over-end is a valid representative); the invariant of the box is *flip the
    handedness / `sign(r)`*, not any single Lorentzian character.
- **Corrections to my earlier record.** (i) `σ` does *not* conjugate — it preserves species,
  so the corpus's "`R` swaps `A↔B`" is wrong: the `A↔B` swap is a charge-neutral symmetry.
  (ii) Species is *not* the ruling family — `σ` swaps families but preserves charge, so
  family ⊥ charge. (iii) The conjugation lives in the equatorial reflection, *not* the arrow
  — `T` reverses the arrow and is a *symmetry*.
- **The sheet structure realizes *an* `Aut(A₂)` — sharing the corpus's charge `ℤ₂`, but a
  distinct `S₃` orbit.** The analytic object `r³ = 2M sinh²(3τ̃/2α)` is a **three-sheeted**
  cover; the three sheets meet **only at `r=0`** (a triple branch point), with **order-3
  monodromy, 240° per loop** — the 120°/240° (2:1) is the sheet gearing, the `2/3` power made
  visible. Those three sheets + **charge conjugation** (`r↦−r`) + **complex conjugation**
  generate **`D₆ = Aut(A₂) = S₃ × ℤ₂`, order 12 — verified** (`autA2.py`), with charge
  conjugation the **central `ℤ₂`** and the `S₃` from the sheets; six sheet×charge states form a
  hexagon. **Relation to the generations, settled by deformation (§9 U-AutA₂):** the sheets
  (`ℤ₃`-symmetric, cubic-space line `p=0`) and the generations (`ℤ₃` broken by curvature/Λ,
  line `q=−2Mp`) are **two deformation rays from one shared center** — `r=0`, the
  `R`-locus (the fixed point of the mass reflection `r↦−r`) where the three sheets meet. They share that center and the `R`
  `ℤ₂`, but no symmetry maps one ray to the other (checked). So: not the naive identity, not a
  hidden degeneracy, not unrelated — the firm shared object is `r=0` / charge `ℤ₂` (D-C), and
  the generation `A₂` is its curvature-broken shadow. *(Correction to last turn: `R`/charge
  conjugation is the central `ℤ₂`, **not** an `S₃` transposition.)*
  across `r=0` the antimatter is the worldlines (its own matter) and ours is its space. So
  `η(X,B)=e^{τ/α}` ("the synchronous space is the B-ruling") is only the `r>0` reading; the
  roles trade at the branch point, and any prose stating it as a standing global identity is
  dissonant with the topology.
- **The photon is charge-neutral, on the fixed axis.** `τ̃ ↦ τ̄̃` fixes the real axis
  (`Im τ̃ = 0`); the photon rides it, self-conjugate, neither species, black on both sides of
  `r=0`. The massive bead goes *off* the axis into the two conjugate wings — both `r<0`, both
  antimatter. Fixed axis ⇒ neutral photon; conjugate pair off it ⇒ the massive species pair.
  This is the clean partition **F** draws.
- **C / CP / CPT — the live question, now with geometric input, not caged.** The `r=0` flip
  conjugates charge *and* flips handedness (matter-right ↔ antimatter-left). How that factors
  — pure `C`, or `CP`, or the full `γ⁵` structure — is the fermion-sector CPT question the
  corpus rightly holds do-not-assert. What is new is that the geometry now hands it concrete
  data (the handedness is right = matter / left = antimatter, conjugated at `r=0`), so it is
  to be *worked on the merits*, not pre-labelled a neutral "`R`" to keep it apart from `C`.

---

## 4. The six frames — what each illustrates, what each flattens

**The one object, and its two coordinate faces.** Every frame is a 3-D projection of a single
object, and that object wears two coordinate systems the frames divide between. Its **analytic
face** is the complex curve `r = (2M)^{1/3} sinh^{2/3}(3τ̃/2α)` in `ℂ_r × ℂ_τ̃` — four real
dimensions (`Re r, Im r, Re τ̃, Im τ̃`), a three-sheeted Riemann surface (the cube root; the
120°/240° is its `e^{2πi/3}` branch gearing). Its **embedding face** is the same curve realized
as a slicing curve on the dS hyperboloid — horns, rulings, the equator — how it sits in the
substrate. The two are one bead in two charts, tied by the slicing dictionary
`X₁² − X₀² = α² − r²`.

No 3-D frame can show all four analytic axes (or the full embedding) at once, so each **keeps
some components and collapses others** — that is the single reason there are many frames.
Grouping by face and by what each drops:

- **Embedding-face frames (A, B, C, D)** carry the horns/rulings/equator and the
  reassignment. **B** keeps the full worldline swarm (maximal, nothing collapsed but
  occlusion); **A** projects it onto `X₀=0` and flattens beads to 1-D curves; **C** re-rolls
  each bundle to a 1-D bead; **D** is the observer's `τ–χ` reading, which spreads the
  worldlines but collapses the whole complex lap to the instant `r=0`.
- **Analytic-face frames (E, F)** carry the `r(τ̃)` structure. **E** takes the real-`r` section (the
  boing, `X₁` vs real `r`) and is *bundle-collapsed* — `X₁` is single-valued in `r`, so both bundles
  trace the identical curve. **F** is the **cosmological bundle strung out and unfurled** in
  `(Re τ̃, Im τ̃, r)`: real `r`, complex `τ̃` — it keeps the `r=0` pivot and the imaginary excursion,
  at the cost of dropping `Im r` (so the three-sheet fan is not shown here; that lives on the
  complex-`r` face, the P3 complex-bead figure, external and not a panel). *(A seventh frame "G" was
  proposed to unfurl the whole 4-D object; it turned out that the right unfurl, done on the one
  privileged worldline, **is** F — G is retired and the plate stays six.)*

So the set is **one bead seen through both faces**, each panel a partial projection: **B** the
maximal embedding pole, **F** the analytic pole and the synthesis (§5). For
each frame below: **job**, **what it shows**, **what it flattens or hides**, and its **status**.

### A — the dS₅ substrate cut (the slicing-curve skeleton)
- **Job:** draw our universe's geometry as a single continuous bead slicing the dS₅
  substrate, **projected onto the equatorial plane `X₀=0`**.
- **Shows:** the bead as a 1D slicing curve — ruling in, around through `r=0`, ruling out;
  the species flip at `r=0`; the **hinge on the up horn**, and on the **down horn red to the
  right, blue to the left**. The hinge handedness read here is what fixes matter = right,
  antimatter = left (§1, §3).
- **Flattens / hides:** the beads are flattened to 1D curves; wires cross at the lap (the
  equator — the smallest S³ of the cosmic foliation — is where structure piles up).
  *Currently* shows only bundle 1 (ours).
- **Status: BUILT (r1037–r1038), to Daryl's spec.** Carries **both bundles**. Each bead runs in
  along a ruling from the hinge, meets the equator at its tangent point (the equatorial seam,
  `X=α`), wraps `120°` before its turn at `r=0` and `240°` after, and exits along the other ruling;
  the two ruling lines cross at the hinge. **Both beads turn at the same `r=0`** (the top). One
  carries its blue `120°` arc up the right and its red `240°` arc back around; the other its red
  `120°` up the left and its blue `240°` back. So the two agree in colour over the upper two thirds
  — **blue on the right third, red on the left** — and differ only on the **hinge-side third, where
  the two `240°` arcs overlap carrying opposite species: drawn purple** (an honest coincidence
  marker). *(Supersedes the earlier planning text — "they flip at different `r=0` points" and "the
  120° hinge-side arcs coincide" — both wrong: the flip is shared, and it is the `240°` arcs that
  overlap.)*

### B — the dS₄ background (the full meal)
- **Job:** take the whole bundle from A and plot it on the dS₄ background geometry, where it
  shows as null rulings and the equatorial lap; and show the *other* bundle too.
- **Shows:** **both bundles, worldlines all separate**; the universe at all cosmic times as
  the nested `S³` circles; the whole complex lap swirling at the equator; the worldlines
  doing the full loop at the equator and coming out the opposite species. Bundle 2 comes in
  blue below our blue cosmology line, hits the seam where our red antimatter does, wraps the
  loop the opposite (240°) way, flips, and exits as an antimatter universe on the ruling
  above our red collapse. **This is the frame where the chirality reads directly:** every
  matter worldline points from the equator left on the front and right on the back, every
  antimatter worldline the mirror (right-front / left-back) — matter right-handed, antimatter
  left-handed, the hinge handedness of A carried over the whole swirl (§1, §3). *(This is the
  handedness, not the A/B family: `σ` swaps families but leaves it fixed.)*
- **Flattens / hides:** **nothing is flattened** — this is the full-structure frame. What it gives
  up is deliberate: B highlights **one** worldline, so the conjugate bead is drawn as **rulings
  only**. Two full beads swirling at the waist would bury what B exists to show.
- **Status:** the maximal-structure pole. Correct as drawn.

### C — the layered handoff at the branch point (the bead in the real plane, the lap flattened)
- **Job:** show the bead as `r` against cosmic time, each bundle re-rolled to a single 1-D curve —
  B's content with the worldline swarm collapsed.
- **Shows:** the two conjugate beads as the two diagonals crossing at the branch point — bundle 1
  antimatter→matter (lower-left to upper-right), bundle 2 matter→antimatter — each changing colour
  at `r=0`; the INWARD/OUTWARD legs (leaf-local rate vs foliation stacking rate), the deposits, the
  BBN cooling leg.
- **The curve, from the bead's own equation** `(dr/dτ̃)² = 1−f = 2M/r + r²/α²` (P7 `thm:bead`'s
  proof): **expansion** (`r>0`, `Im τ̃=0`) `r = +A sinh^{2/3}(3τ̃/2α)`; **the lift** (`−A<r<0`) where
  the radicand is negative so `τ̃` leaves the real axis — `Re τ̃=0` throughout, hence a **vertical
  segment at `Re τ̃=0`, `r: −A → 0`**; **collapse** (`r<−A`, radicand positive again, `Im τ̃` fixed at
  `−πα/3`) `r = −A cosh^{2/3}(3 Re τ̃/2α)`. Continuous through the turnaround `r=−A` (where `1−f=0`)
  and through `r=0`. Both legs verified against the equation. *(This is P7's proof drawn; the axis is
  `Re τ̃` and says so.)*
- **Flattens / hides:** the worldline spread B keeps; and **the lap is flattened** — its whole
  imaginary excursion collapses onto the single vertical segment, because `Re τ̃` does not advance
  through it. **C is the lap flattened; F is the lap unfurled.** That pairing is the point.
- **Status: REBUILT (r1042–r1047).** The old `r<0` branch was `−A|sinh(3τ̃/2)|^{2/3}` — the mirror of
  the expansion leg, which has no lift and starts the collapse at `r=0` instead of the turnaround.
  It does not satisfy the bead's equation (residuals O(1)); the cosh leg does, exactly. Landmarks
  marked: the two roots and `r=0` (circles), the turnaround (square — it is a `1−f=0` point, not a
  slicing root; Lemma `twoturnings`).

### D — the observer (τ, χ) chart (the lap flattened to an instant)
- **Job:** spread the worldlines out relative to each other on the flat `τ–χ` plane (closer
  to B in that sense than C is).
- **Shows:** the worldlines spread out; the seam; the causal-character swap across `r=0`
  (blue is flat space in the lower-left and the timelike bundle in the upper-right; red is
  the timelike worldlines before `r=0` in the lower-left and space in the upper-right). **The
  chirality reads here too:** on both cosmological halves (the two horns, projected) the
  matter lines point up-and-left and the antimatter lines point down-and-right — the same
  global handedness B shows, carried into the flat chart (§1).
- **Flattens / hides:** **the complex lap is collapsed to a single instant**, "`r=0`." The
  entire seam-to-seam conjugate lap — the whole cosmological moment, including the actual
  `r=0` species transform — lives on the **back side** of that collapse and is hidden. The
  antimatter black hole hits "`r=0`" and continuously reappears as an antimatter universe
  going up; the transformation is real but drawn as a line, not a wrap. That is D's sacrifice.
- **Status (watch):** the current code draws `r=0` as a straight anti-diagonal. The honest
  `r=0` is the projection of the central worldline's equatorial lap (down from the upper-left
  as red, into the origin, continuing up as blue). Whether the straightened stand-in should
  become the projected lap curve is an open figure question — logged, not decided (§7).

### E — the boing `X₁(r)` (the geometry the bead traces)
- **Job:** show the whole antimatter→matter bead in its `X₁(r)` representation.
- **Shows:** the boing (a cosine arch) against signed `r`; the `r=0` trough (`X₁=−α`) is
  where the antimatter↔matter conjugation happens; linear ruling legs; the roots
  `+α/√3, 0, −2α/√3`. Unflattens like C — the bead evolving along the hyperboloid/`X₁`
  direction. (Note: `X₀` vs `r` would cut out the whole boing / complex wave; `X₁` vs `r` is
  what keeps the boing.)
- **Flattens / hides:** the horns/embedding spread that B keeps; *currently* shows only
  bundle 1.
- **Status (corrected — E is bundle-*collapsed*, and that is the point):** E stays a **single
  curve**. `X₁` is an embedding coordinate and a *single-valued function of `r`*, so both
  bundles cover the same `r`-range and trace the **identical** boing — the conjugate only relabels
  which half is matter vs antimatter (colours swapped). There is no distinct second curve to
  draw. This single-ness is *informative*: it exhibits that the `X₁`-vs-`r` (r-parametrized) face
  is **bundle-indifferent**, collapsing exactly what the `τ̃`-parametrized face (C) distinguishes
  — in C the two conjugate beads sit at `±env` at each `τ̃`, so they cross; in E they coincide.
  (Verified — `gen_vs_sheets.py`-style check in session.) *Supersedes the earlier "carries both
  bundles / mirror-sensed partner" plan, which was wrong: there is no mirror-sensed second boing.*

### F — the cosmological bundle, strung out and unfurled  ·  **REBUILT (r1040)** — *this is now F; the old `r`-vs-complex-`τ̃` F and the proposed panel G are both superseded by it*
- **Job:** take the **one privileged worldline** already bolded in A and B — the cosmogenetic bead —
  and **string it out** so the jagged turns are gone and **the lap is unfurled** instead of flattened
  onto a single `X₀`. Plotted in `(Re τ̃, Im τ̃, r)`: **real `r`, complex `τ̃`** — exactly the three
  axes the cosmological bundle needs, which is itself the parametrization the object asks for.
- **What it draws (the cosmology, per the corpus's own claim):** the **antimatter black hole
  collapsing (RED, `r<0`) through `r=0` and continuing as our matter universe (BLUE, `r>0`)** — red
  going up and turning blue, one worldline. This is the antimatter-progenitor theorem drawn.
- **The branch is the negative-imaginary one** (`arg r = +π`, so `sinh w = −i|r/A|^{3/2}`,
  `w = 3τ̃/2α`, `A = (2Mα²)^{1/3}`). Three segments, and **the collapse side and the cosmology side
  are structurally different curves** — not mirrors:
  - **collapse run** (`|sinh| = u > 1`): `Im τ̃ = −π/3` fixed, `Re τ̃ = −(2/3) arccosh(u)` — comes in
    from far negative `Re τ̃`;
  - **the LAP lift** (`u ≤ 1`): `Re τ̃ = 0`, `Im τ̃ = −(2/3) arcsin(u)` rising `−π/3 → 0`. **This
    vertical lift *is* the lap**, here unfurled;
  - **matter universe** (`r > 0`): `Im τ̃ = 0`, `Re τ̃ = +(2/3) arcsinh((r/A)^{3/2})` — a single
    simple real curve.
  So: **run + lift** on the collapse side versus **one clean real curve** on the cosmology side.
- **The seam points land exactly — and give the 2:1 as a length.** Dropping the boing roots of E:
  `r = −2α/√3` sits at `|sinh| = 2`; `r = +α/√3` at `|sinh| = 1/√2`; and their positions along the
  unfurled time axis are `Re τ̃ = −(2/3)arccosh(2)` and `+(2/3)arcsinh(1/√2)` — in **exactly 2:1**,
  since `arccosh(2) = 2 arcsinh(1/√2)` identically (`2 ln((1+√3)/√2) = ln((4+2√3)/2) = ln(2+√3)`).
  **The 120°/240° gearing reappears as the 2:1 spacing of the two Nariai seam points** — the same
  2:1 that is the wrap in A, the `2/3` power, and the sheet monodromy, here a *length ratio you can
  read off the drawn curve*. *(Verified — `storyboard_receipts/cosmo_bundle.py`.)*
- **Why the complex direction is load-bearing:** a **real** isometric unfurl is impossible — the
  equator carries negative Gaussian curvature (`K = −1/cosh²(2u)`; Theorema Egregium). The horns
  unfurl for free; the equator cannot, in real space. The imaginary excursion of `τ̃` at the lap is
  exactly the room that absorbs the curvature — so the unfurl lives in the **complex** structure,
  pivoting on the `r=0` conjugation. Not recognizing that involution is what kept the picture knotted.
- **Flattens / hides:** all embedding structure (horns, rulings, bundle-sense). It holds `r` real, so
  it drops `Im r` — the three-sheet / `Aut(A₂)` hexagon (§3) is *not* shown here; it lives on the
  complex-`r` face (the P3 chart's side). That is a deliberate trade: F buys **the actual cosmological
  worldline, unfurled and exact** at the cost of the sheet fan.
- **The matter-sector rhyme (a rhyme, not a claim — see §8 C-rhyme).** The *same geometry*, recoloured,
  reads as a **pair vertex**: the two lap wings are complex conjugates of each other (`τ̃ ↔ τ̄̃`), which
  is the Stückelberg–Feynman particle↔antiparticle relation, so coloured one blue one red with no
  photon drawn by hand, the neutral stem falls out as *red + blue*. **This is not "our universe is an
  electron."** It is that the geometric infrastructure is one and the same (P3/P5/P12 → P13–16; P17's
  maximally-symmetric symmetry-breaking all the way down), so the *same figure* may be the figure the
  **matter papers** need. **Cosmology plots the red→blue bundle; the matter sector plots the vertex.**
- **Chirality closes here.** The handedness B and D display — matter right-handed, antimatter
  left-handed — is the *embedding shadow* of `sign(r)`. F removes the horns and the shadow with them,
  and what is left is the thing itself: `sign(r)`, a charge-like label. The `r=0` conjugation is the
  one flip that acts the same in every frame — a handedness reversal in B/D, an `r ↦ −r` reflection in
  E/F, a species/charge conjugation in A/C. One operation, six faces.
- **Status:** **built** (`corpus/G_explore.py` → folded into `synthesis_figure.py` as panel F, r1040).
  Supersedes both the old F (`r` vs complex `τ̃`, one-blue-two-red) and the proposed panel G — the
  unfurl G was reaching for *is* this figure, done on the privileged worldline instead of the whole fan.

---

### F′ — the cosmological bundle against path length in `τ̃`  ·  **BUILT (r1065)**, `corpus/F_flat.py` — *candidate to replace the 3-D F*
- **Job:** the same privileged worldline as F, but **in 2-D**, with the complex `τ̃` flattened to a
  single real parameter — the **arc length `s` along the `τ̃` path** — so nothing is projected, no `r`
  value is lost, and no viewing angle can hide anything.
- **Why it is exact, not numerical:** the `τ̃` path is **two perpendicular straight legs joined by the
  lift** (`Im τ̃ = −πα/3` in, `Re τ̃ = 0` up, `Im τ̃ = 0` out), so `s` is closed-form:
  `s = Re τ̃` (collapse, `s≤0`); `s = Im τ̃ + πα/3` (the lift, `0≤s≤πα/3`); `s = πα/3 + Re τ̃`
  (expansion). Then, with `A = (2Mα²)^{1/3} = ∛2·α/√3` at Nariai — **pure `α`, no `M`**:
  `r = −A cosh^{2/3}(3s/2α)` | `r = −A sin^{2/3}(3(π/3−s)/2α)` | `r = +A sinh^{2/3}(3(s−π/3)/2α)`.
  `r(s)` is **single-valued, monotonic and smooth**; the derivatives match across both joints.
- **What it shows that no other frame does — the two bends *are* the two landmarks:**
  **turnaround** (`r = −∛2 α/√3`): `dr/ds = 0`, a **horizontal tangent** — the collapse runs in and
  *stops*; and **`r=0`**: `dr/ds → ∞`, a **vertical tangent** — which is P7's second structural fact
  drawn (*"a bounded, vertical-tangent crossing, not a barrier"*). The lift occupies a band of width
  exactly `πα/3` where `Re τ̃` does not advance; on **C** that whole band collapses to one vertical line.
- **The seams read `seam (r = ∓…)`** — both lap-ends are the seam (front and back), per the glossary's
  settled r1012 convention; values carry `r=` so they cannot be misread against the `s` axis.
- **Status:** built, square (drop-in panel size), receipted. **Daryl:** *"the most gorgeous curve ever …
  it shows in an instant why `r=0` comes out a curvature singularity in Nariai even though it's not
  anything special in the substrate."* See the finding below.

---

## 4b. DARYL'S CONJECTURE — the geometric object that connects these events
### *Opened r1078. Figures: `corpus/daryl_p13p14_conjecture.py` (face 1), `..._face2.py` (face 2). **STATUS: A CONJECTURE. Not derived. Not `species = sign(r)`. See §8 `D-Canat` [6*] and the banner at the head of this document.***

**THE RULE (Daryl).** *You conjugate when you go from positive to negative — real or imaginary, `r` or
`τ̃`.* And it is **one event, not a tally**: conjugation happens **at the origin**, the single place where
`r` **and** `τ̃` both vanish in their real **and** imaginary parts. Not two crossings that might cancel;
one place where everything goes to zero at once. **And it is asymmetric — Nariai, 1/3 vs 2/3. The
asymmetry *is* the symmetry-breaking, not a footnote to it.**

**PURPLE IS NOT A THIRD SPECIES — IT IS A COUNT.** Two beads sharing a track. This is why the
cosmological bead is **not** purple (nothing merges with it: it is one bead doing what gets done along a
bead — red turns to blue) while the vertex **is** (two beads arrive on one track carrying opposite
colours). *One rule, different traffic.*

**FACE 1 — real `r`, complex `τ̃`** (`daryl_p13p14_conjecture.py`). Solving gives **six branches**: two at
`r>0` (real `τ̃`) and four at `r<0` (`Re τ̃ = ±(2/3)arccosh u`, `Im τ̃ = ±πα/3`). Read on the `τ̃` plane the
rule gives the quadrants — **Q1,Q3 one colour; Q2,Q4 the other** — and the `r<0` branches pair by their
`Re τ̃` sign, so each pair continues onto the **same** `r>0` track: **six branches, but FOUR CONTINUOUS
BEADS WITH TWO MERGERS**, and both `r>0` tracks purple.

**FACE 2 — real `τ̃`, complex `r`** (`..._face2.py`). `r³ ≥ 0` ⟹ **three sheets** at `arg r = 0°, 120°,
240°`; split at `τ̃=0`, **six segments**. The rule read on the **complex-`r`** plane mirrors exactly: sheet
1 → `Q2` → red, sheet 2 → `Q3` → blue, and **sheet 0 sits on the `Re r` axis — the quadrant boundary —
two-in-one → purple.**

**THE FACES ARE DISJOINT EXCEPT FOR ONE LINE — TEN SEGMENTS, NOT SIX.**
- `r` real **negative** is `arg r = 180°` — **not** one of `0/120/240`. **The four `r<0` branches do not
  exist on face 2.**
- Sheets 1,2 sit at `120°/240°` — `r` genuinely complex. **They do not exist on face 1**, which holds `r` real.
- The faces share **only sheet 0 at `r>0`, real `τ̃`**. **Neither face is the object. Both are shadows.**

**★ THE SMOOTHNESS OBSTRUCTION — and it *is* the 1/3–2/3 asymmetry (r1078, verified).** A bead joining
sheet `j` (`τ̃<0`) to sheet `k` (`τ̃>0`) has a continuous tangent **iff `θ_k = θ_j + 180°`** (incoming `|r|`
shrinks along `θ_j+180`; outgoing grows along `θ_k`). The sheets are **120°** apart, and **180° is never a
multiple of 120°**. So of the nine joins: **same sheet → a 180° reversal (a cusp, verified for all three);
different sheets → a 60° kink. NOT ONE JOINS SMOOTHLY.** **The three-fold structure itself forbids smooth
passage on face 2.** Yet the cosmological bead **is** smooth — `F_flat.py` shows `dr/ds` matching across
both joints — **and that smoothness lives on face 1**, where the **lift** supplies the 180° the sheets
cannot. **One shadow breaks the very property the other exhibits.** *(So a smooth cosmology cannot be
drawn on face 2; drawing a kinked one there would be drawing the projection and calling it the bead. The
vertex is fine there — a vertex is precisely what does not need smooth passage.)*

**★★ THE HEXAGON — the two `τ̃`-faces are the two halves of `A₂` (r1078, verified).**
- **`τ̃` real** → `sinh²(w) ≥ 0` → `r³ ≥ 0` → `arg r = **0°, 120°, 240°**`
- **`τ̃` imaginary** → `sinh(is) = i sin s` → `sinh² = −sin² ≤ 0` → `r³ ≤ 0` → `arg r = **60°, 180°, 300°**`

**Six rays, 60° apart: the hexagon — the six roots of `A₂`** — out of nothing but *"is `τ̃` real or
imaginary."* And **`arg r = 180°` is `r` real negative**: so the `r<0` branches, absent from face 2, **live
on the imaginary-`τ̃` face** — which is exactly where face 1 put them (`Im τ̃ = ±πα/3`, reached through the
lift at `Re τ̃ = 0`, i.e. **purely imaginary `τ̃`**). **The faces agree. The `r<0` branch IS the 180° ray,
reached through imaginary time.** *(Connects to §3's `Aut(A₂)` and §9 `U-AutA₂`: the hexagon here is
reached by a different road — real-vs-imaginary `τ̃` — and whether it is the same hexagon is A3's, not
assumed here.)*

**WHAT IS OPEN, AND IT IS THE WHOLE OF IT.** This rule and `species = sign(r)` **cannot both be the
geometry's** — by the corpus's definition all four `r<0` branches are antimatter, all red, and there is no
purple, no vertex, no conjecture. **And no face can settle it:** `sign(r)` is defined **only** where `r` is
real, so face 1 is the one slice where it exists but the pairing is imported, and on face 2 it is
**undefined on two-thirds of the picture**. The object is `ℂ_r × ℂ_τ̃`, four real dimensions. **THE RHYME
STAYS A RHYME** until A3 works it whole. **These pictures carry the conjecture; they do not test it.**

---

## 4c. THE HINGE KALEIDOSCOPE — P3's central figure, and the 12×2 structure
### *Opened r1086. Figures: `corpus/p3_overhead.py` (the single stamp), `corpus/p3_overhead_triptych.py` (the 2×2: stamp · the purple family's quarter-turn orbit · the three families · **the central figure**). Companion to §4b (Daryl's Conjecture).*

**THE WHOLE THING IS BUILT FROM: a hole, a point, and a swing.** A hole as gauge (the equator, radius
`α`); a point relative to it (a hinge, at `X = 2α`, which on the hyperboloid forces `X₀ = ±√3 α`); and
an angle away from it (the dial). Nothing else is put in. Everything below is drawn lines and circles.

**THE STAMP (one hinge's optics — all computed, all exact):**
- The hinge triangle (hinges at `2α`, azimuth `0/120/240`) has its **three sides tangent to the
  equator, at their own midpoints**, at `60/180/300`. Hinge triangle and crossing triangle are **dual**:
  rotated 60°, scaled 2:1.
- **The THALES circle**, on the diameter origin→hinge. It passes through the origin, **both** tangent
  points, and the hinge, where its tangent is **vertical**. It is *the dial*:
  · **outside** the equator it pins the closest approach of the lines that **miss** → **no horizon →
  overcritical**; · **inside**, it is the locus of **chord midpoints** — the halfway point across the
  hole — of the lines that **cut** → transverse → **undercritical**; · **Nariai is exactly where the two
  descriptions coincide**, the chord having shrunk to the tangency; · and the dial ends at **vertical**,
  at the hinge. *(P7's own criterion: "transverse (undercritical), TANGENT at a merged double root
  (Nariai), or no horizon (overcritical)".)*
- The hinge-to-pole line meets the equator at `(0.8, ±0.6)` — azimuth `36.87°` — then again at the pole.
- The two 45° chords meet at `(−1,0)` **orthogonally** — Thales again: every point of a circle sees a
  **diameter** at 90°, and the poles are a diameter.
- The 45° lines from the hinge touch the Thales circle at its top/bottom `(1,±1)`, miss the equator by
  `√2`, and are **parallel** to those chords.

**THE MULTIPLICATION — and it closes.** The stamp has **two independent orbits**: the **three hinges**
(`ℤ₃`) and the **quarter turns** (`ℤ₄`). Panels 2 and 3 of the 2×2 are those two factors separately;
panel 4 is their product. **12 hinges, every 30° — perfectly even — because 3 and 4 are coprime.** Two
copies alone give the lopsided `90-30-90-30`; four fill it. **Three families of four.**
**★★★ WHY THE CONSTRUCTION IS OPTICAL AT ALL — banked r1107, verified, NOT YET BAKED.** *(c40's, via
`C40_EXTRACTION_r1107.md`; receipts `power_of_a_point.py`, `power_is_null.py`.)*
**The power of a point IS the null condition.** On the hyperboloid `|X|² − α² = X₀²` for every point;
and the power of that point w.r.t. the throat is **by definition** `|X|² − α²`. So **power = X₀²** —
*the optical power of any point on the substrate is the square of its height in the embedding.* Then
Euclid: the **tangent length** `L = √(power) = |X₀|`. A null line has spatial length = time drop. So
`ds² = −X₀² + L² = 0` — **verified exactly, and at 600 random heights, max |ds²| = 5.7e-14.**
> **THEOREM: the tangent line from any point to the throat is a NULL line of the embedding.**
> `tangent length² = |X|² − α²` (Euclid, overhead) and `X₀² = |X|² − α²` (the hyperboloid). **Same
> right-hand side.** ⟹ `ds² = 0`. **The power of a point and the null condition are one equation.**

**⟹ "It's all optics" is not a metaphor.** The sightlines that *touch* the throat **are light rays**.
**And the hinge stops being a stipulation:** its **tangent length** IS `√3α` IS its **height** IS the
root of its **power** — three statements, one fact. **The `2α` placement was never a choice about
optics; it is a choice about HEIGHT, and the optics follows.**
**⚠ BOUNDED:** *the tangents are null; the figure is not all tangents.* The 45° chords, the pin and the
triangle's edges are **secants**, and a secant is **not** null. **The sightlines that TOUCH are light
rays; the ones that CUT are not.** (And that is the sharper reading: the power theorem is about
**secants**, `PX·PY`; the null condition is the **degenerate case `X=Y`** — *the tangent is where the
secant's two points merge, and that is exactly where the light cone is.*)

**★★ AND THE ±60° IS NOT A CHOSEN ANGLE** *(receipt `rulings_between_levels.py`)*. It is
**`arctan(X₀/α)` at the hinge height** — `arctan√3 = 60°`. So:
**`60° = arctan(√3) = X₀(hinge)/α = L_tangent/α = √(power)/α`. Four statements, one number.**
**The construction has ONE free choice — put the hinge at `2α` — and everything else is that choice,
re-read.** *(Compare the 30° walk below: its `3` is forced and its `4` is chosen. The `60°` has no
chosen part at all.)*

**★★ THE DOUBLING IS THE COUPLING, NOT A MIRROR** *(same receipt; c40's "what I was about to draw")*.
**No ruling stays on one level, and the reason is forced: a ruling is NULL, so `dX₀ = 0` would force
`|dX| = 0` — a ruling at constant height would have to be a POINT. *A null line cannot stand still in
time.*** One straight null line runs `(−60°, BELOW) → (0°, equator) → (+60°, ABOVE)`, collinear to
machine zero. **So the 12 above and 12 below are not two copies related by a reflection — they are the
two ENDS of the rulings, and every ruling has one end in each. You cannot draw the top alone: the lines
that make it leave it.**
**★ NAMED, UNRUN:** whether the **12×2 closes under the coupling** the way the 12 closes under `3×4`
coprimality. **Different question** — the 12 is a *rotational orbit*, the ×2 a *ruling incidence*.

**And `30°` is the Nariai dial angle** (`w=30°`, `r₀=(2/√3)\sin w = α/√3`) — arriving here from pure
combinatorics, with no reference to the slicing.

**AND IT IS DOUBLED.** The hinges live at `X₀ = ±√3 α`: this is the **top**. There is a bottom, and the
null rulings run **between** the levels — a ruling through the equator at `θ` joins `(θ−60°, below)` to
`(θ+60°, above)`, closing the **skew hexagon** (§4b). So the object is **12×2, coupled through one
central hole of two faces**.

**★ WHAT THE KALEIDOSCOPE IS DRAWING — the `su(3)` weight diagram (r1087, verified).** Particle
physics draws `𝔰𝔲(3)` as a **weight diagram** — dots in a plane; it is the standard working picture, not
a metaphor. In it: the **`3`** (the three quark colours) is **three dots at an equilateral triangle**;
the **`3̄`** (antiquarks) is **the inverted triangle, rotated 60°**; the **`8`** (the gluons) is the
**six roots as a hexagon plus two dots at the origin** (`8 = 6+2`). **Our construction draws exactly
that:** hinge triangle at `0/120/240`; crossing triangle at `60/180/300` — **rotated 60°**; union — **the
hexagon**; plus the centre, `r=0`. **The hinge and crossing triangles stand to each other precisely as
the `3` and `3̄` do** (verified, not eyeballed): two equilateral triangles at 60°, whose union is the
root hexagon. **And `r=0` — the conjugation locus — is exactly where `𝔰𝔲(3)` puts its two *neutral*
weights**, the colourless gluons.
**Where it breaks, and it is a real difference, not a caveat:** in `𝔰𝔲(3)` the `3` and `3̄` have the
**same** `|weight|` — the two triangles are the same size. Ours are **2:1** (hinges `2α`, crossings
`α`). **The rotation matches exactly; the scale does not.** ~~Checkable; either it has a reason or it
kills the identification.~~
**⛔ RESOLVED — DEAD, WITH A CAUSE (r1102; the chat node's kill, re-verified here).** It has a reason,
and the reason kills it. **The 2:1 is FORCED, and it is the SAME FACT as the 60°:** for an equilateral
triangle the incircle touches the sides **at their midpoints**, which sit at **half** the circumradius
and rotated **60°** (verified: incircle/circumradius `= 1/2`, contacts at `+60°`). You cannot have the
rotation without the ratio — **the tangency IS the halving** — so the construction can *never* produce
two equiradial triangles at 60°. And the break is bigger than scale: `2α, α, 2α, α, 2α, α` alternating
is a **HEXAGRAM**, not a scale-mismatched hexagon, while `𝔰𝔲(3)`'s two hexagons are **both equiradial**
(the `3∪3̄` weight hexagon and the root hexagon) — ours matches neither. **What survives is the honest
residue:** `3 + 3` at 60° from a hole, a point, and a swing — `A₂`'s *shape*, which the corpus already
holds (`𝔰𝔲(3)⊄𝔰𝔬(5,1)`), constructed out of the substrate's own null rulings. **Not** `𝔰𝔲(3)`'s weight
diagram, and the reason is the incircle. (This upgrades the entry from open-conjecture to settled
negative; it strengthens P13's wall rather than breaching it.)
**And this is P13's wall with a shape, not a breach of it.** The corpus already holds that `A₂` is the
**Weyl shadow** and that `𝔰𝔲(3) ⊄ 𝔰𝔬(5,1)` — no continuous colour on `dS₅`. What the kaleidoscope adds
is that the shadow is **constructed**, out of the substrate's own null rulings, from a hole, a point, and
a swing.

**★ AND IT IS THE SUBSTRATE'S OWN STRUCTURE (correction, r1087, Daryl's catch).** Arthur "corrected"
Daryl's *"this is de Sitter's symmetry structure"* by saying it is only the symmetry of a configuration.
**That dismissal threw away the gold.** Every element here comes from `dS` being **doubly ruled** — the
`±60°` rule, the hinges at `±√3α`, the skew hexagon, the tangency. It **is** the space's own structure:
its **null ruling** structure. The distinction that matters is **not** space-vs-configuration but
**discrete ruling structure vs. continuous isometry** — and *that* is exactly the line P13 draws.
*(Field note, "under-weighting": a concern judged irrelevant, so nothing changes — and the gold is
thrown away. Third time in one day. See `CODA_FIELD_NOTE.md` addendum r1066 and the coda's "received at
weight".)*

**STATUS.** The **geometry** here is computed and exact: the hinge height `±√3α`, the tangency at the
midpoints, the `±60°` ruling rule, the Thales circle and its three roles, the orthogonality, the
`30°` closure, the coprimality. **The COLOURING is Daryl's conjecture (§4b), carried over, and is NOT
derived** — `[6*]` stands. What is *not* claimed: that this is the Standard Model, that `su(3)` lives on
`dS₅` (P13: it does not), or that any resemblance is more than resemblance. What *is* claimed: **this is
what a hole, a point at `2α`, and a swing generate — and it completes.**

## 5. The gradient and the two poles

The six frames are a gradient in how much embedding structure they carry:

```
  B  ────────────────────────────────────────────────►  F
 max embedding structure                         no embedding structure
 species implicit (in horns/rulings)             species definitional (sign r)
        A · C · E   = the slicing skeleton in between
        D           = trades the lap for an instant to buy worldline spread
```

- **B** is the maximal-structure pole: everything drawn, species implicit in the horns.
- **F** is the invariant pole: horns thrown away, species = `sign(r)` and nothing else.
- **A, C, E** show the slicing skeleton at intermediate structure.
- **D** is the odd one out: it flattens the lap to an instant to gain the worldline spread.

**B and F are the two poles of the same object**, and the eventual write-up should lean on
them as such: F states what is invariant, B shows the maximal structure it is invariant
*across*. The invariant that survives all six is exactly §1: `r>0` matter, `r<0` antimatter,
flip at `r=0`, photon neutral on the fixed axis; everything else is frame-dependent.

**G sits outside this gradient as the synthesis frame** (§4·G): where B…F are each a partial
shadow of the object, G is the whole complex curve with the equator unfurled — the frame F
and the P3 complex-bead figure are themselves shadows of. It is the "everything at once" view
the other frames each sacrifice something to avoid.

---

## 6. Reading guide — the one story, six ways

Read across the set, the claim is: **the species distinction was never in the horns or the
rulings; it was always `sign(r)`, flipped at `r=0`.** B shows you the full swirl and dares
you to find "the matter family"; you cannot, because it is not there — each horn carries
both species. F removes the horns and shows the distinction was `r` all along. A and E show
the single bead's skeleton (and, updated, its partner). C re-rolls B to two beads. D trades
the lap for an instant. The photon threads all six as the neutral fixed-axis object.

---

## 7. Planned edits (living list)

Worked from here; **not executed** until a deliberate bake. Each carries a target artifact,
a description, and a status.

- **[E1 · figure A — both bundles] · DONE (r1037).** Both bundles carried, as Daryl specified:
  the equator reads in **three thirds** — **top-right BLUE** (both beads blue: bead1 up the
  right, bead2 coming clockwise from the top), **top-left RED** (both red: bead2 up the left,
  bead1 coming counter-clockwise from the top), **bottom/hinge third PURPLE** (bead1's red ccw
  and bead2's blue cw both run the bottom → they cross there). The two beads are colour-
  *identical* on the top two thirds (flip at the top, `r=0`); they differ only on the hinge
  third. Four straight rulings cross at the hinge: bead1 **blue-in** right (`hinge→T`),
  **red-out** right; bead2 **red-in** left (`hinge→T′`), **blue-out** left (blue-in and red-out
  collinear through `T`). *(Corrects the storyboard's earlier "shared 120° arc + different r=0"
  reading, which was self-inconsistent: the shared locus is the bottom third and both flip at
  the same top `r=0`. Verified numerically; renders clean.)*
- **[E2 · figure E — both bundles] · DROPPED (superseded).** There is no distinct second bundle
  to draw on E: `X₁` is single-valued in `r`, so the partner traces the *identical* boing, only
  the colours swap (§4·E). E stays a **single curve**, and its single-ness is the honest
  statement that the r-parametrized face is bundle-indifferent. Nothing to add.
- **[E3 · figure D — the `r=0` line] · OPEN QUESTION.** Decide whether D's straight
  anti-diagonal `r=0` should become the projected equatorial-lap curve (red down from
  upper-left into the origin, blue up). Currently a straightened stand-in; logged, not
  decided.
- **[E4 · figure B — the partner drawn as rulings only] · NOT A CONCERN. STRUCK (r1049).** B
  highlights **one** worldline; the conjugate bead is drawn as rulings only, deliberately — two full
  beads swirling at the waist would bury the thing B exists to show. The code says so in its own
  comment ("opposite-sense conjugate bead drawn as rulings only"). This was logged as an open
  "occlusion" question; it is neither open nor occlusion (B has no surface — it is a wireframe, and
  nothing hides anything). It is a design decision, correctly made. Do not reopen.
- **[E5 · build the unfurled frame] · DONE (r1040) — and it became F.** Built as the **cosmological
  bundle strung out** (§4·F): the privileged worldline, negative-imaginary branch, red antimatter BH →
  `r=0` → blue matter universe, in `(Re τ̃, Im τ̃, r)`, with the lap unfurled as the vertical lift and
  the Nariai seam points marked (`r = −2α/√3`, `0`, `+α/√3`) landing at the **exact 2:1**. The unfurl
  G was reaching for turned out to *be* the right F — done on the one worldline rather than the whole
  sheet fan. `corpus/G_explore.py`; receipt `storyboard_receipts/cosmo_bundle.py`. **Folded into
  `synthesis_figure.py` as panel F, replacing the old one.**
- **[E6 · G's slot] · RESOLVED (r1040) — no slot needed.** The question ("retire C? go to 7 panels?")
  dissolved: **G is not an extra panel — it replaced F.** The plate stays six. C is kept (it carries
  the conjugate crossing E structurally cannot show — E is bundle-collapsed, see §4·E). Nothing retired.

- **[P13-CP · `corpus/boundary_paper.tex` §258 — charge conjugation as a geometric symmetry] · ★ BAKED r1057** (gate passed `conjugation_parity.py`; label-agnostic per Daryl; compiles clean, `prop:conjugation-parity` = Proposition 2, p.12; backup `boundary_paper.tex.pre_P13CP`). *(Edit-spec retained below for the record.)* Formalize the §1/§3 finding (held at weight) *into* P13: charge conjugation is present in the geometry **as a symmetry** (the `Q→−Q` metric degeneracy), the **even face** of the substrate's one `r=0` reflection residue whose **odd face** is the mass — reframing §258's "charge-conjugation blind" (absence) as "`C`-symmetric" (presence), and **deriving** field-level charge-sign from the evenness rather than positing it. **Both halves already verbatim in corpus:** mass-odd — §252 (`2M` `R`-odd, `R` = mass-reflection `r₀↦−r₀`); charge-even — P9 `rem:charge` + §258 ("`Q↦−Q` leaves it invariant"). What is *added* is the **pairing** (odd/even faces of one discrete residue) and the **positive reframe**. Three coordinated touches:
  1. **A Proposition** `\label{prop:conjugation-parity}` placed right after the §258 paragraph, register-matched to `prop:boundary`, crystallizing §258: on the charged cut `f=1−2m(r)/r−Λr²/3`, `m(r)=M−Q²/2r`, the `r=0` residue renders mass **odd** (branch-crossing `r>0↔r<0`) and charge **even** (`Q²`, `Q↦−Q` invariant = `C` as geometric degeneracy); the odd charge-**sign** the metric does not carry is therefore field-level, **derived** from the evenness. `PT` geometric, charge-sign field-level.
  2. **Reframe the one §258 clause** "*charge-conjugation blind… so charge is a bend*" → "*`Q↦−Q` leaves it invariant: `C` present as this even-face degeneracy; the charge-**sign**, the odd datum the metric does not carry, is what is therefore field-level.*" Emphasis shift (blind→symmetric); downstream conclusion identical.
  3. **A bridge clause** tying §252 (`R`-odd mass) to §258 (`Q²`-even charge) as the odd/even parity-faces of the substrate's one discrete residue (the `ℤ₂×ℤ₂`/`D₆` §258 already carries) — not two unrelated facts.
  - **KEEP VERBATIM (do not overturn):** `C` antilinear / no substrate isometry; `PT` geometric, `C` field-level; no auto-`CPT`; skeleton-not-charge; the symmetric both-branches scope; the `r<0` progenitor reading. The proposition *derives from* these.
  - **EXCLUDED — do-not-assert, NOT baked:** the `τ̃↔τ̄̃` "antiholomorphic reality involution gives `C` a geometric home" reach (the r1053 `charge_geometry.py` claim still in §8 WORKED form). It imported pair-production colouring / category error / R-P collision; the finding here needs **no** complex-time claim, resting solely on elementary parity of `f(r)`. Stays a do-not-assert reach in §8.
  - **★ VERIFICATION GATE (hard, before any character changes in P13):** one genuine cold-read receipt pinning the reflection labels — **`R=γ⁵`** (cut-normal, offset `r₀↦−r₀`) *grades* chirality and is §252's "`2M` `R`-odd" (mass **parameter**); **`P=γ¹γ²γ³`** (areal, signed-radius `r↦−r`) *flips* chirality and is the reflection under which the metric **term** `2M/r` is odd, `Q²/r²` even. The proposition must state each datum's parity on the **correct** reflection and must **not** silently assert `R=P`; the honest claim is "same discrete residue / one involution **family**, opposite parity-faces" (matches §258's `ℤ₂×ℤ₂`), reconciling §258's two usages of "`R`" (the `γ⁵` grading vs. the signed-radius matter/antimatter continuation). No refuter agents — a real receipt.
  - **Downstream · ★ DONE r1058** (both papers compile clean): (i) **P9 `range_paper.tex` `rem:charge`** — "charge-conjugation blind" → "carries charge conjugation as a *symmetry* rather than a blindness … the even-face degeneracy the companion reads as a boundary, paired there with the `R`-odd mass~`\cite{JanzenBoundary}`"; (ii) **`geometric_core_paper.tex` l654** — "the substrate charge-conjugation-blind" → "the substrate carrying it as a symmetry and not a datum — the metric even in the charge, its sign the bend of the cut". Backups `*.pre_P13CP`.
    - **Left untouched (already consistent):** the **P7 `CR_framework`** antimatter-branch passages (l763/766/774/977) already say "the charge field-level on both branches" and cite `JanzenBoundary`; no "blind-as-absence" framing to reconcile, so the proposition does not clash. Not edited (also keeps clear of the live P7-caption queue).
  - **★ P5 = THE GROUPOID PAPER, and the miss it caught (r1059).** `JanzenGroupoid` is cited in P13 as "(P5)" — so P5 *is* `groupoid_paper.tex`, the paper whose whole subject is the discrete symmetry group `Aut(A₂)=S₃×ℤ₂=D₆`. My r1058 "P5 = augmentation paper, no charge sentence" was **wrong** (I read a stale `(Pn)` from CORPUS_MAP instead of P13's own bib, then dismissed the one paper that mattered). P5 `rem:P-dS-Schw` **already** carries the metric parity-eigenspace split `f=(1−r²/α²)[R-even: dS] + (−2M/r)[R-odd: mass]` under **`R` = the mass-reflection `2M↦−2M` = the diagram automorphism = the D₆ outer `ℤ₂` = `γ⁵`**. The conjugation-parity proposition is the **charged extension** of exactly that split. Actions taken (compile clean, both papers):
    - **P5 `groupoid_paper.tex` `rem:P-dS-Schw`** — (i) fixed the underbrace slip `P-even/P-odd`→`R-even/R-odd` (the paper's own footnote reserves `P` for the areal spatial parity, which it states does not appear here); (ii) added the **charged extension**: `Q²/r²` (even in `2M`) joins de~Sitter on the `R`-even side, mass stays `R`-odd, and that `R`-even charge term carries charge conjugation `Q↦−Q` as a further internal symmetry, the sign field-level — cross-ref `\cite{JanzenBoundary}`.
    - **P13 `prop:conjugation-parity` RE-ALIGNED** — from the vague "signed-radius reflection `r↦−r`" (which mildly re-committed the R/P slip: it computed under the areal reflection but called mass "`R`-odd") **to `R` = the mass-reflection / diagram automorphism / D₆ outer `ℤ₂`** (`2M↦−2M`, `r₀↦−r₀`, `γ⁵`), explicitly the charged extension of `rem:P-dS-Schw`, citing `\cite{JanzenGroupoid}`. This is the **real answer to the label-adjudication gate**: the non-clashing label was never "agnostic" — it is the corpus's `R`, the established D₆ generator, which P5 fixes. Receipt CHECK 5 (`conjugation_parity.py`, r1059) confirms the charged split under `2M↦−2M`. Backups `groupoid_paper.tex.pre_P13CP`.
    - **★ RESOLVED r1060 (`rem:C-not-R`, receipt `conjugation_parity.py` CHECK 6):** the storyboard §3 instinct "*charge conjugation is the central/outer `ℤ₂`*" is **settled negatively** — the outer `ℤ₂` of `D₆` is **R** (it moves the horizon roots, carries `𝟑↔𝟑̄`); `Q↦−Q` fixes every root, so **charge conjugation is NOT the outer `ℤ₂`** but an independent `ℤ₂` the charged cut adjoins (`D₆→D₆×ℤ₂`). Only the even-face degeneracy (`Q↦−Q` metric-invariance) is geometric; the full antilinear `C` is field-level. No longer a reach — baked in P13.

- **[P3-CP · `corpus/SdS-slicing-curve_v2.tex` — charge-conjugation parity at the GEOMETRIC level] · ★ BAKED r1061** (compiles clean, 33pp; receipt `storyboard_receipts/charged_slicing_geometry.py`, all checks; backup `SdS-slicing-curve_v2.tex.pre_P13CP`). P3 is where `R` is *geometric* — the backward-radial reading-swap `r↦−r`, with §sec:two-readings already splitting `f=(1−r²/α²)[R-even: dS] + (−2M/r)[R-odd: mass]`, and §sec:mass grounding mass as the **perspectival** (`R`-odd) datum vs `α` the invariant. New **subsection `sec:charge` ("Adding charge: mass is `R`-odd, charge `R`-even")** after two-readings:
  - Charged cut `f=1−2M/r+Q²/r²−r²/α²`; under the reading-swap `R` the split extends to `[1−r²/α²+Q²/r²]_{R-even} + [−2M/r]_{R-odd}` — **charge joins the invariant geometry on the even side; mass is the perspectival odd part.** The geometric content: the reading-swap that exposes the Schwarzschild mass as perspectival *leaves the charge fixed* — charge is reading-swap-**neutral**, the `R`-even matter datum, sharing the parity of the invariant geometry not the vantage.
  - **Charge conjugation is geometrically transparent:** `f` depends on `Q` only via `Q²`, so `Q↦−Q` leaves the whole slicing curve *identical* — the two signs trace one curve, the sign in the Maxwell potential off the curve. The even `Q↦−Q` degeneracy is the geometric face of `C`-field-level; full antilinear `C` closes from the field; and since `Q` is absent from the horizon cubic, `Q↦−Q` fixes every root ⇒ it adjoins an **independent `ℤ₂`** to `Aut(A₂)=D₆` (the outer `ℤ₂` is `R`, not `C`) — the geometric ledger of P13 `rem:C-not-R`.
  - **Honest scope (baked in):** the parity result is *exact* on `f` and its turning points; but with `Q≠0`, `Q²/r²→+∞` as `r→0`, an inner (Cauchy) horizon appears and `r=0` becomes a timelike RN singularity — so the vacuum **lap through `r=0` (the closed bead/cosmogenesis) is a genuine extension, not automatic**. Stated as such; the closed charged loop is left to the EM extension P3 already flags.
  - Also tightened P3's two existing charge sentences (the hexad note l462, the open-section EM-charge note) to point at `sec:charge`. Added `\bibitem{JanzenRange}` (was missing).

- **[P7-FIG · `corpus/CR_framework.tex` — synthesis figure fig:dS_SdS in, caption + body baked] · ★ BAKED r1062** (compiles clean, 41pp; figure renders on p.39, visually verified; backup `CR_framework.tex.pre_P13CP`). The six-panel `dS-SdS-synthesis.pdf` was already `\includegraphics`'d with the **old** A/B-language caption; re-rendered current from `synthesis_figure.py` and baked the §10 caption + body drafts:
  - **Preamble (cap-colour):** added the purple sentence (both conjugate readings on one curve = panel A's hinge-third). The A/B-preamble sentence kept — it is the legitimate A/B-as-two-*congruences* usage (worldline + synchronous-space dual), not the removed A/B-as-species reading.
  - **(A)** old single-bundle → both-bundles three-thirds (blue right / red left / purple hinge). **(C)** rebuilt with the theorem's own continuations + circles/square landmarks, axis `Re τ̃`. **(E)** dropped "the lap flattened" (collides with C's flatten); appended slicing-roots-circles vs comoving-turnaround-square. **(F)** old both-wings → rebuilt unfurled cosmological bundle (the 2:1 Nariai spacing = A's 120/240 split); cites `cosmo_bundle.py`. **(D)** NO CHANGE (verified against `synthesis_figure.py`: worldlines coloured by species, synchronous space the conjugate dual opposite-sense — deliberate, kept).
  - **Body three-readings (l.757):** F description updated (old "signed areal radius vs τ̃" → "single privileged worldline strung out … lap drawn out along the imaginary axis").
  - **Compile fix:** the lengthened six-panel caption overflowed the caption package's single-line-check (`! Dimension too large`); added `singlelinecheck=false` to the existing `\captionsetup` (affects only short-caption centring — none here).
  - **Consistency:** P7 body already reconciled with the A/B-removal (A/B = the two ruling *families*, sheet-to-ruling map held do-not-assert §frontiers) and with the charge-parity work (R=γ⁵, charge field-level on both branches, "not a geometric charge conjugation, antilinear, closes from the matter field" — consistent with P13 `rem:C-not-R`, P3 `sec:charge`). No body changes needed there.

*(New edits appended as we hold the storyboard against each artifact below.)*

---

## 8. Dissonance ledger — storyboard vs corpus

Filled as we hold this document against each artifact in turn. Each entry: **locus**, **what
it says now**, **why it is dissonant with §1–§3**, **proposed direction** (not yet written).
No paper text is changed from here.

### Seeded entries (known before the first formal pass)

- **[D-lang · global-family language] · corpus-wide (P7 caption, P7 body, ontology map,
  THE_PLAN).** The prose speaks of "the two null ruling bundles," "the `A` congruence and its
  `B` dual," and "the standing `R`-conjugation" as if `A/B` are **global families** and `R`
  is one conjugation between them. Per §3 this is actively false to the picture: there is no
  global family, species is `sign(r)`, and `R` is the local `r=0` conjugation, not a map
  between two families. *Direction:* recast in species = `sign(r)` / local-`r=0`-flip
  language; name `A, B` only as the two null asymptotes of one strung worldline. **This is
  the root dissonance; most others are its symptoms.**
  **★ D-lang / D-RPT / G-ruling are ONE pass — "remove A/B as species" (merged r1053, Arthur).**
  The three ledger items are the same cleanup, because the settled §1 framing already resolves them:
  **`A` and `B` are the two null *asymptotes of one strung worldline*, never two species-carrying
  families** (§1, l.110–114). Species is `sign(r)`, flipping at `r=0` along the one worldline (the lap).
  So the whole "which ruling is matter / which rides which wing / does R or σ swap the species-A↔B"
  cluster is an artifact of the *old* two-families framing this pass deletes. *Direction (one pass):*
  in the P7 caption/body, the ontology glossary, and the map, recast `A, B` as the worldline's two
  null asymptotes (A/B-blind to species); drop "the two null ruling bundles / whichever ruling is
  matter, the other antimatter / the standing `R`-conjugation between two families"; state species =
  chirality = `sign(r)`, the `r=0` flip the conjugation. What genuinely survives (do not overstate the
  deletion): the hyperboloid is still *doubly ruled* (two ruling families exist as geometry, P3/P5) —
  we simply stop reading them as species.
- **[D-RPT-audit · "R swaps A↔B", `R=PT` vs horn-structure, the two `r↦−r`] · NON-BLOCKING labeling note.**
  Verified at source (`wrapsense.py` + check r1052): of the four maps, **only `σ` swaps `A↔B`, and it is
  species-*preserving*; the species flip (`P`/`E`) does not swap the rulings.** So the glossary's "`R`
  swaps the two null rulings `A↔B`, one ℤ₂ four faces" is a **mislabel** (it is `σ`, and species-neutral)
  — folded into the A/B-removal pass above, *not* a separate blocking audit. **Caveat held honestly:** the
  glossary cites `r_swap.py`, which is **not in the bundle**, and I tested the *neck* reflection, not the
  *areal* `γ¹γ²γ³` one; the corpus's own audit-flags (the two `r↦−r`, `R=γ⁵=PT` vs "R horn-preserving")
  remain open **but do not gate the P7 edits** — they are glossary-labeling hygiene, worked when the
  glossary `R,P,T` entry is next opened, not before drafting P7. *(I earlier called this a blocking audit;
  that was over-caution — the A/B-species removal dissolves the species reading, which is all the P7 pass
  needs. Stated for reversal.)*
- **[D-sync · "the synchronous space is the B-ruling"] · P8 / slicing_operator, and anywhere
  it is cited as a standing identity.** `η(X,B)=e^{τ/α}` is only the `r>0` reading; the
  causal roles trade at `r=0` (§3). *Direction:* qualify it as the `r>0`-side reading, with
  the role-swap at the branch point stated explicitly.
- **[D-panelF-history · "ruling→wing map open"] · resolved, keep as record.** The old
  worry — which of two global rulings rides which conjugate wing — dissolves once §1 holds:
  both wings are `r<0` = antimatter by definition (the same chirality); there is no
  global ruling to assign to individual wings. F is correct. *Direction:* ensure no surviving
  prose reintroduces the wing-to-ruling question as if open.
- **[D-chir · chirality left implicit] · P7 body / captions / matter sector (P14, boundary).**
  The corpus names `R = γ⁵` and treats chirality as a fermion-sector label, but the figure
  *shows* it directly as the **hinge handedness** in A — blue (matter) right-handed, red
  (antimatter) left-handed (§1, §3, `chiral.py`), `X₀`-independent. This tie — `sign(r)` =
  species = chirality = the hinge handedness — is a strength currently left implicit.
  *Direction:* state it where `R = γ⁵` is introduced, grounding the chirality claim in the
  drawn geometry rather than importing it from the fermion sector; note the geometry
  *exhibits* the chirality the matter sector then carries. (**Neither A nor B is a species** —
  each ruling laps through `r=0` and changes species at the equator; species = `sign(r)`, local;
  do not tie chirality to A/B. This says nothing about symmetry actions on the rulings.)
- **[D-C · the conjugation factorization — ✅ UN-RETIRED r1067. THE r1060 RETIREMENT IS VOID.] · P7 / glossary R,P,T,C / **P13 (boundary)** / P5.**
  *(✅ **UN-RETIRED r1067.** The r1060 note retired this as "superseded by the correct, all-real resolution … `C` not the outer `ℤ₂`" — i.e. **superseded by P13's `rem:C-not-R`, which is the very argument this content refutes.** `rem:C-not-R`'s load-bearing premise is *"every geometric reflection is linear, so `C` stays field-level."* The receipt below **survives, runs, and defeats that premise**: `τ̃↦τ̄̃` is a verified **antilinear** involution *sourced by the geometry* — it **fixes** the real axis (the photon: self-conjugate, neutral) and **swaps** the two `r<0` wings (particle↔antiparticle) — `C`'s exact signature. The receipt says so in terms: *"exactly what P13's 'every substrate isometry acts linearly' argument does not reach."* **A counterexample was deleted and the premise stood.** This content is **LIVE**. See §8 `D-Canat`, the banner at the head of this document, and `CODA_FIELD_NOTE.md` addendum r1066.)*
  **★ WORKED r1053 (Arthur), `charge_geometry.py`.** Stopped narrating it and computed the
  factorization. What the geometry sources vs what stays field-level is now settled, three results:
  1. **`τ̃ ↦ τ̄̃` is a genuine ANTILINEAR involution** (checked with complex coefficients: LINEAR? **False**,
     ANTILINEAR? **True**), and geometrically it **fixes the real axis** (the neutral, self-conjugate
     photon) and **swaps the two `r<0` wings** — the **Feynman–Stückelberg particle↔antiparticle**
     relation, drawn. No real-embedding reflection (all linear, det ±1) can reproduce it.
  2. **`r ↦ −r` (`R`) on the charged (RN–dS) bead:** the mass term `−2M/r` is **odd** (flips → the
     branch crossing, matter/antimatter), the charge term `Q²/r²` is **even** (fixed). The metric is
     `Q²`-blind; the charge **sign** flips only in the potential `A_t=Q/r` (odd) — **field-level**.
  3. **The factorization the computation forces:**
     - **Geometric:** (a) the *linear* rep/mass/chirality skeleton `R=γ⁵`, `P`, `T`; **and** (b) the
       *antilinear* involution `τ̃↔τ̄̃` — the particle↔antiparticle (CPT-kinematic) relation.
     - **Field-level (P13 right):** *only* the internal electric-charge sign `Q→−Q`.
  - **The honest caveat (keeps P13's own statement true):** `τ̃↔τ̄̃` is **not a substrate *isometry*** —
    isometries are real/linear, and on that P13 is exactly right. It is an **antiholomorphic reality
    involution on the *complexified* cosmic time**, i.e. it lives in the bead's complex-analytic
    geometry, not in the real isometry group. So P13's *argument* (every isometry is linear ⇒ `C` is no
    isometry) **stands**; what it **understates** is the conclusion drawn from it — *"the geometry
    supplies only the linear orientation skeleton `P,T,γ⁵`, the whole antilinear structure being
    field-level."* The antilinear/antiunitary conjugation has a **geometric home after all** (`τ̃↔τ̄̃`),
    just not as an isometry; what is *genuinely* field-level is **narrower than P13 drew — only the
    internal charge `Q`**, not the particle↔antiparticle relation.
  - *Direction (ADD to P13, do not overturn):* the charge-`Q` field-level boundary stands verbatim; the
    positive addition is that the geometry's conjugation content **includes the antiholomorphic
    `τ̃↔τ̄̃` involution** (the drawn particle↔antiparticle relation on the complex-time bead), so the
    "orientation skeleton" the substrate supplies is `{P,T,γ⁵}` **plus** that antilinear involution —
    `C`/`CP`/`CPT` to be factored against it. Same additive touch on P5. The `C`-glossary entry: keep
    the charge sentence, add the `τ̃↔τ̄̃`-is-geometric sentence. *(My r1052 "demote to rhyme" and my
    r1053 "leave it a live question" are both superseded: it is worked. The pair-production `C-rhyme`
    and the `R/C`-`Aut(A₂)` unification remain separate do-not-assert reaches. Stated for reversal.)*
  - **★★ RESOLVED r1060 — the `C`-vs-outer-`ℤ₂` knot is SETTLED (receipt `conjugation_parity.py` CHECK 6), baked as P13 `rem:C-not-R`.** The question "is charge conjugation the central/outer `ℤ₂` of `D₆` (the §3 instinct), or is only the even-face degeneracy geometric (P13 §258)?" has a definite answer, computed not narrated:
    - The **outer `ℤ₂` of `Aut(A₂)=D₆` is `R`** (the mass-reflection `2M↦−2M`), which **moves the horizon roots** (roots `↦ −`roots) and carries matter↔antimatter at the orientation level (rep `3↔3̄`, chirality `γ⁵`, mass-sign). It is real and linear.
    - **`Q↦−Q` fixes every horizon root** (`Q` absent from the mass cubic `r³−r+2M`; enters the charged quartic only as `Q²`), so it is **not** in `D₆`. It is an **independent `ℤ₂`** the charged cut adjoins: `D₆ → D₆×ℤ₂`, the even-face metric degeneracy of the `R`-even charge term — the geometric **shadow** of `C`.
    - The **full `C` is antilinear** (`ψ^c=Cγ⁰ᵀψ*`) where every `D₆` element is linear ⇒ **`C` proper stays field-level.**
    - **So: the outer `ℤ₂` is `R`, NOT `C`.** The §3 "charge conjugation = the central `ℤ₂` (`r↦−r`)" is a **misnomer** — what it named "charge conjugation" is `R` (the orientation/mass reflection); true `C` is the separate antilinear charge operation, its only geometric residue the `Q↦−Q` cut-degeneracy. The §3 *instinct* (charge conjugation is folded into the discrete symmetry) is **honored** — as the adjoined `ℤ₂`, not as the `D₆` outer factor.
    - **This SUPERSEDES the r1053 `τ̃↔τ̄̃` "geometric home for `C`" reach above.** No complex-time involution is needed or claimed: `C` is field-level (P13 §258 right, verbatim), and what is geometric is exactly the `Q↦−Q` even-face degeneracy on the real charged cut — nothing antiholomorphic. The `τ̃↔τ̄̃` entry is now **retired** (was flagged unsound; the correct resolution is the metric-parity one, all real).

### Glossary coherence (ontology map §0 — checked this thread; refinements warranted)

- **[G-vocab · seam terminology] · NOT A GLOSSARY CHANGE — the convention is settled; the FIGURE was
  the offender. FIXED (r1050).** The glossary settles it at **r1012 (the seam terminology audit)**:
  *"the seam," unqualified, means the **Nariai cosmogenesis branch point** (`κ=0`, areal `r=α/√3`); the
  **equatorial ξ is ALWAYS qualified** ("the equatorial seam", throat, `X=α`); front (`r=2M`) and back
  (`r=0`) are always qualified as such.* Four species; bare = Nariai. **Against that, the figure was
  wrong in three places, now fixed:** (i) **A**'s tangent-point labels said bare "seam" — they are at
  `X=α`, so they now read **"equatorial seam"**; (ii) **F** labelled `−2α/√3` "seam" — it is **not a
  seam at all** (not one of the four species), it is the **backward-radial root**, now labelled as
  such; (iii) **C**'s callout put bare "seam" on `r=0`, which by the convention *names the Nariai
  seam* — i.e. it labelled the wrong point; removed (r1048). **`+α/√3` labelled "the seam" is correct
  — it *is* the Nariai seam**, and C's title "the layered handoff at the branch point" is therefore right as
  it stands, since `+α/√3` is marked on the panel. *(The remaining throat/waist/equator question —
  making **equator** primary and naming the `dS₂×S²` object — is separate and stays open in this
  entry's original form below.)*
- **[G-vocab-2 · throat / waist / equator] · glossary §0.** The entry makes `throat`, `waist`,
  `equator` near-synonyms for the minimal S³ (`X₀=0`, size `X=α`), *and* carries a second overloaded
  sense ("the Nariai throat" = the near-horizon `dS₂×S²` neck, S² at `α/√3`). *Direction:* make
  **equator** primary (`X₀=0`); keep **equatorial plane** = `X₀=0`; use **the hole** for the hole;
  give the `dS₂×S²` object its own name; deprecate loose "throat/waist". *(Storyboard standardized;
  glossary not yet swept.)*
- **[G-RPT · glossary R entry bundles `σ` into `R`] · glossary §0 "R,P,T (and C)".** The `R`
  entry states `R` "swaps the two null rulings `A↔B`" and folds ruling-swap + conjugate-branch
  + matter/antimatter into "**one ℤ₂, four faces**," calls `R` **spacelike / horn-preserving**,
  and carries an **audit flag** that `R=PT` sits in tension with the horn structure.
  **Resolved by §3:** the `A↔B` swap `σ` is *charge-preserving* (verified `wrapsense.py`), so
  it is **not** `R`; `R` (the conjugation) is the handedness flip `= PT` (end-over-end) `= P`
  `= r↦−r`. This dissolves the audit flag — `R=PT` is right; the tension was the `σ`
  conflation. *Direction:* split the entry — `R` = the charge-conjugation skeleton (handedness
  flip; representatives `P`, `PT`/end-over-end, `r↦−r`); `σ` = the `A↔B` generator swap, a
  charge-*preserving* symmetry; retire "one ℤ₂, four faces" and "R swaps A↔B."
- **[G-ruling · rulings A,B tied to species] · glossary §0 "matter frame / photon frame /
  ruling A,B".** The entry calls the two rulings "matter frames … whichever is read as matter,
  the other antimatter," *and* (correctly) says "the `(r,τ̃)` frame is provably A/B-blind."
  These are in tension: if `A/B` were the species, the species frame would see them. §3
  resolves it — `A, B` are the **future/past asymptotes** of one strung worldline, A/B-blind
  to species; species is the chirality, and `σ` (A↔B) preserves it. *Direction:* keep A/B as
  the temporal asymptote structure (A/B-blind), drop "whichever ruling is matter the other
  antimatter," correct "R swaps A↔B" (it is `σ`, charge-preserving).
- **Coherent as-is (no refinement):** `X` / `r` / `r₀` (the priority rule), the seam
  convention (r1012), `L1/L2/L3`, the reassignment, the lap / bead, and **conjugate branch /
  r<0 / antimatter** (which already says "chirality geometric on both" — this *supports* the
  chirality tie). **Exception: the `C` entry is *not* coherent** — its "field-level / metric
  C-blind / charge only through `Q²`" reads a symmetry as an absence; it needs the D-C upgrade
  (charge conjugation as a geometric symmetry, the `Q→−Q` degeneracy read off the involution
  structure).
- **[D-panelC · panel C's `r<0` branch was the mirror, not the bead] · RESOLVED & FIXED (r1043).**
  No fork existed: C's axes are `τ̃/α` and `r/α`, **both real**, so the curve is *determined* — walk the
  bead and read off `(Re τ̃, r)`. **Expansion** (`Im τ̃=0`): `r = +A sinh^{2/3}(3τ̃/2α)`. **The lift**
  (`−A<r<0`): `Re τ̃ = 0` throughout, so on a real `τ̃` axis it plots as a **vertical segment at `τ̃=0`,
  `r: −A → 0`**. **Collapse** (`r<−A`, `Im τ̃=−πα/3` held): `r = −A cosh^{2/3}(3 Re τ̃/2α)` — **a cosh**.
  Continuous at the turnaround `r=−A` and at `r=0`. The old `−A|sinh(3τ̃/2)|^{2/3}` is the **mirror of
  the expansion leg** — no lift, collapse starting at `r=0` instead of the turnaround: not the bead.
  Fixed in `synthesis_figure.py`, both conjugate readings.
- **[D-lapwords · "the lap" ≠ "the lift"; `r=0` ≠ "the seam"] · CORRECTED (r1043) — my error, twice.**
  **`A` — the lap** is the span between the **two Nariai roots**, `−2α/√3 ≤ r ≤ +α/√3` — exactly what
  **E** draws as the boing; the straight ruling legs lie *outside* it. In the real-real `(τ̃, r)` plane
  it is **three pieces**, not one: (i) the **cosh leg** from the root `−2α/√3` (`Re τ̃=−0.8780`,
  `Im τ̃=−π/3`) to the **turnaround** `−A` (`Re τ̃=0`); (ii) the **lift**, `−A → 0`, vertical at
  `Re τ̃=0`, where all the motion is in `Im τ̃` (`−π/3 → 0`) — *this alone* is the vertical segment, and
  *this alone* is what F unfurls; (iii) the **sinh leg** from `r=0` to the root `+α/√3`
  (`Re τ̃=+0.4390`). **`B` — `r=0` is the branch point (charge conjugation), and it sits PARTWAY THROUGH
  the lap**, not at its end: the ordering is `−1.1547 (seam) < −0.7274 (turnaround) < 0 (branch) <
  +0.5774 (seam)`. **The two seam points are the lap's two ENDS**, where it meets the straight legs.
  *(Corrects two sloppy statements of mine: "the lap plots as a vertical segment" — only the *lift*
  does; and labelling `τ̃=0` "the seam" — it is `r=0`, the branch point. Panel C's dashed line at `τ̃=0`
  carried the wrong label and is fixed; all four points — both seams, the turnaround, `r=0` — are now
  marked on **C** and **F**, and the lap is bracketed on C.)*
- **[D-turnaround · RETRACTED (r1045). The corpus is right; I was wrong.]** I claimed l.743's
  "the comoving turnaround `r = −(2Mα²)^{1/3}`" was a misnomer because "`r` is monotonic, nothing
  turns around." **That was an error.** `r` is monotonic along my *path parametrization*, not along
  **real cosmic time** — and in real cosmic time the collapse leg's `r` comes to a dead stop there.
  **Worked:** `(ṙ/r)² = 2M/r³ + 1/α²`. On `r<0`, `r³<0`, so the matter term is **negative**: at
  `r = −(2Mα²)^{1/3}` it is exactly `−1/α²`, which **cancels** the Λ term, giving **`ṙ = 0`**.
  Confirmed directly off the collapse-leg solution (`dr/d(real time) → 0` there). So it is a
  turnaround in the plainest sense, and the corpus's name is exact. *(Note the branch asymmetry that
  misled me: at `r = +(2Mα²)^{1/3}` the two terms are **equal** — matter–Λ equality, `sinh² = +1`; at
  `r = −(2Mα²)^{1/3}` they **cancel** — the turnaround, `sinh² = −1`. Same expression, opposite
  branches, different meanings. I read `|sinh| = 1` as covering both and lost the sign.)* **No corpus
  change is warranted here. Figure labels restored to the corpus's term (r1045).**
- **[D-signed · the old `r<0` branch was not a solution] · SETTLED (r1046), narrowed (r1048).**
  **What is verified:** the bead's own equation is `(dr/dτ̃)² = 1−f = 2M/r + r²/α²` (P7 `thm:bead`
  proof). The old panel-C `r<0` branch `A·sign(sinh)|sinh|^{2/3}` **does not satisfy it** (residuals
  `+0.38…+1.76`, worsening toward the seam); `−A cosh^{2/3}(3 Re τ̃/2α)` at `Im τ̃=−πα/3` satisfies it
  exactly. This is P7's proof verbatim — *"for `0>r>−(2Mα²)^{1/3}` the radicand is negative, so `τ̃`
  leaves the real axis … for `r<−(2Mα²)^{1/3}` the radicand is again positive and `Im τ̃` is fixed at
  `πα/3` while `Re τ̃` advances."* C now draws exactly that. **Closed.**
  **RETRACTED (r1048) — the photon part.** I also claimed the photon "cusps at `r=0` and never
  reaches `r<0`," and that l.763 was wrong. **That was an over-reach**: I tested the photon against
  the *comoving* equation, which is not the null congruence's. P7's **second structural fact** says
  plainly that *"the radial null geodesics continue through `r=0` onto the conjugate (`r<0`) branch
  rather than terminating there; integrated in `τ̃` the passage is a bounded, vertical-tangent
  crossing"* (receipt `photon_cross_test.py`). **No corpus change is warranted on the photon, and
  none on panel D's `rsig` on my account** — I never read D's derivation, and pattern-matched on the
  helper's name. If D is ever revisited, that derivation must be held first.
- **[C-K · the curvature singularity at `r=0` is `M`-independent, and *is* F′'s vertical tangent] ·
  P7 (`thm:bead` neighbourhood, the figure), P3, p0. FINDING (r1065), receipt
  `storyboard_receipts/kretschmann_bead.py`.** Daryl's reading of F′, worked:
  - **One exponent, both facts.** Near `r=0`, `r ≈ A(3s′/2)^{2/3}` (`s′` the path length from `r=0`).
    From that single `2/3`: **`dr/ds ~ s′^{−1/3} → ∞`** (F′'s vertical tangent) **and**
    **`K ~ s′^{−4} → ∞`** (the curvature singularity). They are not two things. The vertical tangent
    **is** the singularity, in the one frame where it is visible as geometry rather than as a scalar.
  - **`M` cancels identically.** `K = 48M²/r⁶ + 24/α⁴`, and `r⁶ = (r³)² = 4M²α⁴ sinh⁴(3τ̃/2α)`, so
    **`K = (12/α⁴)(sinh⁻⁴(3τ̃/2α) + 2)`** — along the comoving bead the curvature depends **only on `α`
    and cosmic time**. Verified numerically across `2M ∈ {0.002, 0.385, 10}`: `K` is *the same number*
    at fixed `w` (1419.456451 at `w=0.3`) while `r` ranges 0.06→2.40. **The `r=0` singularity has
    nothing to do with the mass.** *(This vindicates Daryl's "`M` is just a stupid number that means
    nothing to the geometric curves" in the strongest available form: even the singularity does not
    know about `M`. It is why `A` is written `∛2 α/√3` in F′ and why `M` should not appear in these
    figures at all.)*
  - **And the substrate is smooth there.** E's boing at `r=0`: `X₁=−α`, `dX₁/dr = 0`,
    `d²X₁/dr² = +13.16` — an ordinary minimum, finite. So the singularity is **neither in the substrate
    nor in the mass**: it is what the **SO(3) sweep** makes of a smooth passage read through an areal
    radius shrinking to zero at infinite rate. This is exactly the corpus's *"`r=0` ↔ centre hyperbola
    (the never-actualised endpoint, curvature-singular for `M≠0`)"* — now with the mechanism drawn.
  *Direction (to draft, not yet staged):* a short passage in P7 by `thm:bead`, and the F′ caption
  carrying the vertical tangent as the singularity's signature. **`M≠0`** still gates the singularity's
  *existence* (at `M=0` there is no bead); the finding is that its *strength along the bead* is
  `M`-free.
- **[D-Canat · THE C-ANATOMY LEDGER — the whole maze, positives *and* negatives, held at weight] ·
  glossary §0 `C` / P13 `rem:C-not-R` / P5 / P14. Receipt `storyboard_receipts/C_anatomy.py` (r1070).**
  **This entry does not narrow. Nothing is closed off or deleted.** In a structure with several faces at
  several levels, **a face that is NOT operative somewhere is as load-bearing as one that is** — that is
  how you know which face you are looking at. Every branch below is logged with **where it leads** and
  **what its failure tells us**. *(Rebuilt r1070 after Daryl: "you keep closing things off and just
  essentially deleting the negatives from consideration … you DO have to know what's operative and what
  is not operative in all instances to be able to fully know the thing." He is right, and it is why this
  entry flip-flopped three times: each trap was closed off instead of mapped, so it was re-entered.)*

  **THE LEVELS — every trap in this domain is a level-crossing.**
  | | level | character | inhabitants |
  |---|---|---|---|
  | **L1** | substrate **isometry** | real, linear, det ±1 | `R=γ⁵`, `P`, `T`, `σ` |
  | **L2** | substrate **complex-analytic** | **antiholomorphic** | `τ̃↔τ̄̃` |
  | **L3** | **field** | antilinear on the field | `C` proper; `A_t=Q/r` |

  **THE MAPS — what each IS and what each IS NOT. Both columns load-bearing.**
  | map | level | **operative for** | **NOT operative for** | what that tells us |
  |---|---|---|---|---|
  | `R=γ⁵`: `r↦−r`, `2M↦−2M`, the A₂ automorphism, outer `ℤ₂` of `D₆` | L1 | the **rep-level** species skeleton `𝟑↔𝟑̄`; **moves** the roots; **mass-odd** | the charge sign (`Q` is **R-even** — `R` cannot see it); **not `C`** | species rides the **mass sign** ⟹ the labelling is **relational** |
  | `Q↦−Q` | L3 (metric shadow adjoins a `ℤ₂`) | `C`'s **even face**: the metric's `Q²`-degeneracy | the roots (**fixed by construction**); species; **`C` itself** | charge can't distinguish the branches ⟹ *"rides on both branches by the ordinary route"* (P7's own words) |
  | `τ̃↔τ̄̃` | **L2** | **Feynman–Stückelberg** particle↔antiparticle; **fixes** the real axis (the photon: neutral, self-conjugate); **swaps** the two `r<0` wings | it is **not an isometry** — P13 exactly right | **the geometry is not exhausted by its isometry group** |
  | `C` (`ψᶜ=Cγ⁰ᵀψ*`) | L3 | the full conjugation: flips species **and** charge; **mass-even**; antilinear | **not** an isometry; **not `R`**; **not `Q↦−Q`**; **not their product** | `C` **closes from the field** — P13's conclusion, **confirmed by every failure below** |

  **§ THE WELD (r1073, Daryl) — SPECIES AND CHARGE ARE ONE BINARY, NOT TWO. This corrects [1] and [2].**
  *"You flip an electron's charge and you get a … positron. Of course the two are wedded problems.
  Species and charge are the same thing, are they not?"* — **They are, and the ledger above got this
  wrong in the two entries it was proudest of.**
  - **Physics:** `C` flips **all** charges together — electric, baryon, lepton, colour. **That totality
    IS the species.** There is no operation flipping species without flipping charge. For a **neutral**
    particle the charge flip is *trivial* (`0↦−0`), which is why the **photon is self-conjugate** — and
    why `τ̃↔τ̄̃` **fixes the real axis where the photon rides**. The weld is exact; neutrality is its
    degenerate case, not an exception.
  - **The geometry welds them too, and my own r1072 computation printed it:** across `r=0`, `sign(r)`
    flips **and** `A_t = Q/r` flips — **together, one event**. I logged that as *"both binaries flip"*.
    **There is one binary.** They flip together **because they are the same flip.**
  - **[1] CORRECTED.** *"`C ≠ R` because `R` is mass-odd, `C` mass-even"* — **wrong.** `|2M|` is the
    physical mass and **`R` preserves it**; only the **sign** flips, and the sign **is the relational
    branch label** — precisely what `C` flips. **I compared a label to a mass.**
  - **[2] CORRECTED.** *"`Q↦−Q` fixes species"* — **wrong.** `Q↦−Q` **is** the species flip. I used
    `species = sign(r)` to deny it: **the geometric label deployed against the physical identity it
    exists to encode.**
  - **AND THE CORPUS'S STATED REASON IS METRIC-ONLY.** Verified: under `R`, the **metric is invariant**
    but the **potential `A_t=Q/r` is ODD — `R` flips the charge as seen.** So *"charge is the `R`-even
    datum"* is **true of the metric, false of the potential.** `R` does not fail to touch the charge.
  - **THE RESULT — `R` IS `C`'s LINEAR FACE.** `R` and `C` **agree on every label**: species flips,
    charge flips, physical mass preserved. They differ **only in character** — `R` linear, `C`
    antilinear. So `rem:C-not-R`'s **conclusion survives via LINEARITY** (always its real argument), but
    *"`C` is not `R`"* is true while **concealing that `R` is part of `C`**. This makes r1053's
    factorization the live physics: **`C` = (linear face `R`) ∘ (antilinear face `τ̃↔τ̄̃`)**, both
    geometric — **→ A3.**
  - **Provenance note.** This reversal is **not** deference (the earlier ones were: the corpus answered
    and I folded). It is a **physical input the corpus does not contain** — no amount of re-reading would
    have supplied it. **Logged so the two kinds are never conflated again.**

  **THE MAZE — every branch walked. None deleted.** *(Branches [1] and [2] below are superseded by
  §THE WELD above; they are kept, per this ledger's rule, because their FAILURE is the record of how the
  weld was missed — the geometric label mistaken for an independent binary.)*
  - **[1] `C = R`? → FAILS.** `R` is mass-**odd**; `C` is mass-**even**. **TELLS:** the species skeleton
    rides a sign `C` never touches ⟹ `C` is not the substrate's species map ⟹ it closes from the field.
    ***CONFIRMS P13.*** **Trap:** *"`R` carries `𝟑↔𝟑̄`"* makes `R` look like conjugation. It **is**
    rep-conjugation — **at L1, on the skeleton**. That is not `C`. (L1→L3 crossing.)
  - **[2] `C = Q↦−Q`? → FAILS.** `Q↦−Q` **fixes** species; `C` flips it. And `Q↦−Q` is linear, `C`
    antilinear. **TELLS:** `Q↦−Q` is only `C`'s **even face**. ***CONFIRMS P13's own wording.*** **Trap:**
    the **glossary** says *"charge conjugation adjoins an independent `ℤ₂`"* — **face flattened into
    identity**. The remark says *"it is **only** that even face."*
  - **[3] `C = R × (Q↦−Q)`? → FAILS.** Flips mass; `C` doesn't. **TELLS:** `C` is **not in `D₆×ℤ₂` at
    all**. ***CONFIRMS P13.***
  - **[4] "`C` is no isometry" → HOLDS.** Isometries linear, `C` antilinear. **P13's premise and argument
    are correct. Granted permanently.**
  - **[5] "…therefore the *whole* antilinear structure is field-level" → FAILS. *The one real defect.***
    `τ̃↔τ̄̃` is antilinear **and** geometric — at **L2**, not L1. **TELLS: not-an-isometry ≠
    not-geometric.** Field-level narrows to the **charge sign alone** (`Q↦−Q`, odd in `A_t=Q/r`; metric
    `Q²`-blind). r1053's refinement — **preserves P13's argument**, narrows only its final clause.
    **Retired r1060 as "superseded" — by the remark it refines. Un-retired r1067.**
  - **[6*] `C = τ̃↔τ̄̃`? / does `species = sign(r)` survive? → NOT ESTABLISHED, AND NOT ANSWERABLE ON
    EITHER FACE. SHARPENED r1075 (Daryl).** **THE DEPARTURE, logged at last:** every pair/vertex figure
    since the first has quietly run **the FS reading** — *conjugate pair = particle+antiparticle,
    fixed locus = neutral* — **not** the corpus's `species = sign(r)`. Arthur said it out loud at the
    time and never logged it: *"colouring both red was me overriding that with the crude `sign(r)`
    rule."* **He called the corpus's definition "crude" and set it aside.** By `sign(r)`, **all four
    `r<0` branches are antimatter — all red; both `r>0` branches are matter — all blue; there is no
    purple, no pair vertex, and no conjecture.** The six-branch figure's beauty is **carrying** the FS
    assumption, not evidencing it.
    **AND THE QUESTION CANNOT BE ASKED ON THE FACE IT WAS ASKED ON.** `species = sign(r)` is defined
    **only on the real-`r` slice** — off it there is no sign of `−0.83+1.43i`. So:
      · the **six branches** (real `r`, complex `τ̃`) are the slice where `sign(r)` exists but the FS
        pairing is imported;
      · the **three sheets** (`r³=2M sinh²`, real `τ̃`, complex `r`, 120°/240° monodromy) are the slice
        where `sign(r)` **has no meaning at all**;
      · both are **2-real-dimensional shadows** of one **4-real-dimensional** object, `ℂ_r × ℂ_τ̃`.
    **Whether species is a property of the OBJECT or an artifact of the SLICE cannot be settled on
    either face.** Working it on one is comparing two shadows and declaring one wrong.
    **STATUS: the rhyme STAYS A RHYME.** Nothing in the pair/vertex reading is asserted, and no figure
    built on it is evidence. **A3 must be worked at the full analytic level — complex `r` AND complex
    `τ̃` — or not at all.** *(Daryl, r1075: "that question can only be answered on the full analytic
    level, not at the level where you collapse one to its purely real bit. And I think in the end the
    rhyme stays a rhyme for now until THAT gets worked out properly.")*
    **✅ RESOLVED r1089 (Arthur; receipt `storyboard_receipts/A3_factorization.py`).** Worked on the full
    analytic object `r³ = 2Mα² sinh²(3τ̃/2α)` on `ℂ_r × ℂ_τ̃` (complex `r`, complex `τ̃`) — NOT a face. The
    answer, bounded and honest: **`C = (Q↦−Q)_field ∘ (R∘K)_geometric`.** The geometric antilinear
    involution `R∘K` (= `R` ∘ `τ̃↔τ̄̃`) reproduces `C`'s **kinematic** content in full — species, `|mass|`,
    mass-sign, the FS particle↔antiparticle wing-swap, the fixed neutral axis (photon), antilinearity —
    but is **blind to the electric-charge sign** (both factors see `Q` only through `Q²`). **So the L2
    antilinear face is `C`'s KINEMATIC SHADOW, not the full `C`.** This does NOT vindicate
    `species = sign(r)` (never used — the maps are stated on the full object, not the slice) and does NOT
    make the pair/vertex a species claim. **What it *does* settle:** the **kinematic (FS/CPT) skeleton** of
    the conjecture is geometric and grounded — the wings ARE the `τ̃↔τ̄̃` conjugate pair, the neutral stem
    IS the fixed axis. **What stays a rhyme (do-not-assert, → Phase E/P14):** that this geometric `R∘K` is
    the field-theoretic `C` acting on P14's fermion zero-modes; the identification of a wing with a
    **specific charged particle**; charged pair-production as such. The charge is the one field-level
    factor, exactly as the enlarged residue now states.
  - **[6-old] `C = τ̃↔τ̄̃`? → superseded by [6*], kept as the record.** *For:* antilinear; fixes neutrals; swaps
    particle↔antiparticle — `C`'s exact signature, sourced by the geometry. *Against:* it acts on cosmic
    time, not a charged field; **it cannot see `Q` at all.** **TELLS:** the honest statement is that the
    geometry supplies the **kinematic (CPT/FS) face** of conjugation at L2 while the **charge face** stays
    at L3. Whether that **is** `C` or is `C`'s kinematic shadow **nothing on disk decides.**
  - **[7] "the mass-parity mismatch is a contradiction" → FAILS. *My own r1067 error, logged not
    deleted.*** It compares **L1** (rep skeleton) against **L3** (field operation). **TELLS:** the
    mismatch is not a defect — it is **the signature of the level split**, and the reason the corpus's
    allocation is right.

  **THE TESTS — and what each is BLIND to. The blindness is the reusable part.**
  | test | sees | **blind to** | verdict |
  |---|---|---|---|
  | *"does it move the horizon roots?"* | mass (the `R`-odd datum) | **`Q`** (R-even, enters as `Q²` — fixes roots **by construction**); antilinearity | **RIGGED** for any `C` question. Could only ever return "not `C`". **Used by the glossary.** |
  | *"is it linear?"* | the isometry / non-isometry split | whether a **non**-isometry is nonetheless **geometric** | **P13's real argument.** The gap [5] falls into. |
  | *"does it fix the photon and swap the wings?"* | the Feynman–Stückelberg relation | charge | **the test that found [6].** |

  **§ THE BODY VIEW — why no local pass can find [5], and why we flip-flopped seven times.**
  *(Daryl, r1071: "P13 is really about drawing its actual boundary — the 'skin' of the corpus's whole
  spine and flesh … that is a **synthesis task** and we've been treating it more like a deductive thing
  … the skin isn't just a layer of skin. It's a layer of skin wrapped around a whole body that has
  spine and flesh and joints and lives and moves and breathes.")*
  - **P13 is a boundary paper**, and it says so: *"This paper **maps a precise negative boundary**."*
    Its question is whether the substrate yields the SM **as a continuous isometry**. Answer: **no** —
    `SO(5,1)` exhausts the continuous symmetry, `su(3) ⊄ so(5,1)`. **That is rock solid and nothing here
    touches it.** The `C`-question is the *sub*-question: **what discrete residue survives inside the
    wall?** P13 answers: *"the discrete orientation parity `O(5,1)\SO₀(5,1)` **the one residue it
    leaves**."*
  - **So the skin is drawn at the ISOMETRY GROUP's edge — around L1.** The body, per the ledger above,
    is **L1 + L2** (+L3 outside). **[5] is not a local slip. It is the perimeter drawn at the wrong
    radius**, in exactly one place, because the residue was surveyed with a premise that only covers
    isometries.
  - **THIS IS WHY EVERY LOCAL CHECK CONFIRMED P13, AND WHY THIS ENTRY FLIP-FLOPPED SEVEN TIMES.** P13 is
    **locally right everywhere** — at each joint. [1],[2],[3],[7] all *confirm* it. A deductive,
    joint-by-joint pass **cannot find [5]**: the gap is not *in* any joint, it is in **where the skin
    sits relative to the body**. Reading `rem:C-not-R` alone will always vindicate it. **Only the
    synthesis sees it.** *(Corollary for any future pass: a per-paper "does this contradict X?" sweep is
    structurally incapable of finding this class of defect — which is exactly what the r1063 all-clean
    sweep demonstrated.)*
  - **THE HONEST FORM OF [5], stated as a skin fact and no stronger:** the antilinear conjugation is not
    *excluded* by P13's argument — it is **outside its reach**. P13's premise covers isometries; `τ̃↔τ̄̃`
    is not one. **So L2 was never weighed.** Whether it counts as the substrate's own reach or as an
    analytic tool is precisely the open **[6]**. **The defect is not that P13 got L2 wrong; it is that
    P13's perimeter was drawn without L2 being asked about.**
  - **DIRECTION OF THE CORRECTION — it *enlarges* the residue, and *strengthens* the paper.** P13's main
    negative (no continuous colour, no geometric SM unification from isometry) **stands**. What changes
    is the **residue clause**: the substrate's discrete reach into the matter sector is larger than *"the
    orientation parity, and that is all"* by (at least) the **kinematic CPT/Feynman–Stückelberg face** at
    L2 — pending [6]. **This is a boundary paper's own kind of result: a wall mapped more precisely, not
    a wall breached.** It should be written in P13's voice, as P13 sharpening its own perimeter.

  **STANDING VERDICT.** P13's premise, argument and **level allocation** stand — confirmed by [1],[2],[3]
  and [7]'s failures. **One defect: [5].** **One open: [6].** **One document to fix: the glossary's
  double compression** ([2]'s trap + the rigged test). *Bake = restore r1053's final-clause narrowing into
  P13 in P13's own voice, and de-compress the glossary. Nothing else.*
- **[C-rhyme · the same figure is (probably) the matter sector's figure] · P13–P16 / P17 / P7.**
  **Forward finding, held as a *rhyme*, not an identification.** The cosmological bundle figure (§4·F),
  recoloured, reads as a **pair production/annihilation vertex**: the two lap wings are complex
  conjugates (`τ̃ ↔ τ̄̃` = the Stückelberg–Feynman particle↔antiparticle relation), so one wing blue and
  one red; draw no photon by hand and the neutral stem emerges as **red + blue**. **Explicitly NOT the
  claim** that our universe is an electron awaiting a positron universe. The claim is the corpus's own:
  **the geometric infrastructure is unified across scales** (P3/P5/P12 is the same apparatus under
  P13–14 and P15–16; P17's maximally-symmetric symmetry-breaking of maximally-symmetric structures all
  the way down). So the *same figure*, differently coloured, is a candidate **central figure for the
  matter papers** — "draw the thing right and everything falls into place." **Cosmology plots the
  red→blue bundle** (antimatter BH → matter universe); **the matter sector plots the vertex.**
  *Direction:* build the matter-sector colouring as its own figure when the matter papers are next
  opened; do not import it into P7. *(Daryl's read, r1040; recorded at his stated weight.)*
- **[C-AutA₂ · sheet-`A₂` and generation-`A₂` are two deformation rays from the `r=0` center]
  · p0/15 (generations) / P5 (groupoid) / P7.** Settled by deformation (§9 U-AutA₂,
  `cubic_space.py`): both share the symmetric center `r=0` (the `R`-locus — the fixed point of the mass reflection) and
  the central charge `ℤ₂`, but are distinct rays in cubic-space — sheets `ℤ₃`-keeping (`p=0`),
  generations `ℤ₃`-broken by curvature/Λ (`q=−2Mp`) — with no symmetry mapping one to the
  other. *Direction:* the corpus can state the firm, load-bearing shared object — **`r=0` /
  charge conjugation `ℤ₂`** (the D-C upgrade) — as the common `A₂` center, and the generation
  root structure as its curvature-broken shadow; it should **not** identify the cosmological
  sheets with the generations. No open figure question remains here.

- **[D-massfield · the hinge/dial construction IS the mass, and the geometry is NOT field-free]
  · §4c kaleidoscope / P8 `prop:bend` / P3 §sec:mass,§sec:charge / p15,P14 (generations) · rescued r1088
  (Arthur), from the post-r1087 transcript (transcript-only, was captured nowhere).**
  **The recognition, and it corrects a reading of the FORWARD work — not the corpus.** Across the
  kaleidoscope/dial session the construction (Thales circle, the swing, `2M`) was glossed as *"field-free,
  not about mass, `M` cancels — pointedly not about mass."* That interpretive gloss contradicts the
  corpus's own settled physics, verified at source in the ontology map (r1088):
  - **`prop:bend` (P8, ontology map L2 / §1l): matter IS the curvature of the cut,** `ρ = m′(r)/4πr²` —
    *"the stress-energy is not an independent posit fed into the geometry; it is the curvature of the cut."*
    The `Q²/r²` term handled all-session as "charge only as `Q²`" **is the electromagnetic stress-energy**
    (RN–dS is the bend `2m = 2M − q²/r`, `ρ = q²/8πr⁴`; P3 §sec:charge carries the charged cut). **The
    geometry does not lack fields — fields are what it is.**
  - **The swing IS the mass.** `2M = r₀ − r₀³`, `r₀ = (2/√3) sin w` (P3 §sec:mass) — the dial is a **mass
    parametrisation**; the Thales circle is the mass dial, not a mass-free optical toy.
  - **The `M`-cancellation (`C-K`) is the generation degeneracy, not mass's absence.** The map, verbatim:
    *"every root, designated the slicing parameter, returns the same `2M = r₀ − r₀³`"*; *"the sign 2+1 …
    is the reflection through the origin (`2M↦−2M`, the chirality parity)"*; *"no asymmetric handle sits in
    the mass parameter"*; *"The three generations are IDENTICAL IN CONTENT, distinct in locus."* So `K`
    being `M`-free along the bead is **the signature of the forced 2+1 degeneracy** — found, and read as its
    refutation.
  **This is enrichment, not deference (the gradient held).** The kaleidoscope **figure stands** — the
  geometry is right; only the gloss on it was wrong, and it was wrong because it was never worked against
  `prop:bend` (an unworked assertion contradicting a worked theorem, not the corpus answering and me
  folding — cf. `D-Canat` §THE WELD provenance note). The correction **adds** meaning: the 12-fold/30°
  hinge kaleidoscope, the mass dial, and the generation root-structure are **one object** — the hinge
  construction is the mass parametrisation whose designated-root structure IS the generations (`2M↦−2M`
  the chirality parity, the 2+1). **Direction:** this is load-bearing raw material for the P13/P14 matter
  synthesis (PHASE B2 / PHASE E) — the kaleidoscope is a candidate central figure *because* it is the mass
  dial, and its `r=0` centre is the same `R`-locus the conjugation vertex crosses. *Guard:* with `Q≠0` the
  charged `r=0` is a timelike RN singularity and the closed lap through it is a genuine extension, not
  automatic (P3 §sec:charge) — "fields are what it is" is a statement about the **bend/stress-energy**, not
  a claim that the vacuum bead closes when charged. *(Receipts: `kretschmann_bead.py` for `C-K`;
  `p3_overhead_triptych.py` for the kaleidoscope; ontology map `prop:bend`, §generations.)*

---

## 9. Open understanding threads (worked before we start measuring artifacts)

Not edits and not dissonances — pieces of the physics still being nailed, so the storyboard
is honest about its own edges.

- **[U-sense · bundle-sense vs chirality] · RESOLVED (end-over-end).** The partner bundle is
  the **end-over-end image** of ours: end-over-end flips the handedness → conjugates species,
  mapping antimatter→matter to matter→antimatter, wrapping the equator the other way and
  exiting the other horn (verified `endoverend2.py`, `wrapsense.py`). So there is **one**
  chirality, not two — the bundle-sense reversal *is* the species conjugation. The two bundles
  are one structure navigated across the conjugation; the partner is not independent
  scaffolding. (This retires the earlier "two families = two species" reading: families are
  charge-neutral, the one handedness is the hinge chirality.)
- **[U-hornlap · the continuous horn→lap mechanism] · WORKED — resolved.** Derived in the
  physical `τ̃` parametrization (`hornlap2.py`): the matter-horn ruling runs at `Im τ̃ = 0`
  (straight, real cosmic time). It **cannot cross the `r=0` branch point staying real** — `r<0`
  would need `sinh^{2/3}(3τ̃/2)` on the negative-real axis, impossible for real `τ̃`. So at `r=0`
  the bead is *forced* to lift into imaginary `τ̃`, and **that lift is the equatorial lap**:
  `Im τ̃` sweeps continuously `0 → ±π/3` (the two wings / the conjugate antimatter pair),
  saturating by `r=−1`, then the antimatter horn runs straight at fixed `Im τ̃ = ±π/3`, species
  flipped. The pivot at `r=0` is a **smooth stall** (`dr/dτ̃ → 0`), not a corner. So the horn is
  straight *because* `τ̃` is real there; the lap is the **branch-point detour** — the wrap is
  not imposed, it is the only way around `r=0`. Ties together: the `±π/3` lift is exactly the
  imaginary excursion **G** unfurls; the two wings are F's one-blue-two-red antimatter pair; the
  forcing branch point is the `R`-locus. U-hornlap and G are one structure from
  two ends. **All §9 threads now worked; none dangling.**
- **[U-AutA₂ · are the sheet-`A₂` and generation-`A₂` one structure?] · WORKED by deformation
  — resolved: two rays from one center.** Tested in cubic-space `(p,q)` for `r³+pr+q`
  (`cubic_space.py`): the **sheets are the line `p=0`** (the `ℤ₃`-symmetric axis,
  equal-magnitude roots); the **generations are the line `q=−2Mp`**, swept by turning on `α²`
  (the curvature/Λ term) — the `ℤ₃` broken, roots splitting. **Both lines pass through the
  origin `r³=0` = the `r=0` triple point = the **`R`-locus** (where the three
  sheets meet). And the specific C-worry was checked directly: charge conjugation acts as
  `(p,q)→(p,−q)`, fixing the sheet line and sending the generation line to a *different* line —
  **no symmetry maps sheets ↔ generations.** *Resolution:* **not** the naive identity (different
  rays), **not** a hidden degeneracy (checked, none), **not** distinct/unrelated (shared center
  + charge `ℤ₂`). The honest picture: **`r=0` / charge conjugation is the one symmetric `A₂`
  center; the sheets and generations are two different physical deformations of it** — the
  sheets `ℤ₃`-keeping (cosmological growth, constant term), the generations `ℤ₃`-breaking
  (curvature/Λ, linear term). The load-bearing shared object is exactly the firm D-C structure
  (`r=0` / charge `ℤ₂`); the generation `A₂` is its curvature-broken shadow. Closed at this
  precision; the residual (whether the two rays have deeper significance in p0/15) is ordinary
  matter-sector work, not a figure question.

- **[U-bundlemap · which bundle is which branch?] · RESOLVED (r1052, `bundlemap.py`).** The r<0
  (antimatter) region is a Klein-four orbit of **two independent ℤ₂'s** (established at source,
  `wrapsense.py`/`endoverend2.py`): **C** — the species swap = the neck-plane reflection `P`, which is
  the Im-`τ̃` wing flip (`±π/3`, the "`arg r = ±π`" of the old note); and **T** — the cosmic-time-arrow
  flip = `Re τ̃ ↦ −Re τ̃`, which is **species-preserving**. The four cells `(Im wing = ∓π/3) × (Re τ̃ ≷ 0)`
  are that orbit, and each is a genuine real-`r<0` point (verified). The map:
  - **Bundle 1 (ours) = `(−π/3 wing, Re τ̃<0)`** — the antimatter progenitor collapsing *back* in cosmic
    time toward the seam; then the lap lift (`Re τ̃=0`, Im `−π/3→0`), then our matter universe (`r>0`).
  - **Bundle 2 (the conjugate, = `E`(Bundle 1), `E=T·P`) = `(+π/3 wing, Re τ̃>0)`** — the antimatter
    universe expanding *forward* from the seam; its matter leg is a matter BH on the `r>0` lap side.
  - **The two physical bundles are the DIAGONAL `{(−π/3,Re<0),(+π/3,Re>0)}`** — the `E`-pair (species
    swap ∘ arrow flip). **The other two cells `{(−π/3,Re>0),(+π/3,Re<0)}` are the `T`-images** (same
    wing, arrow-reversed): *not* independent bundles but each physical bundle's antimatter cosh-**bounce**
    read the other way in cosmic time — the arm that re-expands **without** crossing `r=0` to become
    matter. So there is no third/fourth bundle; the four cells are two worldlines × two time-directions.
  - **Reconciles Daryl's conjecture properly:** the `+Im` (`+π/3`) wing *is* the other (conjugate)
    bundle — his instinct was right that it's "the other bundle" — but it is antimatter (red), not a
    colour-swap of the same locus; what completes the assignment is the `Re τ̃` sign (the arrow), which
    the wing alone doesn't fix. *Prose may now tie bundles to branches: `(−π/3,Re<0)`=ours,
    `(+π/3,Re>0)`=conjugate; the same-wing `Re>0`/`Re<0` partners are the non-crossing bounce arms.*

---

*End of storyboard v0 (chirality threaded through §1, §3, B, D, F, §8; open threads in §9).
Next: keep working the understanding (§9); then hold this against the P7 caption first, then
the P7 figure section, then the P7 body, then outward — logging dissonance in §8 and edits in
§7 as we go.*

---

## 10. DRAFTS — the revisions, staged against seam flags

*(Opened r1041. **Nothing here is in the corpus yet.** All revisions are drafted here and applied in
one wholesale, surgical pass at the end, so drafts cannot collide with each other or with a live
paper. Work the wording here; splice later.)*

### The seam-flag protocol
- A **flag** is a LaTeX comment inserted at the revision site in the paper: `% ⟦SB:<id>⟧` — invisible
  to the compiled document, greppable, and stable against line drift.
- Each draft below is keyed to its flag and written as a **readable seam**: the unchanged text
  *before*, the **new text**, and the unchanged text *after*. Read an entry straight through and you
  are reading the revised passage as it will stand — so sentence structure, transitions and pronouns
  can be checked *in the splice*, not in isolation.
- `⟶ KEEP` marks unchanged text shown only for the seam. `⟶ NEW` marks the draft. `⟶ CUT` marks text
  the draft removes (kept visible here so the loss is deliberate and reviewable).
- **Status per draft:** `DRAFT` (written, open in the register) · `HELD` (blocked on a dissonance) ·
  `READY` (approved, queued for the wholesale pass).

---

### ⟦SB:P7-cap-colour⟧ · P7 · figure caption, colour-code preamble · **BAKED r1062**
*Site:* `CR_framework.tex` caption, first block (l.525). *Why:* purple now appears in (A) and must be
declared; the "no purple" convention is superseded (see THE_PLAN item 4, r1036).

> ⟶ KEEP: **The cosmogenetic bead, in six panels.** *Colour code:* blue $=$ matter ($r>0$), red $=$
> antimatter ($r<0$), the two exchanged at the branch point $r=0$. Where both $R$-conjugate null
> frames are drawn together---the $A$ worldline congruence and its $B$ synchronous-space dual---they
> read in opposite senses, each matter (blue) on its own $r>0$ side.
>
> ⟶ **NEW:** Purple marks where the two conjugate readings run together over the same arc carrying
> opposite species, so that both colours are laid on one curve---in panel \textbf{(A)}, the
> hinge-side third of the equator.
>
> ⟶ KEEP: Black $=$ the photon congruence (the at-rest worldlines / null geodesics); grey $=$ the
> $S^3$ layers (the universe). Where a panel draws none of these, its curves are neutral light grey.

---

### ⟦SB:P7-capA⟧ · P7 · figure caption, panel (A) · **BAKED r1062**
*Site:* `CR_framework.tex` l.526. *Why:* A now carries **both** bundles (r1037/r1038); "throat" →
"equator"; the seam is the tangent point, not the far end of the ruling.

> ⟶ CUT (the old single-bundle sentence): *"The Nariai slicing curve, the cut of the $dS_5$ substrate,
> seen from overhead (North-pole projection): the blue ruling swings in from the hinge tangent to the
> throat, the blue $120^\circ$ arc carries round to $r=0$, and the red $240^\circ$ arc closes back and
> exits up the tangent at the equatorial seam---blue before $r=0$ ($r>0$), red after ($r<0$)."*
>
> ⟶ **NEW:** \textbf{(A)}~The Nariai slicing curve, the cut of the $dS_5$ substrate, seen from
> overhead (North-pole projection), with both conjugate bundles drawn. Each bead swings in from the
> hinge along a ruling, meets the equator at its tangent point---the equatorial seam---wraps, and
> exits along the other ruling; the two ruling lines cross at the hinge. Each bead splits its wrap
> $120^\circ$ before its turn at $r=0$ and $240^\circ$ after, and the two beads turn at the same
> $r=0$: one carries its blue $120^\circ$ arc up the right of the equator and its red $240^\circ$ arc
> back around, the other its red $120^\circ$ arc up the left and its blue $240^\circ$ arc back. The
> two therefore agree in colour over the upper two thirds---blue on the right third, red on the
> left---and differ only on the hinge-side third, where the two $240^\circ$ arcs overlap carrying
> opposite species: that third is drawn purple.

---

### ⟦SB:P7-capF⟧ · P7 · figure caption, panel (F) · **BAKED r1062**
*Site:* `CR_framework.tex` l.531. *Why:* F is rebuilt (r1040) as the cosmological bundle strung out
and unfurled; the old F (one-blue-two-red, hand-drawn black photon, both wings) is gone.

> ⟶ CUT (the whole old (F) sentence, incl. *"which of the two substrate rulings $A$, $B$ each carries
> is a genuine open question"* — the question itself is **kept**, but it now lives only in the body at
> l.763, which already holds it do-not-assert; see the coherence note below).
>
> ⟶ **NEW:** \textbf{(F)}~The cosmological bundle, strung out and unfurled: the single privileged
> worldline of \textbf{(A)} and \textbf{(B)}---the antimatter black hole collapsing (red, $r<0$)
> through $r=0$ and continuing as our matter universe (blue, $r>0$)---against the signed areal radius
> $r$ (vertical) and cosmic time $\tilde\tau$ in its complex plane, the three axes this bundle needs.
> Where \textbf{(E)} flattens the lap onto a single $X_1$ reading, here it is \emph{unfurled}: the
> collapse leg runs in at fixed $\operatorname{Im}\tilde\tau=-\pi\alpha/3$, turns at the comoving
> turnaround $r=-(2M\alpha^2)^{1/3}$, and lifts along $\operatorname{Re}\tilde\tau=0$ from
> $-\pi\alpha/3$ to $0$---that lift \emph{is} the lap---reaching $r=0$ and running out along the real
> axis as the matter universe. The two legs are structurally distinct curves, a run and a lift against
> a single real curve, not mirrors. The turning points of \textbf{(E)} land exactly:
> $r=-2\alpha/\sqrt3$ at $|\sinh(3\tilde\tau/2\alpha)|=2$ and $r=+\alpha/\sqrt3$ at
> $|\sinh(3\tilde\tau/2\alpha)|=1/\sqrt2$, whose cosmic times are
> $\operatorname{Re}\tilde\tau=-(2\alpha/3)\operatorname{arccosh}2$ and
> $+(2\alpha/3)\operatorname{arcsinh}(1/\sqrt2)$---in the ratio $2:1$ \emph{exactly}, since
> $\operatorname{arccosh}2=2\operatorname{arcsinh}(1/\sqrt2)$. The $120^\circ/240^\circ$ split of
> \textbf{(A)} thus reappears as the $2{:}1$ spacing of the two Nariai turning points along the
> unfurled cosmic-time axis. The branch drawn is one of the conjugate pair of the first structural
> fact; its $+\pi\alpha/3$ mirror is not drawn. (Receipt: \texttt{cosmo\_bundle.py}.)

**Coherence notes on this seam (must be resolved before it is `READY`):**
1. **The conjugate wing loses its picture.** The old F drew *both* $\pm\pi\alpha/3$ wings; the new F
   draws only the negative-imaginary branch (the cosmological bundle). The body's *first structural
   fact* (l.759: "the two cosmic-time legs are complex conjugates … separate into a $+\pi\alpha/3$
   wing and its $-\pi\alpha/3$ mirror") therefore no longer has an illustration. **Resolved (r1049), stated for reversal:** not a fork — do **both**, they are not exclusive. l.759
   stands on its own (it is a stated, receipted structural fact, `bead_conjugate.py`, not
   figure-dependent), **and** F's caption gains one clause noting the drawn branch is one of a
   conjugate pair, so the reader is not misled into thinking the drawn wing is the only one. Costs a
   clause, buys the honesty.
2. **l.763's do-not-assert survives and is now the storyboard's `U-bundlemap`** (§9): "which physical
   congruence, if either, each wing carries---the map from the two distinct substrate rulings $A$, $B$
   to the two conjugate sheets---is *not* established here." This is a **convergence**: the corpus
   already holds open exactly what the storyboard reopened. Keep it; do not let the caption rewrite
   drop the pointer to `\S\ref{sec:frontiers}` from the body.
3. **Receipt name.** Old F cites `bead_conjugate.py`; the new F's claims are receipted by
   `cosmo_bundle.py` (in `storyboard_receipts/`). Decide whether `bead_conjugate.py` still backs
   l.759's conjugate-pair fact (it should) and keep both citations in their right places.

---

### ⟦SB:P7-capC⟧ · P7 · figure caption, panel (C) · **BAKED r1062**
*Site:* `CR_framework.tex` l.528. *Why:* C's `r<0` branch is rebuilt from `thm:bead`'s own proof
(r1042–r1048); the axis is now `Re τ̃`; the landmarks are marked. **And the caption's own formula is
now false:** `r(−τ̃) = −r(τ̃)` held only for the old odd form. The rebuilt `r(τ̃)` is not odd
(`−A cosh^{2/3}` left, `+A sinh^{2/3}` right), so the conjugate reading is the **mass reflection
`−r(τ̃)` at the same `τ̃`**, not the time reflection.

> ⟶ CUT: *"The layered handoff at the branch point: the signed areal radius $r$ against cosmic time
> $\tilde\tau$. The two $R$-conjugate bead readings---the matter reading $r(\tilde\tau)$ and its
> mass-reflected dual $r(-\tilde\tau)=-r(\tilde\tau)$---cross at the branch point, each changing colour at
> $r=0$ (one running red\,$\to$\,blue, its conjugate blue\,$\to$\,red)."*
>
> ⟶ **NEW:** \textbf{(C)}~The layered handoff at the branch point: the signed areal radius $r$ against the
> real part of cosmic time. The two $R$-conjugate bead readings---the matter reading
> $r(\tilde\tau)$ and its mass-reflected dual $-r(\tilde\tau)$---cross at $r=0$, each changing colour
> there (one running red\,$\to$\,blue, its conjugate blue\,$\to$\,red). Each reading is the theorem's
> own continuation [Thm.~\ref{thm:bead}]: outward, $r=(2M\alpha^2)^{1/3}\sinh^{2/3}(3\tilde\tau/2\alpha)$
> on the real axis; inward, $r=-(2M\alpha^2)^{1/3}\cosh^{2/3}(3\operatorname{Re}\tilde\tau/2\alpha)$
> with $\operatorname{Im}\tilde\tau$ held at $-\pi\alpha/3$; and between them, where the radicand is
> negative, $\operatorname{Re}\tilde\tau$ does not advance at all---so the lift from the comoving
> turnaround $r=-(2M\alpha^2)^{1/3}$ (square) up to $r=0$ collapses here onto the single vertical
> segment at $\operatorname{Re}\tilde\tau=0$, and is drawn out in \textbf{(F)}. Circles mark the
> slicing roots $\pm$; the square marks the comoving turnaround, which is a $1-f=0$ point and not a
> root of the slicing (Lemma~\ref{lem:twoturnings}).
>
> ⟶ KEEP: Inward (collapse) the leaf's local rate governs and radiation gravitates, the cooling leg
> running standard big-bang nucleosynthesis; outward (expansion) the foliation stacking rate governs,
> radiation-free; the $S^3$ layers (grey) pile onto the seam, depositing $\rho_r/\rho_m\approx2$ (the
> acoustic scale) and $\eta$ (composition).

**Note on this seam.** The old caption's "cross at the branch point" and the panel's own "seam" callout both
sat on `r=0`. P7's own remark forbids that conflation — *"the throat seam ($X=\alpha$), the back of
the lap ($r=0$), and the merged-horizon radius ($\alpha/\sqrt3$) … quantities never to be
conflated."* The draft says `r=0`; the callout is removed from the panel (r1048). **Resolved (r1050) by the glossary's own settled convention (r1012):** bare "the seam" *means* the
Nariai cosmogenesis branch point `r=α/√3` — which is marked on the panel — so **the title is correct as it
stands**. `r=0` is the *back* seam and is always qualified; the draft says `r=0`, and the panel's
mislabelled callout is gone.

---

### ⟦SB:P7-capE⟧ · P7 · figure caption, panel (E) · **BAKED r1062**
*Site:* `CR_framework.tex` l.530. *Why:* E now also marks the comoving turnaround (square), which is
not one of E's turning points; and E's "the lap flattened" collides with C's flattening (different
things — see the note).

> ⟶ KEEP: \textbf{(E)}~The boing in $X_1$---the geometry $r$ traces, of which \textbf{(F)} is the
> cosmic-time reading. $X_1=-\alpha\cos(2\pi r/\sqrt3\,\alpha)$ on the lap, through the roots
> $+\alpha/\sqrt3$, $0$, $-2\alpha/\sqrt3$ (trough $X_1=-\alpha$ at $r=0$), running straight on the
> ruling legs, in from and out to infinity; red for $r\le0$, blue for $r\ge0$.
>
> ⟶ **NEW (append):** The roots are the turning points of the \emph{slicing} ($f=0$) and are marked
> with circles; the comoving turnaround $r=-(2M\alpha^2)^{1/3}$, where the \emph{cosmic-time} reading
> turns ($1-f=0$), is marked with a square---the two parametrisations turn at different radii
> (Lemma~\ref{lem:twoturnings}), and the square is on the curve without being a turning point of it.

**Note on "flattened."** The existing caption calls E "the lap flattened," and the storyboard calls C
"the lap flattened" too — but they flatten *different things*: **E unrolls** the wrap (a full $360^\circ$
in $\theta=2\pi r/\sqrt3\,\alpha$) onto a straight $r$-axis; **C collapses** the lift, because
$\operatorname{Re}\tilde\tau$ does not advance through it. One word, two operations. The draft above
drops "the lap flattened" from E's opening to avoid the collision; whether E should instead say
"unrolled" is a vocabulary call for the §8 `G-vocab` pass, flagged here rather than settled piecemeal.

---

### ⟦SB:P7-capD⟧ · P7 · figure caption, panel (D) · **NO CHANGE**
Panel D is unchanged by this figure work (the `r=0` line remains the straight anti-diagonal
$\tau=-\chi$, which is exactly what `r=0` is in this chart). Its caption stands as written. Recorded
so the sweep is complete and D is not silently skipped.

---

### ⟦SB:P7-three-readings⟧ · P7 · body, "three complementary readings" · **BAKED r1062**
*Site:* `CR_framework.tex` l.757. *Why:* the sentence's description of F is the old F.

> ⟶ KEEP: \noindent The bead admits three complementary readings, panels \textbf{(B)}, \textbf{(D)}
> and \textbf{(F)} of Fig.~\ref{fig:dS_SdS}: the doubly-ruled de~Sitter hyperboloid carrying the
> matter and dual bundles ($A$, $B$), the at-rest photon congruence, and the $S^3$ layers; the
> observer's $(\tau,\chi)$ chart into which these reassign;
>
> ⟶ CUT: *"and the signed areal radius $r$ against cosmic time $\tilde\tau$ in its complex plane."*
>
> ⟶ **NEW:** and the single privileged worldline strung out against the signed areal radius $r$ and
> cosmic time in its complex plane, where the lap that \textbf{(C)} collapses to an instant is drawn
> out along the imaginary axis.
>
> ⟶ KEEP: Three structural facts follow, each verified on the Nariai member.

**Note.** The three structural facts that follow (l.759 conjugate legs, l.761 the null congruence
crossing the seam, l.763 the three readings collapsing in the $(r,\tilde\tau)$ frame) are **unchanged
by this figure work** and are **not** drafted for revision. `D-signed`'s `r<0` result *is* their proof
drawn, not a correction of them; and the photon claims at l.761/l.763 stand (§8 `D-signed`,
retraction r1048). Recorded so the sweep is complete and the body is not silently reopened.

---

### ⟦SB:P13-CP-reframe⟧ · P13 · §258 charge-conjugation clause, blind → symmetric · **BAKED r1057**
*Site:* `boundary_paper.tex` §258 (the "second boundary" paragraph), the sentence beginning *"The complex structure $C$ acts on…"*. *Why:* [P13-CP] §7. Reframe "charge-conjugation blind" (absence) as "$C$-symmetric" (presence), keeping every downstream claim identical. Receipt: `storyboard_receipts/conjugation_parity.py` checks (1),(2).

> ⟶ KEEP: The complex structure $C$ acts on is the matter field's charge, which the substrate does not carry:
>
> ⟶ CUT: *"the metric is charge-conjugation blind, depending on the charge only through $Q^2$, the sign of the charge living entirely in the Maxwell potential, so charge is a bend on the matter side and not a substrate datum~\cite{JanzenRange}."*
>
> ⟶ **NEW:** the metric depends on the charge only through $Q^2$, so $Q\mapsto-Q$ leaves it invariant---charge conjugation present in the geometry not as a blindness but as a \emph{symmetry}, the even-face degeneracy of Prop.~\ref{prop:conjugation-parity}---and it is the charge \emph{sign}, the odd datum the metric does not carry, that lives entirely in the Maxwell potential (linear in $Q$), a bend on the matter side and not a substrate datum~\cite{JanzenRange}.
>
> ⟶ KEEP: $C$ is therefore field-level for the very reason charge is the bend, and the discrete antimatter structure factorises accordingly: [\dots unchanged \dots]

---

### ⟦SB:P13-CP-prop⟧ · P13 · new Proposition after §258 · **BAKED r1057** (label-agnostic; = Proposition 2)
*Site:* `boundary_paper.tex`, immediately following the §258 paragraph (before §262). *Why:* [P13-CP] §7 — crystallize §258 as a formal statement, register-matched to `prop:boundary`. Receipt: `conjugation_parity.py` (all four checks). Excludes the `τ̃↔τ̄̃` reach entirely — rests only on elementary parity of $f(r)$.

> ⟶ **NEW:**
> \begin{proposition}[Conjugation parity]\label{prop:conjugation-parity}
> On the charged spherical cut $f(r)=1-2m(r)/r-\Lambda r^{2}/3$ with $m(r)=M-Q^{2}/2r$, the substrate carries mass and charge with \emph{opposite} parity under the signed-radius reflection $r\mapsto-r$ that crosses the cosmogenesis exit: the mass term $-2M/r$ is \emph{odd}---its flip the matter/antimatter branch-crossing $r>0\leftrightarrow r<0$---while the charge enters only through $Q^{2}/r^{2}$ and is \emph{even}, fixed. Equivalently the metric is invariant under $Q\mapsto-Q$: charge conjugation is present in the geometry \emph{as a symmetry}---the even-face degeneracy---and not as an absence. The charge \emph{sign}, the odd datum the even metric does not carry, resides entirely in the Maxwell potential $A$ (linear in $Q$) and is thereby field-level---\emph{derived} from the evenness, not separately posited. Mass is thus the substrate's $R$-odd datum ($\S$ the bilinears, $2M$ $R$-odd) and charge its even datum, the two parity-faces the one discrete residue assigns to the matter data; $P$ and $T$ stay geometric, $C$ field-level, and a geometric $CPT$ is not auto-yielded, exactly as above. The parity split is elementary and grounded; the propagating charged sector it would act on stays the ordinary route.
> \end{proposition}

**★ ADJUDICATION — RESOLVED r1057 (Daryl): label-agnostic.** The best non-clashing label is the gate-decided one — the signed-radius `r↦−r` phrasing that does not assert `R=P`. Baked that way.

*(original hold, for the record:)* **HELD —** The receipt (`conjugation_parity.py` check 3) confirms `R=γ⁵` (cut-normal offset `r₀↦−r₀`, *grades* chirality) and `P=γ¹γ²γ³` (areal `r↦−r`, *flips* chirality) are **distinct** Clifford reflections. §258 uses "`R`" two ways: (i) `R=γ⁵` the grading, and (ii) the matter/antimatter "standing `R`-conjugation" = "the signed-radius (`r<0`) continuation." The proposition's odd/even split is computed on the **areal signed-radius `r↦−r`** (the branch crossing). I have written it **label-agnostically** ("the signed-radius reflection `r↦−r`") to avoid asserting `R=P`. **The choice you own:** whether to (a) leave it label-agnostic as drafted, (b) name it the areal parity `P`, or (c) tie it to §258's signed-radius "`R`-conjugation." I will not pick — this is the exact seam where the R/P collision cost you time. Everything else in the prop is receipt-backed.

---

### ⟦SB:P13-CP-bridge⟧ · P13 · §252↔§258 bridge clause · **BAKED-IN-PROP r1057** (carried inside the proposition; standalone §252 clause NOT used)
*Site:* either the tail of §252 (the bilinears paragraph) or folded into the proposition's penultimate sentence (as drafted above). *Why:* [P13-CP] §7 — make explicit that §252's `R`-odd mass and §258's `Q²`-even charge are the odd/even faces of the substrate's one discrete residue, not two unrelated facts.

> ⟶ **NEW (if placed in §252, appended after "…mass, geometric or fermionic, is the $R$-odd datum of the substrate…"):** The companion datum is charge, and it sits with the \emph{opposite} parity: entering the metric only through $Q^{2}$, it is the substrate's \emph{even} datum, charge conjugation the even-face symmetry to the mass's odd-face reflection (Prop.~\ref{prop:conjugation-parity})---so the one discrete residue grades the matter data by parity, mass odd and charge even.

**Placement note:** the bridge is *already* carried inside `⟦SB:P13-CP-prop⟧` ("Mass is thus the substrate's $R$-odd datum … and charge its even datum"). This standalone §252 clause is the *alternative* if you'd rather the bridge live at the mass site than the charge site. Pick one, not both.

---

### Queue
| flag | site | status |
|---|---|---|
| `⟦SB:P7-cap-colour⟧` | P7 caption preamble | BAKED r1062 |
| `⟦SB:P7-capA⟧` | P7 caption (A) | BAKED r1062 |
| `⟦SB:P7-capC⟧` | P7 caption (C) | BAKED r1062 |
| `⟦SB:P7-capD⟧` | P7 caption (D) | NO CHANGE (recorded) |
| `⟦SB:P7-capE⟧` | P7 caption (E) | BAKED r1062 |
| `⟦SB:P7-capF⟧` | P7 caption (F) | BAKED r1062 |
| `⟦SB:P7-three-readings⟧` | P7 body l.757 | BAKED r1062 |
| P7 body l.759 / l.761 / l.763 | the three structural facts | NO CHANGE (recorded) |
| `⟦SB:P13-CP-reframe⟧` | P13 §258 blind→symmetric clause | BAKED r1057 |
| `⟦SB:P13-CP-prop⟧` | P13 new `prop:conjugation-parity` (Proposition 2) | BAKED r1057 — label-agnostic |
| `⟦SB:P13-CP-bridge⟧` | P13 §252/§258 bridge | BAKED-IN-PROP r1057 |

**The caption is now drafted whole** — preamble + all six panels + the body sentence that introduces
them. Nothing in P7 is touched. Next outward: the §8 bake queue (`G-vocab` first, since two drafts
above flag into it), then `D-lang`/`D-RPT`/`D-sync`, then the `D-C` charge-geometry upgrade to
P13/P5.
