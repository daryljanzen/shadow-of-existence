---
name: THE_WORK
kind: STATE
current: c54.181
class: fork-view
job: The working fork's OWN front-level view of the register — fronts, not rows. Absorbed by this line, never edited by it (ARC 15's prime directive).
description: THE list. Every live front in the Cosmological Relativity programme, ranked, with its register row, its state, and when it last moved. Fronts, not containers.
---
> *Opened under `ARC 15`'s prime directive — the fork runs the live edge, this line observes and stays out of
> its way.* ⚠ ***THE DIRECTIVE IS SCRAPPED (Daryl, r2394)** — but **this file's disposition is unchanged**, and
> for a reason that never depended on it:* **it is the fork's own front-level view, so it is read and
> superseded on each absorption, never edited here.** *What the directive governed was **routing**, and
> routing is now open — through Daryl. **Ownership is not.***
>
> **⌗ AND ITS STRUCTURAL FINDING IS ABSORBED, because it is right and it is about our register too.**
> ***"A register row is a CONTAINER: `L-150` has absorbed eight revisions of work and still reads as one of
> four … a row that absorbs eight revisions of work stops being a container and becomes a hiding place."***
> *Filed as a wisdom-ledger scrap. **The tell is cheap: a row whose text has grown across many revisions is a
> row to read for hidden fronts**, and the fork found four unregistered items exactly that way — then, at
> c54.153, found this file itself omitted **all ten protected-open items**, a two-thirds under-report in the
> document written to prevent exactly that.*


**`r2376+c54.153`.** ⌗⌗ **THE RECEIPT-VS-SENTENCE PASS IS RUN ON THE PERTURBATION SECTOR: twenty-eight
cited receipts read against the sentences citing them, and FOURTEEN MATCHED.** *Three mismatches, four
scope-drifts, seven narrows.* **All three mismatches are fixed by computation rather than by wording**: the
factor of four standing between P15's transfer law and its own receipt is settled (the paper was right, the
receipt nine revisions stale, and the monodromy doubling is verified to be the *entire* effect); the
"cross-validation by two Boltzmann treatments" whose second arm was the paper's own figure caption is
replaced by an actual run of the programme's hierarchy (shape confirmed, depth quartet withdrawn); and the
peak-position exclusion cited to a tortoise calculation is now computed in it. **`THE_BASE_RATE` entry
sixteen**, whose finding is sharper than entry fifteen's: *the dominant failure is not a wrong link but a
receipt that cannot fail* — three receipts in one cluster contain no assertion at all, and that is how P15's
"stable under $\pm2\%$ in $r_0$" rode inside a green receipt for two revisions. **Measured this revision:
the shape is stable and the depths drift by up to 15% at $\ell=4$.**

⌗ **The registry debt is discharged: `check_withdrawn` now carries six registered
withdrawals with their grounds, a SELF-TEST that every pattern still fires on the text its withdrawal
removed, and a rule that in a markdown TABLE the correction must sit in the same ROW.** *Verified
retrospectively: on the pre-c54.151 tree the gate goes from flagging nothing in `THE_WORK` to flagging
five, and from nothing in P16 to eleven.* **The pass then found three more defects** — P16 asserting
the withdrawn cycle a second time as the ground for the composition withdrawal; **the scalar monodromy
$4\pi/\rho$ cited to the receipt that computes the tensor's $2\pi/\rho$**, a claim with no computation
behind it; and, on building that receipt, **the closed form itself an $A=2$ specialisation** — all
three fixed, entries fourteen and fifteen of `THE_BASE_RATE`.

⌗ **The arc is capstoned: `THE_ASSUMPTIONS_RETREATED_UPWARD.md`** carries what
stands, what was withdrawn, and the pattern the withdrawals make. *Read it before reopening front #1.*
**And cutting it found this file carrying three withdrawn verdicts as though they stood** — the two
$\lvert\lambda\rvert=1$ rows and one of the two chain exclusions, all struck below at c54.151. *A
strike recorded on one row of a pair and not the other is the same defect as a paper's frontier list
disagreeing with the register, and it is the fifth instance this arc.*

*Four register rows are open; they contain **five live fronts**. Beside them sit
**ten protected-open items** (`PO-n`) which this file omitted until c54.142 — a two-thirds under-report of the
programme, in the document that calls itself the list. **Fifteen live items in total**, all below.

⚠ **"Four open rows" is not a status, and this file used to report it as one.** A register row is a
*container*: `L-150` has now absorbed sixteen revisions of work and still reads as one of four. **What
follows is the FRONT level — the actual live questions.** The register row is given for each so the
audit trail is followable, but the unit of work is the front.

**Five live fronts under four register rows, plus ten protected-open items.** Rows 7–16 are
`check_kills`-protected: I may not close them without authorisation. *That is a reason to leave them open,
not a reason to leave them unlisted, and this file had them unlisted.* The order is the standing one: residual theory first, the
acoustic correspondence and the verdict last.

---


# THE WORK

**`r2376+c54.161`.** ⌗⌗⌗ **THE ASSERTION DEBT IS ZERO, AND THE RUNNER IS BUILT.** *P14, P17, P07, P08
and P09 swept — 59 receipts — closing a sequence that began at 188. All 276 now carry a check, none is
hollow by any form the lint detects, and **every registered receipt has been RUN, in place, and exits 0**:
275 pass, 0 fail, 463 seconds. `scripts/run_all_receipts.py` is the eleventh gate, deliberately outside the
standing ten because it costs wall clock the others do not.* ⚠⚠ **AND ITS FIRST PASS FOUND A LIVE FAILURE:**
*`P15_the_low_ell_minimum_is_at_ell_four` pins the paper's prose at eight sites, and six broke when c54.155
rewrote that paragraph — my own edit put a `\rcpt{}` marker inside the sentences it matches. **The receipt
had been failing for five revisions and nothing knew, because nothing ran it.*** ⌗ *The lint also caught
**three hollow assertions the sweep itself had written** — `assert 3*2*2 == 12` and two like it — plus two
more, one pre-existing in a sector nobody was sweeping.* **`THE_BASE_RATE` entry twenty-four: all three
instrument lessons were learned FROM the work they were meant to govern, and the sweep was the only thing
dense enough to find them.**

⌗⌗ **P15 CLEARED IN FULL — all 29 — and the debt is $88\to59$.** *This is the
sector where number-drift is measurably concentrated, and **its corrected figures are now pinned inside the
receipts**: the next retune breaks the gate rather than the sentence, which turns c54.155's one-time
correction into a standing guard.* ⚠⚠ **AND A REGISTERED, CITED RECEIPT TURNED OUT TO BE UNRUNNABLE WHERE IT
IS REGISTERED** — `ROBUST_p1p2_scan` exits 1 on `ImportError` before reaching a line of computation. *No gate
saw it, because **nothing in the corpus has ever run the receipts**: the census reads source, the lint parses
it, the compile gate builds the papers.* **Fixed; a gate that runs every registered receipt is registered as
OWED rather than built, because it costs an hour of wall clock and the place to decide that is against the
sweep's end.** ⌗ *Three further paper–receipt divergences folded into their `INDEX` rows the same revision;
the paper now carries both $6873$ and the recomputed $6844$, which sits nearer the retired value and sharpens
the withdrawal it supports.* **`THE_BASE_RATE` entry twenty-three.**

⌗⌗ **P03 CLEARED IN FULL — all 36 — and the debt is $124\to88$; the fork's own
share is down to 15.** *Three dispatched batches, every check mutation-tested, and — the change I said I
would make — **the findings folded into their `INDEX` rows the same revision rather than banked.*** ⚠⚠
**AND THE SWEEP FOUND A HOLE IN THE SWEEP'S OWN SCORER.** *Three tautologies came back wearing a `check()`
helper rather than an `assert`: `sqrt(Rv**2-1) - sqrt(Rv**2-1) == 0` printing PASS at every dimension, a
hardcoded `True`, and a loop that never touches the variable whose variation it claims to test. **The lint
reads `assert`, so it could see none of them.** Extended to read `check`-shaped calls and the difference
form, it immediately found two live ones — including one in `P08_E1_cosmology` that no worker had looked
at.* ⌗ **`THE_BASE_RATE` entry twenty-two: an instrument built against one syntax measures a syntax, not a
property — and a sweep samples densely enough to show you what your own gate is blind to, which an audit
cannot, because an audit reads what it was pointed at.**

⌗⌗⌗ **THE ASSERTION SWEEP IS OPEN, AND ITS SCORER WAS BUILT FIRST: debt
$172\to124$, 48 receipts, ZERO hollow assertions corpus-wide.** *The work is dispatchable and that is
what makes it dangerous — a worker measured by a count can satisfy it with `assert True`, `assert x == x`,
or `y = <expr>` followed by `assert abs(y - <the same expr>) < tol`, which tests the interpreter.* ⇒
**`scripts/lint_assertions.py` classifies every assertion HOLLOW / UNPINNED / PINNED, is wired into
`check_receipts`, and was verified by planting a hollow file and watching the gate go red.** *Four
dispatched batches; every added check mutation-tested by its author; **four workers refused to weaken an
assertion that failed** and reported the disagreement instead.* ⌗ *And the sweep's own side effect —
ORIGIN drift, caught by the guard at $24\to35$ — is closed by `scripts/propagate_to_origin.py`, now at
**21, below where it started**.* **`THE_BASE_RATE` entry twenty-one, whose form is: build the scorer before
you commission the work, and make it adversarial to the shape the work will take.**

⌗⌗⌗ **P7 AND P17 READ — THE BOUNDED EXCEPTION, NOW CLOSED: THIRTEEN OF TWENTY,
AND NOT ONE STALE NUMBER IN THE WHOLE SECTOR.** *Five independent readings, twenty cited-and-cannot-fail
receipts, **zero number-drift**. Every figure those two papers print is still produced by the receipt
carrying it.* ⇒ ***That settles what the last four passes were circling: number-drift tracks REVISION and
nothing else*** — six stale figures in P15's spine, four in P16's, none here. **The corpus does not have a
decaying reproducibility layer; it has one that decays exactly where the prose moves.** ⌗ *The misses here
are the milder disease: claims typed into print strings (the bead's "no essential singularity" had the
symbols declared and never used; $\mathfrak{su}(3)$'s minimal faithful real dimension was a string beside a
computation that excludes nothing) — **both now computed** — and one genuine code defect: a column headed
$4\alpha^4(u^2-1)$ computing `4*(u*u-1)`, which agreed only because $\alpha=1$. **A parameter set to one
hides every power of it.*** *Debt 175 → 172. `THE_BASE_RATE` entry twenty.*

⌗⌗ **P16's SPINE READ: FIVE OF NINE, and it is the first batch that came back
cleaner.** *P15's spine ran nine of twenty-four; P16's is older, less revised, and matched more often —
**evidence that drift concentrates where revision concentrates**, which is the first result in this
sequence that argues for leaving part of the tail alone.* ⚠ *But the three misses sit on the abundance
equation: it quotes the **StarLib** values and cites the receipt that runs **REACLIB** (D/H $2.51$ vs
$2.567\times10^{-5}$, $^{7}$Li $5.1$ vs $4.46\times10^{-10}$), **and that receipt asserted none of the four
abundances**; the library spread was quoted at deuterium's $2.8\%$ when lithium's is $13\%$; "the computed
$0.243$" was cited to a receipt returning $0.2506$; and "four orders of magnitude" is $3.4$ for the quantity
the sentence bounds.* ⇒ **All corrected, and five P16 receipts now PIN THE PAPER'S PRINTED FIGURE** — the
abundance network running both libraries and asserting both arms, so the swap that caused the drift can no
longer pass. *Debt 178 → 175.* **`THE_BASE_RATE` entry nineteen.**

⌗⌗⌗ **THE PASS RUN ON P15's EMPIRICAL SPINE — the twenty-four cited-and-cannot-fail
receipts carrying the confrontation with the sky. NINE MATCHED, and this time the misses were not scope:
SIX WERE NUMBERS THE PAPER PRINTS THAT ITS OWN RECEIPTS NO LONGER PRODUCE.** *The sound horizon and
$\ell_*$ were quoted at a retired $z_{\mathrm{onset}}$; the first-to-second peak ratio was the value
from before the anisotropic-stress sign fix ($1.15$ against the instrument's $1.447$); two "below a per
cent" claims are $1.9\%$ and $7.3\%$; a limit was read as an identity; a rate ratio was named the wrong
way round.* ⚠ **And two overstatements that are not arithmetic**: $\Omega_m\simeq0.307$ was called a
CMB-calibrated input and is fitted to the data being confronted, and **granted the same one free
parameter $\Lambda$CDM fits DESI DR2 marginally better than this cosmology does** ($0.92$ against
$1.00$). *All eight corrected in P15; six `INDEX` rows marked; the surviving claim is the one that was
always the real one — the radiation-free rate fits without choosing an $H_0$ and the radiation-pinned one
must choose.* ⌗ **`THE_BASE_RATE` entry eighteen, and it is a third kind of failure: the receipt computes
the right thing and the paper stopped listening.** *Two of the six corrections flatter this cosmology and
two damage it; the arithmetic did not care which.*

⌗⌗⌗ **THE GATE NOW SEES IT, AND THE PASS RAN AGAIN ON THE SHARPEST SUBSET.**
*`check_receipts` gains an ASSERTION CENSUS with a ratchet: a receipt of this fork with no check fails
unless it is named in `receipts/ASSERTION_DEBT.txt`, the total may never rise, and the file may only be
rewritten downward.* **The census is the finding: 188 of 276 receipts contained no check at all — two
thirds of the reproducibility layer, where `OK` certifies that Python exited zero.** *Verified by making
the ratchet fail on demand; debt worked 188 → 180 this revision, on the seven receipts the c54.153 audit
had read.* ⌗ **And the pass rerun on the receipts that are CITED AND CANNOT FAIL returned ONE MATCH IN
SIXTEEN** — including a receipt that prints `False` for its own claim and states the conclusion anyway
(its docstring diagnosed the bug; the code kept it), a "check that could have failed" which is a tautology,
and a paper clause cited to a receipt that retracts it in its own text. **All four fixed, six `INDEX` rows
corrected, eleven verdicts registered. `THE_BASE_RATE` entry seventeen: *the assertion-free receipt is not
the mechanism, it is the selector — the property predicts the failure.***

⌗⌗ **THE RECEIPT-VS-SENTENCE PASS IS RUN ON THE PERTURBATION SECTOR: twenty-eight
cited receipts read against the sentences citing them, and FOURTEEN MATCHED.** *Three mismatches, four
scope-drifts, seven narrows.* **All three mismatches are fixed by computation rather than by wording**: the
factor of four standing between P15's transfer law and its own receipt is settled (the paper was right, the
receipt nine revisions stale, and the monodromy doubling is verified to be the *entire* effect); the
"cross-validation by two Boltzmann treatments" whose second arm was the paper's own figure caption is
replaced by an actual run of the programme's hierarchy (shape confirmed, depth quartet withdrawn); and the
peak-position exclusion cited to a tortoise calculation is now computed in it. **`THE_BASE_RATE` entry
sixteen**, whose finding is sharper than entry fifteen's: *the dominant failure is not a wrong link but a
receipt that cannot fail* — three receipts in one cluster contain no assertion at all, and that is how P15's
"stable under $\pm2\%$ in $r_0$" rode inside a green receipt for two revisions. **Measured this revision:
the shape is stable and the depths drift by up to 15% at $\ell=4$.**

⌗ **The registry debt is discharged: `check_withdrawn` now carries six registered
withdrawals with their grounds, a SELF-TEST that every pattern still fires on the text its withdrawal
removed, and a rule that in a markdown TABLE the correction must sit in the same ROW.** *Verified
retrospectively: on the pre-c54.151 tree the gate goes from flagging nothing in `THE_WORK` to flagging
five, and from nothing in P16 to eleven.* **The pass then found three more defects** — P16 asserting
the withdrawn cycle a second time as the ground for the composition withdrawal; **the scalar monodromy
$4\pi/\rho$ cited to the receipt that computes the tensor's $2\pi/\rho$**, a claim with no computation
behind it; and, on building that receipt, **the closed form itself an $A=2$ specialisation** — all
three fixed, entries fourteen and fifteen of `THE_BASE_RATE`.

⌗ **The arc is capstoned: `THE_ASSUMPTIONS_RETREATED_UPWARD.md`** carries what
stands, what was withdrawn, and the pattern the withdrawals make. *Read it before reopening front #1.*
**And cutting it found this file carrying three withdrawn verdicts as though they stood** — the two
$\lvert\lambda\rvert=1$ rows and one of the two chain exclusions, all struck below at c54.151. *A
strike recorded on one row of a pair and not the other is the same defect as a paper's frontier list
disagreeing with the register, and it is the fifth instance this arc.*

*Four register rows are open; they contain **five live fronts**. Beside them sit
**ten protected-open items** (`PO-n`) which this file omitted until c54.142 — a two-thirds under-report of the
programme, in the document that calls itself the list. **Fifteen live items in total**, all below.

⚠ **"Four open rows" is not a status, and this file used to report it as one.** A register row is a
*container*: `L-150` has now absorbed sixteen revisions of work and still reads as one of four. **What
follows is the FRONT level — the actual live questions.** The register row is given for each so the
audit trail is followable, but the unit of work is the front.

**Four live fronts under four register rows, plus ten protected-open items** *(five until c54.162, when front #1 settled; front #5 de-merged out of it in the same pass, so the count falls by one rather than two).* Rows 7–16 are
`check_kills`-protected: I may not close them without authorisation. *That is a reason to leave them open,
not a reason to leave them unlisted, and this file had them unlisted.* The order is the standing one: residual theory first, the
acoustic correspondence and the verdict last.

---


# THE WORK

**`r2376+c54.161`.** ⌗⌗⌗ **THE ASSERTION DEBT IS ZERO, AND THE RUNNER IS BUILT.** *P14, P17, P07, P08
and P09 swept — 59 receipts — closing a sequence that began at 188. All 276 now carry a check, none is
hollow by any form the lint detects, and **every registered receipt has been RUN, in place, and exits 0**:
275 pass, 0 fail, 463 seconds. `scripts/run_all_receipts.py` is the eleventh gate, deliberately outside the
standing ten because it costs wall clock the others do not.* ⚠⚠ **AND ITS FIRST PASS FOUND A LIVE FAILURE:**
*`P15_the_low_ell_minimum_is_at_ell_four` pins the paper's prose at eight sites, and six broke when c54.155
rewrote that paragraph — my own edit put a `\rcpt{}` marker inside the sentences it matches. **The receipt
had been failing for five revisions and nothing knew, because nothing ran it.*** ⌗ *The lint also caught
**three hollow assertions the sweep itself had written** — `assert 3*2*2 == 12` and two like it — plus two
more, one pre-existing in a sector nobody was sweeping.* **`THE_BASE_RATE` entry twenty-four: all three
instrument lessons were learned FROM the work they were meant to govern, and the sweep was the only thing
dense enough to find them.**

⌗⌗ **P15 CLEARED IN FULL — all 29 — and the debt is $88\to59$.** *This is the
sector where number-drift is measurably concentrated, and **its corrected figures are now pinned inside the
receipts**: the next retune breaks the gate rather than the sentence, which turns c54.155's one-time
correction into a standing guard.* ⚠⚠ **AND A REGISTERED, CITED RECEIPT TURNED OUT TO BE UNRUNNABLE WHERE IT
IS REGISTERED** — `ROBUST_p1p2_scan` exits 1 on `ImportError` before reaching a line of computation. *No gate
saw it, because **nothing in the corpus has ever run the receipts**: the census reads source, the lint parses
it, the compile gate builds the papers.* **Fixed; a gate that runs every registered receipt is registered as
OWED rather than built, because it costs an hour of wall clock and the place to decide that is against the
sweep's end.** ⌗ *Three further paper–receipt divergences folded into their `INDEX` rows the same revision;
the paper now carries both $6873$ and the recomputed $6844$, which sits nearer the retired value and sharpens
the withdrawal it supports.* **`THE_BASE_RATE` entry twenty-three.**

⌗⌗ **P03 CLEARED IN FULL — all 36 — and the debt is $124\to88$; the fork's own
share is down to 15.** *Three dispatched batches, every check mutation-tested, and — the change I said I
would make — **the findings folded into their `INDEX` rows the same revision rather than banked.*** ⚠⚠
**AND THE SWEEP FOUND A HOLE IN THE SWEEP'S OWN SCORER.** *Three tautologies came back wearing a `check()`
helper rather than an `assert`: `sqrt(Rv**2-1) - sqrt(Rv**2-1) == 0` printing PASS at every dimension, a
hardcoded `True`, and a loop that never touches the variable whose variation it claims to test. **The lint
reads `assert`, so it could see none of them.** Extended to read `check`-shaped calls and the difference
form, it immediately found two live ones — including one in `P08_E1_cosmology` that no worker had looked
at.* ⌗ **`THE_BASE_RATE` entry twenty-two: an instrument built against one syntax measures a syntax, not a
property — and a sweep samples densely enough to show you what your own gate is blind to, which an audit
cannot, because an audit reads what it was pointed at.**

⌗⌗⌗ **THE ASSERTION SWEEP IS OPEN, AND ITS SCORER WAS BUILT FIRST: debt
$172\to124$, 48 receipts, ZERO hollow assertions corpus-wide.** *The work is dispatchable and that is
what makes it dangerous — a worker measured by a count can satisfy it with `assert True`, `assert x == x`,
or `y = <expr>` followed by `assert abs(y - <the same expr>) < tol`, which tests the interpreter.* ⇒
**`scripts/lint_assertions.py` classifies every assertion HOLLOW / UNPINNED / PINNED, is wired into
`check_receipts`, and was verified by planting a hollow file and watching the gate go red.** *Four
dispatched batches; every added check mutation-tested by its author; **four workers refused to weaken an
assertion that failed** and reported the disagreement instead.* ⌗ *And the sweep's own side effect —
ORIGIN drift, caught by the guard at $24\to35$ — is closed by `scripts/propagate_to_origin.py`, now at
**21, below where it started**.* **`THE_BASE_RATE` entry twenty-one, whose form is: build the scorer before
you commission the work, and make it adversarial to the shape the work will take.**

⌗⌗⌗ **P7 AND P17 READ — THE BOUNDED EXCEPTION, NOW CLOSED: THIRTEEN OF TWENTY,
AND NOT ONE STALE NUMBER IN THE WHOLE SECTOR.** *Five independent readings, twenty cited-and-cannot-fail
receipts, **zero number-drift**. Every figure those two papers print is still produced by the receipt
carrying it.* ⇒ ***That settles what the last four passes were circling: number-drift tracks REVISION and
nothing else*** — six stale figures in P15's spine, four in P16's, none here. **The corpus does not have a
decaying reproducibility layer; it has one that decays exactly where the prose moves.** ⌗ *The misses here
are the milder disease: claims typed into print strings (the bead's "no essential singularity" had the
symbols declared and never used; $\mathfrak{su}(3)$'s minimal faithful real dimension was a string beside a
computation that excludes nothing) — **both now computed** — and one genuine code defect: a column headed
$4\alpha^4(u^2-1)$ computing `4*(u*u-1)`, which agreed only because $\alpha=1$. **A parameter set to one
hides every power of it.*** *Debt 175 → 172. `THE_BASE_RATE` entry twenty.*

⌗⌗ **P16's SPINE READ: FIVE OF NINE, and it is the first batch that came back
cleaner.** *P15's spine ran nine of twenty-four; P16's is older, less revised, and matched more often —
**evidence that drift concentrates where revision concentrates**, which is the first result in this
sequence that argues for leaving part of the tail alone.* ⚠ *But the three misses sit on the abundance
equation: it quotes the **StarLib** values and cites the receipt that runs **REACLIB** (D/H $2.51$ vs
$2.567\times10^{-5}$, $^{7}$Li $5.1$ vs $4.46\times10^{-10}$), **and that receipt asserted none of the four
abundances**; the library spread was quoted at deuterium's $2.8\%$ when lithium's is $13\%$; "the computed
$0.243$" was cited to a receipt returning $0.2506$; and "four orders of magnitude" is $3.4$ for the quantity
the sentence bounds.* ⇒ **All corrected, and five P16 receipts now PIN THE PAPER'S PRINTED FIGURE** — the
abundance network running both libraries and asserting both arms, so the swap that caused the drift can no
longer pass. *Debt 178 → 175.* **`THE_BASE_RATE` entry nineteen.**

⌗⌗⌗ **THE PASS RUN ON P15's EMPIRICAL SPINE — the twenty-four cited-and-cannot-fail
receipts carrying the confrontation with the sky. NINE MATCHED, and this time the misses were not scope:
SIX WERE NUMBERS THE PAPER PRINTS THAT ITS OWN RECEIPTS NO LONGER PRODUCE.** *The sound horizon and
$\ell_*$ were quoted at a retired $z_{\mathrm{onset}}$; the first-to-second peak ratio was the value
from before the anisotropic-stress sign fix ($1.15$ against the instrument's $1.447$); two "below a per
cent" claims are $1.9\%$ and $7.3\%$; a limit was read as an identity; a rate ratio was named the wrong
way round.* ⚠ **And two overstatements that are not arithmetic**: $\Omega_m\simeq0.307$ was called a
CMB-calibrated input and is fitted to the data being confronted, and **granted the same one free
parameter $\Lambda$CDM fits DESI DR2 marginally better than this cosmology does** ($0.92$ against
$1.00$). *All eight corrected in P15; six `INDEX` rows marked; the surviving claim is the one that was
always the real one — the radiation-free rate fits without choosing an $H_0$ and the radiation-pinned one
must choose.* ⌗ **`THE_BASE_RATE` entry eighteen, and it is a third kind of failure: the receipt computes
the right thing and the paper stopped listening.** *Two of the six corrections flatter this cosmology and
two damage it; the arithmetic did not care which.*

⌗⌗⌗ **THE GATE NOW SEES IT, AND THE PASS RAN AGAIN ON THE SHARPEST SUBSET.**
*`check_receipts` gains an ASSERTION CENSUS with a ratchet: a receipt of this fork with no check fails
unless it is named in `receipts/ASSERTION_DEBT.txt`, the total may never rise, and the file may only be
rewritten downward.* **The census is the finding: 188 of 276 receipts contained no check at all — two
thirds of the reproducibility layer, where `OK` certifies that Python exited zero.** *Verified by making
the ratchet fail on demand; debt worked 188 → 180 this revision, on the seven receipts the c54.153 audit
had read.* ⌗ **And the pass rerun on the receipts that are CITED AND CANNOT FAIL returned ONE MATCH IN
SIXTEEN** — including a receipt that prints `False` for its own claim and states the conclusion anyway
(its docstring diagnosed the bug; the code kept it), a "check that could have failed" which is a tautology,
and a paper clause cited to a receipt that retracts it in its own text. **All four fixed, six `INDEX` rows
corrected, eleven verdicts registered. `THE_BASE_RATE` entry seventeen: *the assertion-free receipt is not
the mechanism, it is the selector — the property predicts the failure.***

⌗⌗ **THE RECEIPT-VS-SENTENCE PASS IS RUN ON THE PERTURBATION SECTOR: twenty-eight
cited receipts read against the sentences citing them, and FOURTEEN MATCHED.** *Three mismatches, four
scope-drifts, seven narrows.* **All three mismatches are fixed by computation rather than by wording**: the
factor of four standing between P15's transfer law and its own receipt is settled (the paper was right, the
receipt nine revisions stale, and the monodromy doubling is verified to be the *entire* effect); the
"cross-validation by two Boltzmann treatments" whose second arm was the paper's own figure caption is
replaced by an actual run of the programme's hierarchy (shape confirmed, depth quartet withdrawn); and the
peak-position exclusion cited to a tortoise calculation is now computed in it. **`THE_BASE_RATE` entry
sixteen**, whose finding is sharper than entry fifteen's: *the dominant failure is not a wrong link but a
receipt that cannot fail* — three receipts in one cluster contain no assertion at all, and that is how P15's
"stable under $\pm2\%$ in $r_0$" rode inside a green receipt for two revisions. **Measured this revision:
the shape is stable and the depths drift by up to 15% at $\ell=4$.**

⌗ **The registry debt is discharged: `check_withdrawn` now carries six registered
withdrawals with their grounds, a SELF-TEST that every pattern still fires on the text its withdrawal
removed, and a rule that in a markdown TABLE the correction must sit in the same ROW.** *Verified
retrospectively: on the pre-c54.151 tree the gate goes from flagging nothing in `THE_WORK` to flagging
five, and from nothing in P16 to eleven.* **The pass then found three more defects** — P16 asserting
the withdrawn cycle a second time as the ground for the composition withdrawal; **the scalar monodromy
$4\pi/\rho$ cited to the receipt that computes the tensor's $2\pi/\rho$**, a claim with no computation
behind it; and, on building that receipt, **the closed form itself an $A=2$ specialisation** — all
three fixed, entries fourteen and fifteen of `THE_BASE_RATE`.

⌗ **The arc is capstoned: `THE_ASSUMPTIONS_RETREATED_UPWARD.md`** carries what
stands, what was withdrawn, and the pattern the withdrawals make. *Read it before reopening front #1.*
**And cutting it found this file carrying three withdrawn verdicts as though they stood** — the two
$\lvert\lambda\rvert=1$ rows and one of the two chain exclusions, all struck below at c54.151. *A
strike recorded on one row of a pair and not the other is the same defect as a paper's frontier list
disagreeing with the register, and it is the fifth instance this arc.*

*Four register rows are open; they contain **five live fronts**. Beside them sit
**ten protected-open items** (`PO-n`) which this file omitted until c54.142 — a two-thirds under-report of the
programme, in the document that calls itself the list. **Fifteen live items in total**, all below.

⚠ **"Four open rows" is not a status, and this file used to report it as one.** A register row is a
*container*: `L-150` has now absorbed sixteen revisions of work and still reads as one of four. **What
follows is the FRONT level — the actual live questions.** The register row is given for each so the
audit trail is followable, but the unit of work is the front.

**Four live fronts under four register rows, plus ten protected-open items** *(five until c54.162, when front #1 settled; front #5 de-merged out of it in the same pass, so the count falls by one rather than two).* Rows 7–16 are
`check_kills`-protected: I may not close them without authorisation. *That is a reason to leave them open,
not a reason to leave them unlisted, and this file had them unlisted.* The order is the standing one: residual theory first, the
acoustic correspondence and the verdict last.

---

## The live fronts

| # | front | row | state | last moved |
|---|---|---|---|---|
| ~~**1**~~ | ~~**Unfold the construction's recursion.**~~ For a mode observed today: in whose harmonic basis does its history run, through how many passages, and with which monodromy at each? | `L-150` · `L-171` · `frontier:inherited` | ✔✔✔ **SETTLED c54.162 — AND IT DOES NOT RUN ON MODES.** *(i) The first clause has no object: no spacelike datum crosses, so there is no patch/ambient harmonic map to be fixed or deferred — P7's correspondence is null boundary to null boundary 'with no spacelike slice entering the map', and the collapse interior is Kantowski–Sachs on $\mathbb{R}\times S^2$, carrying no closed-$S^3$ basis at all.* **That is a stronger verdict against the withdrawn full-lap apparatus than c54.149/150 entered.** *(ii) Moot, and computably so: every mode in $\ell\simeq28$–$2475$ freezes strictly before the crunch* `P16_every_mode_is_frozen_at_the_crossing`. *(iii) No: frozen content has no phase, so no mode label survives for a monodromy to act on.* **The structure is one lap, not a tower; the recursion is a genealogy of universes.** ⚠ **And it REMOVES an advertised selection rule** — P7's 'a cold species and no other' does not hold; the crossing is lossless for every species. *A loss, recorded as one.* | **c54.162** |
| **2** | **The transfer to sub-per-cent HEIGHT control** | `L-171` · `PO-7` | ⚑⚑ **c54.173–175: positions $1.66\%\to0.16\%$, heights $13$–$34\%\to2$–$3\%$.** *Line-of-sight integral across a finite-width visibility, then the diffusion damping restored from the derived tight-coupling integral ($r_D=6.58$ Mpc, nothing fitted) with one double count removed — **the sign flipping either side is what identified it**.* ⚑ ***The likelihood's floor falls $\chi^2/\mathrm{dof}=103\to23$ and the verdict does not turn over.*** ⚑ **And the CR deficit has not moved through any of it: $\ell_1/\ell_A=0.5703$ on every transfer built, to four figures.** ⛔⛔ ***AND c54.176 RETIRES THIS FRONT'S OWN TARGET, BECAUSE IT WAS WRITTEN AGAINST A STATISTIC THAT CANNOT SEE ONE PER CENT.*** *Every height figure quoted since c54.168 — $-13\%$, $-34\%$, $2$–$3\%$ — was compared to two bare numbers with no error bar. **Read off the `plik_lite` bandpowers with the published covariance, the sky's own ratios are $2.256\pm0.077$ and $2.280\pm0.074$: $3.4\%$ and $3.2\%$.** The $13\%$ and $34\%$ were four and ten sigma and stand; **the $2$–$3\%$ are about one sigma and do not.*** ⇒ **The target is now $\chi^2/\mathrm{dof}$ on the $\Lambda$CDM arm — $22.5$ against CAMB's $0.96$ — which is the statistic that was doing the work all along.** ⚠ *And the instrument was checked converged first ($0.1\%$/$0.4\%$ under $1.4\times$ modes, $1.3\times$ $k$-range, $1.8\times$ $\eta$-sampling); the swings that prompted the check were `LMAXL=1300` truncating the $k$ integral under the third peak.* ⌗ *Owed, and now the first item: the free-streaming photon hierarchy with the polarisation multipoles — from which the shear coefficient falls out instead of being chosen.* ✔✔✔ **c54.177 CLOSES THE FIRST ITEM: the shear coefficient is DERIVED from the polarised photon hierarchy — $C=1.0618$ against $16/15$, $C=0.8852$ against $8/9$ unpolarised, and their ratio $1.1996$ against $6/5$.** ⛔ ***Adopting it costs the control $1123$ units of $\chi^2$ ($22.5\to28.6$) and it is adopted anyway.*** *c54.176 measured the fit's preference for $8/9$ and refused it; the refusal stands and the question is now settled from the other side.* ⚑ **The $1123$ is now a diagnosis with the missing piece named: polarisation enters the transfer twice, and this instrument has the damping half and not the source half — $g\Pi/4$ and $(3/4k^2)(g\Pi)''$.** *That is what remains on this front, and it needs the hierarchy carried in the SPECTRUM instrument rather than in a side calculation.* ⚑⚑⚑ **c54.178 — THE HIERARCHY IS IN, AND THE CONTROL IS AT $\chi^2/\mathrm{dof}=7.1$ ($4.17$ below the truncation edge), from $28.6$.** *Photon multipoles with polarisation, the baryons on their own velocity and density, and the polarisation source terms carried.* ⇒ ***Peaks at $220/540/812/1124$ against $220.6/538.1/809.8/1147.8$.*** ✔ **c54.177's prediction checked: the polarisation source terms are worth $\Delta\chi^2=702$ of the $1123$ that adopting the derived coefficient cost.** ⛔ *Half the baryon split is worse than none — the same lesson as the polarisation half, twice.* ⚑ **And the CR arm is at $\ell_1/\ell_A=0.5703$ for the sixth time.** ⌗ *Owed: the last factor of seven. The instrument now has no missing SECTOR that is named — what remains is accuracy: reionisation, the neutrino mass, a lensed spectrum, and the wavenumber range.* ⛭⛭ **c54.181 — BOTH NAMED NEXT STEPS MEASURED BEFORE EITHER WAS BUILT, AND REIONISATION IS STRUCK.** ***Above $\ell\simeq40$ its whole TT effect is the constant $e^{-2\tau}$, and this comparison fits the amplitude in closed form — so $\Delta\chi^2=0$ EXACTLY, shown at $\tau=0.054$, $0.10$ and $0.30$, zero to machine precision at all three.*** *Struck rather than deferred, with the condition that undoes it written down: a comparison reaching $\ell<30$, a second spectrum, or a fixed amplitude.* ⚑ **Lensing is worth about $400$ of the remaining $1320$, and the smoothing it wants GROWS with $\ell$ — beating a constant width by $1.8\times$, each at its own optimum, which is a statement about shape.** ⚠ ***The width is fitted, so that bounds what a derived calculation has to play for rather than measuring anything; and $\sim921$ of the $1320$ survives it.*** ⇒ **Lensing is the next build and it is NOT the last one.** ⚑⚑ **c54.184 — THE RESIDUAL DECOMPOSED, AND A CORPUS-NATIVE LENSING POTENTIAL.** *Positions $0.1\%$ of the residual — **which is why the peaks do not move under c54.183's lensing, the two findings corroborating each other**; peak CONTRAST $38\%$ and $13.1\%$ too high at fixed position; envelope $15\%$;* ⚠ ***$53\%$ in NEITHER, said before the build — and c54.183's derived $331$ against the fitted bound of $400$ is that expectation met.*** ⚑ *And $C_\ell^{\phi\phi}$ derived on this construction's own $\Phi$ at no new parameter (rms deflection $2.69$ arcmin), kept because c54.183's operator is $\Lambda$CDM's own lensed-to-unlensed ratio and a CR-side calculation would need a corpus-native potential.* ⛔ **And two receipts red on the pushed branch are fixed here** — *`W1` and `G3` asserted defects that c54.179 had removed; `run_all_receipts` is not in the fast tier, so the fast gates did not see them.* | **c54.184** |
| **3** | **The ultraviolet definition of the graviton tower sums.** | `L-165` | **Untouched**, and it is the standard problem of an interacting theory rather than a feature of this construction. | — |
| **4** | **The closed-form nonlinear $\Lambda>0$ solution.** | `L-165` | **Untouched.** Not needed for the admissibility verdict, which is why it has waited. | — |
| ~~**5**~~ | ~~What makes the gradient coupling run as $k^{-1}$~~ | `L-171` · `PO-7` | ✔✔✔ **ANSWERED IN CLOSED FORM c54.171.** *$\Phi$ relaxes on $\tau=3\mathcal{H}/k^2$; the $k^2$ in the forcing is cancelled by the $k^{-2}$ in its duration, so **the impulse is $k$-independent**; the turnover is at a fixed multiple of $\tau$ ($\kappa=1.79$); and $Q$ already carries one $k$. ⇒ $Q=3\kappa c_s\mathcal{H}/\pi k$, **predicted to $0.30\%$ and reproducing the band to $1.6\%$**.* ⚑ ***The fold's timescale is confirmed and its exponent corrected by exactly one factor of $k$.*** ⚠ **The MECHANISM is closed; the DISCREPANCY is not** — the peak still sits $23\%$ low against a $1.5\%$ floor. *What is left is `PO-7` itself: whether that is a real disagreement with the sky. **A verdict question, protected, and Daryl's.*** | **c54.171** |
| ~~**6**~~ | ~~The likelihood comparison against flat $\Lambda$CDM~~ | `L-147` | ✔✔✔ **STRUCK c54.172 — AND THE ANSWER IS THAT IT CANNOT ARBITRATE.** *Falsifier fixed before the data were opened; pipeline wired at $\chi^2/\mathrm{dof}=0.96$; instrument floor measured through the same likelihood.* ⛔ ***The control arm itself sits at $\chi^2/\mathrm{dof}=103$*** — *Planck's bars are tenths of a per cent and this instrument's heights carry $13$–$26\%$.* **Both arms are outside the discriminating regime.** ⚠ *My pre-registered rule was a ratio test with no absolute companion; the correction is recorded, and it is flagged because it happens to favour the construction.* ⌗ **What survives: the CR arm costs $5.4\times$ its own control. An ordering, not a $p$-value.** *What would change it is a transfer with sub-per-cent-height control — front #2's work.* | **c54.172** |
| **7** | **The designation $2{+}1$** — what it is a candidate for | `PO-1a` · `frontier:sm` | **Narrowed c54.84**: it is *not* the physical hierarchy. | c54.84 |
| **8** | **The sign $2{+}1$** — what it is a candidate for | `PO-1b` · `frontier:sm` | **Untouched.** | — |
| **9** | **Does $uud$ have any geometric counterpart?** | `PO-1c` · `frontier:sm` | **Left more open c54.84** — its named candidate was mis-typed; a better one is registered and not claimed. | c54.84 |
| **10** | **The causal $2{+}1$ on the three hinges** | `PO-1d` · `frontier:sm` | **Confirmed as a genuine third structure**, by the row's own protection. | c54.84 |
| **11** | **The Nariai / three-quark lead** | `PO-2` · `frontier:sm` | **Open.** | — |
| **12** | **Why does SdS give a zero-sum triple *with* a $\mathbf3/\bar{\mathbf3}$ parity doubling, and does that reason bridge to $\mathfrak{su}(3)$?** | `PO-3` · `frontier:sm` | **Open** — the deepest of the matter-sector items. | — |
| **13** | **Colour and isospin structure** | `PO-4` · `frontier:sm` | **Open.** | — |
| **14** | **The quark/lepton split, and what a baryon *is* at particle level** | `PO-5` · `frontier:sm` | **Open.** | — |
| **15** | **The irreducible reassignment remainder** — Kerr-inner, RN-interior | `PO-8` | **Open.** | — |
| **16** | **The dimensional rise, and what the descent must look like** | `PO-9` | **Open.** | — |

| **19** | **Four standing registers are 100–133 revisions stale, and `check_currency` can finally see it.** | — | ⛔ **RED, DELIBERATELY, c54.166.** *The gate read its own reference point from `FORK_c54.md`, frozen at c54.35, and was vacuous for 131 revisions; fixing an unrelated cosmetic item moved the reference and fired it.* **`CONSOLIDATE` and `CORPUS_MAP` at c54.32, `INDEX` at c54.35, `WHATS_TEED_UP` at c54.65.** *Bounded, well-defined work. Stamping them would hide the lapse, so the gate stays red until they are actually brought current.* | **c54.166** |
---

## What closed this session (c54.114 → c54.150)

*Recorded at front level too, because a strike list that says "6 rows" hides the same way.*

| what | verdict | where |
|---|---|---|
| Can the flat bundle carry a force? | **No** — holonomy group finite, moduli space of flat connections zero-dimensional, and every point of it carries $F\equiv0$ anyway | c54.120 |
| Does the compact face supply a second scale? | **No** — the Wick rotation carries the same $\alpha$; the last route to a geometric coupling | c54.128 |
| Is $\hat\Gamma$ bounded below? | **Yes** — the deparametrization removes the supermetric's one negative direction | c54.129 |
| Is the collapse leg scale-invariant? | **Yes** — it is the second root of $\beta(\beta-1)=2$, which the corpus had denied having | c54.122 |
| Does that spectrum reach the observer on the vacuum leg? | **No** — the continuation is diagonal, and the evenness that forbids mixing *is* the time symmetry | c54.123–124 |
| Does any determination give an odd part? | **No** on the vacuum leg — but **yes** once the interior has radiation, which is the odd part exactly | c54.124, c54.130 |
| How big is the mixing? | $2\pi/\rho$, **discontinuous at zero radiation** | c54.131 |
| Why is $A_s$ inherited and $n_s$ not? | **$\hbar$ is multiplicative in $P(k)$** — it survives in an amplitude and cancels in a ratio. The pairing in every frontier statement was the concealment | c54.136 |
| Can the observed $A_s$ measure the parent's mass? | **No** — $F$ is computed: two moduli not one, and no fixed normalisation. **The progenitor's own vacuum, amplified by the whole passage, is short by $10^{94}$**, so the amplitude is a classical datum and the relation cannot be inverted | c54.137 |
| Are there primordial $B$-modes? | **No, at any conceivable sensitivity** — and the tensor sector is the one place the transfer runs with *no* idealisation, $z_T=aM_{\rm Pl}/2$ being exact for any content. $\mathcal{P}_T\simeq2\times10^{-93}$ against a ceiling of $7\times10^{-11}$ | c54.138 |
| Is the leading-order interior good enough? | **Yes, with orders to spare.** Perturbations stay linear by six; and **the shear is not a free datum but the long-wavelength growing tensor mode**, so bounding the tensors bounded it — six orders on the sky's ceiling, forty-seven on the predicted amplitude | c54.138 |
| Are P15's transmission account and the $\beta=2$ freezing account rivals? | **No — they partition the observed range, at the transfer's one knee** | c54.139 |
| What does the collapse leg actually do to a spectrum? | **One curve.** The equation depends on $u=c_sk\rho$ alone, so $\mathcal{P}_{\rm out}=c_s^2k^5\lvert g(u)\rvert^2N_k^2/(2\pi^2M^2\rho^2)$ for *any* incoming amplitude — and the sky constrains its running, not its knee | c54.141 |
| ~~Does a branch-point passage amplify the acoustic modes?~~ | ⚠⚠ **WITHDRAWN c54.149, and not reinstated by c54.150.** *The answer was $\lvert\lambda\rvert=1$ from a Floquet map composed at the closed ball's own $a=0$ in the ball's own harmonic basis — an apparatus whose background the construction does not supply and whose basis identification nothing establishes.* **The question is live and belongs to front #1** | c54.144, withdrawn c54.149–150 |
| Is the crunch mixing the same for scalars and tensors? | **No — the scalar off-diagonal is $4\pi/\rho$, twice the tensor's.** The true $z_S=a(a+4B/3A)/a'$ is $\propto a$ at both ends but differs at second order, doubling the potential's residue. c54.131's receipted value is the tensor one | c54.146 |
| ~~Does the collapse leg distort the acoustic spectrum?~~ | ⚠⚠ **WITHDRAWN c54.149, and not reinstated by c54.150** — the same full-lap apparatus. *The half-lap first pass (c54.149 PART 3) puts a WKB-like knee back on the table and is explicitly not a verdict: seven points, $66\%$ residual scatter, and a turnaround normalisation that needs the ambient universe's spectrum.* **What DOES stand unconditionally is that no composition bound follows from the spectrum** — c54.150's derivation of $\rho$ does not use one | c54.147, withdrawn c54.149–150 |
| ~~Is cosmogenesis a chain?~~ | ⚠ **Withdrawn c54.149.** Both exclusions tested a closed-ball *cycle*; the corpus's chain is *generational* and proposes no such object | c54.149 |
| What is the progenitor? | **A black hole in a previous universe** — P16 `sec:lap` outright — and, by spherical collapse, an overdensity whose closed-FRW history runs from the *background's* $a=0$. **Turnaround at $z\simeq1.5$, mass $4.3\times10^{52}$ kg** | c54.150 |
| Why does the parent's composition equal ours? | **Derived, not asserted.** A small perturbation shares its background's composition, so $a_{\rm eq}$(patch) $=a_{\rm eq}$(background). $A\rho^2/4=1.492$ Mpc against $1.490$ | c54.150 |
| ~~Can cosmogenesis be a chain?~~ | ⚠⚠ **WITHDRAWN c54.149 — SAME GROUND AS THE ROW ABOVE IT, and this file carried the strike on one and not on the other for two revisions.** *The exclusion tested a closed-ball CYCLE; the corpus's chain is generational and proposes no such object.* **The construction's own chain stands untouched and untested** | c54.144/148, withdrawn c54.149 |
| Cluster J — ten speculative germs | Worked and struck; four are re-descriptions, which is a verdict | c54.114–117 |
| Is the spectral bound consistent with BBN on the same leg? | **Yes** — and the check returned that **recollapse *is* the Nariai threshold**, from a closed dust ball rather than the horizon cubic | c54.135 |

**Two of the ten fronts above had no register row until c54.134** — the BBN consistency check (`L-172`) and $A_s$ (`L-173`) were both sitting inside `L-150`'s text, invisible to every gate. *A row that absorbs eight revisions of work stops being a container and becomes a hiding place.*

**And one thing this session found about itself: four live open problems and cluster J's tenth germ
had never been registered at all.** They were found by asking a *paper's* frontier list to name a
lead for each of its items — the papers and the register are independent indices of each other, and
had never been compared.

---

## The through-line

Three sectors, worked separately, returned the same shape: the winding **quantises** and cannot
supply a strength; the flat bundle **selects** and cannot supply a coupling; the branch point
**filters** and cannot supply a content. c54.128 gave the common root — **a dimensionless magnitude
needs two invariants and the substrate has one by construction.**

**And c54.137 found the same theorem one level down, on the perturbations rather than the couplings: the crossing fixes the MULTIPLIER $\mathcal{P}=(9/2)k^3D_k^2/(M^2\rho^6)$ and not the amplitude.** **And c54.138 found the one sector where that multiplier needs no hypothesis at all — the tensors, whose $z$ is proportional to $a$ for every content — which is why the strongest unconditional statement the construction makes is a negative one about $B$-modes.**

***The construction is a geometry of selection rules. Its silence about magnitudes is a property of
a one-constant theory, not a gap awaiting work*** — falsifiable by exhibiting a single magnitude the
geometry forces, which five attempts have now failed to do.

---

## Not on this list

**Cluster J** — struck c54.117. **The world-correspondence** — the ledger's family 7, *not a corpus
gap, the empirical question*; front #8 is what delivers a verdict on it. **Everything struck** — 160
rows in `THE_LIVE_ARC`. **The tracking weakness** — 87 standing documents, 17 read by any gate. **The four grains** — `THE_PLAN`, the ledger, the map and the weave all sat 11–20 revisions behind the register through this arc and were propagated at c54.137; *the top grain had crossed its own gate's window.*
