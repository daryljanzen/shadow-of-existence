# Bundle r1088 — the tidal shift: the gradient turned, stages 0–6 baked, and three of this session's own claims killed at source

The c40 transcript (r1064–r1087) harvested, its receipts re-run, and the **tidal shift** worked to
Daryl's stated gradient (*not outright inconsistent → compliant → consistent → coherent → cohesive →
thorough & fulsome*, r1066). **Stages 0–3, 5, 6 baked. Stage 4 dissolved.** Every paper touched
compiles clean. **Route: `retired/TIDAL_SHIFT_PLAN.md`. Harvest: `C40_HARVEST_r1064-r1087.md`.**

## The headline

**Six of the eight receipts written this session returned against something I had said** — the `R`
implementation, the `F` parity, the III.1 headline, the su(3) "reason," "no shared mechanism," "three
costumes." Each was cheap, each was mine, none needed a tribunal. **The one gate I proposed could only
have killed** (below).

## Stage 0 — the method layer (foundation-first; no physics)
- **`THE_CODA` §"The negatives are the map"** *(new)* — **a face that is NOT operative somewhere is as
  load-bearing as one that is.** The coda's three routing rules (retrieval / decisions / when-the-source-
  answers-back) all assume **the finding survives**; this is the rule for the paths that die. Worked case:
  the `C ≠ R` computation **withdrawn as a wrong turn when it was the paper's own conclusion** — proof
  thrown away because it arrived wearing an objection's clothes. And the harder half: **you keep them even
  when they turn out wrong** — a dead branch's job is not to be true, it is to mark where the wall is.
- **`THE_CODA` §"The source answers back"** — **the two kinds of reversal.** Read alone, the gradient rule
  hardens into *never change your mind*. Discriminator: **could re-reading have supplied it?** Deference →
  revert. A physical input neither document contained → bake.
- **`THE_METHOD` §4b** *(new; added as **4b**, no renumber — `THE_SIFT_AND_SORT_PLAN` cites §6, the kickoff
  §4)* — **the perimeter defect**: a boundary at the wrong radius, every local statement correct, the paper
  *locally right everywhere*. **A joint-by-joint pass returns clean and the clean is honest — that is the
  trap.** The r1063 null was **the method reporting on itself**. Binds dispatch: **synthesis is never
  farmed.**
- **`CODA_FIELD_NOTE` face 29** — the gradient's **other** failure, both poles in two consecutive turns:
  the corpus was **right** about the `Q²/r²` catch (fold — the forward doc misread what it was holding),
  and the recoil overshot one turn later. **An over-correction arrives wearing contrition.** *Run the
  receipt before the second sentence.*
- **`THE_PLAN` banner r1011 → r1088** (76 revisions stale; oriented a fresh node to the wrong edge).
- **`KICKOFF_ARTHUR`** — the dispatch bar, and **"a clean sweep is a verdict"** given its discriminator
  without being broken: *a sweep that could have returned work and didn't is a verdict; one that could not
  is the tell.* **Name the falsifying finding before the sweep.**

## Stage 1 — both r1069 AUDIT FLAGs DISCHARGED
- **P13 `rem:C-not-R`:** premise narrowed to what it carries — *"every substrate **isometry** is real and
  linear"* (was *"every geometric reflection"* — **that** was the overdraw). Linearity restored as **the**
  argument; the root test demoted to corroboration **and marked rigged in the paper** (`Q` enters as `Q²` ⟹
  fixes the roots *by construction*). *"Only that even face"* restored.
- **P13 `rem:C-scope`** *(new)*: **not-an-isometry ≠ not-geometric.** *"The perimeter this paper draws is
  drawn at the isometry group's edge; the complex-analytic level was never weighed against it."* Residue
  **enlarges**; wall stands. **[6] settled not at all** → A7.
- **Map §0 `C`** de-compressed; the rigged test now carries **"Never run it as a `C` discriminator."**
- Flag boxes removed; provenance preserved as a `%` source comment (invisible in the PDF, permanent in the
  `.tex`).

## Stage 2 — the weld (Daryl's physical input, r1073; not deference)
- **P3 `rem:charge-scope`** *(new)*: `A_t = Q/r` is **`R`-odd** ⟹ `A↦−A`, `F↦−F` — **the electromagnetic
  field reverses.** *"`Q²/r²` is reading-swap-neutral"* is exact; *"the swap touches the charge not at
  all"* is exact **of the metric** and false of the potential. **Both blind tests recorded** so they are
  not run as discriminators: the roots, and now `F·F` (even, quadratic).
- **P13 `rem:C-linear-face`** *(new)*: **species and charge are one binary**; neutrality is the weld's
  degenerate case, *not an exception*. `R` and `C` agree on every label they both carry; differ **only in
  character**. `rem:C-not-R` survives **by linearity** while concealing that **`R` is `C`'s linear face**.
  **What is corrected is a reason, not a result.** Factorisation → A7, not claimed.
- **Restraint:** **P5 and P9 needed nothing** — both already scoped to *"the term"* / *"the metric"* and
  both already locate the sign in the potential. The plan named four targets; the source said two.

## Stage 3 — the uncontested physics (P7, 41pp, unchanged)
- **`rem:M-scale`** *(new)*: **`K(τ̃) = (12/α⁴)(sinh⁻⁴(3τ̃/2α) + 2)`** — `M` cancels identically. **Every
  progenitor, whatever its mass, meets the identical curvature at the identical cosmic time.** **M sets
  the bead's scale, not its shape.** Scope: `M≠0` gates whether there is a bead at all.
- **`rem:two-thirds`** *(new)*: the **2/3 does three jobs and they are one job** — the vertical tangent
  (`s′^{−1/3}`), the singularity (`s′^{−4}`), the M-cancellation (`6×⅓=2`).
- **Not baked, refuted:** *"the M-cancellation **is** the generation result"* — different quantifiers
  (three roots at fixed `M` vs `M` at fixed `w`), both verified separately.

## Stage 4 — DISSOLVED. The gate was a cyanide pill; the claim was wrong.
Daryl: *"refuter Excalibur is a cyanide pill… Are you looking to kill all the development before it can
get off the ground?"* **He was right, and three rules baked the same day forbade it:** a refuter set to
default-to-refuted, aimed at a **synthesis**, can kill and **structurally cannot confirm** (§4b: synthesis
is not dispatchable) — a rigged discriminator *"pointed at the gallows instead of the throne."*
**The instrument actually owed was a source question — *does `thm:bead` already know this?* — and it
killed the claim in ten minutes:** P7 **never claims `C¹`** (it claims *continuity* of the curve and
*smoothness of the substrate*), and **P7's own proof already derives that the lap requires imaginary
time**. **P7 gets nothing at this stage. It was right.** → `CODA_FIELD_NOTE` **face 30**: the kill switch
wearing *procedure*, and **the over-reach inside a synthesis** — *the synthesis was real; the promotion
was not.*

## Stage 5 — the kept negatives (0.1's own rule, applied)
- **su(3) identification DEAD, with its cause.** The **2:1 is FORCED by equilateral tangency** — the
  incircle touches at the **midpoints**, at **half** the circumradius, rotated **60°**: **the rotation and
  the ratio are one fact.** And the union is a **hexagram**, not a hexagon; su(3)'s two hexagons are
  **both equiradial**. *Better than a live coincidence.*
- **The 30°, WALKED** — *"arriving from pure combinatorics"* is **wrong**, and it cuts both ways. **Both
  30°s are `360/(3×4)` with the same 3** (more than credited); **the fours are different animals** — the
  dial's is forced (a sine's quarter period), the kaleidoscope's is **chosen** (every `n` coprime to 3
  closes evenly). **It rhymes on its 3 and coincides on its 4.** ★ **Named, unrun:** can the Thales
  vertical tangent at the hinge derive the quarter turn? If so the two 30°s become one.
- **III.3, WALKED** → **two 2s, not three.** `(r−ρ)²(r+2ρ)` hands you the third root **and** the constant
  term in one stroke, so **the seam `α/√3`, the backward root `−2α/√3`, the turnaround `∛2·α/√3`, and the
  120:240 split are ONE factorisation** — and **`Mα² = ρ³` falling out of `q = 2ρ³` IS the Nariai
  condition: Nariai is what the double root *means*.** The lift's 1:2 does **not** merge (a `sinh` fact;
  `lem:twoturnings` already says why).
- **III.1 demoted** at §4b, the inflation sited where it grew. **The entry stands:** `180 mod 120 = 60` ⟹
  **the sheets are not the bead's home**; *one shadow forbids the very property the other exhibits* — A7's
  actual warrant.

## Stage 6 — the plan re-cut
- **★ A4.10 · P13's positive closure** — Lane 3, **UNGATED**, Arthur-held, **not dispatchable**. The
  destination (Daryl, r1071). The banner had said it was *"on no lane below"*; it is now on one.
- **★ A7 · The full analytic object (`ℂ_r × ℂ_τ̃`)** *(new; the plan had no such item)*. ⚠ **SYMBOL
  COLLISION NAMED:** the storyboard's **"A3"** means **A7**; the plan's `A3` is the **quantum completion**.
  Every deep bead-open was funnelling to a label that meant something else. Now sinks: `[6*]`,
  `species=sign(r)` vs FS, the C-factorisation, **A2.8 as a piece of it**, the origin gate-count,
  sheets-not-the-bead's-home, and the `A = ∛2·ρ` bridge.
- **A4.10 is NOT gated on A7** — it *states* the perimeter and builds forward, and **names** A7 as the open
  edge. That is what a boundary paper does.
- **How this plan may be run** (binding) written into the plan.

## New receipts (8; all re-run clean)
`su3_check.py` · `hexagon_check.py` · `M_cancellation.py` · `smoothness.py` · `weld.py` ·
`thm_bead_source_check.py` · `thirty_degrees.py` · `one_to_two.py` — in `storyboard_receipts/` (30 total).
**`weld.py` carries two of my own errors logged, not swept** — `[N1]` `R` applied as a double substitution
(the corpus was right: `f_M(−r) = f_{−M}(+r)`, **one map two readings**, which is the *proof* of "one ℤ₂,
four faces"); `[N3]` `F`'s parity read off a component without its basis. Both kept because the next
instance will reach for the same shortcut.

## Compile state
P13 16pp · P3 34pp · P7 41pp (unchanged) · P5 23pp · P9 12pp — **all zero undefined refs, zero errors.**

## Standing / next
**Stage 7 — A4.10, P13's positive closure.** The instrument is now honest: flags discharged, weld baked,
the perimeter drawn **and known to be drawn at the isometry group's edge**, the su(3) resemblance with a
cause of death, the 30° with an unrun question instead of a shrug. **Built whole, then found wrong at
source in the specific places it is wrong** — not gated, not farmed, not defended. **Stage 8** (the
fulsome enrichment) last, and run so it *can* return work.
