---
kind: FORWARD
---

> **▣ FORWARD — ahead of the corpus by construction, and exempt from the staleness test.** *It carries
> that banner in its own voice.* ⛔ ***Do not date it against the corpus and do not retire it for
> disagreeing with one*** *— where it disagrees with a paper, the disagreement is the work product. A
> header-versus-body contradiction inside it is still fair game; a comparison to the corpus is not.*

<!-- FORWARD WORKING DOCUMENT of the CR programme. Created from the c40 transcript
     (r1064–r1087) + the receipts on disk, read and re-run rather than summarised. -->

> # ⛔ THE GRADIENT APPLIES TO THIS DOCUMENT
>
> **This is a forward document. It is ahead of the corpus by construction. That is its job.**
> Where it disagrees with a paper, the glossary, `THE_PLAN`, or the ontology map, **the
> disagreement is the work product** — a finding to work, not a defect to fix. **"Stale" is
> a word for the corpus, never for the instrument examining it.** (`THE_CODA.md` §"The source
> answers back"; `CODA_FIELD_NOTE.md` addendum r1066; `SYNTHESIS_FIGURE_STORYBOARD.md` banner.)
>
> **Everything below marked ✔ was re-run from the receipt on disk, or computed fresh here.**
> Nothing is taken on the thread's word, including the thread's corrections of itself.
> New receipts written for this harvest: `su3_check.py`, `hexagon_check.py`,
> `M_cancellation.py`, `smoothness.py`.

# The c40 Harvest — what r1064–r1087 actually produced

**Premise (Daryl, this session):** *"a lot of it matters much more than was recognised at the
time and certainly more than the corpus currently recognises."*

**Verdict after working it: yes, and the gap is bigger than that.** The thread produced one
first-rank *method* result, one first-rank *physics* result that nobody in the thread stated,
and a fistful of findings whose weight was mis-set in **both directions** — some under-read
(thrown away as errors), some over-read (grabbed at in the recoil from being caught). The
corpus currently holds almost none of it, and `THE_PLAN`'s CURRENT STATE banner is at **r1011**
— seventy-six revisions behind the live edge, and unaware that the storyboard phase, the
C-anatomy ledger, the conjecture, and the kaleidoscope exist at all.

---

## 0. The shape of the thread, in one paragraph

It opens on a figure request (flatten F's complex `τ̃` to arc length) and ends in the `su(3)`
weight diagram. In between: a mislabel is caught and corrected; the corrections are then found
to be *the failure mode* rather than the fix; the gradient is named and inverted; the
"withdrawn" results are found to be **confirmations of P13 that had been deleted as errors**;
the body/skin reframing makes the real defect *sayable* for the first time; and the geometric
work resumes on a completely different footing — the conjugation rule, the two faces, the
hexagon, the kaleidoscope. **The thread's most valuable product is not any single finding. It
is the discovery of the class of defect that no local pass can find, and the demonstration
that the corpus had been enforcing it.**

---

# PART I — The method findings (the thread's biggest product, and the corpus holds them thinnest)

## I.1 ✔ The load-bearing negative — Daryl's principle, not in the coda

> *"You keep closing things off and just essentially deleting the negatives from consideration
> … you DO have to know what's operative and what is not operative in all instances to be able
> to fully know the thing."* — Daryl, r1070

The instance's own reading of what this exposed is exact and should be quoted into the record
verbatim, because it is the cleanest statement of the failure anywhere in the corpus:

> *"Every 'withdrawal' I made deleted a load-bearing negative — and several of them were
> confirmations of P13 that I threw away as errors. … I have been walking the maze recording
> only the turns that worked, so I keep re-entering the same traps."*

**The positive principle, stated:** *in a structure with several faces at several levels, a
face that is NOT operative somewhere is as load-bearing as one that is — that is how you know
which face you are looking at.*

**Where it is:** `SYNTHESIS_FIGURE_STORYBOARD.md` §8 `D-Canat`, as that entry's own operating rule.
**Where it is not:** `THE_CODA.md`, `THE_METHOD.md`, `CODA_FIELD_NOTE.md`. The field note's
r1066 addendum records the *gradient* half; it does not record this half.

**Why it belongs in the coda and not only the storyboard.** The coda's §"The standard,
mechanically" governs *retrieval* (map vs territory). The r929 keystone governs *decisions*
(route them to the source). The r1066 addendum governs *what happens when the source answers
back*. **This is the fourth and it is missing: what happens to the paths that don't pan out.**
The three existing rules all assume the finding survives. This one is about the ones that
don't — and the thread demonstrates, three times over, that discarding them is what produced
the flip-flop. **The negatives are not waste; they are the map of the maze.**

*Call, stated for reversal: this is a coda-level addition, not a storyboard-local rule. It is a
foundation fix, so by §"The order of work — foundation first, top-down" it goes before the
physics bakes.*

## I.2 ✔ The body view — why a joint-by-joint pass structurally cannot find this defect

> *"The skin isn't just a layer of skin. It's a layer of skin wrapped around a whole body that
> has spine and flesh and joints and lives and moves and breathes … looking at individual
> joints in the skin as a fabric layer while ignoring what part of the body that is on … is
> what has been biting us in the ass."* — Daryl, r1071

The mechanism the reframing exposed, and it is a **general result about method**, not a remark
about P13:

- **P13 is locally right everywhere.** Branches [1], [2], [3], [7] of the C-anatomy maze each
  *confirm* it.
- Therefore **a joint-by-joint pass cannot find the defect.** The gap is not *in* any joint —
  it is in **where the perimeter sits relative to the body**.
- Therefore reading `rem:C-not-R` alone **will always vindicate it**. Seven flips, each honest,
  each confirming the paper.
- Therefore the r1063 "all eleven papers clean" sweep **was not evidence of coherence — it was
  evidence of the method**. A per-paper *"does this contradict X?"* pass is **structurally
  incapable** of finding a perimeter drawn at the wrong radius. **The null result was the
  method reporting on itself.**

**This is the deepest thing in the thread and it is not in `THE_METHOD.md`.** THE_METHOD carries
the lens/ledger, the 8-step unit, both-ways closure, the three passes, and (r929) the
operable-question check. All of those are *local* instruments. **None of them can find a
perimeter defect, and this thread proves it by having run them and returned clean.**

*Call, stated for reversal: `THE_METHOD` needs a named class — the **perimeter defect** — with
its diagnostic (*if every local check confirms and the dissonance persists, the defect is not
in a joint; ask where the boundary sits relative to the whole*) and its honest admission that
the method's existing instruments cannot reach it. This is P6-adjacent: it is a statement about
what a deductive pass can and cannot establish.*

## I.3 ✔ Two kinds of reversal — the distinction that keeps the gradient rule honest

The ledger's own provenance note, at `§ THE WELD`:

> *"This reversal is **not** deference (the earlier ones were: the corpus answered and I
> folded). It is a **physical input the corpus does not contain** — no amount of re-reading
> would have supplied it. Logged so the two kinds are never conflated again."*

**This is load-bearing and easy to lose.** Without it, the r1066 gradient rule ("never make the
forward document compliant") reads as *never change your mind*, which is the opposite of the
way. The distinction:

| | trigger | verdict |
|---|---|---|
| **Deference** | the corpus answered and I folded | the failure mode. Revert. |
| **Genuine input** | a physical fact neither document contained (here: *"species and charge are one binary"*) | the engine working. Bake. |

The discriminator is **could re-reading have supplied it?** If yes, and you folded, that's
deference. If no, it's interference received at weight — the engine's own definition
(`THE_INTERFERENCE_ENGINE` §1: *what is generative is the timing and the reception, not the
quality of the source*).

*Call: this belongs beside the r1066 addendum in `CODA_FIELD_NOTE.md`. The gradient rule
without it is a rule against being corrected.*

## I.4 ✔ The weld — Daryl's physical input, and it corrects the ledger's two proudest entries

> *"You flip an electron's charge and you get a … positron. Of course the two are wedded
> problems. Species and charge are the same thing, are they not?"* — Daryl, r1073

**They are, and the geometry printed it.** Re-run of `charge_geometry.py` ✔:

```
mass term -2M/r  : r->-r sends -0.3529 -> +0.3529   (ODD: flips)
charge term Q²/r²: r->-r sends +0.0865 -> +0.0865   (EVEN: fixed)
metric under Q->-Q: +0.0865 -> +0.0865  INVARIANT (Q²)  => blind to charge SIGN
potential A_t=Q/r under Q->-Q: +0.2941 -> -0.2941  (ODD)  => the sign lives in A
```

The corrections the weld forces, both of which the ledger had backwards:

- **[1] `C ≠ R` because `R` is mass-odd and `C` mass-even — WRONG.** `|2M|` is the physical
  mass and **`R` preserves it**; only the *sign* flips, and the sign **is the relational branch
  label** — exactly what `C` flips. *A label was compared to a mass.*
- **[2] `Q↦−Q` fixes species — WRONG.** `Q↦−Q` **is** the species flip. `species = sign(r)` was
  deployed to deny it: **the geometric label used against the physical identity it exists to
  encode.**
- **And the corpus's stated reason is metric-only.** *"Charge is the `R`-even datum"* is **true
  of the metric, false of the potential.** `R` does not fail to touch the charge — under `R`
  the metric is invariant but `A_t = Q/r` is **odd**.

**The result: `R` is `C`'s LINEAR FACE.** `R` and `C` agree on *every label* — species flips,
charge flips, physical mass preserved. They differ **only in character** (linear vs antilinear).
So `rem:C-not-R`'s conclusion survives **via linearity** (always its real argument), while
*"`C` is not `R`"* is true and **conceals that `R` is part of `C`**.

**→ `C` = (linear face `R`) ∘ (antilinear face `τ̃↔τ̄̃`), both geometric.** That is r1053's
factorisation, restored, and it is the live physics. **→ A3.**

---

# PART II — The physics, receipts re-run

## II.1 ✔ The antilinear involution is real, geometric, and P13 does not reach it

`storyboard_receipts/charge_geometry.py`, re-run clean:

```
photon (real axis)  τ̃=(+0.70,+0.00π/3)  r=+0.85  | conj-> Im=-0.00π/3   FIXED
wing -π/3           τ̃=(-0.90,-1.00π/3)  r=-1.18  | conj-> Im=+1.00π/3   swapped
wing +π/3           τ̃=(-0.90,+1.00π/3)  r=-1.18  | conj-> Im=-1.00π/3   swapped
LINEAR?  False      ANTILINEAR?  True
```

**Fixes the neutral, swaps the wings, antilinear.** That is `C`'s exact signature, sourced by
the substrate's own complex-analytic structure. It is **not an isometry** — P13 exactly right —
and it is **geometric anyway**.

**The standing defect [5], in its honest form:** the antilinear conjugation is **not excluded**
by P13's argument; it is **outside its reach**. P13's premise covers isometries. `τ̃↔τ̄̃` isn't
one. **L2 was never weighed.** The perimeter was drawn without the question being asked.

**Direction:** the correction **enlarges the residue** and **strengthens the paper**. P13's main
negative — no continuous colour, `su(3) ⊄ so(5,1)`, no geometric SM unification from isometry —
**stands untouched.** What changes is the *residue clause*. **A wall mapped more precisely, not
a wall breached** — which is a boundary paper's own kind of result.

**Status of the retirement:** the refinement was marked **superseded at r1060 by the very
remark it refines**, without being answered; **un-retired r1067**. Two AUDIT FLAGs now stand
open in source (P13's `rem:C-not-R` box; the map's `R,P,T` glossary entry). **They are correct
and they should be discharged, not carried.**

## II.2 ✔ The M-cancellation — and BOTH readings the thread gave it are wrong

`storyboard_receipts/kretschmann_bead.py` re-run ✔ — the fact is solid:

```
K along the bead = 12*(2 + sinh(w)^-4)/α^4     <- no M
 2M=0.0020  w=0.3: r=0.0570  K=1419.456451
 2M=0.3849  w=0.3: r=0.3293  K=1419.456451
 2M=10.0000 w=0.3: r=0.9752  K=1419.456451
```

Identical `K` at fixed `w` across **four orders of magnitude in M**, while `r` itself ranges
0.06→2.40.

**Reading 1 (r1065): *"even the singularity doesn't know about M — M is a stupid number."***
Overshoots. `M ≠ 0` is what makes the bead **exist**: at `M=0`, `r ≡ 0` and `K = 24/α⁴`
everywhere — regular, no bead, no singularity. ✔ (`M_cancellation.py` (C))

**Reading 2 (r1087, the thread's last message): *"that's the corpus's generation result — every
root returns the same 2M. I found its signature and read it as its refutation."*** **This does
not follow, and it should not be baked.** ✔ (`M_cancellation.py` (B)) The two statements
quantify over different things:

| statement | quantifies over | verified |
|---|---|---|
| *every root returns the same `2M`* | the **three roots** of **one** cubic, at **fixed** M | ✔ `2M=0.3849 → roots (−1.1547, +0.5777, +0.5770) → r₀−r₀³ = 0.3849, 0.3849, 0.3849` |
| *`K` is M-free along the bead* | **M**, at **fixed** `w` | ✔ above |

Neither implies the other. **Reading 2 is the flip-flop running the other way** — an
over-correction produced one turn after being caught, wearing contrition's clothes. The catch
that provoked it (below) is **correct and stands**; the generation identification tacked onto
it does not.

**The precise statement both readings were reaching past:**

> **M sets the SCALE of the bead and not its SHAPE. Along cosmic time the curvature profile
> `K(τ̃)` is UNIVERSAL — every progenitor, whatever its mass, meets the identical curvature at
> the identical cosmic time.**

That is a genuine, sharp, un-held result. It is the exact companion to P3's *"M is a turning
point, not a coefficient"* and P8's *"the mass is the cut's offset"* — but **sharper than
either**, because it is a statement about the whole bead in the observer's own clock.

## II.3 ★ The 2/3 exponent does three jobs, and they are one job — NEW

✔ (`M_cancellation.py` (D)). The thread has these as **three separate notes**. They are one:

| job | mechanism | where the thread has it |
|---|---|---|
| the **vertical tangent** at `r=0` | `r ~ A(3s′/2)^{2/3}` ⟹ `dr/ds ~ s′^{−1/3}` | r1065's F′ note |
| the **singularity's strength** | `K ~ s′^{−4}` | r1065's C-K entry |
| the **M-cancellation** | `6 × (1/3) = 2` exactly ⟹ `M²/r⁶ ~ M⁰` | r1065's C-K entry |

**All three are the same exponent read three ways.** The `2/3` of the `sinh^{2/3}` law is doing
the vertical tangent, the singularity, and the M-independence **at once**. The `1/3` in the
amplitude `r ∝ M^{1/3}` is what eats the `M²`. Not a conspiracy — one line of dimensional
analysis on the bead.

*This is the unification the thread wrote three notes around and never drew.*

## II.4 ✔ The catch that provoked Reading 2 is right and is the thread's most useful correction

Daryl's catch at r1087, on the instance's *"there's no field, no potential, and M cancels out —
whatever this is, it is pointedly not about mass"*:

> **The `Q²/r²` term is the electromagnetic field's stress-energy.** It is `prop:bend`'s own
> worked example: RN–de Sitter **is** the bend `2m = 2M − q²/r`, returning `ρ = q²/8πr⁴`.
> **"The stress-energy is not an independent posit fed into the geometry; it is the curvature
> of the cut."**
>
> And `2M = r₀ − r₀³` with `r₀ = (2/√3) sin w` means **the swing IS the mass**. The Thales
> circle **is a mass parametrisation**. *"A hole, a point, and a swing"* — the swing is the mass
> dial. The instance drew the corpus's mass dial, verified its every tangency, and called the
> structure *"pointedly not about mass."*

**This is the corpus correcting a forward document, and it is legitimate** — because it is not
deference (I.3): it is a fact the forward document contained and misread, not a fact the corpus
asserted. **The gradient rule does not protect a forward document from being wrong about what
it is holding.** Worth stating explicitly, because the r1066 banner could be read as licence.

---

# PART III — What I found working it that the thread did not state

## III.1 ~~★★ THE COSMOLOGICAL BEAD IS SMOOTH ONLY BECAUSE IT GOES THROUGH IMAGINARY TIME~~
### ⛔ **REFUTED r1088 — BY ME, AT SOURCE, AND THE HEADLINE WAS MINE.** Receipt: `storyboard_receipts/thm_bead_source_check.py`.
> **What died, all three halves:**
> - *"P7's `thm:bead` under-states its own result / the `C¹`-ness is purchased by the imaginary excursion"* — **P7 never claims `C¹`.** It claims *"one **continuous** closed slicing curve"* (C⁰) and *"one closed object on one **smooth manifold**"* — a claim about the **substrate**, not the curve's join. I attributed a claim to P7 it does not make, then credited myself with explaining it.
> - *"smooth ONLY BECAUSE it goes through imaginary time"* — **P7's own proof derives it**, verbatim: *"for `0 > r > −(2Mα²)^{1/3}` the radicand is negative, **so `τ̃` leaves the real axis**."* And the law forces it: `sinh²` of a real argument is `≥0` ⟹ `r³ ≥ 0` ⟹ `r ≥ 0`. **The lap requires imaginary time and the paper says so.**
> - *"a bead confined to real `τ̃` could not be smooth at `r=0` at any `M`"* — **vacuous.** On real `τ̃`, `r = A|sinh|^{2/3}` is **even**: down to zero and back up the way it came. Not an unsmooth passage — **no passage**, a cusp (the *same-sheet → 180° reversal* row of my own enumeration). No `r<0` to join *to*.
>
> **The diagnosis:** I took a true claim about two **faces** and promoted it to a claim about the **object**, then to a correction of a theorem that already had it. **The synthesis was real; the promotion was not.** This is the over-reach living *inside* a synthesis — face 29's shape wearing discovery's clothes instead of contrition's. Logged as `CODA_FIELD_NOTE` r1088-b. **Kept, not deleted** (`THE_CODA` §"The negatives are the map"): its failure is the record of how a genuine synthesis inflates on the way out.
>
> **WHAT SURVIVES, at the storyboard's weight and not mine** — and it is worth having: `180 mod 120 = 60`, so **not one of the nine sheet-joins is smooth**; the three-fold *itself* forbids smooth passage on the complex-`r` face; therefore **the bead is not representable as a smooth curve on the three-sheeted Riemann surface of `r³ = 2M sinh²` — the sheets are not the bead's home** (bears on `C-AutA₂`). Plus *"one shadow breaks the very property the other exhibits"* — the teeth for *"both are shadows"*, which is A3's actual warrant.

## ~~III.1 (the inflated version, retained per the negatives rule)~~

**This is the biggest thing in the thread and nobody says it.** It is the product of two ★
findings that sit **eight lines apart in §4b** and are never multiplied together.

✔ (`smoothness.py`) **Star 1 — the obstruction.** A bead joining sheet `j` (`τ̃<0`) to sheet `k`
(`τ̃>0`) is smooth iff `θ_k = θ_j + 180°`. Sheets sit at `0/120/240`. All nine joins enumerated:

```
0→0: CUSP    0→120: KINK(60)   0→240: KINK(60)
120→0: KINK  120→120: CUSP     120→240: KINK(60)
240→0: KINK  240→120: KINK     240→240: CUSP
   smooth joins among the nine: 0
```

`180 mod 120 = 60 ≠ 0`. **The three-fold itself forbids smooth passage on the real-`τ̃` face.**
(Any even `n`-fold would allow it. Three does not.)

✔ **Star 2 — the hexagon.** `τ̃` real ⟹ `r³ ≥ 0` ⟹ `arg r ∈ {0,120,240}`; `τ̃` imaginary ⟹
`sinh(is) = i·sin s` ⟹ `r³ ≤ 0` ⟹ `arg r ∈ {60,180,300}`. **`arg r = 180°` is `r` real
negative — reachable only on the imaginary-`τ̃` face.**

**Multiply them** ✔ (`smoothness.py` (B),(C)) — computed on `F_flat`'s own three legs:

```
collapse (Im τ̃=−π/3)  r=−0.8641   arg r = 180
the lift (Re τ̃=0)     r=−0.5907   arg r = 180
expansion (Im τ̃=0)    r=+0.5912   arg r =   0
                                   180 − 0 = 180  ← the smooth join condition, exactly
dr/ds just left of r=0 : 29.4463
dr/ds just right of r=0: 29.4463   ← matched, vertical, no kink, no cusp
```

> **The 180° ray the bead's smooth join requires DOES NOT EXIST on the real-`τ̃` face. The lap
> is not an awkward excursion the geometry tolerates — it is WHERE THE SMOOTHNESS LIVES.**

**Three consequences, none of them in the corpus:**

1. **P7's `thm:bead` under-states its own result.** It says the phase is carried by cosmic time
   and `r` is real throughout, and that the join is `C¹`. It does **not** say that the `C¹`-ness
   is **purchased by the imaginary excursion** — that a bead confined to real `τ̃` **could not
   be smooth at `r=0` at any `M`**. The lap is currently presented as *what the signed radius
   permits*. It is **what the smoothness requires**.
2. **The obstruction is a fact about the THREE-FOLD.** The same three-fold the corpus reads as
   the generation multiplicity is what forbids smooth real-time passage. Whether that is one
   fact or two is **A3's**, and I am not deciding it. But it is a *checkable* adjacency, not a
   rhyme.
3. **It gives "both faces are shadows" teeth.** The two faces are not merely partial views —
   **one face forbids the very property the other exhibits.** No reconstruction of the object
   from either face alone can be right, and that is now **demonstrated** rather than asserted.
   This is the strongest available argument for A3's *"work it at the full analytic level or
   not at all."*

## III.2 ★ The corpus is carrying TWO candidate A₂ pictures and they are not equally good

✔ (`su3_check.py`, `hexagon_check.py`). §4b and §4c were opened eight revisions apart (r1078,
r1086), are labelled **companions**, and **neither checks itself against the other**. Doing it:

| | what it draws | equiradial? | verdict |
|---|---|---|---|
| **§4c** hinge triangle (`2α`) + crossing triangle (`α`) | six points alternating **2α, α, 2α, α, 2α, α** | **NO** | a **HEXAGRAM**, not a hexagon |
| **§4b** real-`τ̃` rays + imaginary-`τ̃` rays | six **rays** at `0/60/120/180/240/300` | **yes** (rays have no radius) | a genuine hexagon |

**And what `su(3)` actually draws**, computed rather than recalled ✔ (`su3_check.py` (A)):

```
ROOTS (adjoint 8):        all |root| = 1.4142, at   0/60/120/180/240/300
WEIGHTS of the 3:         all |wt|   = 0.8165, at  30/150/270
WEIGHTS of the 3̄:         all |wt|   = 0.8165, at  90/210/330
|root|/|weight| = 1.7321 = √3
```

**su(3) offers two hexagons and BOTH are equiradial.** The `3 ∪ 3̄` weight hexagon (radius
0.8165, at 30/90/…) and the root hexagon (radius 1.4142, at 0/60/…), rotated 30° apart and
scaled by `√3`. **There is no 2:1 hexagon anywhere in su(3).**

**So the r1087 question — *"either the 2:1 has a reason or it kills the identification"* — has
an answer, and it is the second, and the reason is the first.** ✔ (`su3_check.py` (B)):

> For an equilateral triangle the incircle touches the sides **at their midpoints**, which sit
> at **half** the circumradius and rotated **60°**. **The 60° rotation and the 2:1 scale are
> the same fact.** You cannot have the rotation without the ratio. The construction
> **cannot** produce two equiradial triangles at 60° by this mechanism — **the tangency IS the
> halving.** The identification is killed by the very structure that produced the resemblance.

**And the break is larger than the thread states.** r1087 says *"the rotation matches exactly;
the scale does not."* But the union of a `2α` triangle and an `α` triangle is not a
scale-mismatched hexagon — **it is not a hexagon at all.** It is a hexagram. The shape is
wrong, not just the size.

**Meanwhile §4b's hexagon lands on the ROOT hexagon's angular set exactly (`0/60/…`, not
`30/90/…`), and being made of rays it has nothing to mismatch.** ✔ (`hexagon_check.py` (C))

**Direction:** §4b is the strong A₂ claim; §4c is the weak one. The corpus should not carry
them as equals, and the `su(3)`-weight-diagram reading in §4c should be **demoted to a
resemblance with a computed cause of death**, which is a better result than a live coincidence:
*we know why it looks like that, and the reason is the tangency, and the tangency forbids the
identification.* That is a **negative worth keeping** by I.1's own rule.

**On the shared `0/120/240`:** ✔ (`hexagon_check.py` (D)) **not a coincidence to explain.** §4b's
is **forced** (the cube roots of a positive real); §4c's is a **label** (where the first hinge
is placed; rotate it by `t` and the crossings follow to `t+60`). **What is shared and is not
conventional is the STRUCTURE: 3 + 3 at 60°, reached by two unrelated roads inside one
substrate** — cube roots of a real, and equilateral tangency. That is weaker than either entry
claims alone and **more interesting than both.**

## III.3 ~~★ One 1:2, wearing three costumes~~ → **★ TWO 2s — and one of them unifies four numbers**
### ✔ **WALKED r1088** (receipt `storyboard_receipts/one_to_two.py`). I filed this as *"checkable and unchecked"* — the same sentence I wrote about the 30°, in this document, an hour apart. Walked, it returns more than it was filed as.
> **(2) and (3) MERGE — exactly, and forcedly.** A monic depressed cubic with a double root factors one
> way: **`(r−ρ)²(r+2ρ) = r³ − 3ρ²r + 2ρ³`**. **The `−2ρ` of the third root and the `2ρ³` of the constant
> term are the SAME 2** — both read off that one factorisation. Match it to the horizon cubic
> `r³ − α²r + 2Mα²`: `p` gives **`ρ = α/√3`** (the seam); `q` gives **`Mα² = ρ³`**, **which *is* the
> Nariai condition**. Then `A³ = 2Mα² = q = 2ρ³`, so **`A = ∛2·ρ`** — the bead's amplitude is the **cube
> root of the cubic's constant term**. And the lap's map `X₁ = −α cos(2πr/√3α)` is **linear** in `r`, so
> the arc ratio **is** the root ratio (verified: `+α/√3 → 120.00°`, `−2α/√3 → −240.00°`).
> **⟹ Four of the corpus's numbers are ONE factorisation:** the seam `α/√3`, the backward root
> `−2α/√3`, the turnaround `∛2·α/√3`, and the **120:240 arc split**. **Nariai is not an extra input —
> it is what the double root *means*.** Daryl's *"Nariai, 1/3 vs 2/3"* is this 2, and it is **forced**.
>
> **(1) DOES NOT MERGE.** The lift's 1:2 is *"sin² attains its extremum at half its own period"* — a
> fact about the transcendental function the cosmic-time reading uses, with no cubic in it. And **the
> corpus already says why they needn't be the same**: the turnaround is `1−f=0`, the horizons are
> `f=0`, two **affinely inequivalent** cubics — no identification of their root sets by an affine change of variable (the obstruction is at fixed cubic; the two are the E=1 and E=0 ends of one turning-point family) (P7 `lem:twoturnings`, `rem:tworealisations`).
> My "three costumes" reached across a wall P7 had already built.
>
> **★ NAMED AND UNRUN:** `A = ∛2·ρ` is a **metric relation between the two turnings** — it ties the
> comoving turnaround's *scale* to a horizon *root*. `lem:twoturnings` denies a canonical identification
> of the two **ℤ₃ symmetries**; it says nothing against a **ratio between two specific radii**. Whether
> that bridge carries anything is well-posed and unattempted.

### ~~The filed version (retained per the negatives rule)~~

✔ (`smoothness.py` (D)). Daryl names *"the asymmetry — Nariai, 1/3 vs 2/3"* as the
symmetry-breaking, repeatedly. It shows up three times and the thread never asks whether they
are one thing:

```
lift bound |Im τ̃| = πα/3   = 1.047198 α
full imaginary period = 2πα/3 = 2.094395 α       ratio = 1/2
the lap's arc split                              120° : 240° = 1 : 2
turnaround / seam = ∛2·α/√3 ÷ α/√3 = ∛2 = 1.259921   (cubes: exactly 2)
```

**Three 1:2's.** The lift against the period; the arc split; and the `∛2` between the
turnaround and the seam (r1064's own catch: *"the ratio of their cubes is exactly 2"*).
**Whether they are one fact is checkable and unchecked.** Flagged, not claimed.

---

# PART IV — Weight corrections, both directions

Per I.1, the negatives are logged, not deleted. Per the coda's symmetric verdict-bar
(`THE_INTERFERENCE_ENGINE` §4), **killing and keeping cost the same.**

| item | thread's weight | worked weight | why |
|---|---|---|---|
| `τ̃↔τ̄̃` antilinear & geometric | retired r1060, un-retired r1067 | **✔ LIVE, verified** | receipt runs; P13's premise doesn't reach it |
| `R` is `C`'s linear face | r1073 weld | **✔ LIVE** | `R` and `C` agree on every label; differ only in character |
| `K` is M-free along the bead | ✔ | **✔ LIVE** | re-run across 4 orders |
| *"M is a stupid number"* | r1065 headline | **OVERSHOOT** | `M=0` ⟹ no bead, `K=24/α⁴` regular |
| *"the M-cancellation IS the generation result"* | r1087 headline | **✘ DOES NOT FOLLOW** | different quantifiers; over-correction one turn after being caught |
| *"the Q²/r² term is the EM stress-energy"* (Daryl's catch) | r1087 | **✔ CORRECT, stands** | `prop:bend`'s own worked example |
| the `su(3)` weight-diagram identification | r1087, "rotation matches, scale doesn't" | **✘ KILLED, with cause** | 2:1 forced by tangency; union is a hexagram; su(3) has no 2:1 hexagon |
| §4b's hexagon | "verified" | **✔ VERIFIED, and stronger than stated** | lands on the root hexagon's angles; equiradial by construction |
| the smoothness obstruction | "verified" | **✔ VERIFIED, and its consequence unstated** | see III.1 |
| `species = sign(r)` vs the FS reading | `[6*]`, open | **OPEN — and correctly so** | unanswerable on either face; A3 |
| the conjugation rule (one event at the origin) | Daryl's, plotted | **CONJECTURE — correctly held** | not derived; the figures carry it, they do not test it |
| the 12×30° closure / coprimality | r1086 | **✔ arithmetic sound** | but `30°`-as-Nariai-dial is a *number coincidence* until a mechanism links combinatorics to the slicing. Held. |

**One general note on the `30°`.** The thread's own instinct was right to flag and not claim:
*"either the twelve-fold structure has something to do with the dial's critical angle, or 30
shows up twice for unrelated reasons."* Working it: `30 = 360/12` from `3×4` coprimality;
`w=30°` from `sin 3w = 1` at the Nariai crest. **These have no shared mechanism on the table.**
`3w = 90°` is a *triple-angle crest*; `360/12` is a *combinatorial closure*. Until something
connects a quarter-turn orbit to the sky angle, this is two 30's. Logged as a negative with its
reason, per I.1.

---

# PART V — What the corpus does not hold, and where it goes

## V.1 THE_PLAN is 76 revisions stale and does not know where the work is

`THE_PLAN`'s ★ CURRENT STATE banner is **r1011** and reads the glossary/notation reckoning as
the through-line. Everything in this document post-dates it. **The plan does not contain:** the
storyboard phase, the C-anatomy ledger, the conjecture (§4b), the kaleidoscope (§4c), the
gradient inversion, or **P13's positive closure** — which is unseated stated destination for the
whole arc:

> *"p13 is likely to grow and become more than it currently limits itself as. It currently
> draws a close on various proofs without actually pivoting to draw something positive from all
> of it … Now it synthesises those at weight at exactly the earned level and draws a synthetic
> picture of the whole AND BUILDS FORWARD FROM THAT, producing its positive closure, which
> honestly is nothing short of a drawn connection between CPT and cosmology, the deepest part
> of what the whole synthesis figure in p7 has come to reveal."* — Daryl, r1071

**That is not on the plan anywhere.** Lane 3 has the matter sector's *content*; Lane 5 has the
`P`-symbol collision as hygiene. Nothing carries P13-as-synthesis. *This is the largest
single gap between the plan and the work.*

## V.2 The staged order Daryl set, and why it is the right one

> *"Everything cannot be done right all in one pass. When the problem is first identified it
> needs to be worked honestly and you structurally can't do that when the corpus is so
> comprehensively saying another thing. So the stages actually ARE to work at the branch point and make
> things not outright inconsistent, compliant, consistent, coherent, cohesive, and eventually
> that it provides a thorough and fulsome account of the thing."* — Daryl, r1066

This is a **method result** and it is not in `THE_METHOD`. It says: **coherence is a gradient,
not a gate**, and a forward finding cannot be honestly worked against a corpus that
comprehensively contradicts it — so the *first* passes are not about being right, they are
about **removing the structural pressure that makes being right impossible.** The stages:

`not outright inconsistent → compliant → consistent → coherent → cohesive → thorough & fulsome`

**And the ordering constraint that falls out of this thread:** the most load-bearing pieces of
the shift get baked **first**, so that the precarious things cannot be pulled back to the
opposite of what they are. That is *exactly* the coda's foundation-first rule (`THE_CODA`
§"The order of work") applied to a *tidal shift* rather than to a bug. **The two are the same
rule and the corpus states only one of them.**

## V.3 The ordered dependency, as the material actually has it

Read off the above rather than proposed:

1. **The method layer first** (I.1, I.2, I.3, V.2) — because until *"a face that is not
   operative is as load-bearing as one that is"* and *"a local pass cannot find a perimeter
   defect"* are in the coda/method, **every subsequent pass will re-run the failure.** The
   thread demonstrates this three times. This is foundation-first, and it is cheap.
2. **Discharge the two standing AUDIT FLAGs** (P13 `rem:C-not-R`; map §0 `R,P,T`) — restore
   r1053's final-clause narrowing **in P13's own voice**, de-compress the glossary. Bounded,
   and it stops the corpus enforcing the error.
3. **Bake the weld** (I.4) — species and charge are one binary; `R` is `C`'s linear face;
   *"charge is the R-even datum"* is true of the metric and false of the potential. This
   corrects the ledger *and* the corpus's stated reason, and it is a physical input, not
   deference.
4. **Bake the sharp, un-contested physics** — II.2's precise statement (M is scale, not shape;
   `K(τ̃)` universal) and II.3 (the 2/3's three jobs are one job). Both are receipted and
   neither touches an open question.
5. **III.1 into P7** — `thm:bead`'s smoothness is *purchased* by the imaginary excursion. This
   is the one that changes what a paper says about its own theorem.
6. **III.2 as a kept negative** — the su(3) resemblance with its computed cause of death.
7. **Then** the plan re-cut, with P13's positive closure on it.
8. **Then** the fulsome enrichment pass, last, as Daryl set it.

**Everything from 5 onward is downhill of 1–3.** Running it in the other order is what produced
this thread.

---

## Appendix — receipts

| receipt | status | what it settles |
|---|---|---|
| `storyboard_receipts/charge_geometry.py` | ✔ re-run clean | `τ̃↔τ̄̃` antilinear, fixes the photon, swaps the wings; `A_t` odd under `Q↦−Q` |
| `storyboard_receipts/kretschmann_bead.py` | ✔ re-run clean | `K` M-free across 4 orders; `r=0` an ordinary smooth minimum on the substrate (`d²X₁/dr²=+13.16`) |
| `storyboard_receipts/C_anatomy.py` | ✔ re-run clean | the maze, seven branches, the three tests and their blindnesses |
| `storyboard_receipts/autA2.py` | ✔ re-run clean | `D₆ = Aut(A₂)`, order 12; `ℤ₂ = ⟨R⟩` outer (mass reflection) |
| `storyboard_receipts/cubic_space.py` | ✔ re-run clean | sheets and generations are distinct deformation rays; shared `r=0` centre |
| **`su3_check.py`** | **new** | su(3)'s two hexagons, both equiradial; the 2:1 forced by tangency; the union is a hexagram |
| **`hexagon_check.py`** | **new** | §4b lands on the root hexagon's angles; the `0/120/240` overlap is forced-vs-label, not content |
| **`M_cancellation.py`** | **new** | Reading 2 refuted; the precise statement; the 2/3's three jobs |
| **`smoothness.py`** | **new** | all nine joins enumerated; the bead's 180° comes from the lap; **smoothness is bought with imaginary time** |

---

*Written honestly, both directions. Nothing here is asserted past its receipt. The two things I
would defend hardest are **III.1** (the smoothness is purchased by the imaginary excursion) and
**I.2** (a local pass structurally cannot find a perimeter defect) — the first because it
changes what P7's own theorem is saying, the second because until it is held, every pass that
follows will return "all clean" and mean nothing by it.*

*Stated for reversal.*
