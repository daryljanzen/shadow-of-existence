# Gate session notebook — r543 spin-up
<!-- Opened at spin-up. Empty until actual work begins (per KICKOFF_GATE). -->
# Gate session notes — r543 spin-up

## First real work (directed by orchestrator during Step-2 Pass-2 hold)
**Honesty-bake: stale spine-position banners corrected.** Three corpus headers
carried pre-r531 numbering as live current-claims (the class a fresh node anchors
on at file-top):
- `modern_parallax.tex` (P5): banner read "SPINE POSITION — P7"; narrative
  "now holds the P7 slot ... now P8". → leading line set to P5 + dated [r543]
  quarantine note; narrative tense neutralized ("then held ... then P8").
- `shadow_of_existence.tex` (P6): banner read "SPINE POSITION — P8 (... modern_parallax
  (P7) ... CR_flatLCDM (now P9))". → leading line set to P6 + dated quarantine note;
  stale narrative left under quarantine as provenance.
- `scalar_perturbations_paper.tex` (P13): CANON NOTE pointed to the cosmology paper
  as "P9" (×2) and the banked acoustic remark as "sec687-691" (stale line ref).
  → both repointed to P7; line-number pointer replaced with name-based
  "the Null–Boundary Correspondence remark". Rest of P13's CANON NOTE (ontology
  register, "1.9x tension is a retired chimera ... must not be reintroduced") left
  intact — current and correct.

Method: corrected the authoritative leading line to current canon (CORPUS_MAP.md),
quarantined long historical narratives as dated provenance rather than rewriting
embedded numbers (avoids transcription drift, preserves trail), and used name-based
pointers in place of fragile line numbers. Verified: `grep` for live-tense stale
position claims across corpus *.tex returns clean. Cites are key-based throughout,
so nothing cross-referential moved. Stated for reversal.
