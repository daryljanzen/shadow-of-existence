# THE TABLE OVER TIME -- the weight-loss chart.
#
# ** WHY THIS FILE.  r2622, Daryl: ** "I expect the whole deal will end up working like a weight loss
# chart.  That would be a sign of it working -- and working right ... The big gains due to bloating right
# at the start and all the plateaus and such.  ** If it were to go down at a regular rate we should be
# wary of that. **  Experience suggests something more like a healthy weight loss chart."
#
#   ⇒ *** And that is a real diagnostic rather than an analogy.  A smooth linear decline would mean the
#       number is being MANAGED rather than measured -- a node shaving one item per turn to show
#       progress.  The healthy shape is: big early drops as bloat comes off, PLATEAUS while genuine work
#       is done, and UPTICKS when a correction or a discovery adds a real item. ***
#
# ** FORMAT: ** revision, table count, dark halves answered, one-line reason.
# ** Appended each turn the table moves.  Never rewritten. **
#
# ⚠ ** The first four rows are the only history that exists: ** `queue.py` was built at r2615, so
# nothing before it was counted.  *** The chart starts where the instrument does, and saying so is
# better than reconstructing numbers nobody measured. ***

r2615  34  2/4  the queue is first assembled and printed -- five sources, nothing deduped
r2616  32  2/4  PO-8 struck (authorised at r2378, held 238 revisions); answered dark halves no longer double-counted
r2618  27  2/4  five ledger entries deduped -- three onto PO-11, two the same traced statement twice
r2621  28  2/4  ONE dedupe reversed: the compact-face sector is a second unbuilt object, not PO-11's
r2631  27  2/4  the {0,1,2} derivation discharged NEGATIVELY -- the two triples are typed differently
r2635  26  2/4  FOR_54 item 26 discharged: {6,7,10} computed from so(5,1) symmetric decompositions; P12 now owns it
r2636  25  2/4  FOR_54 item 9 discharged -- the convention was already enforced by three gates; ROUTED QUEUE EMPTY
r2639  21  2/4  OPEN-DOWNSTREAM retired and PRECISION reclassified -- the bucket name inverted the papers relation
r2640  19  2/4  parked items no longer counted (book x2); PO-9 link (e) reproduced
r2644  18  2/4  PO-9 ANSWERED: the dimension is not settled by least-arbitrariness and stays bounded below only
r2645  17  2/4  PO-3 ANSWERED -- both clauses, and both were already in its own row since c54.42-c54.62
