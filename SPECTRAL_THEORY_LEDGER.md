---
name: spectral-theory-ledger
kind: FORWARD
current: r3487
job: The spectral-theory field bake — what bit, what bounced, and the boundary. Tier B's largest never-thrown field (×189 on the r3453 measure), verified at r3438 as NOT covered by the harmonic ledger, which mentions spectral, self-adjoint and deficiency zero times.
sources: [chat]
---

> **▣ FORWARD — the spectral-theory bake.** *Three registers kept apart: **what bit**, **what
> bounced**, **what the boundary is**. Thrown as the first of Tier B, the four fields never thrown at
> all, and the largest of them.*

# THE SPECTRAL-THEORY LEDGER

## ⌗ S0 — THE BASELINE, RUN FIRST, AND IT CUTS THE FIELD IN HALF

*The gate `609` installed, and the one this sweep has been repaid by four times. **Measured before any
hole is asserted.***

⛔ **The two largest counts are not this field's.**

| term | count | what it is |
|---|---|---|
| `quantisation` / `quantization` | **×66** — `P10` 25, `P07` 13, `P11` 9 | ***canonical quantisation*** — quantum theory, not spectral theory |
| `gap` | **×30** | *mostly a **physical** gap — a mass gap, a gap in a sequence, a gap in an argument* |

⇒ ***The genuinely spectral vocabulary is thinner and sits elsewhere:*** `eigenvalue` *(`P09` 8, `P14`
7, `P03` 4)*, `discrete spectrum` *(`P15` 2)*, `WKB` *(`P15` 3, `P16` 2)*, `resolvent`, `essential
spectrum`, `Sturm–Liouville`.

⌗ **AND THE FIELD IS VERIFIED NOT COVERED BY ITS NEIGHBOUR.** *r3438 checked
`HARMONIC_ANALYSIS_LEDGER` and found `spectral`, `self-adjoint` and `deficiency` mentioned **zero**
times, with the operator half named there as station Ⓗ's. The functional-analysis bake did the
**boundary classification** (`F1`–`F3`: limit-circle at $a=0$, limit-point at $r=0$) and **never asked
what the spectrum IS.***

---

## ⌗ THE ESTIMATE, FROM CONTENTS — *recorded so it can be scored*

| paper | estimate | reason |
|---|---|---|
| **`P10`** *canonical time* | **HIGH** | *the deficiency-index paper. `F1`–`F3` classified the **endpoints**; **nobody has asked what the spectrum of the deparametrised Hamiltonian actually is** — discrete, continuous, or mixed* |
| **`P14`** *matter sector* | **HIGH** | *a Dirac operator with `eigenvalue` ×7; `H11` already found its wall to be the **reflectionless Pöschl–Teller pair** — one bound state plus a reflectionless continuum, which IS a spectral statement* |
| `P07` *framework* | **MEDIUM–HIGH** | *the tower's $\mu_n=\sqrt{n(n+2)-2}$ is a **spectrum**, and `H13` showed its floor at $n=2$ is forced* |
| `P09` *range* | **MEDIUM** | *`eigenvalue` ×8 — of the structure functions or the Carter cut's separation constants; needs reading* |
| `P15` *cosmology* | **MEDIUM** | *`WKB` ×3, `discrete spectrum` ×2 — the acoustic ladder* |
| `P03` *slicing curve* | **MEDIUM** | *`eigenvalue` ×4 — plausibly the cubic's roots, which would be a **different** sense* |
| `P06` *geometric core* | **MEDIUM** | *`gap` ×6, `eigenvalue` ×2* |
| `P16` *cosmogenesis* | **MEDIUM** | *`WKB` ×2* |
| `P12`, `P13`, `P11` | **LOW–MEDIUM** | *small counts, to be read* |
| `p0`, `P01`, `P02`, `P04`, `P05`, `P08` | **LOW** | ***to be read, not assumed*** |

⌗ *The "LOW" label is written knowing that on the harmonic side it turned into bites four times
(`P13`, `P05`, `p0`, `P09`), and on the functional side it held twice (`P02`, `P05`). **Both outcomes
are results; neither is assumed.***
