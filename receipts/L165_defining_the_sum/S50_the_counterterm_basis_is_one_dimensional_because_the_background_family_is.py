#!/usr/bin/env python3
r"""
RECEIPT -- `PO-6` / `L-165`: ** THE UV HALF, AND `PO-6`'s DARK QUESTION -- *"can a theory with ONE
dimensionful constant regulate at all?"* -- HAS A STRUCTURAL ANSWER ON THE SUBSTRATE. ⛭⛭ *** THE
COUNTERTERM BASIS IS ONE-DIMENSIONAL BECAUSE THE ADMITTED BACKGROUND FAMILY IS: every curvature
invariant is a pure power of $1/\alpha^2$, so every candidate counterterm is a constant times
$\int\!\sqrt g$, and one constant absorbs whatever the degree. *** AND THE LAYER IS WHERE THAT WOULD
LIFT, WHICH MAKES THE REMAINING QUESTION DETERMINATE RATHER THAN OPEN-ENDED. **

Built r2671+c54.210, lead `L-543`.  VEIN: `L-165` (PO-6, what a quantum of this geometry is).
** ⛔⛔ AMENDED c54.211 -- PART 4's SCOPE WAS WRONG AND IS WITHDRAWN HERE.  `L-544`. **
*c54.210 wrote: "the tower does not live on the substrate, it lives on the layer, whose $\sinh^{2/3}$
Ricci scalar runs."*  ⇒ ** THAT PUT P15's OBSERVABLE RATE WHERE P10's SLICING BELONGS. **  P10 states
its own background one sentence into the section: *"Its closed synchronous slicing is the evolving
round three-sphere of radius $a(T)=\alpha\cosh(T/\alpha)$ in cosmic time $T$"* -- and that geometry has
** $R=12/\alpha^2$, constant.  It is exactly de Sitter. **
  ⇒⇒ *** SO THE DEGENERACY HOLDS ON THE VERY BACKGROUND THE FREE TOWER USES, AND THE RESULT IS
      STRONGER THAN c54.210 CLAIMED, NOT WEAKER.  The caveat was about a different object. ***
  ⌗ ** AND THE REAL LIMIT IS THE ONE P10 ALREADY NAMES: ** the free tower evolves on $a(T)$ as a FIXED
  CLASSICAL background; the coupled sector is where the scale factor is quantized and back-reacts, and
  that is the regime $\hat\Gamma$ belongs to.  *A counterterm basis is a statement about a class of
  fixed backgrounds, and in the coupled sector there is no fixed background to state it on.*
  ⚠ *This is the FIFTH substitution of a nearby object this session across both lines, and the second
  by this one.  It was found by asking which background the tower is actually defined on -- a question
  c54.210 should have asked before importing an answer from another paper.*
** KIND: LATENT ** -- *p0 states the invariant fact for a different job; nothing here is new physics.*

===================================================================================================
** ⓪ WHERE THIS SITS, AND WHY IT IS THE PIECE LEFT **
===================================================================================================

`D50` (c54.209) restored P10's affirmative verdict on the floor and said what it did NOT touch: *"P10
names what stays open in the same breath -- THE ULTRAVIOLET DEFINITION OF THE TOWER SUMS."*  This is
that piece.  ** Two measurements already exist and they bracket it: **

  · `D2` -- the tower sum diverges ** QUARTICALLY **, the ordinary zero-point degree, and compactness
    buys the INFRARED free while leaving the ultraviolet untouched.
  · `L809/S1` (cc54, A7) -- the quartic is a mass- and field-INDEPENDENT constant vacuum energy, so
    ** its counterterm is a cosmological-constant term, which IS the framework's one constant. **
    ⌗ *And that receipt names its own successor:* *"only the sub-leading terms (the quadratic
    $\Lambda_c^2m^2$ and the log $m^4$) carry the mass, i.e. only they renormalise mass/couplings."*

  ⇒ *** SO THE OPEN QUESTION IS THE SUB-LEADING TOWER, AND THE REASON IT IS SHARP IS THAT ITS
      COUNTERTERMS ARE NOT COSMOLOGICAL-CONSTANT TERMS: in the standard expansion the quadratic goes
      with $R$ and the logarithm with CURVATURE-SQUARED, and a curvature-squared coupling is not in
      this framework's ledger. ***

===================================================================================================
** ⓵ AND NONE OF THIS IS IN A PAPER.  `counterterm` AND `one-loop` ARE AT ZERO USES. **
===================================================================================================

*A result that lands in no paper is not banked* -- the corpus's own rule, and `check_receipts`
enforces it.  ** A7's verdict has lived in ledgers and receipts since r2570 and has never reached
print. **  PART 1 measures that rather than asserting it.

===================================================================================================
** ⛭⛭ ⓶ THE ANSWER, AND IT IS ASSEMBLED FROM TWO SENTENCES ALREADY IN THE CORPUS **
===================================================================================================

  ** ⓐ p0, written to show the construction cannot force a dimensionless coupling: **
     *"every curvature invariant on either face is a pure power of $1/\alpha^{2}$"*
     `\rcpt{P17_no_second_scale_on_either_face}`.
  ** ⓑ AND THE ADMITTED BACKGROUNDS ARE A ONE-PARAMETER FAMILY: ** p0's $\alpha$-family is a
     FOLIATION (`\rcpt{P17_the_alpha_family_is_a_foliation}`), and `L-533` (c54.207) showed every
     descent step above the last is a plane section that ** changes the scale and nothing else. **

  ⇒ *** PUT TOGETHER: on the class of backgrounds this framework admits, $\int\!\sqrt g\,R^2$,
      $\int\!\sqrt g\,R_{\mu\nu}R^{\mu\nu}$, $\int\!\sqrt g\,R_{\mu\nu\rho\sigma}R^{\mu\nu\rho\sigma}$
      and $\int\!\sqrt g$ are ALL PROPORTIONAL -- each is a constant times $\alpha^{-4}\int\!\sqrt g$.
      THE COUNTERTERM BASIS IS ONE-DIMENSIONAL. ***
  ⇒⇒ ** So a divergence of ANY degree needs exactly ONE counterterm here, and the framework carries
     exactly one dimensionful constant. **  *`PO-6`'s dark question is not answered by luck at the
     leading order; it is answered by the background family having one parameter.*

  ⌗ ** AND THAT IS WHY p0's SENTENCE IS DOING TWO JOBS. **  *It was written to say the construction
   cannot FORCE a coupling -- a limitation.  Read at the counterterm basis it says the construction
   does not NEED one -- and those are the same fact from opposite sides.*

===================================================================================================
** ⛔ ⓷ AND THE SCOPE, WHICH IS THE WHOLE REMAINING QUESTION **
===================================================================================================

** A degeneracy of a basis is a property of the BACKGROUND CLASS, not of the theory. **  Counterterms
are distinguished by how they respond to VARYING the background; on a one-parameter family they
cannot be told apart, and that is exactly why one suffices there.

  ⇒ ⛔ ** THE TOWER DOES NOT LIVE ON THE SUBSTRATE.  IT LIVES ON THE LAYER **, whose observable rate
    P15 gives as the radiation-free $\sinh^{2/3}$.  PART 4 computes its Ricci scalar: ** it runs from
    divergent at early times to $12H^2$ asymptotically -- NOT constant. **
  ⇒⇒ *** SO THE DEGENERACY IS THE SUBSTRATE'S AND THE LAYER IS WHERE IT WOULD LIFT.  `PO-6`'s dark
      half becomes determinate: DOES THE ONE-DIMENSIONAL COUNTERTERM BASIS SURVIVE ON A BACKGROUND
      WHOSE CURVATURE RUNS? ***
  ⌗ *That is a question with a stated object, an instrument (the sub-leading Schwinger--DeWitt
   coefficients on the $\sinh^{2/3}$ layer) and a decidable answer -- which the previous form,* "can
   one constant regulate at all", *did not have.*

===================================================================================================
** ⛔ WHAT IS NOT CLAIMED **
===================================================================================================

** Not that `PO-6` closes. **  *The sub-leading coefficients are not computed here and the layer
question is posed, not answered.*
** Not that the sub-leading divergences vanish ** -- nothing here says they do; what is shown is that
ON THE SUBSTRATE their counterterms have nowhere else to go.
** Not a renormalisability claim ** -- one counterterm on a one-parameter family is a statement about
a basis, not a proof that the tower sums converge or that the theory is predictive at all orders.
** And not that the layer's running curvature breaks it ** -- *that is the question, and stating a
question as a finding is the failure this line has been paying for all session.*

⚠ ** THE HONEST SHAPE OF THIS RESULT: ** *it is an assembly of two standing sentences and one
computation, not a discovery.  **Marked `LATENT`.**  ⌗ *And the degeneracy argument would be worth
very little if the background class were wider -- so it rests on `L-533`'s scale-only descent, which
is this line's own result and should be read as load-bearing here rather than as decoration.*

SETTINGS: none -- no instrument, no spectra.  Maximally symmetric curvature contractions in general
$D$, the $\sinh^{2/3}$ layer's Ricci scalar, term counts over the papers, and source checks.

rc=0 on success.  Run: python3 S50_the_counterterm_basis_is_one_dimensional_because_the_background_family_is.py
                        (sympy; ~8 s)
"""
import glob
import os
import re
import sys

import sympy as sp

print(__doc__.split("rc=0")[0])

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.abspath(os.path.join(HERE, '..', '..'))
CORPUS = os.path.join(ROOT, 'corpus')
fail = []

PAPERS = {os.path.basename(f): open(f, encoding='utf-8', errors='replace').read()
          for f in sorted(glob.glob(os.path.join(CORPUS, '*.tex')))
          if 'appendix_receipts' not in f}


def body(t):
    """what the paper PRINTS -- `L-535`'s lesson, applied by default now"""
    return '\n'.join((l[:m.start()] if (m := re.search(r'(?<!\\)%', l)) else l) for l in t.split('\n'))


BODIES = {k: body(v) for k, v in PAPERS.items()}
ALL = '\n'.join(BODIES.values())

# =====================================================================
print("=" * 78)
print("PART 1 — WHAT THE PAPERS SAY ABOUT COUNTERTERMS, MEASURED IN THE PRINTED BODY")
print("=" * 78)
TERMS = ['counterterm', 'one-loop', 'Gauss--Bonnet', 'curvature squared', 'renormalisation of ']
print(f"  {'term':>22s} {'uses in the 17 papers':>24s}")
_counts = {}
for t in TERMS:
    n = len(re.findall(re.escape(t), ALL, re.I))
    _counts[t] = n
    print(f"  {t:>22s} {n:>24d}")
print()
print("  ⇒ *cc54's A7 verdict (r2570) -- the quartic's counterterm IS the one constant -- has lived")
print("     in ledgers and receipts since, and has never reached print.*")
print("  ** A result that lands in no paper is not banked. **")
# the BEFORE count must come from the committed tree, not from the file this revision just edited
import subprocess as _sp
_prev = _sp.run(['git', 'show', 'HEAD:corpus/canonical_time.tex'], cwd=ROOT,
                capture_output=True, text=True).stdout
_before = len(re.findall('counterterm', body(_prev), re.I))
print(f"  'counterterm' in P10 at HEAD (before this revision) : {_before}")

# =====================================================================
print()
print("=" * 78)
print("PART 2 — ⛭⛭ THE INVARIANT BASIS ON A MAXIMALLY SYMMETRIC BACKGROUND, COMPUTED")
print("=" * 78)
al = sp.symbols('alpha', positive=True)
D = sp.symbols('D', positive=True, integer=True)
K = 1 / al ** 2
R_ = D * (D - 1) * K
Ric2 = D * (D - 1) ** 2 * K ** 2
Riem2 = 2 * D * (D - 1) * K ** 2
print(f"  R_(abcd) = (1/alpha^2)(g_ac g_bd - g_ad g_bc)   ->   R = {R_}")
print(f"  {'D':>3s} {'R':>16s} {'R^2':>16s} {'R_ab R^ab':>16s} {'R_abcd R^abcd':>18s}")
_exps = set()
for d in (4, 5, 6):
    v = [sp.simplify(x.subs(D, d)) for x in (R_, R_ ** 2, Ric2, Riem2)]
    print(f"  {d:>3d} {str(v[0]):>16s} {str(v[1]):>16s} {str(v[2]):>16s} {str(v[3]):>18s}")
    for x in v[1:]:
        _exps.add(sp.degree(sp.simplify(1 / x), al))
print()
print(f"  ** every QUADRATIC invariant is const * alpha^(-4) : exponent set = {_exps} **")
print("  *** SO ON THIS BACKGROUND CLASS int(sqrt g)R^2, int(sqrt g)R_ab R^ab,")
print("      int(sqrt g)R_abcd R^abcd AND int(sqrt g) ARE ALL PROPORTIONAL. ***")
print("  ⇒ ** THE COUNTERTERM BASIS IS ONE-DIMENSIONAL, and the framework carries one constant. **")
if _exps != {4}:
    fail.append(f"the quadratic invariants are not all alpha^-4: {_exps} — the degeneracy fails")

# =====================================================================
print()
print("=" * 78)
print("PART 3 — AND THE TWO STANDING SENTENCES THE ARGUMENT IS ASSEMBLED FROM")
print("=" * 78)
STANDING = [
    ("p0: every curvature invariant on either face is a pure power of 1/alpha^2",
     r'every curvature invariant on either face is a pure power of \$1/\\alpha\^\{2\}\$'),
    ("and it carries its own receipt", r'\\rcpt\{P17_no_second_scale_on_either_face\}'),
    ("⛭ p0: the alpha-family is a FOLIATION, so the admitted backgrounds are ONE-PARAMETER",
     r'the family is a \\emph\{foliation\}'),
    ("and that carries a receipt too", r'\\rcpt\{P17_the_alpha_family_is_a_foliation\}'),
    ("P12/`L-533`: a step above the last changes the scale and nothing else",
     r'a step above the\s*\n?last changes the scale and nothing else'),
]
for what, pat in STANDING:
    ok = re.search(pat, ALL, re.I | re.S) is not None
    print(f"  {'OK ' if ok else 'MISSING'}  {what}")
    if not ok:
        fail.append(f"a standing sentence the argument rests on is gone: {what}")
print()
print("  ⇒ *p0's sentence was written to show the construction cannot FORCE a coupling.  Read at the")
print("     counterterm basis it says the construction does not NEED one.  Same fact, opposite side.*")

# =====================================================================
print()
print("=" * 78)
print("PART 4 — ⛔ WITHDRAWN AND REPLACED c54.211: THE TOWER'S BACKGROUND IS CONSTANT-CURVATURE")
print("=" * 78)
T, alx = sp.symbols('T alpha', positive=True)
aT = alx * sp.cosh(T / alx)
HT = sp.simplify(sp.diff(aT, T) / aT)
Rtower = sp.simplify(6 * (sp.diff(aT, T, 2) / aT + HT ** 2 + 1 / aT ** 2))   # closed FRW, k = +1
_const_tower = sp.simplify(sp.diff(Rtower, T)) == 0
print(f"  P10's own words: the closed synchronous slicing is the round three-sphere of radius")
print(f"                   a(T) = {aT}")
print(f"  its Ricci scalar : R = {Rtower}")
print(f"  ** constant in T : {_const_tower}   -> EXACTLY DE SITTER, maximally symmetric **")
print()
print("  ⇒ *** SO THE DEGENERACY HOLDS ON THE BACKGROUND THE FREE TOWER ACTUALLY USES. ***")
print()
print("  ⛔ AND THE OBJECT c54.210 SUBSTITUTED, kept here so the error is checkable:")
t, H = sp.symbols('t H', positive=True)
a_ = sp.sinh(sp.Rational(3, 2) * H * t) ** sp.Rational(2, 3)
Rlayer = sp.simplify(6 * ((sp.diff(a_, t) / a_) ** 2 + sp.diff(a_, t, 2) / a_))
_const_layer = sp.simplify(sp.diff(Rlayer, t)) == 0
print(f"     P15's observable rate a(t) = {a_} : R constant in t = {_const_layer}")
print("     *That object's R does run -- it is simply not P10's slicing.*")
print("  ⇒ ** THE REAL LIMIT IS P10's OWN: the free tower has a FIXED CLASSICAL background; the")
print("     coupled sector quantizes the scale factor, and there is no fixed background there. **")
if not _const_tower:
    fail.append("P10's slicing came out non-constant-curvature — the withdrawal's premise fails")
if Rtower != 12 / alx ** 2:
    fail.append(f"P10's slicing gives R = {Rtower}, not 12/alpha^2")
if _const_layer:
    fail.append("the substituted object came out constant too — the two would then not be distinguishable "
                "and the withdrawal would have nothing to correct")

# =====================================================================
print()
print("=" * 78)
print("PART 5 — AND WHAT P10 NOW SAYS")
print("=" * 78)
P10 = BODIES['canonical_time.tex']
WRITTEN = [
    ("the quartic's degree is named in print", r'diverges quartically'),
    ("⛭ and its counterterm is identified as the one constant",
     r'the counterterm\s+a constant vacuum energy requires is a cosmological-constant term'),
    ("the sub-leading successors are named rather than left implicit",
     r'quadratic and\s+logarithmic successors\} carry the mass'),
    ("the basis degeneracy is stated with its reason",
     r'the counterterm basis is\s+one-dimensional because the admitted background family is'),
    ("⛔ and the scope names P10's OWN slicing, not another paper's rate",
     r'the closed synchronous slicing\s+\$a\(T\)=\\alpha\\cosh\(T/\\alpha\)\$'),
    ("stating that the degeneracy holds on the tower's own geometry",
     r'the degeneracy holds on the very background the free tower\s+uses'),
    ("and the real limit is the coupled sector, where there is no fixed background",
     r'in the coupled sector there is no fixed background to state it on'),
]
for what, pat in WRITTEN:
    ok = re.search(pat, P10, re.I | re.S) is not None
    print(f"  {'OK ' if ok else 'MISSING'}  {what}")
    if not ok:
        fail.append(f"P10 does not carry: {what}")

print()
_now = len(re.findall('counterterm', P10, re.I))
print(f"  'counterterm' in P10's printed body : {_before} before this revision -> {_now} now")
if _now == 0:
    fail.append("'counterterm' is still at zero in P10 — the banking did not happen")

# =====================================================================
print()
print("=" * 78)
if fail:
    print("FAILED: " + "; ".join(fail))
    sys.exit(1)
print("ALL CHECKS PASS — every quadratic curvature invariant on a maximally symmetric background is a")
print("constant times alpha^-4, so the counterterm basis is one-dimensional on the one-parameter family")
print("this framework admits; the two sentences the argument assembles are the corpus's own; P10's own")
print("slicing a(T)=alpha*cosh(T/alpha) has R = 12/alpha^2 CONSTANT, so the degeneracy holds on the")
print("background the free tower uses; and the real limit is the coupled sector, where the scale factor")
print("is quantized and there is no fixed background for a counterterm basis to be stated on.")
print("=" * 78)

# ============================================================================================
# GATE — r2671+c54.210, `L-543`.  ** The danger in this one is over-reading a BASIS DEGENERACY as
# a renormalisability result, so the pins are on the degeneracy being computed and on the scope
# being inseparable from it:
#   (1) the quadratic invariants computed in three dimensions and their alpha-exponent asserted to
#       be exactly 4 -- ** if they differed, the invariants would be independent and the whole
#       argument would invert **;
#   (2) five STANDING-SENTENCE checks.  ** This result is an ASSEMBLY: p0 supplies the invariant
#       fact and the foliation, P12 supplies the scale-only descent.  Remove any one and it is a
#       new claim this line is not entitled to make **;
#   (3) *** the layer's Ricci scalar asserted NON-CONSTANT ***.  ** This is the gate's centre.  The
#       finding is worth having only with its scope attached; a version of this file that quietly
#       dropped the layer would be claiming PO-6 closed **;
#   (4) and its de Sitter limit pinned at 12H^2, so the layer is the right object and not a
#       mis-substituted one;
#   (5) six source checks on P10, including that the SCOPE is in print beside the degeneracy, plus
#       the counterterm count moving off zero -- ** A7's verdict was unbanked for a hundred
#       revisions and the point of this revision is that it stops being **.
#   NOT gated: the sub-leading coefficients themselves.  ** They are not computed here, the layer
#   question is posed and not answered, and PO-6 does not close. **
# ============================================================================================
assert _exps == {4}, "the quadratic invariants are not all alpha^-4"
for what, pat in STANDING:
    assert re.search(pat, ALL, re.I | re.S), f"standing sentence missing: {what}"
assert _const_tower and Rtower == 12 / alx ** 2, "P10's slicing is not the constant-curvature de Sitter one"
assert not _const_layer, "the substituted object is not distinguishable from P10's slicing"
for what, pat in WRITTEN:
    assert re.search(pat, P10, re.I | re.S), f"P10 does not carry: {what}"
assert _now > 0, "'counterterm' is still at zero in P10"
print(f"GATE c54.211 (r2676), `L-543` amended at `L-544`: every quadratic curvature invariant on a "
      f"maximally symmetric background is const*alpha^-4 (checked at D = 4,5,6), so on this "
      f"framework's one-parameter background family the counterterm basis is one-dimensional and one "
      f"constant absorbs any degree; the argument assembles p0's invariant sentence, p0's foliation "
      f"and `L-533`'s scale-only descent, all five checked present; ⛔ and c54.210's scope is WITHDRAWN "
      f"here — P10's own slicing a(T) = alpha*cosh(T/alpha) has R = {Rtower}, CONSTANT, so the "
      f"degeneracy holds on the tower's own geometry and the real limit is the coupled sector — "
      f"pinned against `D2`, cc54's `L809/S1` (A7) and `D50`.")
