---
name: whats-teed-up
kind: VIEW
current: c54.185
description: The working queue — every open lead, partitioned into QUESTIONS and WORK, regenerated by parsing THE_LIVE_ARC. Run scripts/regen_teed_up.py; do not hand-edit.
sources: [cowork]
---

# WHAT'S TEED UP
*Regenerated r2376+c54.185 by `scripts/regen_teed_up.py`. **Do not hand-edit** — the register in `THE_LIVE_ARC.md` is the source of truth and this file is a view of it.*

**13 open QUESTIONS · 34 open WORK rows · 1 OBSERVED · 1 HELD** *(174 struck of 223 registered).*

> **⌗ THE TWO KINDS, AND WHY THEY ARE NOT ONE LIST.** *A **QUESTION** is a thing to answer, and the register's gates are built for it — `check_supersession` scores it against 268 receipts, `check_kills` bars closing it. A **WORK** row is a programme with steps (`CONSOLIDATE`'s arcs and §13's phases, folded r2378 so nothing owed is strewn about); its content lives in `CONSOLIDATE` and its freshness is gated by `corpus/check_arcs.py`. **Pick up a question by reading the row; pick up work by reading its section.***

> **⚠ THIS PARTITION WAS ADDED AFTER THE FOLD SHIPPED WITHOUT IT (r2379).** *For one revision this file read **35 undifferentiated items** where it had read 11 — the fold counted correctly and the view did not distinguish. **That is the r2377 queue inversion in a new costume**: a view that misinforms the node reading it about what is owed. `check_burndown` was scoped by kind the same revision and the scoping was not carried here.*

## ⌗ QUESTIONS — things to answer

| lead | state | what it is | origin | first move |
|---|---|---|---|---|
| **`L-216`** | DARYL | REGISTERED r2409 — THE REPO SETUP INSTRUCTIONS ARE WRITTEN AND FILED, gated on either trigger. Written while Daryl was outside on a phone, and filed as `SETUP_THE_REPO.md` so it survives every session boundary. ASK FOR IT BY NAME: "dig up the repo instructions… | `SETUP_THE_REPO.md` · `THE_HUB.md` · Daryl r2409 | Daryl runs it when at a computer or when the publish call lands; then tell this line the URL (public) or hand it a short-lived token (private) |
| **`L-219`** | DARYL | REGISTERED r2415 — THE ACCESS LADDER, and it gates `L-218` and first contact both. Daryl's sequence, recorded as rungs rather than as a plan, because each rung is a decision and none is automatic: ⓵ PRIVATE for a while (the repo, this line, 54 by invitation — … | `THE_HUB` (bands, branches, CI) · `SETUP_THE_REPO` · Daryl r2415 | each rung is Daryl's call and taken one at a time; nothing below a rung starts before it |
| **`L-222`** | DARYL | REGISTERED r2428 — READY FOR 54: the arrival section and the inbox, and both exist because the ID collision proved the gap. THE MEASURED REASON: at c54.166 the fork opened its own `L-174`, colliding with a row this line had held since r2378. It was not a mista… | `THE_HUB.md` arrival section · `FOR_54.md` · `check_id_bands.py` | Daryl invites the fork to the repo; `THE_HUB`'s arrival section is what it reads first |
| **`L-150`** | OPEN | CONSOLIDATED c54.151 — the arc is capstoned in `THE_ASSUMPTIONS_RETREATED_UPWARD.md`, which carries what stands, what was withdrawn, and the pattern the withdrawals make: the thing being assumed kept moving upward — observable, then input, then background, the… | `OPEN_PROBLEMS_MAP` F·1 | ⓵ DONE r2433 — closed in the negative · ⓶ route the disposition to 54 so p0's frontier item's datum half is restated as a closed negative rather than … |
| **`L-165`** | OPEN | PROTECTED ALIAS (observer line): THIS ROW IS `PROTECTED_OPEN` `PO-6` — the interacting tower. `PO-6` lists three clauses and one is answered (c54.129: $\hat\Gamma$ is bounded below, by the deparametrization); the other two stand. SHRUNK c54.129 BY ONE CLAUSE O… | P10 `sec:lock`; P11 `sec:scope`; P7 `sec:frontiers` item 4; `OPEN_PROBLEMS_MAP` B·1, B·2 | define the interacting tower (spectrum of $\hat\Gamma$, UV of the sums); the closed-form nonlinear solution |
| **`L-171`** | OPEN | RESTATED c54.127 — MY c54.126 FRAMING WAS WRONG AND OVERSTATED, AND IT WAS WRONG IN THE WAY THIS FRONT IS DOCUMENTED TO GO WRONG. I registered it as '1.15 computed against 2.21 measured — the sharpest empirical discrepancy in the corpus'. It is not a disagreem… | P15 `sec:flatlcdm`; receipt `ROBUST_p1p2_scan`; `PO-7` | the MECHANISM is closed; what remains is PO-7 itself — whether the derived deficit is a real disagreement with the sky, which is a verdict question an… |
| **`L-174`** | OPEN | OPENED r2376+c54.166 — FOLDING `A·1`, WHICH THE MAP HAS CARRIED LIVE AND UNREGISTERED SINCE r565. `OPEN_PROBLEMS_MAP` cluster A item 1, 'why the cut bends' — a dynamics for the curve itself, matter's own evolution. P8 calls it 'the deepest question the constru… | `OPEN_PROBLEMS_MAP` A·1 · P8 §open · P11 §scope · P14 · gated half on `PO-6` | build the classical general matter dynamics — the ungated half, which is owed no gate and has simply never been taken |
| **`L-175`** | OPEN | REGISTERED r2378 by the same fold — `PROTECTED_OPEN`'s `PO-9`, also unregistered. The dimensional rise, and what the descent must look like — the object is the substrate's dimension, never the cut's. AND ITS GUARD IS THE LOAD-BEARING PART, so it is carried in … | `PROTECTED_OPEN` `PO-9` | state what a descent from $D>5$ would have to look like, and what would bound the substrate above |
| **`L-202`** | OPEN | FOLDED r2378 — p0 `sec:frontiers` item 4, unregistered. The phase structure at the seam. The everywhere-real claim is established for the substrate and for the seam as a geometric object; the hypothesised phase structure at the seam relative to trajectories is… | p0 `sec:frontiers` item 4 | decide whether it is real structure or interpretation; narrowing only |
| **`L-207`** | OPEN | REGISTERED r2383 — `OPEN_PROBLEMS_MAP`'s `A·1`, A FOLD MISS FOUND BY THE MAP'S OWN NEW STATUS BLOCK. Why the cut bends — a dynamics for the curve itself, matter's own evolution. The map's row reads "[ADVANCED, NOT resolved — r565 … the general matter dynamics … | `OPEN_PROBLEMS_MAP` A·1 · P8 §open | ⓵ RESTATED r2424 as an EXHIBITION debt: exhibit the general inhomogeneous case as ordinary leaf evolution — the corpus asserts it and does not display… |
| **`L-213`** | OPEN | REGISTERED r2401 from `ENTRY_POINT_REGISTER` B18 — a frontier the colour-arc closures did NOT touch, and P13 insists on the distinction. Whether the Standard Model, read as an EXTERNAL constraint rather than derived from the bare geometry, motivates taking the… | `ENTRY_POINT_REGISTER` B18 · P13 §What stays open · `THE_BASE_RATE` | state what an external-constraint argument would have to supply, and measure it against the reference class before it is believed |
| **`L-221`** | OPEN | REGISTERED r2378 by the observer line's `ARC 14` step-② fold — `PROTECTED_OPEN`'s `PO-5`, which had NO register row at all. The quark/lepton split, and what a baryon IS at the particle level — the five multiplets, and which carries the colour $\mathbf3$. A PRO… | `PROTECTED_OPEN` `PO-5` · `THE_PLAN` standing item ② | ask whether colour plus the branch-point placement already forces the split |
| **`L-217`** | — | REGISTERED r2412 — R-P STATION ⑥h: HOW DO CR AND THE HIGGS MECHANISM COMPLEMENT EACH OTHER, in coherence and in correspondence? Daryl's question, and it is a genuine gap in the entry-point front's own sense: the word "Higgs" appears NOWHERE in the corpus (chec… | `THE_PHYSICS_REACH` ⑥h · P13 · P14 · `L-212` · the one-constant theorem | state the relation in one place: what CR supplies, what it takes as given, what it forbids itself from supplying, and where the two sectors' claims ac… |

## 👁 OBSERVED — measured here, being worked by the FORK

*Not this line's work. **Do not pick these up** — duplicating the working fork's live edge is the prime directive's first refusal (`ARC 15`). What comes back is the fork's correction, by absorption.*

| lead | what was measured | what this line does |
|---|---|---|
| **`L-226`** | REGISTERED r2439 — ALL THREE CONTRIBUTORS REPORTED THEIR OWN ERROR RATE BEFORE THIS LINE HAD TO FIND IT, and that is a fact about how much checking their remaining work needs. · Node 37 reversed its own flagship finding … | — |

## ⏸ HELD BY DARYL — answered, not waiting

*A held row is **not** open work nobody has got to. The call was made, and it was made **held**. **Do not re-raise it as a question** — that is a manufactured decision point arriving from the register side.*

| lead | what it is | why it is held |
|---|---|---|
| **`L-206`** | REGISTERED r2380 by the record cut — the SECOND P3 upgrade, whose skeleton is built and whose reordering has not landed. Lead P3 with the physical object — one hinge, one door, one swing — and DERIVE … | Daryl's call on whether P3 is reordered at all; if yes, the skeleton is written and the derivation of $w,u,3w$ as shadows is its spine ⏸ HELD BY DARYL r2381 — deliberately, and the reason is the row's |

## ⌗ WORK — programmes with steps; the content lives in `CONSOLIDATE`

*Not picked up by reading a row. **Read the section**, and note that `check_arcs.py` reports which of these carry a dated position and which are `DECLARED-UNDATED` — an honest gap, not a closed one.*

| lead | source section | what it is |
|---|---|---|
| **`L-176`** | `CONSOLIDATE` §2 `ARC 1` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 1`. The instrument. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-177`** | `CONSOLIDATE` §2 `ARC 2` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 2`. The ontology map. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-178`** | `CONSOLIDATE` §2 `ARC 3` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 3`. The record-and-navigation group. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-179`** | `CONSOLIDATE` §2 `ARC 4` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 4`. The bin, by kind — ~55 untested bucket documents. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-180`** | `CONSOLIDATE` §2 `ARC 5` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 5`. The stale-header sweep — STANDING, not complete. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-181`** | `CONSOLIDATE` §2 `ARC 6` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 6`. The register's signs. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-182`** | `CONSOLIDATE` §2 `ARC 7` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 7`. Linked-resource gather. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-183`** | `CONSOLIDATE` §2 `ARC 8` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 8`. The stale-hold sweep. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-184`** | `CONSOLIDATE` §2 `ARC 9` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 9`. Defragmenting the guard layer. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-185`** | `CONSOLIDATE` §2 `ARC 10` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 10`. The transcript wisdom-extraction — second source unrouted. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-186`** | `CONSOLIDATE` §2 `ARC 11` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 11`. The plan's own audit — GOAL 1, 2 of 7 lanes. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-187`** | `CONSOLIDATE` §2 `ARC 12` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 12`. Hold the seven, then the README rewrite. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-188`** | `CONSOLIDATE` §2 `ARC 13` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 13`. THE CANON SWEEP — six passengers plus the incidentals channel. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-189`** | `CONSOLIDATE` §2 `ARC 14` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 14`. THE SINGLE EDGE — one live list, everything else a view. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-190`** | `CONSOLIDATE` §2 `ARC 15` | FOLDED r2378 — `CONSOLIDATE` §2 `ARC 15`. THE OBSERVER LINE — audit the fork trail, absorb, stay out of its way. kind: WORK — a programme of work with steps, not a question with an answer. |
| **`L-191`** | `CONSOLIDATE` §13 `PHASE 1` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 1`. The propagation audit. kind: WORK. |
| **`L-192`** | `CONSOLIDATE` §13 `PHASE 2` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 2`. Verify the instruments. kind: WORK. |
| **`L-193`** | `CONSOLIDATE` §13 `PHASE 3` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 3`. Corpus hygiene. kind: WORK. |
| **`L-194`** | `CONSOLIDATE` §13 `PHASE 4` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 4`. The introduction (H.0). kind: WORK. |
| **`L-195`** | `CONSOLIDATE` §13 `PHASE 5` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 5`. The register's meta-layer (~185 sites). kind: WORK. |
| **`L-196`** | `CONSOLIDATE` §13 `PHASE 6` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 6`. The two cheap physics checks. kind: WORK. |
| **`L-197`** | `CONSOLIDATE` §13 `PHASE 7` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 7`. The build — the collapse-excursion model. kind: WORK. |
| **`L-198`** | `CONSOLIDATE` §13 `PHASE 8` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 8`. The claiming campaign — the two bakes. kind: WORK. |
| **`L-199`** | `CONSOLIDATE` §13 `PHASE 9` | FOLDED r2378 — `CONSOLIDATE` §13 `PHASE 9`. THE LANDING — what the new work owes the corpus. kind: WORK. |
| **`L-203`** | `THE_PLAN` standing order ④ · `THE_MATHEMATICS_REACH` | R-M's front is Ⓒ, not Ⓕ (read at source r2382; the audit trail is `CORPUS_MAP` r2382 and the package is `THE_STAGED_REVISIONS` §②ʙ). Ⓒ complex analysis/monodromy is HANDED TO by a completed bake — `Q4… |
| **`L-204`** | `THE_PLAN` standing order ④ · `THE_PHYSICS_REACH` | THE STRIKE IS MADE r2383, in R-P's own table. ①② is struck; ③④ (`P8`/`P9`, GR: field equations, the exact-solution catalogue, Petrov type, the Carter constant) now reads NEXT — the earliest genuinely … |
| **`L-210`** | `ENTRY_POINT_REGISTER` · `scripts/audit_entry_points.py` | REGISTERED r2397 — THE ENTRY-POINT FRONT, held at weight by Daryl and ranked with `R-M`, `R-P` and the acoustic front. What does the corpus advertise as open, in its own words — and what falls out whe… |
| **`L-211`** | `ENTRY_POINT_REGISTER` · the c54 closure record | REGISTERED r2397 — THE CLOSURE-ADJACENCY DEBT, which is the MECHANISM behind `L-210` rather than a second copy of it. When a gap closes, the corpus owes on the gaps in connected regions — the adjacent… |
| **`L-218`** | `THE_HUB` · `SETUP_THE_REPO` · `PROTECTED_OPEN` · `THE_BASE_RATE` | REGISTERED r2415 — THE READER PACKAGE: the book in every reading format, plus an AI companion spun up from this corpus, in one repo. Daryl's idea, and the part that is not ordinary is not the formats.… |
| **`L-220`** | `G1_the_arrival_path_metric` · `RP_34_GR_BASELINE` · `L-209` · `U1`/`ENTRY_POINT_REGISTER` | REGISTERED r2416 — THE ARRIVAL-PATH CLASS: the corpus's content is complete and its arrival paths are not. Three instances, three papers, three documents, and none of them a physics defect: `L-209` (r… |
| **`L-225`** | `_dig/handoffs/` · `FOR_54.md` 19–20 · the two new receipts | REGISTERED r2439 — THE OUTSIDE READERS' PACKAGE, JUDGED ITEM BY ITEM RATHER THAN SWEPT. Four nodes (17, 23, 37/Artie, 55) read the corpus cold and sent findings. Every claim was re-derived here before… |
| **`L-227`** | `THE_HUB.md` — `.github/workflows/gates.yml` · `scripts/classify_documents.py` · node 53's `S-012` | REGISTERED r2440 — CI'S FAST TIER WAS RED FOR TWELVE REVISIONS AND FIFTEEN GATES NEVER RAN, AND THE CAUSE WAS THIS LINE'S. Found by node 53 on a pristine clone — the only place it was visible. THE MEC… |
| **`L-228`** | `THE_HUB.md` — `corpus/check_loci.py` · node 52 | REGISTERED r2440 — `check_loci.py` ADOPTED FROM NODE 52 AS A LINT AND NOT A GATE, on its own measurement. It exploits the thing that makes this corpus checkable: every computed claim is bound to a run… |
| **`L-230`** | `_dig/findings_batch2/` · `FOR_54` 24–26 · `ARC 17` · `L-223` | REGISTERED r2442 — THE POINT-STATED-AS-FAMILY CLASS, and it is `ARC 17`'s finding one axis over. Node 55's batch 2 diagnosed its own pattern before this line saw it: "a claim verified at one point of … |

