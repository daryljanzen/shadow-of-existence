# Lapse, Shift, and the Relativity of Synchrony — a reconciliation

**What this is.** A recording and a *first* formalization of the synthesis Daryl stated this session — *the
lapse is Newton, the shift is Einstein, and they have never been reconciled with the relativity of synchrony*
— together with the vicious cycle it diagnoses and the reconciliation that breaks it. Daryl's measure: this is
the clearest and most stripped-down he has ever gotten it. The aim is to flesh it out toward the papers, and
possibly into **P7** if we get it right.

**Status / caution.** This is a **first formalization**, drafted at compression #5 (caution maxed). The

> **BUILT INTO P7 AS A CORPUS-SETTLED PROPOSITION (c19, 2026-06-17).** Upgraded past the solid-floor
> remark, per Daryl's integrity correction (the REACH must be settled *by the corpus*, not asserted): a
> Proposition *[Lapse, shift, and the relativity of synchrony]* + proof + remark now stands in
> `corpus/CR_flatLCDM_v2.tex` §Ontological Simultaneity. The proof assembles the programme's existing
> results — the shift's foliation-gauge freedom (description groupoid, \cite{JanzenGroupoid}); the
> existent-fixed cosmic foliation (horizon-selected orientation \cite{JanzenBHcausality}, fixed by the
> ontology the gauge is defined over \cite{JanzenCanonicalTime}); deparametrization to the true
> Hamiltonian, frozen vs true = same content differing only in granting $M$ existence
> \cite{JanzenCanonicalTime}; the structure function = coset-metric = problem-of-time obstruction,
> deparametrizing on the preferred foliation \cite{JanzenAlgebroid}). Two new bib entries added to P7
> (JanzenCanonicalTime, JanzenAlgebroid); all cites resolve; envs balanced. **Held at weight in the
> remark, not smuggled into the proof:** the *ontologically-absolute* simultaneity inference (vs a merely
> preferred cosmological frame) is the framework's defining inference, adopted at weight; and the
> block-error's meta-lapse / fifth-dimension as a *formal* necessity (the formal counterpart of the 5D
> trope) is stated but its full formalization left to refine during BST. So §5's P7 placement is **built
> as a theorem**; §6's reach items remain the refinement targets. Stated for reversal.
conceptual content is Daryl's, held at full weight as his understanding of his own construction. The ADM
correspondences below are standard and solid; the precise *theorem-shaped* statement of the reconciliation, and
its placement in P7, are **reach** — to be refined from a fresh head and **verified against P7 and the CR
construction when those are deep-read**. Stated for reversal throughout.

---

## 1. The synthesis, in Daryl's words (recorded at weight)

> The shift *is* relativity — that is what relativity actually contributes. The lapse is Newton. And the shift
> and lapse have been misunderstood far too long: they have not been reconciled with the relativity of
> synchrony. That is the simplest statement of the issue.
>
> Lapse is needed for existence. Existence breeds spacetime as its map. The shift removes objective now from
> the map. The map as a whole gets reified, and in comes the lapse again, smuggled in at the end of the loop.
> It is a vicious cycle if you let it be.

The corollary, from the arc: the now never lived in the map. It lives in **existence** — the evolving layer and
its cosmic time, where the lapse legitimately belongs. The shift was *right* to strip the now from the map; the
error is reifying the map and re-importing the lapse to put the now back (the meta-time / fifth dimension of
*Time Travel: A Five-Dimensional Trope*).

---

## 2. The ADM correspondences (solid)

The 3+1 line element, with lapse `N` and shift `Nⁱ` on a spatial slice with 3-metric `h_ij`:

> `ds² = −N² dt² + h_ij (dxⁱ + Nⁱ dt)(dxʲ + Nʲ dt)`

and the time-flow vector decomposes as `∂_t = N n + Nⁱ ∂_i`, with `n` the unit normal to the slice.

- **Lapse `N` = Newton = existence.** `N` sets the proper time of the normal (Eulerian) observer per unit
  coordinate time, `dτ = N dt`: it is the *clock* — duration, being-that-lasts. This is exactly *5D Trope*'s
  "existence is what the clock measures." In the comoving cosmological case `N = 1`: cosmic time **is** the
  comoving proper time — Newton's absolute time, restored not as a prior arena but as the measure of the
  evolving layer's existence.
- **Shift `Nⁱ` = Einstein = the relativity of synchrony.** `Nⁱ` is the tilt between the coordinate-time flow
  `∂_t` and the slice normal `n` — how the spatial frame is carried from one slice to the next, i.e. *which*
  later events count as "the same place, later." Re-choosing the shift re-chooses what is at rest and what is
  synchronous: the shift is the formal carrier of the synchronization/rest convention. This is precisely the
  *Wild Goose Chase* relativity of synchrony (one-to-one with the choice of rest), and it is what relativity
  actually contributes to the split.
- **The lapse and shift are non-dynamical** (Lagrange multipliers enforcing the Hamiltonian and momentum
  constraints): freely specifiable. This *is* the foliation gauge freedom — **the bare 4-geometry does not
  single out a preferred slicing.** That is the formal content of "the shift removes objective now from the
  map": in the geometry alone, no foliation, hence no "now," is preferred.

## 3. The vicious cycle, formalized

1. **Lapse needed for existence.** To have existence as duration at all, one needs the lapse `N` — the
   proper-time structure of an evolving slice. (Existence = the layer 𝒮ₜ with its clock.)
2. **Existence breeds spacetime as its map.** The foliation {𝒮ₜ} with lapse and shift reconstructs the
   4-geometry (the ADM reconstruction). Spacetime is the *record* of the evolving layers.
3. **The shift removes objective now from the map.** Once the bare 4-geometry is in hand, the lapse/shift are
   gauge — *any* spacelike foliation is admissible. The geometry alone fixes no preferred now (the relativity
   of synchrony, formalized as the foliation freedom).
4. **The map is reified.** The 4-geometry — which has no preferred now — is mistaken for the existent (the
   block universe).
5. **The lapse is smuggled back in.** To give the reified block "existence," one needs a clock for *it* — an
   evolution of the whole 4-block along an external parameter: a meta-lapse / meta-time. That is the fifth
   dimension of the trope, re-importing at the end exactly the lapse that step 3 had (correctly) banished from
   the map.

The cycle is vicious because the now, correctly expelled from the *map* at step 3, is illegitimately
re-introduced by reifying the map (step 4) and re-smuggling the lapse (step 5).

## 4. The reconciliation (the break) — reach, for reversal

The objective now is **not** a feature of the bare 4-geometry; relativity (the shift / foliation gauge freedom)
is *right* that the geometry singles out none. The objective now is supplied by the **existent** — the matter
distribution / cosmic rest frame, empirically the CMB (*Wild Goose Chase*, piece 2): a physically preferred
comoving foliation (`N = 1`, `Nⁱ = 0` for comoving observers; a non-comoving observer carries `Nⁱ ≠ 0`).

So the reconciliation is a *separation of where each lives*:

- the **lapse** (cosmic time, the objective now) belongs to **existence** — fixed by the matter/CMB rest frame,
  not by the geometry;
- the **shift** (relativity of synchrony, the foliation gauge freedom) is relativity's true statement about the
  **map** — the bare geometry has no preferred now;
- and these do not conflict, because **the now comes from the existent, never from the map.** The century-old
  misunderstanding is forcing both lapse and shift to live in the spacetime map (where step 3 vs step 5 fight);
  CR puts the lapse in existence and lets the shift be relativity's contribution to the map. The cycle stops at
  step 4: do not reify the map, and there is nothing to smuggle.

## 5. Connection to P7 (reach — verify on the P7 deep read)

P7 (`CR_flatLCDM_v2.tex`) carries the comoving congruence / objective cosmic rest frame (CMB) and the layered
construction. The synthesis formalizes *why* that comoving cosmic time is the objective now and *how* it is
consistent with relativity: the preferred foliation is supplied by the existent (the matter rest frame), while
the geometry's foliation freedom (lapse/shift gauge) is relativity's correct statement that the bare map fixes
no now. If this holds up against P7's actual construction, it is a candidate to state there — the reconciliation
of the ADM lapse/shift with the relativity of synchrony, grounded in the existence/occurrence ontology.

## 6. Marks

- **Solid:** the ADM definitions; lapse = proper-time clock = the comoving cosmic time (`N=1`); shift = the
  synchronization/rest convention = the relativity of synchrony; lapse/shift non-dynamical = the foliation
  gauge freedom = "no objective now in the bare geometry"; the comoving/CMB foliation as the physically
  preferred slicing supplied by the matter distribution.
- **Reach (for reversal, verify against P7 / the thesis):** the precise theorem-shaped statement of §4 (e.g.
  "the bare 4-geometry admits a foliation gauge orbit that the matter/CMB rest frame breaks, selecting the
  cosmic-time foliation as the objective now"); the §5 P7 placement; and any claim stronger than "the
  cosmological evidence *supports* the preferred frame" (piece 2's abductive bound still applies — a
  cosmologically preferred frame is standard; reading it as *ontologically* absolute simultaneity is the
  CR-defining inference, held at weight, not proven).
- **Not claimed:** that this is finished or P7-ready as written; it is a first formalization to refine.
