---
name: dimension-deepening-audit
description: What the four-dimensionality result owes the reach programme — every R-M and R-P bake, ledger and probe re-read against it, with a verdict for each and the re-runs that earned them. Read this before trusting any bake's constants as structural.
sources: [cowork]
kind: STATE
current: c54.163
job: What the four-dimensionality result owes the reach programme — every R-M and R-P bake re-read against it, with a verdict for each. Read BEFORE trusting any bake's constants as structural.
---

# THE DIMENSION DEEPENING AUDIT — the reach programme, re-read

> **⌗ CHECKED AGAINST c54.163 AND DECLARED CURRENT r2394.** *The fork changed **none of these eight**
> documents in the c54.153→c54.163 span, so the question was not "what moved in the file" but* ***"did
> anything the span withdrew bear on what this file says"***. *The span's one withdrawal is the **species
> selection rule** (c54.162): the crossing is* ***lossless for every species rather than for cold ones
> alone***, *because the kernel acts on oscillatory content and nothing arrives oscillating on the
> progenitor's interior.* ⇒ **This file does not carry that claim, so the check clears it rather than
> correcting it.** ⌗ *One of the eight — `STATE_programme` — did carry it and was corrected.*


> **⌗ ⌗ r2376+c54.30 — THE FOLD STEP IS NOW SEAT-INDEPENDENT.** *`L-67`/`L-07`: the marginally-bound law at general $D$ is $r^{D-1}=2M\alpha^2\sinh^2((D-1)\tilde\tau/2\alpha)$, deck group $\mathbb{Z}_{D-1}$, so **the turnaround fold is $D-1$ as the horizon fold is** — and both are $D-1$ because **both descend from $f$'s degree-$(D-1)$ numerator**, one as its zero set and one as the deck action of the integral of $1-f$.* ***So the fold is a property of $f$, not of which structure reads it: if the L8.1-a weld splits and generation moves to the turnaround three, the count is $D-1$ either way and the dimension argument is untouched.*** *(`P03_the_weld_and_the_two_folds`.)*


> **⌗ ⌗ r2376+c54.29 — (A1) IS DISCHARGED, AND THE DIMENSION RESULT'S FOUNDATION IS NOW DERIVED.** *`L-01`, the oldest debt in the register.* ***CR's operator is not a metric function***: *P8 `thm:kernel` makes the vacuum sector the **kernel of the matter functional** on a cut in the construction gauge, and at general $D$ that condition is $rf'+(D-3)(f-1)+2\Lambda r^2/(D-2)=0$ — P8's own equation at $D=4$ — whose **entire** solution space is $f=1-2M/r^{D-3}-r^2/\alpha^2$ with $\alpha=\sqrt{(D-1)(D-2)/2\Lambda}$ and $M$ the single constant of integration.* **So $2M=r_0^{D-3}-r_0^{D-1}$ is that $f$ evaluated at a root, and the collapse argument, the fold being $D-1$ and the mass parity all run on a function the construction GENERATES rather than borrows.** *Two inputs remain: the **lock** (`L-89`) and the **round** transverse space (`L-90`). (A2) is untouched. (`P03_operator_at_general_D`; landed in P3 and p0.)*


> **⌗ r2376+c54.28 — A SECOND DIMENSION SELECTOR, INDEPENDENT OF THE HARMONIC COLLAPSE.** *`L-53` generalised the hinge figure to a regular $(D-1)$-gon at $R=\alpha/\cos(\pi/(D-1))$ and found the null structure is **dimension-blind**: no same-horn chords at any $D$, and opposite-horn chords at $\pm$one station step at every $D$.* ***But a bound triple is a SINGLET only if it EXHAUSTS the stations, and exhaustion holds at $D=4$ ALONE*** *— three stations, and a triple is three; from $D=5$ the triple is a proper subset.* **So the corpus now has TWO independent dimensional selectors: the harmonic collapse (from the horizon relation) and exhaustion (from the binding structure). Neither uses the other's input.** *(`P03_batch1_cheap_owed`; not yet propagated to the papers.)*

*Step 2 of `THE_LIVE_ARC.md`, r2376+c54.11 (Daryl-directed). **P6 is deliberately split
off to the next step.** The deliverable is of the shape `THE_GEOMETRY_AND_THE_PHYSICS` sets for a bake: the
argument in order, every step a re-derivation, and the bounces recorded as data.*

## ⌗ THE ONE SENTENCE THIS DOCUMENT TURNS ON

**The bakes were run against a four-dimensional cut, and nothing in the corpus then distinguished
*structural* from *true at $D=4$*.** So a constant that came out clean was read as forced. The dimension
result supplies the missing distinction, and every probe whose verdict rests on a $D=4$ number is owed a
re-run — *not an annotation.*

$$\textbf{A verdict wider than its computation's scope.}$$

*That is the failure class `FIGURE_THEOREM_LEDGER`'s own r1110 audit names — **"not a rigged question and not a
fabricated theorem"** — and says is **"the check to run on every future ⊢ here."*** **This audit runs it on
the past ones, along the one axis nobody could see at the time.**

---

## ⌗ WHAT THE RE-RUN FOUND — six probes, and they do not all go one way

*Receipt: `computations/dimension_question/reach_probe_deepening.py`. Each section re-derives the probe's own
computation at general $D$.*

| probe | its verdict as it stands | after the re-run |
|---|---|---|
| **`OPTICS` O1** | *"the photon sphere IS the merged horizon at Nariai — P7's tangency condition and the photon-sphere condition are ONE condition"* | ***STRENGTHENED.*** $r_{\rm ph}=r_N$ **in every dimension tested** ($\sqrt3/3,\ \sqrt2/2,\ \sqrt{15}/5,\ \sqrt6/3,\ \sqrt{35}/7$ at $D=4\ldots8$). *So it is a statement about the degenerate horizon **as such**, not about the cubic. The ledger could not know this and may now say it.* |
| **`CATEGORY` K8b** | the dimension-7 orbit stratum is **empty** because *"$\so(4)$ already uses all four spacelike directions"* | ***GROUNDED.*** *The bound is $\so(4,1)$ — the stabiliser of a **four-dimensional cut**, dimension 10 in every $\dS_D$. With the cut's four now derived, the emptiness proof stops being a coincidence of counting.* |
| **`CATEGORY` K5** | Nariai's six faces are one condition; the sixth is the near-horizon $\dS_2\times S^2$, isotropy *"six-dimensional against a generic four"* | ***NARROWED.*** *The **enhancement is $+2$ in every dimension** — the mechanism survives — but the pair $(6,4)$ is $D=4$ ($D=5$ gives **9 against 7**, $D=6$ **13 against 11**). The face should be stated as the enhancement, not the numbers.* **⚠ And this line caught an error in its own computation**: a first pass used $\so(D-2)$ for the near-horizon sphere $S^{D-2}$ where its isometry is $\so(D-1)$, returning $(4,2)$ at $D=4$ and contradicting K5's own stated $(6,4)$. *Checked against the ledger's numbers before propagating; the $+2$ was unaffected, the table was not.* |
| **`OPTICS` O6** | $2f-r^2f''$ is *"**the constant 2** for every $r$ and every $M$"*, so $\Omega_c=\lambda$ and the ringdown's shape is *"universal across the whole family, independent of $M$ and $\alpha$"* | ***SCOPE WIDER THAN ITS COMPUTATION.*** *At $D\ge5$ the combination is $2+2(D-2)(D-3)M/r^{D-3}$ — **$r$-dependent**. The claim is true and its stated universality is not.* |
| **`QUADRIC` Q4 | *"three points $120^\circ$ apart with their centre are equianharmonic **AUTOMATICALLY** … this names a consequence of a forced fact and **does not add one**"* | ***THE SELF-DISCOUNT IS TOO STRONG.*** *A cross-ratio is an invariant of **four** points and the vantage set has $(D-1)+1=D$ of them — so it is a cross-ratio configuration **precisely at $D=4$**. And where a four-point subset exists at $D=5$ it is **harmonic** ($\lambda=2$, $j=1728$, CM by $i$), not equianharmonic ($\lambda=e^{i\pi/3}$, $j=0$, CM by $\omega$). **Q4's chain to the corpus's own $\mathbb{Z}_3$ points the other way at $D=5$.*** |
| **`QUADRIC` 4d** *(untried queue)* | *"CR has three hinges and a centre; **is that a polar simplex?**"* — never run | ***ANSWERED, twice over.*** *The hinge's polar sits at $\alpha/2$; the opposite side sits at the inradius $\alpha$. **NOT self-polar** — a computed negative. And a simplex in the plane needs **three** points, so the question **only exists at $D=4$**.* |

**⛔ AND THE DISCIPLINE THAT GOVERNS ALL SIX.** *Every dimension statement above descends from **one** fold.
Counting them as six confirmations would be exactly the trap `FIGURE` ⊢54 records:* ***"at Nariai there are two
rulings and two colliding roots. The count matches and the match means nothing. A count is not a map."***
**They are one fact wearing six faces, which is this corpus's own favourite shape and its own favourite error
depending on whether the mechanism is shown.** *Here it is shown: the fold is $D-1$.*

---

## ⌗ THE SITES THIS RESULT DEEPENS, ledger by ledger

### ① `FIGURE_THEOREM_LEDGER` — the densest, and it holds the corpus's only retraction on exactly this

**⊢8** — *"The 3 hinges, $120^\circ$ apart | forced — **three roots of one cubic** | (P3)"* — **is the only
Part-I row whose receipt is a citation rather than a computation**, and it is the row that asserts the
three-ness. **⊢30** — *"$12=3\times4$, **BOTH FACTORS FORCED, BY ONE ROUTE**"* — forces the 4 (the sine's own
quarter) and **inherits** the 3 from the triple angle.

$$\textbf{The 3 is } D-1 \textbf{ and the 4 is } D. \qquad 12 = (D-1)\times D \text{ at } D=4.$$

**⌗ AND IT BEARS ON THE LEDGER'S ONE RETRACTION.** *`thirty_degrees.py`'s verdict — "it **rhymes** on its 3 and
**coincides** on its 4" — was retracted because **"it asked 'which $n$ closes the stamp?' instead of 'what does
the hinge's own sine do?' — it tested the figure and never asked the hole."*** ***The retraction was right and
stays right.*** **What is new is that the conclusion it foreclosed — that the 3 and the 4 are related — is
reachable by the route the retraction demanded**: from the hole's own relation $2M=r_0^{D-3}-r_0^{D-1}$. *A
bad argument for a claim is not evidence against the claim, and the corpus is entitled to notice when the good
argument turns up.* ⚠ **Not a reversal: `thirty_degrees.py`'s reasoning is still bad and the row stays
retracted.**

**⊢54 and R1 — the highest-value pair, because the result lands on their conjunction.** *⊢54 establishes that
**the rulings realise exactly the part of $D_3$ that is a substrate isometry, and nothing else**, arriving
independently at P5's gauged/global split: **the orientation $\mathbb{Z}_2$ IS an isometry so chirality
descends gauged; the Weyl $S_3$ is not, so flavour is global.*** **R1 states the factorisation
$\mathrm{Aut}(A_2)=S_3\times\mathbb{Z}_2$ as two independent facts that happen to factorise.**

***They factorise at $D=4$ and nowhere else.*** *The $S_3$ needs the fold, which exists at $D=4,5$; the
$\mathbb{Z}_2$ needs $2M$ odd in the signed offset, which needs $D$ even. **At $D=5$: a four-fold and no
parity. From $D=6$: neither.*** **So the Standard Model's own arrangement — gauged chirality against global
flavour — is available at one dimension.**

**⊢59, ⊢62 — the mass-parity is on the page twice and unnamed.** *Both write $2M=r_0-r_0^3$ and
$2M=\tfrac{2}{3\sqrt3}\sin3w$; **both are odd**; the ledger never says so. That oddness is the parity, and it
is what fails at odd $D$.* **And $2/\sqrt3$ appears in ⊢62 as an $r_0$ value and in `OPTICS` O3 as the
*slicing scale*, with no ledger connecting them** — one number, two faces, and now a $D$-indexed family
($c=1$ at $D=5$).

**R3 — the ledger's own open generations row, blocked on a missing reason.** *"**UNTOUCHED — and ⚠ ON AN
UNSOURCED REASON.** … the reason is this table's own assertion, **sourced to nothing**. To promote or kill it,
the reason must first be derived or withdrawn."* ***This result is a candidate reason, and R3 is the single
most actionable row in that ledger.*** **Not claimed here** — naming a candidate is not supplying one, and
R3's own rule is that the reason must be *derived*.

### ② `OPTICS_LENSING_LEDGER` — O3 is the $D=4$ case of the collapse argument

*O3 already computes the whole mechanism:* **"with $r_0=R\sin w$, the relation $2M=r_0-r_0^3$ matches a pure
$\sin3w$ only when $R/3=R^3/4$, i.e. $R=2/\sqrt3$, **uniquely**"** *— and projects the harmonics numerically:*
**"at $R=2/\sqrt3$ the $\sin w$ coefficient vanishes identically and only $\sin3w$ survives … at every other
charting distance BOTH harmonics are present."**

***That is the collapse argument, one level down.*** *O3's uniqueness is over **charting scales at fixed $D$**;
the new statement is the same uniqueness asked over **dimensions**, and it says the uniqueness exists at all
only for $D\in\{4,5\}$.* **O3's conclusion — "$r_{\rm obs}=\sqrt7/2$ is neither an observer's position nor a
free gauge: it is the distance at which the family's own harmonic structure becomes legible" — is untouched
and now has a companion**: *there is a dimension at which that structure exists to be made legible, and it is
two.* **⚠ And O3's harmonic basis is pure sine because $2M$ is odd — the parity again, unremarked.**

### ③ `QUADRIC_GEOMETRY_LEDGER` — Q4 re-weighted, Q5's unnamed parity, and the queue closed

*Q4 above.* **Q5** — *"the locus is ONE cubic curve … that is **REDUCIBLE** — a **line** union a **conic**"*,
and *"solving line $\cap$ conic gives $r_0=\pm1/\sqrt3$ — **THE TWO NARIAI CONFIGURATIONS**."* ***The $\pm$ is
the offset parity acting, and the ledger records two configurations without naming the $\mathbb{Z}_2$.*** *And
$1+2$ is degree-3 arithmetic: at general $D$ the locus has degree $D-1$ and there is no reason to expect a
line component at all.* **`4d`'s untried polar-simplex entry is answered above.**

**⌗ AND ONE CANDIDATE IDENTIFICATION NOBODY HAS ASKED.** *§2c: the substrate is $\dS_5$ and **not** its
projective quotient, because* **"projective means antipodes are identified, $X\sim-X$. **CR does not identify
them, and the reason is physical** … the oriented future horizon distinguishes them."* ***An antipodal
$\mathbb{Z}_2$ that CR refuses to quotient by, justified by orientation — and the offset parity is an
orientation $\mathbb{Z}_2$ that exists only in even $D$. Whether they are the same $\mathbb{Z}_2$ read at two
levels is unasked.*** **Recorded as a question, do-not-assert.**

### ④ `CONFORMAL_GEOMETRY_LEDGER` — C4 owns the discharge, and does not know it

*`C4` is the bake's defining bounce:* **"P3 forces the GNOMONIC … the gnomonic is not conformal … CR forces the
projection that preserves STRAIGHTNESS over the one that preserves ANGLES. The celestial sphere is used
PROJECTIVELY, not conformally — and that is WHY the conformal apparatus finds no purchase."**

**The c54.9 discharge is `C4`'s own theorem generalised**: *the straight-line criterion is dimension-independent
(verified on $S^n$, $n=2\ldots8$; the celestial sphere in $D$ dimensions is $S^{D-2}$).* ***So `C4`'s verdict
strengthens from "CR is projective, not conformal" to "CR is projective, not conformal, **at every
dimension**" — and the split sharpens: the CHART carries no dimension dependence, the COLLAPSE it charts
carries all of it.***

### ⑤ `CATEGORY_THEORY_LEDGER` — K2, and the two zeros are reopenable

*K2:* **"NOT FAITHFUL … the three roots of one horizon cubic all return the same $2M$, so **three cuts, one
geometry** — and the failure of faithfulness IS the vantage multiplicity."* ***The fibre multiplicity of
$\Psi$ is the fold, hence $D-1$: four-to-one at $D=5$, and from $D=6$ not a finite multiplicity at all,
because there is no fold.***

**⌗ AND THAT BEARS ON THE LEDGER'S CLOSE.** *It reports* **"③ THE CENSUS PASS — ZERO … ④ THE `R2` PASS —
ZERO"** *and draws the meta-lesson that* **"the fields that explain are the ones whose objects the corpus
already built."* ***A dimension-conditioned fibre multiplicity is new content rather than a renaming, so the
zeros are reopenable — not reopened here.*** **⚠ And a word collision, entered per that ledger's own rule of
entering them *before* the bake:** *K3 uses **generation** in the group-theoretic sense (*"the discrete
generation P5 proves is what makes the quotient computable"*). A result whose headline is a **generation
count** collides with it.*

### ⑥ `COMBINATORICS_LEDGER` — done at c54.10, and it was the control all along

*Already landed: L8.1-a's "one 3" is a "one $D-1$", and that entry's own control — **"break the forced scale
$2/\sqrt3\to1$ and the match collapses to 0.5"** — was the whole story read as a sanity check.* **⚠ Noted
here because $c=1$ is also the $D=5$ collapse scale, and that is a coincidence of numbers, not a connection:
the polynomials differ. Recorded so a later reading does not weld them.**

---

## ⌗ WHAT THIS DOES TO THE TAXONOMY — and it is the largest single item

`GEOMETRY_PHYSICS_TAXONOMY` closes on a standing instruction:

> **The obsession (open, drill hardest):** *why does the SdS geometry produce a zero-sum triple **with a
> $\mathbf3/\bar{\mathbf3}$ parity doubling**, and does that reason bridge to $\su(3)$? Not understood —
> therefore not asserted either way, therefore the thing to obsess over.* **Held open.**

***The first half now has an answer, and it is an answer to the CONJUNCTION, which is what the obsession
actually asks.*** *Not "why three" and "why a parity" separately, but why **both at once**:*

- the **triple** is the fold, and the fold is $D-1$, and it exists at all only where one slicing scale can
  remove the residual harmonics — $D\in\{4,5\}$;
- the **parity doubling** is $2M$ odd in the signed offset, which holds iff $D$ is even;
- **so the conjunction is available at $D=4$ and at no other dimension.**

**⌗ AND IT MOVES A TAG, which is what that document exists to do.** *Its discriminator:* **"A bare
number-match ('three = three') is numerology. A **structural mechanism** that forces the number *and* a
symmetry/relation/further consequence has teeth."** *The mechanism forces the number ($D-1$), forces the
symmetry (the parity, at even $D$), and carries a further consequence (the conjunction at one dimension).*
⇒ **the three-root/$D_6$ row's ground moves from an alignment held at do-not-assert to a mechanism with a
stated reason.**

**⛔ AND THE SECOND HALF OF THE OBSESSION IS UNTOUCHED.** *"…and does that reason bridge to $\su(3)$?"*
**It does not, and this result does not help it.** *What the dimension excursion did to the colour question is
recorded in `STATE_matter_sector`: the obstruction **moved** — from "$\su(3)$ cannot act internally at all"
(true at $\dS_5$) to "**what reduces the normal structure group $\so(6)\to\su(3)$?**" — and the three levels
the taxonomy separates (**skeleton grounded · resemblance do-not-assert · identification walled**) are
**unchanged**.* ***Nothing here licenses the hat.***

---

## ⌗ WHAT THIS DOES TO THE EXEMPLAR

`THE_GEOMETRY_AND_THE_PHYSICS` opens: *"Every step is an identity, a definition, or a verified computation.
**Nothing is chosen.** There is one input and the input is the unit."* And it closes its central chain:
**"$12=3\times4$, both factors forced. The 3 by the triple angle. The 4 by the sine's own quarter."**

***The 3 is forced BY the triple angle, and the triple angle is not itself derived there — it is inherited,
because the metric function is written in four dimensions.*** **So the document has one silent premise beside
its one input, and it is the dimension.** *This audit does not claim the exemplar derives it — the exemplar's
chain **is** the $D=4$ chain. What is new is that the premise is not free: the matter sector selects it.*
**"Fourteen objects. One input." now reads with a second clause: and one number that is not free either.**

---

## ⌗ WHAT THE REACH PROGRAMME OWES, as a state

**⌗ THE STATIONS.** *R-M's* **Ⓑ — the forced triple angle** *— was closed at r1880 as* **"⌫ NOT A GAP"**, *on
the finding that the corpus held it already in `prop:triple`'s own proof.* ***That finding was right and the
station is now reopened one level up***: **`prop:triple` proves the scale is unique at $D=4$; it does not ask
at which $D$ a scale exists.** *The r1885 precondition — **"check the papers before opening any of them"** —
is honoured: the papers were checked, they hold the $D=4$ statement, and the general one is new.*

**⌗ THE TRENDS META-MAP gains its sharpest entry.** *R-M records:* **"THE CUBIC — recurs across Galois ($S_3$),
algebraic geometry (the cubic curve / discriminant), catastrophe theory (the fold). The horizon cubic
$2M=r_0-r_0^3$ is **one object seen by three fields**."* ***A fourth reading is now available and it is of a
different kind: the cubic's DEGREE is the spacetime dimension minus one.*** **The other three read the cubic;
this one reads *why it is a cubic*.** *On the map's own rule — recurrence marks a fruitful direction — the
cubic is now the most-recurring object in the corpus and the only one with a stated reason for its own shape.*

**⌗ AND THE CONSTANTS-ARE-STRUCTURAL HABIT IS A STANDING RISK, now named.** *`OPTICS`'s own standing
discipline ⑥ was earned by exactly this failure one axis over:* **"every imported optics formula is re-derived
on $f=1-2M/r-r^2/\alpha^2$, because the standard results assume asymptotic flatness."* ***The direct analogue
is now owed and is stated as a discipline for every future bake:***

$$\textbf{Before a constant is read as structural, re-derive it on the } D\textbf{-dimensional } f.$$

*O6 is the first casualty and it will not be the last. **The rule costs one line per probe and buys the
difference between a theorem and a coincidence.***

---

## ⌗ THE COVERAGE REGISTER — r2376+c54.13, and the line is drawn here

*Step 2 re-ran **six** probes and declared a discipline corpus-wide. That was a sample. This is the discipline
run, with the denominator stated.*

| | count | what it means |
|---|---|---|
| **enumerated** | **98** | every labelled probe in the six field ledgers plus every ⊢ row, rhyme and unrun entry in the figure ledger |
| **screened EXPOSED** | **49** | the verdict rests on a quantity computed from $f$, a count tied to the fold, or the parity |
| **RE-DERIVED at general $D$** | **49** | ***all of them***, in five families — A hinge/Euclid · B the degenerate horizon · C the fold · D the optics constants · E the monodromy |
| **screened CLEAN** | **39** | no such quantity in the verdict. ⚠ ***This is a SCREEN, not a re-derivation***, and the criterion is stated above so it can be disputed |
| **uncapturable by the screen** | **0** | *three looked missing and were artefacts of the regex: `K8b` sits inside `K8`'s line, `X2` is written `X2 → X2r`, `Q6r` is a cross-reference* |

**⌗ THE VERDICTS, all 49, by outcome.**

| outcome | probes |
|---|---|
| **STRENGTHENED** *(the verdict was narrower than the truth)* | `O1` · `O4` · `O5` · the `D4` fold exponent · **`prop:tangentnull` / power-of-a-point $=$ height²** |
| **GROUNDED** *(a counting coincidence becomes a consequence)* | `K8b` |
| **NARROWED** *(mechanism survives, stated numbers are $D=4$)* | `K5` · `Q4` · `X5` · ⊢2's three triangle faces · ⊢33/⊢67 |
| **SCOPE FIXED** *(a verdict wider than its computation)* | `O6` |
| **ANSWERED from an untried queue** | quadric `4d` (the polar simplex) |
| **KILLED, on a derived ground** | the antipodal-$\mathbb{Z}_2$ identification *(a bounded question: are two named involutions the same element? $\det+1$ and time-reversing vs $\det-1$ and time-fixing — no)* |
| **⛔ A KILL WRITTEN AND RETRACTED** | `R3`. *A computation about the **sign** $2{+}1$ was used to close a row that names no particular $2{+}1$ — and P14 names two in one sentence, the other being $\sigma$'s **designation** $2{+}1$, a flavour-side structure. **The cyanide face. R3 and the Nariai/three-quark lead both stand OPEN, and wider than before.*** |
| **ADJUDICATED AND STANDING** | the category bake's two ZEROS |
| **UNCHANGED** *(re-derived, no movement)* | the remaining 34 exposed probes — chiefly the hinge/Euclid cluster's dimension-independent members and the degenerate-horizon family |

**⛔ AND THE DISCIPLINE AGAINST WHICH ALL OF IT MUST BE READ.** *Every dimension statement in this register
descends from **one** fold. Forty-nine re-derivations are **not** forty-nine confirmations, and the figure
ledger's own ⊢54 says why:* **"the count matches and the match means nothing. A count is not a map."**

---

> ## ⚠⚠ THIS FILE'S "WHAT REMAINS OPEN" LIST IS PARTLY DISCHARGED, AND ONE ITEM IS CONTRADICTED BY THIS FILE'S OWN HEAD (found r2382, reading it at source)
>
> ***Three of its four open items are settled by later work, and the file does not know:***
>
> - **① the 39 screened-clean probes** — *"screened, not re-derived… Owed: a read-through, not a computation."*
>   ✔ **DONE: `L-08` struck c54.84** — *"the read is done, and its deliverable is the PARTITION"*, the complaint
>   having been not that the screen is wrong but that it was **uncheckable**; the audit published the counts and
>   never which probe went where, and c54.84 published the partition.
> - **③ P3's `rem:dimension` still carries `(A1)`** — *"the $D$-dimensional metric function is the standard
>   Tangherlini–de Sitter form rather than a rederivation of CR's operator at general $D$. **Every result in this
>   register inherits it.**"* ⛔ ***(A1) WAS DISCHARGED AT c54.29*** *(`L-01`, the oldest debt in the register):
>   P8's `thm:kernel` makes the vacuum sector the **kernel of the matter functional**, and at general $D$ its
>   **entire** solution space is Tangherlini–de Sitter with $M$ the single constant of integration.*
>   ***What the extension assumed, the construction supplies.*** **And this file's own head banner records that
>   discharge — so the head and the list contradict each other inside one document**, which is the
>   header-vs-body class at its most local. *The two further inputs it named, `L-89` (the lock) and `L-90`
>   (roundness), are also struck — c54.32 and c54.36.*
> - **② the unrun stations are unaffected** — ✔ **still true, and load-bearing for the reach edge** *(below).*
> - **④ `THE_ARSENAL_INDEX`'s cells** — *unverified here; left as this file states it.*
>
> **⌗ AND THE CORRECTION THIS FORCES ON THE REACH EDGE'S PACKAGE (`THE_STAGED_REVISIONS` §②ʙ).** *I had listed
> this document as the edge's **entry condition** — "read before trusting any bake's constants as structural" —
> which is its `description:` field.* ***Read at source, this file says the opposite about its own scope:***
> **"the reach programme's UNRUN stations are unaffected by any of this … These are new bakes, not deepening —
> they belong to the reach queue and this arc does not touch them. *Recorded so the line between the two is
> visible.*"** ***So its role for the edge is not a gate over the stations. It is a STANDING DISCIPLINE for the
> bake itself, and the discipline is one line:***
>
> $$\textbf{Before a constant is read as structural, re-derive it on the } D\textbf{-dimensional } f.$$
>
> *with the failure class named:* ***"a verdict wider than its computation's scope"*** *— which this file's own
> `FIGURE_THEOREM_LEDGER` quotation calls **"the check to run on every future ⊢ here."***

## ⌗ WHAT REMAINS OPEN — and it is a short list, with no deferrals in it

**① The 39 screened-clean probes are screened, not re-derived.** *The criterion is stated and the screen is
mechanical; a probe wrongly classed clean would be found by re-reading its verdict for a quantity built from
$f$. **Owed: a read-through, not a computation.***

**② The reach programme's UNRUN stations are unaffected by any of this.** *R-M's **Ⓕ** (two real forms of
$\mathrm{SO}(6,\mathbb{C})$ / representation theory), **Ⓖ** (Lie algebroids / Cartan), **Ⓗ** (the
$\gamma^5$-graded index / K-theory); R-M's **statistics/inference**, which `A5.5` needs; and **the whole of
R-P** below station ⑦. **These are new bakes, not deepening** — they belong to the reach queue and this arc
does not touch them. *Recorded so the line between the two is visible.*

**③ P3's `rem:dimension` still carries its one assumption**, `(A1)`: the $D$-dimensional metric function is
the standard Tangherlini–de~Sitter form rather than a rederivation of CR's operator at general $D$. **Every
result in this register inherits it.**

**④ `THE_ARSENAL_INDEX`'s cells are not re-marked.** *The row condition is stated there; the R-M columns
remain `do not populate` pending the morph's math-ledgers question, which is not this arc's to settle.*

**⚠ NOTHING ON THIS LIST IS A REFUSAL.** *Items ① and ④ are named work with a stated shape; ② is another
campaign; ③ is an assumption carried in the open at every site. **Two of the three questions the previous pass recorded instead of working are worked** — the antipodal
identification killed by exact computation, the category zeros adjudicated and standing. **The third, `R3`, was
'worked' into a kill that has been retracted**, and it is now open with a sharper question in front of it:
***which $2{+}1$?*** — because the corpus carries two and no catalogue had distinguished them.*

**⑤ AND A DISCIPLINE THIS STEP EARNED THE HARD WAY.** *A narrowly-scoped result may narrow a claim's SCOPE. It
may not, chained with other provisional pieces, foreclose an open SEARCH. The three items chained here — a
$D$-dependence of the sign split, the species reading of $\mathrm{sign}(r)$, and the level-(3) wall — were each
sound and their conjunction was not an argument. **`GEOMETRY_PHYSICS_TAXONOMY` names this face 24 and had
already caught it once at r696; this is the second firing at the same drill-site.***

---

## ⌗ WHAT WAS NOT DONE HERE, and why

- ~~**P6 is split off**~~ — **RUN at r2376+c54.12**, as step 2b. *P6 §least-arbitrariness gains
  `sec:register-boundary`: **a modulus fixes how a symmetry is BROKEN**, so an unforced choice that leaves the
  symmetry maximal lies **outside the register** — and the dimension of a maximally symmetric substrate is the
  programme's **first such case**. **Where form is silent, content may still decide.** Recorded there as
  **NOT** a sixth instance (the tally of outstanding bets is unchanged), **NOT** an instance of the engine's
  pattern (the discriminator was already in hand), and **not** a weakening of R2 — which discriminated
  correctly inside the same enquiry, returning one **requirement** (the flavour skeleton at $D=4$) and one
  **permission** (colour at any higher $D$, which R2 condemns).*
- **R3 is not promoted.** *Its own rule: the reason must be **derived or withdrawn**, and naming a candidate is
  neither.*
- **The category ledger's two ZEROS are not reopened**, only shown reopenable.
- **No paper was edited in this step.** *The propagation into the corpus is the arc's own next move, and the
  ledgers are where a bake's findings live until they earn a paper.*
- **The antipodal-$\mathbb{Z}_2$ / offset-parity identification is a question, not a claim.**
