---
name: representation-theory-ledger
kind: FORWARD
current: r3514
job: The representation-theory field bake — what bit, what bounced, and the boundary. The largest unbaked vocabulary in the corpus (×241 tight), thrown r3437 after the Phase 4 survey named it the standing first pick on measured usage.
sources: [chat]
---

> **▣ FORWARD — the representation-theory bake, and part of the corpus.** *Three registers kept apart:
> **what bit**, **what bounced**, **what the boundary is**. Thrown because `622`'s Phase 4 survey
> (r3435) measured it as the largest never-thrown vocabulary in the corpus — and because a **probe** at
> r3170 is not a bake: that probe returned one finding, wrote no ledger, and `622` says so itself.*

# THE REPRESENTATION-THEORY LEDGER

## ⌗ THE LANDING TABLE — where each register stands against the corpus, checked r3538

> **▣ THE `\ldg` GATHER FOR THIS LEDGER.** *Live prose only. Routed-clause column per the r3535 division.*

### ⛔⛔ WHAT THE GATHER FOUND FIRST: THE REAL-FORM COUNT NEVER LEFT `P13`

*Chasing `R11` — which turns on `rem:a2-distinct`'s shared-complexification claim — surfaced a defect
`R11` is not about.* ***Four papers stated that the compact $\SO(6)$ and the Lorentzian $\SO(5,1)$ are*
**the two** *real forms of $\SO(6,\mathbb{C})$.*** *There are **five** — the involution bake's `I1`,
which P13 applied at `sec:face-status` and which reached **no other paper**:*

| paper | the clause | now |
|---|---|---|
| **P7** | *"are **the two** real forms of the single complex $\SO(6,\mathbb{C})$"* | *"are **two of the** real forms"* |
| **P5** `rem:a2-distinct` | *"is **one of the two** real forms of the single complex group"* | *"is **one of the** real forms"* |
| **P2** | *"**makes the two** real forms of the single complex group"* | *"**makes two of the** real forms"* |
| **p0** | *"These are **the two** real forms of the one complex group"* | *"These are **two of the** real forms"* |

⇒ ***Fixed r3538, all four, and the enumeration claim is now zero corpus-wide. All four compile.***
⌗ *The programme's own phrase — "the substrate's **two real forms**", meaning the two the construction
**reaches** — is correct and untouched; it is P13's own scoped language and carries the title. **Only the
enumeration was wrong, and only where a count of $\SO(6,\mathbb{C})$'s forms was asserted.***

⚠ ***This is the third correction-reaches-one-grain failure in this campaign*** *— after the dependency
matrix and its figure, and the deck/monodromy word across eight papers. **A correction landing in the
paper that owns a claim does not reach the papers that quote it, and nothing in the corpus looks.***

### ⌗ THE TABLE

| register | routed clause · its paper | disposition |
|---|---|---|
| **`R1` the hexad is TWO hexads — P3's triple lies along the *weight* directions at *root* normalisation, and the ratio is exactly $\sqrt3$; P3's prose reads the six marks as $\mathbf3\oplus\bar{\mathbf3}$ (weights) while its own receipt establishes them as the differences $e_i-e_j$ (roots)** | *"the root triple $(-2,1,1)/\sqrt3$, whose directions are those of a fundamental $\mathbf3$'s weights, at root normalisation"* · **P3 `sec:cubic`** | ***STILL OWED.*** *P3 today reads exactly as quoted — "carry the root triple $(-2,1,1)/\sqrt3$---the three weights of a fundamental $\mathbf 3$" — and `root normalisation` is ×0 corpus-wide.* ⌗ **And the ledger's own note on why it is invisible is the sharp part: the $\sqrt3$ between roots and weights is the same $\sqrt3$ the seam carries at $\alpha/\sqrt3$** |
| **`R11` the common complexification is $A_3$, not $A_2$ — colour's $A_2$ is its node-deletion subsystem, while the geometric $A_2$ is not a subsystem of it at all** | *the exact clause is written out* · **P5 `rem:a2-distinct`** | ***STILL OWED.*** *`node-deletion` and `sub-root-system` are live in P7, P12, P14 and p0 — **but not at this remark**, which still grounds the analytic connection in "that common complexification" without saying which root system it carries.* ⌗ *The ledger's own framing is right: the remark is **not wrong, it is less precise than it can be, and the precise version is stronger for its own conclusion*** |
| **`R10` the rate's two parameters ARE the $A_2$'s two Weyl invariants, of Casimir degrees 2 and 3 — $e_2=-\alpha^2$ quadratic, $e_3=-2M\alpha^2$ cubic, $e_1=0$ the tracelessness — and the Nariai condition is the vanishing of their discriminant, the Weyl wall** | *one sentence, written out* · **P3 `sec:cubic` or P15 `sec:flatlcdm`** | ***STILL OWED, and cleanly absent:*** *`Casimir` ×0, `elementary symmetric` ×0, `Weyl wall` ×0 corpus-wide.* ⌗ **This is a *connection*: the corpus holds both halves — r3401's rate fixed by $\alpha$ and $x_0$ and nothing else, and the horizon cubic's $A_2$ — and does not join them** |
| **`R12` the six Nariai marks are the WALL-CROSSINGS of the $A_2$ chamber structure, the six arcs its six Weyl chambers, the twelve designations its full $\mathrm{Aut}(A_2)$, and the sign of $2M$ the chamber's $\mathbb{Z}_2$ label** | *the clause is written out* · **P3 `sec:cubic`** | ***STILL OWED.*** *`Weyl chamber` ×0 and `chamber` ×0 corpus-wide; P14's two `wall-crossing`s are the chiral wall, a homonym.* ⌗ **The weight is that three facts the corpus states *separately* — six Nariai, twelve designations, and $R$ exchanging $\mathbf3/\bar{\mathbf3}$ — are ONE fact, which is why $R$ *is* the diagram automorphism rather than analogous to one** |
| **`R2` triality is the weight-lattice quotient** · **`R3` the sum-to-zero, already in the corpus** · **`R5` the dial really does realise $\mathrm{Aut}(A_2)$** | — | **⊢ / BOUNCE, no change owed.** *P3 and P5 carry the hexad, the twelve designations and $\mathrm{Aut}(A_2)=D_6$* |
| **`R4` the one-per-hinge condition is the singlet's antisymmetry** · **`R8` the colour obstruction is representation-theoretic** | — | **LANDED** — `singlet` ×11 and `antisymmetr*` ×4 in P14, plus P13. **Pointer owed** |
| **`R6` the ceiling, confirmed from a second direction — the corpus needs neither Casimirs nor weight multiplicities, and its own ×33 "multiplicity" is *catalogue* multiplicity, with P14's one exception already named as undelivered** | — | **HELD, and correctly invisible** |

### ⌗ WHAT THIS GATHER FOUND

***Four routed clauses, all four still owed, and every one of them written out as an exact sentence by the
bake.*** *That is the gen-2 signature the involution and Cartan gathers did not show: those two had rows
that were **stale** — discharged, or wrongly corrected. **This one's rows are accurate and simply
unapplied.*** ⇒ *So gen 2 has three failure modes among three ledgers — a discharged clause still marked
routed, a correction that was itself wrong, and clauses correctly routed and never taken. **The only
thing they share is that the row's own verdict cannot be trusted without re-running it against the
paper.***

⌗ **And `R10` and `R12` together are one object**: *the $A_2$'s invariant theory — degrees 2 and 3, the
discriminant, the chambers — is what the horizon cubic already is, and the corpus reads the cubic
everywhere without once naming the invariant theory it is an instance of.*

---

## ⌗ THE LEAD REGISTER — *no new field while any row is unmarked (`THE_FIELD_BAKE_PLAN` r3439)*

| # | lead | state |
|---|---|---|
| `R0` | the survey's ×233 is inflated — baseline it | ⊢ **WORKED** r3437 |
| `R1` | is the Nariai hexad the ROOT hexad or the WEIGHT hexad? | ⊢ **WORKED** r3440, receipted |
| `R2` | is the corpus's triality the weight-lattice quotient `P/Q ≅ ℤ₃`? | ⊢ **WORKED** r3440 |
| `R3` | the horizon cubic sums to zero — is that the weight system's trace condition? | ⊢ **WORKED** r3440 — *the corpus already has it* |
| `R4` | the one-per-hinge condition as the singlet of `3⊗3⊗3` | ⊢ **WORKED** r3437, receipted |
| `R5` | `Aut(A₂) = S₃ × ℤ₂ ≅ D₆` — is the corpus's `D₆` that group, realised the same way? | ⟐ **NO CHANGE OWED** r3441, receipted |
| `R6` | `P14`'s ceiling: "no representation content" | ⟐ **NO CHANGE OWED** r3437 |
| `R7` | `\otimes` ×1 against `\oplus` ×27 — the grading/combining boundary | ⊢ **WORKED** r3437 |
| `R8` | `su(3) ⊂ so(6)`, `⊄ so(5,1)` — a branching question never asked as one | ⊢ **WORKED** r3441, receipted |
| `R9` | the deck `ℤ₃`'s irreps are one-dimensional — no multiplet for three generations | ⟐ **NO CHANGE OWED** r3437 |
| `R10` | does the corpus ever need a Casimir, or a weight multiplicity > 1? | ⌷ **ROUTED** r3442, receipted |
| `R12` | the six Nariai marks against the Weyl chamber structure — 3 walls, 6 chambers? | ⌷ **ROUTED** r3443, receipted |
| `R11` | `rem:a2-distinct`'s "analytic, not accidental" — is the common complexification a **branching** statement? | ⌷ **ROUTED** r3441, receipted |

---

## ⌗ R0 — THE BASELINE, RUN FIRST, AND IT CORRECTS THE SURVEY THAT ORDERED THE THROW

*`reach_baseline` before asserting any hole, which is the gate `609` installed and which caught two
false openings on the audit that installed it.* ⛔ **The survey's ×233 is inflated, and its top two
terms are not representation theory at all.**

| the survey counted | actual usage |
|---|---|
| `representation` ×93 | ***ontological*** — "the maximally extended **representation**", "the invariants of the **representation** of that layer" |
| `character` ×68 | ***causal*** — "causal character" ×20, and the rest cognate |
| `irreducible` ×21 | *partly* `casus irreducibilis` — the cubic, not a module |

⇒ ***The throw survives the correction, on different evidence.*** *Measured on the tight vocabulary,
de-macroed, comments stripped, 17 paper bodies:*

| term | count | | term | count |
|---|---|---|---|---|
| real form | **×106** | | triality | ×16 |
| root system | ×30 | | fundamental/adjoint rep | ×12 |
| multiplet | ×29 | | weight (space/lattice/diagram) | ×4 |
| Weyl group | ×23 | | irrep | ×3 |
| character (rep-theoretic) | ×16 | | decomposes as | ×2 |

**TIGHT TOTAL ×241** — *so the field is genuinely present and genuinely unbaked; the survey was right
for the wrong reason, and the reason matters because a bake ordered on `representation` ×93 would have
gone looking in the ontology.*

---

## ⛔⛭⛭ R1 — **THE HEXAD IS TWO HEXADS, AND THEY DIFFER BY $\sqrt3$**

**`P03` `sec:cubic` states:** *"The six Nariai marks are the $A_{2}$ hexad. The three at
$2M=+\tfrac{2}{3\sqrt3}$ ($w=30,150,270^{\circ}$) carry the root triple $(-2,1,1)/\sqrt3$ — **the three
weights of a fundamental $\mathbf 3$**."*

**⌗ WHAT IS CONFIRMED.** *The three marks carry the three **cyclic permutations**, three distinct
vectors summing to zero and permuted by $S_3$.* ***That is a genuine fundamental weight system, and the
identification is right in its directions and its symmetry.***

**⛔ WHAT IS OFF, AND IT IS EXACT.** *The vectors have $|v|^{2}=2$, which is the $A_2$ **root** length.
The true weights of the fundamental have $|v|^{2}=2/3$.* ⇒ ***The ratio is $\sqrt3$: `P03`'s triple lies
along the weight directions at ROOT scale.*** *And it is not a root either — the $A_2$ roots in this
realisation are the differences $e_i-e_j$, which is what the corpus's own receipt
`P03_the_adjoint_is_entailed` uses.*

**★ THREE OBJECTS, ONE WORD.** *`P03`'s prose calls the six **Nariai marks** the $A_2$ hexad and reads
them as $\mathbf3\oplus\bar{\mathbf3}$ — **weights**. Its own receipt establishes the hexad as the six
**differences** $e_i-e_j$ — **roots**. **Roots and weights of $A_2$ are different hexagons**: same
centre, rotated $30^{\circ}$, scaled by $\sqrt3$.* ⇒ ***The corpus uses one word for both, and the
$\sqrt3$ between them is the same $\sqrt3$ the seam carries at $\alpha/\sqrt3$ — which is why the
discrepancy is invisible in the corpus's own units.***
*(receipt `storyboard_receipts/RT_R1_the_hexad_normalisation.py`)*

**⌷ ROUTED, NOT APPLIED** *— `P03`'s sentence is right about the weight system and loose about which
hexagon. The exact clause owed: **"the root triple $(-2,1,1)/\sqrt3$, whose directions are those of a
fundamental $\mathbf3$'s weights, at root normalisation"**. Paper-holder's call.*

**⌗ A WORRY RAISED AND WITHDRAWN, recorded because it cost a step.** *At Nariai the cubic has a
**double** root, $(-1.1547,0.5774,0.5774)$, so the multiset is degenerate while a fundamental's weights
are always distinct. **Not a contradiction**: the three MARKS carry three distinct ORDERED triples, and
the degeneracy is of the multiset, not of the weight system.*

⚑ **AND `R1` GENERATES A LEAD — `R11`.** *`rem:a2-distinct` says the two $A_2$'s coincide "no accident
of abstract type" because colour's $\mathfrak{su}(3)$ rides the substrate's **conjugate real form**, so
the connection is **analytic**. That is a **branching** statement — $\mathfrak{su}(3)\subset
\mathfrak{so}(6)$ — and it has never been asked as one. **Open, and owed before this field is done.***

---

## ⌗ R2 — **TRIALITY IS THE WEIGHT-LATTICE QUOTIENT** ⊢

*For $A_2$ the weight lattice modulo the root lattice is $P/Q\cong\mathbb{Z}_3$, and the class of a
representation with Dynkin labels $(a,b)$ is $(a+2b)\bmod 3$: the $\mathbf1$ and the $\mathbf8$ carry
$0$, the $\mathbf3$ carries $1$, the $\bar{\mathbf3}$ carries $2$, the $\mathbf{10}$ carries $0$.*
⇒ ***So "triality" is not a name the corpus coined for its deck $\mathbb{Z}_3$ — it is the standard
$A_2$ lattice quotient, and `P14`'s use of it is the field's own object.*** *No correction owed; the
identification is exact and the corpus is using the right word for the right thing.*

## ⌗ R3 — **THE SUM-TO-ZERO, AND THE CORPUS ALREADY HAS IT** ⊢

*The horizon cubic $r^{3}-\alpha^{2}r+2M\alpha^{2}$ is **depressed** — no $r^{2}$ term — so its roots
sum to zero identically in $M$ and $\alpha$; and a fundamental weight system of $\mathfrak{su}(N)$ sums
to zero because the trace of a traceless generator vanishes.* ⌗ ***The corpus states this and receipts
it***: `P03`'s `prop:factor` factors on the ellipse, and `P03_the_adjoint_is_entailed` shows that
ellipse **is the Killing form on the Cartan** — $|x|^{2}=2(a^{2}+ab+b^{2})$ — with the six differences
satisfying the root-system axioms, so ***the adjoint is entailed rather than owed***. *Nothing new is
found here, and that is the result: the field's first question was already answered, in a receipt.*

---

## ⛔⛭ R4 — **THE ONE-PER-HINGE CONDITION IS THE SINGLET'S ANTISYMMETRY**

**`P14` states it, and states that it does not come from the grading:**
> ***"triality zero is necessary for a colour singlet and not sufficient, and the one-per-hinge
> condition comes not from it but from the causal trichotomy of the six hinge-ends."***

**The field supplies the same condition in one line.** *For $SU(3)$,*
$$\mathbf{3}\otimes\mathbf{3}\otimes\mathbf{3} \;=\; \mathbf{10}\oplus\mathbf{8}\oplus\mathbf{8}\oplus\mathbf{1},$$
*and the singlet is the totally antisymmetric piece — the $\epsilon_{ijk}$, non-zero only when the three
indices are **all distinct**.* ⇒ ***That IS one index per colour, which is one per hinge.***

**⌗ AND THE FIELD REPRODUCES `P14`'s NECESSARY-NOT-SUFFICIENT EXACTLY, with its counterexamples.**
*`(0,0,0)` and `(1,1,1)` carry triality $0$ and are not singlets; `(0,1,2)` carries triality $0$ and is.
So triality zero is necessary and not sufficient, and the sufficient condition is the antisymmetry.*
*(receipt `storyboard_receipts/RT1_the_singlet_is_the_one_per_hinge_condition.py`)*

⇒ ***TWO INDEPENDENT ROUTES TO ONE CONDITION.*** *The corpus reaches it from the causal trichotomy of
the six hinge-ends; representation theory reaches it from the $\epsilon$-tensor. **The corpus does not
have that agreement**, and it is a check rather than a replacement: the geometric route is the one the
construction needs, and the field's route is the independent confirmation that the condition it lands
on is the physical one.*

**⌷ ROUTED, NOT APPLIED** *— whether `P14` should carry the agreement is the paper-holder's call. What
is owed is at most a clause, and the paper's own derivation is untouched by it.*

---

## ⟐ R2 — **THE BOUNDARY, AND THE CORPUS STATES IT ITSELF**

`P14`'s own closing line is the boundary this bake would otherwise have to find:
> ***"So the ceiling is uniform: exact selection rules, exact counts and exact gradings, and no
> representation content."***

**The field confirms the ceiling rather than breaching it.** *A finite group is abelian exactly when
every irreducible representation is one-dimensional, so the deck $\mathbb{Z}_3$ offers no multiplet into
which three generations could be placed — which `P14` already says, in those words.* ⇒ *`R2` returns
**NO CHANGE OWED**, the register the variational ledger keeps for exactly this.*

---

## ⌗ R3 — **WHAT BOUNCED, AND THE BOUNCE IS STRUCTURAL**

**`\otimes` appears ONCE in seventeen paper bodies. `\oplus` appears ×27.** *The corpus builds by
**direct sums** — $\mathbf 3\oplus\bar{\mathbf 3}$ for the $A_2$ hexad, exchanged by the mass-reflection
parity $R$ — and never forms a **tensor product**.*

⇒ ***That is why the singlet route was unavailable to it, and it is not an oversight.*** *A direct sum
is what a **grading** gives you; a tensor product is what **combining** several charged objects gives
you. The construction's colour arrives as a grading on one structure, so its natural operation is
$\oplus$, and the one place the physics needs $\otimes$ — three quarks making a hadron — is exactly
where `P14` had to reach outside the grading for "the causal trichotomy of the six hinge-ends".*

**⌗ SO THE FIELD'S VERDICT ON THE SECTOR IS NARROW AND POSITIVE.** *It supplies no multiplet the corpus
lacks and no content the ceiling forbids. What it supplies is **one operation the corpus does not use**,
and the single place that operation is needed is the single place the corpus reached elsewhere for the
answer — and got the same one.*


---

## ⟐ R5 — **THE DIAL REALLY DOES REALISE $\mathrm{Aut}(A_2)$** — *no change owed*

*`P03` claims `Aut(A₂) = S₃ × ℤ₂ ≅ D₆` "realised as the dial's hexagonal symmetry", and **both halves
check**.* The group: the automorphisms of $A_2$ number **12**, six rotations and six reflections,
dihedral, and $S_3\times\mathbb{Z}_2\cong D_6$ since both have an element of order six. The
**realisation**: the dial parameter enters the roots as a *phase*, $r_k=(2/\sqrt3)\sin w_k$ with
$w_k=w,\,w\pm120^{\circ}$, verified against the cubic — so $w\mapsto w+120^{\circ}$ **permutes the three
roots cyclically** (the Weyl $S_3$) and $w\mapsto w+60^{\circ}$ **flips the sign of $2M$** ($R$, the
diagram automorphism exchanging $\mathbf3$ and $\bar{\mathbf3}$). ⇒ ***The corpus's claim is exact.***

## ⊢ R8 — **THE COLOUR OBSTRUCTION IS A REPRESENTATION-THEORETIC ONE**

*$\mathfrak{su}(3)\subset\mathfrak{so}(6)$ is **exhibited** here, not inferred: eight real antisymmetric
$6\times6$ generators spanning an eight-dimensional space.* *And $\mathfrak{su}(3)\not\subset
\mathfrak{so}(5,1)$ because any **compact** $\mathfrak{su}(3)$ must lie in the maximal compact
$\mathfrak{so}(5)$, which acts on $\mathbb{R}^5$, while $\mathfrak{su}(3)$'s smallest **faithful real**
representation is six-dimensional.* ⇒ ***The obstruction is the dimension of a smallest faithful real
representation — the field's own object, and the corpus makes the argument without naming it as one.***

## ⌷ R11 — **THE COMMON COMPLEXIFICATION IS $A_3$, NOT $A_2$** — *routed*

*`rem:a2-distinct` grounds "no accident of abstract type" in the shared complexification: colour's
$\mathfrak{su}(3)$ rides the substrate's **conjugate real form** $\mathfrak{so}(6)$, so the connection is
**analytic**.* ⛔ ***But $\mathfrak{so}(6,\mathbb{C})=D_3\cong A_3$ has rank 3 and TWELVE roots — neither
$A_2$ is its root system.*** *$A_2$ enters as the sub-root-system obtained by **deleting one Dynkin
node**, which is exactly $\mathfrak{su}(3)\subset\mathfrak{su}(4)\cong\mathfrak{so}(6)$.*

⇒ ***So the analytic route runs through the complexification on COLOUR's side only.*** *The geometric
$A_2$ is not a subalgebra root system at all — **the remark says so itself**, "not the root lattice of a
continuous internal Lie algebra" — so the two meet as **abstract** root systems and the shared
complexification relates one of them to $A_3$, not the two to each other.*

**⌷ ROUTED, NOT APPLIED.** *The remark is not wrong; it is **less precise than it can be**, and the
precise version is stronger for its own conclusion. The clause owed:* ***"the shared complexification
$\mathfrak{so}(6,\mathbb{C})\cong\mathfrak{sl}(4,\mathbb{C})$ carries $A_3$; colour's $A_2$ is its
node-deletion subsystem, while the geometric $A_2$ is not a subsystem of it at all — which is why the
coincidence is of abstract type and the realisations differ."*** *Paper-holder's call.*
*(receipt `storyboard_receipts/RT_R5_R8_R11_dial_branching_complexification.py`)*


---

## ⛔⛭⛭ R10 — **THE RATE'S TWO PARAMETERS ARE THE $A_2$'s TWO CASIMIR-DEGREE INVARIANTS** — *routed*

**The literal answer is that the corpus needs neither**: `Casimir` ×0 and weight-multiplicity ×0 across
seventeen paper bodies, and its own ×33 of "multiplicity" is *catalogue* multiplicity — how many
distinct solutions — with one exception, `P14`'s *"the multiplicity of a generation is the bundle's
rank"*, which that paper already names as undelivered. ⇒ ***So the ceiling `R6` recorded is confirmed
from a second direction.***

**⌗ BUT THE OBJECTS ARE PRESENT UNDER OTHER NAMES, AND THAT IS THE FINDING.** *The horizon cubic
$r^{3}-\alpha^{2}r+2M\alpha^{2}$ has elementary symmetric functions*
$$e_1 = 0,\qquad e_2 = -\alpha^{2},\qquad e_3 = -2M\alpha^{2}.$$
*$W(A_2)=S_3$ on the sum-zero plane has an invariant ring generated in **degrees 2 and 3** (Coxeter
exponents $1,2$), and $\mathfrak{su}(3)$ has exactly **two** independent Casimirs, of **degrees 2 and
3**.* ⇒ ***So $\alpha^{2}$ is the quadratic invariant, $2M\alpha^{2}$ the cubic one, and $e_1=0$ is the
tracelessness that puts the roots in the Cartan at all.***

**★ AND THE DISCRIMINANT IS THE WALL.** *$\mathrm{disc}=-4e_2^{3}-27e_3^{2}$, verified identical to the
cubic's own discriminant, vanishing exactly where two roots coincide.* ⇒ ***The Nariai condition IS the
vanishing of the Weyl-invariant discriminant: **Nariai is the wall of the Weyl chamber**, and the
undercritical dial is its interior.***

**⌗ WHY THIS REACHES BEYOND THE FIELD.** *r3401 reframed the rate as fixed by $\alpha$ and the offset
$x_0$ **and by nothing else**. This says what those two parameters **are** in the field's terms: the
complete invariant content of the $A_2$ the same construction already carries.* ***The corpus holds
both halves — the rate's two parameters, and the horizon cubic's $A_2$ — and does not join them.***

**⌷ ROUTED, NOT APPLIED.** *The clause owed, and it is one sentence:* ***"the rate's two parameters are
the $A_2$'s two Weyl invariants, of the Casimir degrees 2 and 3, and the Nariai condition is the
vanishing of their discriminant — the Weyl wall."*** *Paper-holder's call, `P03` `sec:cubic` or `P15`
`sec:flatlcdm`.*
*(receipt `storyboard_receipts/RT_R10_the_rate_parameters_are_the_casimirs.py`)*

⚑ **AND `R10` GENERATES `R12`.** *If Nariai is the wall, the chamber structure is a live question:
$A_2$ has **three** reflection hyperplanes and **six** Weyl chambers, and the corpus has **six** Nariai
marks and **twelve** designations. Whether those numbers are the chamber count, the wall count, or
neither is unasked. **Open, and owed before this field is done.***


---

## ⛔⛭⛭⛭ R12 — **THE DIAL IS A CLOSED PATH THROUGH THE $A_2$ WEYL CHAMBERS** — *routed*

*Generated by `R10`: if Nariai is the wall, then $A_2$ has **three** reflection hyperplanes and **six**
chambers, while the corpus has **six** Nariai marks and **twelve** designations. Chambers, walls, or
neither?*

**★ EVERY COUNT MATCHES, AND NONE OF THEM IS A COINCIDENCE OF SIZE.**

| corpus object | count | $A_2$ object |
|---|---|---|
| Nariai marks | **6** | the **wall-crossings** — $\mathrm{disc}=0$ at $w=30,90,150,210,270,330^{\circ}$ |
| arcs between them | **6** | the **Weyl chambers**, $\lvert W(A_2)\rvert=\lvert S_3\rvert=6$ |
| designations, every $30^{\circ}$ | **12** | $\lvert\mathrm{Aut}(A_2)\rvert=\lvert W\rvert\times\lvert\mathbb{Z}_2\rvert=12$ — **two per arc** |
| sign of $2M$ | alternates arc to arc | the $\mathbb{Z}_2$ promoting $W(A_2)$ to $\mathrm{Aut}(A_2)$ — the diagram automorphism $\mathbf3\leftrightarrow\bar{\mathbf3}$ |

*The discriminant's zeros are the corpus's own six marks exactly — $+2M$ at $30,150,270^{\circ}$ and
$-2M$ at $90,210,330^{\circ}$ — verified by tracking the three root branches continuously around the
dial.*

⇒ ***So the six Nariai marks are the six WALL-CROSSINGS, not six chambers, and the dial is a closed
path that crosses a wall at each Nariai and visits all six chambers in turn.***

**⌗ WHY THIS IS MORE THAN A COUNT.** *Three facts the corpus states **separately** — six Nariai, twelve
designations, and the $R$ parity exchanging $\mathbf3$ and $\bar{\mathbf3}$ — are **one fact** about the
chamber structure. The mass parameter's **sign** is the chamber's $\mathbb{Z}_2$ label, which is why
$R:2M\mapsto-2M$ is the diagram automorphism and not merely analogous to one.*

**⌷ ROUTED, NOT APPLIED.** *The clause owed:* ***"the six Nariai marks are the wall-crossings of the
$A_2$ chamber structure, the six arcs between them its six Weyl chambers, and the twelve designations
its full automorphism group — with the sign of $2M$ the chamber's $\mathbb{Z}_2$ label."*** *`P03`
`sec:cubic`, paper-holder's call.*
*(receipt `storyboard_receipts/RT_R12_the_dial_is_a_path_through_the_weyl_chambers.py`)*

---

## ⛭⛭ THE BAR, MEASURED — *`THE_FIELD_BAKE_PLAN`'s table*

| | this bake | the FULL tier's range |
|---|---|---|
| lines | **~290** | 189–978 ✔ |
| numbered probes | **13** (`R0`–`R12`) | 10–17 ✔ |
| receipts, all running | **4** | 2–10 ✔ |
| papers touched | `P03`, `P05`, `P07`, `P13`, `P14`, `P15` = **6** | 6–11 ✔ |
| lead register | **13 rows, none unmarked** | — |

⇒ ***The representation-theory field meets the bar. Four clauses are ROUTED and named; none is
applied, and each states the exact sentence owed and its paper.***

---

## ⛭⛭⛭ REACH CLOSE — all seventeen papers WORKED or CHECKED-NEGATIVE by name (r3514)

> ⛔ *The r3500 whole-set measurement / `THE_FIELD_BAKE_PLAN` recorded this field THINNER (r3437: 100 lines,
> ~1 receipt, "1 paper"). Read in full this pass (three sweeps, screening `fundamental` [rep vs
> congruence/scale], `character` [group vs causal], `adjoint` [rep vs self-adjoint], `weight` [rep vs
> weighting], `representation` [group vs "representation of the geometry"]). **The reach is `14/17` — the field
> is carried in fourteen papers, and the "1 paper" record was the throw's, not the field's.***

**⌘ THE CORE, IN FOURTEEN PAPERS.** *The three sum-to-zero horizon roots = the $A_2$ root/weight system of
$\mathfrak{su}(3)$ (`P03`: Cartan integers $\{-2,-1,1,2\}$, $\dim\mathfrak{sl}(3,\mathbb C)=6+2=8$, $|Z|=3=\det$
Cartan matrix; `P12`,`P08`,`P02`,`P11`,`p0`); $\mathrm{Aut}(A_2)=S_3\times\mathbb Z_2\cong D_6$ order 12
(`P05`,`P12`,`P11`,`P02`,`P07`,`p0`); the diagram automorphism $\mathbf 3\leftrightarrow\bar{\mathbf 3}$ because
$-1\notin W(A_2)$ (`P13`,`P02`); the smallest faithful real rep of $\mathfrak{su}(3)=\mathbb R^6$ (`P13`,`p0`);
the full SM generation as five irreps $(\mathbf 3,\mathbf 2)_{1/6},(\bar{\mathbf 3},\mathbf 1)_{-2/3},\dots=15(16)$
(`P14`); triality $t=(p-q)\bmod 3$, baryon/diquark/meson $=1/0/1$ (`P14`); the enlarged $W(A_3)$ order 48
(`P12`,`P14`).* ⇒ **The persistent NEGATIVE framing everywhere: the $\mathfrak{su}(3)$ is a discrete Weyl/Cartan
shadow, $\mathfrak{su}(3)\not\subset\mathfrak{so}(5,1)$ — not a continuous colour isometry.**

**⌘ AND ONE REP COMPUTATION THIS LEDGER HAD NOT CARRIED — `P10`'s PETER–WEYL.** *`L^2(SU(2))\otimes V_s$,
Peter–Weyl returns level-$j$ totals $1,3,5$ times $(2j+1)^2$ for $s=0,1,2$ — the $\dim=2j+1$ SU(2) irreps — and
the TT part is the two extreme summands, $2/5$ of the symmetric-tracefree total, with degeneracy
$2(n-1)(n+3)$.* **A genuine rep-theory computation in the canonical-time paper, reach addition.**

| paper | status | where |
|---|---|---|
| `P01` | **CHECKED-NEGATIVE** | English "maximally extended representation"; "root" = idiom |
| `P02` | **WORKED** | $A_2$ root config = su(3); antifundamental $\bar{\mathbf 3}$; $\mathrm{Aut}(A_2)=D_6$ |
| `P03` | **WORKED** | `R1`/`R3` + the $A_2$ root system, Cartan integers, adjoint dim 8, $|Z|=3$ (`P03_the_adjoint_is_entailed`) |
| `P04` | **CHECKED-NEGATIVE** | no rep-vocabulary occurrences |
| `P05` | **WORKED** | Weyl $S_3$ permutes the three roots; $\mathrm{Aut}(A_2)=D_6$ order 12 |
| `P06` | **WORKED** (light) | $R=\gamma^5$ on the spinor rep; su(3)⊂so(6) structure-group reduction |
| `P07` | **WORKED** | su(3)⊄so(4,1); $\mathbf 3\leftrightarrow\bar{\mathbf 3}$; monodromies+3-cycle → SU(3); 12→4 classes (`order3_bridge`, `two_realisations`) |
| `P08` | **WORKED** | $A_2$ weights = SU(3) root system; Weyl $S_3$ vs $\mathbb Z/3$ (`order3_bridge`) |
| `P09` | **CHECKED-NEGATIVE** | "Weyl" = Weyl tensor (Petrov); "irreducible"/"decomposition" = English/tensor |
| `P10` | **WORKED** | Peter–Weyl on $L^2(SU(2))$, SU(2) irrep dims $1,3,5$, TT $=2/5$ — *reach addition* |
| `P11` | **WORKED** | $A_2$ root system; $\mathrm{Aut}(A_2)=S_3\times\mathbb Z_2\cong D_6$; Weyl $S_3$ = deck |
| `P12` | **WORKED** | roots = zero-sum $A_2$ weights; $R=\gamma^5$ spinor; $W(A_3)$ order 48; $A_3=$ so(6,ℂ) root system (`GROUP_full_order48`, `EMBEDDING_is_Td_equals_WA3`) |
| `P13` | **WORKED** | su(3) smallest faithful real rep $=6$; rank 4 vs SO(6) rank 3; su(3)-shadow $S_3$ triplet; AH index in the rep ring (`P13_cascade_rank`, `P13_conjugation_parity`) |
| `P14` | **WORKED** | full generation = 5 irreps = 15(16); triality test; SU(3) from monodromies; $D_6$ irrep dims $1,1,2$ (`P14_the_count_specified`, `B24_the_triality_test_run`, `P14_two_rank_threes`) |
| `P15` | **WORKED** (light) | "sixteenth Weyl fermion of each generation"; $S_3$ relating families |
| `P16` | **WORKED** (light) | colour su(3); su(3)⊄isometry algebra; $S_3$; $\gamma^5$ |
| `p0`  | **WORKED** | smallest faithful real rep $=\mathbb R^6$; $\mathbf 3\oplus\bar{\mathbf 3}$ matter/antimatter; $A_2\subset A_3$ by node deletion (`P17_qm_S4_vs_S5`) |

⇒ ***Seventeen of seventeen accounted for: FOURTEEN WORKED, THREE CHECKED-NEGATIVE. Reach: **17/17 ACCOUNTED FOR** — 14 **WORKED**, 3 **CHECKED-NEGATIVE by name** (the reading standard; the worked-only figure is $14/17$) — the record
"1 paper" was the r3437 throw's, not the field's, and the `R1`–`R12` probes plus this reach pass now name the
whole $A_2$/su(3)/triality/generation structure across fourteen papers.*** *No new receipt owed — the core is
`R1`/`R4`/`R8`-receipted and `P14`/`P12`/`p0`/`P08` carry their own; `P10`'s Peter–Weyl is receipted in the
graviton-lift lineage. Cross-field: the $A_2$ root system overlaps quadric (the fundamental ellipse),
involution (the real-form skeleton) and Cartan (the flat colour holonomy); shared, no correction.*
