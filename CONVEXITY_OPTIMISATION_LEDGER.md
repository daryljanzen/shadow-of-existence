---
name: convexity-optimisation-ledger
kind: FORWARD
current: r3519
job: The convexity / optimisation field-bake ledger — what bit, what bounced, and the boundary. One of the three fields listed but never thrown (the r3505 overnight order: convexity ×143, constraint ×141). `OWED` 622.
sources: [cowork]
---

> **▣ FORWARD — the convexity bake, and part of the corpus.** *Three registers kept apart: **what
> bit**, **what bounced**, **what the boundary is**. The overnight order named it —* ***"the three
> never thrown: convexity ×143 …"*** *— and the ×143 is carried almost entirely by one word,
> `constraint` ×141. The bake's first job is to ask whether that word is the optimisation term (a
> feasible region, a Lagrangian, a KKT condition) or a homonym (the **Hamiltonian/Dirac constraint** of
> canonical gravity, the **gauge constraint**), because the answer decides whether the field bit at all.*


## ⛭ THE BASELINE, MEASURED FIRST

**Nothing below was called a hole before the corpus was asked what it holds.** *Seventeen paper bodies,
comments and bibliography stripped, de-macroed, via `corpus/reach_baseline.py`.*

| present | homonym-suspect | absent across all seventeen |
| --- | --- | --- |
| `constraint` ×141 (P07×25, P10×31, P12×20, P13×16) · `stationary` ×21 (stationary-action / stationary point) · `constrained` ×19 · `extremum` ×5 · `gradient` ×10 (P15×7) · `Lagrange multiplier` ×1 (P07) · `saddle` ×1 (P10) · `minimise`/`minimize` ×1 (P15) · `maximize` ×1 (P04) | `constraint` (Hamiltonian/Dirac/gauge?) · `stationary` (stationary spacetime? or stationary-action?) · `gradient` (metric gradient?) | `convex` · `convexity` · `concave` · `convex hull` · `optimisation` · `objective function` · `supporting hyperplane` · `epigraph` · `subgradient` · `KKT` · `linear programming` · `saddle point` (as a NAMED optimisation object) |

⚠ **CONVEXITY PROPER IS ×0, AND THAT IS THE HEADLINE.** *`convex`, `convexity`, `concave`, `convex
hull`, `epigraph`, `supporting hyperplane`, `subgradient`, `KKT`, `objective function` are absent from
all seventeen bodies. The ×143 the order flagged is the substring behind `constraint` — and the bake
must READ those constraints to say whether any is an optimisation constraint or all are the physics
homonym. The genuine optimisation-shaped content the corpus DOES carry is variational
(stationary-action, already the VARIATIONAL bake's) and one derived-ratio minimisation in `P15`'s
refit-bound. The expected verdict is a BOUNCE: the field's own objects (convex sets/functions, duality,
KKT) are not in the corpus; what shares its words is canonical-constraint and stationary-action work
already owned elsewhere.*

---

## ⛭⛭⛭ REACH CLOSE — all seventeen papers WORKED or CHECKED-NEGATIVE by name (r3520)

*Two readers swept the seventeen bodies — the six constraint-heavy papers (P07/P10/P11/P12/P13/p0,
carrying `constraint` ×90+) and the other eleven. The headline the baseline predicted holds: `convex`,
`convexity`, `concave`, `convex hull`, `epigraph`, `supporting hyperplane`, `subgradient`, `KKT`,
`objective function` are ×0 across all seventeen, and the ×143 the order flagged is the substring
behind `constraint` — the **ADM/Dirac Hamiltonian constraint** of canonical gravity (P08 l.264/439,
P10 l.144, P07 l.313 all state it as such), the corpus's "problem of time" object, not an optimisation
constraint. The optimisation-adjacent tokens are all other-field homonyms: `Lagrange multiplier` = the
ADM lapse/shift; `variational principle` = a least-action instanton; `saddle-point` = a path-integral
saddle; `stationary` = stationary (Killing) spacetime; `duality`/`hyperplane` (p0) = projective duality
/ polar hyperplanes; `minimum`/`maximum` = geometric throat radius or ordinary physical extrema.*

| paper | status | where / why |
|---|---|---|
| `P01` | **CHECKED-NEGATIVE** | `BH_causality`: `stationary` = stationary/static chart (Killing); no minimum/constraint/extremal |
| `P02` | **CHECKED-NEGATIVE** | `janzen_circle`: `maximum`/`minimum` = ordinary calculus extrema of $r(z)$ (Morse); `constraint algebra` = GR (companion title) |
| `P03` | **CHECKED-NEGATIVE** | `SdS-slicing`: `extremum`/`maximum`/`stationary` = turning points of the slicing curve / $f'=0$ locus; no optimisation |
| `P04` | **CHECKED-NEGATIVE** | `modern_parallax`: `maximizes` (l.157) = a worst-case-direction **bounding** argument (independence maximises $N$ ⇒ strict lower bound), not an optimisation — no objective, no convexity |
| `P05` | **CHECKED-NEGATIVE** | `groupoid`: `constrained` = a parity constraint from unimodularity; `constraint algebra` = GR (companion title) |
| `P06` | **CHECKED-NEGATIVE** | `shadow_of_existence`: `constraint` = epistemic admissibility condition ("an admissible world must explain"); no feasible region |
| `P07` | **CHECKED-NEGATIVE** | `CR_framework`: `constraint` ×25 = the Dirac/ADM Hamiltonian & momentum constraints, Wheeler–DeWitt $\hat{\mathcal H}\Psi=0$, the Dirac constraint algebra; `Lagrange multiplier` = shift; `variational principle` = instanton |
| `P08` | **CHECKED-NEGATIVE** | `slicing_operator`: `Hamiltonian constraint` stated verbatim ($16\pi\rho={}^3R+K^2-K_{ij}K^{ij}-2\Lambda$) — the ADM constraint, not optimisation |
| `P09` | **CHECKED-NEGATIVE** | `range_paper`: `stationary` = stationary axisymmetric (Killing); `constraint` = "no restriction" English |
| `P10` | **CHECKED-NEGATIVE** | `canonical_time`: `constraint` ×31 = the Dirac fully-constrained system, scalar+momentum constraints, lapse/shift as Lagrange multipliers, Wheeler–DeWitt; `saddle-point` = path-integral saddle |
| `P11` | **CHECKED-NEGATIVE** | `dynamics_paper`: `constraint` = ADM/Gowdy Hamiltonian & momentum constraint (frozen vs true Hamiltonian); `stationary` = stationary spacetime |
| `P12` | **CHECKED-NEGATIVE** | `algebroid`: the paper's very subject is "GR's **constraint algebra** as a symmetric-space structure" — the hypersurface-deformation bracket, not a feasible region |
| `P13` | **CHECKED-NEGATIVE** | `boundary_paper`: `constraint` = GR constraint algebra (l.204/495) or structural/group-theoretic "hard constraints" ($\mathfrak{su}(3)\not\subset\mathfrak{so}(5,1)$); no optimisation |
| `P14` | **CHECKED-NEGATIVE** | `matter_sector`: `constraint`/`constrained` = the proved structural no-go and the GR constraint algebra; `stationary` = stationary sector; no convex objective |
| `P15` | WORKED | `CR_cosmology`: the **Hubble tension read as a convexity statement** — the radiation-pinned ΛCDM $\chi^2(H_0)$ is a "steep parabola minimized near 67 and excluding 73" (strictly convex, **unique** argmin) while CR's $\chi^2$ is "flat in $H_0$" (degenerate Hessian, argmin a line **including** 73), because $H_0$ cancels in the dimensionless BAO ratio when the ruler scales as $c/H_0$ (`CX1`, ALL PASS; numbers from `P15_hubble_expansion_confrontation_v2`, `P15_desi_dr2_confrontation`). Also `minimises one derived ratio` (the refit-bound, `M2_reabsorption_minimization`) — a bare minimisation with no convexity content of its own |
| `P16` | **CHECKED-NEGATIVE** | `cosmogenesis`: `constraint algebra` (companion title); `extremum` = turning point of the collapse branch; no optimisation |
| `p0`  | **CHECKED-NEGATIVE** | `geometric_core`: `constraint` = GR constraint algebroid (Dirac); `duality`/`hyperplane` = projective duality / polar hyperplanes (quadric field, receipted there); `minimum` = geometric throat radius, and l.1061 explicitly **disclaims** a "chosen minimum" of a potential |

⇒ ***Seventeen of seventeen accounted for: ONE WORKED, SIXTEEN CHECKED-NEGATIVE.*** *Reach: **17/17 ACCOUNTED FOR** — 1 **WORKED**, 16 **CHECKED-NEGATIVE by name** (the reading standard; the worked-only figure is $1/17$) — the
lowest of any field, and the finding IS the bounce: convexity proper (`convex`/`convexity`/`KKT`/
`epigraph`/`objective function`) is **×0 corpus-wide**, and the ×143 the order flagged is the
Hamiltonian-constraint homonym of the "problem of time" programme. The field does not bite as an
optimisation field. **The one genuine bite** — worth recording because it is load-bearing and
sky-adjacent — is `P15`'s Hubble tension, which IS a convexity statement: strict convexity (unique
$\arg\min$ excluding 73) vs affine-degeneracy (flat, $\arg\min$ a line including 73), the switch being
the $H_0$-cancellation in the geometric BAO ratio. `CX1` supplies the convexity/degeneracy reading the
paper's "parabola" / "flat" language already carries — the same move `CH1` and `KT1` made for their
fields (the field supplies the step the paper leaves to the reader). The two readers rightly flagged
P15's minimisations as **bare** (no convexity ASSERTED by the paper); the field's WORKED verdict rests
on `CX1`'s checkable lift, and this note records both honestly.*

**⚑ PO-13 / sky-closure relevance.** *`CX1` is directly on the Hubble-tension axis: CR's geometric rate
fits the BAO ladder at $\chi^2/\text{dof}\simeq1$ at ANY $H_0$ (73 included) precisely because its
objective is $H_0$-flat — the tension is dissolved by degeneracy, not by a better fit. This is the
optimisation-geometry statement of the same result the statistics bake receipted (`P15_desi_dr2_confrontation`).
It does not move the acoustic-peak POSITION problem (the ~70σ phase intercept), which remains the sky
grind's binding residual, but it closes the $H_0$ axis cleanly.*
