---
name: probability-ledger
kind: FIELD-BAKE
current: r3618
job: The probability and stochastic-processes field-bake ledger — the sixth and last. It bounced completely, and the bounce is measured rather than asserted.
sources: [chat]
---

> ⛭ **`R1`'s RECEIPT — named here at r3660, because it was named nowhere.** *`receipts/L_probability/R1_the_whole_footprint_is_three_geometry_words.py`* — *the whole footprint is three geometry words — `Gaussian` curvature, general `covariance`, and the string inside `invariance`.* ⌗ *It ran and was registered; **the ledger whose probe it settles did not name it**, so the computation had no argument attached. `check_citation_chain` now fails on that.*

# THE PROBABILITY LEDGER — the sixth field, and the cleanest bounce of the six

> **▣ THE ORDER SAID: *"thinnest. A bounce here is a result."*** *It is a bounce. This ledger's job
> is to make it a **result** — measured, controlled and reproducible — rather than a shrug.*

---

## ⛭ R0 — THE BASELINE

### ⛔ THE FIELD'S VOCABULARY IS ABSENT OUTRIGHT — nine terms, nine zeros

| term | count |
|---|---|
| `probability` · `stochastic` · `Markov` · `martingale` · `Brownian` | **×0 · ×0 · ×0 · ×0 · ×0** |
| `ensemble average` · `Wiener` · `Itô` · `Fokker` | **×0 · ×0 · ×0 · ×0** |

⇒ ***THE CORPUS FITS CMB LIKELIHOODS, QUOTES $\sigma$-LEVELS, AND NEVER WRITES THE WORD
"PROBABILITY".*** *That is not a defect. It is a measurement, and it is this field's headline.*

### ⛭⛭ AND THE ENTIRE APPARENT FOOTPRINT IS THREE WORDS FROM DIFFERENTIAL GEOMETRY

| term | raw | what it actually is |
|---|---|---|
| **`variance`** | **×86** | ⛔ *the string sitting inside **`invariance`** (×69) and **`covariance`** (×47).* **Word-bounded it is ×27** |
| **`Gaussian`** | **×20** | ⛔ ***`curvature` follows it in EVERY SINGLE OCCURRENCE*** — $K_G=1/\alpha^2-M/r^3$, the slicing surface's intrinsic curvature. **Not one Gaussian distribution in the corpus** |
| **`covariance`** | **×25** | ⛔ **GENERAL covariance** — and `P08`'s own **title** is *"Covariance of geometries over the de Sitter substrate"*. ***Zero covariance matrices, zero estimators*** |
| `random` | ×7 | *"600 random heights"* (a numerical check), *"randomised phases wash out"* (a physical argument), and **the Silk random walk** — one genuine stochastic process, named and cited correctly |
| `Gaussian`, `covariance`, `variance` together | | ⇒ ***this field ranked ×7 and its footprint is a curvature, an invariance, and a substring*** |

### ⌗ WHAT SURVIVES, AND IT BELONGS TO A BAKE ALREADY RUN

| term | count | whose |
|---|---|---|
| `cosmic variance` | **×24** | ***genuine and correctly used*** — `P15` carries an *exact cosmic-variance likelihood* over $2\le\ell\le10$. **The statistics-inference bake's object, not this one's** |
| `likelihood` | ×29 | `P15` ×26 — same bake |

---

## ⌗ THE PROBE REGISTER

| # | probe | verdict |
|---|---|---|
| **`R1`** | *is any of this field's apparent footprint actually probabilistic?* | ⛭ **SCORED — NO, and it is measured term by term.** *Nine core terms at ×0; `Gaussian` followed by `curvature` in **20 of 20** occurrences; `covariance` as a matrix or estimator **×0** against general covariance ×9 including `P08`'s title; `variance` ×86 raw reduced to ×27 by the word boundary and to **cosmic variance** by reading.* ⇒ **CONTROL: the same machinery over `likelihood` FINDS it, ×29 with `P15` dominant — so the absences are absences and not blindness** |
| **`R2`** | *the corpus quotes $\sigma$-levels and fits likelihoods. **Is a probabilistic claim being made without the vocabulary?*** | ⛭ **SCORED — NO. Nothing owed.** *`P15`'s statistics are done in the statistics bake's language and receipted there. **Writing "probability" into a paper that already writes "an exact cosmic-variance likelihood" would add a word and no content*** |
| **`R3`** | *the Silk random walk — a genuine stochastic process. Is anything owed there?* | ⛭ **SCORED — NO.** *`P15` and `P16` name it, cite Silk 1968, and use diffusion damping correctly. **A field cannot fence a use that is already right and already attributed*** |

---

## ⛭ THE REACH REGISTER — **17 of 17**

| paper | verdict | what was read |
|---|---|---|
| **`P03`** | ⛭ **CHECKED-NEGATIVE — and it owns the `Gaussian` homonym outright** | ***×10 of the corpus's ×20***, every one Gaussian **curvature** of the slicing surface, with the closed form $K_G=-r''(l)/r$ |
| **`P08`** | ⛭ **CHECKED-NEGATIVE — and its TITLE is the `covariance` homonym** | *"Covariance of geometries over the de Sitter substrate"*. `covariance` ×9, all geometric |
| **`P15`** | ⛭ **WORKED — the only paper with statistics, and it owes this field nothing** | `variance` ×19, of which **cosmic variance** dominates; `likelihood` ×26; the **Silk random walk**. *All correct, all the statistics bake's* |
| **`P07`** · **`P09`** · **`p0`** | ⛭ **CHECKED-NEGATIVE, all three** | `covariance` in the general-covariance sense throughout; `p0`'s ×2 are the *"covariance-of-geometries reading"* |
| **`P16`** | ⛭ **CHECKED-NEGATIVE** | One `Gaussian curvature`, one `random walk`, correctly cited |
| **`P04`** | ⛭ **CHECKED-NEGATIVE — and this is the last of six** | *`P04` returned empty on **all six fields**: integrable systems, index theory, information theory, number theory, numerical analysis and probability.* ⇒ ***That is now a fact about `P04` established six ways: it is the measurement paper, and none of the six fields below the floor reaches it*** |
| **`P01`** · **`P02`** · **`P05`** · **`P06`** · **`P10`** · **`P11`** · **`P12`** · **`P13`** · **`P14`** | ⛭ **CHECKED-NEGATIVE, all nine** | *Screened on all sixteen field terms.* **No probabilistic content in any of them** |

---

## ⛭⛭⛭ THE THREE REGISTERS

### ⌗ WHAT BIT — nothing, and it is stated plainly

### ⌗ WHAT BOUNCED — three, with their blind spots

| # | why it bounced | **what the test that killed it was blind to** |
|---|---|---|
| `R1` | the footprint is three geometry words | ⛔ *`variance` is caught by the word boundary. **`Gaussian` and `covariance` are NOT** — they are real words, used in full, in another field's sense. **The only thing that separates them is the word that comes next**, and that is what this receipt measures.* |
| `R2` | the statistics are done in another bake's language | *A vocabulary screen sees `probability` ×0 and calls it a hole. **It cannot see that the work is being done under different and equally correct names.*** |
| `R3` | the Silk random walk is right and attributed | *An "unnamed theorem" screen looks for a mechanism without a citation. **Here the citation is present and the mechanism is standard**, which is the case such a screen most often mistakes for an opening.* |

### ⌗ THE BOUNDARY

***THIS CORPUS DOES NO PROBABILITY, AND WHERE IT DOES STATISTICS IT DOES THEM UNDER ANOTHER BAKE'S
NAMES.*** *The one genuine stochastic object — photon diffusion as a random walk — is named,
cited and correct.*

⇒ ⚠ ***AND THE FIELD'S ONE CONTRIBUTION IS TO THE SURVEY, NOT TO THE CORPUS.*** *A field can rank
last of six on ×7 and still have its ×7 be **entirely a curvature, an invariance and a substring**.
**The rank was not merely inflated; it was made of a different subject.***

---

## ⌗ THE LEAD REGISTER · THE LANDING TABLE

| register | verdict | destination | state |
|---|---|---|---|
| `R1` | BOUNCED — three geometry words, measured with a control | the receipt | **CHECKED-NEGATIVE** |
| `R2` | BOUNCED — another bake's language | — | **CHECKED-NEGATIVE** |
| `R3` | BOUNCED — right and attributed | — | **CHECKED-NEGATIVE** |

⌗ ***DEPTH BESIDE COVERAGE.*** **Coverage: 17 of 17 read, by name.**
**Depth: 0 bites, 3 bounces — and ALL SEVENTEEN papers owed nothing.**
***That is the cleanest possible result for a field bake and it is not a failure: it is the corpus
telling a field, with a control to prove the screen can hear, that it has no business here.***
