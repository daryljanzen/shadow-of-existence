---
name: the-priority
kind: METHOD
current: r2603+c54.207
description: THE PRIORITY — how the open work is ranked and in what order it is taken up. Imports the predecessor's grounded×informs axes, names the third they were missing, and states when the order stops being obvious. Written r2603.
sources: [chat]
---

# THE PRIORITY

> ***The order has been obvious so far. It will not stay obvious, and the mechanism should exist before
> it stops.***

## ⓵ THE PREDECESSOR'S MECHANISM — **it already exists, and it is `grounded × informs`**

*`scripts/regen_board.py` has carried `score(k) = (grounded × informs, len(veins), grounded)` throughout,
with the board's own header: **"THE LEADS — ordered by how grounded and how informative"**, and an
unlinked lead scored **zero on the map**.*
⇒ ***Imported rather than reinvented.***

**⌗ AND APPLYING IT TO THE ELEVEN `PROTECTED_OPEN` ITEMS EXPOSES WHAT IT MEASURES:**

| | grounded × informs | |
|---|---|---|
| `PO-5` · `PO-9` | **6** | |
| `PO-6` | 4 | |
| `PO-7` · `PO-8` | 3 | | ⛭⛭ **⟨STRUCK r2993 — both clauses answered: the phase is FORCED then DERIVED, and the $0.615$ deficit is a real disagreement at **$76\sigma$** with a **$0.7\%$** substitution error measured on Planck's own spectrum. `kills/PO-7.md`.⟩**
| `PO-2` `PO-3` `PO-4` `PO-10` | 2 | | ⛭⛭ **⟨STRUCK r2996 — the full-spectrum refit performed (215 bins; CR $1.891$/dof vs $\Lambda$CDM $0.983$/dof, $\Delta\chi^2=190.7$) and the odd/even pattern produced ($2.185$ vs $2.200$, sky $2.256\pm0.077$). `kills/PO-10.md`.⟩**
| ***`PO-11` · `PO-12`*** | ***1*** | ⛔ ***and `PO-11` is the LARGEST unbuilt thing in the corpus*** |

⇒⇒ ***So `grounded × informs` ranks TRACTABILITY, not importance: how much a turn spent here will teach
you, given what is already under it. That is the right thing for a LEAD and the wrong thing alone for a
research question.***

---

## ⓶ THE AXIS IT WAS MISSING — **REACH: what would a result BUY?**

*· **3 — a sector.** *Closing it delivers physics the corpus does not currently have.* `PO-11` (the
propagating spinor sector — ⛭ **built at `r3802`**), `PO-5` (the bridge from a grading to a field).*
*· **2 — a claim.** *It converts an open question into a stated result.* `PO-9`, `PO-7`, `PO-10`, `PO-12`.*
*· **1 — a precision or a scope.** *It sharpens something already claimed.* the `PRECISION` and
`OPEN-DOWNSTREAM` ledger entries.*

⌗ ***REACH is a judgement and GROUNDED is a measurement. Keeping them separate is the point: a highly
tractable item with low reach is a good turn and a bad decade.***

---

## ⓷ AND THE THIRD, WHICH THIS SESSION DEMONSTRATED REPEATEDLY — **CONVERGENCE**

***When one calculation serves several items, it outranks its own scores.***
*· ⛭ **The $\mathbb{Z}_2$-graded index** serves `PO-5`'s mod-2 route, P14's **traced** Atiyah–Singer
statement, and P14's **multiplicity** gap — ***one calculation, three names***.*
*· **`PO-seam`'s progenitor derivation** is `PO-7`'s live route ⓷ — ***one derivation, two items, and it
fixes their ORDER***.*
*· `L-207`'s B·2 unlocks on `PO-6`; `PO-12`'s transfer is what makes the 8% signature confrontable.* ⛭⛭ **⟨STRUCK r3001 — all three clauses answered; what remains is the ORDERING, which IS "does the graviton tower's zero-point energy gravitate at the horizon?" — the cc problem, and the decomposition survives either way. `kills/PO-6.md`.⟩**

⇒ ***Convergence is why the order has been obvious: the highest-convergence item has been visibly next
each time. When two items tie on convergence, the order stops being obvious — and that is the moment this
file exists for.***

---

## ⓸ AND A FOURTH AXIS THE FIRST RUN FORCED — **WORKABLE**

⛔ ***The ranking's first run put `PO-7` SECOND.*** *And `PO-7` cannot be worked at all: its one live route ⓷ **IS**
`PO-seam`'s progenitor derivation, and all three computational routes are shut.*
⇒ ***A gated item must not outrank its gate. Convergence measures how much a result would move; it says nothing
about whether the work can START.***
⌗ *So the rank is **(workable, convergence, reach × grounded)**, and a gated item is listed **with its gate named**,
below everything that can be picked up.*

**⇒⇒ AND THAT CORRECTED THE HAND-WRITTEN ORDER BELOW IN ITS FIRST LINE.** *This file had `PO-seam`'s derivation at
#1 and the $\mathbb{Z}_2$-index at #2. **The script puts `PO-5` first** — convergence 3 against `PO-seam`'s 2.*
⇒ ***Both are right about their own axis, and the script's is the one that generalises: `PO-seam` is next only for
`PO-7`, while the index serves three items with no gate on any of them.***

---

# THE ORDER, AS IT STANDS r2603# THE ORDER, AS IT STANDS r2603

| # | item | why it is here |
|---|---|---|
| **1** | ⛭ ***the $\mathbb{Z}_2$-graded index*** | ***convergence 3, the highest in the corpus*** — `PO-5`'s mod-2 route, P14's traced Atiyah–Singer statement, P14's multiplicity gap. **One calculation, three names, and no gate on any of them** |
| **2** | ***`PO-seam`'s progenitor derivation*** | **convergence 2, and it GATES `PO-7`** — nothing else moves `PO-7`, all three computational routes being shut |

| **3** | `PO-6`(c) the Nariai series | **grounded 2** — the expansion parameter is named ($\epsilon\propto\sqrt{M_N-M}$) and $f''(r_n)=-6/\alpha^2$ is computed |
| **4** | `PO-10`'s two runs | **reach 2, grounded 1** — both specified in P7's own words; instrument work on a built instrument |
| **5** | `PO-9`'s three unreproduced links | its kill receipt names them exactly: **the maximal-symmetry step, the plane-section reduction, the rank-1 scale map** |
| ~~6~~ | ⛭ ***`PO-11` the propagating spinor sector — BUILT `r3802`: it propagates*** | ***reach 3 — the largest thing here*** — but **grounded 1**, and *`boundary_paper` calls its construction "the major undertaking"* |
| **7** | `PO-12` the bespoke transfer | reach 2, and `CR_cosmology` says what it would expose |
| **8** | the $\{0,1,2\}$ derivation | **the corpus's one `DISCOVERABLE-PROOF`** — and the corpus says exactly what it would have to produce |
| — | `PO-2` `PO-3` `PO-4` | open physics with no current handle; **mapped, and that is the most that can be true of them** |

---

# ⚠ WHEN TO DISTRUST THIS ORDER

*· **when two items tie on convergence** — the axis that has been doing the work silently;*
*· **when `grounded` is high because a thing has been WORKED, not because it is TRACTABLE** — the two look
identical from the register;*
*· ***when the top item has been top for several sessions*** — that is either a genuinely large thing or a
question stated wrongly, and **r2557's rule applies: when a convention isn't adopted, check whether it is
asking the wrong population**.*

⌗ ***And the honest note: this file ranks eight items and defers three. The three deferred are not
low-priority — they are the ones with no handle, and a handle appearing is itself a reason to re-rank.***

*Written r2603. Stated for reversal.*
