# The book introduction — cosmiCave collection
> **⌖ r1594 — THERE IS ONE INTRODUCTION, AND IT IS `../INTRODUCTION.md`.** Daryl: *"They should be the same…
> It is the book's introduction, and the programme's introduction. Because they're really one and the same,
> aren't they? It all gets published eventually on cosmiCave."* **Correct — the split was a distinction I
> invented at r1475 and it is now closed.** The material this file gathered is **in** the introduction: the
> dipole reading, the reader's-doors map, and the matrix — the last as **Figure 1**, explained in the text.
> **This directory is now the introduction's ASSETS home**, holding the live matrix and these gathering notes.



> **⌖ r1475 — this file is the GATHERING NOTES for the book introduction on cosmiCave.** Candidates land here
> before they are shaped; nothing here is written yet. **Its framings are held at *reach*** — the dipole
> reading, the reader's-doors map (sceptic → P1/P4 · curious → p0/P7 · everyone through P3) — and its prose
> still counts *fourteen papers* and *fifteen documents* against a corpus of seventeen. **The matrix asset
> itself is current** (regenerated 17×17 at r1473).
>
> *(r1474 promoted this material into `INTRODUCTION.md`; struck r1475. It is orientation material for the
> cosmiCave page, not the corpus-and-scope overview the introduction is for.)*

*Started r644 (2026-07-02, Daryl-directed). A running gather-point for the **book introduction on cosmiCave** — the page where* The Shadow of Existence *is eventually published. This is a collection, not a draft: assets and framings land here as the corpus reveals them, to be shaped into the intro/map later. Stated for reversal; nothing here is a paper.*

> **⌖ STATE r1473.** The r796 notice here said this collection and its `assets/dependency_matrix.html` were
> *"stale on purpose, flagged"* — still reading fourteen papers / p0/15 — and *"allowed to grow more stale
> until the arc lands."* **That notice is now itself out of date in both directions.** The html's **prose** was
> brought current at r1284 and names all seventeen (P1–P16 + p0), with P16 described as the newest synthesis
> node. But the **table was not there at all**: the file carried a literal `<table>...</table>` placeholder, so
> the showpiece — the reader's entry-map for the book — **rendered empty**, while the generator that produces
> it prints *"replace `<table>...</table>` in dependency_matrix.html"* in its own output banner. **Regenerated
> from the corpus r1473**: the full 17×17 styled matrix, P1–P16 and p0, both poles marked. What remains owed
> here is this document's own prose numbering and framings, which still speak of *fourteen papers* and the
> *fifteen documents*.
---

## What this page is

The introduction/map for the book on cosmiCave. Unlike the papers, this page is **HTML** — it can carry interactive, prominent showpieces the LaTeX corpus cannot. It is the natural home for the *living* view of the corpus (the map, the dependency structure, the dipole), where a reader meets the whole shape before descending into the fourteen papers. The papers carry the static self-maps; the intro carries the living one.

## The organizing insight (the shape the intro should open on)

The corpus is a **dipole**, not a line. The dependency matrix (below) shows it as data: of the fifteen documents, exactly two — **P7 (the framework)** and **p0 (the geometric core)** — are bidirectionally coupled to the *entire* corpus (full row *and* full column). Everything feeds P7 and P7 feeds everything; p0 is built from every paper and points back to every paper. The other fourteen cluster and gap; these two touch all.

This is (held at **[reach]**, the centre-vs-edge musing) likely *why* the corpus shape resisted pinning for so long: a one-hub corpus is a tree with a root that sits on a line; a **two-hub** corpus has no linear slot — it has a *between*, the fourteen papers the field stretched between the two synthesis poles. p0 "wouldn't take a number" and kept wanting to be "the border" for exactly this reason. The intro is where that dipole belongs — as the frame, not a spine slot.

## The matrix as the reader's entry-point map (Daryl, r646)

The dependency matrix is not only the corpus's self-portrait — it is a **reader's map**. It shows, as data, where each kind of reader should enter: two paths through the corpus, **two doors each**, plus one load-bearing waypoint under both.

- **The deductive path — for the sceptic.** The reader who wants what is *forced* enters at the two forcing keystones: **P1** (`BH_causality`, the *structural* forcing — event horizons are metric singularities, from GR alone) and **P4** (`modern_parallax`, the *empirical* forcing — the redshift-isotropy floor forces the cosmic foliation). These are the ◇ complementary pair the matrix marks — one *proves* it from causal structure, the other *measures* it. The sceptic starts from what cannot be denied.
- **The synthetic path — for the curious.** The reader who wants the *whole* enters at the two synthesis poles of the dipole: **p0/15** (`geometric_core`, the geometric-ontological core — the one intrinsically-real maximally-symmetric object everything is a reading of) and **P7** (`CR_framework`, the framework, where the fourteen converge). These are the two full-row/full-column nodes: enter either and the whole corpus is one step away.
- **The load-bearing waypoint — under both paths.** **P3** (`SdS-slicing-curve`, the constructive home) is the matrix's most-drawn-on hub (the largest column sum): whichever door a reader takes, the road runs through P3, where the substrate's geometry is actually built. A sizeable, load-bearing entry in its own right.

So the intro hands each reader their door straight off the matrix — sceptic → P1/P4, curious → p0/P7, everyone through P3 — with the interactive `assets/dependency_matrix.html` the showpiece that makes the two paths and the hub visible at a glance. This **actively uses the matrix** as the reader's orientation, not as decoration. *(Grounded in the matrix data: P1↔P4 the ◇ pair; p0/P7 the dipole poles, full row and column each; P3 the top column-sum hub. Held at the intro's framing [reach]. Subsumes and sharpens the "two forcings" candidate below.)*

## Assets collected so far

1. **`assets/dependency_matrix.html`** — the interactive 15×15 dependency matrix (heat-shaded; P7 and p0 marked as the poles; ◇ the P1↔P4 complementarity). Built r643 from source citation counts, verified against the r628 sub-block. The showpiece: the dipole is visible at a glance as the two full rows/columns. *(Source data also lives statically as `tab:dependency-matrix` in P7.)*

## Candidates still to gather (grow this list as we go)

- A one-screen **map of the fourteen papers + p0** (the spine, the two poles, the field between) — the reader's first orientation.
- The **P6 through-line** — the book *is* "The Shadow of Existence"; P6 (shadow) carries the title and the epistemic altitude (coherence-not-correspondence, self-consistency≠soundness). The intro should state the discipline the whole book is held at.
- The **two forcings** framing (P1 structural + P4 empirical, the co-equal roots — the ◇ pair) as the entry into *why* the augmentation is forced.
- The **maximal-symmetry unification** (p0 §unification, "worn six ways") as the spine of the argument, at [reach].
- Provenance: seventeen years, the 2012 thesis, the resources folder — the immense interdependency the matrix finally makes legible.

*(Kept deliberately sparse; this fills as the end-of-pass self-knowing pass and the centre-vs-edge musing are worked.)*
