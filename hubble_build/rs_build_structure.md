# r_s build — structure established from source (P9 CR_flatLCDM_v2.tex), do-not-commit
# c22, post-r430 reground. All not claimed; this is problem-posing, not a result.

## Source-confirmed frame (not summary)
- §683: the two open problems stated verbatim by the corpus (medium/c_s ; beginning/limits).
- §665: cosmological beginning = finite-curvature horizon seam; "no finite-time layer S_t reaches r=0."
- §657: reassigned rulings read sinh^{2/3} (the observers); cosh closed-slicing is a DIFFERENT family.
  Two levels: noumenal cosh background (throat R=alpha) vs sinh^{2/3} observer reading (the shadow).
- §531-541 (eq:friedmann-coth, eq:omega-ratio): H^2=(Lc^2/3)coth^2(.) ; Om/OL=csch^2(.).
  => rho_eff ∝ a^-3 at ALL epochs (csch^2 ~ a^-3 for small arg). NO radiation a^-4 term in the rate, ever.
  Omega_m is "the reading of a clock," not independent content. [Hard fact for problem #1.]
- §521-527: sinh^{2/3} amplitude = (2^{1/3}/sqrt3) * alpha, alpha=sqrt(3/Lambda). Only scale is Lambda.

## Where the +74% chimera lives
- Matter-like rate H∝(1+z)^{3/2} => 1/H ∝ (1+z)^{-3/2}; ∫_{z_rec}^∞ = 2(1+z_rec)^{-1/2}, converges but
  accumulates across every decade out to z→∞. Standard radiation era: H∝(1+z)^2 => tail SUPPRESSED.
- Numerically: matter-era comoving sound horizon to a→0 ≈ 277 Mpc (c_s=c/sqrt3); standard ≈144.
  The balloon = the high-z tail that the radiation era would have suppressed. (Diagnostic of the WRONG
  calc, NOT a CR result.)

## The DIRECTION TARGET (sharp, new this turn; not claimed whether the build hits it)
- theta_* = r_s/D_M is DATA (peaks at l≈220; 100theta*=1.041, 0.03%). Fixed.
- D_M = (c/H0)*∫dz/E(z); shape flat-LCDM. So D_M ∝ 1/H0.
- l.681 claim: CR's single H0 = the DIRECTLY measured (late-time) value ≈73; the CMB's 67 is the artifact
  of the radiation-governed r_s CR doesn't share.
- => with H0≈73, D_M ≈ 0.923 * D_M(67.4) ≈ 12800 Mpc. To match the OBSERVED theta_*, CR's intrinsic
  r_s must be ≈ 0.923*144.4 ≈ **133 Mpc — about 7% BELOW standard.**
- So: chimera (251, +74%, LARGER) is doubly wrong — wrong physics AND wrong direction (it infers H0≈39).
  The construction must yield r_s ~7% BELOW standard to make good on the tension-dissolution claim.
  If it yields ≈144 → CR just reproduces Planck (H0≈67, no resolution). If ≈133 → tension dissolved.
  If other → falsifiable. NONE asserted; set by the build.

## Why the build is noumenal-first, not an observer-frame integral (grounded reason)
- In the observer (sinh^{2/3}) frame the beginning (the finite seam) sits at a→0 / z→∞: the PROJECTION
  sends the finite cosh throat (R=alpha) to a→0. So a "finite upper limit because the seam is finite"
  does NOT come for free in the observer frame — the projected beginning is still z→∞.
- Therefore the cut cannot be a clever observer-frame upper limit. It must come from EITHER:
  (#1) the medium ceasing to ring (no oscillating plasma out there), OR
  (the locked frame) the scale being laid on the FINITE noumenal throat first, then projected — the
  observer-frame ∫ to z→∞ is the chimera's error precisely because it integrates in the projection
  across the manufactured a→0 that the real geometry never reaches.
- This is exactly c22 turn-87 / the locked frame, now with the source reason: build r_s on the finite
  cosh throat (noumenal), THEN project to the angle. Do NOT integrate c_s/H dz to z→∞ in the shadow.

## Next build step (the real one)
1. Throat/seam geometry at source: P5 §211/§synchronous, BST §5.4.6 — the cosh background R(T)=alpha cosh(T/alpha),
   throat R=alpha, the seam (past null boundary B), and how the projection maps a throat-era physical scale
   to the observer angle.
2. Problem #1 on the throat: does an oscillating medium instantiate near the throat, or is the imprinted
   scale geometric (the alpha lever: "r_s length needs only c and alpha")? A geometric horizon/throat scale
   vs a thermodynamic plasma sound horizon — different mechanisms, neither built.
3. Whatever the scale is noumenally, project it and read theta_*; compare to 133 (resolve) / 144 (=Planck) / other.

## ===== UPDATE: P5 §211/§synchronous at source — the seam, and a correction =====

### What §211 establishes (source)
- Fundamental metric = closed-dS sphere R(T)=alpha cosh(T/alpha), NEVER below throat alpha.
- §200-209: comoving worldline X(tau)=e^{tau/a}A + e^{-tau/a}B, A,B null. Future asymptote A = per-worldline
  cosmological horizon (NBC p->H+(p)); PAST asymptote B = COMMON to whole congruence. Flat synchronous
  slices = horospheres normal to B; "the synchronous space IS the second ruling."
- §211: as tau->-inf, congruence converges on the single common asymptote B (the "point at t=-inf");
  B is a SMOOTH null generator, finite curvature. The flat-reading r=0/a->0/Big Bang is the comoving
  artifact ("ill-definition of a derivative metric") laid over the throat. Beginning = finite-curvature
  null boundary B, the event-horizon species; NOT the infinite-curvature centre.
- §186: the horizon is "the seam the one congruence CROSSES, not a wall" — dust collapse read inward,
  dust cosmology read outward, ONE congruence through the seam.
- §263 (open problems, source): "the radiation-filled early universe ... is NOT settled here ... principal
  open problem." §266/§263: no-radiation-in-the-rate is "the in-principle distinction ... decided by
  early-universe DATA, not by the geometry." => corpus itself flags problem #1 as unbuilt.

### CORRECTION to last turn's lean (caught at source)
- I leaned: "projection sends finite throat to a->0, so build noumenally / the finite throat floors it."
- §211 + H-check refute the FLOOR reading: the physical CMB-frame observer is the reassigned-ruling
  (sinh^{2/3}) congruence (§657, crux 1), and it GENUINELY reads a->0, H=(1/alpha)coth(3tau/2alpha)->inf
  as tau->0. The throat-finiteness is the ONTOLOGICAL CHARACTER of that beginning (finite-curvature B),
  NOT an observable size floor. So the finite throat does NOT cut the comoving sound-horizon integral.
  The integral still runs to z->inf and converges to the +74% balloon IF a c/sqrt3 medium rings throughout.
- => "build on the finite throat so the size is finite" is NOT a free cutoff. Eliminated.

### Therefore the +74% is killed ONLY by one of (both corpus-flagged open):
- #1 MEDIUM: rho_eff ∝ a^-3, NO radiation term ever => early universe is matter/dust-dominated in its RATE,
  never radiation-dominated. Standard sound horizon needs a relativistic plasma ringing at c_s≈c/sqrt3.
  Open fork: (a) photons instantiate as real ringing plasma (present as projected content, not driving the
  rate) -> c_s≈c/sqrt3 against the matter-like rate = essentially the CHIMERA setup -> +74% -> FATAL
  (predicts first peak l≈117 vs observed 220, ~2x off); OR (b) acoustics reorganized / no standard
  relativistic-plasma era -> no standard c/sqrt3 sound horizon -> different object. UNCONSTRUCTED.
- #2 SEAM-CROSSING: §186+§211+NBC(P9 §665): one congruence crosses B; "before" B is the collapse side.
  Live candidate seam=recombination -> no this-side pre-recomb integral, r_s inherited from collapse side.
  BUT in the comoving reading recombination sits at FINITE a=1/1091 while B is at a->0, so there is naively
  a sub-recombination this-side range (a in (0, 1/1091)). "seam=recombination" therefore REQUIRES that range
  be non-actualised / collapse-side — which routes back through the NBC seam-crossing and into #1.

### Net: the source dig CLARIFIED + eliminated a false resolution; both open problems remain genuinely
### open (corpus-confirmed). The decisive fork is #1: does a c/sqrt3 plasma ring against the matter-like
### rate (=> fatal +74%), or are the early-universe acoustics reorganized? Resolution REQUIRES non-standard
### early-universe physics; the geometry alone does not deliver it (P5 §263). Do-not-assert.
