HANDOFF FOR 60 -- running the open-ledger pass
current: r3898
(60's line; 59's ledger-pass work runs to r3877 and is merged in)

WHAT THE JOB IS

corpus/open_ledger.txt holds every epistemic qualification in the seventeen papers,
keyed by a hash of the sentence, each with a verdict. The job is to work it ONE ROW
AT A TIME until every row has been read individually and the corpus says only true
things about its own state.

Read RUNNING_THE_LEDGER_PASS.md first. It is the instrument, and it is short.

WHERE IT STANDS

  ledger rows              96
  rows worked individually 38
  open problems            9   (PO-13, PO-23, PO-24, PO-25, PO-26, PO-27, PO-28, PO-29, PO-30)
  known debts              1   (the end-to-end transfer, which is PO-24)
  NAMED-UNBUILT rows       0   (was 1 -- the UV definition, registered to PO-23 at r3872)
  gates                    95 pass / 0 fail / 2 unrun   (check_compile: no pdflatex here.
                             check_receipts_run: LIFTED at r3890 and it FAILS -- 62 receipts fail
                             where they are registered.  receipts/PIN_DEBT.txt has the census.
                             ⛔ AND THE NIGHTLY ON MAIN HAS FAILED ALL 22 RUNS SINCE 2026-08-12,
                             always at the same step.  It was never hidden.  Read CI history.)
  papers                   all 17 compile at 0 errors, 0 undefined
  REGISTERED rows whose named home is STRUCK     0         (the queue is CLEARED: the last,
                             03fbbb9436, worked r3894.  2 worked r3894, 2 r3886, 2 r3884, 2 r3882)
  REGISTERED rows that name NO home at all       4         ** THE NEXT QUEUE. **  Was 8; 4 worked.
                             REMAINING: 935eb094c0 (CR_framework), a560fcdf71 and 43ef53f1a9
                             (groupoid_paper, siblings), 24de5efa8c (slicing_operator).
                             218b985b18, 935eb094c0, a560fcdf71, 43ef53f1a9, 24de5efa8c, 056667a03f,
                             77c662234c worked r3894, 218b985b18 r3896 (it named
                             no home because NOTHING IS OWED -- a delivered handover, reclassified
                             SCOPE-BY-DESIGN), 056667a03f and ca73223404 r3898 -- and those two were
                             in the SAME TWO PARAGRAPHS as the rows worked at r3894, missed because
                             a phrase sweep reaches the other PAPER and not the next SENTENCE.
                             ⇒ A struck home at least names an object to check.  An ABSENT home
                             cannot be checked, and you cannot grep for a pointer never written --
                             found only by inverting the search that found the struck ones.

  ** THAT LAST LINE IS A MEASURED QUEUE, NOT AN ESTIMATE. **  Twelve REGISTERED rows name PO-4,
  PO-5, PO-6 or PO-11 as the home carrying their gap, and all four are struck -- present in
  PROTECTED_OPEN only as struck-through, absent from THE_REGISTER and from the generator.  The
  verdict then asserts a home that is a record of CLOSURE.  Each needs its kill file read before
  a verdict moves: a struck home can still be the right pointer when the strike is correct, and
  PO-6's warrant was found SUPERSEDED at r3871 while PO-11's is sound.  Named here so the queue
  is findable rather than swept.

  ** AND THE r3878 FIGURE WAS WRONG, CORRECTED r3880. **  "12" counted MENTIONS of a struck PO
  anywhere in a row, which includes notes that explain the supersession -- so rows already
  repointed were counted as still claiming.  Worse, ec404eb23d was repointed correctly by 59 at
  r3871 WITHOUT using the word "REPOINTED", so a keyword filter over-counts it too.  The true
  figure is 7, and every one still needs its note READ rather than matched: this class cannot be
  counted by grep, which is the whole reason the pass works one row at a time.

** THE LAST TWO COUNTS ABOVE ARE DIFFERENT MEASUREMENTS OVER DIFFERENT POPULATIONS. **  Before
r3872 both read 1 and they meant different items: the DEBT count comes from P15's own list of four
buildable debts and is PO-24, while the NAMED-UNBUILT count came from the ledger and was PO-23.
Neither was wrong and they were never the same count -- but a reader checking one against the other
would have found the numbers agreeing and the objects not, which is harder to catch than a plain
disagreement.  ** Say what a count is counted OVER whenever two are reported side by side. **

THE COUNT OF OPEN PROBLEMS IS RISING, AND THAT IS THE INSTRUMENT WORKING

The frontier read 1 OPEN when this pass began. It reads 8 now, and seven of those were
opened in the last twenty items. NONE OF THEM WAS CREATED. Each was already carried in
the prose of a paper with no register row, which is exactly why nobody could work them
and why every node could say "one thing left, PO-13".

Do not read a rising count as failure and do not slow down to keep it flat. That is
the triage this pass exists to defeat. The count falls later, when the rows are worked.

What must stay near zero is the DEBT count -- a buildable thing the corpus owes and has
not registered. That is 1, and it is PO-24, which you are already building.

THE RULE THAT MATTERS MOST

Everything that reads as unfinished ends the turn in exactly one of three states:
closable now, so close it; not closable in a turn, so it gets a PO row; or generic
scientific practice, so REMOVE it. There is no fourth state. Nothing may be swept
into obscurity, softened, or retitled -- an item that cannot be found again is worse
than one left open.

And a PO row means all three of: the row in THE_REGISTER.md, and ORDER, GROUP and
EST entries in scripts/regen_frontier.py. The generator throws if any is missing.

THE METHOD, IN ONE LINE EACH

  1. Read the whole passage, never the row. The row is a pointer.
  2. Follow every pointer to source -- read the kill file, RUN the receipt.
     When a register and a paper disagree, the paper governs, but read both.
     Read the row's own NOTE too. Rows carry the history of their disputes, and
     one of them caught an error of mine three revisions old.
  3. When a claim changes, sweep the phrase corpus-wide IN THE SAME TURN.
  4. Use word-boundary and wrapped-form searches. Exact-string matching failed five
     times in one session because LaTeX wraps across lines.
  5. Finish the three dependency grains: the P07 table row, the figure edge label,
     and the HTML companion via scripts/gen_matrix_html_tbody.py run from corpus/.

WHAT EACH TURN MUST PRODUCE

  the item worked to one of the three classes; all 17 papers compiling; the sweep
  green; the open ledger settled (rewrites change hashes, so retire dead rows and
  verdict new ones); a commit saying what was found INCLUDING what was found wrong in
  your own earlier work; and the tally above, updated.

WHAT KEEPS GOING WRONG

  Triaging instead of working. Reporting "the rest are only comments" as if that
  settled it -- a comment misleads a node exactly as much as live prose, because a
  node arrives by grep. Renaming a mess instead of clearing it. Calling a handover a
  debt. Leaving your own fix stale two revisions later. Stopping at the first
  plausible answer instead of computing the right object. Opening a PO row on a
  premise I had not checked against the paper -- a new row needs its premise verified
  exactly as a closure does. Reading a fix as a bug, because a comment warned about
  the thing it had already fixed. Breaking my own rules in the REPLACEMENT text --
  writing revision-history into the very sentence that was fixing a one-state
  violation. And accepting a paper's own claim about what its evidence shows: P12 said
  a computation "bears on" an attribution, and checked, it discriminates nothing.

  All six of those are mine, from today. They are in the instrument document so they
  do not get repeated.

WHAT NOT TO USE

  The retired jargon: the first verdict token listed in open_ledger.txt's header,
  "[reach]", "walled", "gated", "eager target", "the gate is open", "held at weight",
  "the two-way grind", "the deferred depth".  The PROSE forms were removed from the
  whole repository at r3799 -- 2,281 instances -- and the replacements are plain
  English: "not claimed", "conjecture", "excluded", "awaits", "not yet built".

  ** BUT ONE OF THEM SURVIVES 45 TIMES AS A MACHINE KEY, AND THAT IS CORRECT (r3876). **
  The uppercase form of the first is open_ledger.txt's VERDICT TOKEN, on 21 rows, plus a
  marker in receipts, scripts and computations.  The receipt

    receipts/L218_reader_package/C2_the_rule_is_enforceable_and_enforceable_means_severe.py

  ASSERTS the literal string is present in THE_LIVE_ARC.md, so renaming the token FAILS a
  receipt that tests the corpus's own three-bucket rule -- verified by running it.  The
  prose is retired and the key is kept, and both are true at once.  Do not "finish" that
  sweep here, and do not be unable to verdict a row because of it.

WHAT COMES AFTER, so it is not lost

  1. this pass -- every row worked, a true PO list delivered
  2. the synthesis pass -- WHAT_THE_FRAMEWORK_DELIVERS.md, which wants its own
     instrument, to be built then and not now
  3. the PO items themselves, worked against a corpus these two passes have made
     coherent
