---
name: routing-plan
kind: RECORD
current: r2428 / c54.178, HEAD aa2b6ee
job: Where each S-item goes, under whose ownership, and which ID band it takes. Written before any commit so the allocation is deliberate rather than "the next integer".
sources: [chat]
---

# ROUTING PLAN — eleven items, two destinations, one band

## ⛔ THE BAND, DECIDED BEFORE ANYTHING ELSE

`corpus/check_id_bands.py` declares:

```
(221, 499)  the observer line (56) -- allocate here
(500, 799)  THE WORKING FORK (54) -- allocate here (adopted r2428: it now reads THE_HUB)
```

**The register's frontier is `L-222`.** This node's work is **instruments, audits, registers and
consolidation** — the observer line's character, and `THE_HUB`'s asymmetry is explicit: *"The fork
owns the papers and the physics. This line audits, instruments, and consolidates … when this line
finds something in the fork's text, it **ROUTES rather than edits**."*

⇒ **Allocate from `L-223` upward, inside `221–499`.** ⌗ *And per `THE_HUB`'s corrected rule —
**allocate far from the other line's frontier, never merely above it** — the fork now sits in
500–799, so 223 onward is far from it in the right direction. No collision is possible.*

---

## THE ROUTING

| item | what it is | destination | owner |
|---|---|---|---|
| **`S-005`** | P15 l.184 assigns r_s/r_D/recombination/the perturbations to the leaf's rate, which its own paragraph calls fatal and which §1·SHADOW names as the seeded collapse | **`FOR_54.md`** | fork — *a paper* |
| **`S-010`** | `P15_two_arm_control_and_guard.py` pins figures from the c54.168 instrument; rebuilt at c54.178; three of twelve integers stale, floor 1.66% → 0.16% | **`FOR_54.md`** | fork — *its instrument and receipt* |
| **`S-002`** | the receipt's PART 4 attributes an exponent of −2 to the fold that the fold's own data never gave; and the c54.170 split shows the fold's −0.97 is the **Euler** column, not the driven one | **`FOR_54.md`** | fork — *a receipt narrative* |
| **`S-004`** | PART 5's registered comb residual (0.72–0.79 of π/r_s) **is** PART 2/3's k-dependent phase; relation stated, control passes on ΛCDM | **`FOR_54.md`** | fork — *offered as a candidate resolution of front #5* |
| **`S-007`** | the r2123 convention (seam ≠ branch point) has three residual sites in `ONTOLOGY_FOUNDATION_INDEX`, one of them §1b link 3 | **this line** | lens |
| **`S-009`** | the heavy tier IS verifiable in this container; 288/288 pass; `RUN_RESULT.txt` stale at 264/8/4; `THE_HUB`'s CI prose behind its own workflow | **this line** | instruments |
| **`S-011`** | the stale-pin scan — *was the instrument revised after the pin was taken?* — with its scope stated (15 of 302 receipts declare a build) | **this line** | a new gate, offered |
| **`S-012`** | **CI's fast tier is red at HEAD; `set -e` means fifteen text gates and the lint never run on any push** | **this line** | CI · ⚑ **take first** |
| **`S-013`** | `THE_METHOD` §7's status column wrong on three rows in three ways; and `JARGON_LEDGER`'s r1724 diagnosis unacted-on for ~700 revisions | **this line** | a METHOD document |
| **`S-003`** | self-correction: the shipped handoff led with raw ℓ₁ and "invariant" against `PO-7`'s explicit rewording | **neither — mine** | *filed as the record; the handoff cannot be recalled* |
| **`S-006` / `S-008`** | one resolved negative, one withdrawn at its own prior-art check | **neither** | *kept as worked instances of restraint* |

## ⇒ ORDER

1. **`S-012` first and alone.** *It is the only item that makes the others harder to land: while the
   fast tier aborts on its first command, no push of mine gets a gate report either.*
2. **Then the four `FOR_54` items as inbox entries** — routed, not edited, per the standing rule.
3. **Then the four this-line items**, each with its register row.
4. **`S-003` filed wherever the fold's arc is carried**, as a correction to a shipped document.

## ⌗ WHAT I HAVE NOT DONE, AND IT IS DELIBERATE

- **No file in the repository has been modified.** *The clone is pristine apart from four PNGs the
  receipt suite regenerated, which are reverted.*
- **No register row has been opened and no ID claimed.** *`S-` numbers are private to this reading.*
- **Nothing has been pushed**, and pushing needs a fine-grained token that `THE_HUB` says is
  **never stored** — supplied per session, held in a shell variable, never written to a tracked file.
