---
name: collapse-excursion-transfer-build
description: Attack manual for the reshaped bounded cluster (open-problems families 1/3/4/5) — ONE build on the progenitor collapse excursion (L2), family 5's high-ℓ acoustic driving envelope the first target. Read WITH THE_OPEN_PROBLEMS_LEDGER family 5 (r1408) + OPEN_PROBLEMS_MAP E3 (r1408).
kind: RECORD
current: r2517+c54.201
job: PHASE 7's ATTACK MANUAL — the one build on the progenitor collapse excursion (L2) behind open-problems families 1/3/4/5. Referenced live by eleven documents including THE_PLAN, PHASE7_BUILD_LEDGER and both open-problems documents.
sources: [chat]
---

> **⌖ RECORD — an attack manual for a cluster that no longer exists.** *It plans one build on the
> progenitor collapse excursion for "open-problems families 1/3/4/5", and **those families belong to the
> ledger's `L-`era**; its `PO-8` is struck in the register that closed at r3001. It also directs the
> reader to `OPEN_PROBLEMS_MAP` E3 — **that map is retired, every one of its rows struck**.*
>
> ***Its object is live under a different name:*** *the high-$\ell$ acoustic driving envelope on the
> collapse leg is register row **`PO-13`**, the misplaced phase. Work it there.*


> **⌗ CHECKED AGAINST c54.163→c54.185 AND DECLARED CURRENT r2449.** *The span's substantive content is **the derived
> lensing and the acoustic front** (c54.181–184), **a lint rebuild** (c54.180), **bibitem repairs** (c54.185), and
> the fork's application of routed items 1–9. This line's own r2417–r2448 added `ARC 16`/`ARC 17`, the `L-150` and
> `L-213` closures, six outside readers' findings, and two lints.*
> ⇒ ***None of it bears on the collapse-excursion transfer build's own setup and layer assignment.*** *The question asked was not "what moved in the file" but* ***"did anything the
> span established or withdrew bear on what this file says"*** *— and the answer here is no.*
> ⚠ *Stated rather than assumed: **the fourteen documents swept beside this one all turned out to hold real missed
> landings**, so a declaration of no-effect is a claim and is made as one.*


> **⌗ INDEXED r2380, NOT RETIRED — and this is the `ARC 4` lesson instance for this batch.** *It sits in a
> `*_build.md` filename among four siblings that were all records, and **it is a live resource**: eleven
> documents reference it, `THE_PLAN` names it as `PHASE 7`'s **attack manual**, and `PHASE 7` is register row
> `L-197`. **Filename does not predict disposition — three for three in `ARC 4`, and now four.***
>
> **⚠ TWO OF ITS FOUR FAMILIES HAVE SINCE CLOSED, and the reshaping it rests on is therefore half spent.**
> *It was built at r1408 as **one build, four readings** across open-problems families 1/3/4/5.* **Family 1
> closed c54.113** *(there is no worldline crossing to compute)* **and family 4 closed c54.118** *(the
> irreducible remainder ceased to be a frontier of this construction; `PO-8` struck r2378).* **Families 3 and 5
> are live — register `L-150` and `L-147`.** ⌗ *So the "one build, four readings" claim is now **one build, two
> readings**, and a node reading this manual should know that before planning from it. **The build itself is
> untouched by the closures** — the collapse excursion is still the object — which is why this is an annotation
> and not a retirement.*
>
> **⌗ AND ITS SUBJECT IS WHERE THE WORKING FORK IS RIGHT NOW.** *`computations/beyond_the_wall/` gained
> **29 scripts** across c54.114–134 on exactly this — the collapse leg's scale invariance, the mode map, what
> the sky permits of the parent. **This manual is the plan; that directory is the execution**, and the two have
> never been read against each other. *Not a defect this line resolves — it is the fork's live edge.**

# The collapse-excursion transfer — the one build behind families 1/3/4/5

> **⌗ RE-READ AGAINST c54.163 AND DECLARED CURRENT r2396 — same caveat as `ACOUSTIC_BUILD_SETUP`, and sharper
> here.** *Its `species` is **species densities on the L2 leg**, untouched by the withdrawal.* ⚠ *But this is
> **`PHASE 7`'s attack manual** (`L-197`), its four-family reshaping is already **half spent** (families 1 and 4
> closed at c54.113/c54.118 — its own newest marker being the revision that reduced it), and* ***the fork has
> since worked precisely this front to c54.163.*** ⇒ **The manual is the plan; `beyond_the_wall/` is the
> execution; they have never been read against each other.** *Dated so the gate reports a number rather than
> nothing — **not because the front behind it has stood still.***


> **⌗ DATED r2390 — `c54.118`, which is exactly where its own reshaping stopped being whole.** *It was built
> r1408 as **one build, four readings** across open-problems families 1/3/4/5.* ***Family 1 closed c54.113 and
> family 4 closed c54.118 — so its newest marker is the revision that made it "one build, TWO readings."***
> ⌗ *That coincidence is worth stating plainly: **a document's last touch is often the change that reduced it**,
> and here the two are the same revision.* **The build itself is untouched by the closures — the collapse
> excursion is still the object — which is why this is a date and an annotation rather than a retirement.**


Established **r1408**, Daryl-directed, from holding the CR expansion ontology (P7 bead +
three-level rate rule; P15 perturbation sector). This is the *positive path* for the cluster the
family-5 reshaping brought into focus. Stated for reversal.

## What this is (the reshaping — one build, four readings)
Holding the ontology showed that the "closable four" open-problems families are NOT four separate
items. They are four readings of **one object — the progenitor collapse excursion / seam-handover**,
the bead's contracting **L2** leg, the same history the cosmogenesis paper **P16** already integrates
a nuclear network on (compression → peak → turnaround → re-expansion):
- **1** — the matter worldline-and-field dynamics ON the excursion.
- **3** — the datum (ρ_r/ρ_m≈2, A_s, n_s) the excursion DEPOSITS; its open half is the handover derivation.
- **4** — the irreducible interior reassignments, gated on 1's dynamics (same excursion).
- **5** — the acoustic driving envelope evolved THROUGH the excursion (the piece that pulled this into focus).

So "close the bounded four" is really **"build/extend the one collapse-excursion model (P16-anchored),
off which 1/3/4/5 close together."**

## The ontology held (do NOT violate — this is what tripped two nodes)
The three-level rate rule (P7 lapse/shift/bend remark; P15 §tensions):
- **L1** — the foliation stacking rate, radiation-free sinh^{2/3}, the lapse; what observable cosmology
  rides from the seam OUTWARD; r_s AND r_D are both L1. Modes are read out through this.
- **L2** — the leaf's local rate, ordinary Friedmann, radiation GRAVITATING NORMALLY, on the progenitor's
  collapse excursion INWARD; where BBN runs. **The acoustic driving lives here.**
- **L3** — the E=1 projection, the shift, observation as distance/redshift.
The bead (P7 Thm:bead) ties L1 and L2: driving is set on the contracting L2 leg (radiation genuinely
gravitates — not a trick), modes cross the seam sub-horizon carrying it, then are read through the
radiation-free L1 expansion. The physics is carried ON the layer, THEN projected (§CR-FLRW capstone).

## ★ THE TRAP (burned two nodes — do NOT re-run as a finding)
Taking ΛCDM's C_ℓ as the high-ℓ peak envelope and applying the +8% radiation-free damping
(`full_transfer_verdict`) manufactures a spurious "decisive high-ℓ deficit." §coherence DISOWNS it:
assuming the high-ℓ peaks equal ΛCDM's "is precisely the unbuilt part, and it may not be imported for
free." The time-reversal result (`time_reversal_driving`) buys the driving equality ONLY at the acoustic
scale |Ψ̃(ω=1)|, NOT the whole k-dependent envelope. Importing ΛCDM's transfer wholesale is the
§CR-FLRW capstone's named reification — running the synchronous shadow's machinery as the existent.

## The shared object (now concrete, from holding P16)
**P16's collapse-worldline thermal history — `fig:history`.** Along the E=1 (Painlevé–Gullstrand,
flat-slice) collapse worldline: adiabatic infall HEATS the plasma to a peak T_pk ≳ 170 MeV (the QCD
scale, M-independent, full dissociation; §peak) → TURNAROUND → the COOLING LEG re-expands and cools it
back through the nuclear window down to the ~1.6 eV onset (z≈6850), the seam. On the cooling leg the
rate is the STANDARD Friedmann rate |H|=√(8πGρ/3) with radiation GRAVITATING (L2; §rate) — "identical
to standard BBN at every temperature in the window," differing from an expanding radiation era only by
the sign of ȧ. Freeze-out is time-reversal-violating (§trev): the abundances (and the coherent handover)
are made only on the COOLING leg, after turnaround — not the infall. P16's nuclear network already runs
on exactly this history (Y_p, D/H at observed values; η the baryon loading, held distinct from ρ_r/ρ_m
the acoustic scale). THIS worldline is the object families 1/3/5 stand on; step 1 is extracting it as a
clean reusable background. (Its computational backing — `peak_temperature.py`, `validate_bbn`,
`cooling_leg_reduction`, `freezeout_trev_toy` — is among c47's receipt-suspect P15–16 set; see the flag.)

## The Friedmann–BH identity (P2/P3/p0) — one congruence, read two ways [pulled in r1408]
The collapse leg (L2) and the expansion leg (L1) are NOT two backgrounds stitched at the branch point — they are
ONE Friedmann congruence read two ways, and the corpus already says so:
- **P2 (the cycloid):** the Schwarzschild-interior cycloid r(z)=M(1+cos z) is TERM FOR TERM the closed-
  Friedmann dust scale factor a(η)=(a_m/2)(1+cos η) — the Oppenheimer–Snyder identity, "the black-hole
  interior and a closed universe are one curve." The classical (Λ=0) seed; the continuation through r=0
  carries it not to a recollapse but to our OPEN expanding cosmology.
- **P3 §603 (the load-bearing one):** "read INWARD it is the collapsing leaf's own self-gravitating
  dynamics — the local rate, its content gravitating, where the light elements freeze out — and read
  OUTWARD it is the observable foliation's radiation-free stacking rate, the two being ONE congruence
  crossing the one seam rather than two regimes, exactly as the marginally-bound cosmological geodesic
  is dust collapse read inward and dust cosmology read outward." This IS this build's L2/L1 split, stated
  as a Friedmann–BH identity.
- **p0 §228 does NOT belong here — ⚠ DO NOT CONFLATE (guard, r1408):** the static / Hubble–Eddington
  radius r_⋆=(Mα²)^{1/3} (f′=0, K_G sign-change) is a PRESENT-UNIVERSE structure boundary — the largest
  shell that stays bound against the Hubble flow (Pavlidou–Tomaras; P3 §639). It is a DIFFERENT locus
  from the collapse-excursion **comoving turnaround** r=−(2Mα²)^{1/3} (1−f=0, where the E=1 congruence
  turns, on the lap) and from the **seam** (Nariai, f=0 horizon merge). P7 `lem:twoturnings` proves the
  turnaround cubic (r³+2M=0) and the horizon cubic (r³−r+2M=0) "affinely inequivalent, no canonical
  identification." The Friedmann–BH connection used here is the collapse↔expansion reading of the BEAD
  (P2 + P3 §603) ONLY; the Hubble–Eddington radius does not enter it. (An earlier draft mashed them —
  corrected r1408.)

**How it sharpens the build:**
1. The L2 collapse background is a RIGOROUS Friedmann background (Oppenheimer–Snyder), so the photon-
   baryon oscillator machinery validated on ΛCDM (step 2a) transfers to the collapse leg EXACTLY, read
   inward — not by analogy.
2. Set the whole transfer up as ONE marginally-bound (E=1) Friedmann congruence carrying the acoustic
   modes, the rate-READING (which content sources the stacking) flipping at the branch point — one equation, not
   two stitched backgrounds. The flip is at the SEAM (the Nariai reassignment, f=0 merge), which is a
   DISTINCT point on the lap from the comoving turnaround where the collapse stops (1−f=0) — P7
   `lem:twoturnings`. Keep those two separate.
3. It sharpens the seam→recomb crux (step 2b): "radiation content on a radiation-free rate" is the
   OUTWARD reading — the density is leaf-carried (gravitates the LEAF, per the slicing operator) while
   the stacking is radiation-free (the foliation). So the driving there is the leaf dynamics read through
   the L1 stacking — a principled thing to POSE and compute, not an ad-hoc "no driving." Whether it
   leaves residual driving is the open piece; the layered reading tells us how to set the question.
   Do-not-assert on the outcome.

## The path (ordered; grounded first, compute second)
**Step 0 — GROUNDING (now):** hold **P16** (the cosmogenesis paper) at weight — the collapse-excursion
model, the cooling-leg physics, what is actually built. Completes P7 + P15 + P16, the ontology Daryl
said must be held for this calculation. *(Distinct from — and prior to — the LATER audit of c47's r1391
receipt code, which is receipt-suspect; see the flag below.)*

**Step 1 — the shared background:** extract P16's collapse-excursion history as a clean, reusable
background object (the L2 leg: rate + species densities vs the collapse-time coordinate). One object,
read by 1/3/5.

**Step 2 — the acoustic driving envelope (family 5's open core; the decisive computation):**
- **2a VALIDATE FIRST** — build the photon-baryon acoustic oscillator (Θ0'' + c_s²k²Θ0 = driving, with
  baryon loading R), run it on a STANDARD radiation era, and reproduce ΛCDM's peak-height pattern
  (P1/P2≈2.2, P3/P1, the envelope) from the machinery itself. NEVER import ΛCDM's C_ℓ. This is the code
  check the held picture §7 mandates and the step skipped last time.
- **2b RUN ACROSS THE TWO LEGS** — holding P16 sharpened *where* the CR-specific piece lives; the
  driving splits:
  · **Cooling leg (L2, radiation gravitating, standard Friedmann):** a LONG standard radiation era
    (T_pk ~170 MeV → 1.6 eV seam), so the acoustic driving here is ORDINARY — modes reach the seam
    carrying essentially ΛCDM's driven amplitude. NOT the exotic part.
  · **Seam→recombination leg (L1, radiation-free):** here CR departs from ΛCDM. The plasma still has
    radiation CONTENT (ρ_r/ρ_m ~2 at the branch point → ~0.2 at recomb — acoustic pressure, baryon loading),
    but the RATE is radiation-free (L1). ΛCDM crosses matter-radiation equality on this same leg and
    gets substantial residual radiation-driving; CR, radiation-free, does NOT. How the potential Ψ (the
    driving) evolves under "radiation content + radiation-free rate" — a combination ΛCDM never has — is
    THE genuine open piece. This is exactly P15's "the radiation-free rate reshapes the high-ℓ driving
    envelope, removing the radiation-driving boost" — a real effect at a specific leg, not a confusion.
  Evolve Ψ(k,t) across both legs. Schematically the CR high-ℓ envelope = (standard cooling-leg driving)
  − (the seam→recomb residual driving ΛCDM has and CR lacks), then the L1 Silk damping (+~9%). Whether
  the net is deficit / wash / enhancement is the transfer — HELD OPEN, as P16 itself holds it (§scoping:
  "genuinely open, neither a demonstrated tension nor a wash"). This is precisely why importing ΛCDM's
  full C_ℓ is wrong: it keeps the seam→recomb residual driving CR does not have.

**Step 3 — assemble the transfer ON THE LAYER:** seam handover (faithful transmission, κ=0 Nariai seam;
the modes sub-horizon, coherent phase, inherited A_s/n_s) → propagate on the L1 radiation-free rate with
Silk diffusion (r_D on L1, the +~8% scale) → E=1 (L3) projection → C_ℓ. Compare to ΛCDM and to the data
(peak heights + the full-spectrum likelihood). Family 5 to a verdict, built correctly.

## The first move to recommend
**Hold P16, then specify the shared background object (steps 0–1).** Grounded, non-barrelling, and it
serves the whole cluster before any spectrum is touched.

## Discipline (pinned)
- **Do-not-assert** on the high-ℓ envelope until 2b is actually run; hold both outcomes (tracks / departs).
- **Validate the machinery on ΛCDM (2a) before concluding anything (2b).** Source and one's own build
  over any ✔✔ stamp.
- **★ RECEIPT-SUSPECT (Daryl, r1408 — LATER, not now):** the r1391 P15–16 receipts this build touches —
  `cr_collapse_driving`, `time_reversal_driving`, `full_transfer_verdict`, `damping_ratio_clean`, and the
  P16 cooling-leg network — were run by node **c47** while grinding near the end of its rope, and they
  misled a later node into the false "decisive tension." Owed a critical re-read (avenue 11 / phase R)
  before being leaned on. The cross-corpus clue-maps in the ledger/map trace what each feeds.
- The damping *scale* (~8%, r_D on L1) is settled and non-reabsorbable; only its high-ℓ *spectral*
  consequence — entangled with the step-2b driving envelope — is open.

## ▶ STEP 1 DONE (r1415) — the collapse-worldline background as ONE reusable object
Built + verified at source (P16 sec:rate + sec:scoping), `transfer_build_work/background.py`. Fresh from
the physics, NOT leaning on the c47-suspect receipts — and it reproduces the P16 anchors independently:
z_onset≈6817 (P16 ≈6850), ρ_r/ρ_m=2.000 at the branch point, H_L2/H_L1=√3 exactly. Exports the reusable object:
- content (leaf-carried, SAME in CR and ΛCDM): `rho_ratio(z)`, `R_baryon(z)`, `cs2(z)` (sound speed, baryon loading).
- the TWO rates: `E_L1(z)` radiation-FREE stacking (√(Ω_m(1+z)³+Ω_Λ)) and `E_L2(z)` radiation-INCLUDED
  window/ΛCDM (√(Ω_m(1+z)³+Ω_r(1+z)⁴+Ω_Λ)); seam markers `z_onset`,`z_eq`.
- **THE CR-SPECIFIC PIECE made explicit:** on the seam→recomb leg (z 6817→1100, ρ_r/ρ_m 2→0.32) the rate
  ratio H_L2/H_L1 runs √3→1.15 — ΛCDM crosses matter-radiation equality WITH radiation gravitating, CR
  does NOT. The transfer rides radiation CONTENT (c_s, R) on the radiation-FREE rate (L1). That is the four-
  way knot's physical seat. **Next (Step 2a): the photon-baryon oscillator, validated on ΛCDM first (NEVER import C_ℓ).**

## ▶ STEP 2a PARTIAL (r1416) — oscillator machinery: scale/phase VALIDATED, driving envelope is the crux
`transfer_build_work/oscillator.py`. **Validated on ΛCDM (no C_ℓ imported):**
- **r_s = 144.4 Mpc** (P15/CAMB: 144.4) — EXACT. CR preview (L1 rate, onset z_onset): r_s=145.6, consistent
  with P15's matched scale (the L1 r_s ≈ the ΛCDM r_s ≈ 144, which is why θ_* is met; the ~8% signature is
  r_D 9% longer while r_s matched).
- **oscillation positions:** peaks at k r_s ≈ nπ ✓.
**NOT yet validated (deliberately not faked):** the driving ENVELOPE / peak HEIGHTS — a naive driving boost
gives P1/P2=10.4 vs ΛCDM's 2.2. Reproducing the ΛCDM heights needs the real potential evolution (radiation-
driving boost + Silk damping from the coupled photon-baryon-matter-Φ system), NOT a heuristic. This is the
crux of the four-way knot (the high-ℓ driving envelope), so it gets the real computation.
**Step 2a-proper / 2b (next):** build the coupled tight-coupling + potential ODE, validate the ΛCDM heights
(P1/P2≈2.2, P2/P1≈0.45, P3/P1≈0.44), THEN swap the driving to the collapse-phase (time-reverse of the
radiation era) + the radiation-free seam→recomb leg, and TEST at high-ℓ whether the driving equality holds
or the radiation-free rate reshapes the envelope. This is the critical re-read of the c47-suspect receipts
(`time_reversal_driving`, `cr_collapse_driving`, `full_transfer_verdict`), built from scratch.

## ▶ STEP 2a-proper STATUS (r1417) — Boltzmann built + stabilized, NOT yet validated; a strategy fork
`transfer_build_work/boltzmann.py`. Built a compact tight-coupling Boltzmann (photon+baryon+CDM+Φ,
Newtonian gauge). **Fixed the instability** (initial Φ violated the super-horizon constraint → blow-up;
cured by evolving Φ dynamically via the momentum constraint with consistent adiabatic ICs) — it now gives
stable O(1) oscillating Θ₀+Ψ. **But it does NOT yet reproduce the ΛCDM peak structure:** the radiation-
driving boost overdrives (no neutrino free-streaming; Silk cutoff not yet right), so high-k peaks run away
instead of P1 dominating (scan found one dominant peak near k r_s≈2π). Reproducing the ΛCDM peak HEIGHTS
from scratch is a genuine Boltzmann-code project — the corpus used CAMB (`camb_reference.py`) for that
baseline. NOT faked. **r_s validated exactly (144.4) stands; the knot is precisely localized.**
**STRATEGY FORK (Daryl to steer):** (A) debug the from-scratch Boltzmann to full validation (slow, re-does
CAMB); (B) use a validated code (CAMB/CLASS) for the ΛCDM baseline + CR as a modification; (C) [Arthur's
lean] compute the CR−ΛCDM DIFFERENCE directly via the driving integral |Ψ̃(ω=1)| — collapse-side (time-
reversal-matched to ΛCDM) vs the seam→recomb residual driving on the radiation-free rate — the actual open
physics, needs only the (more stable) potential evolution, doesn't duplicate CAMB, and is the cleanest
high-ℓ test of the time-reversal driving equality. No census harvest this pass (a compute pass, not a new
paper read). Bundle cut r1417.

## ▶ (C) FIRST RESULT (r1418) — the seam-straddling diagnostic: the reshaping is REAL and LOCATED
`transfer_build_work/seam_straddle_diagnostic.py`. (C) run as the targeted compromise. The potential ODE
blew up a 3rd time (I do not cleanly implement the stable GR perturbation potential in-sandbox — taken as a
real limit, not bashed further). But the ODE-FREE horizon-crossing diagnostic delivered a genuine sharpening:
- **Every acoustic mode crosses the horizon far ABOVE the seam** (z_cross ≈ 3×10⁴ [peak 1] → 6×10⁶ [ℓ~3000]),
  while its radiation-driving DECAY runs down through matter-radiation equality z_eq=3408. **The seam
  z=6817 sits BETWEEN crossing and equality**, so the driving decay STRADDLES the seam:
  pre-seam (collapse, L2, radiation-included) = time-reversal-matched to ΛCDM; **post-seam (seam→eq, L1
  radiation-free) = where CR departs.** The reshaping is REAL, located, non-trivial — NOT complete on the
  collapse side.
- **Vindicates the corpus caution with a clean argument:** you cannot assume peaks=ΛCDM + extra damping,
  because the driving decay itself lives partly on the radiation-free leg. `full_transfer_verdict`'s deficit
  IS a trap, and now we know why (the straddle).
- **The competing damping (settled):** r_D 9% longer → damping-only CR/ΛCDM = 0.95→0.74 across ℓ~1000–2500
  *if peaks matched* — but they don't simply match, so the NET sign is open.
- **(C)'s reach (Daryl's caveat, hit exactly):** (C) LOCATED and CONFIRMED the open physics but cannot SIGN
  it — the sign needs the post-seam driving reshaping (the potential ODE). **Escalation: a validated
  Boltzmann (CAMB/CLASS) for the post-seam potential decay on the two rates, or a careful stable potential
  implementation.** This is the four-way-knot's remaining core, now precisely posed.
**Knot status: sharpened from "open, unlocated" → "open, located at the post-seam driving decay, corpus
caution confirmed, sign pending a validated transfer."** A real advance on family 5 / the four-way knot.

## ▶ CAMB VALIDATED + the ell-dependent potential diagnostic (r1419)
CAMB 2.0.0 installed and validated: LCDM peaks ℓ=220/536/813/1126/1421/1726, P1/P2=2.21, P2/P1=0.45,
P3/P1=0.44 — matches the corpus camb_reference exactly. The validated Boltzmann baseline is in hand.
**Weyl potential (frac of super-horizon) at seam(6817)/eq(3408)/rec(1089):** k=0.02→0.945/0.856/0.574;
k=0.05→0.74/0.43/0.21; k=0.10→0.30/0.11/0.10; k=0.20→0.05/0.045/0.037.
**Diagnostic — the driving-decay is ell-DEPENDENT relative to the seam:** HIGH-ℓ (k≳0.1) decayed BY the
seam → driving complete pre-seam → collapse-side → time-reversal-matched to ΛCDM; LOW-ℓ (k~0.02) still
large at the branch point → driving continues POST-seam on the radiation-free leg.
**Two consequences, both do-not-assert:**
1. **Corrects r1418:** that diagnostic used E1 (radiation-free) for the horizon (per prop:subhorizon),
   forcing a uniform far-pre-branch-point crossing; on the physical radiation-era rate the crossing is ℓ-dependent,
   as CAMB's potential confirms directly. The "uniform straddle" was wrong.
2. **Tension with the corpus framing** (low-ℓ matches through 3rd peak / high-ℓ open): CAMB puts the
   pre-seam/matched driving at HIGH-ℓ and the post-seam/radiation-free-leg driving at LOW-ℓ — the opposite
   split. NOT claiming the corpus wrong (needs CR's actual potential, not ΛCDM's), but the tension is real.
**DECISIVE NEXT STEP:** test the time-reversal driving equality DIRECTLY — |Ψ̃(ω=1)| from CAMB's ΛCDM
potential vs the time-reversed collapse-side version, mode by mode. Settles whether pre-seam driving matches
(the hinge for both the high-ℓ deficit and the low-ℓ tension). Also feasible now: extract CAMB's full seam
state as ICs and re-evolve post-seam on the L1 rate (short, well-conditioned) for CR's own potential.

## ▶ THE HINGE + THE CONDITIONAL DEFICIT (r1420) — careful, and it tilts against CR at high-ℓ
Computed with validated CAMB. **The four-way knot reduces to ONE hinge: does the high-ℓ acoustic driving
match ΛCDM?** And the careful reasoning tilts toward YES → a likely real high-ℓ deficit:
- **CONDITIONAL deficit (IF high-ℓ peaks match ΛCDM + settled 9% r_D):** CR/ΛCDM D_ℓ = 0.92(ℓ800) /
  0.75(1500) / 0.59(2000) / 0.44(2500) / 0.31(3000). Large, ACT/SPT-visible → a real tension, not subtle.
- **Why the driving likely matches (→ deficit stands):** CAMB's validated Weyl potential is already decayed
  to ~0.05 by the seam for k=0.2 (ℓ~1700) — high-ℓ driving is complete PRE-seam = collapse side = ΛCDM by
  the corpus's own time-reversal equality. So high-ℓ peaks ≈ ΛCDM's, and the settled damping deficit stands.
- **The escape is narrow and argues against:** avoiding a tension needs CR's high-ℓ peaks HIGHER than ΛCDM's
  (to offset the damping), i.e. collapse-phase driving EXCEEDING ΛCDM's — but time-reversal says it EQUALS,
  and the corpus's stated reshaping (removing the radiation boost) makes peaks LOWER, worsening the deficit.
**Honest verdict (do-not-assert):** the corpus's "neither tension nor wash" is fair agnosticism, but the
careful direction — CAMB potential + the corpus's own time-reversal claim — points toward a LIKELY REAL
high-ℓ deficit / tension for CR, exposable by damping-tail data. The one thing that overturns it: CR's actual
collapse-phase driving exceeding ΛCDM's (the remaining computation; time-reversal argues against it).
**This is a place careful work suggests the corpus is more exposed at high-ℓ than its framing conveys —
flagged for the open-problems map (family 5) and P15 §coherence/§scope, do-not-assert.**

## ▶ DECISIVE (r1421) — high-ℓ driving is PRE-seam/matched → real deficit; the corpus ℓ-split is INVERTED
`transfer_build_work/driving_epoch_test.py`, CAMB. Pinned the radiation-driving decay epoch per mode:
k=0.05(ℓ~700) z_drive=3995, 32% pre-seam · k=0.10(ℓ~1400) 8266, 77% · k=0.15(ℓ~2110) 12524, 98% ·
k=0.20(ℓ~2810) 16810, 98% · k=0.30(ℓ~4220) 25469, 98%.
- **HIGH-ℓ (ℓ≳1400): driving 77–98% PRE-seam** = collapse side, L2 (radiation-INCLUDED) → matched to ΛCDM
  by the corpus's own time-reversal equality; the radiation-free L1 is POST-seam and does NOT touch it. So
  the corpus caveat "the radiation-free rate removes the high-ℓ driving boost" is **MISATTRIBUTED** — the
  boost lives on L2 pre-seam. **No driving escape → the settled 9% r_D gives a REAL high-ℓ deficit** (sign
  robust; magnitude leading-order ~tens of %, wants the full transfer).
- **INVERSION (bonus):** the driving that runs on the radiation-free leg is at **MID-ℓ (ℓ~700, 32% pre-seam)**,
  not high-ℓ. So the CR-specific reshaping lives at MID-ℓ; high-ℓ matches-then-damps. The corpus's
  low-ℓ-matches / high-ℓ-open split is **inverted**. This also questions the low-ℓ "match through 3rd peak":
  if ℓ~700's driving is mostly post-seam on L1, that match needed the L1 driving COMPUTED, not assumed from
  the equality.
**VERDICT (well-supported, no longer merely do-not-assert on the sign):** CR likely has a REAL high-ℓ deficit;
the corpus's "neither tension nor wash" is undermined by its own driving-equality mechanism; and the ℓ-location
of the CR-specific physics is inverted from the corpus account. **Residual caveats (magnitude, not sign):** the
leading-order |Ψ̃(ω=1)| characterization and the exact-time-reverse premise are the corpus's own; the deficit
MAGNITUDE and the mid-ℓ reshaping want the full seam→recomb transfer. **Flag for the open-problems map
(family 5) + P15 §coherence/§scope: the high-ℓ is a likely tension, the ℓ-split is inverted, the low-ℓ match
wants re-examination.**

## ▶ CORRECTION (r1422) — the high-ℓ "tension" was MY artifact; the 8% scale-signature is REAL and confirmed
Recomputed θ_D/θ_* consistently on one geometry with CAMB's REAL recombination opacity (`transfer_build_work/`).
Result: **r_D CR/ΛCDM = +8.9%, θ_D/θ_* = +7.0%** — matches the corpus's ~8–9% independently (my absolute
θ_D/θ_* sits a factor π below CAMB's thetad/thetastar — a definitional convention that CANCELS in the ratio,
so +8.9% is solid; an earlier x_e=1 kernel gave a spurious +70% by being high-z-dominated instead of
recombination-dominated — fixed with CAMB's ionization history).
- **The 8% diffusion-SCALE signature is REAL** — a forced consequence of the geometric rate, NOT manufactured.
- **The 40–70% high-ℓ POWER deficit / "tension" (r1420/r1421) was MY ARTIFACT** — the bare-envelope shortcut
  (ΛCDM peaks × CR damping envelope exp(−2(ℓ/ℓ_D)²)), i.e. the `full_transfer_verdict` trap I'd flagged and
  then fell into. **r1420/r1421 STRUCK. No high-ℓ tension established.**
- **The corpus's status is correct and now independently confirmed:** a real, non-reabsorbable ~8% diffusion-
  SCALE signature whose observable POWER consequence needs the full transfer — **"neither tension nor wash."**
- **The one genuinely open piece** (narrow, honest, not urgent): does the real 8% scale feature, through the
  full spectrum with CR's parameter freedom, register in the data or wash into n_s/ω_b — a likelihood question.
**Net of the whole detour:** one caution re-learned (never multiply one cosmology's peaks by another's damping),
Step-1 background object + validated CAMB toolchain banked, and the corpus's high-ℓ status confirmed not overturned.

## ▶ THE OPEN PIECE, ANSWERED (r1423) — the 8% is ~90% degenerate with n_s; mostly a wash, small residual
`transfer_build_work/degeneracy_test.py`, CAMB Fisher (CV-limited, ℓ=30–3000). The one genuinely open
question — does the real +8.9% r_D / +7% θ_D/θ_* register in the data or wash into n_s/ω_b — now has a
quantitative answer:
- **~90% degenerate with n_s + ω_b + ω_c at fixed θ_*** (the corpus's named test); n_s alone absorbs 82%.
  This is the ROBUST number (a shape property, insensitive to the construction amplitude). **Mostly a WASH.**
- **A ~10% residual survives** — the damping envelope's ℓ² curvature, a shape no tilt/peak parameter
  reproduces. That residual is the genuine CR-vs-ΛCDM distinguisher in the tail.
- **The residual's SIGNIFICANCE is NOT robust** (stated plainly): 82σ CV-limited to ℓ=3000 is the most
  optimistic data conceivable (Planck is noise-limited above ℓ~2000 → current data sees a fraction); and the
  signature was built from the bare envelope (peaks-match), so a mid-ℓ peak reshaping would move it.
**Net:** the 8% is mostly a wash into n_s, NOT a present tension — the corpus's "neither tension nor wash"
quantified: ~90% degenerate, a real but small curvature-shaped residual, future-distinguishable, not a
current conflict. **The four-way knot / family 5 is resolved in the honest direction.**
