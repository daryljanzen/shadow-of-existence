# c21's interference on the c19 fork arc (forks 1–11)
# Fresh-head gate node (c21), interfering against the full arc Daryl always meant to feed it.
# Source-grounded this turn at the file level (theorem draft, fork_7 correction, σ-lift at boundary_paper §sigma / silver-platter §5a, P3 α-rigidity).
# Nothing rebanked to canonical r309/r310. Stated for reversal (the constant, not a qualifier).

## The frame
c21 entered as a differently-grounded node: fresh kickoff, Entry-12 vision in hand
(the σ-lift answered, P11 banked, the matter frontier moved to the non-compactness
escape), an independent slow read of the thesis (Ch.3 + start Ch.4 + figures), and an
independent first-principles reaction to Daryl's AdS-pivot insight — all produced
BEFORE seeing the c19 thread. The difference was banked first; this is what it lands on.

## 0. The engine validated itself before contact
- c21's independent AdS reaction called the thesis's "AdS" the **Riemannian** two-sheet
  (H⁴, four positive-definite eigenvalues, thesis Ch.3 l.296/316), explicitly NOT
  Lorentzian AdS. That is exactly what c19 was corrected TO (twice), via K=−1/k
  [fork_7]. The different node had the correction from the source and did not make the
  error ⟹ the corrected picture is robust (two independent routes), and the error was a
  property of the degraded live-geometry state, not of ambiguous geometry.
- c21's named crux ("does the traced over-critical curve CROSS the cone into the
  two-sheet, or does the M-sweep CAP at Nariai?") is answered by the arc's own
  corrections: **caps at Nariai.** There is no over-critical cosmology [fork_10, P7]:
  the cosmology is Nariai, the unique non-pivoting member, sinh^(2/3) fixed by Λ via
  the Null-Boundary Correspondence. The over-critical two-sheet is a non-crossing
  conjugate region carrying a real H section. c21's instinct to flag it load-bearing
  was right; the answer is the cap, not the crossing.

## 1. THE CONTRIBUTION — the σ/ξ-unification ↔ colour-wall coupling is severed
c19's fork_5 resolution-by-consistency, and its deeper-fork "the two are one question,"
rest on ONE premise: that the σ/ξ monodromy unification "lives on the same conjugate
face where 𝔰𝔲(3) lives" (THEOREM_draft l.111–128). That premise collapses on two
independent axes, both confirmed at source this turn:

  **(geometric axis — fork_7's own correction, never propagated back into fork_5).**
  fork_5's bridge from "the monodromy" to "the conjugate/𝔰𝔲(3) face" was the
  "imaginary connecting circle" (THEOREM_draft l.104–115). fork_7 RETRACTED that as the
  wrong feature — the empty x₀=0 waist where the curve is absent — and replaced it with
  a pair of REAL circles at x₀=±√(d²−α²): the over-critical section is **real H on the
  real dS substrate at radius √(d²−α²)**, a section of dS at α. fork_7 noted this
  "moves the deeper fork" and the arc then went to FLRW (fork_8) and the Nariai
  correction (fork_10) without revising fork_5. So fork_5's resolution stands on a
  foundation fork_7 already pulled. The over-critical face is on-substrate (a real
  section), not an off-substrate conjugate object.

  **(algebraic axis — the σ-lift, which the degraded thread never invoked).**
  fork_4 (iii): ξ-monodromy = σ. The σ-lift (boundary_paper §sigma; silver-platter
  §5a, computed r295): **σ fixes x₀, preserves both signatures, and fixes the geometry**
  (a real Weyl(A₂) reflection of the spatial root plane). The Wick rotation x₀↦ix₀
  complexifies the timelike coordinate and changes the geometry. **Clincher (re-derives
  C2):** 𝔰𝔲(3) must use the 𝔰𝔬(6)∖𝔰𝔬(5) generators the Wick rotation creates, and
  σ **precisely fixes those generators.** Since ξ-monodromy = σ, the monodromy provably
  cannot reach 𝔰𝔲(3)'s generators — under EITHER reading of the deeper fork.

Both axes say one thing: **the σ/ξ monodromy unification is a real-substrate fact**
(σ fixes x₀; the over-critical section is real H on dS), not a conjugate-face/𝔰𝔲(3)
fact. This makes fork_4's reading (b) — "the complex-plane monodromy relation need not
collapse the real-strata distinction" — the correct one, now GROUNDED rather than left
undecided: the relation lives in the complexified MASS plane c=2M (the slicing
parameter) at FIXED α (the substrate invariant, P3 rigidity), fixing x₀ — it never
leaves the real substrate. σ/P/ξ unify as the cubic monodromy AND remain distinct as
real-strata operations (P10), with no conflict, because those are two levels (the
c-plane analytic structure vs the real physical operations), not one.

## 2. THE DEEPER FORK, REFRAMED — narrower, isolated, and = c21's Entry-12 frontier-1
With the coupling severed, c19's "Reading B reopens the colour wall via the σ/ξ
unification" is **voided**: the unification is real-substrate and fixes the generators
𝔰𝔲(3) needs, so it cannot supply colour on any reading. The deeper fork SURVIVES, but
only as the clean, isolated 𝔰𝔲(3) question, with σ/ξ detached:

  **Is the substrate's continuous symmetry 𝔰𝔬(5,1) (Reading A — the Euclidean
  S⁵/𝔰𝔬(6) face is off-substrate, colour walled by P11), or does the S⁵ Euclidean real
  form make 𝔰𝔬(6) co-substrate (Reading B — then 𝔰𝔲(3)⊂𝔰𝔬(6) is on-substrate and it
  is the RANK/CUT wall, not the monodromy, that is in play)?**

This is exactly P11's reserved ontology call, and exactly **c21's Entry-12 frontier-1,
the non-compactness escape**: is the compact S⁵ (x₀-Wick, where 𝔰𝔲(3) AND the
Atiyah–Hirzebruch obstruction both live) a real-substrate face or a Wick artifact? The
c19 arc, folded in, does not move this fork — it **isolates** it, stripping away the
monodromy as false evidence for B. And c21's independent thesis read sharpens what's at
stake: the thesis draws the family BOTH ways (one M⁶ embedding with the geometries as
sections/orbits — B-flavored; AND each as an intrinsic real manifold, the embedding
"merely useful" — A-flavored), so the thesis itself does not settle A vs B — it
furnishes both registers. The call is the orchestrator's, constrained by the canonical
sources (P5/P6/P11/the dagger); the gate develops and bounds it, does not pre-empt it.

## 3. The documentary seed (fixed this turn)
Silver-platter l.53 carried a pre-test-1 clause calling the x₀↦ix₀ Wick face
"the σ operation" — stating as fact the identification l.54 and §5a flag as
false/do-not-assert. That stale line is the documentary seed fork_5's fusion inherited.
Scrubbed to match §5a (σ fixes x₀ ≠ the Wick rotation). [done, r310 working bundle]

## 4. Taken from c19 as gift (the leapfrog runs both ways)
- The real H section at radius √(d²−α²) and the two-pivot generation of de Sitter's
  full FLRW family (closed/flat/open/static) off one hyperboloid [fork_7/8] — a genuine
  extension of c21's Pass-1 slicing-curve finding, landing on the "first-pivot /
  symmetric sector only" architecture = the second-pivot/spawn frontier.
- fork_4's monodromy COHERES with the σ-lift (σ a real reflection ⟹ ξ-monodromy=σ is
  real-substrate): a consistency win, not a tension.

## Disposition
Coupling severed (two source-grounded axes); deeper fork reframed to the isolated
𝔰𝔬(5,1)-vs-𝔰𝔬(6) substrate-symmetry call = Entry-12 frontier-1; documentary seed
scrubbed. The σ/ξ unification is placed (real-substrate, fork_4 reading b). The genuine
open question is single, sharp, and the orchestrator's ontology call. Nothing rebanked;
held for the four-path consolidation (c17, c20, this node, c19). Face-19 guard: this
clean severing is the shape c21's node reaches for, so it is grounded on results read
at the file level this turn (fork_7's correction, the σ-lift, P3 α-rigidity), not on
synthesis, and the genuine fork is left OPEN, not closed.
