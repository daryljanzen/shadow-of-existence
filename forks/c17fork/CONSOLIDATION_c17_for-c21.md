# c17 node — consolidation handoff for the four-node assimilation (for c21)
The fork NOTES carry the GRIND (fork_1-4) and receipts; this doc adds the RECEPTION step that
preceded it (it was conversational, not in the bundle) so the c17 contribution is complete for
assimilation. Register throughout: stated for reversal; do-not-assert held both ways; one node of
four. Backing detail + receipts: NOTES_c17fork.md, scripts/, CHANGELOG_c17fork.md.

## 1. Reception of the three thesis cold reads (the interference step)
CONVERGENCE (all three nodes, source-grounded): the thesis confirms the A2 "gem" is
gravitational-cosmological -- the SdS horizon triplet -- which confirms P11 FROM THE SOURCE;
colour does NOT reopen (su(3) not in so(5,1); AH stand); the r=alpha pivot/seam unifies the
Euclidean-sphere and Lorentzian-dS faces (the Wick relation realized geometrically). No swing.
GENERATIVE DIVERGENCE: c17 and c20 stayed inside the colour frame; c19 STEPPED OUT -- the gem was
never internal, it is the symmetry of the solution space. c19's frame is the correct one and it
absorbs the other two. The grind (below) is c19's reach, worked.

## 2. The pivot-vs-sigma adjudication (resolves c20's C7-vs-5a dissonance) -- grounded at source
"sigma" named TWO DISTINCT objects (thesis sec:GPSMRF line 78; P3):
 (a) root-exchange sigma = the Weyl(A2) HORIZON-PERMUTATION -- signature-PRESERVING, maps a
     geometry to an equivalent horizon-description. This is the sigma-lift's verified !=Wick object.
     It STANDS (thrice-verified; c20 right to guard it).
 (b) the PIVOT at r=alpha = the signature-CHANGING geometric hinge between the two faces (the seam).
C7 mislabeled the pivot as sigma. The sigma-lift RESULT is correct; C7's "signature-flip" should
have referred to the pivot. Two objects, one name -- that is the dissonance, now adjudicated.

## 3. c17's owned correction (face 11 / the engine)
My initial read OVERREACHED: "the sigma-lift leg is undermined / reopen around the pivot." The
pivot reaches the S^4 equator (SO(5)), NOT the SO(6) Wick face su(3) needs, so it does NOT reopen
colour or undermine P11's claim. c19's frame is right; my SURVIVING contribution (convergent with
c19): the seam unifies the faces, so the sigma-lift framing was the long way around. Banked at that
weight, not more.

## 4. The grind (fork_1-4) -- the A2/S3 as a theorem about the slicing/solution space
 - fork_1: the SdS solution space carries an EXACT A2 root-system structure: horizon triplet sums
   to zero (Cartan); fundamental ellipse = the A2 quadratic form (Vieta, exact); S3=Weyl(A2)
   permutes interchangeable horizon-descriptions of one geometry.
 - fork_2: the discriminant Delta=4-108M^2 = prod(r_i-r_j)^2 chambers the solution space:
   black-hole (Delta>0) | Nariai (Delta=0, on a Weyl wall) | cosmology (Delta<0, r=cosmic time);
   negative mass the Z2 reflection; crossing a Weyl wall = interior-becomes-cosmology.
 - fork_3 (THEOREM TARGET): S3 LIFTS to the slicing cuts. The slicing operator is a generically
   3-fold cover {cuts: sky angle w, offset r0=(2/sqrt3)sin w} -> {geometries} via the triple-angle
   2M=(2/(3sqrt3))sin 3w; S3 is its deck group, the transpositions the explicit involution
   f(r0)=1/2(-r0+sqrt(4-3r0^2)) on the cut parameter, fixed point = Nariai = branch point. So A2/S3
   is a symmetry of the cut-STRUCTURE, not the labels. CAVEAT kept intact (P3): the three cuts are
   NOT equivalent labellings (de Sitter sweeps the axis; the others pivot off-axis = the cascade),
   so S3 acts within the groupoid of observer descriptions (P4), not as a free symmetry.
 - fork_4: P10 ALREADY places this S3 as the discrete skeleton in so(5,1) (root-permutation, fixed
   pt Nariai); fork_1-4 sharpen, don't add. NEW grounded tie: P10's algebroid connection vanishes
   at exactly two symmetric strata (Type O, Nariai); the S3-cover's branch point (Nariai) is one of
   them -- the discrete (deck) and continuous (connection) faces meet at Nariai.

## 5. The genuine uncertainty (where c17 stopped)
P10 lists THREE DISTINCT discrete involutions -- root-permutation S3 (the fork's), null<->timelike
reassignment, Riemannian<->Lorentzian signature-flip -- "different involutions, not one," with the
explicit "we make no claim that these unify into a single discrete action." Whether they assemble
into one discrete structure is the natural next reach, and it is BOTH P10's do-not-assert AND the
face-19 flavor-match spot (fusing distinct involutions by shared signature-flip flavor = the C7
trap). c17, the synthesis node, did NOT force it -- it is exactly what the cold/different nodes are
required to audit. Other live reaches (none source-preferred): the P4 groupoid form; the
Petrov/isotropy stratification; the P9 dynamical reading.

## 6. Do-not-assert boundaries held (for the consolidation to preserve, both ways)
 - colour stays CLOSED: su(3) not in so(5,1); AH chirality. The pivot reaches SO(5), not SO(6).
 - over-critical complexification = the equatorial-seam continuation: GROUNDED (P3). That it = the
   Wick-to-su(3) colour bridge: DO-NOT-ASSERT (the SO(5)/SO(6) gap).
 - unification of the three discrete involutions: DO-NOT-ASSERT (P10's own).
