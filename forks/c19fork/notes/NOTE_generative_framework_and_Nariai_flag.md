# Pivot/sweep/translate as the generative groupoid + Nariai/over-critical flag  [fork_9]
# From Daryl's architectural synthesis. Affirmations grounded; one precision call made
# (stated for reversal). Architecture = orchestrator's; gate grounds the checkables.

## Affirmed (clean mapping to corpus, grounded)
Pivot/sweep/translate split = the within/between groupoid (P4), read generatively:
  - PIVOTS = between-member ops (change WHICH geometry): sigma, xi, P. First pivot =
    dS/Sch symmetry-break = xi (the seam). "Pivots break the symmetries that fix the
    geometry."
  - SWEEPS + TRANSLATIONS = within-member isometries (fill out the chosen geometry).
  - Refinement for P10-consistency: from inside one geometry a pivot looks like
    symmetry-breaking, but in the algebroid so(5,1)|x|C it is a move in the BASE (orbit
    structure), not loss of symmetry. Same picture; keeps the algebroid honest.

## Verified (computed/keystone)
flat-LCDM scale factor a ∝ sinh^{2/3}(3Ht/2): ((adot/a)^2 - H^2)*a^3 = const => matter
∝ a^-3 + Lambda/3. Standard flat LCDM. = P7 (CR_flatLCDM_v2.tex).

## PRECISION FLAG (CR-dissonance-lens; clear call, stated for reversal)
The sinh^{2/3} flat-LCDM form belongs to OVER-CRITICAL SdS (our universe, P7), NOT to
Nariai. Nariai = the CRITICAL config (dS2 x S^2, maximal-horizon boundary) = the BIRTH
locus. Thesis sec_CBHO chain: prior cluster -> collapses to critically dense (Nariai),
asymptotic to its horizon -> product is "a 3-sphere in de Sitter space ... described
globally by the OVER-critical SdS line-element." So: our universe = over-critical =
flat LCDM (sinh^{2/3}), BORN as a 3-sphere from the critical(Nariai) collapse of a prior
cluster. "3-sphere = prior collapsed object's horizon" is correct & sourced. Do NOT fuse
"Nariai" (birth/critical/dS2xS^2) with "over-critical" (evolution/sinh^{2/3}); Nariai is
not a flat-LCDM cosmology.

## Coherence win (the wall and the architecture agree)
The cosmogenesis null<->timelike reassignment is the op WALLED in the between-member
theorem (fork_4: causal-character move, not a root-monodromy object, does not unify with
sigma/xi/P). The architecture explains WHY: it is not a first-pivot (symmetric-sector) op
at all -- it lives in the second-pivot-and-beyond sector. Symmetric sector closes under
sigma/xi/P; cosmogenesis is a different kind of move, further out. Wall + frame consistent.

## Scope (hold as DIRECTION, not result)
"Only developed the symmetric sector because only the first pivot." Honest scope: P1-P11
IS the first-pivot (spherically-symmetric stationary) sector; slicing operator (P5/P6) is
the first-pivot machinery. Second-pivot+ (full cosmological dynamics, less-symmetric
geometries) is a real but UNDEVELOPED frontier -- the map of where to go, not banked
ground. Flag so the cold read does not mistake frontier for territory.

## Geometry (engaged, not over-committed)
2nd-pivot axis in the equatorial plane (orthogonal to 1st, glued to hole's axis) = the
right KIND of 2nd orientation DOF. Two recipes read right vs figures: LR = pivot1=0
(dS, M=0), fix pivot2 at null angle, translate up (flat comoving stack); STATIC = sweep
pivot2 through full range null-pair to null-pair (static line element). Exact axis
placement not set up here; the fix-translate vs sweep-range distinction is sound.

## Fork: untouched. All real slicing/sweep/translate on the ONE dS substrate
(Reading-A-consistent); does not touch the conjugate-face ontology. NOT re-resolved.
