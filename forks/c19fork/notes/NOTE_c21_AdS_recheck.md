# RECHECK of c21's AdS realization (surfaced to c19)   [r309_c19fork_6]
# Stated for reversal; held for the four-path cold read. Suspect-the-model:
# the excitement ("M.F.") is exactly the trigger to go to source + compute.

## c21's claim
The Observer-2 pivot sweeps the slicing curve's full range: angle 0 = dS/Schwarzschild
pair; under-critical -> Nariai = the null X (rulings/null duals); carry the pivot
through over-critical SdS and "you hit AdS" at 90 deg; then by symmetry it returns to 0.
"The over-critical SdS slicing curve is the two-sheeted hyperbola."

## What is RIGHT (credit c21 — it is in the Lorentz_sphere figure)
The slicing-curve SECTION sweeps the Lorentz-sphere alpha^2-family SHAPES:
  - under-critical (offset d<alpha):  x1^2 - x0^2 = alpha^2 - d^2 > 0  -> x1-opening
    one-sheeted curve = the DASHED (0<alpha^2, dS) curve of Lorentz_sphere.png;
  - Nariai (d=alpha):                 x1^2 - x0^2 = 0 -> the SOLID null X (the rulings,
    the null duals);
  - over-critical (d>alpha):          x0^2 - x1^2 = d^2 - alpha^2 > 0 -> x0-opening
    two-sheeted curve = the DOTTED (alpha^2<0, AdS) curve of Lorentz_sphere.png.
So the section visits all three signature-SHAPES, Nariai the null cone between dS-shape
and AdS-shape. This is a genuine, complete reading of the curve's range; c21 saw a real
pattern that none of c17/c19/c20 surfaced.

## What is an OVER-REACH (the satisfying-shape trap; the recheck the excitement demanded)
"You hit AdS" is not literal. Decisive computation (between/this fork, sympy):
  **SdS solves R_mu nu = (+3/alpha^2) g_mu nu for ALL M  =>  Lambda = 3/alpha^2 > 0.**
Over-critical (large M) SdS is a de-Sitter-type (Lambda>0) vacuum -- the cosmology --
NOT AdS (which needs Lambda<0). The over-critical SECTION has the AdS SHAPE (two-sheeted),
but it is a section of the Lambda>0 dS hyperboloid; swept, it is dS-cosmology. The thesis
agrees (Ch4 l.72: over-critical singularity "is of the de Sitter-type"; l.514: over-
critical = the dS slicing continued past the Nariai crest -- not a crossing into AdS).

Why the shape is not the geometry: a single quadric has planar sections of ALL conic
types (ellipse/parabola/one- & two-sheeted hyperbola) -- elementary. So the dS
hyperboloid having two-sheeted (AdS-shaped) sections does NOT mean dS "contains" AdS.

The two Wicks (the precise point): actual AdS is alpha^2<0, reached from dS by the
**alpha-Wick** alpha -> i*alpha (equivalently Lambda -> -Lambda). That is a DIFFERENT
continuation from (a) the SEAM x0-Wick x0 -> i*x0 (dS -> Euclidean sphere S^5, where
su(3) lives) and (b) the over-critical ANGLE continuation theta -> pi/2 + i*psi at
FIXED real alpha (dS BH -> dS cosmology). The slicing/seam path is alpha-preserving, so
it reaches the Euclidean-sphere face (seam) and the over-critical dS cosmology, but
NOT AdS. AdS is one more, different Wick away.

Parametrization nit: the real sky-angle w runs dS(w=0) -> Nariai(w=pi/6) -> through
under-critical -> the OTHER Nariai(w=pi/2). Over-critical is NOT in the real-w range
(it is the continuation past Nariai). So "AdS at 90 deg" does not hold even as a real
sky-angle; the P-reflection/periodicity c21 senses is real, but its endpoints are the
two Nariai points, not AdS.

## Bearing on the DEEPER FORK (sharpens, does NOT tip)
c21 adds a third face to the family picture: dS (real Lorentzian substrate), the
Euclidean sphere (x0-Wick, su(3)'s home), and AdS (alpha-Wick). The slicing curve's
range covers the dS->null->AdS SHAPES (elementary conic fact), but structurally
CONTINUES only to the Euclidean sphere (the seam); AdS needs the separate alpha-Wick,
off the slicing path. So this is NOT new evidence for Reading B (one substrate = whole
family); the AdS-shape of an over-critical section is the conic-section fact, not a
structural continuation into alpha^2<0. It clarifies the fork's anatomy: three faces,
three different continuations (x0-Wick, angle-continuation, alpha-Wick), only one of
which (the seam x0-Wick) is on the slicing curve. The deeper fork (is the conjugate
family one substrate or faces off it?) stays exactly where it was -- now with AdS
named as a third face reached by its own Wick. Symmetric bar: real insight credited,
over-reach flagged by computation, fork neither manufactured-open nor -closed.
