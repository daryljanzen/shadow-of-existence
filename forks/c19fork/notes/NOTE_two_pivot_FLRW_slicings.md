# Two-pivot slicing family -> dS's three FLRW slicings + static  [r309_c19fork_8]
# From Daryl/c21 live geometric reasoning + thesis figures (read this fork).
# Stated for reversal; held for the four-path cold read. Source-grounded, not asserted.

## Figures read (thesis/resources/PhD_thesis)
- dS_LR1/LR2 (Lemaitre-Robertson): doubly-ruled hyperboloid, two families of flat
  null-lines (rulings) at 45deg; the flat "simultaneous-space" slices stack up the horn
  (dS_LR2 dashed horizontals = the "stack"). = the FLAT (k=0) comoving cosmology.
- dS_static1/2: near-vertical static worldlines (= boost orbits = constant-accel
  worldlines) + static spatial sections. = the STATIC patch slicing.
- Thesis text backs it: ch4 l.322 cosmic time tau-bar=tau+chi "comoving at 45deg",
  coords = "collections of oppositely directed flat null-lines"; l.287 LR comoving
  spatial slices proven "Euclidean" (flat); l.133 Eddington statical r=alpha coord
  singularity "formally different from" the LR cosmological horizon (static != LR, genuinely
  two slicings, not a coord change).

## The solid structure (Daryl's two pivots = the two plane-orientation DOF)
The ONE dS hyperboloid, cut by a 2-parameter family of plane orientations, yields all
of dS's FLRW slicings:
  - FIRST pivot (about axis glued to the hole): regime/curvature sweep --
      spacelike cut near throat  -> closed S^3 (k=+1, under-critical);
      tangent                     -> null X (Nariai);
      past it (offset d>alpha)    -> two-sheeted OPEN/hyperbolic H (k=-1, over-critical;
                                     real, radius sqrt(d^2-alpha^2); confirmed fork_7).
  - SECOND pivot (about the fixed seam set by the first): tilt toward null --
      at the NULL orientation ("45deg" in the Minkowski-embedding picture, light at 45)
                                  -> FLAT k=0 = Lemaitre-Robertson comoving cosmology
                                     (the dS_LR stack, horosphere/flat sections);
      tilt the other way          -> the STATIC slicing (dS_static, boost-orbit worldlines).
So closed/flat/open + static all fall out of where the cutting plane sits; the two thesis
figures (LR flat, static) are two members. "45deg" = the null plane orientation, not a
protractor angle in an arbitrary frame.

## Affirmed sub-points (Daryl)
- Self-correction right: H is homogeneous/isotropic; the over-critical arc's apparent
  lopsidedness is embedding vantage, not intrinsic. The asymmetric horosphere stack in
  dS_LR2 is the flat slicing's embedding, not an asymmetry of the space.
- "Two observers, two slices for the full analytic extension": the two-sheeted hyperbola's
  far-side pair (x2=-d plane, antipodal horn) -- O2/O2' antipodal pair, where the dS/Sch
  dual lives. One observer's slice is half the analytic story.

## Bearing on the DEEPER FORK (enriches; NOT re-resolved -- c19 erred twice this run)
Everything here lives on the ONE real Lorentzian dS hyperboloid: closed/flat/open/static
are all real slicings of it; the H sections are real slices at radius sqrt(d^2-alpha^2).
Strong "one substrate, sliced every way" weight -> reads as Reading A (dS the substrate;
slicings realize the FLRW family). Reading-B pull only that one real slice IS the
"AdS"(=H) face -- but as a slice of dS at a different radius, not a separate space via a
Wick. Sharpens the same fork, settles nothing; still touches the colour wall via the
identical on/off-substrate question. Banked, not closed.

## Open next (a check, not an assertion)
The second pivot's SEAM (the fixed line the first pivot leaves) is where static and LR
branch -- and that seam is the horizon locus. IF the second-pivot branch point is
literally the horizon, then static-vs-cosmological = under-vs-over-critical seen through
the 2nd pivot, and the two pivots are NOT independent (they share the seam). Check whether
the two pivots commute or the seam couples them. (Do not assert; compute/source.)
