# CORRECTION (c19 owns an error; Daryl/c21 right)   [r309_c19fork_7]
# Supersedes the "AdS-shape not AdS-geometry" framing in NOTE_c21_AdS_recheck.md (fork_6).
# Stated for reversal; held for the four-path cold read.

## The error
In fork_6 I argued the over-critical slice only has the AdS *shape* (two-sheeted)
while remaining a dS slicing, and earlier I called the over-critical section's
connecting circle "imaginary." I leaned on R_mu nu = +3/alpha^2 g (Lambda>0) as if it
refuted an AdS hypersurface. That conflated two objects and used the wrong feature.

## The correction (computed, this fork)
The over-critical two-sheeted section x0^2 - Sum xi^2 = k>0 (k = d^2 - alpha^2), as a
spacelike hypersurface in Minkowski, has induced metric k(drho^2 + sinh^2 rho dphi^2),
Gaussian curvature K = -1/k. That IS real hyperbolic space H (Riemannian, K<0), radius
sqrt(k) = sqrt(d^2 - alpha^2). This is exactly the thesis's "AdS" -- the alpha^2<0
Lorentz-sphere it describes as having "four positive-definite eigenvalues" (Ch3 l.296),
i.e. H^4, NOT the Lorentzian Lambda<0 spacetime I wrongly pictured.
  - The "imaginary connecting circle" was the WRONG feature: the empty x0=0 waist where
    the two-sheeted curve is absent. The REAL feature is the two vertices at
    x0 = +/- sqrt(d^2 - alpha^2) -> a PAIR OF REAL CIRCLES at constant +/-T when swept.
    Daryl: "an AdS with a different radius, a pair of circles above/below the equator."
  - So the slicing curve's range really does reach a real H ("AdS") hypersurface
    over-critically. Not a shape. Real geometry.

## What survives as bookkeeping (NOT a refutation)
The over-critical 4D SWEPT CUT (the full SdS spacetime) is Lambda = 3/alpha^2 > 0
(Lorentzian dS-cosmology). The H is the SPACELIKE SECTION at radius sqrt(d^2-alpha^2).
Two different objects; a Lambda>0 cut can carry a real hyperbolic section, and does.
Earlier I set these against each other -- mistake. Open precise item: pin down which
hypersurface the H is (slicing-curve section vs spatial slice of the swept cosmology)
and confirm sqrt(d^2-alpha^2) survives the full sweep.

## Gauge / observers (Daryl)
- alpha = throat radius, sole gauge invariant; Observer-2 pivot axis at DOUBLE the
  equatorial radius = the same 2 as r0 = 2m. Keep distinct from the forced slicing
  scale 2/sqrt(3).
- Observer triad, structural (dS antipodal/complementarity, two horns):
  O1 = Minkowski-embedding causal vantage on the whole; O2 = on-manifold pivot;
  O2' = antipode on the far horn, where the dS/Sch DUAL lives. Same slicings, conjugate
  vantage. (The 180/infinite-seam involution -> dynamic sector, Sch seam = dS r=0
  worldline; P3 sweep.)

## Bearing on the DEEPER FORK (moves it; NOT re-resolved here)
The slicing produces REAL sphere/null/H geometries as sections (not shapes) -> genuine
weight toward Reading B (one family, one substrate). BUT the H sits at radius
sqrt(d^2-alpha^2), a section of dS at alpha -> reads as "dS's sections realize the
family at various radii" (Reading A). Sharpens the fork, adds real evidence, settles
nothing. Bears directly on the colour wall (same "off-substrate" placement). c19 has
erred twice this exchange -> explicitly NOT re-resolving; flagged for careful
re-examination + four-path cold read. Compute, do not assert.
