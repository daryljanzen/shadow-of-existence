---
name: complex-analysis-ledger
description: R-M's third field bake — complex analysis and monodromy thrown at CR. Opened r1867 (Phase 8), handed to by the quadric bake's Q4 (equianharmonic ⟺ j=0 ⟺ CM by ω). Discharges R-M arc station Ⓒ, marked NO ENTRY since r1805. Baseline, probes, four-part close.
current: r2491+c54.194
---

## ⛭⛭ STATION Ⓒ WALKED r2491 — AND THIS LEDGER'S OWN FIELD SUPPLIES THE DISTINCTION

*`Q4`'s handoff: the vantage triple's cross-ratio is the equianharmonic $e^{i\pi/3}$.* ✔ **Checked: the
$j$-invariant is exactly 0**, so the attached curve is $y^2=x^3+1$ with complex multiplication by $\omega$.

**⇒ AND HERE IS WHAT COMPLEX ANALYSIS ADDS, which is a distinction and not a result:**

| object | group | order | |
|---|---|---|---|
| the equianharmonic curve's automorphisms | **$\mathbb Z_6$** | 6 | ***cyclic*** |
| the horizon cubic's deck/monodromy | **$S_3$** | 6 | ***non-abelian*** |

⇒ ***SAME ORDER. NOT ISOMORPHIC.*** *And P5 already establishes that one group is **"worn three ways"** —
deck/monodromy, the Weyl group of $A_2$, the Galois group.* ⇒ ***$\mathbb Z_6$ is a fourth object and is not among
them.***

**⌗ AND THIS LEDGER IS WHERE THAT MATTERS, because the same field already supplied the deck-versus-monodromy
correction** *(routed item 23: **"deck group $S_3$" on a three-sheeted cover is impossible, $6\nmid3$**, and
`rem:equianharmonic` gives a third answer, $\mathbb Z_3$).*
⇒ ***Three order-six-adjacent objects — $S_3$, $\mathbb Z_6$, $\mathbb Z_3$ — and the corpus's texts have at points
called the same thing all three. "Degree six" is not one fact it can carry loosely.***

⚠ **Not a defect report.** *`Q4` and `rem:galois` are **both correct and about different objects**, and nothing
exhibits a $\mathbb Z_6$ action on anything in the construction. ***The finding is the adjacency under one word.***


## ⛭⛭⛭ BROUGHT CURRENT r2449, TO c54.185 — **AND THE FIELD'S OWN VOCABULARY IS MISUSED IN TWO PAPERS**

> *This ledger is the corpus's home for covers, branch points, monodromy and deck actions — **31 uses of "deck",
> 14 of "monodromy", 11 of "Galois"**. Two findings since c54.163 are squarely its subject, and one of them is a
> **misuse of the distinction this field exists to keep**.*

**⛔ ① "DECK GROUP $S_3$" ON A THREE-SHEETED COVER IS IMPOSSIBLE — routed as `FOR_54` 23.**
*P5's `prop:deck` reads **"The deck group of the three-sheeted cover is $S_3$"** and closes, correctly,* ***"$S_3$
is therefore the MONODROMY group of the branched cover, not a free relabelling of the members."***
⇒ **A deck (covering-automorphism) group acts FREELY on a fibre, so its order divides the degree: $6\nmid3$.**
*The proposition corrects its own heading four sentences later, and `rem:equianharmonic` inherits the wrong word
and then gives a **third** answer — "the deck action is $\mathbb{Z}_3$".* ⌗ ***Three statements, one object.***

**⛭ AND EVERYTHING FITS ONCE THE WORD IS "MONODROMY", which is this ledger's own material:**
*$\operatorname{disc}(r^3-r+2M)=-4(27M^2-1)$ is **not a square in $\mathbb Q(M)$**, so the Galois group of
$r^3-r+2M$ is $S_3$;* ⇒ ***the root cover is NON-REGULAR with TRIVIAL deck group, and its Galois closure has degree
six with deck $S_3$ — which is exactly what `C3` established from the dial.***
⌗ **So the corpus holds three objects where its prose holds one name:** *the **3-sheeted root cover** (monodromy
$S_3$, deck trivial); its **6-sheeted Galois closure**, the dial (deck $S_3$, regular); and the **$\mathbb{Z}_3$
turnaround deck** at $r=0$, which `C2` proved does **not** act on the root cover's fibre.*

**⌗ ② AND THE SKY COVER IS THE ROOT COVER, verified r2447** *(node 17's `D02`, whose proposed edit was declined but
whose mathematics is exact).* *With $r=\tfrac{2}{\sqrt3}\sin w$ the triple-angle identity gives
$\tfrac{2}{3\sqrt3}\sin 3w = r-r^3$ **exactly** — so the sky-angle parametrisation **is** $2M(r)$ in trigonometric
coordinates, and the Nariai branch $2M=\pm\tfrac{2}{3\sqrt3}$ maps precisely onto $\sin 3w=\pm1$.*
⚠ ***But "same cover" needs care, and this is the ledger's to state: the $120°$ shift $w\mapsto w+120°$ is a
symmetry of the REAL undercritical trivialisation, not a global automorphism over the complex $2M$-line*** *— which
is why the monodromy is $S_3$ and not $\mathbb{Z}_3$, and why `prop:deck`'s own closing sentence about the
over-critical regime realising **only the order-two subgroup** is the correct and careful statement.*

⇒ ⛭⛭ **THE LEDGER'S LESSON FROM THIS ROUND:** ***a field whose whole content is a distinction will have that
distinction collapse in the prose of papers that use it correctly in the mathematics.*** *P5 computes the monodromy
right and calls it the deck; the correction is a word, and the word is this field's.*


# THE COMPLEX ANALYSIS AND MONODROMY LEDGER — R-M's third bake

> **⌗ RE-READ AGAINST c54.163 AND DECLARED CURRENT r2395.** *The fork changed **none of the four field-bake
> archives** in the c54.153→c54.163 span, so the question was again* ***"did anything the span produced
> bear on what this bake concluded"*** *rather than "what moved in the file". **The span produced two
> substantive results and one instrument class**, and each was tested against this ledger:*
> *· the **species-selection withdrawal** (c54.162) — the crossing is **lossless for every species**;*
> *· the **recursion unfolded** (c54.163) —* ***"the first clause of that question has no object, and once
> that is seen the other two answer themselves"***: *there is no map between the patch's closed-$S^3$
> harmonics and the ambient universe's, because the correspondence is **null boundary to null boundary,
> "with no spacelike slice entering the map"**, and the collapse interior is Kantowski–Sachs on
> $\mathbb{R}\times S^2$, **carrying no closed-$S^3$ basis at all**;*
> *· the **receipt sweep** — 186 receipts gained assertions and the hollow-assertion lint was extended twice.*
> ⇒ **None bears on this bake's verdicts**, *which is what `CATEGORY`'s own rule predicts:* ***a bake's
> close is about its FIELD, and a row's content may still grow.*** **Reopen it when the field has more to
> give, not when its subject moves.**
> ⌗⌗ **AND THIS ONE UNBLOCKS THE QUEUED EDGE.** *R-M's front is **Ⓒ complex analysis/monodromy** — handed to
> by `Q4`'s equianharmonic result, and **R-M's own rule is that the handoff outranks the list order**. Ⓒ is a
> **re-entry, not a first bake**, so* ***the bake form's §0 BASELINE is this file*** *— and a baseline 150
> revisions behind would have been the edge's first defect rather than its first move.* **It is current now,
> so `L-203` starts from a current baseline whenever it is opened.**


> **⌗⌗ READ AND DATED r2385 (observer line) — CLOSED EXCEPT ONE ENTRY, WHICH IS LIVE AND UNASKED (`L-209`).**
> *This is the ledger of the field R-M's station **Ⓒ** names, so reading it sharpens the queued edge as well as
> dating the file. Five probes, all run r1867–r1906. Its yield has a shape the other bakes do not:*
> ***"the quadric field EXTENDED laws · the conformal field REFUSED · the optics field CONFIRMED-AND-LOCATED ·
> this field TURNS PRACTICES INTO THEOREMS"*** *— in every probe the corpus was already doing the right thing
> and the field said **why** it works, which twice converted a checked fact into a guaranteed one.*
>
> **⌗ ITS 4d QUEUE, read at source: two entries closed, ONE STILL OPEN.**
> *· the $\mathrm{PSL}(2,\mathbb C)$ action on the thrice-punctured sphere — **struck r1906: there is no third
> puncture**, it was `X1`'s compactification artefact, withdrawn r1874, and* ***the queue entry outlived the
> thing it was about by 32 revisions*** *· the second reality axis — **closed r1888**: $\sin$'s reality set is
> the real axis plus every line $\mathrm{Re}\,z=\pi/2+k\pi$, and the crossings sit at $r=\pm\alpha$, **exactly
> the de Sitter member's two nonzero horizons**, with $r=0$ on the real axis away from any crossing. Landed in
> P3.*
> ⛔ *· **GROWTH AND ORDER AT INFINITY** — *"$\sinh$'s essential singularity at infinity is outside the finite
> lap —* ***noted, not used*** *— and whether the construction's boundedness has an analytic statement is
> **unasked**."* ***That is a live item in a document nothing was watching, and it was not in the register.
> Registered `L-209`.***
>
> ⌗ **AND IT SHARPENS `L-203`:** *R-M's Ⓒ is a **re-entry on a handoff, not a first bake** — this field has been
> baked once and closed. Ⓒ is `Q4`'s equianharmonic result arriving at a field whose ledger already exists, so
> the bake form's §0 BASELINE is **this file**, and the opening question is `Q4`'s, not a fresh survey's.*


*Opened r1867. **Phase 8**, fourth field. **Handed to by `Q4`**: the vantage triple's cross-ratio is the
equianharmonic $e^{i\pi/3}$, and **equianharmonic $\iff j=0 \iff$ complex multiplication by
$\omega=e^{2\pi i/3}$** — which is the corpus's own $\mathbb{Z}_3$ deck action, its three-sheeted cube-root
cover, and its $2\pi i\alpha/3$ period. ***This discharges R-M's arc station **Ⓒ**, marked "NO ENTRY" since
r1805 — the arc named a field the list did not carry, and a handoff supplied it.***

**⌗ THE CLOSE HAS FOUR PARTS** *(r1862–r1866)*: **① `scripts/check_bibliography.sh` · ② the theorems ledger in
its four parts · ③ the CENSUS PASS — which of `THE_DISSOLUTION_CENSUS`'s six clusters each result enriches, and
the landing in that cluster's home · ④ the P6 PASS — does any result move a claim from PERMIT toward REQUIRE
under `R2`, and if none does, SAY SO.**

---

## ⛭ 0 · BASELINE — surveyed r1867, citation keys stripped and every count word-bounded

***The corpus does complex analysis constantly and owns none of the field's machinery.***

**⌗ WHAT IT HAS, heavily:** *`analytic continuation` **×43** · `branch point` **×62** · `deck` **×54** ·
`monodromy` **×33** · `sheet`/`sheets` **×14** · `covering` **×8** · `contour` **×13**.*

**⌗ WHAT IT DOES NOT HAVE — checked, all zero:** *`Riemann surface` · `uniformisation` · `elliptic function` ·
`modular` · `theta function` · `Weierstrass` · `j-invariant` · `period lattice` · `Picard` · `essential
singularity` · `residue theorem`.*

***So the same shape as the optics bake: the corpus performs the operations — continuing across a seam,
tracking monodromy around a branch point, working a three-sheeted cover with a $\mathbb{Z}_3$ deck action —
without ever naming the objects the field built to hold them. There is no Riemann surface in the corpus, and
the corpus has been working on one.***

**⛭ AND THREE WORD COLLISIONS, entered in §0 BEFORE the bake** *(the rule `O1`'s baseline earned)*:
*• **`residue`** — the corpus's **discrete residue** $\mathrm{Aut}(A_2)$, ~46 word-bounded uses, against the
field's **residue at a pole**. Any analytic use says *"the residue AT the pole"*.
• **`pole`** — three senses already live: **pole–polar** (projective, baked in at `Q1`), **a rotation pole**
(the North-pole projection), and the **analytic pole**. ~53 word-bounded uses split across them.
• **`Schwarz`** — ×386, and **every one is Schwarzschild**. The field's Schwarz reflection principle and
Schwarzian derivative would collide head-on; **neither may be used unqualified.***

---

## ⛭ 1 · THE OPENING QUESTION — from the handoff

**`Q4` established that the vantage triple with the centre is equianharmonic, hence $j=0$, hence complex
multiplication by $\omega$. And the corpus separately carries a three-sheeted cover of the horizon cubic,
branched at Nariai, with monodromy the root-exchange $\sigma$ and a $\mathbb{Z}_3$ deck action.**

> ***THE QUESTION: are these the same object? Is the corpus's branched cover an elliptic curve with $j=0$ —
> and if so, does the field's theory of such curves say anything the corpus does not already have?***

*The real negative is available and sharp: **the horizon cubic $r^3-r+2M=0$ defines a curve, but a curve in
what?** If the cover is a genus-zero object — a thrice-punctured sphere rather than a torus — **then $j$ is not
defined on it, the CM reading is a coincidence of the cross-ratio alone, and `Q4`'s $j=0$ remark is decoration
rather than structure.** ***That is a real bounce and it must be checked first.***

---

## ⛭ 2 · THE PROBES

| # | probe | why it is worth throwing | verdict |
|---|---|---|---|
| **★★★ X1 → X1r ✔ CORRECTED r1874 — the computation was on the wrong base, and redoing it on the corpus's own dial returned a better result.** | **What is the genus of the corpus's cover?** ***⛔ THE r1867 RUN FAILED CONFIRMATION ④.*** *P5 says **"a cover of the 2M-PLANE"**; Riemann–Hurwitz needs a **compact** base, so the run compactified to the sphere **silently** — which **manufactured** the branch point at infinity it then reported as the corpus's own omission. **And $|2M|\le2/3\sqrt3$ on the real dial: P3 — "the strict overcritical regime is reached by no real swing." The point at infinity is not somewhere the construction goes.*** ***★ REDONE ON THE DIAL:*** *$w\mapsto2M=\tfrac{2}{3\sqrt3}\sin3w$ is **six-to-one** over a generic value, deck **$D_6$** (rotation $w{+}60^\circ$ and reflection $w\mapsto-w$, both sending $2M\mapsto-2M$ — verified), branched at the **six Nariai crests** where $\cos3w=0$. **All of it already in P3 §sec:tour, as the dial's hexagonal symmetry.*** ***AND THE JOINT: since $r_0(w)$ is a root at $2M(w)$, the dial maps INTO the root cover, two-to-one, with covering involution $w\mapsto w+\pi$ — which sends $2M\mapsto-2M$ AND $r_0\mapsto-r_0$: it is $R$.*** **So $\mathrm{Aut}(A_2)=S_3\times\mathbb{Z}_2$ is a two-storey tower of covers, each factor with its own branch set — the two Nariai VALUES below, the six Nariai CRESTS above.** *Landed as P5 `rem:cover-tower`, replacing `rem:genus`.* `storyboard_receipts/X1r_cover_tower.py` |
| **★★★ X2 → X2r ✔ CORRECTED r1875 — the closed-forms result stands, and GENUS WAS NEVER THE MECHANISM.** | **Is the $2\pi i\alpha/3$ a lattice period or a single one?** ***Single, verified: $\sinh^2$ has the one period $i\pi$, no real period, and none with both parts nonzero.*** **But the r1868 statement reached for *genus zero* and leaned on `X1`'s bad cover; both are dropped.** ***THE MECHANISM IS SINGLE vs DOUBLE PERIODICITY:*** *a singly periodic meromorphic function is a **rational function of an exponential** — elementary; a doubly periodic one is **elliptic**, and no elementary closed form exists.* **⛭ AND CONFIRMATION ③ FOUND THE TANGLE: the $\tilde\tau$-period belongs to the TURNAROUND cubic $T$; the dial belongs to the HORIZON cubic $H$. P3 keeps these apart — `lem:twoturnings`, and $\operatorname{disc}T=-108M^2\alpha^4$ never vanishes while $\operatorname{disc}H$ does, at Nariai.** *Checked both: $\sin3w$ carries the single real period $2\pi/3$ and **no imaginary one**.* ***★ THE JOINT: each three-ness is ONE SINGLY-PERIODIC ELEMENTARY FUNCTION COMPOSED WITH A THREE-FOLD*** — **$\sinh^2$ with the cube root, $\sin$ with the triple angle** — *and that is why $\sinh^{2/3}$, $\sin3w$ and $\sigma$ all close in elementary terms.* **And the two periods differ in kind as the two three-nesses do: $2\pi i\alpha/3$ purely imaginary along the clock, $2\pi/3$ purely real along the dial — the analytic face of P3's own "deeper than their affine inequivalence."** `storyboard_receipts/X2r_single_periodicity.py` |
| **★★★ X3 ✔ RUN r1869 — BITES. The seam continuation IS a Schwarz reflection, and the principle supplies four things the corpus states none of.** | **The seam continuation.** *P3 continues $\theta\mapsto\pi/2+i\psi$, carrying the round spherical line element to the de Sitter one with an **automatic** signature flip.* ***THE KEY OBSERVATION: $r=\sin z$ is real not only on the real axis but on the vertical line $\mathrm{Re}\,z=\pi/2$, where $\sin(\pi/2+i\psi)=\cosh\psi$ — so THE SEAM IS A REALITY AXIS OF $\sin$, AND IT IS NOT THE REAL AXIS.*** *Reflection in it is $z\mapsto\pi-\bar z$, and **$\sin(\pi-\bar z)=\overline{\sin z}$ exactly — verified.*** **⛭ AND THE PRINCIPLE SUPPLIES FOUR THINGS:** ***(a) UNIQUENESS** — the continuation is not one admissible extension among several but **the only holomorphic one** · ***(b) REALITY — real data on the axis reflects to real data, BY THE THEOREM***, which is **the mechanism, at this seam, of "reached through the imaginary, real by construction"** · **(c) HOLOMORPHY AT the seam**, so the seam bounds no part of the analytic object · **(d) the SIGNATURE FLIP is the reflection's ANTI-holomorphy read on a degree-two form** — the increment is $i\,d\psi$ and a metric is quadratic, so $d\theta^2=-d\psi^2$ where a scalar would carry across unchanged.* **Landed as P3 `rem:schwarz`, Ahlfors cited.** `storyboard_receipts/X3_seam_schwarz_reflection.py` |
| **★★ X4 ✔ RUN r1870 — the two things the corpus says about $r=0$ are BOTH true, and the field says why they are compatible.** | **Where are the singularities, and of what type?** *The corpus asserts, of the same locus, **"a branch point, not a barrier"** and **"a genuine curvature singularity — real, not a coordinate artefact"** — and never says they are consistent.* ***COMPUTED, and they are statements about different functions:*** *near the crossing the bead's law gives $r\sim\tau^{2/3}$ — **an algebraic branch point of order three**, the turnaround cubic's own three-sheetedness — while the Kretschmann invariant, going as $r^{-6}$, has **a pole of order four in $\tau$** (verified: $K\tau^4\to2.37037$ constant as $\tau\to0$).* ***AND THE FIELD'S TRICHOTOMY SUPPLIES THE LICENCE:*** **a finite-order branch point admits continuation onto a COVER; a pole admits continuation as a map to the SPHERE; neither obstructs the continuation the closure performs. An ESSENTIAL singularity would obstruct both — and by Picard nothing could be said of the values beyond it.** *Checked: $\sinh$ is entire, so $r(\tau)$'s only finite-plane singularities are those algebraic branch points at $\tau=ik\pi\cdot2\alpha/3$; **no poles of $r$, no essential points.*** **So "branch point, not a barrier" is licensed, and the licence is finite order in both senses.** *Landed in P7 at the closure passage, Picard cited.* `storyboard_receipts/X4_singularity_types.py` |
| **★★ X5 ✔ RUN r1871 — the generation is VERIFIED, and the base has a third puncture.**  **⌗ r2376+c54.13 — NARROWED, and the branch locus IS the degenerate member.** *At general $D$ the cover is $r^{D-1}-r^{D-3}+2M=0$ over the $2M$-line, degree $D-1$, and **the discriminant vanishes exactly where $f=f'=0$** — verified to exact radicals at $D=4\ldots8$. ⇒ **P5's $S_3$ is the SYMMETRIC GROUP OF THE FOLD.** ⚠ *But this row's own check does not transfer as stated: "two transpositions generate the group only if they differ" is a fact about a **degree-3** cover — $S_3$ needs two, $S_{D-1}$ needs $D-2$ — so the analogous verification at higher $D$ is a **larger** check, not the same one. **Verified at $D=4$; the general claim is a different claim.*** `P03_step3_sweep` | **Monodromy as a group, not a map.** *P5 says the deck group is $S_3$ **"generated by the monodromies about the branch points"** — ***and two transpositions generate $S_3$ ONLY IF THEY DIFFER, which is a computation nobody had run.*** **Computed, all loops based at one point: $\tau_+=(0\,2)$, $\tau_-=(1\,2)$ — they DIFFER, and the group they generate has order six.*** *Had the same pair collided at both, the group would be $\mathbb{Z}_2$ and P5's claim **false**. **The alternative was real; the claim is now verified rather than assumed.*** ***AND THE LOOP ABOUT INFINITY IS A 3-CYCLE*** — **confirming `X1`'s total branching by an independent route, monodromy rather than asymptotics** — with the relation $\tau_-\tau_+c_\infty=\mathrm{id}$ **closing exactly** on the thrice-punctured sphere. *So the corpus names two branch points and the representation needs three; the third is what makes it consistent, and it is the same total branching that gives genus zero.* **⛔ INSTRUMENT NOTE: a first pass computed $c_\infty$ on a circle based at $m=60$ rather than at the common base point, and the relation appeared to fail. Permutations compose only when all loops share a base point and consistent paths.** `storyboard_receipts/X5_monodromy_group.py` |

**⌗ STANDING DISCIPLINE, carried forward with the additions each bake earned:** *① check the corpus first · ②
the bounce is data · ③ the discriminator adjudicates every BITES · ④ receipt, register, checker both sides · ⑤
invariance-checked before being read · ⑥ **every imported formula re-derived on the corpus's own objects** ·
***⑦ NEW, from this baseline: word-bound every count and strip the citation keys — three substring failures in
one session.***

---

## ★★★ 4 · THE THEOREMS, UNDER THIS APPLICATION — and this bake's yield has its own shape

*Five probes, all run. ***The quadric field EXTENDED laws · the conformal field REFUSED · the optics field
CONFIRMED-AND-LOCATED · this field TURNS PRACTICES INTO THEOREMS.*** **In every probe the corpus was already
doing the right thing, and the field said WHY it works — which converts a practice into a theorem and, twice,
converts a checked fact into a guaranteed one.***

### ⛭ 4a · IMPORTED, AND EXACT

| the theorem | under CR | bound |
|---|---|---|
| **Riemann–Hurwitz** | ***the cover is GENUS ZERO*** — three sheets over the $2M$-sphere, simple branching at the two Nariai points, **total branching at infinity**, so $2g-2=-2$ | `X1_cover_genus_zero` |
| **A genus-zero curve is rational** | ***P3's SKY ANGLE is the cover's uniformising parameter*** — not a convenient coordinate but the uniformisation the genus permits | `X1` |
| **Single vs double periodicity** | ***the $2\pi i\alpha/3$ is a SINGLE period***: $\sinh^2$ has one period and no real period, so the time-plane is a **cylinder**, genus 0 | **second-period search is numerical over a bounded box** · `X2_single_period_not_lattice` |
| **The Schwarz reflection principle** | ***the seam continuation IS one***: $\sin$ is real on $\mathrm{Re}\,z=\pi/2$ — **a reality axis that is not the real axis** — and $\sin(\pi-\bar z)=\overline{\sin z}$ | `X3_seam_schwarz_reflection` |
| **Pole / branch / essential** | ***$r=0$ is an order-three branch point of $r(\tau)$ AND an order-four pole of $K$*** — both finite, neither essential | `X4_singularity_types` |
| **Monodromy representation and the $\pi_1$ relation** | ***$\tau_+=(0\,2)$, $\tau_-=(1\,2)$ DIFFER, so they generate $S_3$***; $c_\infty$ is a 3-cycle; $\tau_-\tau_+c_\infty=\mathrm{id}$ closes | `X5_monodromy_group` |

### ⛭ 4b · IMPORTED, AND IT LOCATED SOMETHING THE CORPUS DID NOT NAME

| the probe | what it located |
|---|---|
| **`X1` + `X5`** | ***THE BRANCH POINT AT INFINITY.*** *`X1` needed it for Riemann–Hurwitz and found it by asymptotics; `X5` needed it for the $\pi_1$ relation and found it by monodromy.* **Neither was looking for it, and the corpus names it in neither place. Two independent computations both require it** |
| **`X4`** | ***THE COMPATIBILITY OF TWO CLAIMS THE CORPUS ALREADY MADE.*** *"A branch point, not a barrier" and "a genuine curvature singularity" are both true, of different functions, and the corpus never said they were consistent* |
| **`X1`** | ***`Q4`'s OVER-REACH***, from two bakes earlier: *"$\iff j=0\iff$ CM by $\omega$"* implies an elliptic curve; the corpus's cover is genus 0. **Withdrawn in place** |

### ⛔ 4c · DOES NOT IMPORT — and `X2` says exactly why

| the theorem | why not |
|---|---|
| **The whole ELLIPTIC apparatus** — Weierstrass functions, period lattices, modular forms, the $j$-invariant, complex multiplication | ***both of the corpus's complex structures are GENUS ZERO*** — `X1`'s cover is a sphere, `X2`'s time-plane a cylinder. **There is nothing for the elliptic machinery to attach to, so the baseline's zeros are correct rather than owed** |
| **Uniformisation in its hard form** (the uniformisation theorem for genus $\ge1$) | *the corpus's objects are rational; uniformisation is the trivial case here, and what it buys — **elementary closed forms** — the corpus already spends everywhere* |

### ⛭ 4c′ · RE-ENTRY r2413 — **`C4`: THE QUEUE'S PREMISE WAS WRONG, and the correction is better than the entry**

**⌗ THE ENTRY, verbatim:** *"The same question on $\sinh$. `X2r` gives its single period $i\pi$; **its reality
set is the analogous grid**, and the turnaround sits somewhere on it."* ⇒ ***There is no analogous grid.***

$$\operatorname{Im}\sin(x{+}iy)=\cos x\,\sinh y \;\Rightarrow\; y=0 \ \text{ or } \ x=\tfrac\pi2+k\pi
\quad\textbf{a GRID, with CROSSINGS}$$
$$\operatorname{Im}\sinh(x{+}iy)=\sin y\,\cosh x \;\Rightarrow\; y=k\pi \ \text{only}
\quad\textbf{a PENCIL, NO crossings}$$

*because **$\cosh$ has no real zero**.* ⌗ **`X3r`'s whole seam result was that the seam is where two reality-lines
of $\sin$ CROSS at a critical point** — one fact giving four of P3's seam properties. ⇒ ***So no analogue of `X3r`
can exist on the clock: the queue asked for the analogue of a structure the other function does not have.***

**⌗⌗ AND THEN THE CORRECTION PAYS, because the corpus solves with $\sinh^2$, not $\sinh$.**
$$\operatorname{Im}\sinh^2=\tfrac12\sin(2y)\sinh(2x) \;\Rightarrow\; y=k\pi/2 \quad\textbf{HALF the spacing}$$
*· on $\operatorname{Im}u=0$: $\sinh^2=+\sinh^2x\ge0$ ⇒ $r^3\ge0$, **the expanding branch**;*
*· on $\operatorname{Im}u=\pi/2$: $\sinh^2=-\cosh^2x\le-1$ ⇒ $r^3\le0$, **the conjugate branch**;*
*· and $\operatorname{Im}u=\pi/2$ **is exactly the bead's bound** $\lvert\operatorname{Im}\tilde\tau\rvert=\pi\alpha/3$.*
⇒ ***THE TWO LOWEST REALITY LINES OF $\sinh^2$ ARE THE BEAD'S TWO BRANCHES — and the bead's bound is not a bound
but the NEXT REALITY LINE UP.*** *P3 gives the mechanism ("half that period carries $\sinh^2\mapsto-\cosh^2$,
placing the conjugate collapse"); this says **what it is** in the field's own terms.*

⛭ **AND THE ASYMMETRY IS STRUCTURAL RATHER THAN CURIOUS:** *$\sin$'s grid has crossings and carries a **SEAM**;
$\sinh^2$'s pencil has none and carries a **BRANCH SEPARATION**.* ***Two three-nesses, two reality sets, two kinds
of feature — `C1`/`C2`'s disjointness seen a third way.***
⚠ *Not shown: that the turnaround sits at a **distinguished point** of the pencil. It sits **on** the line
$\operatorname{Im}u=\pi/2$ and is held there along the collapse leg.* ***The entry was right about position and
wrong about the set.***
*Receipt `C4_the_reality_grid_and_the_pencil` — 11 checks.*

### ⛭⛭ 3a′ · THE CLOSE, RE-ENTRY r2413 — **the station is complete**

*The bake form closes on `scripts/check_bibliography.sh`.* **Run: one issue in the whole corpus, and it is not
this station's** — *P5 names the **photon orbit's Lyapunov exponent** without a source in its sentence, while P7
carries the same object **cited to `Cardoso2009`**.* ⇒ ***A named classical result cited in one paper and named
bare in another is `check_citations`' own first-use class, one paper over*** — **routed to the fork's list rather
than edited here** *(P5 is the fork's text).*

⌗ **STATION Ⓒ, CLOSED r2413 — four probes, four results, and the shape of the yield is worth stating:**
*· `C1` **located** a discipline's analytic cause (`4b`) — the two covers' branch sets are the two loci the corpus
pries apart;*
*· `C2` **gave** a statement stronger than the lemma it extends (`4a`) — the two three-nesses are not one tower;*
*· `C3` **corrected this bake's own record** (`4a`) — the deck is $S_3$, the dial is the Galois closure, the tower
is closed at the top;*
*· `C4` **killed a queue entry's premise and returned more than it asked** (`4c`) — no grid on the clock, but the
bead's two branches are $\sinh^2$'s two lowest reality lines.*
⇒ ***Two of the four corrected the corpus's own record rather than adding to it, which is what a RE-ENTRY is
for — and none of the four needed the fork's next bundle.***

### ★★★ 4a″ · RE-ENTRY r2411 — **`C3`: THE DECK IS $S_3$, NOT $D_6$ — a correction to this bake's own record, and it CLOSES THE TOWER**

**⌗ THE QUESTION WAS UPWARD, and it turned out to be malformed as asked.** *`C2` closed the tower's outward
question; its remaining one was **what sits above the dial, and is $D_6$ the top?*** ⌗ ***The reason the question
is malformed is in `X1r`'s own parenthesis.*** *`X1r` recorded the dial as "6-to-1, deck $D_6$ (rotation
$w+60^\circ$ and reflection $w\mapsto-w$, **both sending $2M\mapsto-2M$**)" —* **and a deck transformation of the
map dial $\to$ $2M$-plane must PRESERVE $2M$.** *Both named generators flip it, and the record says so in the same
breath.*

**⌗ COMPUTED.** *With $2M(w)=\tfrac{2}{3\sqrt3}\sin3w$:*
*· **preserve $2M$**: $a:w\mapsto w+2\pi/3$ (order 3) and $b:w\mapsto\pi/3-w$ (order 2), satisfying
$a^3=b^2=1$, $bab=a^{-1}$ ⇒* ***the group is $S_3$, order 6***;
*· **send $2M\mapsto-2M$**: $w+\pi/3$, $-w$, $w+\pi$ — **the other coset, which is exactly $R$**: `X1r`'s own
second storey, correctly identified there and misassigned in the parenthesis.*
⇒ **The dial over the $2M$-plane is degree 6 with a deck of order 6 — a REGULAR (Galois) covering.**

**⌗⌗ AND THAT IS WHY IT IS NOT PEDANTRY.** *A degree-3 covering with $S_3$ monodromy is **not** regular — its deck
is **trivial** — and the regular cover is the degree-6 **Galois closure**.* ⇒ ***So "6-to-1 regular with deck
$S_3$" IS the statement that the dial is the GALOIS CLOSURE of the root cover*** *— which the corpus already
carries at P5 `rem:galois-closure` ("the sky angle IS the Galois closure", landed r1882).* **The two records agree
once the group is named correctly, and disagree while it is not.**

⇒ ⛭ **THE TOWER IS COMPLETE AND CLOSED AT THE TOP:** *dial (**Galois closure**, deck $S_3$) → root cover (degree
3, **non-regular, trivial deck**) → the $2M$-plane, with **$R$** the involution relating the dial to its
$2M\mapsto-2M$ image.* ***Nothing sits above a Galois closure that adds monodromy — a regular cover of a regular
cover adds none.***
⚠ *Not shown: that $D_6$ is wrong **about the dial** — it is right, as the dial's own symmetry **as a figure**
(P3 §sec:tour's hexagonal symmetry).* ***What is corrected is which group is the DECK OF WHICH MAP.***

⚠⚠ **AND ONE CHECK IN THIS RECEIPT WAS HOLLOW ON ITS FIRST DRAFT — `True and 6 == 6`.** ***A tautology, and
exactly the class the fork's `lint_assertions` was extended twice to catch*** *("arithmetic dressed as claims").*
**It passed this line's own `check_receipt_asserts`, which tests for the PRESENCE of a failure path and not for
whether the condition could ever be false** — ***the boundary written into that gate's own docstring at r2394, met
in this line's own receipt three revisions later.*** *Replaced with the thing actually claimed: the deck's
**order**, counted from the group elements, against the covering's **degree**, counted from the fibre.*
*Receipt `C3_the_dial_deck_is_S3_not_D6` — 13 checks.*

### ★★★ 4a′ · RE-ENTRY r2410 — **THE FIELD GAVE: the two three-nesses are NOT one tower, and that is stronger than the lemma it extends**

**⌗ THE QUESTION CAME FROM THE TOWER.** *`X1r` established $\mathrm{Aut}(A_2)=S_3\times\mathbb Z_2$ as a
**two-storey tower of covers** — the dial ($6{:}1$, deck $D_6$, six Nariai **crests**) over the root cover
(3 sheets, deck $S_3$, two Nariai **values**), with **$R$ the covering involution**. `L-212` (r2404) and `C1`
(r2408) then put a **third** three-fold in play: the turnaround deck, which **fixes $2M$** and branches at
$r=0$.* ***A three-fold that fixes the tower's base is either a further storey, a parallel cover, or unrelated —
and the three are distinguishable.***

**⌗ `C2` — TWO LINES DECIDE IT.** *With $r$ a root of $r^3-r+2M$:*
$$(\omega r)^3-\omega r+2M \;=\; r^3+2M-\omega r \;=\; r-\omega r \;=\; r(1-\omega),$$
*using $r^3=r-2M$.* ***That vanishes only at $r=0$.***
⇒ **The turnaround deck fixes the root cover's base and does NOT permute its fibre**, *so it is **not a deck
transformation of the root cover in any storey** and the tower does not extend to it.*

| | base | fibre | deck | branches at |
|---|---|---|---|---|
| **root cover** | the $2M$-plane | the cubic's roots | $S_3$ | $\lvert r\rvert=\alpha/\sqrt3$ |
| **turnaround cover** | the $r^3$-plane | the cube roots | $\mathbb Z_3$ | $r=0$ |

**⌗⌗ WHAT IT ADDS, and why this is `4a` rather than `4b`.** *P7's `lem:twoturnings` says **no affine change of
variable identifies the horizon roots with the turnaround's** — a statement about **ROOTS**.* ***This is the
statement about COVERS, and it is strictly stronger: not merely that no affine map identifies the triples, but
that NEITHER THREE'S DECK ACTS ON THE OTHER'S FIBRE AT ALL, so they cannot be related by any covering-space
construction over the $2M$-plane.*** **The field gave a statement the corpus does not have** — *which is this
bucket's definition, and getting the bucket right is the whole of what makes a four-bucket ledger worth keeping.*

**⛭ AND IT RETROACTIVELY LICENSES A MOVE THE CORPUS ALREADY MADE.** *P14 `sec:whichthree` seated the generations
**on the turnaround rather than on the hinges**, which required the two threes to be genuinely different
objects.* ***Had the turnaround deck been a storey of the root tower, the "which index is it" question would have
had no room to move. The move P14 made is licensed by a fact nobody had computed.***

⚠ **NOT SHOWN, and must not be read in:** *that the two covers share no structure whatever. They share the
variable $r$ and the parameter $2M$, and **both count three because $f=0$ has degree $D-1$** — which P14 states
and this does not touch.* ***What is shown is that they are not related AS COVERS.***
⌗ *Receipt `C2_the_two_threes_are_not_one_tower` — 10 checks, and it can fail.*
⇒ ⛭ **PAPER-GRADE, AND ROUTED RATHER THAN LANDED HERE:** *it belongs beside `lem:twoturnings` in P7 and
`sec:whichthree` in P14 — **and landing it in a paper is the fork's territory**. On the next routing list.*

### ⛭⛭ 4b′ · RE-ENTRY r2408 — **IMPORTED, AND IT LOCATED SOMETHING THE CORPUS ENFORCES BY DISCIPLINE**

*Station **Ⓒ** re-entered on the runway (r2407). §0's baseline is **this file**, current at c54.163; §1's opening
question came **from the corpus, not the field** — from `L-212`'s closure at r2404.*

**⌗ THE QUESTION.** *The corpus carries **two** three-fold structures, and `lem:twoturnings` says no affine
change of variable identifies the horizon roots with the turnaround's —* ***but that is a statement about ROOTS.
The complex-analytic question is about the COVERS: where does each branch?***

**⌗ `C1` — THE ANSWER, and it is disjointness.**
*· **Cover A**, the horizon root cover ($S_3$ monodromy, **moves $2M$**): branches where
$\mathrm{disc}(r^3-r+2M)=-4(27M^2-1)$ vanishes, at $2M=\pm\tfrac{2}{3\sqrt3}$, **the roots colliding at
$r=\mp\alpha/\sqrt3$ — the Nariai merged-horizon areal radius**.*
*· **Cover B**, the turnaround deck ($\mathbb Z_3$, **fixes $2M$**): branches where the deck has a fixed point,
$\omega r=r$, i.e. **$r=0$ — the branch point**.*

⇒ ***THE TWO BRANCH SETS ARE DISJOINT, AND THEY ARE EXACTLY THE TWO LOCI THE CORPUS INSISTS ON PRYING APART.***
*P7: **"$\alpha$ (the throat $3$-sphere's SIZE) versus $\alpha/\sqrt3$ (the merged horizon's AREAL RADIUS) —
quantities never to be conflated"**, and **"conflating the two reads a discontinuity into what is one smooth
curve"**; both are pry-apart census entries (r2388).*
⇒ ***So the naming guard the corpus enforces by DISCIPLINE is, on the complex-analytic side, the statement that
its two three-fold covers DO NOT BRANCH IN THE SAME PLACE*** *— which is why conflating the loci manufactures a
discontinuity: **it identifies a point where one cover is branched with a point where the other is.***

⌗ **Why `4b` and not `4a`:** *the field imported nothing new — discriminants and deck fixed-points are ordinary.*
***What it did was LOCATE a rule the corpus states as a warning, and give it a structural cause.*** *That is this
bucket's definition.*
⚠ **NOT SHOWN, and must not be read in:** *that the two covers are unrelated as spaces — only that **they do not
branch at the same places**, which is what makes the corpus's insistence a structural fact rather than a
notational preference.*
⌗ *Receipt `C1_the_two_covers_branch_where_the_corpus_pries` — **12 checks, and it can fail; it DID fail on its
first run**, because `sp.solve` **deduplicates roots and so cannot see a double root at all**.* ***A check that
cannot find the thing it is looking for is not a negative result, it is a broken check*** *— and it failed
loudly, which is what a receipt that can fail is for.*

### ⟐ 4d · UNTRIED

*• ***⌫ STRUCK r1906 — "the $\mathrm{PSL}(2,\mathbb{C})$ action on the thrice-punctured sphere": THERE IS NO THIRD PUNCTURE.** It was `X1`'s compactification artefact, withdrawn at r1874, and this entry outlived it.*
• ***✔ CLOSED r1888 — "the second reality axis": $\sin$'s reality set is the real axis plus EVERY line $\mathrm{Re}\,z=\pi/2+k\pi$, and the crossings sit at $r=\pm\alpha$ — **exactly the de Sitter member's two nonzero horizons**, with $r=0$ on the real axis away from any crossing. Landed in P3.*
• **Growth and order at infinity — ✔ WORKED r2391 (`L-209`), AND THE ANSWER WAS TWO SECTIONS ABOVE THIS LINE.**

  ⚠ ***This entry called the question unasked while §4a of this same document answered it twice.*** *That is the
  seventh instance of "two indices of one object never compared" in this audit and **the first inside a single
  file** — the queue at §4d and the theorems at §4a, three screens apart.*

  **⓵ THE FINITE-PLANE RESTRICTION IS NOT A GAP; IT IS THE CORRECT DOMAIN.** *Read at P7 at source: the licensing
  argument is* **"$r(z)=M(1+\cos z)$ and $\sinh$ are entire, so the invariant is meromorphic and the bead's radius
  carries only algebraic branch points — no essential singularity anywhere in the **finite plane**"**, *and Picard
  enters* ***negatively***: *"an essential singularity would continue as neither [a map to the sphere nor a
  finite-order cover], and by Picard's theorem nothing could be said of the values beyond it."*
  ⇒ **The act being licensed is a continuation through a turning point at a FINITE radius** — the seam at
  $X=\alpha$, the back of the lap at $r=0$, the merged-horizon radius $\alpha/\sqrt3$ — ***so the finite plane is
  not a convenient restriction but exactly the domain the claim quantifies over. $\sinh$'s essential singularity
  at infinity is real and no crossing happens there.***

  **⓶ AND INFINITY IS ALREADY CHARACTERISED, in §4a, in two independent ways.** *`X1`: the cover is genus zero —
  three sheets over the $2M$-sphere, simple branching at the two Nariai points, and* ***total branching at
  infinity***, *so $2g-2=-2$. `X2`: the $2\pi i\alpha/3$ is a **single** period, $\sinh^2$ having one period and no
  real period, so* ***the time-plane is a cylinder***. ⇒ **So the analytic statement the queue asked for exists:
  infinity is the total branch point of the three-sheeted cover and the cylinder's end.** *It was never missing;
  it was never joined to the question.*

  **⓷ [reach — do-not-assert] THE ORDER IS DETERMINATE AND MIGHT SAY SOMETHING.** *$\sinh$ is entire of **order 1,
  exponential type** — the minimum order for a non-polynomial entire function with a period — and Hadamard's
  factorisation determines an order-1 entire function from its zero set up to $e^{az+b}$.* ⌗ ***Whether "the rate
  is fixed by $\Lambda$ alone up to a gauge" is the analytic content of that*** *is a suggestion and not a result:
  it has no receipt, the gauge correspondence is not checked, and it would need the zero set of the actual
  invariant rather than of $\sinh$ alone.* **Held at reach weight; a positive here would be new, and the ledger's
  own prior — "noted, not used" — means a negative costs nothing.**


---

## ★★ 5 · THE FOUR-PART CLOSE

**① `check_bibliography.sh` — CLEAN.** *Ahlfors added for the Schwarz principle and Picard.*

**② The theorems ledger — above.**

**③ THE CENSUS PASS — ZERO enrichments, and saying so is the pass.**
*Checked all five against `THE_DISSOLUTION_CENSUS`'s six clusters. **None enriches one.*** `X1`, `X2`, `X3`
and `X5` **explain corpus practices rather than dissolve standing problems**; `X4` **licenses a construction**.

**⛔ AND THE FIRST PASS GOT THERE BY A FALSE ROUTE, corrected r1873 — the reasoning was wrong, the answer
right.** *It rejected cluster B on the ground that **"the cosmic beginning is the branch point $r=0$ at $r=\alpha$."***
***The error is the BARE WORD.*** §0, settled r1012 with a corpus sweep: ***"'The seam,' unqualified, means the
Nariai cosmogenesis branch point ($\kappa=0$, areal $r=\alpha/\sqrt3$)"*** — **and that IS the cosmological beginning.**
*(The **qualified** "throat seam at $r=\alpha$" is correct and is the corpus's own: §0's `r` entry lists
**"$r=\alpha$ ↔ the de Sitter horizon / equatorial-ξ seam"** among its **values to hold**. The throat's SIZE
$X=\alpha$ and the seam's AREAL radius $r=\alpha$ are different quantities, kept apart on one line of §0.)*

***The correct reason `X4` does not enrich cluster B:*** *cluster B's dissolution is the cosmological beginning
— **the Nariai seam, areal $r=\alpha/\sqrt3$** — and `X4` is about **the $r=0$ crossing**, the crossing onto
the conjugate branch. **Distinct turning points of the one bead; `X4` licenses the crossing rather than
dissolving the beginning.*** **Answer unchanged: zero. Reasoning replaced.**

**④ THE P6 PASS — TWO moves on `R2`, against ONE across the previous three bakes combined.**
*• ***`X3` MOVES.*** *Before: the continuation lands on real data, and the corpus **verifies** that it does.
After: **it must**, by the Schwarz principle.* ***A verified fact becomes a theorem's conclusion — which is the
mechanism, at this seam, of "reached through the imaginary, real by construction."***
• ***`X4` MOVES.*** *Before: "a branch point, not a barrier" — **asserted**, the two descriptions left
unreconciled. After: **licensed**, and the licence names what would have blocked it (an essential singularity,
and by Picard nothing could be said beyond it).*
• **`X1`, `X2`, `X5` do not move.** *`X1` supplies a second forcing of an already-forced object; `X2` explains a
capacity never in doubt; **`X5` discharges an unrun check, and discharging is not a modal upgrade.***

***AND THE ASYMMETRY BETWEEN ③ AND ④ IS THIS BAKE'S SIGNATURE: the lowest census yield of any bake so far, and
the highest `R2` yield. It dissolved nothing and it upgraded two claims — because turning a practice into a
theorem is exactly the move `R2` credits and exactly the move a dissolutions census has no column for.***

## ★★★ 4 · THE CLOSE — re-run r1880, after the audit

***All five probes were corrected. The bake's honest yield is below, and it is smaller and better than the
first pass claimed.***

### ⛭ 4a · WHAT THE FIELD GAVE

| result | what it is |
|---|---|
| **`X1r`** | ***$\mathrm{Aut}(A_2)=S_3\times\mathbb{Z}_2$ IS A TWO-STOREY TOWER OF COVERS.*** *The root cover (3 sheets over the $2M$-plane, deck $S_3$, branched at the two Nariai **values**) and the dial (6-to-1, deck $D_6$, branched at the six Nariai **crests**); **the dial is a double cover of the root cover and its covering involution is $R$.*** Each factor carries its own branch set |
| **`X2r`** | ***EACH THREE-NESS IS ONE SINGLY-PERIODIC ELEMENTARY FUNCTION COMPOSED WITH A THREE-FOLD*** — *$\sinh^2$ with the cube root, $\sin$ with the triple angle.* **That is why the corpus closes in elementary terms**, and the two periods differ in kind as the three-nesses do: **imaginary along the clock, real along the dial** |
| **`X3r`** | ***THE SEAM IS WHERE TWO REALITY-LINES OF $\sin$ CROSS, AT A CRITICAL POINT.*** *One fact giving four of P3's seam properties — maximal $r$, the tangency that **defines** a seam, reality on both pieces, the $C^1$ join — and the signature flip from the right angle between the lines* |
| **`X4r`** | *no **essential** singularity in the finite plane, in either parametrisation — **which is what makes "not a barrier" analytic rather than geometric*** |
| **`X5`** | *P5's generation claim is a **computation**, and it holds: $\tau_+=(0\,2)$, $\tau_-=(1\,2)$, order six. **The alternative was real*** |

### ⛔ 4b · WHAT THE FIELD DID NOT GIVE, and the first pass claimed it did

*• **A genus.** *`X1`'s Riemann–Hurwitz was run on a compactification of P5's **plane**, manufacturing a branch
point at infinity the construction never reaches.* • **A Schwarz reflection.** *The principle extends a function
across a line; $\sin$ is **entire**, so it had no work to do.* • **A reconciliation of two claims about $r=0$.**
*P2 `prop:Kretschmann` had already classified it — a twelfth-order pole, by the chain rule at a non-degenerate
critical point.* • **An elliptic structure of any kind.** *Correctly absent, and `X2r` says why without needing
genus.*

### ⟐ 4c · UNTRIED — rebuilt r1884, the r1873 queue having rested on withdrawn results

*• ***THE OTHER REALITY-LINE CROSSINGS — ✔ CLOSED r1888.*** **The crossings sit at $r=\pm\alpha$, alternately.** *And for the de~Sitter member the mass vanishes, so the horizon cubic is $r^3-\alpha^2r=0$ with roots $\{+\alpha,0,-\alpha\}$: ***the two crossings ARE the member's two nonzero horizons, and the third root $r=0$ lies on the real axis away from any crossing.*** **So the reality grid sorts the de~Sitter triple by TYPE: the two $R$-conjugate horizons are where two reality-lines MEET; the branch point lies on ONE line only** — and the alternation $+\alpha,-\alpha$ along the grid is the mass-reflection read on it. Landed in P3.*
• ***The same question on $\sinh$.*** *`X2r` gives its single period $i\pi$; **its reality set is the analogous
grid, and the turnaround sits somewhere on it.***
• ✔ *Growth and order at infinity — **WORKED r2391 (`L-209`); see §4d above.** The finite-plane restriction is
**the correct domain and not a gap** (the licensed act is a continuation through a turning point at a FINITE
radius), and **infinity is already characterised in §4a twice**: `X1`'s **total branching at infinity** and
`X2`'s **cylinder**. ⌗ **A duplicate of this queue line sat here as well as at §4d**, which is the same defect
one floor down: the question was written in two places and answered in neither.*

**⌫ WITHDRAWN from the r1873 queue:** *the $\mathrm{PSL}(2,\mathbb{C})$ action "on the thrice-punctured sphere"
— **there is no third puncture**; it was `X1`'s compactification artefact.*


### ★★ 4d · THE FOUR-PART CLOSE

**① `check_bibliography.sh` — CLEAN.** *Ahlfors retired from P3 with the Schwarz claim; still live in P7 for Picard.*
**② The theorems ledger — §4a and §4b above.**
**③ THE CENSUS PASS — one enrichment, and it is not from this field.** *None of `X1r`–`X5` enriches a cluster;
they explain corpus practices. **The quadric bake's `Q6r` enriches F, found by the audit.***
**④ THE `R2` PASS — ZERO from this field.** *`X3`'s move went with its claim: **explaining** the reality is not
**guaranteeing** it. `X4`'s went too. **The field's whole yield is explanatory, and that is worth recording as
its shape.***

### ⛔ 4e · THE HANDOFF — WITHDRAWN r1882, and the withdrawal is the finding

*`X1r` was read as handing to R-M's station **Ⓓ, Galois theory** — on the true fact that a branched cover's deck
group is the Galois group of its function-field extension. ***The corpus already says so.*** P5 `rem:galois`:
**"the deck group is, equivalently, the Galois group of the horizon cubic over $\mathbb{C}(2M)$… one $S_3$ worn
as monodromy, Weyl, and Galois symmetry alike,"** with a citation to Harris (1979); and P7 carries the* casus
irreducibilis *at `lem:twoturnings`.*

***So Ⓓ is not an open station, and the field bake opened on it was retired before a probe ran.*** **What
survives is the one thing `rem:galois` does not say — it mentions neither *splitting*, nor *closure*, nor the
*dial*: that the sky angle IS the Galois closure.** *Landed at `rem:galois-closure`, now placed immediately
after `rem:galois` where it belongs.*

***AND THE PATTERN THIS MAKES WITH Ⓑ IS THE REAL RESULT: twice in one audit a reach list named as open a station
the papers already held.*** **A reach list records what the LIST knows, not what the CORPUS knows.**


---

## ⛭ 3 · WHAT LANDS WHERE

*• **P5** `groupoid_paper` — the branched cover, the deck action, the monodromy. **`X1`, `X2`, `X5`'s home.**
• **P3** `SdS-slicing-curve` — the seam continuation. **`X3`'s home.**
• **P7** — the bead's conjugate wings and the bounded contour. **`X2`'s if it reaches the lap.**
• **p0** — the imaginary-as-instrument reading. **Anything about what the continuation IS.**
• `ONTOLOGY_FOUNDATION_INDEX` §0 — **`residue`, `pole` and `Schwarz` are entered; any further collision goes in
before use.***
