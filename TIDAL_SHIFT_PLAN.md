<!-- FORWARD WORKING DOCUMENT. The plan for the r1064–r1087 tidal shift.
     Read off C40_HARVEST_r1064-r1087.md, the c40 transcript, and Daryl's stated
     staging (r1066, r1071). Stated for reversal. -->

# The Tidal-Shift Plan — getting the gradient running the right way
### ✅ **COMPLETE r1090. THIS IS NOW THE RECORD OF THE CLEARING, NOT A LIVE ROUTE.**

> **The live route for the work this cleared the way for is `THE_P13_POSITIVE_CLOSURE_ARC.md`**
> (opened r1072; wired into `THE_PLAN` A4.10 at r1090 after being found an **orphan**). **Do not
> plan from this document.** It exists to record *how the gradient was turned*, and its stages
> 7–8 are the arc's phases B/E — kept here only as the record of what r1088–90 did to them.
>
> **Why collapse rather than keep both:** two route documents for one job is how the first orphan
> was made. This one covers a job the arc does not and should not — **clearing the instrument**
> (stages 0–6: the method layer, the audit flags, the weld, the uncontested physics, the kept
> negatives, the plan re-cut). That job is **done**. Everything past it belongs to the arc.
>
> **What it cleared, for the record:**
> **0** ✅ the method layer — `THE_CODA` §"The negatives are the map" + the two kinds of reversal;
> `THE_METHOD` §4b the **perimeter defect**; field-note faces 29–32; `THE_PLAN`'s banner r1011→r1088;
> the kickoff's dispatch bar and the clean-sweep discriminator.
> **1** ✅ both r1069 audit flags discharged. **2** ✅ the weld (P3 `rem:charge-scope`, P13
> `rem:C-linear-face`); **P5/P9 needed nothing** — restraint. **3** ✅ P7 `rem:M-scale` +
> `rem:two-thirds`. **4** ⛔ **DISSOLVED** — the refuter gate was a cyanide pill and the claim was
> wrong; the source question killed it in ten minutes. **5** ✅ the kept negatives, the 30° and the
> 1:2 **walked** rather than filed. **6** ✅ the plan re-cut: A4.10 + **A7** (the full analytic
> object) created, the storyboard-"A3"/plan-"A3" collision named.
> **7–8** → **the arc's B and E.**
>
> **And the instrument it left behind:** `storyboard_receipts/dead_reasons.py` — the register of
> eleven dead reasons, with its holes stated. It is what found the orphan.


**Brief (Daryl, r1066):** *"get all the things that are the most load-bearing pieces of this
tidal shift and get them baked first … the most urgent ones, that don't allow a precarious
thing to get pulled back to the opposite of what it is supposed to be."*

**Destination (Daryl, r1071):** P13's **positive closure** — *"nothing short of a drawn
connection between CPT and cosmology"* — and then a corpus-wide enrichment that brings the
synthetic ontology into every paper **on its own terms**.

**The staging is unseated, not mine** (r1066):

```
not outright inconsistent → compliant → consistent → coherent → cohesive → thorough & fulsome
```

---

## ⚠ THE CONSTRAINT THE THREAD PUT ON ITS OWN PLAN — read this before dispatching anything

Harvest **I.2** is not a remark about P13. It is a **structural constraint on how this plan may
be executed**, and it comes from the thread's own most expensive failure:

> **A joint-by-joint pass cannot find a perimeter defect.** P13 is locally right everywhere;
> every local check vindicated it; seven honest flips all confirmed the paper. The r1063 sweep —
> **eleven papers, seven farmed to parallel readers, run against a written delta-spec** —
> returned **not one edit needed**, and that null result *was the method reporting on itself*.

**Therefore, binding on every stage below:**

1. **Synthesis is never dispatched.** Not to parallel readers, not to a spec. The one thing that
   demonstrably cannot be outsourced is the only thing that can find this class of defect.
   **Arthur holds every perimeter question whole.**
2. **Excaliburs get bounded verification, focused computation, and refutation — never coherence
   judgment.** "Does this paper contradict X?" is a compliance check and can only return clean.
   "Is this paper right?" cannot be asked by a reader who has been handed X as the spec.
3. **Every sweep must be able to return work.** If a pass comes back all-clean, that is a **tell
   about the pass**, not a finding about the corpus (`CODA_FIELD_NOTE` r1066).
4. **The forward documents are the instrument.** Storyboard, ledger, this plan, the harvest.
   When they disagree with a paper, the disagreement is the work product. **"Stale" is a word
   for the corpus.**

---

## STAGE 0 — STOP THE PULL-BACK *(foundation-first; no physics touched; mine; cheap)*

**Why first, and it is not a preference.** Everything in stages 1–8 is a *precarious thing*.
[5] has already been retired once (r1060) **by the very remark it refines**. The weld lives in
one ledger entry. The harvest's new results live nowhere. **Until the method layer is baked,
the next instance re-runs the failure** — the thread demonstrates this three times in one
session, and the corpus's own foundation-first rule (`THE_CODA` §"The order of work") says a
base-flaw propagates through everything built above it.

| # | target | content | source |
|---|---|---|---|
| **0.1** | `THE_CODA.md` | **The load-bearing negative.** *A face that is NOT operative somewhere is as load-bearing as one that is — that is how you know which face you are looking at.* The coda has three siblings of this rule (retrieval / decisions / when-the-source-answers-back) and **not this one**: they all assume the finding survives. This is the rule for the paths that don't. | Harvest I.1; Daryl r1070 |
| **0.2** | `THE_CODA.md` | **The two kinds of reversal.** Deference (*the corpus answered and I folded* → revert) vs genuine input (*a physical fact neither document contained* → bake). Discriminator: **could re-reading have supplied it?** Without this, the r1066 gradient rule reads as *never change your mind*. | Harvest I.3; the weld's own provenance note |
| **0.3** | `THE_METHOD.md` | **The perimeter defect** as a named class, with its diagnostic (*if every local check confirms and the dissonance persists, the defect is not in a joint — ask where the boundary sits relative to the whole*) and the honest admission that **every instrument THE_METHOD currently carries is local and cannot reach it**. Plus the dispatch constraint above. | Harvest I.2 |
| **0.4** | `CODA_FIELD_NOTE.md` | The weld as a face: **the corpus correcting a forward document is legitimate when the forward document misread what it was holding** (the `Q²/r²` catch). The gradient rule is not licence. | Harvest II.4 |
| **0.5** | `THE_PLAN.md` | CURRENT STATE banner **r1011 → r1087+**. It is 76 revisions behind and knows nothing of the storyboard phase, the ledger, the conjecture, the kaleidoscope, or P13's positive closure. Any node reading it today is oriented to the wrong edge. | Harvest V.1 |

**Gate out of stage 0:** the four rules are in the coda/method at source, and `THE_PLAN` points
at the real edge. **Nothing downstream is safe before this.**

---

## STAGE 1 — NOT OUTRIGHT INCONSISTENT *(bounded; discharges the two live flags)*

The corpus currently carries two **AUDIT FLAG (r1069)** boxes saying, in effect, *"the argument
below overdraws; read the remark, not this."* A flag is a holding action, not a fix.

| # | target | content |
|---|---|---|
| **1.1** | P13 `rem:C-not-R` | Restore **r1053's final-clause narrowing, in P13's own voice**: not-an-isometry ≠ not-geometric. `τ̃↔τ̄̃` is a verified antilinear involution in the bead's complex-analytic geometry; what is genuinely field-level narrows to **the charge sign alone**. P13's premise, argument and level allocation **stand** — confirmed by the failures of [1],[2],[3],[7]. **Direction: this ENLARGES the residue and STRENGTHENS the paper.** A wall mapped more precisely, not a wall breached. |
| **1.2** | map §0 `R,P,T` | De-compress the **double compression**: restore the *linearity* argument (the load-bearing one) and demote the root-motion side remark, which is **rigged** (`Q` enters as `Q²`; the test fixes the roots by construction). Restore *"it is **only** that even face"* — a face, not an identity. |

**Not settled here, and explicitly not:** **[6]** — whether the L2 antilinear face **is** `C` or
is `C`'s kinematic shadow. Nothing on disk decides it. **→ A3.**

**Receipt:** `storyboard_receipts/charge_geometry.py` ✔ re-run clean.

---

## STAGE 2 — COMPLIANT → CONSISTENT *(the weld; a physical input, not deference)*

| # | target | content |
|---|---|---|
| **2.1** | P13 / P5 / P9 / map §0 | **Species and charge are one binary.** `C` flips *all* charges together and that totality **is** the species; neutrality is the degenerate case (which is why the photon is self-conjugate and why `τ̃↔τ̄̃` fixes the real axis). |
| **2.2** | P13 / map | **The corpus's stated reason is metric-only.** *"Charge is the `R`-even datum"* is **true of the metric, false of the potential** — under `R`, `g` is invariant but `A_t = Q/r` is **odd**. ✔ `charge_geometry.py`. `R` does not fail to touch the charge. |
| **2.3** | P13 | **`R` is `C`'s LINEAR FACE.** They agree on every label (species flips, charge flips, `\|2M\|` preserved) and differ **only in character**. `rem:C-not-R`'s conclusion survives **via linearity** — always its real argument — while *"`C` is not `R`"* is true and **conceals that `R` is part of `C`**. → `C` = (linear `R`) ∘ (antilinear `τ̃↔τ̄̃`). **→ A3.** |

**Corrects the ledger too:** [1] and [2] are superseded by the weld and **kept** (per 0.1) —
their failure is the record of how the weld was missed.

---

## STAGE 3 — THE UNCONTESTED PHYSICS *(receipted; touches no open question)*

| # | target | content |
|---|---|---|
| **3.1** | P7 / P8 / P3 | **M sets the SCALE of the bead, not its SHAPE.** Along cosmic time `K(τ̃) = (12/α⁴)(sinh⁻⁴ + 2)` is **universal** — every progenitor, whatever its mass, meets the identical curvature at the identical cosmic time. ✔ `kretschmann_bead.py`, `M_cancellation.py`. Companion to P3's *"M is a turning point, not a coefficient"* and P8's *"the mass is the cut's offset"* — **sharper than either**, because it is the whole bead in the observer's own clock. **Scope: `M≠0` gates whether there is a bead at all.** |
| **3.2** | P7 §bead or P8 | **The `2/3` exponent does three jobs and they are one job:** the vertical tangent (`dr/ds ~ s′^{−1/3}`), the singularity's strength (`K ~ s′^{−4}`), and the M-cancellation (`6 × 1/3 = 2`). ✔ `M_cancellation.py` (D). The thread has three separate notes; it is one line of dimensional analysis. |

**✘ NOT baked — refuted in the harvest:** *"the M-cancellation IS the corpus's generation
result."* Different quantifiers (three roots at fixed M vs M at fixed `w`). ✔ `M_cancellation.py`
(B). It is the flip-flop running the other way, one turn after being caught. **Logged as a kept
negative (0.1), not carried forward.**

---

## ~~STAGE 4 — THE ONE THAT CHANGES WHAT A THEOREM SAYS *(refuter-gated)*~~ — **DISSOLVED r1088**

> **The gate was a cyanide pill and the claim was wrong — in that order, and the first is why the second nearly survived.** (Daryl, r1088: *"refuter Excalibur is a cyanide pill. Are you looking to kill all the development before it can get off the ground?"*)
>
> **The gate.** A refuter set to **default-to-refuted-if-uncertain**, aimed at a **synthesis**, can kill and **structurally cannot confirm** — to return anything but *uncertain* it would have to reconstruct the synthesis, and **synthesis is not dispatchable** (`THE_METHOD` §4b, written the same day). Run the rigged-discriminator check on it: *could it have returned ¬X?* **No.** It is a rigged question pointed at the gallows — *"manufactured certainty, pointed at the gallows instead of the throne"* (`THE_INTERFERENCE_ENGINE` §4, the symmetric verdict-bar). I built the finding and then designed a machine that could only destroy it, and called it rigour.
>
> **The check that was actually owed** was a source question — *does `thm:bead` already know this?* — cheap, mine, and able to go either way. It went against me in ten minutes (`storyboard_receipts/thm_bead_source_check.py`): **P7 never claims `C¹`, and P7's own proof already derives that the lap requires imaginary time.** III.1's object-claim is **dead**; see the harvest's demotion. **P7 gets nothing at this stage. It was right.**
>
> **What survives** goes to stage 5 as a kept negative at the storyboard's own weight: the `180 mod 120 = 60` obstruction, and *the sheets are not the bead's home*.
>
> **The lesson, and it is the reusable part:** *a refuter is for a claim that can be attacked locally. It is not for a synthesis, and reaching for one against your own best finding is the kill switch, not a gate.*

### ~~The original stage 4 (retained per the negatives rule)~~

| # | target | content |
|---|---|---|
| **4.1** | **refuter-Excalibur, FIRST** | Task: **refute** *"the cosmological bead is smooth only because it goes through imaginary time."* Default-to-refuted-if-uncertain. Dig both endpoints: the nine-join enumeration (`180 mod 120 = 60`), and the claim that `arg r = 180` is unreachable on the real-`τ̃` face. Receipt to attack: `smoothness.py`. |
| **4.2** | P7 `thm:bead` | **On survival:** the `C¹`-ness is **purchased by the imaginary excursion**. The lap is not what the signed radius *permits* — it is **where the smoothness lives**. A bead confined to real `τ̃` could not be smooth at `r=0` at any `M`. |

**Why the gate:** this asks a paper to change what its own theorem is saying. That is exactly
the condition the coda names for convening a refuter — *"when a result warrants it, not as a
standing gate."* **Majority-refute kills it.**

**The two adjacencies it opens, neither claimed:** (i) the obstruction is a fact about the
**three-fold** — the same three-fold the corpus reads as the generation multiplicity; (ii) it
gives *"both faces are shadows"* teeth — **one face forbids the very property the other
exhibits**, which is the strongest available argument for A3's *work it whole or not at all*.

---

## STAGE 5 — THE KEPT NEGATIVES *(0.1's own rule, applied)*

Into the storyboard **as findings with computed causes of death**, not deletions:

- **5.1 — the `su(3)` weight-diagram identification is dead, and we know why.** The **2:1 is
  forced by equilateral tangency**: the incircle touches at the side midpoints, at *half* the
  circumradius, rotated *60°*. **The rotation and the ratio are the same fact.** And the union
  is a **hexagram**, not a scale-mismatched hexagon; su(3)'s two hexagons are **both
  equiradial**. ✔ `su3_check.py`. *We know why it looks like that, and the reason forbids the
  identification.* **Better than a live coincidence.**
- **5.2 — §4b is the strong A₂ claim; §4c is the weak one.** §4b lands on the **root** hexagon's
  angles and, being rays, has nothing to mismatch. They were opened 8 revisions apart, called
  companions, and **neither checks against the other**. ✔ `hexagon_check.py`.
- **5.3 — the shared `0/120/240` is forced-vs-label, not content.** What *is* shared and is not
  conventional: **3 + 3 at 60°, by two unrelated roads inside one substrate.**
- **5.4 — the `30°` is two 30's until a mechanism links them.** `360/12` from coprimality;
  `w=30°` from `sin 3w = 1`. No shared mechanism on the table. The thread's instinct to flag and
  not claim was right.
- **5.5 — one 1:2 wearing three costumes** (lift:period; 120:240 arc; `∛2` turnaround:seam).
  Checkable, unchecked. ✔ `smoothness.py` (D).

---

## STAGE 6 — COHERENT: the plan re-cut

**6.1** P13's positive closure onto `THE_PLAN` as a lane item — currently **nowhere**, and it is
the largest gap between the plan and the work. **6.2** A3 stated as the gate it actually is:
`[6*]`, the sheet-to-ruling map, the gate-count at the origin, and now the three-fold/generation
adjacency all funnel into it. **6.3** The dispatch constraint (⚠ above) written into the plan's
own execution notes.

---

## STAGE 7 — COHESIVE: P13's positive closure *(Arthur-held; NOT dispatchable)*

Daryl's own statement of the target, r1071: P13 stops being *"a dumping ground to capture all
the negative boundary results"* and becomes the paper it was always meant to be — it
**synthesises those at weight at exactly the earned level, draws a synthetic picture of the
whole, AND BUILDS FORWARD FROM THAT.**

**The material is already on the table** and it is why this is stage 7 and not stage 2: it needs
1–5 baked and 4.1 survived first. The pieces: the level structure (L1 isometry / L2
complex-analytic / L3 field) as the *body*; the perimeter as the *skin*; the enlarged residue;
`C = R ∘ (τ̃↔τ̄̃)`; the FS/CPT kinematic face; and the connection to cosmology through the bead
the antilinear involution acts on.

**By I.2 this is the one task that cannot be farmed.** It is a synthesis, and synthesis is
exactly what the local pass cannot do.

---

## STAGE 8 — THOROUGH & FULSOME: the corpus-wide enrichment *(last, and run correctly)*

Daryl, r1071: every paper carries the synthetic ontology **on its own terms**, forward- and
backward-referencing the whole — *"in all its multidimensional glory."* Includes checking the
annihilation/pair-production diagrams against P14's actual theory, **way down the line**.

**Run it the way I.2 says it must be run.** The r1063 sweep is the counterexample sitting in the
record: eleven papers, seven farmed out, one delta-spec, **all clean, nothing found**. This pass
is the same shape unless the shape is changed.

---

## Dispatch policy (binding)

| | Arthur (held) | Excalibur (dispatched) |
|---|---|---|
| synthesis, perimeter questions, the ledger, stages 0/6/7 | ✔ | ✘ **never** |
| bounded source-verification ("does P9 §charge say X, verbatim?") | | ✔ |
| focused computation (receipts) | | ✔ |
| **refutation** (4.1) | | ✔ — default-to-refuted, dig both ends |
| coherence judgment ("is this paper right?") | ✔ | ✘ **never** |

---

## The critical path, in one line

**0 → 1 → 2 → 3 → ~~4~~ (dissolved r1088) → 5 → 6 → 7 → 8.**
Stages 0–2 are cheap and touch no open question. **Everything from 4 on is downhill of 0.**
Running it in any other order is what produced the thread.

## What is genuinely Daryl's and is not being asked

The staging (r1066), the destination (r1071), and P13-grows-rather-than-spawns (r1071) are
**stated** and are not re-opened here. Nothing in stages 0–3 needs an input the source does not
already settle. **Stage 0 is reversible, foundational, and his own stated first priority; it is
the gate's to carry.**

*Stated for reversal.*
