---
name: figure-theorem-ledger
kind: FORWARD
current: n/a
job: The figure–theorem ledger: which classical theorem each figure carries, and its receipts.
sources: [cowork]
class: forward-document
---

> **▣ FORWARD — ahead of the corpus by construction, and exempt from the staleness test.** *Which classical
> theorem each figure carries, with its receipts. Declared exempt (`current: n/a`) as a `FORWARD` document,
> and that declaration stands.* ⚠ *The reason originally given — its own head's **"banked, not baked —
> nothing here has gone into a paper"** — **is no longer true and was struck at r3524**; the exemption
> rests on the document being ahead of the corpus by construction, not on nothing having landed. A sweep
> at r3552 overwrote `n/a` with a revision on the strength of the landing table's "checked r3524" and was
> reverted: **a declared-exempt marker is a statement, and a staleness sweep must not silently answer it.***
>
> ⛔ ***Do not date it against the corpus and do not retire it for disagreeing with one.*** *Where it
> disagrees with a paper, **the disagreement is the work product** — a finding to work, not a defect to
> fix. A header-versus-body contradiction inside it is still fair game; a comparison to the corpus is not.*

<!-- FORWARD WORKING DOCUMENT. The figure programme's theorem ledger, r1108.

> **⌗ RECLASSIFIED r2385 — `current: n/a`, DECLARED-EXEMPT, because this is a FORWARD DOCUMENT and it says so.**
> *Its own head: **"FORWARD WORKING DOCUMENT … Banked, not baked: nothing here has gone into a paper"** and
> **"⛔ THE GRADIENT APPLIES · AND SO DOES THE WEIGHT RULE."*** *Dating it against a later fork front invites the
> inverted-gradient repair its banner forbids —* ***"stale" is a word for the corpus, never for the instrument
> examining it.*** *It is the second document to earn this classification, after
> `SYNTHESIS_FIGURE_STORYBOARD`, and both earned it by carrying the banner themselves.*
> ⌗ **What it is NOT exempt from is the header-vs-body check**: *a forward document contradicting **itself** is
> always a finding. Its three registers — **⊢ PROVED · ≈ RHYME · and the third** — are kept apart on purpose,
> "because the whole value of this document is that they do not blur," and that separation is checkable.*
     Banked, not baked -- STRUCK r3524, see THE LANDING TABLE below: much of Parts II and XII
     is in p0 sec:power in the paper's own voice, with its own bibliography, and P3 prop:twoalpha
     carries the 2-alpha. What remains unlanded is named there, register by register.
     Every ⊢ is receipted. -->

> # ⛔ THE GRADIENT APPLIES · AND SO DOES THE WEIGHT RULE
> **Three registers, kept apart on purpose, because the whole value of this document is that they
> do not blur:**
> **⊢ PROVED** — an identity, a definition, or a verified computation, with its receipt named.
> **≈ RHYME** — recorded *as* a rhyme, with what would promote it. **Asserted nowhere.**
> **? NAMED, UNRUN** — a well-posed question nobody has computed. *Not* "no mechanism": a task.
>
> **And the rule c40 paid for and this session re-learned twice:** *when Daryl offers something
> provisionally, hold it at that weight and **work** it. Do not inflate it into a claim and then
> correct the claim.* Several entries below are **his, at his weight**, and are marked so.

# THE FIGURE PROGRAMME — THEOREM LEDGER (r1108)

## ⌗ THE LANDING TABLE — where each register stands against the corpus, checked r3524

> **▣ THIS SECTION IS THE `\ldg` GATHER FOR THIS LEDGER.** *Built by testing each register against the
> seventeen paper bodies as they stand, **not** against this file's own claims about where things went.
> Three dispositions, and a fourth for what was already there: **LANDED** · **POINTER OWED** (the
> substance is in a paper and only the `\ldg` marker is missing) · **SUBSTANCE OWED** (established here,
> verified, and absent from every paper) · **BOUNCED / OPEN** (recorded, not to be cited).*

### ⛔ THE HEADER IS FALSE, AND BY A LARGE MARGIN

*This file's banner says **"banked, not baked — nothing here has gone into a paper."*** ***It is wrong,
and not by the two landings a filename search finds.*** *p0's `sec:power` carries the Euclid protocol's
whole paying half in the paper's own voice, with its own bibliography: **Steiner's 1826 invariant**
(`\cite{Steiner1826}`), **Euclid III.36** (`\cite{Euclid}`), the **tangent-secant/null identity**, the
**secant-versus-tangent bound**, the **pole–polar**, the **radical axis**, the **nine-point circle**,
and — as the honest boundary — **Ptolemy, La Hire and Casey each holding on the projection and carrying
no height** (`\cite{CoxeterGreitzer1967,Coxeter1987b,Casey1866}`). **⊢9, ⊢10, ⊢38, ⊢40, ⊢42, ⊢44, ⊢45
and the ⚠ secant bound are all in the corpus.** *And P3 `prop:twoalpha` carries ⊢2/⊢7/⊢45's `2α`.*
⇒ ***A forward document contradicting itself is a finding, and this is the one check this file's
exemption does not cover. The banner is struck below.***

### ⌗ THE TABLE

| register | disposition | where it stands |
|---|---|---|
| **⊢9 · ⊢10 · ⊢38** power = `X₀²`, tangent is null | **LANDED** | p0 `sec:power`, `prop:tangentnull`, with `Steiner1826`/`Euclid` cited — **pointer owed only** |
| **⚠ the secant bound** (touch = null, cut ≠ null) | **LANDED** | p0 `sec:power` carries it as Euclid's own distinction |
| **⊢40 · ⊢41 · ⊢42 · ⊢43** pole–polar, polar triangle, radical axis, inversion | **LANDED** | p0 `sec:power`'s boundary paragraph — **pointer owed** |
| **⊢44 · ⊢58** Ptolemy, Casey, the one privileged circle | **LANDED** | p0, as the boundary where the run stopped paying — **pointer owed** |
| **⊢45** nine-point circle **is** the throat | **LANDED** | p0 (×2) and P3 `prop:twoalpha`; also P14 — **pointer owed** |
| **⊢2 · ⊢7** the hinge at `2α`, five equivalences | **LANDED** | P3 `prop:twoalpha`; the 2-and-3 split (substrate faces vs triangle faces) is p0's |
| **⊢4 · ⊢39** triple angle ⟹ Nariai | **LANDED** | P3, P5, P7, P13, P14, p0 all carry the triple angle |
| **⊢46 Feuerbach degenerates** | **PARTLY LANDED — narrowed r3527** | p0 carries the degeneration **as a fact** — the throat is the hinge triangle's incircle *and* its nine-point circle, and p0 says so at two sites and works the $D$-dependence of it (*"Euler's nine-point circle is a theorem about triangles and has no analogue at a square"*). **Owed is the theorem's name and one reading**: classically the nine-point circle is *tangent* to the incircle; here they are *identical* — two distinct classical objects forced onto each other by the one input, **the same species as the Nariai double root**. That last clause is the claim, and it is nowhere |
| **⊢14–⊢18 the Thales circle is the dial** | **SUBSTANCE OWED — confirmed r3527 against the corpus's own words** | *"the dial" is in P3 (×7) and P5 (×6) and is a **different object**: the sky-angle dial, the swing of the slicing plane about the hinge.* The Thales circle as the **locus of every cut's closest approach** — vertex on one arc, secant midpoint on the other, degenerating at Nariai into the two null rulings — is in no paper, and neither is "Thales" |
| **⊢59** P2's cycloid circle is a Thales circle | **SUBSTANCE OWED** | absent from P2 |
| **★★★ ⊢61** P7's central forcing **is** a tangency condition, and P1 supplies the tangency | **LANDED — CORRECTED r3527, see the note below** | ***P7 carries it in its own voice, with the P1 citation attached***: *"at no finite time~\cite{JanzenBHcausality}. The limiting orientation the collapse selects is therefore a null direction **grazing** the horizon---tangent to it, never transverse,"* followed by the full trichotomy — transverse crossing at $\Lambda G^2M^2/c^4<1/9$, tangency at a merged double root, no real horizon above. **The physics is landed and was landed all along.** What is owed is narrower and belongs elsewhere: **the third vocabulary** — that Euclid's *cuts / touches / misses* is the *same* trichotomy, verified by the vertical line's intersection count with P3's fundamental ellipse partitioning the family identically. That is a figure-side identification and its home is **p0 `sec:power`**, the Euclid section, not P7 |
| **⊢62** the tangency is designation-dependent, the geometry is not | ***LANDED r3562*** — *placed in p0 with its `\ldg` marker.* | rides with the third-vocabulary statement and is the bound that keeps it honest; both go to p0 `sec:power` |
| **⊢63** P4 and P6 have no geometric locus, **and that is load-bearing** | **SUBSTANCE OWED** | absent. It is the negative that says the figure programme has a boundary, which makes it worth more than most positives |
| **⊢50 · ⊢57** the excircles; `3α` is not a third scale | ***LANDED r3562*** — *placed in p0 with its `\ldg` marker.* | absent. Directly relevant to p0 `sec:ledger`'s one-scale claim, which currently argues the point without this |
| **⊢51 · ⊢54** `τ` is a light-path; `σ` is a relabelling | **SUBSTANCE OWED (τ) / LANDED (σ)** | `σ`-as-relabelling is throughout P2/P3/P5/P14/P16; **`τ` as a light-path is in no paper** |
| **⊢64–⊢70 · R5 the waves** | **OPEN** | the three routes were assessed r1760–r1762 and the gap is precise: **EM content as geometry ✔** (P8 carries RN–dS, `ρ=q²/8πr⁴`, the bend of the cut), **gravitational radiation as geometry ✔** (P11), **EM *radiation* as a bend ✘**. Still ✘ on today's corpus. Not citable |
| **⊢69 · ⊢70** no spin-2 on the figure; "not excluded" ≠ "open" | **LANDED (in substance)** | P11 carries the spin-2 count; P1 and P14 carry the not-excluded discipline. The figure-side derivations are not cited |
| **⚠37** face 3, imaginary `r`, **suspect by its author** *(the body carries this as a `⚠` warning, not a proved `⊢` — sigil corrected r3565)* | **BOUNCED — and still bounced** | the corpus's imaginaries are all *elsewhere*: `dη` purely imaginary on the lift (P7), `2πiα/3` along the clock (P3), `x₀` imaginary inside the waist (p0). **None of them is imaginary `r`.** c40's caution stands unrefuted after ~2400 revisions — record it as a settled bounce rather than an open question |
| **R1** hexagon → `A₂`/su(3) | **OPEN, re-scored** | the wall stands (`su(3) ⊄ so(5,1)`); what the object reaches is p0 way 6's **gauged/global split**, not the colour group. *"Nothing here reaches it" is false as written and was corrected r1758* |
| **R2 · R4** quark charges; the su(3) weight diagram | **DEAD, with computed cause** | recorded, never to be cited |
| **R3 → R3a/R3b · R3c** which `2+1` | **OPEN, and wider than when written** | a kill was written here and **retracted the same session as the cyanide face**. There are at least two `2+1`s — the **designation** one (`σ`'s own) and the **sign** one — they cut across each other, and they are different *kinds*: one a property of the geometry, one of the vantage. The row must first say **which** |
| **U2** `A = ∛2·ρ` | **RUN, exact, Nariai-only** | 2e-16, not vacuous; `lem:twoturnings` untouched — it denies identifying the two `ℤ₃` *symmetries*, this is a ratio of two *radii* |
| **U3** the full 3D, 24 hinges both sides | **OPEN — figure work, staged** | Daryl: *"which we will get to plotting eventually, in stages, not now"* |
| **U4 / Parts XI–XII** the Euclid question | **PARTLY LANDED** | the protocol's paying outputs are in p0 `sec:power`; the **protocol itself**, written down as a protocol rather than performed, is not. The tradition gap Daryl named — Wasan, and the Chinese/Arab/Indian sources — is still a real gap in what can be cited |

### ⛔⛔ THE TABLE ABOVE WAS BUILT WITH A FAULTY INSTRUMENT, AND ONE ROW MOVED — corrected r3527

***The first pass searched the LEDGER's vocabulary against the papers, and the papers use their own.***
*Re-run against corpus-native terms, the biggest entry on the owed list changed disposition:* **⊢61 is
landed**, *in P7's own voice and with the P1 citation attached — `grazing`, `tangent rather than
transverse`, `merged double root`, all present. It was scored `SUBSTANCE OWED — the largest debt in this
file` because `tangency condition` and `supplies the tangency` are the **ledger's** phrasings and appear
nowhere.* ⇒ ***Its remainder is real but small and has a different home**: the third vocabulary, the
Euclid identification, into p0's `sec:power` rather than into P7.*

⌗ **⊢46 and ⊢14–⊢18 also narrowed** *(p0 carries Feuerbach's degeneration as a fact but not its reading;
"the dial" in P3/P5 is a different object from the Thales locus).* ⌗ **And two ABSENTs survived a
homonym check rather than being taken at face value:** *⊢63's `no locus` and `geometric locus` **do**
occur in P7 — at the rate-switch and at the Hubble–Eddington radius, neither of them this claim — so
⊢63 stands owed for the right reason.*

⇒ ***THE STANDING RULE, paid for three times in one session:*** *filename search cannot see a landing;
an escaped underscore returns a clean zero; a word list is a claim about the searcher's vocabulary.*
***Every `ABSENT` in a landing table is now searched in the corpus's term, then homonym-checked at the
site, before it is written.***

### ⛔⛔⛔ ONE ROW OF THIS TABLE WAS FABRICATED, AND IT IS STRUCK — r3565

*The `r3524` gather listed* ***`⊢56` — "the horizon is real at its ends and fictional in its middle",
`SUBSTANCE OWED`, "absent, and it touches P1's own subject"***. ⛔ ***There is no `⊢56`.*** *The token
appeared in exactly one place in the corpus — that row — and the pre-gather ledger at `95559d5` carries
no `⊢56`, no "fictional" and no "real at its ends". The worked second-run block has `⊢49`, `⊢54`, `⊢55`,
`⊢57` and `⊢58` **and no `⊢56`**.*

⇒ ***A register was invented, given a plausible claim, routed to `P1`, and carried in the owed count for
eleven revisions.*** *It was caught only because landing it meant reading its worked statement first, and
there was none. **No gate would have found this** — the gates check that counts agree and that markers
resolve, not that a claim exists. Had it landed, the `\ldg` marker would have lent a fabricated sentence
the provenance of a bake that never made it.*

⌗ ***THE STANDING RULE:*** *a landing-table row is a **quotation**, not a summary; a register's worked
statement is read in the ledger body before it is landed, and a row with nothing behind it is struck.*
⌗ *An audit of all sixteen tables against their bodies found this the **only** fabricated row — `⊢37` was
flagged and is real (the body carries it as `⚠ 37`, a warning rather than a turnstile; sigil corrected
above), and `Q6r` is in the quadric body three times.*

### ⛔ WHAT THIS GATHER DID **NOT** FIND

***No row here was newly settled by a corpus result that post-dates it.*** *The candidate was the
four-dimensionality result, and `r2376+c54.11` had already re-read this file against it and re-derived
the `D`-dependent verdicts at general `D` — so the rows conditioned on `D=4` were already handled and
this pass adds nothing to them.* ⇒ ***The debt in this file is not stale conditioning. It is landing:
eleven registers established, verified and receipted here, and absent from every paper.***



> **⌗ r2376+c54.11 — RE-READ AGAINST THE FOUR-DIMENSIONALITY RESULT.** *This bake ran against a **four-dimensional cut**, when nothing distinguished *structural* from *true at $D=4$*. The probes whose verdicts turn on a $D=4$ number were **re-derived at general $D$**, not annotated — verdicts are **at the probes themselves** (⊢8 · ⊢30 · ⊢54+R1 · ⊢59/⊢62 · R3 · the retracted row). Method and full working: `THE_DIMENSION_DEEPENING_AUDIT.md`; receipt `P03_reach_probe_deepening`.*


**One input: `α`. And `α` is not a number — it is the gauge, the unit.** Everything below is `α`
re-read, or a question about `α` re-read. *(Daryl, r1108: "alpha alone sets the whole entirety of
everything and all we are doing here is doing a thorough job of dropping every forced anchor.")*

---

## PART I — THE CONSTRUCTION. Fourteen objects, one input, nothing chosen.

| # | statement | why | receipt |
|---|---|---|---|
| **⊢ 1** | **The Thales circle is the hole, translated onto its own edge.** Centre `E=(α,0)`, radius `\|EO\|=α`. | forced — it is the only circle "on the edge, through the centre" can mean | `alpha_alone.py` |
| **⊢ 2**  **⌗ r2376+c54.13 — THE FIVE EQUIVALENCES SPLIT TWO-AND-THREE, and the split is the finding.** *The hinge figure is a regular $(D{-}1)$-gon with the throat as incircle, vertex distance $\alpha/\cos(\pi/n)$.* ***SUBSTRATE'S (true at every $n$):*** *tangent length $=$ the hinge's height (both are $\sqrt{R^2-\alpha^2}$ — **this is `prop:tangentnull` and the power-of-a-point identity, the load-bearing member, and it is SAFE**); and midpoint tangency (any regular polygon's incircle).* ***TRIANGLE'S ($D=4$ only):*** *$R=2\alpha$; the $60^\circ$ subtense; and **the nine-point circle IS the throat** — sharper than a changed number, since **the nine-point circle is a triangle theorem**: at $n\ge4$ there is no such circle to identify, so the statement does not become false, **it ceases to exist**.* ⇒ *"one fact wearing five faces" is **two substrate faces and three triangle faces**, indistinguishable at $D=4$ because there they coincide.* `P03_step3_sweep` | **THE HINGE IS AT `2α`** — the Thales circle's far end. **Not placed. It is where the circle ends.** | forced | `alpha_alone.py` |
| **⊢ 3** | `sin(`half-angle at the hinge`) = α/2α = **½**` | forced by ⊢2 | `dial_360.py` |
| **⊢ 4** | **★ `sin w = ½` ⟹ `sin 3w = 3(½) − 4(½)³ = 1`. THE TANGENT FROM THE HINGE *IS* NARIAI.** | **the triple-angle identity.** Not a coincidence of two 30°s — an identity | `one_thirty.py` |
| **⊢ 5** | `sin 3w = 1 ⟺ 3w = 90 = 360/4`. **A sine peaks a quarter turn from its zero.** | forced | `one_thirty.py` |
| **⊢ 6** | `X₀(hinge) = √3 α` | forced by `−X₀²+\|X\|² = α²` | `alpha_alone.py` |
| **⊢ 7** | **The hinge triangle's sides are tangent to the hole at their midpoints — and that IS `⊢2`.** An equilateral triangle's inradius is `R/2`; tangency ⟺ `R = 2α`. | **the 2:1, the 60°, the tangency and the hinge's placement are ONE fact** | `alpha_alone.py` |
| **⊢ 8** | **The 3 hinges, 120° apart** | forced — three roots of one cubic | (P3)  **⌗ r2376+c54.11 — THE 3 IS $D-1$.** *This is the only Part-I row whose receipt is a citation rather than a computation, and it is the row asserting the three-ness. In $D$ dimensions $2M=r_0^{D-3}-r_0^{D-1}$, and one slicing scale removes the residual harmonics **only at $D=4,5$** — so the fold is $D-1$ and exists at two dimensions.* `P03_dimension_collapse` |

## PART II — THE OPTICS IS THE CAUSAL STRUCTURE. *(c40's keystone, verified.)*

| # | statement | receipt |
|---|---|---|
| **⊢ 9** | **`power = X₀²`.** The optical power of any point w.r.t. the throat **is the square of its height in the embedding.** `\|X\|²−α²` is *both* the power (by definition) *and* `X₀²` (by the hyperboloid). | `power_of_a_point.py` |
| **⊢ 10** | **★★★ THE TANGENT FROM ANY POINT TO THE THROAT IS A NULL LINE OF THE EMBEDDING.** `tangent² = \|X\|²−α²` (Euclid) and `X₀² = \|X\|²−α²` (the hyperboloid) ⟹ (spatial length)² = (time drop)² ⟹ `ds² = 0`. **Verified at 600 random heights, max \|ds²\| = 5.7e-14.** | `power_is_null.py` |
| | **⟹ THE POWER OF A POINT AND THE NULL CONDITION ARE THE SAME EQUATION.** *"It's all optics" is not a metaphor: the sightlines that **touch** are light rays.* | |
| **⚠ bound** | **The figure is not all tangents.** The 45° chords, the pin, the triangle's edges are **secants**, and a secant is not null. **The sightlines that TOUCH are light rays; the ones that CUT are not.** *(And that is the sharper reading: the power theorem is about **secants**; the null condition is the degenerate case where the two points **merge** — and the tangent is exactly where the light cone is.)* | `power_is_null.py` |
| **⊢ 11** | **`60° = arctan(√3) = X₀(hinge)/α = L_tangent/α = √(power)/α`. Four statements, one number.** | `rulings_between_levels.py` |
| **⊢ 12** | **No ruling stays on a level — because a null line cannot stand still in time.** `dX₀=0` ⟹ `\|dX\|=0` ⟹ a point. | `rulings_between_levels.py` |
| **⊢ 13** | **THE DOUBLING IS THE COUPLING, NOT A MIRROR.** One null line runs `(−60°,BELOW) → (0°,equator) → (+60°,ABOVE)`, collinear to machine zero. **The 12 above and 12 below are the two ENDS of the rulings.** *You cannot draw the top alone: the lines that make it leave it.* | `rulings_between_levels.py` |

## PART III — THE THALES CIRCLE IS THE DIAL. *(Daryl's reading, verified.)*

| # | statement | receipt |
|---|---|---|
| **⊢ 14** | **The Thales circle is the locus of every cut's closest approach to the axis.** 400 sightlines, max error 2e-16. **Not a construction aid — the dial's track.** | `thales_is_the_dial.py` |
| **⊢ 15** | **Overcritical (`d>α`): the cut is a two-sheeted hyperbola and the Thales point is its VERTEX's overhead position**, at height `√(d²−α²)`. | `thales_is_the_dial.py` |
| **⊢ 16** | **★ At the dial's end (`d=2α`) the vertex IS THE HINGE**, at `√3α`. **The hinge is the vertex of the extreme overcritical cut.** | `thales_is_the_dial.py` |
| **⊢ 17** | **Undercritical (`d<α`): the Thales point is the SECANT'S MIDPOINT.** *One circle, one locus — vertex on one arc, midpoint on the other, **the same point wearing what the hole does to it.*** | `thales_is_the_dial.py` |
| **⊢ 18** | **★ NARIAI (`d=α`): the cut DEGENERATES into two straight lines, and they are NULL. THE CUT AT NARIAI *IS* THE TWO RULINGS.** | `thales_is_the_dial.py` |
| | **⟹ P3's trichotomy IS the conic trichotomy. The dial is the plane, turning.** MISS → two-sheeted hyperbola · TOUCH → two null rulings · CUT → two horizons. | |
| **⊢ 19** | **The hole subtends exactly `±30°` at the hinge** — `arcsin(½)`, forced by ⊢2. | `dial_360.py` |
| **⊢ 20** | **The sky angle is MAXIMAL at the tangent, and the maximum is 30°.** Past `β=60°` the ray goes to the far side and it comes back down. **The sky cannot see past its own tangent — that IS the horizon.** *The hole's whole 360° is crammed into 60° of the hinge's sky.* | `four_vantages.py` |
| **⊢ 21** | **The hole's angle and the hinge's sky angle run OPPOSITE, and it is structural, not a sign choice:** `ψ` is measured *from the hinge toward the hole*; `β` at *the hole's centre*. **The two frames face each other.** | `four_vantages.py` |

## PART IV — THE ISOMETRY. *(Daryl's conjecture, now proved.)*

| # | statement | receipt |
|---|---|---|
| **⊢ 22** | **The physics depends only on `(θ − χ)`.** A plane through the hinge at azimuth `χ` has `d = 2α cos(θ−χ)`, and **`d` alone fixes the regime.** ⟹ **The hinge's azimuth and the dial are not two coordinates. They are one.** | `the_isometry.py` |
| **⊢ 23** | **★★ A HINGE TRANSPOSE BY `δ` *IS* A DIAL TURN BY `−δ`** — verified exact across 300 random triples — **and the map realising it is the azimuthal rotation, an isometry of the substrate** (`SO(2)` in the spatial block, inside `SO(5,1)`). **DARYL'S ISOMETRY EXISTS.** | `the_isometry.py` |
| **⊢ 24** | **`φ_Schwarzschild = χ + 180`.** Four observers, four zeros, **90° apart, one hole** — right→180, top→270, left→0, bottom→90, exactly as called. | `four_vantages.py` |
| | **⟹ THE FOUR HINGES ARE FOUR OBSERVERS OF ONE HOLE, AND EACH CALLS A DIFFERENT DIRECTION "ZERO." The dial's four quadrants are not four regions of one map — they are the four vantages' disagreement about where zero is.** *The corpus's perspectival thesis, drawn.* | |

## PART V — WHY FOUR. *(Daryl's answer; the last principle removed.)*

| # | statement | receipt |
|---|---|---|
| **⊢ 25** | **The dial is a RAY, not a line: 360°, not 180°.** The extra 180° **is the side** — which a line does not have. | `dial_360.py` |
| **⊢ 26** | **★ The physics is invariant under `{front/back}` and under `{mirror}`, and under NO quarter.** So the dial's 360 quotients by `ℤ₂×ℤ₂` ⟹ **THE FUNDAMENTAL DOMAIN IS EXACTLY 90°.** | `why_not_eight.py` |
| **⊢ 27** | **★★ AND THAT IS THE DOUBLE DUTY:** `ψ → ψ+180` is **the same line with its rays swapped** — the plane extends infinitely **front and back**, to the **SdS region** and the **FLRW region behind the hinge**. **One angle, two regions, one description.** *Take the double duty away and the domain would be 180.* | `why_not_eight.py` |
| **⊢ 28** | **`0–90` is the whole story, once:** the hinge's own vertex (`\|d\|=2α`) → **Nariai** (`\|d\|=α`, the two null rulings) → **Schwarzschild** (`\|d\|=0`). Every regime, exactly once. The other 270° is those cuts re-labelled by which ray is "front" and which side you stand on. | `why_not_eight.py` |
| **⊢ 29** | **★★ `n = 4` IS THE STAMP WHOSE STEP *IS* THE DIAL'S PERIOD. THERE IS EXACTLY ONE.** `n=8`'s 45° generator **is not a dial operation** — only its square is. **It is not an extra symmetry; it is a distinction without a difference.** And its extra `ℤ₂` is **not free-floating — it is already there, as the plane's own front/back.** ***Not arbitrary. REDUNDANT.*** | `why_not_eight.py` |
| | **⟹ RULE 2 IS OUT OF THE CHAIN. No principle anywhere: every step is an identity, a definition, or a verified computation.** *(Superseding `why_four.py`'s minimality step, which said so itself and was the chain's weakest link.)* | |
| **⊢ 30** | **★★★ `12 = 3 × 4`, BOTH FACTORS FORCED, BY ONE ROUTE.** `hinge at 2α` ⟹ `sin w = ½` ⟹ `sin 3w = 1` ⟹ `3w = 360/4` ⟹ **`w = 360/(3×4) = 30`.** | `one_thirty.py`  **⌗ r2376+c54.11.** *The 4 is forced here; the 3 is **inherited** from the triple angle.* ⇒ $12=(D-1)\times D$ **at $D=4$**. **⌗ And it bears on this ledger's one RETRACTION** — `thirty_degrees.py`'s "rhymes on its 3, coincides on its 4", struck for *testing the figure instead of asking the hole*. **The retraction was right and stays right.** What is new is that the conclusion it foreclosed — that the 3 and the 4 are related — **is reachable by the route the retraction demanded**, from the hole's own $2M=r_0^{D-3}-r_0^{D-1}$. **Not a reversal.** `P03_dimension_collapse` |
| **⊢ 31** | **★★★ THE TWO 30° ARE ONE 30°.** The kaleidoscope's `360/(3×4)`, P3's Nariai `sin 3w = 1`, and the hole's half-angle `arcsin(½)` are **one number by one route**. **And the two 4s are one 4:** the dial's `{axis, perpendicular}` **is** `{the sine's zero, the sine's peak}`. | `one_thirty.py` |
| **⛔ RETRACTED** | `thirty_degrees.py`'s verdict — *"it rhymes on its 3 and **coincides on its 4**"* — **is FALSE.** Its computation stands. **It asked "which `n` closes the stamp?" instead of "what does the hinge's own sine do?" — it tested the figure and never asked the hole.** | |

## PART VI — THE PLATE: `ℂ_r × ℂ_τ̃`. *(c40's; recorded at its own weight.)*

| # | statement | status |
|---|---|---|
| **⊢ 32** | **The duality is exact: each face is ALL PURPLE under the rule of the variable it pins.** *(Daryl predicted it before it was computed; c40 verified.)* | receipted, r1081 |
| **⊢ 33**  **⌗ r2376+c54.13 — THE SHEET COUNT IS $D-1$, THE SAME FOLD.** *The bead law comes from $(\dd r/\dd\tilde\tau)^2=2M/r^{D-3}$, giving $r\sim\tilde\tau^{2/(D-1)}$ — a $(D{-}1)$-sheeted root. So this row's $\mathbb{Z}_3$ is a $\mathbb{Z}_{D-1}$.* **⚠ AND IT DOES NOT WELD THE TWO THREES.** *`lem:twoturnings` proves no affine change of variable identifies the horizon-root set with the turnaround set, and L8.1-e proves the two $\mathbb{Z}_3$'s distinct. **Both proofs stand.** What is new is **why they agree in count**: both are $D-1$. **Two distinct objects with a common cause is not one object** — and that is the counting trap read correctly rather than sprung.* `P03_step3_sweep` | **★ THE BREAK — and it is the only one:** *"the three-foldness **is** the cube root `r³ = 2Mα²sinh²` — **it belongs to `r`, not `τ̃`.** `τ̃` has no hexagon, only a **strip** of width `2πα/3`. **Exact in colour, broken in structure.** That break is the **1/3–2/3**, and it is **THE ONLY THING DISTINGUISHING `r` FROM `τ̃` AT ALL.**"* | receipted |
| **⊢ 34** | **What is a ray on one map is a whole face on the other.** | receipted, r1082 |
| **⊢ 35** | **Face 1's "6 branches = 4 beads + 2 mergers": the mergers ARE the `r>0` tracks.** Four beads pair by `Re τ̃` sign; each pair continues onto the same track, **arriving with opposite colours — which is why the tracks are purple. *Purple is a count, not a species.*** | c40, r1107 |
| **⊢ 36** | **Face 4 (the lift) is BOUNDED and CLOSES:** `\|r\| ≤ A`, periodic — because `sin` is. | receipted |
| **⚠ 37** | **Face 3 (`r` imaginary) is SUSPECT — by its author.** c40: *"`r` is the areal radius. **I do not think imaginary `r` has a physical reading at all.** Face 3 may be a pure artifact of the continuation. Treat it as suspect until someone shows it means something."* **Unwritten anywhere until r1107.** | held |

---

# PART VII — WHAT THE FIGURES *ARE*. *(Daryl's reading, r1108, at his weight.)*

- **P3 is the triptych's top-left.** The overhead: the hole, the Thales circle, the hinge triangle, the tangencies, the null rulings. **c40: *"You've just drawn the central figure for P3, and it isn't close."*** And with ⊢10, **its lines are light rays.**
- **★ THE WHOLE THING IS THE BOTTOM-RIGHT — with the lines extended** far enough to see *"all the perfect squares and equilateral triangles and circles on the page, the whole symmetry group as one kaleidoscope."*
- **★★ AND THE READING THAT REFRAMES THE PROGRAMME (Daryl, r1108, provisional and recorded as his):**
  > *"After we turned P13 into the synthesis paper it always was supposed to be, now we are finally coming to realise that **p0/17 is actually an awful lot more than we ever thought IT was going to be — because the kaleidoscope is a frickin picture of its perfectly symmetric symmetry-breaking structure.**"*
  **This is not idle.** p0/17's own thesis is *"physics as broken-symmetry shadows"* and *"three nested registers of maximal symmetry."* **The kaleidoscope is that, drawn — and every number in it is `α`.** *If it holds, p0/17 acquires a figure that is not an illustration of its argument but an instance of it.*
- **⚠ And even the kaleidoscope is a FLAT PROJECTION.** The full symmetry group needs 3D: every line on its actual null ruling, the hyperbolas, **the full suite of 24 hinges on both sides of the hyperboloid.** *(Daryl: "which we will get to plotting eventually, in stages, not now.")*
- **⟹ THE WORK IS p17'S BABY: a suite of figures that tie everything together.**

# PART VIII — DARYL'S CONJECTURE, NOW A RESULT IN P14 — the figure spec

**Two labels on ONE geometric structure.** Take the PLATE's **face 1** (`r` real, `τ̃` complex — the six branches), and:
1. **Plot only the RED curve at `r<0` and only the BLUE half of the purple at `r>0`** → label it **THE COSMOGENETIC BEAD.**
2. **Plot BOTH red and blue at `r<0` and the purple at `r>0`** → label **THE SAME SHAPE** *"pair production / annihilation."*

> **The point is that it is the same geometric structure, twice.** *(c40's rhyme board, r1083: "the `r=0` crossing is **one conjugation event**, and the cosmogenesis, pair production and annihilation are **three walks on it**. Not three things that resemble each other — **one object, read at three scales.**")*
>
> **And its weight is now fixed, not free** — the A3 closure narrowed it and c40 re-scored it: **PROMOTED and NARROWED.** Not *"the vertex is pair production"* but ***"the geometry carries the vertex's KINEMATIC face (FS/CPT) and not its CHARGE face."*** **Which is exactly P13 `prop:closure`'s operator/species bound, reached independently from the figure side.** *The figure must be captioned at that weight and no other.*

# PART IX — THE RHYMES. Recorded as rhymes. Asserted nowhere.

| ≈ | the rhyme | status | what would promote it |
|---|---|---|---|
| **R1** | the hexagon → `A₂` / `su(3)` — **colour** | **RE-SCORED r1758.** *The wall stands — `su(3) ⊄ so(5,1)`, colour is not a realised isometry.* **But the same $A_2$ residue now carries a Standard-Model structural fact in p0 way 6: $\mathrm{Aut}(A_2)=S_3\times\mathbb{Z}_2$ factorises because *on* and *tangent* are independent, and a ruling being a line of the substrate makes chirality GAUGED while a root labelling a different cut makes flavour GLOBAL.** *So the object is load-bearing; what it reaches is the gauged/global split, not the colour group.* | **what would promote R1 proper:** `rem:a2-distinct`'s open question — whether the geometric $A_2$ and su(3)'s coincide **structurally** (r1664). *"Nothing here reaches it" is no longer true as written.*  **⌗ r2376+c54.11 — IT FACTORISES AT ONE DIMENSION.** *$\mathrm{Aut}(A_2)=S_3\times\mathbb{Z}_2$ is stated here as two facts that happen to factorise, with the gauged/global split falling out. **The $S_3$ needs the fold ($D=4,5$); the $\mathbb{Z}_2$ needs $2M$ odd in the signed offset ($D$ even). At $D=5$: a four-fold and no parity. From $D=6$: neither.*** ⇒ *the Standard Model's own arrangement — gauged chirality against global flavour — is available at **one** dimension.* `P14_dimension_from_flavour` |
| **R2** | 1/3–2/3 → **quark charges** | **⛔ DEAD.** *c40 wrote the killing condition itself: "needs a reason the wrap FRACTION is a CHARGE." A3: the charge sign closes **from the field**.* **Blind by result, not by ignorance.** | — |
| **R3**  **⌗ r2376+c54.13 — ⛔ RETRACTED THE SAME SESSION IT WAS WRITTEN (Daryl caught it). A KILL WAS WRITTEN HERE AND IT WAS THE CYANIDE FACE.** *What was claimed: that the geometric $2{+}1$ is a split by **species** while $uud$ is a split by **flavour**, so the mapping is the wrong kind and the row dies.* ***The argument is a straw man, and P14 says so in one sentence:*** **"the $2{+}1$ of a designated root against the ellipse pair is the diagonal designation $\sigma$ and the sign $2{+}1$ … the reflection through the origin."** ***There are at least TWO $2{+}1$s in this geometry and this row names neither.*** *I computed the **sign** one, showed it is species-graded, and closed a question whose other candidate — **the designation $2{+}1$, $\sigma$'s own** — is exactly a flavour-side structure. And P14's same paragraph records that the locus **does carry a flavour-breaking structure**: "the slicing singles one root against the ellipse pair", supplied with the triplet in P13.* **So the kill foreclosed an open investigation by chaining provisional pieces into an absolute — the failure this ledger's own r1110 audit names and the taxonomy calls face 24.** ***⇒ R3 REMAINS OPEN, and wider than before: it must first say WHICH $2{+}1$.*** **What survives from the pass, and it is real:** *(i) the unsourced reason is still **withdrawn** — §0 makes $\mathrm{sign}(r)$ species, so the split is not a real-slice artefact; (ii) the sign $2{+}1$ tracks the parity of $D$ exactly, i.e. it is the mass-parity in another register. **Neither licenses a verdict on the row.*** `P03_step3_refused`  **⌗ r2376+c54.17 — THE OBJECT IS NAMED, AND IT IS TWO OBJECTS.** *Computed along the dial: the designation $2{+}1$ $\{$designated root$\}|\{$ellipse pair$\}$ and the sign $2{+}1$ $\{+\}|\{-\}$ **cut across each other** — the ellipse pair generally holds one positive and one negative root, so **the designated root is never simply the odd one by sign** (they agree at 1 of 8 sampled positions, and that one is past the throat-tangent). **And they are different KINDS**: hold one geometry and cycle the designation through all three roots and **the sign split does not move** — so one is a property of the **geometry** and the other of the **VANTAGE**, which is the corpus's own geometry/vantage division appearing **inside a single triple**. At the degenerate member they **meet** ($r_0=2/\sqrt3$: the lone root is both), which is "one geometry under two designations" read a second way. ⇒ **this row is now `R3a` (designation) and `R3b` (sign), and each must be asked separately** — with a third question, **does $uud$ have any geometric counterpart at all**, which is what the row was really reaching for.* `P03_two_two_plus_ones` 
| **R3c** | **the causal trichotomy of the six hinge-ends, and the $2{+}1$ it carries** | **⌗ NEW r2376+c54.18; ⌗ OBJECT CORRECTED r2376+c54.19 (Daryl), and the correction is the substance.** ***The hinge is the LINE the door swings on — three of them, each TIMELIKE and each PIERCING the substrate once per horn. The six are $3\times2$, not six.*** *(And that is why there is one dial: planes through a **line** form a one-parameter pencil.)* **On the right object the reading is a complete trichotomy:** *for ends at stations $a,b$ on horns $\epsilon,\epsilon'$, $X\cdot Y=\alpha^2(-3\epsilon\epsilon'+4\cos(\theta_a-\theta_b))$, and all fifteen pairs classify — **timelike $\iff$ same hinge** (3), **spacelike $\iff$ same horn** (6), **null $\iff$ neither** (6). So Daryl's statement holds with its reason in one line, and* ***the pair the null binding "excludes" is the hinge itself*** *(two ends of one hinge cannot be null-separated — it is timelike), which c54.18 had filed as a new object. The chords project **two-to-one** onto the three sides — ⊢2's midpoint tangency is a statement about the **shadow of a pair**.* ⇒ ***from any end, the causal reach splits the three HINGES $\{$mine, timelike$\}\mid\{$two others, null$\}$.*** **⚠ AND ITS ASSIGNMENT IS RELATIVE, PROVED:** *the symmetry group ($D_3\times\mathbb{Z}_2$, order 12) is **transitive on the six ends** and has **one orbit per causal class**, so **causal character is a complete invariant of a pair** and nothing singles any end out.* **c54.18's "honest gap" is withdrawn — it is not a gap but the structure** *(Daryl: "just as well-posed as $udd$ as it is $uud$ … no geometric distinction in an absolute sense"; species is relative in the same way).* **⚠ And c54.18's claim that this is a THIRD distinct $2{+}1$ is withdrawn to a question** — *its split has the designation split's shape; see L-58.* | `P03_hexagon_null_triple` | ✔✔ | exact in sympy; trichotomy, orbit counts and tangency all verified | **`PO-1d`** — *and the charge lead L-59* | new r2376+c54.18, corrected c54.19 |
| **R4** | the su(3) **weight diagram** | **⛔ DEAD, with a computed cause** — the 2:1 is forced by tangency, so the rotation and the ratio are one fact; and the union is a **hexagram**, not a hexagon | — |
| **★ R5** | **NEW (Daryl, r1108), never said before: THE RIGHT-HAND IMAGES ARE WAVES.** *"The purple, and the red-blue-purple ones, are waves. I think honestly there is a geometric description of electromagnetic radiation in that. And between the purple and the red-blue-purple I think you might even have some connection to wave-particle duality. And I know that's a rhyme."* | **OPEN. Recorded at his weight, unassessed — and this is a live conduit, not a marked exit.** **⌖ THREE ROUTES ASSESSED r1760–r1762 (still unassessed as a rhyme; only the routes were checked):** *(c) the chirality criterion **cannot eat the branches** — it needs two polarization components, they have one, which is its own achiral case · (b) **survives**, P11's "matter and radiation are the bend" being the anchor rather than a conflation · (a) **holds, and P8 already has EM stress-energy as a bend of the cut** — RN–dS, $\rho=q^{2}/8\pi r^{4}$, *"not an independent posit fed into the geometry; it is the curvature of the cut."* **So the gap is precise: EM content as geometry ✔, gravitational radiation as geometry ✔, EM RADIATION as a bend ✘.** With P13's rank count as the standing obstacle to a gauge route: **SM rank 4, $SO(6)$ rank 3, the weak $SU(2)$ does not fit.** | **★ HOW TO GET IN.** What we currently know permits: the branches are **worldline families** with a real oscillation on face 4 (`\|r\| ≤ A`, periodic, closes — ⊢36), and that periodicity is `sin`, not an assumption. What it does **not yet** permit is calling that a **field**. **The routes in, all three walkable:** *(a)* **the thing P15 actually says** is that radiation is excluded **from L1** — the stacking rate — *"because content is read off the clock the geometry sets, never summed into it."* **That is a statement about L1, and it leaves L2 and the field level entirely open.** A geometric EM living at **L2** would not contradict it; nobody has asked what P15's exclusion does or does not reach. *(b)* **P11 already has a wave with a geometric source:** the Gowdy wave's energy and momentum are carried **entirely by the shear of the leaf** — matter-as-bend, with the bend being TT shear. **If a wave can be a bend, ask what bend these branches are.** *(c)* **the polarization criterion is already built** (P11: chirality = the turning of the polarization plane, the `ℤ₂` sign of `d/du arg(h₊+ih_×)`) — **so there is an existing instrument that eats a wave and returns a chirality**, and the purple/red-blue split is a chirality claim. **Point it at the branches.** ⚠ *And the standing caution is c40's, not a bound on the reach:* the figures are not evidence — *"the prettiest thing I drew was the least evidential."* **That governs what a picture may be cited FOR; it does not govern where to look.** |

# PART X — NAMED, UNRUN

| ? | the question |
|---|---|
| **U1** | **RUN (r1110, `U1_twelve_by_two.py`).** ⊢49 below. |
| **U2** | **`A = ∛2·ρ` as a metric bridge** between the two turnings. `lem:twoturnings` denies a canonical identification of the two `ℤ₃` **symmetries**; it says nothing against a **ratio between two radii**. |
| **U3** | **The full 3D plot** — every line on its null ruling, the hyperbolas, **24 hinges both sides.** *Staged, not now.* |
| **★ U4** | **THE EUCLID QUESTION** — see Part XI. |

# PART XI — ★★★ THE EUCLID QUESTION. *(Daryl, r1108. A programme, not a claim.)*

> *"I think we might be just now discovering the thing that Euclid always wanted to figure out, and
> only realising it now because we've allowed ourselves to merge Euclidean and Riemannian geometry,
> and we know what Lorentzian geometry is. **What would Euclid have done with this structure had he
> been able to investigate it as thoroughly as he could have; what theorems would he have proven; how
> would Euclid have described this entire geometric structure in book-form, laying out all its
> properties totally rigorously and comprehensively, had he expanded beyond his rigid Euclidean
> geometry and properly accounted for geometric features that come from embedding a hyperboloid in a
> square Lorentzian space?** … a pure geometric thing of incredible beauty to be fully exhaustively
> understood from a pure geometric standpoint with every theorem that can be imagined already proven
> and documented. … recognising the tradition this really sits on top of, going all the way back to
> the Pythagoreans and Thales as its origins, and probably a whole bunch of Chinese and Arab and
> Indian stuff I don't even know anything about."*

**Why this is not a flourish, and why it belongs in the ledger rather than a wish-list.** **Look at what
is already in Part II.** ⊢10 is *Euclid's tangent–secant theorem* and *the null condition* **turning out
to be the same equation.** Its proof is two lines and both are classical: `tangent² = |X|²−α²` is
Euclid III; `X₀² = |X|²−α²` is the hyperboloid. **The only modern ingredient is the minus sign in the
metric.** And ⊢4 is the triple-angle identity doing the work that "Nariai" does in the physics. **The
programme is not analogy — it is that these theorems already exist and were proved long before the
signature did. ⚠ **CORRECTED r1110 (`euclid8_casey.py`, and a source search):** *"2300 years ago"* was
wrong, and by two centuries. Of the protocol's actual outputs, **⊢38** (tangent–secant) and **⊢40**
(pole–polar) are ancient — and **⊢44** (Ptolemy), also ancient, is the one that **FAILED**. Everything
else that paid is **19th-century**: the radical axis is **Steiner, 1826**; inversion is 19th-c
(Steiner/Plücker/Möbius); the nine-point circle is **Brianchon–Poncelet 1821 / Feuerbach 1822** (Euler
1765 for six of the points; *named* by Terquem 1842); Casey is **1866/1881** — and, the tradition gap
Daryl named, in **Wasan ~30 years earlier (Chochu Siraisi, 1830)**. **And there are TWO modern
ingredients, not one.** ⊢9's *power* is **Steiner's 1826 invariant, not Euclid's relation**: Euclid III
gives a relation among segments; **Steiner shifted the focus from the chords to the POINT**, defining
`|d²−r²|` as a number belonging to it. ⊢9 says *"the power of any point is the square of its height"* —
that is Steiner's framing. **So: the minus sign, AND power-as-a-property-of-a-point.** *(And that
second move — from a relation among segments to an invariant of the point — is the corpus's own: it is
the same move as reading `r₀` as the offset that IS the mass.)***

**The honest shape of the deliverable — unseated. to size:** *"A book, or another large paper,
or maybe just a few pages extra in p0/17 to capture the MAIN theorems fully."* **The ledger's Parts
I–V are the first draft of exactly that** — fourteen objects from one input, every step an identity or a
definition.

**★ AND THE METHOD IS ALREADY IN HAND, which is why this is a programme and not a wish (Daryl, r1108):**
> *"Euclid had all the tools he needed to be able to describe this thing **extrinsically**, because it's
> a maximally symmetric substrate and can be embedded very effectively in `M³` so that meaningful
> hypersurfaces can be visualised. And we know how to go from **Euclidean embeddings with signature
> changes** to **intrinsic descriptions along Lorentzian surfaces**. So it's like **a translation of
> existing theorems** sort of thing that we can do all over the damn place."*

**That is the engine, stated: a TRANSLATION PROTOCOL.** Take a classical theorem about circles, chords,
tangents and triangles in the plane; read it on the throat's overhead projection; carry it back up the
embedding; **and the signature does the rest.** `⊢10` is the protocol's first output and it is exactly
this shape — *Euclid III's tangent–secant theorem* and *the null condition* are the same equation, and
the only modern ingredient is the minus sign. `⊢4` is the second — the triple-angle identity doing
what "Nariai" does in the physics. **The programme is: run the protocol systematically. Every classical
result about a circle and an external point is a candidate, and each one lands somewhere in the
substrate's causal structure.**

**What is not yet done, and each is a way in rather than a fence:** the tradition properly acknowledged
(Thales and the Pythagoreans at the root; and — Daryl's own note — *"probably a whole bunch of Chinese
and Arab and Indian stuff I don't even know anything about"*, which is a real gap in what we can
presently cite and not a decorative one); the protocol written down as a protocol rather than performed
twice; and the obvious next candidates simply **run** — the radical axis, the inversive geometry of the
throat, the pole–polar relation *(which is where the hinge-to-pole line and the pin already live)*,
Ptolemy, the nine-point circle on the hinge triangle. **Each of those is a Euclidean theorem with a
signature waiting to be put in it.**

---

# PART XII — ★★★ THE EUCLID PROTOCOL, RUN. *(r1110. All five candidates Part XI named.)*

**The protocol** (Daryl, r1108): take a classical theorem about circles, chords, tangents and
triangles; read it on the throat's overhead; carry it back up the embedding; **the signature does
the rest.** Part XI named five candidates and left them unrun. They are run. Every receipt names its
falsifier first and every falsifier bit.

| # | classical theorem | what it returns here | receipt |
|---|---|---|---|
| **⊢ 38** | **Euclid III, tangent–secant** | **the null condition** — power = `X₀²`, so tangent length = height. *The only modern ingredient is the minus sign.* | `power_is_null.py` |
| **⊢ 39** | **the triple-angle identity** | **Nariai** — `sin w = ½ ⟹ sin 3w = 1`, of its own accord | `one_thirty.py` |
| **⊢ 40** | **pole & polar** (Apollonius) | **★ THE POLAR OF A VANTAGE IS ITS HORIZON, drawn on the throat.** The chord of contact joins the two points where the hinge's light cone grazes the throat. And `inv(hinge)` **is** the foot of its own polar: `2α · α/2 = α²`. | `euclid3_pole_polar.py` |
| **⊢ 41** | **★ NEW — the polar triangle** | **The three hinges' polars form an equilateral triangle INSCRIBED in the throat** (vertices on `\|v\|=α` exactly), against the hinge triangle circumscribed. Their sides **are** the three horizons. *Only at 2α.* | `euclid3_pole_polar.py` |
| **⊢ 42** | **the radical axis** | **that same horizon, independently** — `x = α/2` reached with no tangent mentioned. Reason: Thales' own theorem makes the Thales circle the **locus of tangency**, so *chord of contact* = *common chord*. | `euclid4_radical_axis.py` |
| **⊢ 43** | **inversion in the throat** | **`\|P\|\|P'\| = α² ⟺ L² = \|P\|²−α² ⟺ X₀² = \|P\|²−α²` — one equation, three classical names.** The **fixed circle of the inversion is the WAIST.** And **the Thales circle inverts to the hinge's horizon** — third route to `α/2`: *the circle that places a vantage and the line that bounds what it sees are one object seen from the two sides of the hole.* | `euclid5_inversion.py` |
| **⊢ 44** | **Ptolemy** *(on the Thales circle)* | **★ the horizon chord = the tangent = the height = `√3α`** — extends ⊢11 by a new member; cands 3–5 found *where* the horizon is, none measured *how long*. **And one number wears all three signatures:** `√3α` is the hinge's **drop** (timelike), its ray's **reach** (null, `ds²=0`), its horizon's **width** (spacelike, `+3α²`). *A light cone in Ptolemy's own quantities.* | `euclid6_ptolemy.py` |
| **⊢ 45** | **the nine-point circle** | **★★ THE NINE-POINT CIRCLE OF THE HINGE TRIANGLE *IS* THE THROAT.** Centre `O`, radius `α`. **A sixth independent route to 2α, using no tangent, no power, no sine, no inversion — just Euler's `R/2`.** The nine points collapse to **six at 60°**: the three tangencies interleaved with the three Euler (hinge-to-centre) midpoints. | `euclid7_nine_point.py` |
| **⊢ 46** | **Feuerbach** | **DEGENERATES.** The nine-point circle is *tangent* to the incircle classically; here they are **identical** — both are the throat. Same species as the Nariai double root: two distinct classical objects forced onto each other by the one input. | `euclid7_nine_point.py` |

### ⊢ 49 · **THE 12×2 IS A GRAPH, AND IT DOES NOT CLOSE THE WAY THE 12 DOES** *(`U1_twelve_by_two.py`, r1110 — c40's last named-unrun)*

The object is not 24 points. **12 hinges above + 12 below, joined by the rulings: a bipartite graph.**
Every hinge sits on **exactly two** rulings (one per family), so it is **2-regular** — 24 vertices, 24
edges — and a 2-regular graph is a disjoint union of cycles. *"Does the 12×2 close"* is therefore
exactly *"what are the cycles?"*

> **FOUR components, each a 6-cycle**, on azimuths `{0,120,240}`, `{30,150,270}`, `{60,180,300}`,
> `{90,210,330}`.

**And the arithmetic is one line, which is why c40's "different question" was right:**

> **The 12 closes because `gcd(3,4)=1`. The coupling splits because `gcd(4,12)=4`.**

The stamp's 4 is **coprime** to the triple's 3, so `3×4` sweeps all twelve as **one** orbit. The
coupling's step is `120° = 4` slots of 30°, and **4 is not coprime to 12** — so it splits them into
four orbits of three. **Same 4, opposite role: the rotation's 4 is a GENERATOR, the coupling's 4 is a
DIVISOR.**

⚠ **The distinction the question turns on, checked not assumed:** a null **chord** between two hinges
is not a **ruling**. The above→below chord at 120° *is* null — but the coupling is about rulings, which
must lie **in** the surface. Verified at the midpoint. (Both survive here; the check is what makes the
answer mean anything.) All 24 rulings verified straight-and-null through (below → equator → above),
collinear to machine zero.

**⚠ BOUNDED** (face 35): quantifies over the **incidence structure**. Says nothing about what the four
cycles *mean* — no cut, no dial, no `f`, no `2M` appears in it.

### ⊢ 50 · **3α IS NOT A THIRD SCALE** *(`U5_excircles.py`, r1110)*

`r_ex = 3·r_in = 3·(R/2) = 3α` **because** `R = 2α`. The **3 is the equilateral triangle's own**
exradius/inradius ratio — verified exactly 3 at every `R`, so it tracks the triangle's symmetry and
not this construction. **3α = the gauge × the hinge's 2 × the triangle's 3.** *The ledger's headline —
one input, and the input is the gauge — survives by computation rather than by assertion, which was
the point of asking.*

**Feuerbach's excircle half HOLDS**, and it is a second independent confirmation of ⊢45: each excircle
is **externally tangent** to the throat, `|centre| = 4α = r_ex + r_throat`, touching at one point on
the line from `O` through the excentre. *If the nine-point circle were not the throat, this would
fail.* So Feuerbach here is **half degenerate and half not**: the incircle half collapses to identity
(both circles **are** the throat); the excircle half is an honest tangency.

⚠ **⊢45's own phrasing CORRECTED by this receipt:** euclid7 wrote the excircles *"sit at 3α with radius
3α."* The **radius** is 3α; the **centres are at 4α** (`= 2R`; the excentre of an equilateral triangle
is `−A+B+C`). I inferred the centre distance and never computed it — **face 35, on the hand that banked
it, one turn later.** The assert caught it, which is that face's own prescription: *put the verdict
inside the receipt, where it can fail.*

**★ NEW, and unasked:** the excentres are **real overhead points at `X₀ = √15 α`** — far above the
hinges at `√3 α`. Nothing has asked what sits up there.

### ⊢ 51 · **★★ τ IS A LIGHT-PATH** *(`U1c_the_light_path.py`, r1110)*

The groupoid's order-3 generator — the sky-angle periodicity, the relabelling of which root of the
triple is designated — is **two steps along a null ruling**. `A0 → B240 → A120`: same horn, azimuth
`+120`, which by ⊢23 (hinge transpose = dial turn) is a dial turn of `−120 = τ²`. Six steps `= τ³ =
id` — **the 6-cycle is τ's order, doubled by the horn.**

> **τ is not an algebraic relabelling. It is a closed path of light through both horns.**

**And the families must alternate.** Same family twice is an out-and-back along one null line. **The
cycle exists because the hyperboloid is doubly ruled.**

**⊢ 52 · ONE RULING-STEP IS NOT A GROUPOID MORPHISM.** It flips the horn — so it is neither `τ` nor
`R` (both horn-preserving); it carries a `T`. And it is a *half*-`τ`, which **cannot exist in ⟨τ⟩,
since τ has order 3 and 3 is odd.** **The ruling is finer than the groupoid**: the light cone sees a
half of the groupoid's own generator and the groupoid cannot express it. Only the square is a morphism.

**⊢ 53 · THE CYCLE'S SIX IS NOT `D₃`'s SIX** *(predicted before running; confirmed)*. The walk
generates **ℤ₆** — cyclic, an element of order 6. `D₃ = S₃` is non-abelian and has **no** element of
order 6. `ℤ₆ = ℤ₃ × ℤ₂` = (the triple) × (the horn); `S₃ = 3!` = the triple's *rearrangements*. **Same
number, different objects.** Checked by explicit order computation, not assumed.

### ⊢ 54 · **★★★ AND WHY: τ IS A MOTION, σ IS A RELABELLING** *(`U1d_is_sigma_a_light_path.py`, r1110)*

U1c's asymmetry — *the rulings give τ and not σ* — asked its successor: **is σ a light-path at
Nariai?** **The question is ill-posed, and the source says so.** P5 `prop:monodromy`, read at source:
σ **is** the analytic continuation of `2M` once around the branch point — **a loop in the complex
`2M`-plane.** Verified here by continuing the roots through 4000 steps: they return **transposed**
(permutation `[0,2,1]`).

> **τ is a light-path. σ is a monodromy. Not the same kind of object.** τ moves along real null lines
> of the real substrate — a walk you could take. σ moves `2M` around a branch point in the complex
> plane; `2M` is not a direction in the substrate, it is a **label on the family**. A ruling connects
> two points of one manifold; σ connects two **descriptions** of one geometry.

**And the reason is the corpus's own kind-distinction, reached from the figure side:** `τ` **is** a
substrate isometry (⊢23: hinge transpose = dial turn = azimuthal rotation in `SO(5,1)`), so the
rulings can walk it. `σ` **is not** — P5: *"the Weyl `S₃` is the deck/monodromy symmetry of the
solution space, **no isometry**."*

> **THE RULINGS REALISE EXACTLY THE PART OF `D₃` THAT IS A SUBSTRATE ISOMETRY, AND NOTHING ELSE.**

That is P5's **gauged/global** argument — the orientation `ℤ₂` *is* an isometry so chirality descends
gauged; the Weyl `S₃` is not so flavour is global — **arriving independently from the null-ruling
side.** Not a new claim: a second route to a standing one, which is what the engine is for.

⚠ **THE TRAP AVOIDED, logged because it is live:** at Nariai there are **two** rulings and **two**
colliding roots. **The count matches and the match means nothing.** The rulings are two *at* the
member where the roots are *one*; the roots are two only *off* it. Two 2s, different values of `2M`,
different spaces, one a motion and one a monodromy. **A count is not a map.**

*(Confirmed en route: σ's fixed point, the dial's tangency, and the cubic's branch point are ONE
locus — `r₀=α/√3`, `w=30`, `2M=2/(3√3)`, discriminant 0. Checked because the whole question was
whether the two objects meet at all.)*

### ⊢ 55 · **★★★ ⊢47 SHARPENED — the criterion is the QUANTITY, not the points** *(`U6_la_hire_lifted.py`, r1110)*

U6 ran its own named promotion — *carry polar-as-horizon into the embedding as a statement about null
hypersurfaces* — **and it fails, by the protocol's own law, which the failure then sharpens.**

**La Hire is the symmetry of the dot product.** `P on polar(Q) ⟺ P·Q = α² ⟺ Q on polar(P)`. The
classical theorem is *one bilinear form's symmetry*, dressed as a statement about two lines. **The
height `X₀` appears nowhere in it** — so there is no `dX₀` for the minus sign to act on, and the lifted
pairs come back **spacelike, 0 of 8 null** (computed).

> **⊢47 said: a theorem translates only if its points sit at different heights.**
> **⊢55 says: it is not about where the points sit. It is about whether the theorem's own QUANTITY is
> one the height can enter.**

- the **tangent length** *is* the height (`power = X₀²`) → **⊢38 translates**
- the **polar's location** is fixed by the power → **⊢40 translates**
- **La Hire's relation** is the dot product's symmetry — the height is not in it → **it does not**
- **Ptolemy's chords** sit on the waist (`dX₀ = 0`) → **⊢47's original form is a special case of this**

**The height-spanning criterion is the SYMPTOM; the quantity criterion is the CAUSE.**

### ⊢ 56 · **★★ THE HORIZON IS REAL AT ITS ENDS AND FICTIONAL IN ITS MIDDLE** *(same receipt)*

The hinge's polar has its foot at `α²/2α = α/2` — **inside the throat**, where `X₀` is **nan**: the
substrate has no point there. So the horizon-chord's **endpoints** are the two tangency points — on
the waist, `X₀ = 0`, real points where the light cone touches — and its **interior runs through the
hole, where the substrate is not.**

> **Only the two endpoints are points of the substrate. The chord joining them is a line in the
> OVERHEAD — a projection surface, not the manifold.**

**And that is exactly why La Hire cannot be lifted:** pole–polar duality is a statement about the
whole projective plane, **hole included**, and its reciprocity runs through the chord's interior — the
part that is not there. Most La Hire pairs cannot both be lifted at all, because a point on an external
vantage's polar is generally *inside* the throat. *(euclid5's inversion finding — the outside of the
waist maps to the inside, where the substrate is not — met from the other side.)*

### ⊢ 57 · **THE EXCIRCLE NAMES A POINT THE CONSTRUCTION ALREADY HAD, FROM THE OUTSIDE** *(`U7_excircles_and_the_dial.py`, r1110 — U5's named successor)*

**No `d` of its own.** Not a cut (a cut's overhead is a *line*; an excircle is a circle — fails by
type). Not a locus (the Thales circle is, max error 2e-16; the excircle has radius `3α` and misses
`O`). And both its distinguished distances — radius `3α`, centre `4α` — are **past the dial's whole
range**: `d = 2α cos(θ−χ)` gives `d ∈ [0, 2α]`. **`d = 2α` is not an arbitrary cap; it IS the hinge.**

**But not invisible.** Analytically, excircle-0 **intersects** hinge-1's and hinge-2's Thales circles
(centre-distance 3.6056, strictly between `|r₁−r₂|=2` and `r₁+r₂=4`) at **four real feet**; hinge-0's
own Thales circle misses it (5.0 > 4.0). **The excircle opposite a hinge is met by the *other two*
hinges' dials and not by its own.**

> **★ THE EXCIRCLE'S ONE TOUCH ON THE THROAT IS THE TANGENCY POINT OF THE SIDE IT IS OPPOSITE — AND
> THAT POINT IS A FOOT OF BOTH OTHER HINGES' DIALS.**

At `(−α, 0)` three routes coincide: **side BC** tangent to the throat at its own midpoint (⊢7); the
**excircle's** single throat contact (Feuerbach, U5); and a **foot** of both hinge-1's and hinge-2's
dials (`X·(H−X) = 0`, verified). **The excircle did not add a point — it named one already there**
(⊢45's third tangency at azimuth 180; `step1_all_points.py`'s rational `(−1,0)`) **from outside.**

⚠ **THE FALSE NEGATIVE, KEPT.** The first version swept 400 cuts per hinge, got a minimum
foot-to-excircle distance of **0.004**, and reported *"none"*. **That 0.004 was a genuine crossing the
sampling stepped over.** **SAMPLING CANNOT PROVE AN ABSENCE.** And the sharper half: **the assert
passed only because its threshold (1e-3) was looser than the discretisation** — it was testing the
resolution, not the geometry. **An assert whose threshold is looser than your sampling is not a test.**

⚠ **THE FLAVOUR-MATCH, NAMED BEFORE IT WAS TAKEN.** The excentres are real substrate points at
`X₀ = √15 α`, far above the hinges at `√3 α`. **A new surd on a real point is the exact shape of the
trap.** It is `|X| = 4α = 2R` read through the hyperboloid, and `R = 2α` is the hinge. Nothing has
shown it enters any dial, cut, `f`, or `2M`. **A surd on a real point is furniture until something
reads it.**

### ⊢ 58 · **★★★ THE LAW, FINAL FORM — THERE IS ONE PRIVILEGED CIRCLE** *(`euclid8_casey.py`, r1110)*

**Casey's theorem was selected by ⊢55 rather than by fame** — it is Ptolemy with the chords replaced by
**tangent lengths**, and tangent² = power. So the law predicted: *Ptolemy died, Casey pays.* **The
prediction FAILED, and the law is right one level down.**

**Casey's identity holds on the figure** — three excircles + an Euler point, all tangent to the throat,
cyclic order verified, difference 1e-14. **And it returns nothing about the signature.** Because `t²` is
a power **with respect to the four tangent circles**, and ⊢9 says **power w.r.t. THE THROAT** = `X₀²`.
The excircles are not substrate objects at all (⊢57: no `d`, past the dial's end). **A power with
respect to something that is not the waist is not a height.**

> **⊢47** (Ptolemy): a theorem translates only if its **points** sit at different heights.
> **⊢55** (La Hire): not the points — whether the theorem's **quantity** is one the height can enter.
> **⊢58** (Casey): not *any* power — **the power WITH RESPECT TO THE THROAT.**

> **`power = X₀²` IS NOT A FACT ABOUT POWERS. IT IS A FACT ABOUT ONE CIRCLE: THE WAIST OF THE
> SUBSTRATE.**

**★ AND THAT IS WHY THE PROTOCOL WORKS AT ALL — stated properly for the first time.** It is **not**
that Euclid's theorems are secretly Lorentzian. It is that **the throat IS the substrate's waist, and
the power w.r.t. the waist IS the height.** Every candidate that paid (⊢38, ⊢40, ⊢42, ⊢43) has **the
throat** as its circle; every one that failed (Ptolemy, La Hire, Casey) either has no power in it or
has a power w.r.t. something else.

> **THE PROTOCOL TRANSLATES EXACTLY WHAT IS BUILT FROM THE POWER OF A POINT WITH RESPECT TO THE
> THROAT. There is one privileged circle, and the protocol is blind to every other.**

**⚠ NOT COMPUTED, named rather than guessed — and ⊢58 PREDICTS IT NULL (r1759): its tangent lengths are bitangents between the throat's tangent objects, none of which is the waist, so by this very law the quantity is not a height and the protocol is blind to it. **Expect the identity to hold numerically and return nothing about the signature, as the circle-form did — which makes running it a cheap FALSIFICATION TEST of ⊢58 rather than a search: a signature result would refute "one privileged circle".** The row stays NOT COMPUTED because a prediction is not a computation.** Casey's form with a **line** among the four (the hinge
triangle's sides are tangent to the throat and would complete a configuration). The bitangent between a
line and a circle is not the same object as between two circles, and no sourced formula was in hand.
**Inventing one is the failure this corpus refuses. Unrun.**

# PART XIV — ★★★ THE FULL-CORPUS SWEEP (r1115–r1116). *The corpus is more geometric than the ledger knew.*

**Why it was run.** At r1115 I baked the material into five downstream papers and wrote of the other
ten: *"no locus the material bears on — adding to them would be decoration."* **I had not opened one of
them.** An absence declared from a search never run, with the restraint logged as a result. Daryl:
*"there may be fascinating connections, far deeper ones, in p1, p2, p4, p6 even."* **He was right about
every one.** Method fixed: **count the geometric vocabulary first, then ask a question that fits the
paper.**

| paper | nulls | tangents | circles | light cones | what the sweep found |
|---|---|---|---|---|---|
| **P1** | **98** | **14** | 4 | **11** | ⊢61 — **it supplies the tangency** |
| **P7** | **139** | **15** | **22** | — | ⊢60 — **its central forcing IS the trichotomy** |
| **P2** | — | — | — | — | ⊢59 — **its cycloid circle IS a Thales circle** |
| P4, P6 | 0 | 0 | 0 | 0 | **no locus — and that is what makes the others mean something** |

### ⊢ 59 · **P2's CYCLOID CIRCLE IS A THALES CIRCLE** *(`sweep1_p2_thales.py`)*
`r(z) = M(1+cos z)`, `y = M sin z` satisfies `(x−M)²+y² = M²` **identically** — centre `(M,0)`, radius
`M`, through `0` and `2M`. **The same construction as ⊢1's Thales circle** (`(α,0)`, radius `α`,
through `0` and `2α`), one scale apart. And **every point of it sees `[0,2M]` at a right angle** (max
|dot| 3e-16) — **Thales' theorem is why `r(z)` has its two critical points where it does.** Neither
paper says so: P2 calls it *"the rim of a rolling circle"*, P3 *"the hole translated onto its own
edge."*
**★ AND THE MISMATCH IS THE FINDING:** P2's diameter spans **the geometry's own two critical points**
(the horizon and `r=0`, which P2 *proves* are of identical analytic character); P3's spans **the
throat's centre and the HINGE** — a point of the geometry and an observer's pivot, **not** of identical
character. ⟹ **one construction, once on each side of the corpus's own geometry/vantage partition.**
⚠ **The identification is refuted by P3's own mass bound**, not by taste: `2M = 2α` needs `r₀−r₀³ = 1`,
and `|2M| ≤ 2/(3√3) ≈ 0.385α`. **No slicing makes P2's circle P3's** — they differ by ≥ 5.2×. *The mass
is bounded; the throat is not.*

### ⊢ 60 · **★★★ EUCLID III.36 IS A STATEMENT ABOUT WHERE THE LIGHT CONE IS** *(`sweep2_discriminant_is_interval.py`)*
For any sightline from an overhead point through the throat, `ds²(t) = t² − pow(P) = t² − t₊t₋` — **the
last step being Euclid's own** (the product of the secant's two segments *is* the power). So:
> **the NEAR intersection is TIMELIKE-separated from P; the FAR one SPACELIKE; and `ds²` vanishes at
> `t = √(t₊t₋)` — THE GEOMETRIC MEAN — which is exactly what III.36 calls the TANGENT LENGTH.**
> **The secant's two points straddle the light cone. The cone crosses at their geometric mean.**

**⟹ this is why ⊢38 is an identity and not a coincidence, and it strictly generalises it:** the tangent
is not null *because it is short*. **It is null because tangency IS the two roots meeting at their
geometric mean, the geometric mean IS the tangent length, and the tangent length IS the height** — so
the merge happens **on the cone**.

### ⊢ 61 · **★★★ P7's CENTRAL FORCING IS A TANGENCY CONDITION, AND P1 SUPPLIES THE TANGENCY** *(`sweep4_p7_trichotomy.py`, `sweep5_p1_supplies_the_tangency.py`)*
**P7, at source:** *"forced, by a trichotomy the horizon's own structure decides: … **transverse
crossing** (two distinct horizons), **tangency at a merged double root**, or **no real horizon**; a
collapse forms a horizon, excluding the last, and **the limiting orientation the horizon selects is
tangent rather than transverse**, excluding the first — so **the unique member whose null direction
grazes its own horizon is the Nariai configuration**."*
**Two points / one point / no points.** Verified: P7's `ΛM²` vs `1/9`, the horizon cubic's discriminant
sign, and the vertical line's intersection count with P3's fundamental ellipse **partition the family
identically**, same middle member (`2M = 2/(3√3)`, merged horizon `α/√3`).
> **Euclid**: the line CUTS / TOUCHES / MISSES · **P3**: three horizons / **NARIAI** / one real horizon
> · **P7**: TRANSVERSE / **TANGENT** / no horizon. **One trichotomy, three vocabularies.**

**And P3 already says it in ink** — *"The Nariai configurations are the VERTICAL-TANGENT POINTS of the
ellipse."* **The tangency was on the page, and on the 2012 figure, the whole time.**

**★ AND P1 IS THE CHAIN'S FIRST LINK, BY CITATION.** P1: *"every generator of `ℋ⁺` is a **LIMIT of the
past-light-cone boundaries**"*; *"every exterior 'now' asymptotically **TANGENT** to a single
generator"*; *"the tangent hyperplanes of `S_τ` become asymptotically tangent to the generator and **the
lapse of the foliation collapses**."* **P1's horizon is the limit of a family of light cones, and the
limit is a tangency at which a measure collapses.** P7 imports exactly this (`P1 §5`, cited at §125,
§222) to exclude the transverse case.
> **P1** (the approach is tangential) **→ P7** (therefore only the geometry that grazes its own horizon
> can host it: Nariai) **→ the figure** (a tangency IS a null condition, as an identity).
> **"The limiting orientation is tangent" and "the limiting orientation is null" are the same sentence
> — and P1 proves the first WITHOUT a metric while the figure proves the equivalence WITH one.**

### ⊢ 62 · **THE TANGENCY IS DESIGNATION-DEPENDENT; THE GEOMETRY IS NOT** *(`sweep4`, and a crude test's failure surfaced it)*
The two Nariai designations are **two different degeneracies in the `(r₀,r)` plane**: at `r₀ = 2/√3` the
double root is the ellipse's own **VERTICAL TANGENCY** (its discriminant vanishes); at `r₀ = 1/√3` the
ellipse is met **twice, not tangentially** — the degeneracy is **the DIAGONAL CROSSING the ellipse**.
**Same geometry, two vantages, two different geometric facts.** *The perspectival thesis, drawn in the
parameter plane.* **And it is why P3's own sentence uses the 2/√3 designation: the paper states the
tangency; the tangency is one vantage's view of it.**
*(⊢ also: `2M = (2/3√3) sin 3w` is bounded, so **`ΛM² = 1/9` is the family's CEILING, not a crossing** —
the dial rises to Nariai and returns; it never goes overcritical along its own dial.)*

### ⊢ 63 · **P4 AND P6 HAVE NO GEOMETRIC LOCUS — AND THAT IS LOAD-BEARING** *(`sweep3_p4_p6.py`)*
P4: **zero** angles, nulls, geodesics, tangents, triangles. P6: **zero** angles, nulls, tangents,
circles. **A sweep that found a connection in every paper would be a template, not a survey.** P2's
Thales circle means something *because* P4 comes back empty.
**But asking P4's QUESTION of P3's figure paid** — P4 is *the modern parallax*, and parallax is
rangefinding: **⊢ the dial's range IS the hole's angular radius** (sightlines within `arcsin(α/2α)=30°`
CUT the hole; at 30° they GRAZE; beyond, they MISS) ⟹ **NARIAI IS THE HOLE'S LIMB** — *not the two-30s
coincidence the faces forbid: the dial's endpoint IS the grazing sightline, and a grazing sightline IS
what an angular radius means.* And **⊢ P3 is rangefinding run through its third variable**: ordinary =
size + angle → distance; **P3 = distance FIXED + angle → MASS**, the hole's apparent size being not an
input but **the range of the instrument**. *The perspectival thesis stated as an instrument.*
**P6:** Steiner's 1826 move **is P6's inference rule executed on a circle** — the appearances (`EA·EB`)
vary line by line, the invariant behind them does not (verified); Euclid proved the relation and
stopped, **Steiner asked what must be true of the point.** Which is the corpus's own move: *2M is what a
vantage reads; α is what the geometry IS.* ⚠ **Demoted after testing:** P6's data are structures
favoured *ahead* of a decisive measurement and then vindicated; Steiner's was a fruitful definition, not
a prediction. **An instance of P6's RULE, not a datum for P6's THESIS.**

### ⚠ THE SWEEP'S OWN NEGATIVES, kept
- **sweep2, two false starts, both caught by the assert:** I predicted `D/4 = −ds²` and a
  secant/tangent/miss = timelike/null/spacelike trichotomy. **Sign backwards AND object wrong** — I
  measured to the line's *foot* placed at height 0, but **off tangency the foot is not on the throat**,
  so that point is on no part of the substrate. *A prediction about a trichotomy, made before checking
  its outer cases had anything to measure.* Then the tangency row vanished from the table (`D/4 ≈
  −4e-16`) — face 37 again.
- **sweep1:** I sampled max/min over a 400-point linspace that never lands on `π`. **Don't sample an
  identity.** Rewritten with sympy.
- **THREE TANGENCIES, THREE SPACES** — P7's is a null congruence meeting a horizon 3-surface in the
  SPACETIME; P3's a vertical line meeting a conic in the PARAMETER PLANE; the figure's a sightline
  meeting the throat in the OVERHEAD. **They partition the family identically** (checked, every row);
  **that one causes another is NOT established.** They agree because all three are one cubic's
  discriminant in three coordinates — *a fact about the cubic, not a mechanism running from Euclid to
  the cosmology.*
- **sweep2's quadratic bound STANDS:** no quadratic on the figure has P1's two events as its roots, and
  P1 carries no metric with which to build one. **The r1115→sweep5 correction is to the OBJECT claim
  only** — P1 is in the chain because **P7 imports its tangency**, not because its degeneracy is the
  figure's.

# PART XV — ★★★ R5, THE WAVES. *Daryl's rhyme, worked at the weight it was offered.* (r1117)

**The rhyme (Daryl, r1108, unassessed since):** *"The right-hand images, the purple and the
red-blue-purple ones, ARE WAVES. I think honestly there is a geometric description of electromagnetic
radiation in that. And between the purple and the red-blue-purple I think you might even have some
connection to wave-particle duality. **And I know that's a rhyme.**"*

**Run under face 34's rule** (c40's own): *"When he offers something provisionally, hold it at that
weight and **WORK** it. Do not inflate it into a claim and then correct the claim."* **So R5 is not
assessed.** The corpus's own instruments were picked up, pointed at the branches, and what they returned
is reported — including *"nothing"* and including *"this instrument is not entitled to answer."*

### ⊢ 64 · **THE OSCILLATION IS REAL AND IS NOT PUT IN BY HAND** *(`R5_the_waves.py`)*
Along the imaginary direction of cosmic time, `τ̃ = is`, `sinh(3is/2) = i sin(3s/2)`, so
**`r = A (sin²(3s/2))^{1/3}`** — real, bounded by `A = (2Mα²)^{1/3}`, periodic with period `2π/(3/2α)`.
Verified (⊢36 re-run). **That is sinh's own behaviour, not an assumption. Daryl's "these are waves" has
a genuine oscillation under it.**

### ⊢ 65 · **THE BRANCHES CARRY A DEFINITE ℤ₂ — continued by continuity, not evaluated**
Off the axes (`τ̃ = u + ic`), **arg(r) turns monotonically along a branch**, one sign throughout, net
`−0.891` rad; the conjugate branch returns the opposite (⊢33: conjugation reverses arg). **4000-step
continuation, no cut crossed.** *The structure P11's criterion reads is present.*

### ⊢ 66 · **★★★ AND THE INSTRUMENT IS NOT ENTITLED TO CALL IT A CHIRALITY — THE FINDING**
P11's criterion is **not** *"some complex thing's phase turns."* It is a statement about a **SPIN-2
POLARIZATION**, and its whole force is the transformation law (P11's own): *"a transverse rotation acts
as the spin-2 phase `e^{−2iθ}` on `h₊ + i h_×`, polarization angle `½ arg(h₊ + i h_×)`."* **The ½ and
the ±2 helicity ARE that law** — it is what makes a turning phase mean *handedness*.
**`r` is the areal radius: a SCALAR, invariant under transverse rotation** (computed, not asserted —
`e^{−2iθ}·r` moves, `r` does not).

> **THE INSTRUMENT FITS THE SHAPE AND FAILS THE TYPE.**
> **Which is ⊢55 arriving from the other side: a theorem translates only if its own QUANTITY is one the
> target can enter; an instrument applies only if its own INPUT is one the source can supply. Same law,
> pointed at instruments instead of theorems.**

### ⊢ 67 · **THE ℤ₂ HAS ITS OWN NAME, AND IT WAS ALREADY IN THE LEDGER**
`r³ = 2Mα² sinh²(3τ̃/2α)` is **three-sheeted** (⊢33 — *the three-foldness belongs to `r`, not `τ̃`*), so
continuing carries `arg(r)` by `2π/3` per sheet. **The turning is the MONODROMY OF THE CUBE ROOT; its
ℤ₂ is which way you went round.** A ℤ₃ read through an orientation ℤ₂ — **not a helicity.** *And that
is ⊢54 one level down: **a monodromy is not a motion.***

### ⊢ 69 · **★★★ ROUTE (b) — THERE IS NO SPIN-2 OBJECT ON THE FIGURE** *(`R5b_is_there_spin2.py`)*
Route (c)'s type-check made (b) well-posed: not *"what bend are these branches"* but **"is there a
spin-2 object on this figure at all?"** — because P11's shear is spin-2 too. **The obvious candidate:
a symmetric traceless tensor has exactly two null directions, and the substrate is doubly ruled.**
**It fails, twice, and the second reason is the deep one:**
- **The tensor is IDENTICALLY ZERO.** Annihilating both rulings forces `h_tx = 0`, `h_tt = −h_xx`;
  tracelessness w.r.t. `diag(−1,+1)` forces `h_xx = h_tt`. Together **`h = 0`**. *The two rulings are
  not two null directions a tensor picks out — **they ARE the 2-plane's own light cone**, and the light
  cone is what the metric already is.*
- **The symmetry group is wrong.** Spin-2's `e^{−2iθ}` comes from **SO(2)** on a **spacelike**
  transverse plane. The rulings span a **Lorentzian** plane, whose group is the **boost SO(1,1)** — and
  a boost **scales each ruling by `e^{±λ}` and never mixes them** (verified). **The pair carries a real
  boost weight (Newman–Penrose ±1), not a phase. The rulings are eigenvectors of the boost, not a
  polarization.**
- **And the overhead is not a transverse plane** — it is the equatorial projection *containing* the
  axis; `h₊+ih_×` lives on the spacelike plane *orthogonal to* a null direction, which is what P11's
  Gowdy leaf supplies.

> **SO ROUTE (c)'s FAILURE IS RESTATED CORRECTLY: it was not that the bead's `r` HAPPENED to be a
> scalar. THE FIGURE HAS NO SPIN-2 SECTOR. There was nothing else to choose.**
> **(b) and (c) end together, for one reason: the corpus's two wave-instruments are both spin-2, and
> the figure carries none. The rhyme is not refuted — the instruments cannot be pointed at it.**

*(My prediction was wrong at the first step: I predicted a spin-2 object would EXIST and be ACHIRAL, on
the grounds that the ruling families are as fixed an axis structure as exists. It does not exist.)*

### ⊢ 70 · **★★★ ROUTE (a) — "NOT EXCLUDED" IS NOT "OPEN"** *(`R5a_what_the_exclusion_reaches.py`)*
**The exclusion reaches L1 and only L1** — *"radiation carries no term in **it**"*, and "it" is the
expansion rate. P15's canon note draws the line itself: *"the PROGENITOR/local dynamics is **ordinary
GR at the standard Friedmann rate** (where the thermodynamics and BBN run); the two regimes are
**DISTINCT**."* **The ledger's route (a) was right about this — now checked, not asserted.**

**But the ledger also said the exclusion "leaves L2 and the field level ENTIRELY OPEN," and that half
is a slip.**
> **L2 is not excluded BECAUSE IT IS ALREADY FULL.** P16's entire abundance result runs there: the
> collapse heats above the deuterium bottleneck and cools back through it **at the standard Friedmann
> rate**, and that cooling leg **is a standard BBN** producing He-4 and D at observed values from one
> inherited `η`. **Radiation is present at L2 and sources the rate there. It is not a gap — it is one
> of the corpus's two empirical wins.**

**So route (a) is not an opening. It is a boundary condition, and a sharp one:**
> **A geometric description of EM at L2 would have to *BE* the ordinary photon–baryon plasma —
> reproducing it exactly, not replacing it** — because P15's peak-height match is explicitly
> *"conditional on the inherited medium being the ordinary photon–baryon plasma"* and P16's abundances
> come out by **ordinary** nuclear physics on a **standard** cooling leg. **Both data-confrontations are
> staked on that plasma being ordinary.**

**A geometric EM would be a RE-DERIVATION of ordinary electromagnetism from the substrate, not a new
field.** *Which is harder and more interesting than the route as written — and it is what the rhyme,
read strictly, says: "there is a geometric **DESCRIPTION** of electromagnetic radiation in that." A
description, not a replacement.*

### THE THREE ROUTES, AFTER THE RUN
- **(c) RUN — closed by a type-check** (⊢66).
- **(b) RUN — closed, and it restates (c)'s closure correctly** (⊢69): **the figure has no spin-2
  sector**, so (c) did not fail on an unlucky variable. *Both die of one fact.*
- **(a) RUN — closed as an opening, re-opened as a constraint** (⊢70): the exclusion reaches L1 only;
  **L2 is full of ordinary radiation that two empirical results depend on**; any geometric account must
  return exactly that plasma.

**⊢ 71 · WHAT SURVIVES OF R5, AT ITS WEIGHT.** A **real oscillation** that is sinh's own (⊢64). A
**real ℤ₂** that is the cube root's **monodromy**, not a helicity (⊢65/⊢67). **No spin-2 anywhere on
the figure** (⊢69). And a **sharpened target**: not *"is there EM in the branches"* but **"can ordinary
electromagnetism be re-derived from the substrate at L2, returning the photon–baryon plasma exactly?"**
**The rhyme is returned intact — three routes worked, instruments type-checked, question sharper than it
was.**

⚠ **WHAT THIS DOES NOT SAY.** It does not say the branches are not waves. **It says: the one instrument
the corpus has for reading a wave's handedness cannot be pointed at them, because its input is spin-2
and theirs is spin-0 — and the turning it would have reported was already in the ledger under another
name.** *The rhyme is returned intact, with one route closed and one route sharpened.*

⚠⚠ **THE PROCESS FAILURE, AND IT IS THE WORST KIND — I WAS RIGHT FOR THE WRONG REASON.** The first pass
evaluated the bead on numpy's **principal branch**, which jumps sheets at the cut, and returned a
103.66 spike and a **sign change**: *"the phase does not turn."* **And I had already written "THE
INSTRUMENT RETURNS A DEFINITE TURNING" in the prose above that output.** The receipt refuted the
verdict; the **corrected** computation then **vindicated** it. **The error and the truth cancelled — I
would have shipped the right claim either way, and nothing in the output would have stopped me, because
in the end it agreed.** *Only reading the table caught it.* → **face 38.**

# PART XVI — THE SWEEP, COMPLETE (r1117)

**All seventeen surveyed, vocabulary counted first.** The last five, counted rather than asserted:

| paper | nulls | tangents | circles | verdict |
|---|---|---|---|---|
| **P9** | 21 | **0** | 2 | no locus — its nulls are *null direction/geodesic/rulings*, not tangencies |
| **P10** | 6 | **0** | 4 | no locus |
| **P11** | 27 | **0** | 0 | no locus for ⊢58 — **but it holds R5's route (b), and its wave is spin-2** |
| **P15** | 23 | 1 | 0 | no locus |
| **P16** | 4 | 1 | **7** | **its circles are all references to P2's circle** — so P16 **inherits** the Thales circle (⊢59) rather than adding one |

**⊢ 68 · THE SWEEP'S OWN VERDICT.** The material reaches **P1, P2, P3, P5, P7, P8, P12, P13, P14** and
**does not reach P4, P6, P9, P10, P11, P15, P16**. **Nine and eight.** *And the eight are what make the
nine mean anything: a sweep that found a connection in every paper would be a template, not a survey.*
**And the law says why it stops where it does (⊢58): the protocol translates what is built from the
power of a point with respect to the throat, and those eight papers do not build from it.**

## ★★★ THE PROTOCOL'S OWN LAW — found by running it, not assumed

**⊢ 47 · THE BOUNDARY** *(`euclid6_ptolemy.py`, predicted before the run)*. **A classical theorem
translates only if its points sit at DIFFERENT HEIGHTS.** Ptolemy on the **throat** stays Euclidean:
the throat is the waist, `X₀=0` on all of it, so `dX₀=0` and **there is no minus sign to put in.**
*On the waist, Euclid is just Euclid.*

**⊢ 48 · AND THERE ARE TWO MODES, NOT ONE** *(`euclid7_nine_point.py`)*. Points that **span** heights
→ the signature is carried **in** (⊢38, 40–44). Points that sit **on** the waist → the theorem carries
the construction's **one input out** instead (⊢45: Euler's `R/2` *is* "the hinge is at twice the hole",
read backwards). **Neither mode is a miss.** The discriminator is `dX₀`.

## THE NEGATIVES FROM THIS RUN — kept per `THE_CODA` §"The negatives are the map"

- **"a radical axis is a level" — FALSE.** Equal power w.r.t. the throat *is* equal height (those are
  the concentric circles, each a level). But a radical axis needs **two** circles and the Thales circle
  is **not** centred on the axis, so its power is not a height. The axis **cuts** the levels. *The
  thing that breaks it is the off-axis centre — the same asymmetry the whole construction turns on.*
- **"inversion is the horn swap" — FALSE.** Inversion sends the outside of the waist **inside**, where
  the substrate has no overhead point (`X₀ = nan`, verified). The horn swap is `T: X₀↦−X₀`, which fixes
  `\|P\|` and is **invisible to the overhead**. Inversion is no substrate isometry — it is the map
  between the substrate's shadow and the hole.
- **⊢41's first phrasing — CORRECTED by ⊢43.** "The two triangles are each other under inversion" is
  **not vertex-wise**: `inv(hinge)` sits at `α/2`, the polar triangle's vertices at `α`. The relation is
  a reciprocal-circumradius pair plus a sides-are-the-other's-structure fact. *"Reciprocal" was doing
  work the computation does not support.*
- **the radical centre — TRUE and TRIVIAL.** Demoted, not deleted. The three Thales circles' common
  power at `O` is **zero**, not `−α²`: every Thales circle **passes through** `O` by construction, and
  three circles through a point have it as their radical centre for free. *(My first claim said `−α²` —
  that is the **throat's** power at `O`, the wrong circle. The assertion **failed its own receipt**.)*
- **U2 · `A = ∛2·ρ` — RUN** (`U2_metric_bridge.py`). **Exact, 2e-16, and Nariai-only** (2.88 at
  `r₀=0.2`, 1.93 at 0.35, 1.01 at 0.7 — not vacuous). Both numbers fall out of the **one** double-root
  factorisation: `ρ` from its `p`, `A³` from its `q`. **The 2 in the third root and the 2 in the
  constant term are the same 2.** ⚠ **r1109's wording corrected:** it is **not** "the vertical gap
  between the turnaround curve and the ellipse" — it is the **ratio of two ordinates on one vertical
  line** at `r₀=α/√3`. `lem:twoturnings` **untouched**: it denies identifying the two ℤ₃ *symmetries*;
  this is a ratio between two *radii*.

## PART XIII — NAMED, UNRUN *(from this run)*

| ? | the question |
|---|---|
| **U5** | **RUN (r1110, `U5_excircles.py`).** ⊢50 below. *(Original question:* **★ THE EXCIRCLES.** Feuerbach says the nine-point circle is tangent to the three excircles too — and unlike the incircle they are **not degenerate with anything**. For the hinge triangle they sit at `3α` with radius `3α`. **A third scale, in a construction that has insisted on exactly one.** Well-posed; nobody has asked.)* |
| **U6** | **RUN (r1110, `U6_la_hire_lifted.py`) — DOES NOT PROMOTE.** ⊢55–56 below. |

## ★★ WIRED IN — r1113. FIVE ROUTES. This document is no longer findable only by knowing it exists.

**The orphan problem, closed properly.** At r1110 this ledger was reachable from `CORPUS_MAP` (a
changelog — *history*, not a map) and `THE_PLAN`. **Neither is what a fresh Arthur navigates by**, and
per `CODA_FIELD_NOTE` r1090 *a live document nothing points at is silently rebuilt*. The routes now:

1. **The ontology map — `§1t`.** The navigation layer, read at kickoff step 8 by every Arthur. Carries
   the one fact, the law, what is baked, what is not, and the guards. **Cross-linked both ways from
   `§1f` (P3's card) and `§1j` (p0/17's card)**, so a node arriving at either paper's card is told this
   exists.
2. **`KICKOFF_ARTHUR` step 7** — named in the on-demand list, routed to §1t and Lane 7, *with the reason
   it is named at all.*
3. **`THE_PLAN` Lane 7** — the route; F.3 marked run and answered.
4. **The corpus itself.** **p0/17 `§sec:power`** cites `power_of_a_point.py`, `power_is_null.py`, and
   names this ledger and `storyboard_receipts/` for the catalogue and the failures. **P3
   `prop:twoalpha`** cites `alpha_alone.py`, `one_thirty.py`, `euclid7_nine_point.py` and names this
   ledger. *That is the corpus's own convention (cf. P15's inline receipts) and it is what stops the
   computation being lost when the prose survives.*
5. **`CORPUS_MAP` r1110–r1113** — the history, kept as history.

**A node can now reach this document from the map, from the kickoff, from the plan, or from either
paper — without knowing it exists.** That was the whole failure mode.

## WIRED IN — r1110

**This document was an orphan** until r1110: `CORPUS_MAP`'s r1109 entry pointed at it and nothing
else did — not `THE_PLAN`, not the ontology map, not any paper. Per `CODA_FIELD_NOTE` r1090 that is
how a live document gets silently rebuilt. It is now **`THE_PLAN` Lane 7** (F.1 the figure suite ·
F.2 the 3D · **F.3 the Euclid question** · F.4 named-unrun · F.5 the waves · F.6 the rhymes), with
the **one open call** — §Euclid in p0/17 or its own paper — named there as Daryl's rather than
defaulted.

**Audited r1110, two gates:**
- **Gate 1 — could each receipt have returned otherwise? 31/31 pass**, and the gate is not vacuous:
  it is the same check that condemns `thirty_degrees.py` (retracted). Every receipt opens as
  *Daryl's claim under test*; `the_isometry.py` tested the killing obstruction first.
- **Gate 2 — does any ⊢ rest on a drawing? None.** No proved statement cites `the_mapping_full.py`
  (the fabricated `r₀<0` half), `fund_ellipse_combined.py` (the pixel-read), `step1_all_points.py`,
  or `p3_chart_vs_ellipse.py`. **The arc's two errors were quarantined to the drawing and never
  reached the chain.**
- **The failure class, named:** not a rigged question and not a fabricated theorem — **a verdict
  wider than its computation's scope.** *"Which n closes the stamp?"* can only answer about stamps;
  it was read as answering about the hole. That is the check to run on every future ⊢ here.

## THE STATE OF THIS DOCUMENT

**58 proved statements across 25 receipts**, all running clean (31 at r1108; ⊢38–58 added r1110 — the
Euclid protocol run, its law found, U1/U2/U5/U6/U7 closed).

## ★ BAKED r1112 — the load-bearing half is in the corpus

**No longer "banked, not baked."** The split is the corpus's own, not one invented for the occasion:
P3's abstract already defers the substrate-level null ontology to p0/17 (*"its null generators the
straight rulings… its light cone the locked asymptote… the substrate-level ontology and the c–Λ
null-ruling gauge are developed in the companion geometric-core paper; here they ground the
construction"*). **p0/17 owns the substrate; P3 owns the construction.** The material split along that
line without being pushed.

- **→ p0/17 `§sec:power`** (new subsection of `§sec:rulings`, its own subject extended): **⊢9/⊢38** as
  `prop:tangentnull` — *the tangent from any point of the substrate to the throat is a null line of the
  embedding; Euclid's tangent–secant relation and the null condition are the same equation.* With the
  identification that **the rulings ARE those tangents**, so *"doubly ruled by straight null lines"* and
  *"every tangent to the throat is null"* are one statement — the classical register adding **reach**
  (every point of the overhead, not only those a ruling passes through). **⊢58** as `rem:onecircle` —
  *the identity is not a fact about powers but about ONE circle, the substrate's waist*, with the
  boundary stated in ink (chords, bilinear relations, and powers w.r.t. other circles carry no height).
  And **the two modern ingredients**: the minus sign, and **Steiner's 1826 power-as-invariant** — with
  the note that the second move *is the corpus's own*, the same as reading `r₀` as the offset that IS
  the mass. New refs: **Steiner 1826**, **Euclid III.36**. p0/17 **22pp, 0 undefined**.
- **→ P3 `§sec:hinge-geometry`** (`prop:twoalpha`): **⊢1–8 + ⊢45** — *the distance 2α is not chosen.*
  Five equivalences: `R=2α` ⟺ the 60° subtense ⟺ midpoint-tangency ⟺ **the nine-point circle IS the
  throat** ⟺ tangent length = height = `√3α`. *The point is not any one of them but their number:* the
  incircle relation P3 already states, the 60° it already computes, and the `√3α` its figure already
  carries are **one fact wearing several faces**. Then `eq:tripleathinge` — the placement **forces
  Nariai** by the triple angle alone — and the chain `α → 2α → sin w = ½ → sin 3w = 1 → w = 360/(3×4) =
  30°`, whose last step **is** P3's own twelve designations at 30°. **This answers a question P3 left
  standing:** it stated the hinge sits at 2α and never said why. P3 **34pp, 0 undefined**.
- **Full corpus recompiled from source: 17/17, 0 undefined.**

**What is NOT baked, and stays here:** the protocol *as a protocol* (the translation method), the
failures that drew the law (Ptolemy, La Hire, Casey — ⊢44, ⊢55–56, ⊢58's derivation), the
construction's remaining entries (⊢40–43, ⊢46–57), the rhymes, and the historiography. **The open call
is now much smaller than it was:** most of §Euclid had a home in a paper already waiting for it; what
is left over is a § and not a paper.
 The next moves, in Daryl's own order: **the figure suite (p17's baby)** · **the
extended kaleidoscope** with the lines run out far enough to show the squares, triangles and circles ·
**the P14 conjecture figure** at its narrowed weight · **the 3D**, staged · and **Euclid**, which may be
the largest thing here and is the least worked.

## The bead's Kretschmann scalar is free of both M and α (added r1598)
**Statement.** On the bead's outward branch r = (2Mα²)^{1/3} sinh^{2/3}(3τ̃/2α), the small-parameter limit gives
r ≈ 6^{2/3}M^{1/3}s^{2/3}/2, and the Schwarzschild Kretschmann invariant becomes
> **K = 48M²/r⁶ = 64/(27 s⁴)** — **independent of M *and* of α.**
**Why.** The amplitude carries M^{1/3}, so r⁶ ∝ M², and **6 × (1/3) = 2 exactly** cancels the M² in the
numerator. *The cancellation is not a coincidence of the invariant; it is the amplitude's exponent.*
**Two results in one expression.** This is simultaneously **the singularity's strength** (K ∝ s⁻⁴) and **the
M-cancellation** (M²/r⁶ ∝ M⁰) — *not two facts that agree, one line read twice.*
**Verified** symbolically (r1598) and **in the corpus at r1600–r1601, reciprocally: P2 §kretschmann and P7 beside `thm:bead`, each citing the other.** *P7 holds it as the theorem's own consequence; P2 holds the contrast.* **Placed in P3 §sec:mass at r1602** — where it is *evidence for the section's own claim that M is a projection artefact*, since a curvature independent of M is what a projection-feature predicts and an intrinsic one forbids. **NOT placed in P16 or P1** — P16's and P1's origin is the **finite-curvature seam**, not the r=0 spike. Originally P2 §kretschmann, beside its own cycloid treatment, with receipt `P02_bead_K_mass_free`. **The contrast is the content:** the cycloid's amplitude ∝ M gives K ∝ M⁻⁴; the bead's ∝ M^{1/3} gives K ∝ M⁰. **Interpretation deliberately not attached:** `C40_HARVEST` §II.2 records
that both readings originally given to the M-cancellation are wrong, so the identity stands and the reading is
open — plan items **A2.9** and **A2.10 §II.2–3**.

## The expansion amplitude is the Hubble–Eddington radius, up to 2^{1/3} (added r1608)
**Statement.** The Hubble–Eddington radius $r_\star=(M\alpha^2)^{1/3}$ — where the slicing surface's Gaussian
curvature $K_G=1/\alpha^2-M/r^3$ vanishes (P3), the local scale at which a bound structure's gravity balances
the cosmic expansion — and the bead amplitude $(2M\alpha^2)^{1/3}$ — which is the areal radius at matter–$\Lambda$
equality, since $\rho_m/\rho_\Lambda=\operatorname{csch}^2(3\tilde\tau/2\alpha)$ is unity at $\sinh=1$ — stand
in the ratio
> **$(2M\alpha^2)^{1/3} / (M\alpha^2)^{1/3} = 2^{1/3}$, exactly, for every $M$.**
**The factor** is the $2$ of the Schwarzschild term $2M/r$ against the $M/r^3$ of the surface curvature, and
nothing else enters. **Content:** the local structure-formation boundary and the cosmological matter–$\Lambda$
equality radius are one length read at two ranges — the quantitative form of P15's *"one constant read at two
ranges."* **Verified** symbolically, receipt `P15_two_ranges_ratio`. **Placed** in P15 and P3, reciprocally
cited. **Owes to** A2.9 (the amplitude = turnaround root = equality scale).
