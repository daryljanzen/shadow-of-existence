## ⌘ `retired_conformal_seed/` — 70 receipts carrying the conformal-time seeding defect (r2350)

*They build `eg` seeded at zero where `ag` begins at $a=10^{-5}$, omitting the **4.596 Mpc** already
elapsed from $a=0$. **Late-time results there are unaffected** — the acoustic scale, the peak positions
and the peak ratios are insensitive to a constant $\eta$ offset — **while early-time quantities and any
cross-code comparison are not**. Stored **as built and used**, not retroactively corrected, so the record
of what was run stays honest. ***None is cited by the corpus and none is imported by anything that is.***
See that folder's own `INDEX.md` for the full list and the reading guide.*

*The three files that were live — `HIER_photon_hierarchy`, `C11TEST_radiation_zeroed`,
`ROBUST_p1p2_scan` — were **fixed and re-run** at r2349–r2350 and remain here.*
