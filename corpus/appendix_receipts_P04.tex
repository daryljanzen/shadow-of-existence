\clearpage
\section*{Appendix R\quad Computational Receipts}\label{app:receipts}
\addcontentsline{toc}{section}{Appendix R: Computational Receipts}
\small\noindent Each computation marked \rcptmarker\ in the text is verified by a runnable receipt in the \texttt{receipts/} directory that ships with this work; status \textsf{OK} = verified (traced, run, output matches the claim). This appendix is generated from the receipt index, not maintained by hand.\par\medskip
\begingroup\renewcommand{\arraystretch}{1.25}
\begin{description}
\item[\label{rcpt:P04_redshift_isotropy_floor}\texttt{P04\_redshift\_isotropy\_floor}]\hfill\textsf{[OK]}\\
\textit{sec:floor} \ (redshift-isotropy floor sigma\_path=sigma\_\{8,eff\}/(3 sqrt N)\textasciitilde{}2.8e-3 exceeds observed monopole isotropy (<\textasciitilde{}3e-6) by \textasciitilde{}3 orders -> matter-tracking expansion excluded). 
\emph{Computes:} d\_lss integral -> N=1174; floor 2.77e-3; exclusion ratio 924\textasciitilde{}10\textasciicircum{}3; genuine-floor (biases down) + delta a/a=-1/3 delta derived
 \emph{Bound:} linear separate-universe deficit taken as input
\ \emph{Run:} \texttt{python3 receipts/P04\_modern\_parallax/P04\_redshift\_isotropy\_floor.py}
\item[\label{rcpt:I20_the_redshift_is_a_conformal_killing_charge_on_the_null_cone}\texttt{I20\_the\_redshift\_is\_a\_conformal\_killing\_charge\_on\_the\_null\_cone}]\hfill\textsf{[OK]}\\
\textit{sec:decomp} \ (I20 --- THE REDSHIFT IS A CONFORMAL-KILLING CHARGE ON THE NULL CONE. Integrable-systems field bake, probe I20. In conformal FLRW form d/deta is shown by explicit Lie derivative to be a CONFORMAL and not an exact Killing vector, so its charge is conserved only where p.p = 0; there it is a*omega, whose constancy IS 1+z = a\_obs/a\_em and whose logarithm is int H dt. Carries a MASSIVE CONTROL: the spatial momentum still falls as 1/a but a*E is not constant for m nonzero, and it recovers constancy in the massless limit -- so the null-cone restriction is load-bearing and P04's line-of-sight integral is a statement about light.). 
\emph{Computes:} runs clean; nine verdicts including a massive control that fails off the cone and recovers in the limit
 \emph{Bound:} derived here; no parameter pinned
\ \emph{Run:} \texttt{python3 receipts/P04\_modern\_parallax/I20\_the\_redshift\_is\_a\_conformal\_killing\_charge\_on\_the\_null\_cone.py}
\item[\label{rcpt:R2_the_papers_correlation_figures_are_the_ones_nothing_checked}\texttt{R2\_the\_papers\_correlation\_figures\_are\_the\_ones\_nothing\_checked}]\hfill\textsf{[OK]}\\
\textit{sec:floor} \ (R2 --- P04's CORRELATION FIGURES ARE CORRECT AND ITS RECEIPT CHECKED A PLACEHOLDER INSTEAD. Probability field bake, probe R2, and it exists only because of the step the six-field campaign skipped. P04's probability vocabulary is near zero --- probability x0, stochastic x0, random x0, variance x2 word-bounded --- so the term side scored it CHECKED-NEGATIVE. Read from contents it is the highest paper in the field: its whole argument is the standard deviation of a path-averaged random field. The read found that P04's two quantitative correlation figures are right --- N\_eff = d\_lss/138 = 68 and the sqrt(N/N\_eff) = 4.2x rise --- while its existing receipt checks the caveat with a placeholder N/4 = 293 giving 2x, so the direction was checked and the stated magnitude never was. Also verifies all three of P04's window values, \textbar{}W\textbar{}\textasciicircum{}2 = sinc\textasciicircum{}2(kL/2) at k = 1/L, 3/L, 10/L, as 0.92, 0.44 and 0.04. Verdict 4 turns 'positive correlations can only increase the variance' into a theorem: Var(mean) = (sigma\textasciicircum{}2/N)(1+(N-1)rhobar), symbolically and by simulation. CONTROL: a NEGATIVE rhobar must reduce the variance, or the positivity clause would be vacuous.). 
\emph{Computes:} runs clean; six verdicts and a sign control
 \emph{Bound:} derived here; the probe that overturned a CHECKED-NEGATIVE verdict, and the ledger's claim that P04 'returned empty on all six fields' is corrected in place
\ \emph{Run:} \texttt{python3 receipts/P04\_modern\_parallax/R2\_the\_papers\_correlation\_figures\_are\_the\_ones\_nothing\_checked.py}
\item[\label{rcpt:R50_the_long_wavelength_bullet_reverses_for_the_observable_and_the_corner_is_three_parts_per_million}\texttt{R50\_the\_long\_wavelength\_bullet\_reverses\_for\_the\_observable\_and\_the\_corner\_is\_three\_parts\_per\_million}]\hfill\textsf{[OK]}\\
\textit{sec:floor} \ (P04's fifth 'biases downward' bullet says modes longer than the path contribute coherently and so add to the scatter; the quantity being bounded is an ANISOTROPY and every sightline is cast from the same observer, so a mode longer than the observable region is a monopole and cancels out of it). 
\emph{Computes:} casts sightlines from one origin through a plane-wave Gaussian field and measures BOTH the single-path variance and the across-sky scatter: the ratio is 0.990 at kL in 100..1000 and 2.0e-4 at kL in 0.01..0.1, monotone between, stable under halving the modes, doubling the sightlines and changing the seed; then integrates a BBKS CDM spectrum against the paper's own d\_lss and finds only 3.1e-6 of the path-mean variance below k = 1/L, with 95 percent of it above kL = 70
 \emph{Bound:} SUPPORTS P04 -- the bullet needs one clause, not a retraction, and the clause is landed. NOT CLAIMED: any value of the floor itself (P04\_redshift\_isotropy\_floor owns that); the other four bullets and R2's along-path correlation theorem are untouched
\ \emph{Run:} \texttt{python3 receipts/P04\_modern\_parallax/R50\_the\_long\_wavelength\_bullet\_reverses\_for\_the\_observable\_and\_the\_corner\_is\_three\_parts\_per\_million.py}
\item[\label{rcpt:B11_three_triples_two_identifications}\texttt{B11\_three\_triples\_two\_identifications}]\hfill\textsf{[OK]}\\
\textit{sec:cycloid} \ (THREE three-fold structures and TWO open identifications carried in two registers that do not reference each other --- the root triple is the shared vertex of both and appears in neither as such). 
\emph{Computes:} asserts all three structures at source, P4s two-triples question and its \{0,1,2\} requirement, and that neither register cites the other (8 checks)
 \emph{Bound:} NOT-A-PAPER-CLAIM --- connects two open items; no identification established
\ \emph{Run:} \texttt{python3 receipts/L221\_the\_bridge/B11\_three\_triples\_two\_identifications.py}
\item[\label{rcpt:B12_the_two_triples_are_typed_differently}\texttt{B12\_the\_two\_triples\_are\_typed\_differently}]\hfill\textsf{[OK]}\\
\textit{sec:cycloid} \ (the two triples are NOT one structure: the ROOT triple is the f=0 locus (roots 1/sqrt3, 1/sqrt3, -2/sqrt3, sum zero) while the CAUSAL triple is three VALUES of f each with one locus --- three-element sets of different KINDS). 
\emph{Computes:} asserts P4s question and its three-equally-spaced-values clause, computes the Nariai cubics roots and zero-sum and double root, matches PO-2s two distinguished roots, and counts the loci per level (8 checks)
 \emph{Bound:} NOT-A-PAPER-CLAIM --- a NEGATIVE result discharging the corpuss only DISCOVERABLE-PROOF
\ \emph{Run:} \texttt{python3 receipts/L221\_the\_bridge/B12\_the\_two\_triples\_are\_typed\_differently.py}
\end{description}\endgroup
