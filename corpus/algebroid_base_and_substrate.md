# The algebroid base 𝒞, the two reticles, and the substrate dimension
*Spine of the algebroid build; working definition for Plan Moves 5–7 and the algebroid paper (Move 14).
Move 4 — re-cut r196 after Daryl's correction: **de Sitter and Schwarzschild are ONE slicing**, the
difference is observer 1's **static(exterior) ↔ dynamic(spacelike)** vantage. Corrects the r195
r=0/r₀ conflation and the same-slice point. Tags [established]/[reading]/[reach]. Reversal §5.*

## 1. Direction and dimension. **[established — P2, P3, P6; substrate dimension dS₅, settled 2026-06-12 (corrected at source from the dS₄/dS₅ conflation)]**
The substrate is **dS₅** — one 5-dimensional de Sitter manifold, realized as a hyperboloid in the flat
ambient $M^6$, isometry $\mathfrak{so}(5,1)$. It must be 5-dimensional because the construction generates
*many distinct* 4D geometries as cuts/sweeps of *one* substrate: slicing a 4D de Sitter only
re-coordinatizes it, so a 4D substrate cannot carry the family. (The earlier "dS₄ substrate, dS₅ dead"
was an error — it conflated the 4D de Sitter *geometry* swept from a cut, isometry $\mathfrak{so}(4,1)$,
with the 5D *substrate* that is cut, isometry $\mathfrak{so}(5,1)$.) This is C1 in the corpus,
"dS₅-in-M⁶," now load-bearing, not deferred.

## 1a. The two construction operations — split on dimension. **[the split and the two distinct involutions settled 2026-06-12 (from the ruling picture, corrected at source); the algebroid-face assignment is [reach]]**
A dSⁿ is a one-sheet hyperboloid in $M^{n+1}$, *generated* by a double null ruling (a restriction of
$M^{n+1}$'s null generators to the $n$-surface; those generators literally generate the geometry). Two
distinct operations generate geometries, and they differ in **what they do to dimension** — this is the
hygiene that ends the dS₄/dS₅ pull, the same kind done for the singularity/topology taxonomy:

- **(A) Reassignment** (P7's move; the seam flip $\theta\mapsto\pi/2+i\psi$): **dimension-preserving,
  discrete.** It keeps *the same background geometry* — the dS₄ a slice already generated — and involutes
  the **causal roles** of the generating ruling: the null-generator bundle is reassigned as the timelike
  comoving worldlines (at rest in $S^3$, "going nowhere," contracting-then-expanding along the embedding's
  level foliations), and the at-rest geodesics are reassigned as the *new* geometry's null lines. One
  swap, null↔timelike, on a fixed geometry. dS↔SdS; P5's $f>0$ hole / $f<0$ cosmos.
- **(B) Slicing** (the slicing curve): **codim-1, dimension-reducing.** It cuts a $(d{+}1)$-substrate
  down to $d$ — and *this* is the operation that forces the substrate dimension up: $\mathrm{dS}_5\to$ 4D.
  The curve has two poles across the throat hole (the two critical points, $r_h$ and $r=0$, each carrying
  a null X, the X's parallel in the embedding); it drops to the equator, is tangentially vertical at the
  critical point, pivots $90°$ and wraps the equatorial ring to the back pole.

**The two involutions are distinct (settled — Daryl).** The slicing side carries a **turning-point
involution** $r_h\leftrightarrow r=0$: a *positional* swap of the curve's two poles — set either critical
point as $r=0$, run around the circle to the other, then off along the hyperbolic interval (from one side
of the higher-D hole to the other). That is the root/$S_3$ involution, a feature of *which pole is the
origin*. The reassignment involution is a *causal-role* swap on a fixed geometry. Different operations on
different things — not the same $\mathbb{Z}_2$, so reassignment is **not** a generating reflection of the
root dihedral; it is its own discrete operation.

**Corrected reach (the discreteness of reassignment forces it).** An earlier reach read the structure
group as reassignment — "$\mathfrak{so}(5,1)\ltimes\mathcal{C}=$ reassignment $\ltimes$ slicing." A
*discrete* involution cannot be the *continuous* $\mathfrak{so}(5,1)$, so that is **retracted**. The
continuous $\mathfrak{so}(5,1)\ltimes\mathcal{C}$ is the **slicing** structure (the substrate isometry
acting on the cut-space $\mathcal{C}$); reassignment is a distinct **discrete** causal-role involution.
*Which exact algebroid face each discrete piece occupies is left open — do not lock it.* **[reach]**

> **Terminology — SETTLED (Daryl, 2026-06-12).** *Substrate* = **dS₅** (the 5D de Sitter manifold the
> slicing curves cut to generate the 4D spacetime geometries; in the $M^6$ embedding — 5D-in-6D *for now*,
> until the colour question may force a dim-up to dS₆/M⁷). *Background geometry* = **dS₄** (what the 3D
> universe lives on — the de Sitter the $S^3$ evolves in; the thing reassignment holds fixed). **Neither
> 4D geometry is the reality:** the de Sitter and SdS readings are the *same ontological layer* under two
> representations (P7's "projections of one ontological layer"), generated from the substrate by slicing —
> the real thing is the evolving layer, the substrate is the manifold its representations are cut from.
> (This is *not* the retracted "purely representational" overshoot — CR stays ontological; "generated /
> representation" mean built-and-real, per the dagger register.) **Re-term DONE, and it was tiny —
> not the corpus-wide sweep first expected (r204, reading every usage at source):** across the corpus
> *substrate* is the **family-carrier** term — the one held invariant that the geometries are cuts of
> (P5 "many geometries, one substrate"; P6 "geometries as cuts of the one de Sitter substrate"; the canon
> ontology line "a 3-sphere evolving in a de Sitter substrate"). **That role *is* the dS₅ role**, so those
> usages were already correct and **stay** — P5 needed no fix. The *only* place "substrate" was pinned to
> **dS₄** was P7 (*"the underlying de Sitter substrate — a point of the waist 3-sphere of dS₄"*); those
> three usages → **background geometry** (P7's cosmological background, the thing reassignment holds fixed).
> P7's dS₄ physics untouched (its slices are $S^3$; dS₅'s are $S^4$). **Still open — the geometric
> reconciliation, NOT terminology — open *physics*, to be settled by computation (a `[reach]` the gate
works and states for reversal, NOT a pending orchestrator call):** how the corpus's "geometries are slicings of the de
> Sitter manifold / 1D curve on a 2D section swept by SO(3)" framing (P5/P6, written dimension-agnostic via
> $\mathfrak{so}$) sits against the dS₅-genuine-codim-1-slicing picture. **NOT $r_0$ (corrected by Daryl,
> r205):** $r_0$ is observer-2's *reticle orientation* — how the slicing curve is aimed into the hole
> (centre-aim → Schwarzschild, edge-aim → Nariai, between → intermediate SdS; $r_0{=}0$ is the hole
> centred). The SdS family is a sweep of *re-aimings* of observer 2, an orientation DOF, not a translation
> along a 5th dimension; the metric "offset $r_0$" is the imprint of that aim. So **what the substrate's
> 5th dimension actually is remains genuinely open** — do not guess it is the family/$r_0$ direction.
> Pinning the substrate dimension to 5 *in the P5/P6 bodies* waits on that reconciliation; the terminology
> re-term above did not depend on it. **[reach — the reconciliation; the 5th-dimension's identity open]**
>
> **SETTLED by computation (2026-06-13, r232 — the gate did what the tag above asked).** The (1,4) dS₅
> coset tangent decomposes as **leaf $(\rho,\theta,\phi)$ — 3 spacelike $=$ the HDA structure function
> $h^{ab}$** $\oplus$ **$t$ — 1 timelike $=$ the lapse/$\varepsilon$** $\oplus$ **$\chi$ — 1 spacelike $=$
> the 5th dimension** ($(0,3)+(1,0)+(0,1)=(1,4)$; `scripts/f1_leaf_coset_reconciliation.py`). So the 3-dim
> Riemannian leaf metric **is** the 3-spacelike block of the 5-dim coset, P5/P6's "1-D curve swept by
> $SO(3)$" builds exactly that block, and **the 5th dimension is $\chi$** (the extra $S^3$ angle, the
> totally-geodesic codim-1 normal). Literal for the vacuum (dS₄$=\{\chi=\pi/2\}$); for matter (SdS) the leaf
> relates **intrinsically** (no $\chi$-slice — class 2), the $M$-variation the algebroid connection. Full
> account: `f1_homomorphism_consolidation.md` §6. **[computed — the reconciliation and the 5th-dimension
> identity ($\chi$); the field-theoretic completion + Move 13 remain]**

> **Resolution (2026-06-13, recorded so it cannot re-seed).** The slicing-vs-reassignment
> distinction this whole region exists to fix is **settled between P3 and P7**: the corrupting locus was
> P7's old *"metric reassignment of the dS₄ substrate"* — one word, *substrate*, doing two jobs — now
> corrected so the dS₄ is the **background geometry** (of the reassigned SdS) and the substrate is dS₅
> (`CR_framework.tex`, confirmed clean). That fix is **done**. The **remaining P5/P6 geometric
> reconciliation above is open *physics*, to be settled by computation** — a `[reach]` the gate works and
> states for reversal, **not** an orchestrator call. The single double-used word seeded a standing
> "Move 4 is Daryl's domain" dissonance that contaminated the calculation; this note retires that seating.

## 2. The one curve, the diametric poles, and what M is. **[established — P3 §throat, §mass §618; P2 §149]**
A single slicing curve $r(l)$ on the dS₅ manifold. It meets the throat circle (radius $\alpha$) at two
**diametric poles**: **$r=0$ at the back**, the **horizon at the front** — the two $r$-poles of P2's
one homogeneous circle ("one point seen twice").
- **$M$ is not a dial.** $2M=\alpha\bigl((r_0/\alpha)-(r_0/\alpha)^3\bigr)$ — the throat radius modulated
  by the slicing offset $r_0$ (§mass). $\alpha$ is the invariant; $M$ rides entirely on $r_0$; $M=0$ at
  $r_0=0$; bounded by $\alpha$. **$M$ is downstream of observer 2, not the de Sitter/Schwarzschild knob.**
- **$r=0$ is a point on the curve** (the back pole), its position set by observer 2's slicing — it is
  **not** a member of $\mathcal{C}$, and it is **not** the offset $r_0$. (This corrects r195's "$r=0$
  lives in $\mathcal{C}$" and its citing of §536's "$r_0$ is a position along the curve" for an $r=0$ claim.)

## 3. The two reticles — and de Sitter/Schwarzschild is STATIC↔DYNAMIC vantage on ONE slice.
**Observer 2 = the slicing orientation.** Sets $r_0$ — hence the pole positions, where $r=0$ sits, and
$M$. **$\mathcal{C}$ = the space of observer-2 slicing orientations (the offsets).** **[reading — P3 §536]**

**Observer 1 = the causal vantage on the rigid substrate.** It is the entire de Sitter/Schwarzschild
difference, *on one and the same slice*:
- **exterior / timelike vantage → Schwarzschild (STATIC)**; the exterior vantage across observer 2's
  off-centre slices ($r_0\neq0$) gives the whole **SdS family** (static, $2M=r_0-r_0^3$);
- **spacelike vantage → de Sitter (DYNAMIC / cosmological)** — not a static massless anything; the
  expanding reading of the same slice;
- at the origin, timelike → no manifold (the hole); at the origin, spacelike → worldlines crossing the ring.

So the de Sitter↔Schwarzschild complementarity is **observer 1's static↔dynamic reassignment of ONE
slicing** — same offset, same curve, **same $M$** — *not* a different slice and *not* different $M$.
This is **P5's own $f>0$ hole / $f<0$ cosmos**, and the **static twin of P7's NBC hinge** (collapse–static
↔ cosmic-expansion–dynamic of one ontological layer under two causal assignments; CORPUS_MAP backbone).
The mechanism is in P3: the **seam signature flip** $\theta\mapsto\pi/2+i\psi$ (Prop flip, §478) carries
the Riemannian $(+,+)$ to the Lorentzian $(-,+)$ on **one** analytic curve — that *is* the vantage flip.
**The sweep depends on observer 1** (§584, "the timelike orientation"), pivoting on the $r=0$ observer 2 set.
**[established that observer 1's vantage carries the static↔dynamic flip — P3 §478, §584; P5; P7]**

**Prose-alignment flag (the reading is settled; the wording is gate work).** P3 §556/§558/§560 frame de Sitter↔Schwarzschild as
*different slicings with $M$ varying* (de Sitter $M=0$, Schwarzschild $M\neq0$) — bundling observer 1's
vantage with observer 2's offset. **The correct reading is settled — one slice, vantage-distinguished**
(the seam section §466, §504 already carries the one-curve reading; the P7 background-geometry fix confirms
it). Reconciling §556/§558/§560's prose to the vantage reading is a **rewording the gate carries and states
for reversal**, done when P3 is next touched — not a pending orchestrator decision. **[the reading ruled; the prose alignment is gate work]**

## 4. For the algebroid — two layers.
- **Base $\mathcal{C}$:** observer-2 slicing orientations (offsets $r_0$; $M=\alpha$-modulated). Vacuum =
  straight cut (SdS kernel, P6); matter = the bend, $\rho=m'/4\pi r^2$; isotropy at a cut = its
  sweep-symmetry subalgebra. **[established/reading]**
- **Observer-1 causal layer:** the **static↔dynamic vantage reassignment** ($=$ P7's $\Psi$/NBC, the
  corpus hinge). The de Sitter/Schwarzschild pair lives here, on one slice. **[reading]**
- **Resolved (`f1_homomorphism_consolidation.md` §§1,5):** where the observer-1 causal layer sits in
  $\mathfrak{so}(5,1)\ltimes\mathcal{C}$ — it is **neither** a second structure-group. The continuous
  $\mathfrak{so}(5,1)$ acts **non-transitively** on $\mathcal{C}$ (`f1_orbit_transversality.py`: different-$M$
  cuts non-isometric), with $M$ the **transverse modulus** (folding at Nariai, `f1_offset_modulus_fold.py`);
  observer-1's causal vantage is the **separate discrete $\sigma$** ($\mathbb{Z}_2$ signature flip), already
  distinguished from the continuous action. The structure-function variation across the transverse modulus
  is the **algebroid connection** (per-stratum grading: symmetric only at {Type O, Nariai},
  `f1_per_stratum_subalgebra_id.py`). Move 5 (the shift) is done (`corpus/anchor_consolidation.md`); this
  bore on Move 6 (bracket closure), now computed across the strata. **[computed; the embedding-uniqueness the
  one soft spot]**

## 5. Reversal condition
Substrate dS₅ (in $M^6$, $\mathfrak{so}(5,1)$); $\mathcal{C}$ = observer-2 slicing orientations; **de Sitter↔Schwarzschild = observer-1's
static↔dynamic vantage on ONE slice (same $M$)**. Reversible by showing the pair requires *different*
$r_0$ (different $M$) — i.e. that it is observer 2's axis after all, not observer 1's vantage.
