---
name: next
kind: RECORD
current: r2470
job: THE WORK ORDER — what to do next, in order, with enough detail to start cold. Read after THE_HUB on arrival. Delete a block when it lands.
sources: [chat]
---

# NEXT — the work order at r2470

> ## ⛭⛭ HOW TO USE THIS
>
> *Each block below is **executable without Daryl**: it names the object, the first command or the first
> question, and what a result would look like. **Take them in order.** When one lands, strike it here and
> log it in `CORPUS_MAP` as usual.*
> ⚠ ***If a block turns out to rest on a stale premise, that IS the result*** — *three rows this session
> (`L-221`, `L-165`, `L-175`) turned out to have premises that had gone stale, and finding that was worth
> more than the work they asked for.*

---

## ⓵ THE $\hat\Theta$ FLATNESS SCAN — **take this first**

**Why:** *c54.187 established that $\ell_1/\ell_A$ is a **seam-datum artefact** (2.21× under phase), and that
**the spacing is not**: $0.79\pm0.04$ of $\ell_A$, never 1.0, across every phase.* ⇒ ***The spacing is now the
load-bearing number on front #2, and the only thing that could take it away is an instrument freedom that
moves it.***

**The object:** *the fork named it — "the phase is one degree of freedom in the datum. **$\hat\Theta$'s
flatness in $k$ is another and I did not scan it.**"*

**What to do:** *scan $\hat\Theta$'s $k$-dependence the way c54.187 scanned the phase; the nine phase spectra
in `computations/beyond_the_wall/spectra/c54.187_cr_phi*.npz` are the template for how a scan is stored and
read.*
**What a result looks like:** *· if the spacing moves → **the ~21% deficit is a datum statement too**, and
front #2 has no surviving number; · if it does not → **the deficit survives two independent freedoms** and is
much stronger than it is today.* ⚠ ***Either way it is a MEASUREMENT result, not a framework verdict. `F5`
unsoftened, `PO-7` protected, the conversion is Daryl's.***
⌗ *This is the fork's own pick and its layer. **If 54 is running, it takes this and 56 takes ⓶.***

---

## ⓶ `L-218` — THE COMPANION SPEC, and it is this line's to write

**Why:** *the row's own next step reads **"the companion SPEC written as a repo document before any code,
because it decides whether the thing is trustworthy."*** ⌗ *At r2463 this line mis-filed this as "held by
Daryl" — **"each rung of the access ladder is Daryl's call" governs WHICH RUNG IS CLIMBED, not whether the
SPEC gets written**.*

**The object:** *the failure mode the row names — **"the agent cannot distinguish what the book ESTABLISHES
from what it merely DISCUSSES, so it answers confidently about both — fatal for a physics framework."***

**What to do:** *write `COMPANION_SPEC.md`. **The corpus already solved the hard half and the SPEC's job is to
say so**: `PROTECTED_OPEN` marks what may not be closed, `THE_BASE_RATE` scores claims, `receipts/INDEX.md`
pins every assertion to a runnable check, and the do-not-assert census marks resemblances. ⇒ **A companion
built on this corpus can distinguish established from discussed because the corpus already declares the
difference** — which is exactly what `L-237` found the gates rest on.*
**What a result looks like:** *a repo document naming what the companion must refuse to assert, and what it
must cite when it asserts.* ⌗ ***No code. The row says the SPEC decides trustworthiness, so the SPEC comes
first.***

---

## ⓷ `L-210` — THE ENTRY-POINT FRONT, one site at a time

**Why:** *44 real sites, all now carrying verdict markers, **and the front is where P14's whole 54-development
came from**.* *Daryl: "working through those one by one we got to like 10 and moved off."*

**What to do:** *`python3 scripts/work_entry_points.py` reports the state. **Take live leads one at a time and
without rushing** — the row's own instruction. The dig is mechanical and re-runnable; **the verdict never
is**.*

---

## ⓸ `L-211` — RE-ARM AFTER EVERY CLOSURE

**Not a task — a standing procedure, and it out-performed everything else this session.** *After any closure,
**read the gaps in the papers it touches**; the deliverable is which adjacent gap the closure just made
answerable.* ⌗ ***Seven runs, seven distinct kinds of result, including two that corrected this line's own
claims one and two revisions old.***

---

## ⌗ WHAT IS DELIBERATELY NOT ON THIS LIST

*· **`L-206`** — genuinely held by Daryl, and it says so in its own words ("⏸ HELD BY DARYL r2381 —
deliberately"). **The only such row on the board.***
*· **`L-207` ⓶** — gated on `PO-6` and must not be opened without it.*
*· **The three gate requests** (`L-220`, the prose-duplicate scanner, the travelling-finding detector) —
**each is one declaration away and the declaration is the other party's to make**. Routed as items 30, 34, 35.
**Do not build any of them speculatively.***

---

## ⌗ THE STANDING DISCIPLINE, in four lines earned this session

> ***CHECK THE DEFECT, NOT JUST THE REPORT.*** *A routed report is a claim about a TREE, and trees differ.*
> ***A MARKER CONSTRAINS AN ACT, NEVER A SUBJECT.*** *`PROTECTED_OPEN` forbids CLOSING, not touching.*
> ***A DICHOTOMY SOMEONE STATED IN PASSING IS NOT A PROOF OF EXHAUSTIVENESS.***
> ***BEFORE ASKING WHY THERE IS NO GATE, ASK WHAT WOULD IT CHECK AND WHO DECLARES IT.***
