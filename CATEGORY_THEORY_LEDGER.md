---
name: category-theory-ledger
description: R-M's fifth field bake — CATEGORY THEORY, the corpus's largest unlisted field. Opened r1892 after the r1890 holes survey found 363 uses and two papers named for its objects, with the field itself absent from the reach list. Its opening question is one the corpus states about itself.
current: c54.163
---

# THE CATEGORY THEORY LEDGER — the field the list did not carry

> **⌗ RE-READ AGAINST c54.163 AND DECLARED CURRENT r2395.** *The fork changed **none of the four field-bake
> archives** in the c54.153→c54.163 span, so the question was again* ***"did anything the span produced
> bear on what this bake concluded"*** *rather than "what moved in the file". **The span produced two
> substantive results and one instrument class**, and each was tested against this ledger:*
> *· the **species-selection withdrawal** (c54.162) — the crossing is **lossless for every species**;*
> *· the **recursion unfolded** (c54.163) —* ***"the first clause of that question has no object, and once
> that is seen the other two answer themselves"***: *there is no map between the patch's closed-$S^3$
> harmonics and the ambient universe's, because the correspondence is **null boundary to null boundary,
> "with no spacelike slice entering the map"**, and the collapse interior is Kantowski–Sachs on
> $\mathbb{R}\times S^2$, **carrying no closed-$S^3$ basis at all**;*
> *· the **receipt sweep** — 186 receipts gained assertions and the hollow-assertion lint was extended twice.*
> ⇒ **None bears on this bake's verdicts**, *which is what `CATEGORY`'s own rule predicts:* ***a bake's
> close is about its FIELD, and a row's content may still grow.*** **Reopen it when the field has more to
> give, not when its subject moves.**


> **⌗ READ AND DATED r2385 (observer line) — CLOSED, and it carries a distinction worth keeping.**
> *All five probes run r1893–r1895; the two ZEROS **adjudicated at c54.13 and they STAND**. Its own reason is
> the keeper:* ***"they are verdicts about what the FIELD brought, and category theory still brought no new
> content — so both stand as written. What grew is a ROW, not the bake."*** *`K2`'s fibre multiplicity became
> $D-1$, but the enrichment's **source is the dimension result, not this field**, so the census cluster it
> touches is enriched **from outside** and the `R2` pass stays ZERO —* ***"narrowing where a requirement lives
> does not move a permission into one."***
> ⇒ ***A BAKE'S CLOSE IS ABOUT ITS FIELD; A ROW'S CONTENT MAY STILL GROW.*** *That is the rule that lets a
> closed bake stay closed while its subject keeps moving, and it is the answer to "should this ledger be
> reopened every time something it touched changes." **No: reopen it when the FIELD has more to give.***


> **⌗ r2376+c54.11 — RE-READ AGAINST THE FOUR-DIMENSIONALITY RESULT.** *This bake ran against a **four-dimensional cut**, when nothing distinguished *structural* from *true at $D=4$*. The probes whose verdicts turn on a $D=4$ number were **re-derived at general $D$**, not annotated — verdicts are **at the probes themselves** (K2 · K5 · K8 · a word collision on “generation”). Method and full working: `THE_DIMENSION_DEEPENING_AUDIT.md`; receipt `P03_reach_probe_deepening`.*


*Opened r1892. **Phase 8, sixth field, and the first to come from a HOLE rather than a handoff.** The r1890
survey found `groupoid` ×149 · `algebroid` ×72 · `morphism` ×84 · `category` ×58 — **363 uses, two papers named
for its objects** (P5 the description groupoid, P12 Lie algebroids) — **and the field absent from
`THE_MATHEMATICS_REACH`'s set, its objects filed under `Group / Lie theory` and `Algebra`.***

***That filing is the reason the bake is worth running: a groupoid filed under group theory gets baked with
group theory's tools, and the questions category theory asks were never put.***

**⛭ RUN UNDER THE FOUR CONFIRMATIONS** *(`PHASE8_DIFF_AUDIT` §4)*, **and with the baseline CHECKED rather than
asserted** — *the Galois bake was opened on asserted zeros that were wrong, and retired before a probe ran.*

---

## ⛭ 0 · BASELINE — surveyed r1891, word-bounded, citation keys stripped

**⌗ THE OBJECTS ARE EVERYWHERE:** *`groupoid` ×149 · `algebroid` ×72 · `morphism` ×84 · `objects` ×215 ·
`composition` ×30 · `invertible` ×14 · `identity` ×132.*

**⌗ THE FIELD'S OWN VOCABULARY IS ABSENT — every one checked and returning zero:** *`functor` · `natural
transformation` · `adjunction` · `universal property` · `colimit` · `Yoneda` · `monad` · `equivalence of
categories` · `2-category`.*

***So the same shape as the last four bakes, and by now it is the diagnostic: the corpus builds the objects and
does not own the theory of them.*** **Here it is starkest — P5 constructs a groupoid, proves its generators and
relations, and proves it complete, without the word `functor` appearing once in seventeen papers.**

**⛭ FOUR WORD COLLISIONS, the sharpest set the canon carries, entered in §0 BEFORE the bake:**
*• **`category`** — ***42 of 58 uses are "CATEGORY ERROR"***, one of the corpus's load-bearing epistemic terms;
the mathematical sense appears nowhere. **Any use must name its objects and morphisms.**
• **`adjoint`** — ***all 19 uses are SELF-ADJOINT***, P10's. **A categorical use says "adjoint functor".**
• **`composition`** — the **nuclear composition datum** against **composition of maps**.
• **`identity`** — an equation (46) · the identity map (8) · ***and the hexad guard's "a resonance, not an
identity", which means NOT THE SAME OBJECT and must never be blurred with the first.***

---

## ★★★ 1 · THE OPENING QUESTION — and the corpus asks it of itself

*P5 `sec:nariai-algebroid`, at source:*

> *"The branch point of the cover---the Nariai configuration---is the locus at which the discrete structure of
> this paper meets the continuous structure of the algebroid completion: it is a stratum at which **the
> algebroid connection vanishes** and the substrate's isotropy enhances. That the deck group's branch point and
> the connection-vanishing stratum **coincide** is a structural meeting of the discrete (deck/monodromy) and
> continuous (algebroid) faces of the one solution space. **The full relation between $\mathrm{Aut}(A_2)$ and
> the algebroid structure is a direction the present paper identifies but does not develop.**"

***That is a category-theoretic question stated in group-theoretic and algebraic language, by a paper that had
no field to hand it to.*** **In category theory the relation is standard vocabulary: a Lie algebroid is the
infinitesimal object of a Lie GROUPOID, and whether a given algebroid integrates to one is the integrability
question (Crainic–Fernandes).** *So the corpus has a groupoid, an algebroid, a proven coincidence of their two
distinguished strata, and an explicit note that the relation is undeveloped.*

---

## ⛭ 2 · THE PROBES

| # | probe | why it is worth throwing | verdict |
|---|---|---|---|
| **★★★ K1 ✔ RUN r1893 — BITES, and it answers the corpus's own undeveloped direction.** | ***THE ALGEBROID IS AN ACTION ALGEBROID, AND P12 NAMES IT SO IN ITS OWN NOTATION:*** *$\mathfrak{so}(5,1)\ltimes\mathcal{C}$ — the infinitesimal action of the substrate's isometry algebra on the space of cuts.* **An action algebroid is ALWAYS integrable** *(the Crainic–Fernandes obstructions vanish for it)*, ***and it integrates to the ACTION GROUPOID $\mathrm{SO}(5,1)\ltimes\mathcal{C}\rightrightarrows\mathcal{C}$: objects the CUTS, arrows the isometries carrying one cut to another.*** **And that is NOT P5's $\mathcal{G}$, whose objects are VANTAGES on one geometry — the corpus already separates them by dimension** *("the continuous algebroid IS THE SLICING STRUCTURE", 5→4; "reassignment is DIMENSION-PRESERVING AND DISCRETE")*. ***★ SO THE RELATION IS ISOTROPY.*** *In an action groupoid the arrows from an object to itself are its isotropy group; the coset grading is symmetric exactly when that isotropy makes a symmetric pair — **so P12's connection MEASURES the isotropy's failure to be of that kind, and vanishes where it is not failing.** Dimensions, from P12's own numbers: **Type O 10 · Nariai 6 · generic 4** — the isotropy enhances at exactly the two strata.* **And $\mathcal{G}$'s branch point is $\sigma$'s fixed point, where two designated roots merge — a vantage BEING a choice of designated root, and the choices collapsing exactly when the isotropy that permutes them grows.** ***So the discrete and continuous faces are not two structures degenerating at one locus: they are the isotropy of ONE action groupoid, read discretely and infinitesimally, and Nariai is where that isotropy jumps.*** *Landed in P5 at `sec:nariai-algebroid`, replacing the undeveloped-direction note.* `storyboard_receipts/K1_action_groupoid.py` |
| **★★ K2 ✔ RUN r1894 — Ψ IS A FUNCTOR, and its three functorial properties ARE three companion papers.**  **⌗ r2376+c54.11 — RE-RUN AT GENERAL $D$.** *"Three cuts, one geometry" is a fibre multiplicity, and the multiplicity is the fold: $D-1$. **Four-to-one at $D=5$; and from $D=6$ not a finite multiplicity at all**, because there is no fold to read.* ⚠ *A dimension-conditioned multiplicity is **new content rather than a renaming**, so this bake's "CENSUS PASS — ZERO / `R2` PASS — ZERO" close is **reopenable**. Not reopened here.* `P03_reach_probe_deepening` | *An isometry taking cut $c$ to cut $c'$ takes the geometry read off $c$ to the geometry read off $c'$, compatibly with composition — **so $\Psi$ carries arrows as well as objects.*** ***The content is not that it is a functor but WHAT KIND, and each answer is a paper:*** *• **ESSENTIALLY SURJECTIVE** onto its image — **and P9 IS that image; "the range of the operator" computes the essential image** · • **NOT FAITHFUL**, and the corpus says how: *verified*, the three roots of one horizon cubic all return the same $2M$, so **three cuts, one geometry** — ***and the failure of faithfulness IS the vantage multiplicity P5 classifies*** · • **FULL? NOT SETTLED — and it is P5's own stated gap**, the within-geometry component established and the cross-$\alpha$ component not.* **So three separate companion results are the three standard questions one asks of a functor.** *Landed in P8.* `storyboard_receipts/K23_functor_and_quotient.py` |
| **★★★ K3 ✔ RUN r1894 — "THE SINGLE INVARIANT IS THE GEOMETRY" IS A UNIVERSAL PROPERTY.** | *P5 carries the parenthesis from P3 as an observation. **Unpacked: "an invariant" is a function constant along arrows; "the SINGLE invariant" says every such function is a function OF the geometry** — ***which is exactly the universal property of the quotient: any assignment constant along the morphisms factors through the geometry, and uniquely.**** **Verified on the corpus's own quantities: $\alpha$ and $2M$ are constant across the three cuts of one geometry and so are functions of it; $r_0$ is not.** ***And the rigidity claim is the same fact read the other way — "the continuous parameter is a CHART LABEL, not a modulus" says precisely that the quotient map kills it, which is what "dimensional collapse" names.*** **So the invariant claim has a uniqueness clause it was never stated with, and the discrete generation P5 proves is what makes the quotient computable.** *Landed in P5 as `rem:universal`.* `storyboard_receipts/K23_functor_and_quotient.py` |
| **★★★ K4 ✔ RUN r1895 — THE THREE LEVELS ARE A SPAN, and the discriminator test is its commutativity.** | ***"The one layer both causal assignments share" is not an arrow L1→L2 — it is an OBJECT they both receive from.*** *So: **L1 ← seam → L2**, with **L3** receiving from each.* ***AND THE TWO "QUANTITATIVELY FATAL" CARRYINGS ARE ATTEMPTS TO COMPOSE L1→L2 DIRECTLY — to use an arrow the diagram does not have.*** **That is why mis-assignment is fatal rather than merely inaccurate: the only route between them is through the seam, and naming the shape says once what the rule says case by case.** *★ **And the commutativity condition is the corpus's own discriminator test**: "projection-independent ratios are clean discriminators **because the L3 map cancels**" — verified, $\theta_D/\theta_*=(r_D/D_M)/(r_s/D_M)=r_D/r_s$.* ***So "a clean discriminator" IS "a quantity on which the span's two legs agree" — a commuting-diagram condition, stated as a cancellation.*** *Landed in §1·LEVELS.* `storyboard_receipts/K45_span_and_nariai.py` |
| **★★★ K5 ✔ RUN r1895 — THE COINCIDENCE IS FORCED, and "coincidence" was the wrong word.**  **⌗ r2376+c54.11 — RE-RUN AT GENERAL $D$ — NARROWED, not killed.** *The near-horizon isotropy **enhancement is $+2$ in every dimension**, so the mechanism survives; but "six-dimensional against a generic four" is the $D=4$ pair — $D=5$ gives **9 against 7**, $D=6$ **13 against 11**. **State the enhancement, not the pair.** Calibration: the computation returns this row's own $(6,4)$ at $D=4$, which an earlier pass of it did not — the error was caught **against this row** before it propagated.* `P03_reach_probe_deepening` | *Nariai is located across the corpus **six ways**: the cubic's **double root** and σ's fixed point · the **singular point** of the reducible locus · the end of the conic's **minor axis** · the **merged horizon** · where the **photon sphere** meets it · and now the **isotropy jump**.* ***VERIFIED: the first five all reduce to the single condition $f(r_N)=f'(r_N)=0$.*** **And the sixth is what that condition IS, read on the near-horizon geometry: a double root makes it $\mathrm{dS}_2\times S^2$, whose isometry $\mathrm{SO}(2,1)\times\mathrm{SO}(3)$ is six-dimensional against a generic four — exactly the stratum P12 records.** ***So one condition wears several faces, and the ISOTROPY is the face that explains why a discrete structure and a continuous one both mark the locus*** — *which closes `K1`'s account.* *Landed in P5.* `storyboard_receipts/K45_span_and_nariai.py` |

---

## ★★★ 4 · THE CLOSE — all five run, r1893–r1895

### ⛭ 4a · WHAT THE FIELD GAVE

*Five probes, five bites — **and every one was already implicit in a sentence the corpus had written without
the vocabulary to finish it.***

| | |
|---|---|
| **`K1`** | ***P12's algebroid is an ACTION algebroid — its own notation says so — hence always integrable, to $\mathrm{SO}(5,1)\ltimes\mathcal{C}
ightrightarrows\mathcal{C}$, objects the CUTS.*** *Not P5's $\mathcal{G}$, whose objects are vantages.* **The relation P5 called undeveloped is ISOTROPY: the connection measures its failure to be a symmetric pair, the deck branch point is where the vantage choices collapse, and both read the jump** |
| **`K2`** | ***$\Psi$ is a functor, and its three functorial properties ARE three companion papers*** — *essentially surjective (**P9 IS the essential image**) · not faithful (**and the failure IS P5's vantage multiplicity**) · fullness unsettled (**and it is P5's own stated gap**)* |
| **`K3`** | ***"The groupoid's single invariant is the geometry" is the UNIVERSAL PROPERTY of the quotient*** — *every function constant along arrows factors through the geometry, **uniquely**. And "the continuous parameter is a chart label, not a modulus" is the same fact read the other way* |
| **`K4`** | ***The three levels are a SPAN, L1 ← seam → L2 with L3 receiving from each*** — *so the two "quantitatively fatal" carryings are **arrows the diagram does not have**, which is why mis-assignment is fatal rather than inaccurate. **And "a clean discriminator" is a commutativity condition, written as a cancellation*** |
| **`K5`** | ***Nariai is ONE condition, $f=f'=0$, wearing six faces*** — *verified on five of them — **and the sixth, the isotropy jump, is what that condition IS read on the near-horizon $\mathrm{dS}_2	imes S^2$*** |

### ⛔ 4b · WHAT THE FIELD DID NOT GIVE

*• **No new object.** *Every one of the five renamed or related things the corpus already had.*
• **No adjunction.** *`K2` found a functor and its three properties; **no adjoint pair appeared**, and none was
manufactured.* • **No 2-categorical structure**, and none looked for — *the corpus's objects are a groupoid, an
algebroid and an operator between them, and nothing in the five probes needed a higher cell.*

### ⟐ 4c · UNTRIED

*• ***`K6` ✔ RUN r1897 — FULLNESS SETTLES WITHIN ONE $\alpha$, and P12's stratification was the proof all along.*** **At every stratum P12 names, the CUT's isotropy is not merely of the same dimension as the GEOMETRY's isometry group but IS that group** — *$\mathrm{SO}(4,1)$ at Type O · $\mathrm{SO}(2,1)\times\mathrm{SO}(3)$ at Nariai · $\mathbb{R}_t\times\mathrm{SO}(3)$ at generic SdS, which P12 writes out by name.* ***So at fixed $\alpha$ nothing is left over: $\Psi$ is FULL.*** **And the remaining half — P5's cross-$\alpha$ component — cannot close the same way: the map between representations of different $\alpha$ is the HOMOTHETY, and a homothety is not an isometry. It is `C1`'s dilation, the generator by which the causal-structure group exceeds the isometry group.** ***So the failure across radii is not a gap in the classification but the ONE SCALE appearing as a functorial fact: the leaves are not related by isometries because $\alpha$ is exactly what the causal structure does not fix.*** *Landed in P8.* `storyboard_receipts/K6_fullness.py`
• ***`K7` ✔ RUN r1898 — YES, AND P9 WAS ALREADY SPEAKING THE LANGUAGE.*** *P9's architecture is an **essential-image computation**, in its own words: **"the range is bounded above by the symmetry-reducible sector, and we show it fills that sector"** — bound above by a necessary condition, then show the bound attained.* ***AND ITS NECESSARY CONDITION IS §0's CUT CRITERION, THE SAME SENTENCE: "a geometry's isometry group CONTAINS A SWEEP-SUBGROUP of the substrate's."*** **And `K6` sharpens the inclusion to an EQUALITY at every stratum P12 tabulates** — *scoped there and no wider, the Type-I classes and the wall being named without isotropies computed.* **⌗ And the orbit-space language is already P9's: "ODE (ONE ORBIT-SPACE VARIABLE) → a finite parameter family (1 for SdS; FOUR for Type-D)."** ***So the answer is yes and the corpus was there: what the field adds is that P9's bound, P9's sufficiency, §0's cut criterion and P12's isotropy table are FOUR ASPECTS OF ONE CONSTRUCTION rather than four results.*** *Landed in P9.* `storyboard_receipts/K7_range_is_essential_image.py`
• ***`K8` ✔ RUN r1899 — THE ORBIT-TYPE FILTRATION, AND IT SURFACES A NUMBER THE CORPUS NAMES AND DOES NOT USE.*** **① ORBIT DIMENSION.** *$\dim\mathfrak{so}(5,1)=15$, so orbit $=15-\dim\mathfrak{h}$: **Type O 5 · Nariai 9 · generic SdS 11 · principal (isotropy 0) 15.*** ***THE MOST SYMMETRIC CUT HAS THE SMALLEST ORBIT*** — *so pure de Sitter sits at the degenerate END of the filtration, not in its interior.* **② AND THE ORDERING $10>6>4>3>2>0$ IS P9's OWN AXIS** — *"the one null-degeneracy axis, Nariai and the wall its opposite ends", ordered there by **algebraic type** and here by **isotropy**, **the two agreeing at the ends P9 names.*** ⚠ *Scoped: agreement at the ends is not agreement in the middle, and no check has been run on the Type-I classes.* ***★★ ③ AND THE FIND: P12 states the admissible symmetric-pair dimensions as $\{6,7,10\}$ and the construction occupies SIX and TEN. Enumerated independently and confirmed — and **dimension SEVEN, realised by $\mathfrak{so}(4)\oplus\mathfrak{so}(1,1)$ and $\mathfrak{so}(3,1)\oplus\mathfrak{so}(2)$, is named by that count and identified with no stratum.**** ***★ AND `K8b` SETTLED IT IN THE NEXT TURN RATHER THAN LEAVING IT: THE STRATUM IS EMPTY, BY P9's OWN BOUND.*** *A swept geometry inherits a subgroup of $\mathfrak{so}(4,1)$, and **neither dim-7 pair embeds there** — $\mathfrak{so}(4)$ already uses all four spacelike directions, and $\mathfrak{so}(3,1)$ leaves only one where a rotation needs two.* **So $\{6,7,10\}$ is about the algebra, "exactly two strata" is about the reachable class, and they agree.** *Landed in P12 as the answer.* `storyboard_receipts/K8_orbit_type_filtration.py`  **⌗ r2376+c54.11 — RE-RUN AT GENERAL $D$ — GROUNDED.** *The emptiness argument runs from $\so(4,1)$, which is the stabiliser of a **four-dimensional cut** and has dimension 10 in **every** $\dS_D$. With the cut's four now derived rather than observed, "$\so(4)$ already uses all four spacelike directions" stops being a coincidence of counting and becomes a consequence.* `P03_reach_probe_deepening`

### ★★ 4d · THE FOUR-PART CLOSE  

> **⌗ r2376+c54.13 — THE TWO ZEROS ADJUDICATED, and they STAND.** *c54.11 recorded them as reopenable. Worked: they are verdicts about **what the FIELD brought**, and category theory still brought no new content — so both stand as written. **What grew is a ROW, not the bake**: K2's fibre multiplicity is $D-1$, and that enrichment's source is the dimension result, not this field. ⇒ the census cluster it touches (**generation-replication**) is enriched **from outside**, and the `R2` pass stays **ZERO** because the generation count was already a REQUIRE — narrowing where a requirement lives does not move a permission into one.* **A bake's close is about its field; a row's content may still grow.** `P03_step3_refused`

**① `check_bibliography.sh` — CLEAN.** *Mackenzie carried into P5; one undefined `JanzenGroupoid` in P8 caught
by the check and fixed.*
**② The theorems ledger — §4a and §4b above.**
**⛭ AND `K6` WAS RUN AFTER THE CLOSE, r1897 — the untried queue's first entry, settled.** *Fullness holds within one $\alpha$ (P12's stratification is the proof) and cannot hold across $\alpha$ (the cross-leaf map is `C1`'s dilation, not an isometry). **Neither adds a census enrichment nor an `R2` move, so the passes stand as written.*** **Queue now: the action groupoid's orbit space against P9's range · the stratified groupoid's invariants.**

**③ THE CENSUS PASS — ZERO.** *Checked all five against the six clusters. **None enriches one.*** *`K1`, `K2`,
`K3`, `K5` explain corpus structure; `K4` explains a **failure mode**, and §1·SHADOW's collapse is not one of the
census's standing problems.*
**④ THE `R2` PASS — ZERO, and `K3` was checked and rejected.** *The move would have been "the geometry is AN
invariant, no other found" → "every invariant factors through it, uniquely" — **which is require-shaped.** But
the corpus does not hold the first form: **P5 proves `prop:completeness`**, and given a completely known
morphism group the quotient's universal property is a **consequence**, not an upgrade. **`K3` makes explicit what
completeness already gives.*** *The rest explain, and an explanation is not a promotion.*

***⛭ SO THIS IS THE SECOND CONSECUTIVE FIELD WITH ZERO ON BOTH PASSES — complex analysis and category theory,
both purely explanatory.*** **And the pattern is worth naming: the fields that dissolve and promote are the ones
that bring NEW CONTENT; the fields that explain are the ones whose objects the corpus already built. Category
theory could not promote anything because it had nothing to add — only names for what was there.**

---

## ⛭ 3 · WHAT LANDS WHERE

*• **P5** `groupoid_paper` — the groupoid, and `sec:nariai-algebroid`'s own open note. **`K1`, `K3`, `K5`'s home.**
• **P12** `algebroid_paper` — the algebroid, the anchor, the connection. **`K1`, `K5`.**
• **P8** `slicing_operator` — the operator. **`K2`'s home.**
• `ONTOLOGY_FOUNDATION_INDEX` §0 — **`category`, `adjoint`, `composition`, `identity` entered r1892; any
further collision goes in before use.***

**⛭ AND THE CLOSE HAS FOUR PARTS** *(r1862–r1866)*: **① `check_bibliography.sh` · ② the theorems ledger in its
four parts · ③ the CENSUS PASS · ④ the P6 `R2` PASS — and if nothing moves, say so.**

---

## ⌗ `L-103` — THE CROSS-REFERENCE THIS LEDGER WAS OWED, AND IT WAS LOST FOR TEN REVISIONS

**⚠ SAME FAILURE AS `L-100`, SAME TURN, SAME CAUSE.** *At `r2376+c54.34` the slate sweep answered the orbit-space
question, gave the follow-up the ID `L-103`, and **never registered it**. Found at `c54.49` by auditing the ID
space against the row count.*

**⌗ THE CROSS-REFERENCE ITSELF.** *The orbit space $X/G$ is **not an unworked object**. It is* ***P9's range
theorem*** *(which classifies exactly the reachable cuts) and* ***P12's stratification*** *(which gives the
strata by isotropy), under the category-theoretic name.* **⇒ The queue entry asks for something two papers
already carry, and this ledger should say so rather than hold it open.**
