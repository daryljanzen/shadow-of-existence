# BUNDLE — `r2376+c54.157`

**Two parts, each under 30 MB, no file split across parts. Together they are the whole programme.**

| part | file | size | contains |
|---|---|---|---|
| 1 | `CR_bundle_r2376+c54.157_part1_corpus.tar.xz` | 24 MB | every top-level document, `corpus/`, `figures/`, `receipts/`, `verification/` |
| 2 | `CR_bundle_r2376+c54.157_part2_working.tar.xz` | 14 MB | `computations/`, `capstones/`, `scripts/`, `forks/`, `kills/`, `resources/`, `retired/`, and the build/work directories |

Both extract to a single top directory `cr_r2376/`; extract part 1 and part 2 into the same place and
the tree is complete. **Verified this revision by extracting both parts and running a recursive diff
against the working tree — zero differences.** Checksums in `BUNDLE_r2376+c54.157.sha256`.

*The split is now a script rather than a hand procedure: `scripts/cut_bundle_two_part.sh`, written
this revision because the hand version had once cut a file across parts.*

---

## What is new since `r2376+c54.156`

**The bounded exception is closed: P7 and P17, thirteen of twenty — and not one stale number in the whole
sector.** Five independent readings, twenty cited-and-cannot-fail receipts, **zero number-drift**. Every
figure those two papers print is still produced by the receipt carrying it.

**That settles what the last four passes were circling.** 14/28 on the sector revised this arc; 1/16 on the
matter sector; 9/24 on P15's spine with six stale figures; 5/9 on P16's with four; **13/20 here with none.**
*Number-drift tracks revision and nothing else. The corpus does not have a decaying reproducibility layer;
it has one that decays exactly where the prose moves.*

The misses here are the milder disease — claims typed into print strings — and the two that mattered are now
computed: the bead radius's branch points (its cube is entire, so no essential singularity is possible), and
su(3)'s minimal faithful real dimension (enumerated by the Weyl formula: 6, so it cannot sit in so(5)).

**And one genuine code defect**: a column headed $4\alpha^4(u^2-1)$ computing `4*(u*u-1)`. The $\alpha^4$ was
dropped and the column agreed anyway because the file sets $\alpha=1$. **A parameter set to one hides every
power of it.**

Assertion debt 175 → 172. `THE_BASE_RATE` entry twenty. **Ten gates pass. All 17 papers compile at 0 errors.**
