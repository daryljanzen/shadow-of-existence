---
kind: REFERENCE
job: projective geometry of quadrics against the CR substrate — the CK metric identification, and the ladder gap it exposes
name: quadric-geometry-ledger
description: R-M's first field bake — projective geometry of quadrics thrown at the CR substrate. Opened r1838 (Phase 8). What the corpus already reaches, the boundary the throat protocol states for itself, and the bites this field can take that the classical catalogue could not.
current: r3512
---

> **▣ REFERENCE — R-M's first field bake, and part of the corpus.** *Projective geometry of quadrics
> thrown at the CR substrate. Its findings are content the corpus draws on: the metric is a
> Cayley--Klein log-cross-ratio with $k=\alpha$ exactly; the vantage $S_3$'s projective invariant is the
> equianharmonic $e^{i\pi/3}$; the horizon locus is a reducible cubic whose singular points are the two
> Nariai; and the import is bounded — **the metric imports exactly, the group does not**, because CK's
> $\mathrm{PO}(5,1)$ identifies antipodes and CR does not.*
>
> **⌗ THE GAP IT FOUND IS NOW CLOSED IN THE PAPER.** *It recorded that the polar of a substrate point
> cuts a totally geodesic $\mathrm{dS}_4$, and that **whether that $\mathrm{dS}_4$ is the background rung
> was open** because the corpus stated the ladder and never said how the background sits in the
> substrate. **p0 now settles it in its own voice** — "a second projective fact follows at once, and it
> settles how the ladder's second rung sits in its first" — with the receipt
> `Q6r_polar_is_the_background`, the polar's $\mathrm{SO}(4,1)$ being a sweep-subgroup of the
> substrate's $\mathrm{SO}(5,1)$.*




> **⌗ CHECKED AGAINST c54.163→c54.185 AND DECLARED CURRENT r2449.** *The span's substantive content is **the derived
> lensing and the acoustic front** (c54.181–184), **a lint rebuild** (c54.180), **bibitem repairs** (c54.185), and
> the fork's application of routed items 1–9. This line's own r2417–r2448 added `ARC 16`/`ARC 17`, the `L-150` and
> `L-213` closures, six outside readers' findings, and two lints.*
> ⇒ ***None of it bears on the quadric bake — the hyperboloid, polarity and the power of a point.*** *The question asked was not "what moved in the file" but* ***"did anything the
> span established or withdrew bear on what this file says"*** *— and the answer here is no.*
> ⚠ *Stated rather than assumed: **the fourteen documents swept beside this one all turned out to hold real missed
> landings**, so a declaration of no-effect is a claim and is made as one.*


# THE QUADRIC GEOMETRY LEDGER — R-M's first bake

> **⌗ RE-READ AGAINST c54.163 AND DECLARED CURRENT r2396 — the deepest lag on the board (−152) and it clears.**
> *The fork changed this file in no span; the test was the same bounded one — **did anything the c54.153→c54.163
> span produced bear on what this bake concluded?** The span gave the **species-selection withdrawal**, the
> **recursion unfolded**, and the **receipt sweep**.*
>
> ⚠ **AND ONE NEEDED A SECOND LOOK, BECAUSE A WORD COLLIDES WHERE THE OBJECTS DO NOT.** *The recursion result is
> about **closed-$S^3$ spatial harmonics** — "there is no map between the patch's closed-$S^3$ harmonics and the
> ambient universe's, because the correspondence is null boundary to null boundary." This ledger says "harmonic"
> thirteen times and* ***none of them is that object***: *`Q4`'s is the **cross-ratio** sense (harmonic
> $\lambda=2$, $j=1728$, CM by $i$ — as against **equianharmonic**, $j=0$, CM by $\omega$), and the rest are
> **Fourier harmonics of $2M(w)$**.* ⇒ ***The word collides; the objects do not*** *— which is the naming guard's
> own class (`ONTOLOGY_FOUNDATION_INDEX`: "the shared 'three' is a coincidence"), and it is written down here so
> the next pass does not re-flag it.*
>
> ⌗ [REPORTED] **AND THIS BAKE FEEDS THE QUEUED EDGE TWICE OVER.** *`Q4`'s finding —* ***equianharmonic $\iff j=0 \iff$
> complex multiplication by $\omega=e^{2\pi i/3}$, the corpus's own $\mathbb Z_3$*** *— is the handoff that makes
> **Ⓒ** R-M's front rather than Ⓕ. **And its §4c carries the standing constraint the edge inherits**: the
> projective isometry group **does not import**, because *"projective identifies antipodes and CR does not — the
> substrate is the double cover"*, so **every Cayley–Klein theorem quantifying over the isometry group must be
> checked on $\mathrm O(5,1)$ and not its quotient.*** ⌗ *Its own honest weight travels too:* **"three points
> $120^\circ$ apart with their centre are equianharmonic AUTOMATICALLY — the content is not the value but that
> CR's vantages ARE at $120^\circ$."**


> **⌗ r2376+c54.11 — RE-READ AGAINST THE FOUR-DIMENSIONALITY RESULT.** *This bake ran against a **four-dimensional cut**, when nothing distinguished *structural* from *true at $D=4$*. The probes whose verdicts turn on a $D=4$ number were **re-derived at general $D$**, not annotated — verdicts are **at the probes themselves** (Q4 · Q5 · §2c · the untried polar-simplex entry). Method and full working: `THE_DIMENSION_DEEPENING_AUDIT.md`; receipt `P03_reach_probe_deepening`.*


*Opened r1838. **Phase 8, the claiming campaign**, first field. The method is `THE_MATHEMATICS_REACH`'s: **throw
a field at the architecture, keep what bites, and the bounce-off is data.** The ordering lens is
`INTRODUCTION` §2's mathematician's arc; this is its **station Ⓔ**, which both that arc and R-M's own list
independently rank first — R-M: **"probably the richest untapped vein."***

---

## ⛭⛭ BROUGHT CURRENT c54.210 — **two results since c54.201 are this bake's own object, and neither was filed here**

**⌗ ⓵ `L-533` (c54.207) — THE DESCENT IS A PLANE SECTION, AND THAT IS WHY IT IS SCALE-ONLY.** *A hyperplane
section $\{X:\eta(n,X)=c\}$ of a $\mathrm{dS}_D$ of radius $\alpha$ returns $\mathrm{dS}_{D-1}$ of radius
$\sqrt{\alpha^{2}-c^{2}}$, for **every** admissible $n$ — the normal is gauge because the isometry group is
transitive on unit spacelike normals, and the offset enters only through the scale.* ⇒ ***The scale map has
RANK 1 over two choices, so a multi-step descent carries a one-parameter fibre no cut can resolve.*** ⌗ *This
is a quadric fact doing ontological work: it is why `PO-9`'s ceiling is absent for a reason rather than for
want of an argument.*

**⌗ ⓶ `L-543` (c54.210) — AND THE INVARIANTS OF THAT QUADRIC COLLAPSE THE COUNTERTERM BASIS.** *On the
maximally symmetric quadric $R_{abcd}=(1/\alpha^{2})(g_{ac}g_{bd}-g_{ad}g_{bc})$, computed at $D=4,5,6$:*
$$R=\tfrac{D(D-1)}{\alpha^{2}},\qquad R_{ab}R^{ab}=\tfrac{D(D-1)^{2}}{\alpha^{4}},\qquad
R_{abcd}R^{abcd}=\tfrac{2D(D-1)}{\alpha^{4}}$$
⇒ ***every quadratic invariant is $\mathrm{const}\times\alpha^{-4}$, so $\int\!\sqrt g\,R^{2}$,
$\int\!\sqrt g\,R_{ab}R^{ab}$, $\int\!\sqrt g\,R_{abcd}R^{abcd}$ and $\int\!\sqrt g$ are four multiples of
one functional.*** ⌗ *p0 already carried the fact — "every curvature invariant on either face is a pure power
of $1/\alpha^{2}$" — **written to show the construction cannot force a coupling**. Read at the counterterm
basis it says the construction does not need one.*

⚠ ***THE SCOPE, and it belongs in a quadric ledger because it is a statement about which quadrics:*** *the
degeneracy holds on the ONE-PARAMETER family of maximally symmetric quadrics.*

> ⛔⛔ **WITHDRAWN c54.214 — this paragraph carried `L-543`'s scope, which `L-544` retired at c54.211 and
> r2713 struck. *I withdrew the claim and left this behind.*** *It read: "**The layer is not one of
> them** — its $\sinh^{2/3}$ rate has a Ricci scalar that runs — so this bake's reach stops at the
> substrate."*
> ⇒ ***FALSE, and in the direction that understates the bake.*** *P10's free tower slices on
> $a(T)=\alpha\cosh(T/\alpha)$, whose $R=12/\alpha^{2}$ is **constant** — a member of this very
> family. And r2713 adds the identity that makes it obvious: **$12/\alpha^{2}$ and $12H^{2}$ are the
> same form with $H\to1/\alpha$, so the layer's late limit IS the tower's background.***
> ⇒⇒ **So the bake's reach does NOT stop at the substrate: the quadric family contains the geometry the
> tower is actually defined on.** *What limits it is not a running curvature but a **back-reacting**
> one — P10's own stated limit, and not a quadric question at all.*
> ⌗ ***THE RULE, 56's at r2713 and paid here: when you withdraw a claim, withdraw what it SPAWNED.*** *A
> retraction reaches the sentence that was wrong; **the successor is downstream and nothing points back
> at it**, and it survives because it reads as work rather than as a claim.* `L-547`.
>
> ⛔⛭⛭ **AND c54.215 SUPERSEDES THE LAST CLAUSE OF THIS VERY BLOCK — the rule applied to itself, one
> revision later and prospectively this time.** *`L-547` closed by saying the limit is a **back-reacting**
> background rather than a running one.* ⇒ ***It is neither.*** *The collapse of the three quadratic
> invariants is not a consequence of maximal symmetry at all: their deficit is exactly
> $C_{\mu\nu\rho\sigma}C^{\mu\nu\rho\sigma}$, **every FRW geometry is conformally flat for every
> scale factor**, and Gauss–Bonnet is an exact total derivative there — so the degeneracy holds whatever
> $a$ does, and **being an identity pointwise in $a(\cdot)$ it survives superposition and descends to the
> quantized sector as an operator relation**.* ⇒⇒ ***So back-reaction does not reach it either. What does
> is the SHEAR — the deficit IS $C^{2}$, an anisotropic shear of amplitude $\sigma$ gives
> $C^{2}=4\sigma^{2}+O(\sigma^{4})$, and the tower IS the transverse-traceless shear.*** ⌗ *And this is
> the quadric ledger's own kind of statement after all: **a degeneracy of curvature invariants that is a
> property of a CONFORMAL CLASS rather than of a symmetry group**, which is why appealing to the
> background family kept mislocating it. Registered `L-549`; the receipt is
> `L549_coupled_counterterms/Q1_…`.*

---

## ⛭ 0 · WHAT THE CORPUS ALREADY REACHES — surveyed before any bake, r1838

**This section exists because Phase 8's job is to inform Phase 7, and a bake that rediscovers the corpus's own
material and calls it a find informs nothing.**

**⌗ THE CLASSICAL PLANE/INVERSIVE CATALOGUE IS DONE, AND IT CLOSED WITH A LAW.** *p0: `rem:onecircle` **"was found
by running the classical catalogue against the throat one theorem at a time, and the boundary is where the run
stopped paying."*** *Its instruments — **the pole–polar relation · the radical axis · inversion in the throat** —
each returned **the same object: the chord of contact of a point's two null tangents.** And the closing law,
**⊢58**:*

> ***"The identity $\mathrm{pow}=x_0^2$ is not a fact about powers. It is a fact about ONE circle: the
> substrate's waist… a theorem whose quantity is a chord (Ptolemy), a bilinear relation among points (La Hire),
> or a power with respect to SOME OTHER CIRCLE (Casey) carries no height at all and returns Euclid unchanged.
> The boundary is not a defect of the reading. It is the statement that the substrate has exactly one waist, met
> in the classical language."***

**⌗ AND THE SURVEY OF THE CORPUS'S VOCABULARY, which matters because two words look like this field's and are not:**
*`polar` **×94 — and it is overwhelmingly POLARIZATION**, gravitational-wave (P11 ×42, P9 ×19), plus polar
coordinates. **Genuine pole–polar duality appears only in p0 and P3**, and only inside the closed classical run. ·
`absolute` **×26 — and NONE is Cayley–Klein's absolute.** *Checked: zero occurrences in a projective/quadric/conic
context.* · `quadric` **×2** · `projective` **×2** · `cross-ratio` **×0** · `regulus`/`reguli` **×0** ·
`self-polar` **×0** · `pencil` **×0**.*
***So the field's own apparatus is almost entirely absent from the corpus, while the ONE part of it that was run —
plane polarity with respect to the throat circle — is closed and bounded by its own law.***

---

## ⛭ 1 · THE OPENING QUESTION, and it comes from ⊢58 rather than from outside

***The classical run worked with respect to THE THROAT CIRCLE, in the equatorial projection. ⊢58 says that is why
it is blind to every other circle.***

**A projective-quadric bake works with respect to THE QUADRIC ITSELF** — the hyperboloid
$-X_0^2+X_1^2+\dots+X_4^2=\alpha^2$ in $\mathbb{P}^5$, not one circle drawn on its shadow. **That is a different
polarity, a different duality, and a different invariant theory.**

> **THE QUESTION: does polarity with respect to the dS QUADRIC reach what the throat protocol is blind to — and
> if so, is what it reaches structure the corpus already has under another name, or new?**

*Stated as a question with a real negative answer available: **if every quadric-polar construction collapses onto
the throat's chord of contact, ⊢58 extends from the plane to the projective setting and that is a strengthening
of a corpus law, not a failure of the bake.***

---

## ⛭ 2 · THE PROBES — a living list, each to be run and its verdict recorded here

*Verdicts: **BITES** (returns structure the corpus lacks) · **CONFIRMS** (returns what the corpus has, from a new
direction — which is a `[reach]`-grade result, not nothing) · **BOUNCES** (returns nothing — and the bounce is
data about the substrate, per the method).*

| # | probe | why it is worth throwing | verdict |
|---|---|---|---|
| **⌫ Q1 ✔ CONFIRMS (r1839)** | **Polarity w.r.t. the quadric.** The dS quadric induces a polarity $\mathbb{P}^5\to(\mathbb{P}^5)^*$: point ↔ hyperplane, via $X\mapsto \eta(X,\cdot)$. **What are the corpus's named points' polars?** — the throat, the hinges at $2\alpha$, the Nariai locus, $r=0$ | **the corpus has no polarity except the throat-circle one, and this one is signature-carrying by construction** — $\eta$ IS the embedding form | ***CONFIRMS — and it extends a corpus law rather than adding one.*** *Polarity w.r.t. the quadric depends on **one** quantity, $\eta(P,P)$ against $\alpha^2$. Classifying the corpus's named points: **the throat ON · the HINGES ON** ($-3\alpha^2+4\alpha^2=\alpha^2$ — each hinge is a substrate point, so its polar is just the tangent hyperplane there) · the hinge-axis foot **OUTSIDE**, $\mathrm{pow}=3\alpha^2$ · the Nariai radius point and $r=0$ **INSIDE**.* **And the one relation that looked new — the tangent length from the hinge-axis foot being $\sqrt3\alpha$, equal to the hinge's own height — is `eq:powerisheight` evaluated at the hinge**: *on the quadric $\mathrm{pow}_{\rm throat}=X_0^2$, so transverse $2\alpha$ forces $X_0^2=3\alpha^2$.* ***So ⊢58's bound EXTENDS from the plane to the projective setting: the quadric's polarity carries no height the throat's power does not already carry.*** `storyboard_receipts/Q1_quadric_polarity.py` |
| **⌫ Q2 ✔ CONFIRMS + a PROPAGATION FIND (r1840)** | **The two reguli.** A one-sheeted quadric carries **two families of rulings**, and CR's substrate is one, with the two rulings **already load-bearing** (P8: *"the synchronous space IS the second ruling"*). **Projective quadric theory says exactly what the two families are and how they intersect.** Does its apparatus — regulus, transversal, the $(1,1)$ correspondence — say anything about $A$/$B$ the corpus does not? | **the rulings are the corpus's most-used structure and the field that owns them has never been asked** | ***CONFIRMS on the mathematics — and the baseline read turned up a live propagation defect, which is the more valuable half.*** **The classical classification is already in two papers:** P13 — *"the distinction is not which factor exchanges the rulings — **every orientation-reversing isometry does that**, so the exchange itself distinguishes nothing — but what the exchanged structure IS"* — and P12: *"restricted to the ruled section, an orientation-reversing isometry swaps the two rulings while an orientation-preserving one fixes each."* *So the probe I had planned — classify the corpus's involutions by ruling-exchange — is answered, and answered better than the classification would have been, because the corpus draws the consequence the classification does not: **the exchange is generic and therefore distinguishes nothing.*** **⛔ THE FIND: three documents still carried the A/B sheet-to-ruling assignment as a LIVE do-not-assert** — `ONTOLOGY_FOUNDATION_INDEX` (twice, including in a count of *"the two genuinely-open items"*), `OPEN_PROBLEMS_MAP`, and `THE_PLAN`'s quoted frontiers list — **while the family was KILLED at r1280, P7 was rewritten, and P7's own `§frontiers` now has ZERO mentions of it.** ***All three corrected.*** |
| **⌫ Q3 ✔ RUN r1847 — CONFIRMS, WITH A MECHANISM. The strongest kind.** | **Cayley–Klein.** A quadric in $\mathbb{P}^n$ **defines a metric** — it is *the absolute*, and dS is a Cayley–Klein geometry by construction. **The corpus never uses the word.** Does the CK reading derive $\alpha$, the signature, or the null structure as projective data rather than metric data? | ***the corpus's central move is "the metric is read off a fixed quadric" and CK is the classical theory of exactly that*** | ***COMPUTED, and exact:*** **the substrate's geodesic separation IS the Cayley–Klein log-cross-ratio against the absolute $\{\eta(X,X)=0\}$, with CK constant $k=\alpha$ exactly.** *Timelike: $\tau=(\alpha/2)|\log \mathrm{CR}|$ — **every rapidity, every $\alpha$, ratio $0.50000000$.** Spacelike: $\sigma=(\alpha/2)|\arg \mathrm{CR}|$ — exact below $\pi\alpha/2$, **where the principal branch wraps, stated as a bound.** And the discriminant $4\alpha^4(u^2-1)$, $u:=\eta(P,Q)/\alpha^2$, decides which: $|u|>1$ the line **meets** the absolute (real log), $|u|<1$ it **misses** (an angle).* ***SO THE CORPUS'S CENTRAL MOVE — "the metric is read off a fixed quadric" — is Cayley–Klein exactly, and $\alpha$ is the CK scale constant.*** **⛭ AND WHAT THAT BUYS, at traced weight: in the CK construction THE SCALE IS THE ONLY FREE CONSTANT** — signature, null structure and isometry group are all fixed by the absolute's projective type. ***So "one scale, no free dimensionless constants" — which the corpus reached by an adversarial hunt returning "an audited exhaustive zero" — is the classical statement about a quadric geometry.*** **The corpus got it by exhaustion; CK says it structurally.** `storyboard_receipts/Q3_cayley_klein.py` |
| **⌫ Q4 ✔ RUN r1850 — YES, and it is the EQUIANHARMONIC value.**  **⌗ r2376+c54.11 — RE-RUN AT GENERAL $D$, AND THIS ROW'S OWN SELF-DISCOUNT IS TOO STRONG.** *A cross-ratio is an invariant of **four** points, and the vantage set has $(D-1)+1=D$ of them — so it is a cross-ratio configuration **precisely at $D=4$**. Where a four-point subset exists at $D=5$ it is **harmonic** ($\lambda=2$, $j=1728$, CM by $i$), **not** equianharmonic, so the chain to the corpus's own $\mathbb{Z}_3$ points the other way there. The $120^\circ$ is automatic **given three** — and the three is $D-1$.* ⚠ *Not a second vote for $D=4$: the same fold read projectively.* `P03_reach_probe_deepening` | **Cross-ratio.** The projective invariant of four collinear points. **The horizon cubic has three roots and the slicing designates one** — four points on a line appear whenever a ruling meets the roots. **Is there a cross-ratio that is constant across the vantages?** | **a projective invariant constant under the groupoid would be an invariant the corpus does not have** | ***THERE IS ONE.*** *`Q3` already showed the metric IS a cross-ratio; this is the other half. **On the r-line the three roots with $r=0$ give no invariant** — the cross-ratio varies with $r_0$ (and degenerates to $1$ at Nariai, where two roots merge). **But the $120^\circ$ structure lives in the SKY ANGLE:** P3's three roots are the three preimages of $3w$ under the sine, i.e. $w,\,w{+}120^\circ,\,w{+}240^\circ$.* ***Their cross-ratio with the centre is $\lambda=\tfrac12+\tfrac{\sqrt3}{2}i=e^{i\pi/3}$, CONSTANT in $w$, satisfying $\lambda^2-\lambda+1=0$ exactly — the EQUIANHARMONIC value.*** **⛭ WHY THAT IS THE ANSWER TO THE QUESTION ASKED:** *$e^{i\pi/3}$ is **the unique cross-ratio fixed by the full $S_3$ action** — the six-value orbit $\{\lambda,1/\lambda,1-\lambda,\dots\}$ **collapses to two points, verified**, so the value is invariant under every permutation up to conjugation. **The vantage groupoid's $S_3$ permutes the three roots and this cross-ratio does not move.*** *And equianharmonic $\iff j=0 \iff$ **complex multiplication by $\omega=e^{2\pi i/3}$** — the corpus's own $\mathbb{Z}_3$.* **⚠ HONEST WEIGHT: three points $120^\circ$ apart with their centre are equianharmonic AUTOMATICALLY.** *The content is not the value but that CR's vantages **are** at $120^\circ$, which P3 forces by the triple-angle identity. **This names a consequence of a forced fact and does not add one** — and the corpus has `equianharmonic` ×0, `j-invariant` ×0, `complex multiplication` ×0.* `storyboard_receipts/Q4_equianharmonic_vantages.py` |
| **⌫ Q5 ✔ RUN r1849 — the PENCIL premise is WRONG, and the probe BITES anyway.** | **~~Self-polar simplices and the pencil.~~** SdS is a one-parameter family; **a pencil of quadrics has a classical invariant theory** (Segre characteristic, the $\lambda$-discriminant). **The horizon cubic's discriminant is already load-bearing — Nariai is its double root.** Is the cubic the pencil's characteristic polynomial? | ***THE PREMISE IS WRONG: the horizon locus is not a pencil.*** *A pencil is a one-parameter family $\lambda C_1+\mu C_2$; **the locus is ONE cubic curve in the $(r_0,r)$-plane that is REDUCIBLE** — a **line** $r=r_0$ union a **conic** $r^2+rr_0+r_0^2=1$, which P3 gives as `prop:locus`.* ***AND THE REDUCIBLE CUBIC'S OWN THEORY BITES WHERE THE PENCIL'S DOES NOT: its SINGULAR POINTS are where the components meet, and solving line $\cap$ conic gives $3r_0^2=1$, i.e. $r_0=\pm1/\sqrt3$ — THE TWO NARIAI CONFIGURATIONS***, with roots $(-2/\sqrt3,\,1/\sqrt3,\,1/\sqrt3)$: the merged pair and the backward-radial third. **⛭ AND P3 COMPUTED THE SECOND CHARACTERISATION IN ITS OWN PROOF WITHOUT DRAWING IT:** *`prop:locus` finds eigenvalues $1/2,3/2$ with the larger giving **the shorter semi-axis $\sqrt{2/3}$ along the diagonal $r=r_0$** — **and that diagonal IS the locus's line component.** $\sqrt{2/3}\cdot(1,1)/\sqrt2=(1/\sqrt3,1/\sqrt3)$ exactly.* ***So Nariai has three characterisations and the corpus states one:*** **(i) ALGEBRAIC** — the cubic's double root, $\sigma$'s fixed point *(in P3)* · **(ii) INCIDENCE** — the reducible cubic's singular point, where line meets conic *(new)* · **(iii) METRIC** — the endpoint of the fundamental ellipse's MINOR AXIS *(new, and P3 computes $\sqrt{2/3}$ itself)*. `storyboard_receipts/Q5_nariai_on_the_locus.py` |
| **⌫ Q6 ✔ RUN r1851 — (A) CONFIRMS · (B) opens a stated gap in the ladder.** | **Conjugate points and the tangent cone.** p0 has *"the tangent to the throat is NULL"* and the power-of-a-point. **Projectively, the tangent cone from a point is the polar hyperplane's contact locus.** Does the corpus's null-tangent result generalise off the equatorial plane? | **⊢58's boundary is stated for the PROJECTION; this asks whether it holds in the full space** | ***(A) IT HOLDS EVERYWHERE.*** *The tangent from **any** substrate point to the throat is null — verified at random non-equatorial points — **because it IS the substrate equation**: $\mathrm{pow}_{\rm throat}(P)=|\vec P|^2-\alpha^2 = P_0^2$ is `eq:powerisheight`, and the tangent length equals $|P_0|$ exactly.* **So ⊢58's bound is not a projection artefact — with `Q1`, the bound now holds for the quadric's polarity and off the equatorial plane alike.** ***(B) AND THE POLAR OF A SUBSTRATE POINT CUTS THE SUBSTRATE IN A $\mathrm{dS}_4$.*** *For $P$ on the substrate $\eta(P,P)=\alpha^2>0$, so $P^\perp$ has signature $(1,4)$ — **verified numerically at four sample points** — and $\{X\in P^\perp:\eta(X,X)=\alpha^2\}$ is a totally geodesic $\mathrm{dS}_4$.* **⚠ WHETHER THAT $\mathrm{dS}_4$ IS THE BACKGROUND RUNG IS OPEN, and the reason is a gap: the corpus states the ladder (substrate → background → exact solutions) and **never states how the background SITS IN the substrate.** P7 works with *"the waist 3-sphere of $\mathrm{dS}_4$"* and *"the $S^3$ that fills the upper sheet of $\mathrm{dS}_4$"*, but no paper gives the embedding.* ***★ A18 CLOSED r1878 BY CONFIRMATION ①.*** *The polar slice's isometry is $\mathrm{SO}(4,1)$, a **sweep-subgroup** of $\mathrm{SO}(5,1)$ — which is §0's own criterion for a four-geometry being a **cut** — and being the maximally symmetric such cut it **is** the background. ***So polarity is how the background sits in the substrate: one background per substrate point, none privileged, the point a slice is polar to being what selects it.*** **The gap was real — the ladder stated here, the embedding stated nowhere — and reading the rung ladder as the confirmation demands is what closed it.** `storyboard_receipts/Q6r_polar_is_the_background.py` |

**⌗ AND THE STANDING DISCIPLINE FOR EVERY PROBE, which is the whole reason this ledger exists:**
*① **Check the corpus first** — §0 above is the baseline, and any probe that returns something must be checked
against it before being called a find. ② **The bounce is data** — a probe that returns nothing says something
about the substrate and is recorded, not deleted. ③ **`GEOMETRY_PHYSICS_TAXONOMY`'s numerology-vs-teeth
discriminator adjudicates every BITES verdict** — a shared shape is not a mechanism. ④ **Every computed result
gets a receipt, registered, checker both sides** — Phase 9 step ⑤, which three separate findings say is the step
that gets skipped.*

---

## ⛭ 2b · THE NEAR-MISS ON Q1 — recorded because it is the field's characteristic trap

**The first pass computed the polar hyperplane's *Euclidean* distance from the origin and got $1/\sqrt7 =
0.37796$ for the hinge.** *A clean-looking number, in a corpus where $\sqrt7/2$ already appears as P3's charting
distance.*

***It is a gauge artifact.*** **The polar of $P$ is $\{X:\eta(P,X)=\alpha^2\}$, and in a Minkowski space the
Euclidean norm of its normal covector is not an invariant** — it depends on a coordinate choice the geometry does
not make. *The only invariant polarity depends on is $\eta(P,P)$ against $\alpha^2$.*

**⛔ AND THIS IS THE TRAP THIS FIELD BRINGS, so it is written at the top of the ledger rather than buried:**
*projective quadric geometry is **conformal/affine by nature** and its classical formulae are written for
**Euclidean or complex-projective** settings. ***Every length, distance and ratio it offers must be checked for
invariance under the substrate's own group before it is read as a result.*** **A number that is clean in
coordinates and not invariant is exactly what `GEOMETRY_PHYSICS_TAXONOMY`'s numerology verdict is for.**

## ⛭ 2c · WHAT Q3 THREW OFF — a refinement to `Q1`, and a bound on the whole import  

> **⌗ r2376+c54.13 — THE ANTIPODAL QUESTION, ANSWERED AND KILLED.** *c54.11 asked whether the antipodal $\mathbb{Z}_2$ this section refuses and the offset parity are one $\mathbb{Z}_2$ read at two levels.* ***They are not.*** *Computed in the ambient: the antipodal map has $\det=+1$ and **reverses** $X_0$; the offset parity $\mathrm{diag}(1,1,-1,1,1,1)$ has $\det=-1$ and **fixes** $X_0$ — separated by determinant **and** by their action on time. **And the kill is informative**: the antipodal map is exactly $T$ composed with spatial inversion, so it sits on $T$'s side of the corpus's own $T$/$R$ line ⇒ **this section's orientation argument is the TIME orientation, while the parity that grades chirality is the SPATIAL one.** Two orientation $\mathbb{Z}_2$'s; a tempting weld refused.* `P03_step3_refused`

**⌗ THE REFINEMENT TO `Q1`: two polarities, not one.** *`Q1` computed polarity w.r.t. **the substrate quadric**
$\eta(X,X)=\alpha^2$ — an **affine** relation, $\langle P,X\rangle=\alpha^2$, a hyperplane not through the origin.
**Cayley–Klein's polarity is w.r.t. the ABSOLUTE** $\eta(X,X)=0$ — a **linear** one, $\langle P,X\rangle=0$, the
$\eta$-orthogonal complement, and it is the genuine projective pole–polar duality.* ***And it yields the classical
name: exactly the NULL points are self-conjugate*** — *"the absolute is the locus of self-conjugate points"*,
which here reads **the null cone is the self-conjugate locus of the substrate's polarity.** ***`Q1`'s verdict
stands (⊢58 extends), and it was computed on the affine relation; this is the linear one, and it agrees.***

**⛔ AND THE BOUND ON THE IMPORT, which the identification forces and the corpus already answers.**
*A Cayley–Klein geometry's isometry group is the **projective** stabiliser of the absolute, $\mathrm{PO}(5,1)$ —
**and projective means antipodes are identified**, $X\sim-X$. **CR does not identify them, and the reason is
physical.*** *P7: the unoriented bifurcate horizon **"is shared by $p$ and its antipode $-p$ and so corresponds
to the axis $\{p,-p\}$; but the oriented future horizon $\mathcal H_c^{+}(p)$"** distinguishes them, and the map
$p\mapsto\mathcal H_c^{+}(p)$ **is a bijection precisely because orientation distinguishes $p$ from $-p$** — which
is what makes *"the $S^3$ family of reassigned horizons IS the fundamental congruence"* work.*
***So: the substrate is $\mathrm{dS}_5$, the DOUBLE COVER, and not the elliptic quotient Cayley–Klein's group acts
on. The metric identity holds; the group does not import unchanged.*** **A field bake's honest output is not
"the field applies" but "the field applies THIS far," and this is where it stops.**

## ★★★ 2d · THE BAKE'S CLOSE — six probes, six verdicts, and what the field actually gave

***Not one probe produced a new physical fact. Every one produced something the corpus did not have.***

| probe | verdict | what it gave |
|---|---|---|
| **Q1** polarity | **CONFIRMS** | **extended a corpus law** — ⊢58 from the plane to the projective setting |
| **Q2** the two reguli | **CONFIRMS** | the mathematics was already in two papers, **and the baseline read found a dead item live in three documents** |
| **Q3** Cayley–Klein | ***CONFIRMS + MECHANISM*** | **the metric IS a CK log-cross-ratio, $k=\alpha$ exactly** — so *"one scale, no free dimensionless constants"*, held by exhaustion, becomes **the classical statement about a quadric geometry** |
| **Q4** cross-ratio | ***NAMES A GROUP INVARIANT*** | the vantage $S_3$ has a projective invariant, **the equianharmonic $e^{i\pi/3}$** — and equianharmonic $\iff j=0 \iff$ CM by $\omega$, the corpus's own $\mathbb{Z}_3$ |
| **Q5** the pencil | **PREMISE WRONG, LANDED ANYWAY** | the locus is a **reducible cubic**, and **its singular points are the two Nariai** — with the metric route sitting unread in `prop:locus`'s own proof |
| **Q6** conjugate points | **CONFIRMS + AN OPEN GAP** | the null tangent holds **everywhere**; **and the polar of a substrate point IS the $\mathrm{dS}_4$ background** — `A18` closed r1878 |

**⛭ AND THE BOUND ON THE WHOLE IMPORT, from §2c: the METRIC imports exactly; the GROUP does not.** *CK's isometry
group is the projective $\mathrm{PO}(5,1)$, which identifies antipodes — **and CR does not, because orientation
distinguishes $p$ from $-p$**, which is what makes P7's bijection work. **The substrate is the double cover.***

**⌗ AND THE OBSERVATION THAT FRAMES THE WHOLE BAKE — Daryl's, checked.** *He called it **"conic sections of a dS
substrate"** long ago and asked why nobody said *quadrics*. **They did.** P8: **"the horizon locus comes out a
conic: it is the shadow of a planar cut of a quadric… the conic the horizon locus traces and the cone the cuts
are read against are the same quadric seen twice."** And `THE_MATHEMATICS_REACH` listed projective quadric
geometry as an untapped **candidate**.* ***`quadric` ×116 programme-wide — and 84 of them are from this bake.***
***A connection can be named, and correct, and still import none of its field's theorems.*** **And the reason is
SEQUENCING, not oversight** *(Daryl, r1852)*: ***you cannot import a field's theorems until there is a worked
application to import them INTO. Taking the general theorems on their own and imagining they could tell you
something is exactly the move that produces nothing.*** **The Euclid protocol is the precedent — its whole value
was in building the theorems UNDER this application, which could not be done until the application existed, and
⊢58 is what that produced.** *So the bake could not have run at r300, or r1000. It runs now because the
application is worked out.*

**⌗ THE PRECEDENT, and it sets the form.** *`FIGURE_THEOREM_LEDGER` is what running the CLASSICAL PLANE
catalogue against the throat produced: **34 distinct $\vdash$-numbered results**, each earned against the
application rather than imported into it, and the run's closing law is **⊢58** — the statement of exactly how far
that field reaches.* ***This ledger is the same operation on a different field, and it is buildable for the same
reason: the application is worked out. It was not at r300, and it was not at r1838 either — every row below
carries a bound only the six probes could supply.***

## ★★★ 4 · THE THEOREMS, UNDER THIS APPLICATION — what imports, with its bound; what does not, with its reason

*Opened r1852, Daryl-directed: **"the value is in building them under this application, which can't be done till
this application is worked out. And that is where we are."** Buildable now and not at r1838, because each row
below carries a bound that only the six probes could supply.*

### ⛭ 4a · IMPORTED, AND EXACT

| the theorem | what it says | under CR | bound |
|---|---|---|---|
| **The Cayley–Klein metric** | a quadric absolute $\Omega$ induces a metric on its exterior, $d=(k/2)\lvert\log\mathrm{CR}\rvert$ against $\Omega$ | ***the substrate's geodesic separation IS this, with $k=\alpha$ exactly*** — timelike the real log, spacelike the angle, and $4\alpha^4(u^2-1)$ deciding which | **spacelike branch wraps past $\pi\alpha/2$** · `Q3_cayley_klein` |
| **The scale is the construction's only free constant** | signature, null structure and isometry group are fixed by the absolute's projective type; $k$ alone is free | ***this is the mechanism for "one scale, no free dimensionless constants"*** — the corpus held it by an adversarial audit returning "an exhaustive zero"; CK says it structurally | **TRACED, not computed** — standard CK theory · landed in p0 |
| **The absolute is the locus of self-conjugate points** | $P$ lies on its own polar iff $\eta(P,P)=0$ | ***the null cone is the self-conjugate locus of the substrate's polarity*** | computed, `Q1`/§2c |
| **Reducible plane cubics: the singular points are where the components meet** | a line $\cup$ conic is singular exactly at their intersections | ***the two Nariai configurations are the horizon locus's singular points*** — and, equivalently, the ends of the fundamental ellipse's minor axis | **adds no new value; three routes to one point** · `Q5_nariai_on_the_locus`, landed as P3 `cor:nariai-locus` |
| **The anharmonic group and the equianharmonic value** | permuting four points sends the cross-ratio through a six-element orbit, which collapses only at $\lambda^2-\lambda+1=0$ | ***the vantage $S_3$ has a projective invariant, $e^{i\pi/3}$*** — and equianharmonic $\iff j=0 \iff$ CM by $\omega$, the deck action's own order three | **automatic once the vantages are $120^\circ$ apart, which P3 forces** · `Q4_equianharmonic_vantages`, landed as P5 `rem:equianharmonic` |

### ⛭ 4b · IMPORTED, AND BOUNDED BY A CORPUS LAW

| the theorem | under CR | the bound |
|---|---|---|
| **Pole–polar duality w.r.t. a quadric** | applies, and the substrate's named points classify by $\eta(P,P)$ against $\alpha^2$ — **throat and hinges ON, the hinge-axis foot outside, Nariai and $r=0$ inside** | ***⊢58: it carries no height the throat's power does not already carry.*** The bound was stated for the projection and **now holds for the quadric's polarity and off the equatorial plane alike** (`Q1`, `Q6`) |
| **Totally geodesic polar slices** | the polar of a substrate point cuts it in a $\mathrm{dS}_4$ | ***✔ IT IS THE BACKGROUND — A18 CLOSED r1878.*** $\mathrm{SO}(4,1)<\mathrm{SO}(5,1)$ is a sweep-subgroup, so the slice is a **cut**, and the maximally symmetric one |

### ⛔ 4c · DOES NOT IMPORT, AND THE REASON

| the theorem | why not |
|---|---|
| **The isometry group is the PROJECTIVE stabiliser $\mathrm{PO}(5,1)$** | ***projective identifies antipodes, $X\sim-X$, and CR does not.*** P7: the unoriented bifurcate horizon is shared by $p$ and $-p$, **but the ORIENTED future horizon distinguishes them**, and $p\mapsto\mathcal H^+_c(p)$ is a bijection precisely because of it. **The substrate is the double cover.** *So every CK theorem that quantifies over the isometry group must be checked on $\mathrm{O}(5,1)$, not its projective quotient.* |
| **The two reguli distinguish structure** | *they do not — P13: **"every orientation-reversing isometry exchanges the rulings, so the exchange itself distinguishes nothing."*** The corpus's live distinction is `on` versus `tangent`, which is finer |

### ⟐ 4d · UNTRIED — the queue, and each is a probe not a hope  

> **⌗ r2376+c54.11 — RE-RUN AT GENERAL $D$ — THE POLAR-SIMPLEX ENTRY IS ANSWERED, and the question turns out to be dimension-bound.** *"CR has three hinges and a centre; is that a polar simplex?"* ***NO*** *— the polar of a hinge sits at $\alpha/2$ and the opposite side sits at the inradius $\alpha$ (exact). **A computed negative.** And a simplex in the plane needs **three** points, so with $D-1$ hinges the question **only exists at $D=4$.** Both halves are new.* `P03_reach_probe_deepening`

*• ***`D3` ✔ RUN r1887 — SEGRE BOUNCES, AND THE BOUNCE HAS A REASON.*** *Segre classifies a **pencil** of quadrics, so the probe needed an honest answer to the queue's own question first. **Checked three candidate pairs:** the substrate with the absolute — *homogenised, the pencil's second generator is a rank-one double hyperplane; that is not a Segre problem, it is the Cayley–Klein setup `Q3` already identified* · the horizon locus — *a reducible cubic, line times conic, not a pencil; `Q5` already read its singular points* · **the throat and the hinge circumcircle** — *a genuine pencil, and computed: $\det(A+\lambda B)=-(\lambda+1)^2(4\lambda+1)$, degenerate at $\lambda=-1$ (**rank 1, the doubled line at infinity**) and $\lambda=-\tfrac14$ (rank 2, the isotropic lines through the common centre) — **the classical signature of CONCENTRIC circles.*** ***So it returns the concentricity the construction started from.*** **★ AND THE REASON IS THE RESULT: every conic the corpus owns is built FROM THE ONE THROAT — the throat ($\alpha$), the hinge circumcircle ($2\alpha$, itself an output of the throat), the contact triangle's circumcircle (the throat again), its incircle ($\alpha/2$). All concentric, all coaxial.** ***The corpus has no pencil in general position and CANNOT: a second quadric independent of the first would be a second scale, and the construction has one.*** **Segre classifies the relative position of two quadrics; where the second is manufactured from the first, the classification returns the manufacture — which is `Q3`'s "the scale is the only free constant a Cayley–Klein construction carries", met from the pencil side.** `storyboard_receipts/D3_segre_no_pencil.py`• ***`D1` ✔ RUN r1885 — PROJECTIVE DUALITY, and the corpus had the dual figure drawn from the beginning.*** *Duality w.r.t. the throat sends the **pencil of doors through a hinge** to the **range of points on the hinge's polar**. The hinge sits at $2\alpha$, so its polar is the line at $\alpha/2$ — **and that is the OPPOSITE SIDE OF THE CONTACT TRIANGLE**, whose vertices are the three tangency points `prop:twoalpha`(iii) already computes as the sides' midpoints.* **THE DICTIONARY: hinge ↔ opposite side of the contact triangle · the swing ↔ a point sliding along it · the hinge triangle ↔ the contact triangle · the throat ↔ itself, being the conic of the duality · and the throat-tangent door is self-conjugate, its pole its own point of contact.** ***The corpus read those three points only as midpoints; they are the dual figure.*** `storyboard_receipts/D1_hinge_duality.py`
• ***`D2` ✔ RUN r1886 — THE KLEIN CORRESPONDENCE, and it explains the hexagon's LENGTH.*** **⌗ CONFIRMATION ④ FIRST, and it mattered: the queue said "CR has a $\mathbb{P}^5$" — but Klein's $\mathbb{P}^5$ is the Plücker space of lines in a 3-space and the corpus's is the projectivised ambient of $\mathrm{dS}_5$. TWO DIFFERENT $\mathbb{P}^5$s, a coincidence of dimension, recorded before the probe so it could not trade on it.** *What Klein does apply to: the equatorial hyperboloid's rulings ARE lines in $\mathbb{P}^3$.* ***Carrying P3's skew hexagon over: each edge meets three others — its two neighbours AND ITS OPPOSITE — and the six split into two triples within which no two meet. That is the two ruling families: same family skew, opposite families meeting.*** **So the alternation is forced, and with it the length — a closed circuit alternating between the families must be of EVEN length, and six is the least that visits all six hinges. P3 draws the hexagon; this says why a hexagon is the only closed figure those rulings can make.** ***★ AND THE THREE OPPOSITE-EDGE CROSSINGS LAND AT $X_0=0$, ON THE THROAT, AT THE SAME THREE POINTS `D1` FOUND.*** *So one triple wears three faces: **the tangency points of the projected hinge triangle** (`prop:twoalpha`(iii), the corpus's) · **the vertices of the dual figure** (`D1`) · **the crossings of the hexagon's opposite edges** (`D2`). The corpus had the first and read it as a set of midpoints.* `storyboard_receipts/D2_hexagon_klein.py`• **Confocal families** — quadrics sharing an absolute. **The corpus has one $\alpha$; a confocal family would be a family of $\alpha$'s, which P5 already calls "a continuous homothety."** **⚠ *object corrected r2376+c54.19 (Daryl): the hinge is the **line** the door swings on — **three** hinges, each piercing the substrate once per horn, so the six are 3×2 **ends**. The statement here is unaffected; the counting is.* (`P03_hexagon_null_triple`)**
• **The polar simplex** — a self-polar configuration. **CR has three hinges and a centre; is that a polar simplex?***

## ★★ 3a · THE CLOSING STEP — run `scripts/check_bibliography.sh` (added r1862, Daryl-directed)

***A field bake imports named classical results, and each one owes a source at the moment it lands.***
*Daryl: **"are you being careful to add references when you bake such things in… at the end of each field's bake
is maybe more prudent?"** He was right that it was in arrears.*

***AND THE CLOSE HAS FOUR PARTS (r1865–r1866, Daryl):*** **① `check_bibliography.sh` · ② the theorems ledger in its four parts · ③ THE CENSUS PASS — which of `THE_DISSOLUTION_CENSUS`'s six clusters each result enriches, and the landing in that cluster's home · ④ THE P6 PASS — does any result move a claim from PERMIT toward REQUIRE under `R2`, and if none does, SAY SO.** ***The two passes are complements: the census counts what a result dissolves, P6 weighs whether the credit moved.*** *A result that reaches a paper and a ledger but not the census is a result the corpus's own synthesis does not know about; a bake whose R2 yield is unrecorded is a bake whose standing is unassessed.* *(superseding:)* **① `check_bibliography.sh` · ② the theorems ledger in its four parts · ③ THE CENSUS PASS — for each result, which of `THE_DISSOLUTION_CENSUS`'s six clusters it enriches, and the landing in that cluster's home.** ***A result that reaches a paper and a ledger but not the census is a result the corpus's own synthesis does not know about.***

**The check runs three things:** *① every `\cite` key resolves to a `\bibitem` in its own file · ② no duplicate
bibitems · ③ **every named classical result has a source somewhere in the paper that uses it.*** **Exit 0 is
clean, and it is a closing step of every bake from here.**

## ⛭ 3 · WHAT LANDS WHERE — the Phase 9 routing, stated before any result exists

*Per Phase 9: a result names what was owed it, walks the grain chain downward, lands in the paper that carried
the gap, trips the standing checks, and gets receipted.*

**The papers this field touches by construction, so a result knows its home before it arrives:**
*• **p0** — `sec:power`, `rem:onecircle`, ⊢58's law. **Any extension or bounding of the classical run lands here.**
• **P3** — the throat, the hinges, the slicing curve's geometry. **Anything about the equatorial projection.**
• **P5** — the description groupoid. **Anything invariant across vantages** (Q4 especially).
• **P12** — the algebroid, $\mathfrak{so}(5,1)$ acting on the quadric. **Anything about the group's action.**
• **P9** — the range and the two rulings. **Q2's home if it bites.**
• `ONTOLOGY_FOUNDATION_INDEX` §0 — **any new symbol** (the field brings its own: polarity, regulus, cross-ratio).*


## ⛭⛭⛭ REACH CLOSE — all seventeen papers WORKED or CHECKED-NEGATIVE by name (r3512)

> ⛔ *The r3500 whole-set measurement put this field at **29%** (5 papers). Read in full this pass (four
> sweeps, `polar` split hard from **polarization** — P11 ×34, P09 ×14, P14 ×14 all the graviton/wave
> polarization — and `pole`/`cone`/`absolute` context-screened). **The reach is `11/17` — the widest of any
> field so far — because ONE object underlies the corpus: the dS substrate as a doubly-ruled hyperboloid
> whose horizon cubic factors into a line and the fundamental ellipse.***

**⌘ THE ONE OBJECT, READ IN ELEVEN PAPERS.** *`p0` gives the Cayley–Klein absolute + log-cross-ratio metric
($k=\alpha$), pole–polar = background, embedding-quadric polarity, Segre/pencil degeneracy = concentricity,
the confocal-family theorem's structural failure, quadric-cut-by-linear-space; `P07` the explicit
one-sheeted hyperboloid $R^2/a^2-X_0^2/b^2=1$ doubly ruled with ruling-crossing $\cos\phi=(a^2-b^2)/(a^2+b^2)$
(perpendicular at $a=b$) and the cubic-family discriminant $\Delta(E)$; `P08` the level-set trichotomy
$S^3/\text{flat}/H^3$; `P03` the ellipse-by-eigenvalues, discriminant $4-3r_0^2$ trichotomy, foci
(chart-flagged), Klein-quadric conjugacy, gnomonic projection; `P05` the equianharmonic cross-ratio and the
horizon-cubic discriminant $4-27(2M)^2$; `P02` the interior circle $(r-M)^2+s^2=M^2$ continuing to the
hyperbola; `P06` the Cayley–Klein absolute reading; `P09` the doubly-ruled → complete Petrov interval and
$I^3-27J^2$; `P12`/`P13`/`P14` the reducible cubic $(r-r_0)(r^2+rr_0+r_0^2-1)$ = line + fundamental ellipse,
the Nariai double root of the discriminant, and (`P14`) the dimension-indexed conic reducibility.*

| paper | status | where |
|---|---|---|
| `P01` | **CHECKED-NEGATIVE** | `null generators` = Schwarzschild horizon; `cone` = light cone; `absolute` ≠ CK absolute |
| `P02` | **WORKED** | the interior conic — circle $(r-M)^2+s^2=M^2$ continuing to the hyperbola; **`Q7` (new receipt)** |
| `P03` | **WORKED** | `Q4`/`Q5` + ellipse by eigenvalues, discriminant $4-3r_0^2$, foci, Klein quadric, gnomonic (`P03_cubic_factor_ellipse_locus`, `D2_hexagon_klein`, `P03_triple_angle_gnomonic`) |
| `P04` | **CHECKED-NEGATIVE** | `absolute` = astronomical magnitude/parallax |
| `P05` | **WORKED** | `Q4` equianharmonic cross-ratio; horizon-cubic discriminant $4-27(2M)^2$ (`Q4_equianharmonic_vantages`) |
| `P06` | **WORKED** | the Cayley–Klein absolute fixes signature/null/isometry; scale the sole free constant |
| `P07` | **WORKED** | one-sheeted hyperboloid ruling formulae; cubic-family discriminant $\Delta(E)$ (`P07_nariai_selection`, `L8_the_pencil`) |
| `P08` | **WORKED** | doubly-ruled hyperboloid; level-set trichotomy $S^3$/flat/$H^3$ (`P08_synchronous_horosphere`, `P08_trichotomy`) |
| `P09` | **WORKED** | doubly-ruled → Petrov interval complete; speciality discriminant $I^3-27J^2$ (`P1_the_petrov_interval...`, `P09_bianchiI_typeI`) |
| `P10` | **CHECKED-NEGATIVE** | `absolute` = absolute time; `polar` = polar coords/polarization |
| `P11` | **CHECKED-NEGATIVE** | `polar` ×34 = graviton polarization; `null generators` = Killing-horizon |
| `P12` | **WORKED** | $\dS_5$ hyperboloid; discriminant $-4\alpha^4(27M^2-\alpha^2)$; reducible cubic = line + fundamental ellipse (`K8_orbit_type_filtration`; ellipse cites `Q5`/`P03`) |
| `P13` | **WORKED** | fundamental ellipse realizes the $A_2$ quadratic form; discriminant $4-3r_0^2$ at Nariai |
| `P14` | **WORKED** | dimension-indexed conic reducibility ($D{=}5$ line+circle, $D{=}4$ genuine conic, $D{=}6,8$ irreducible); $45°$ tilted ellipse (`P14_odd_D_contains_its_own_image`, `P14_odd_D_is_pair_symmetric`) |
| `P15` | **CHECKED-NEGATIVE** | `discriminant` = a statistical discriminant of models; `absolute` = magnitude; `polar` = polarization |
| `P16` | **CHECKED-NEGATIVE** | zero quadric-vocabulary occurrences |

⇒ ***Seventeen of seventeen accounted for: ELEVEN WORKED, SIX CHECKED-NEGATIVE. Reach: **17/17 ACCOUNTED FOR** — 11 **WORKED**, 6 **CHECKED-NEGATIVE by name** (the reading standard; the worked-only figure is $11/17$), up from 5 —
the widest reach of the seven fields baked so far.*** *One new receipt: `Q7` (P02's interior conic, circle↔hyperbola
across $s\to is$, added, runs, ALL PASS). Every other worked paper already carried one.* **Cross-field: `P09`'s
$I^3-27J^2$ and `P14`'s reducibility overlap the conformal (Weyl invariants) and involution (the $A_2$ / real-form
skeleton) bakes; no correction, shared results.**
