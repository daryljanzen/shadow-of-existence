---
name: algebraic-geometry-ledger
kind: FORWARD
current: r3518
job: The algebraic-geometry field-bake ledger — what bit, what bounced, and the boundary. One of the three fields listed but never thrown (the r3505 overnight order: algebraic geometry ×57, discriminant ×28 / genus ×21). `OWED` 622.
sources: [cowork]
---

> **▣ FORWARD — the algebraic-geometry bake, and part of the corpus.** *Three registers kept apart:
> **what bit**, **what bounced**, **what the boundary is**. The overnight order named it —* ***"the
> three never thrown: … algebraic geometry ×57"*** *— and its two live sub-terms are `discriminant`
> ×28 and `genus` ×21. The corpus's spine is a CUBIC, $r^3-\alpha^2 r+2M\alpha^2$, and everything an
> algebraic geometer would ask of a cubic — its discriminant locus, its branch points, the cross-ratio
> of its roots, the genus of the curve it cuts, its equianharmonic point — the corpus already computes,
> under other fields' names. This bake's finding is expected to be a BOUNCE of a particular shape: the
> content is here and is receipted, but under `quadric`, `combinatorics`, and `complex-analysis` heads,
> not as "algebraic geometry".*


## ⛭ THE BASELINE, MEASURED FIRST

**Nothing below was called a hole before the corpus was asked what it holds.** *Seventeen paper bodies,
comments and bibliography stripped, de-macroed, via `corpus/reach_baseline.py`.*

| present, and genuine | homonym / thin | absent across all seventeen |
| --- | --- | --- |
| `discriminant` ×28 (P03×6, P05×6, P07×4 — the cubic's $\Delta=4-27(2M)^2$) · `genus` ×21 (P01×6, P02×6, p0×5) · `cross-ratio` ×5 (P05×4, p0×1) · `moduli` ×28 (parameter moduli) · `singular point` ×5 · `cusp` ×1 (P15) · `blow-up` ×1 (P07) · `divisor` ×1 (P15) | `variety` ×1 (P14, English) · `ideal` ×0 word-bounded · `resolution` ×39 (mostly "resolution" = detail/settling, not blow-up) | `algebraic variety` · `scheme` · `elliptic curve` · `projective/affine variety` · `Zariski` · `canonical bundle` · `Riemann surface` (as a NAMED term) · `intersection number` · `Weierstrass` · `j-invariant` |

⚑ **THE FIELD'S OBJECTS ARE ALREADY RECEIPTED — the finding is WHERE, not WHETHER.** *Before this
bake: the discriminant $\Delta=4-27(2M)^2$ and its zero-locus (`sweep2_discriminant_is_interval`,
`SP_S4_two_discriminants_one_event`, `Q5_nariai_on_the_locus`); the cross-ratio of the four marked
points and its equianharmonic collapse to $\{e^{\pm i\pi/3}\}$ (`Q4_equianharmonic_vantages`); the
cubic pencil and its critical values (`L8_the_pencil`, `cubic_spacing`). So the algebraic-geometry
content is worked; it wears `quadric`/`combinatorics`/`complex-analysis` labels. The bake READS all
seventeen and names where a genuinely algebraic-geometry statement (the discriminant as a plane curve /
branch locus; the genus) is or is not the corpus's own.*

<!-- REACH CLOSE appended after the reader sweep returns -->
