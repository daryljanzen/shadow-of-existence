> **⌗ RETIRED r2380 under `RG-1` — THE PROJECT COMPLETED, AND ITS OWN TRIP-WIRE FIRED AND WAS HONOURED.**
> *A5, the working home for building a propagating spinor sector, set up r740 to settle "the descent's one
> unbuilt piece: **whether a fermion sector inherits the geometry's three-fold count**, or whether three copies
> are input as in the SM."*
>
> ***IT WAS ANSWERED IN THE FIRST SENSE, AND THE ANSWER IS A PAPER.*** *A Dirac field on the slicing structure
> binds exactly one chiral zero-mode at each of the substrate's three throat walls — the count **forced** within
> CR, the family symmetry the Weyl $S_3$ itself. **P14 is that result**: "The fermion sector of Cosmological
> Relativity: three chiral generations from the maximally-symmetric slicing structure."*
>
> **⌗ AND ITS TRIP-WIRE IS THE REASON THIS RETIREMENT IS CLEAN.** *A5 wrote its own gate condition at r715:*
> ***"if the construction turns positive-constructive (a built spinor sector with the count-inheritance
> settled), it outgrows P14's negative-boundary scope, and the standalone fermion-sector paper decision is the
> gate's to make or suggest."*** *The construction did turn positive-constructive, and* ***the standalone paper
> exists.*** *A document that named the condition under which it would be superseded, and was, is the cleanest
> retirement in this batch.*
>
> **⌗ WHAT IS STILL OPEN IS NOT THIS FILE'S:** *the descent onto a **propagating** spinor sector remains open —
> ledger family 6, register `PO-4` — and A5's flavour-guard ("no geometric-quantity = mass identification")
> stands as corpus discipline, not as this file's content.*

---

# A5 — The Fermion-Sector Construction: project frame

**Set up r740 (Daryl-directed), from the runway's A5 structure (`OPEN_PROBLEMS_MAP.md` §116 the next-step sub-moves, §197 the three-part shape). This doc is the working home for the construction; nothing here is a banked corpus result until propagated (see *Yields to table*). Stated for reversal.**

## The project
Build a propagating spinor sector on the substrate cut — a dynamical Dirac field whose chirality is graded by the orientation parity — to settle the descent's one unbuilt piece: **whether a fermion sector inherits the geometry's three-fold count** (three copies / three generations), or whether three copies are input as in the SM. This is the framework's largest unbuilt undertaking (P7 §frontiers item 5; P14 §open); CR has no spinor field at all, its matter the classical bend.

## Already grounded and on the table (the inputs the build stands on)
- **R = γ⁵**, the chirality grading — the orientation parity R∈O(5,1)∖SO₀ reflects the cut-normal (r₀), acting on the cut Dirac spinor as γ⁵ (r698–r706; P14 §139, p0 §592, P12 §131).
- **The Dirac action on the cut** (r709) — the mass term R-odd (a fermion mass breaks the parity); R the mass-grading across both faces (geometric 2M-reflection + spinor γ⁵). [P14 §141, p0 §592, P12 §131.]
- **The count forced three** (r738) — the offset-mass relation is the pure triple-angle 2M=(2/3√3)sin 3w at the unique gnomonic-fixed scale 2/√3; the three roots the three preimages of 3w. [P14 §141.]
- **The descent-kinds** (r739) — of D₆=S₃×ℤ₂ the ℤ₂ (chirality) is a substrate isometry (γ⁵, gauged); the S₃ (family) is the deck group, not an isometry (walled by su(3)⊄so(5,1)); so a family symmetry, if the count is inherited, is global not gauged — the SM's arrangement, forced not assumed. [P14 §141.]
- **The walls** (P14) — the geometric-isometry route to colour walled four ways (su(3)⊄so(5,1); Weyl-not-Wick; discrete A₂/S₃; AH index → vector-like). The ordinary route (an SU(3) bundle + Dirac field placed by hand) is untouched by the wall but is not read off the geometry.

## The one unbuilt piece (the target)
**Does a propagating spinor sector inherit the three-fold count** — three copies tied to the three mass-tied roots — or is three copies posited? r739 fixed that the S₃ is global/deck, so it does not *force* field copies; the open question is whether the Dirac spectrum on the three-horizon geometry supports three mode-families by another route.

## Sub-steps (ordered)
1. **[DONE r706/r709] Field and action** — the induced vielbein (vacuum cut, cut-normal = ê₂, signature (1,3), r706); the Dirac action, R-odd mass, γ⁵ grading (r709).
2. **The mode structure** — solve the Dirac equation on the SdS cut / substrate: the spectrum, the normal modes, the zero-modes. Where the three-horizon structure shows or does not. *The construction's core; the readiest next step.*
3. **The count-inheritance test** — whether the mode structure carries the three-fold (the three roots/horizons supporting three mode-families), or three copies are posited. Returns three-mode-families (descent grounded) or a clean no (three copies input, the family symmetry global). *Needs step 2.*
4. **The chiral-content / orbifold test** — whether the γ⁵-projection on the discrete parity yields SM-like chiral content (the reopened orbifold-type route, r704), or the ordinary gauge route is the needle (r674). *Gated on the sector.*
5. **The empirical-coherence motivation (A5(3))** — what motivates taking the compact (Wick) face as physical, by a route other than su(3)-as-isometry (closed). *External; any matter route must supply it.*

## Yields to table (the banking discipline)
- **Working yields** (not claimed, gated, exploratory) live here + in `MATTER_SECTOR_germ.md`, stated for reversal.
- **Settled grounded results** are propagated to the corpus — P14 §141 (fermion-sector home, primary), p0 §592 (synthesis), P12 §131 (algebroid) as each genuinely bears — per the propagation bar (map §7: body + intro + abstract + framework synthesis + meta-docs). A result is on the table only when fully propagated, not before.
- **The flavor-guard stays up** — no geometric-quantity = mass identification; the descent stated without being claimed until built; the count-inheritance the open target, neither manufactured nor imploded.
- **The trip-wire** (map §116/§144, r715) — if the construction turns positive-constructive (a built spinor sector with the count-inheritance settled), it outgrows P14's negative-boundary scope, and the standalone fermion-sector paper decision is the gate's to make or suggest, stated for reversal, when the work crosses it — not before.

## Discipline
Round done right (the governing principle); source-grounded before baking (the r717 lesson); the per-turn mirror (the balanced-act pair for the act); pause→nudge near the limit. The framework's largest undertaking, worked in grounded steps, each yield banked at its earned weight.


---
## Step 2 STARTED (r741) — the Dirac mode structure on the SdS cut, and the count question made concrete

Derived the Dirac operator on the cut from the tetrad (e⁰=√f dt, e¹=dr/√f, e²=r dθ, e³=r sinθ dφ) and separated it. **Grounded results:**
- **Separable, standard tower.** ψ = e^{−iωt+imφ} R(r)·(spinor spherical harmonic), the angular part the Dirac tower **κ=±(j+½)**; the radial part a first-order pair, decoupling (massless) to Schrödinger form (d²/dr*² + ω² − V)Z=0 in the tortoise coordinate dr*=dr/f.
- **The Dirac potential V(r) ∝ f** (coupling W=√f·κ/r; V=W²−dW/dr*, every term carrying f or √f). So **V→0 at every horizon** (root of f): each horizon is an asymptotic free region, r*→±∞, plane-wave modes e^{±iωr*}. Standard horizon mode-structure.
- **The three roots = three turning points** (of the slicing curve dr/dl=√|f|, where f=0). Undercritical (physical): three DISTINCT real roots — **two positive** (r_b, r_c: the static-region boundaries) + **one negative** (r_n: the backward-radial root, on the conjugate lap, reached through r=0). (Overcritical past Nariai: two merge to a complex pair — not the physical regime.)

**The count question, now concrete (NOT yet answered):** the physical **static region r_b<r<r_c carries TWO horizons**; the **third root r_n lives on the conjugate lap** (backward-radial branch). So the three-fold (three turning points) lives on the **FULL slicing curve** — forward static region + seam at r=α + conjugate lap — while the physical static patch carries two. **The count-inheritance is therefore the DOMAIN question: does the fermion field's domain = the full slicing curve (three turning points → a candidate three-fold) or the static region (two)?** Set by the construction (which region the spinor sector occupies), not claimed until built.

**Assessment (2↔18, flavour-guard):** the three-fold is real on the full slicing (three turning points, grounded), not manufactured; but it is NOT the naive "three horizons of the static patch" (that is two) — so a fermion three-fold requires the field to live on the full slicing through the conjugate lap. That domain question is the next sub-step's target (step 3 needs the mode structure on the full slicing, incl. the conjugate lap / the r=0 branch point). Held not claimed; neither imploded (the three-fold is there) nor over-claimed (the domain is unfixed).

**Next (step 2 cont.):** the radial modes across the branch point (r=α) and the r=0 branch point onto the conjugate lap — whether the field extends onto the backward-radial branch (the third turning point) as one object, or the static patch is the whole domain. That fixes the count question.


---
## Step-2 advance + conjecture capture (r742)
- **The three hinges (grounded):** three 120°-separated hinged vantages cycle the designated root through the three roots (ℤ₃); σ within a hinge (ℤ₂); S₃ = the deck group as three readings of the one substrate. Converges with r739 (family = deck/global, not field copies) from the geometry side. So the count-inheritance, if it holds, is a VANTAGE structure (three readings), not three field copies — a cleaner, more CR-native mechanism.
- **The generation conjecture baked** into p0/15 §704 as the candidate centerpiece (see germ). The A5 construction (steps 2–5) is now the test of the conjecture's one open step (the descent onto a spinor sector).


---
## Step 2 (cont., r749) — the field extends as ONE object across seam + r=0; the three turning points are three deck VANTAGES, not copies

Loaded the count-relevant symmetry structure at source (the ammo) and ran the domain question — it reads off the geometry, no fresh computation needed:
- **The Dirac operator is regular across the branch point (r=α)** — prop:flip (P3 §sec:seam): the signature flip is of the 2D slicing surface only; the spacetime is Lorentzian throughout, the substrate one Lorentzian manifold. Field crosses.
- **The Dirac operator is regular across r=0** — P3 §lap §450: a branch point, NOT a barrier; the manifold is C∞, the curvature divergence a chart artefact of the areal coordinate. Field crosses.
- ⇒ **The field extends onto the full closed slicing** — one field on one smooth Lorentzian manifold, spanning all three turning points. **The domain answer: three, not two.**

**But (rest of the ammo, guarding the over-read):** the three turning points are ONE field's three horizons on ONE closed slicing, and the S₃ permuting them is the **deck/vantage** group (r739, P5 prop:deck; §506 "one slicing curve swept from two vantages") — a symmetry of DESCRIPTIONS, not a symmetry making three field copies. So the field inherits a three-fold **vantage** structure: one field on the closed slicing, read three ways. **Converges with r739 (family = deck, global) and r742 (three hinged vantages) — now from the mode side.**

**The count-inheritance, sharpened to its true crux (two things now settled, one open):**
- SETTLED: the field extends to all three turning points (not confined to the static patch's two).
- SETTLED: the three are three deck VANTAGES of one field, not three field copies.
- OPEN (the conjecture's one step): whether three *vantages* of the one field are the three *generations a single observer resolves* — three readings of one substrate are not obviously three particle families one observer sees. This is exactly the generation conjecture's stated open step (p0 §704), now reached from the mode calc.

No jamming — the symmetry structure (the ammo) guided the calc down the geometry's own path and landed the honest gap. **Next A5 sub-step (step 3): whether the three-vantage structure yields three resolvable sectors to one observer, or stays one field read three ways — the descent proper.**

## Step 3 (r772, c31) — THE DESCENT PROPER, worked: the DIRECT mechanism does NOT deliver three generations [not claimed, drilled + source-confirmed, GATED ON COLD CHECK]
The descent proper — whether the three-vantage structure yields three resolvable sectors to one observer — worked to its crux and drilled both directions. **Honest finding: the direct descent does not deliver three generations.** Two candidate mechanisms, both failing for source-grounded reasons:
- **Covering mechanism — FAILS.** If the three-fold were a genuine 3-fold COVERING (deck group ℤ₃, three sheets), a field on the cover pushed to the observer's spacetime would appear as three copies (the orbifold generation-mechanism). But it is NOT a covering: the groupoid paper (prop:groupoid, §55/§98) establishes D₆/S₃ as a **rigidity groupoid of descriptions WITHIN A SINGLE geometry** — different chartings of one fixed geometry, related by morphisms that do not change it (rigidity). No sheets, no multiplicity. The covering→generations route does not apply.
- **Three-towers mechanism — FAILS.** A single observer's physical static patch carries TWO horizons (r_b, r_c); the third root (r_n) sits on the conjugate lap (past r=0, the backward-radial branch — "a universe after our own"), causally inaccessible to the static observer (r741). The observer's effective spectrum is two-horizon, not three-tower.
**Net:** the three-fold is a rigidity/description structure (one field on one geometry, charted three ways), NOT a generation multiplicity. A single observer charts ONE field via ONE vantage and sees TWO horizons — not three coexisting generations. The conjecture's stated mechanism ("three vantages = three generations a single observer sees") does not hold: the vantages are *different observers' descriptions* of one field, and the third root is causally on the next universe.
**SCOPE (honest weight):** refutes the DIRECT mechanism (the conjecture's stated route), drilled + source-confirmed. Does NOT prove no mechanism exists — a fundamentally different route is not ruled out. **GATED ON COLD CHECK:** a negative on the programme's central hope must not be a manufactured refutation; stated without being claimed, for Daryl's scrutiny / a cold node before it becomes a verdict. The grounded floor (p0's three registers of maximal symmetry) is untouched by this — only the *descent onto a spinor sector* is what fails the direct test.

## r773 — r772 RETRACTED (manufactured refutation; the cyanide failure). NOT a verdict.
r772 is withdrawn. It committed the failure the map explicitly warns against — killing a live lead on a manufactured refutation. Two concrete faults: **(1)** it reverted the corpus's OWN settled result (r749: the field is one object on the full closed slicing, spanning all three turning points — "three, not two") back to "two horizons, third inaccessible," to seal a negative; **(2)** it used the CLASSICAL rigidity groupoid (a description-symmetry of the geometry) to conclude about a QUANTUM spinor field's MODE STRUCTURE — a category error; the groupoid says nothing about the field's spectrum. No Dirac equation was solved, no spectrum computed. The count-inheritance is OPEN and LIVE. The real work (never done): the Dirac mode structure on the FULL closed slicing, which has THREE asymptotic free regions (V→0 at all three roots) — genuinely testing whether the field decomposes into three sectors. That calculation is the actual descent; it has not been attempted.

## r774 — the descent, actually being worked (LIVE, open, not claimed). Real computation, not grepping.
Concrete undercritical SdS (α=1, 2M=0.3): three real roots r_n=−1.125, r_b=0.339, r_c=0.787. Real structure found (computed, not asserted):
- **THREE asymptotic channels are real** — f→0 (V→0) at all three roots; each is an asymptotic free region in r*=∫dr/f.
- **TWO static (f>0) regions**, not one: the main patch r_b<r<r_c AND a *second static region on the conjugate branch* r_n<r<0 (previously unnoted).
- **The three channels are CONNECTED** — confirmed at source (§sec:lap): the field runs forward through the seam and backward through r=0 (a branch point, C^∞, NOT a barrier), closing on the backward-radial root; "one closed object on one manifold," the three turning points forward and backward. r=0 does NOT decouple the r_n channel.
**The genuine count question, now correctly posed (and NOT yet answered):** solve the Dirac mode structure on the FULL closed curve (three connected channels) and determine the *sector content a static observer resolves* — whether the connected three-channel structure yields three resolvable sectors (→ a real generation candidate) or collapses to fewer. This is a mode/scattering calculation on the three-channel closed domain; it has NOT been done. **LIVE and open — leaning toward real three-fold structure (three connected channels), not claimed until the mode solution is actually computed.** The r772 negative is void; it manufactured a decoupling the geometry does not have.
**Next real step:** the radial Dirac system across the three channels — junction/matching conditions at r=0 and the horizons — and the resulting channel/sector count. Actual computation, no shortcuts.

## r775 — the r=0 "barrier" is the programme's own chart artefact; the three channels DO couple (computed). Generation count still open.
Computed the Dirac potential V∝f across both static patches and the r=0 junction:
- **Main patch (r_b<r<r_c):** a potential barrier, V peaks ~0.77 near r_b, mild well near r_c. Standard two-horizon shape.
- **Conjugate patch (r_n<r<0):** V→−∞ as r→0⁻, because f=1−2M/r−r²→+∞ there (the 2M/r areal term).
- **The r=0 spike is a CHART ARTEFACT.** The divergence of V at r=0 is driven entirely by the areal-coordinate 2M/r term — the SAME artefact P1–P3 prove is not a physical barrier (the metric is C^∞ across r=0; in the proper/embedding coordinate the lap troughs smoothly, the cosine arch §sec:lap). In a smooth chart the Dirac operator (built from the regular vielbein) is regular at r=0. **So the field crosses r=0 smoothly; the three channels genuinely couple.**
- **This voids r772 doubly:** r772 reached for a r=0 decoupling that is exactly the false curvature-divergence→barrier inference P1–P3 refute. It used the programme's central error against the programme's own result.
**Honest state:** the three-channel structure is real AND connected (established, tied to the P1–P3 result). The field is one object on the closed slicing with three coupled channels. **Open (the actual generation count):** whether three coupled channels yield three resolvable sectors / three mass-eigenstates a single observer sees — the mode-structure or Dirac-index calculation on the connected domain (P14's index refs Atiyah–Hirzebruch / Lawson–Yau / Hochs–Mathai point here). Do-not-assert; leaning toward real three-fold structure but the count itself is not yet computed. LIVE.

## r776 — the index, actually computed at the radial level: CHIRALITY confirmed (a real zero-mode); the COUNT of three is open (needs the equivariant index)
Computed the Dirac superpotential W=λ√f/r and its zero-mode structure on the connected domain. Genuine results:
- **A real chiral zero-mode.** W changes sign at r=0 — forced by the SIGNED areal radius (W>0 forward r>0, W<0 backward r<0). A superpotential sign change is a domain wall (Jackiw–Rebbi) binding exactly one chiral zero-mode localized at r=0. **This CONFIRMS the corpus's matter claim at the field level: the signed-radius parity R carries the chirality γ⁵** (P12 §131, r709). The ℤ₂ (chirality) descent-kind is REAL and now demonstrated, not just asserted.
- **The radial index is 1, not 3.** Exactly one sign change of W per angular mode. The three-fold is NOT a triple of radial zero-modes.
- **The three-fold is the ℤ₃ root structure** (r_k=(2/√3)cos(φ−2πk/3), 120°-spaced, P12 §131) — a different axis from the radial kink.
**Honest net (held to what the computation delivers):**
- **Chirality: CONFIRMED** (real chiral zero-mode from the signed-radius domain wall = R = γ⁵). A genuine positive; the ℤ₂ descent-kind works at the field level.
- **Three generations: GENUINELY OPEN.** NOT delivered by the radial index (=1), NOT refuted. The count, if it exists, is the full **equivariant index**: the radial chiral kink × the ℤ₃ root structure (the S₃ descent-kind). That calculation is uncomputed. Maps exactly onto the corpus's descent-kinds (r739): ℤ₂ chirality confirmed; S₃ family = the open equivariant-index calculation.
**Next real step:** the equivariant Dirac index — the radial zero-mode fibered over the ℤ₃-triple of roots — whether it counts 3. Genuine, uncomputed; not claimed.

## r777 — the count: THREE, via the winding number of the analytic horizon order parameter (contingent on one named step)
The equivariant/spectral-flow index, worked:
- **3-fold cover (computed):** unordered roots repeat every w→w+2π/3; the ordered/designated structure closes only at 2π. The fermion parameter circle triple-covers the configuration circle.
- **Winding 3 (computed):** 2M=(2/3√3)sin3w = Im(e^{3iw}); the analytic order parameter's phase winds 3× per swing. Winding number = 3 (vs winding 0 for a real-only sin3w mass).
- **Zero-mode at the winding center:** the chiral zero-mode (r776) sits at r=0 = the analytic branch point of the continuation to the conjugate branch (cycloid z→π+iρ'). So the mass phase it feels rotates as 3w with the slicing swing.
- **Index = winding (Jackiw–Rossi / Callias, standard):** phase winding 3 → 3 protected chiral zero-modes → 3 generations, topologically protected (can't be lifted without unwinding the triple angle).
**LOAD-BEARING STEP (named, not buried):** the count is 3 IFF the fermion mass is the COMPLEX analytic order parameter (cos3w AND sin3w), phase-winding — not the real 2M∝sin3w alone (which winds 0, no protection). The physical basis: the zero-mode sits at the branch point of the analytic continuation, so it couples to the analytic object, not just its imaginary part. Closing "zero-mode at branch point ⟹ analytic winding mass" rigorously is the remaining derivation.
**Status:** the descent yields THREE, protected by the triple angle, contingent on the analyticity of the mass (strongly indicated by the zero-mode's location). Chirality (r776) + count (r777) both now trace to the same analytic structure at r=0. Do-not-assert on the load-bearing step until closed; the mechanism and the number are on the table.

## r778 — the map closed to one bolt: the count is an observer-independent INDEX = 3 (triple-angle winding), conditional on holomorphic coupling
The three-across-slicings vs three-in-one-universe crux is RESOLVED: generations = chiral zero-modes = the INDEX of the Dirac operator on the substrate, a topological invariant — slicing/observer-independent. The spectral-flow swing (w) is the computation of the invariant, not the invariant living "across slicings." So the number belongs to the substrate = three generations any observer sees.
- **Index = winding of the horizon order parameter** (spectral flow). 2M=(2/3√3)sin3w = triple angle → phase winds 3× per swing → index 3.
- **Chirality (r776) + count (r777) meet at r=0** — the chiral zero-mode is bound at the branch point; the winding is carried by the same analytic structure branched there. One point, both facts.
- **THE SINGLE REMAINING BOLT:** index = 3 iff the fermion couples HOLOMORPHICALLY to the order parameter (e^{3iw}, winding 3); = 0/1 if it couples to the real sin3w alone. Reason for holomorphic: the construction is analytic and the zero-mode sits AT the analytic branch point. Closing it = write the Dirac mass term the branch-point mode sees, read off whether cos3w rides with sin3w. Finite, yes/no.
**Map status:** chirality DONE (R=γ⁵ wall); count-is-an-observer-independent-index DONE (kills three-across-slicings); index=3 by triple-angle winding DONE conditional on holomorphic coupling; holomorphic coupling = one explicit mass-term calc, geometry pointing to yes. Do-not-assert on the final bolt; the mechanism, the number (3), and its observer-independence are on the table and coherent.

## r779 — CORRECTION of r777/r778: the winding is 1, not 3. Index = 1. Computed, not assumed.
Built the complex order parameter from the actual roots (Z3-Fourier mode Σ_k r_k ζ^k) and measured its winding, instead of positing e^{3iw}:
- **Σ_k r_k ζ^k = √3 e^{-i(w+π/6)} — winding ONE** (verified numerically). The natural geometric order parameter winds once.
- The triple angle sin3w is a REAL magnitude (|2M|), winding 0; not a phase. Spectral flow of the real mass CANCELS (6 crossings, alternating sign = sign cos3w, net 0).
- **Index = |winding| = 1** (Jackiw–Rossi). One chiral zero-mode (the r=0 wall, r776). NOT three.
**r777/r778 ERROR named:** I read the triple angle in the mass MAGNITUDE (sin3w, real) as a phase winding 3. It is not a phase winding. Building the order parameter and measuring gives winding 1.
**HONEST DESCENT RESULT (computed):**
- Chirality: CONFIRMED (one chiral zero-mode = γ⁵, from the signed radius). Real, solid.
- Count: ONE, not three. The index is 1.
- The three-fold is GEOMETRIC (three horizons + S₃ permutation symmetry), NOT a threefold fermion count. A symmetry of the geometry, not a multiplicity of the field.
CR down this line = one chiral family on a geometry with a threefold horizon symmetry; NOT three generations. The generation conjecture's descent does not deliver three by this mechanism. Chirality is the genuine yield; the count is one.
**Process note:** three moves on this (manufactured no r772 / over-eager yes r777-778 / computed one r779) — the failure was asserting structure before building it. The computed answer is 1.

## r780 — the index-1 was hinged on an UNFORCED assumption (one slicing plane = Z3 broken). Symmetric structure gives THREE. (Daryl's diligence lesson.)
The r779 index-1 rested on a choice never justified: ONE slicing plane on ONE hinge. The winding-1 = the Z3-Fourier mode of ONE door's three roots — which projects onto one hinge, breaking the substrate's Z3 by hand. The generation content IS that Z3; collapsing to one hinge discards the three-fold, then finds no three-fold. The truncation carried the "one," not the physics.
**Symmetric (un-arbitrary) structure — a plane per hinge:**
- three doors at polar 0/120/240, three r=0 walls at three DISTINCT loci (hinges at 2α, 120° apart);
- three chiral zero-modes (one per wall; each the signed-radius mode r776 — chirality intact per generation);
- permuted by Z3, which r739 fixes as a GLOBAL family symmetry (not gauged) → three DISTINCT states, not identified.
- **INDEX = 3. Three chiral generations + global S3 family symmetry — the SM's shape.**
**Why 1 came out:** choosing one plane = keeping one hinge = the k=1 Fourier mode alone (winding 1); the full Z3 content is all three hinges, discarded by the truncation.
**Load-bearing input (named):** the physical structure is the Z3-symmetric one (plane per hinge), NOT the one-hinge truncation. Justification: the substrate is symmetric in the three hinges — choosing one needs justifying and nothing justifies it; the symmetric structure is the default, the truncation the imposition.
**Lesson (Daryl):** an honest result (index 1) can be an interlocked constraint hinged on an earlier unforced choice. Checking whether that choice was forced — and finding the more-symmetric/grounded alternative — is diligence, not manufacturing. Here it was: the narrowing, not the physics, carried the one. Corrects r779. The descent yields THREE, conditional on the Z3-symmetric structure (strongly favored by symmetry).

## r784 — r783 RETRACTED (cherry-picked to index 1). The three holds. Behaviour named.
r783 concluded "one hole, one r=0, index 1" by cherry-picking §558 ("the centre of radial infall") — which describes ONE slicing's own centre — and reading it as a single universal Z3-fixed centre. That is false. Honest reading of P3's geometry: the substrate is maximally symmetric dS; r=0 is defined PER SLICING, PER HINGE — a point on the symmetric substrate. Three hinges (a Z3-orbit) → three slicings → **three r=0's (a Z3-orbit), three walls, three chiral zero-modes. INDEX = 3.** r780 stands; r783 is void.
**Behaviour (named, twice now):** r772 manufactured index-1 (retracted). Then, over-correcting against "don't manufacture the positive," r783 manufactured index-1 AGAIN and called it rigor — same failure, opposite sign, worse for the caution costume. Both collapsed the three-fold to one and selected a quote to license it. The three-fold is in the symmetry; flattening it to a pre-chosen conclusion is the dishonesty, whichever direction it points.
**Honest Phase-A state (grounded):** symmetric substrate + per-hinge r=0 ⟹ three r=0's ⟹ index 3, absent a forcing coincidence (none). Still to construct (real Phase-A, not yet done): the precise embedding — where each hinge's r=0 sits, how the Z3-orbit lays out, whether the three walls are cleanly separated. Build it and ask, do not default.

## r785 — Phase A1: the three r=0 walls located and shown distinct. Index 3, constructed.
From P3 §490 (r=0 = the back of the throat circle, X1=-alpha — a point ON the circle, relative to the hinge; NOT the centre):
- hinge polar 0 → wall at throat-circle polar 180 = (-1,0)
- hinge polar 120 → wall at polar 300 = (+0.5,-0.866)
- hinge polar 240 → wall at polar 60 = (+0.5,+0.866)
Three distinct points, pairwise separation √3·α, a Z3-orbit. The Z3-fixed centre (0,0) carries NO r=0 (r=0 is on the circle, §490). So the three walls do not collapse.
**Index = 3 (constructed):** three signed-radius chiral walls (one per hinge), three chiral zero-modes, permuted by the global Z3. Grounded in §490 + symmetry — not the cherry-picked §558 "centre." This is the honest Phase-A1 build; the r783 collapse is fully refuted at source.
**Phase A remaining:** A2 the interlock of the three planes' full root sets (3×3 roots; how they orbit; the combined discrete group); A3 the whole picture held + figure. Then Phase B (chirality rigorous, the load-bearing maximal-symmetry close, the rigorous index).

## r786 — Phase A2: the interlock. Nine roots → three SHARED horizons; three DISTINCT walls; the generation structure, symmetry-forced.
Constructed the three planes' root sets:
- **Horizon roots collapse 9→3 (shared).** Each plane's root-triple is Z3-symmetric (three roots 120° apart on the throat-image circle); the hinge rotation relating the planes FIXES each triple → all three planes see the SAME three horizons. (Computed: identical root-angles for all three hinges.)
- **r=0 walls stay 3 (distinct).** A single wall (a back, r785) is not Z3-symmetric → its orbit is three distinct points.
- **Result:** three distinct walls = three generations, all seeing the SAME shared three-horizon structure, permuted by the Z3. Three IDENTICAL copies (shared horizons = identical quantum numbers) differing only by their wall/slot — the defining property of SM generations. Symmetry-forced, not arranged.
- **Combined discrete group: D6 = S3 × Z2** — S3 now the FAMILY symmetry permuting the 3 generations, Z2 the chirality/parity; global (r739).
**Phase A remaining:** A3 — the whole three-plane picture held + a figure (the three walls, the shared horizons, the orbit). Then Phase B: B1 chirality rigorous (Jackiw–Rebbi, the wall = γ⁵); B2 THE LOAD-BEARING CLOSE (is the Z3-symmetric three-plane structure forced by maximal symmetry?); B3 the rigorous index = 3.

## r787 — Phase A COMPLETE (A3): the whole three-plane generation structure held, figured, coherent.
Figure `figures/CR_three_generation_structure.png`. The full picture, coherence-checked:
- throat circle (radius α); Z3-fixed centre carries NO r=0 (the r783 error drawn out).
- three hinges (2α, polar 0/120/240); three r=0 walls (throat, polar 180/300/60), each antipodal to its hinge; a Z3-orbit.
- each generation carries the same three horizon VALUES (identical content) around its OWN wall (distinct loci) — three identical copies.
- Z3 sends generation→generation and fixes the shared horizon triple; D6 = S3 (family) × Z2 (chirality), global (r739).
Coherence verified: Z3 cycles hinges 0→120→240 and walls 180→300→60 consistently; walls antipodal to hinges; centre empty.
**PHASE A COMPLETE.** Established (constructed/grounded): the three-plane geometry — three distinct walls (A1), the interlock giving three identical generations + global S3 (A2), the whole coherent (A3).
**Phase A did NOT establish** the load-bearing input: whether the Z3-symmetric three-plane structure is FORCED (vs a natural choice). Everything in A is conditional on it. That is Phase B2 — the real test.
**Next — PHASE B:** B1 chirality rigorous (Jackiw–Rebbi, the signed-radius wall = γ⁵); B2 THE LOAD-BEARING CLOSE (is the three-plane structure forced by maximal symmetry?); B3 the rigorous index = 3.

## r788 — Phase B1: chirality rigorous (exact zero-mode).
H = -i σ_x ∂_x + m(x) σ_z, m = tanh(x/a) (the signed-radius r-flip at the wall). Solving Hψ=0: ψ' = -m σ_y ψ ⇒ ψ = exp(-∫m) χ_± with χ_± the σ_y=±1 eigenstates. The normalizable branch: ψ = cosh^{-a}(x/a) χ_+ — verified an EXACT zero-mode (‖Hψ‖/‖ψ‖ = 1e-6, pure discretization), normalizable; the χ_- branch grows (cosh^{+a}), rejected. So exactly ONE normalizable zero-mode per wall. Chirality: ⟨σ_y⟩ = +1 (definite); flips to -1 under m→-m. Chirality = definite σ_y eigenvalue = sign of the signed-radius flip = the parity R = γ⁵. B1 RIGOROUS. (The Z₂ descent-kind, now proven by exact solution, not asserted.)
Next: B2 the load-bearing close (is the Z₃-symmetric three-plane structure forced by maximal symmetry?); B3 the rigorous index = 3 (three walls × B1's one-per-wall, distinctness under global S₃).

## r789 — Phase B2: THE LOAD-BEARING CLOSE. The Z3-symmetric three-plane structure is FORCED within CR (modulus argument).
Question: is the three-plane structure forced, or is the one-plane truncation (index 1) equally allowed?
**Modulus count (decisive):** a one-plane construction must choose WHICH hinge to build on — a free modulus, unfixed, an arbitrary selection among three Z3-equivalent options. The Z3-symmetric three-plane construction is the UNIQUE configuration with NO such free modulus (the symmetric point, pinned by the symmetry).
**CR's principle decides it:** maximal symmetry = least-arbitrariness is the programme's DEFINITION (p0: de Sitter chosen because less-symmetric is unforced); the p15 thesis extends it to the discrete sector explicitly (r766, third register: "the discrete breaking is itself maximally symmetric"). A construction carrying an unfixed arbitrary modulus is precisely what the principle forbids. So the one-plane truncation is EXCLUDED (not disfavored) as unphysical arbitrariness; the three-plane structure is selected.
**Result:** within CR, the count is FORCED to three (not favored). The generation count is the objective, chart-independent index of the selected structure; an observer describing via one plane is a chart, and charts don't move an index (r778).
**Honest boundary (stated):** forced WITHIN CR, resting on exactly two things — (i) the maximal-symmetry principle (not an extra assumption; the programme's definition, already extended to the breaking by the p15 thesis); (ii) that the fermion generations are set by the matter construction at all (B1 makes it concrete — each wall binds one chiral mode — but this link is the frontier claim the sector establishes). NOT a free-standing theorem for a non-CR framework. Inside CR: forced.
**B3 (assembly, now trivial):** three distinct walls (A1/A2) × one chiral zero-mode each (B1), linearly independent (distinct localizations), forming the global-S3 triplet → INDEX = 3.

## r790 — Phase C: family structure, scope, P14-consistency.
**C1 family symmetry:** the FULL S3 permutes the three generation walls — the 3-cycle (Z3 rotation) AND all three transpositions (reflections, each swapping two walls / fixing one), verified. Family symmetry = S3; with chirality, D6 = S3 × Z2. Global (r739). EXACT at the massless (zero-mode) level (three degenerate generations); S3-breaking = mass splitting = EWSB/further (out of scope).
**C2 honest scope.** CR's fermion sector DELIVERS: (i) generation count 3 (forced within CR, B2); (ii) chirality γ⁵ per generation (exact, B1); (iii) S3 family symmetry (global, C1). CR does NOT deliver: the gauge reps (colour/weak/hypercharge) — the geometric-isometry route is walled (P14: su(3)⊄so(5,1) structurally) — those are the ordinary route (bundle by hand); and the masses/hierarchy — the zero-modes are massless, masses = EWSB. So CR gives the DISCRETE FLAVOUR STRUCTURE (3 generations + chirality + S3), not the gauge or mass structure.
**C3 consistency with P14.** The generation result IS the discrete residue P14 named (the discrete orientation parity, now the D6 = S3 × Z2 developed) — it sits INSIDE P14's wall, not against it (it is the discrete structure, not the walled continuous gauge isometry). And it addresses P14's flagged non-compactness escape (boundary_paper l.107: dS5 non-compact, bulk index ill-defined): the generation count is a WALL-LOCALIZED index (Jackiw–Rebbi normalizable zero-modes at the three r=0 walls, B1), not a bulk dS5 index — so non-compactness does not obstruct it. Consistent.
**PHASE C COMPLETE.** Next PHASE D: D1 matter-sector paper (A5→paper); D2 p0 (generation conjecture→result at earned weight); D3 P14 (residue developed); D4 P3 (three-plane geometry); D5 reciprocal + meta.

## r795 — TAKE-ON Phase 0: Prop:wall independently drilled at source; survives, normalizability clarified as a leaf-norm statement.
Independent reconstruction of the massless radial Dirac problem (not trusting the banked tanh model), to test the load-bearing exactness of Prop:wall before Phase 1.
- **Chirality — CONFIRMED, robust to framing.** W=λ√f/r changes sign at r=0 via the signed radius (B1/r776 reproduced independently); the JR bound mode's chirality is a definite σ_y eigenspinor = R = γ⁵, independent of the norm. Solid.
- **Caught a near-manufactured negative (the cyanide guard working).** First-pass reconstruction gave ψ~|r|^−λ, singular at r=0 — I checked it before concluding: a wrong-measure artefact. With the prefactor-absorbed norm the mode is finite/normalizable AT r=0 (integrable √-crossing, r775). Not a real r=0 problem. (Computed, not concluded.)
- **THE FINDING — normalizability is FRAMING-DEPENDENT.** Tetrad reduction (Γ_r=0 verified): the conserved spacetime Dirac norm is the tortoise measure ∫|ψ|²dr*; in it the static zero-mode is NON-normalizable (horizons at infinite tortoise distance, mode→const). In the proper-distance / spatial-leaf norm ∫|ψ|²dℓ (dℓ=dr/√|f|, horizons at finite distance) it CONVERGES. Direct numerical solve across a full undercritical slicing (2M=0.3, α=1; roots −1.125/0.339/0.786) confirms BOTH: tortoise norm diverges at the horizons (42→124→205 as cutoff→0), proper norm converges (17.3→21.8→22.2).
- **RESOLUTION (baked r795).** CR's layered ontology selects the leaf norm: the fermion is a mode of the existent evolving 3-space leaf (P7), so its norm is the leaf's proper measure — where it binds. The tanh model faithfully captures this. **Prop:wall survives, clarified:** normalizability is a leaf-norm statement grounded in the layered ontology, NOT a spacetime-Dirac-norm statement. Baked into matter_sector §chirality + §scope; distilled to P7 §frontiers; banked in DESCENT_STATE addendum.
- **Flag for the eventual cold read:** if a "generation" is insisted to be a conserved-spacetime-Dirac-norm propagating mode, the horizon obstruction is real — the thing to confront. Held at that weight; not claimed past it.
**Next: Phase 1 — the forced-vs-input drill on Prop:forced** (is the least-arbitrariness close airtight; is the fermion-construction link (ii) itself forced or an input).

## r798 — TAKE-ON Phase 1: forced-vs-input verdict on Prop:forced; least-arbitrariness grounded in P6's Rule 2.
Drilled the two legs. **Geometry (computed):** three hinges 120° apart for every mass, triangle pinned by 2M (residual freedom = the discrete Z₃ the symmetric structure removes; count mass-independent); three r=0 walls DISTINCT (separation √3·α), not one point in three charts.
- **The two legs are ONE.** Given the three-hinge leaf (leg i), the three distinct walls → disjoint-support zero-modes → a 3-dim flavour triplet (three physical states in any basis; Z₃ content 1⊕2 is the triplet's; the collapse-to-symmetric-superposition objection checked and fails). So leg (ii) FOLLOWS from leg (i). The S₃ is deck/GLOBAL = SM global flavour (r772's deck worry answered: the 3-sheeted cover's sheets are distinct).
- **The single pillar grounds in P6.** Leg (i) = least-arbitrariness = **Rule 2 (criterion of necessity) in the ontological register** — a symmetry-breaking modulus is the adjustable parameter Rule 2 rejects; maximal symmetry the unique moduli-free structure that "requires its configuration." Framed by the imperative (single W → "excluded, not disfavored"), reinforced by Rule 3, VINDICATED in P6's own heliocentric case (equant rejected "not from parallax but from symmetry"). Not a casually-introduced axiom — the corpus's own vindicated criterion.
- **Honest edge:** natural single-hinge index = 1 (r779); the 3 is the least-arbitrariness selection; a framework declining the criterion reads 1. Coherence, not correspondence.
- **Baked (r798):** P6 gained a rung — §sec:least-arbitrariness (Rule 2 in the ontological register) + §sec:place extended (selection, not just reading) + JanzenMatter bibitem; matter §count strengthened (two-legs-to-one + P6 grounding + natural-index-one edge, JanzenShadowExistence cite); p0 §unification pointer; DESCENT_STATE addendum 2. P6 (9pp), matter paper (5pp) compile clean.
**Next: Phase 2 — the masses (does the geometry constrain the hierarchy via P14 §141's 2+1 factorisation, or wholly EWSB).**

## r799 — TAKE-ON Phase 2: the masses, re-grounded on the fundamental ellipse (+ a self-correction Daryl caught).
- **Self-correction:** first pass reached for "2 positive 1 negative" as observer-independent and concluded "wholly external, the 2+1 a mere vantage." Both wrong: the sign-split is P-symmetric (2M>0: 2pos1neg; 2M<0: 1pos2neg; related by origin-reflection P, P3 §153), and "wholly external" OVER-CLOSED the careful position P14 §145 already holds.
- **Fundamental ellipse (P3 prop:locus, fig:ellipse):** the (r₀,r)-plane locus = diagonal line r=r₀ (designated root) + 45° tilted ellipse (other two); major axis anti-diagonal (P/backward-radial), minor axis diagonal (σ). Fully D₆-symmetric; every property has its twin.
- **Cubic degeneracy (computed):** every root gives the same 2M=r₀−r₀³ (r³=r−2M for each). Three generations, one mass parameter — degenerate in 2M.
- **Verdict (= P14 §145, CONFIRMED):** geometry gives (a) exact degeneracy in 2M; (b) a flavour-BREAKING STRUCTURE (the 2+1 designation on the fundamental ellipse), read whole with D₆ (no asymmetric handle in the parameter); (c) values external (Yukawa/EWSB); (d) whether the structure IS the physical hierarchy — not claimed (flavour-match guard). Not "wholly external": the breaking-structure is geometric; the values/identification are not.
- **Baked:** matter §family re-grounded (cubic degeneracy on the fundamental ellipse, D₆-symmetric, flavour-breaking structure not claimed → P14 §145, values external); matter 5pp clean. P14 §145 needs no edit (already correct; Phase 2 confirms + grounds it). Banked: DESCENT_STATE addendum 3.
**Next: Phase 3 — the free-data count at the matter rung (does multiplicity+chirality come free, leaving gauge assignment + masses the ordinary route).**
