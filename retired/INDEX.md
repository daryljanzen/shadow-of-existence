# retired/ — INDEX

*Every document here was **read end to end** and its live content placed before it moved. Each carries a
banner at its head saying where that content went. **These are records, not plans** — nothing here is worked
from, and nothing earns a place in the programme by having existed.*

**If something here is not reachable from `../CONSOLIDATE_THE_PLAN_AND_INDEX_THE_PROGRAMME.md`, that is a
filtering miss — raise it there rather than reviving the file.**

**115 documents, plus `receipts/` (opened r1708).**
**⌗ `retired/receipts/` — retired computations, kept as record, not registered in `../receipts/INDEX.md`.**
*Same rule as this directory: nothing here is worked from. A receipt lands here when it is found to duplicate
one already registered, so the retirement itself is the record of what was learned.*
| Receipt | Retired | Why |
|---|---|---|
| `lambda_carries_the_threeness_storyboard.py` · `P03_lambda_carries_the_threeness.py` | r1708 | **A4.9,
duplicate.** Every computation in it was already receipted: the triple angle and **$2/\sqrt3$ as the unique
scale removing the residual $\sin w$ harmonic** → `P03_triple_angle_gnomonic.py`; **Nariai at $w=\pi/6$,
$r_0=1/\sqrt3$** → the same; **$\alpha$ alone setting every anchor, $2\alpha$ an output not an input** →
`alpha_alone.py`; the mass profile's range and maximum → P3 §mass in prose. **Found by reading p0
`sec:unification` way 6 and then P3 §mass and `prop:triple` at source — which is what should have happened
before it was written.** |


| Document | Retired | Why — and where its content went |
|---|---|---|
| `MATTER_SECTOR_germ.md` | r2380 | **Its frame is now FALSE, its harvests are all marked DONE, and its value is the walked maze.** Its opening reads *"CR has no fermion sector built … the framework's largest unbuilt undertaking"* — **P14 builds it**, count forced, family symmetry the Weyl $S_3$. *A working document whose opening frame states the absence of the thing that now exists is read wrong by anyone who opens it at the top.* Every harvest landed (P5 r744, P3 r745, P7 r750, p0 r700). ***Its real value is that it is the matter sector's `negatives-are-the-map` document***: "the germ overreached" (r669) · "too thin" (r674) · "prerequisites absent" (r677) · "framing RETRACTED" (r681) · **"'Exhausted' was the collapse, not the truth" (r683)** · **"MAJOR CORRECTION: I over-closed it" (r717)**. *A hundred kilobytes of maze with the walls marked.* Still open and not its: family 6 / `PO-4`. |
| `SILVER_PLATTER_colour-frontier-arc.md` | r2380 | **It was already filed here by `INDEX.md` — under Retired → Briefings — while sitting at top level.** *"RETIRE means the file LEAVES the working directory; a banner at the top is not retirement, it is a sticky note" (c49 r1452).* ***Its own warning is why the move matters***: a distillation **frozen ~1330 revisions before the present**, telling the reader *"do not let this document become a gist you trust in place of the source — that is the exact failure the programme is built to defeat."* **A briefing that warns you not to trust it belongs behind the filter.** Its live register is `PO-4`/`PO-3`. |
| `ANTIMATTER_FRONT_PLAN.md` | r2380 | **Conclusions withdrawn (r989), one open item dead (r1280) — both by its own header.** Both sheet-assignment readings **collapse the DOUBLE ruling** of the one-sheeted hyperboloid; A and B are genuinely distinct rulings, orthogonal at the waist. **Derived and kept: photon → real ($\mathrm{Im}=0$) crossing.** The error it records is a standing guard — the partial-symmetry flatten: *hold the FULL symmetry while pulling at any piece.* |
| `CMB_ACOUSTIC_FRONTIER_STATUS.md` | r2380 | **Self-declared superseded at its own head** (r497–500): its live claim that the acoustic scale was *uncomputed* is no longer true. **The half that stands**: it was right at r366 to kill the *"CR is CMB-excluded"* strawman. ⚠ Refers to `P9 CR_flatLCDM_v2`, two renumbers ago. |
| `THIRD_ARC_T01_heights_firstrun.md` | r2380 | **Retired honestly because it labelled itself honestly**: *"MISCALIBRATED — not a result … a SKETCH."* Its subject moved twice since — qualitative resolution r822–826, then `L-171`. *What survives is the reason it was flagged rather than banked: four failed attempts before r822 stopped and read the source.* ***The run was wrong and the labelling was right, which is the order that costs nothing.*** |
| `PLANCK_from_CR_build.md` | r2380 | **It landed, and the thing it gated is in the paper.** Its own gate: *"the p0 write (T7) is gated on this landing."* **T7 landed** — p0 §sec:ledger carries the reframe: a Planck value is *not* a physical scale but what unit-counting yields when it has only gauges to count; $\alpha/\ell_P\sim10^{61}$ is a number, not a tuning. Its second question landed too, **with the honest residue stated there** that $\Lambda$'s value is not predicted. |
| `TIDAL_SHIFT_PLAN.md` | r2380 | **It declared itself a record five hundred revisions ago** — *"✅ COMPLETE r1090. THIS IS NOW THE RECORD OF THE CLEARING, NOT A LIVE ROUTE"* — and still sat in the working directory. *The fourth sticky-note instance in this cut.* |
| `WORK_ORDER_cowork_r1103.md` | r2380 | **A work order for one bundle, delivered** — 41 receipts run clean from a fresh extraction, zero errors, zero undefined refs. Page-counts and numbering are r1103-era; **the note it tells the reader to open first was already in `retired/`**, so the pointer was already crossing the filter. |
| `c22_keepers.md` | r2380 | **The keepers were kept and its numbering is two eras stale.** Its head is the tell: *"NUMBERING NORMALIZED TO MY (c23) 13-PAPER MAP; c22 used an 11-paper spine."* The corpus has seventeen and neither mapping survives. ⌗ The r2234 locus repairs touched it **four times** — four stranded `$r=0$` symbols in one file, a measure of how much era-scrubbing it carries. ***A document that needs a translation table at its head to be read is a record.*** |
| `gate_notes_bead_audit.md` | r2380 | **The audit ran, the bead is `thm:bead`, and the audit's DISCIPLINE outlived it.** Its opening is the whole method of the r2377–r2380 audit in four lines: *"No trusting the node's writeups. Every claimed edit checked against the current paper text AND against the geometry/source it rests on"* · the collapsed-double-ruling failure it audited is now a wisdom-ledger scrap · and ***"when a real result is verified → cut a bundle + real changelog entry. Every time there's progress"*** — **the four-step state advance, written before it was a rule and now gated by `check_changelog`.** |
| `lapse_shift_synchrony_reconciliation.md` | r2380 | **It asked to become a proposition and it did.** Its aim was *"possibly into P7 if we get it right"* — ***it is P7 `prop:lapse-shift`***. The synthesis it recorded (*the lapse is Newton, the shift is Einstein, never reconciled with the relativity of synchrony*) is the corpus's formal reconciliation: the shift carries the synchronisation convention, the lapse the layer's rate, *"the 'now' carried by the existent and the synchrony freedom by the representational map"* — and the block reading located formally as **re-importing the lapse as a meta-lapse, a fifth dimension GR does not contain.** P4 carries the measured half. ***The second-cleanest retirement in this cut, after A5's trip-wire.*** |
| `FIGURE_STATUS.md` | r2380 | **A one-time verification a gate now performs every run.** Opened r1333 because `\includegraphics` calls sat with no image behind them and **P3/P15/P16/P7 hard-failed**; its closing line — *"Now VERIFIED: all 17 papers compile 0 errors / 0 undefined / PDF"* — is exactly what `check_compile` states every run and fails on when untrue. ***Its lesson is the sharpest sentence in the batch:*** **"'Compiles 0/0' had been ASSUMED corpus-wide and was FALSE for four papers."** *The assumed-not-checked class in one line — and why the answer was a gate, not a fix.* Its inventory is now `FIGURE_SWEEP` and `FIGURE_THEOREM_LEDGER`. |
| `FIGURE_WORK_LOG.md` | r2380 | **The LOG of a phase whose DRIVER stays live.** `SYNTHESIS_FIGURE_STORYBOARD` is the standing working document driving the phase — indexed live and **deliberately undated**, being a forward document; this is the record of what was cut while it drove. *A log is a record by construction, and a node plans from the driver, never from the log.* Its pre-storyboard backup (`corpus/_figure_backup_pre_storyboard/`) is independent of this file, which is what makes the retirement costless. |
| `D3_RECIPROCITY_READING_ORDER.md` | r2380 | **The pass it ordered is complete and the map says so** — *"the reciprocity-engine grind is complete (all 14 papers ↔ p0/15, both ways), the 15×15 dependency matrix and the P7↔p0 dipole in hand."* ***What survives is its r887 self-correction***: **"Reciprocity is two-way: each group enriches BOTH the group papers AND P16, in the SAME turn — NOT a one-way distillation with a reverse pass bolted on after."** That is p0 §sec:landing's *"round complete, both ways"* and `RG-2` at edit scale. ⚠ Its task list carries the **pre-renumber** paper names, so it reads as instructions to a corpus that no longer exists. |
| `CR_PERTURBATION_HELD_PICTURE.md` | r2380 | **Its content is the ontology map's §1·LEVELS and its paper numbering is two renames stale.** Assembled r822 as the prerequisite four failed transfer attempts had skipped. Its central content — **the LEFTWARD reading**, and the "radiation must gravitate and so alter the rate" objection named a category error — is P15 §properframe/§flatlcdm verbatim, and **the general form does what this file could not**: §1·LEVELS makes the which-rate answer *read off the geometry* at each computation instead of re-adjudicated every time. ⚠ It points at **P13** for what is now **P15**. *What survives is not the picture but the reason it was assembled — read the papers, don't grep them — now `THE_PLAN`'s GREP TO FIND, READ TO CONFIRM. **Four failed attempts bought the rule, and the rule outlived the picture.*** |
| `CR_intake_notebook.md` | r2380 | **A single-session intake notebook whose session ended at r251**, and its own scope line settles it: *"light intake notes, single-purpose… The restructure itself is the first task AFTER intake — not performed here."* ⚠ Stale in the way that misleads a spin-up specifically: its reading order routes through **`CR_flatLCDM (P7, cosmology)`**, a bibkey and role that no longer exist, and lists eight documents where the `README` now runs ten steps over seventeen papers. ***A stale reading order is the worst kind of stale card, because it is read first.*** |
| `RATE_HANDOFF_DERIVATION.md` | r2380 | **It answered its question and the answer is a paper section's title.** Opened r899 on P16's r898 item — *"why does radiation 'stop sourcing' the rate at the branch point? Asserted scoping, or derivable?"* — and **P16's section is now titled `The scoping of the two rates, DERIVED`**. Its answer is sharper in the corpus: *radiation never stops sourcing the rate, because on CR's ontology it never sourced any rate*; what changes is whose stacking the matter's history is strung on. The general rule is §1·LEVELS with the fatal-both-ways scoping. ***A question answered into a title no longer needs its working file.*** |
| `PHASE8_DIFF_AUDIT.md` | r2380 | **The audit ran to completion and said so** — marked COMPLETE at r1880: 19 claims, 8 non-claim changes, all ruled at source, 49 KEPT/DROPPED verdicts. Opened r1874 because Phase 8's four field bakes **were run without holding the substrate and slicing apparatus**, by grepping fragments. *Its own framing is why it is a record and not a loss:* ***"the work is genuinely rich and much of it is right, so the operation is not a revert: it is a filtered revert."*** **Its lesson outlived it**: "run without holding the apparatus" became `THE_PLAN`'s **GREP TO FIND, READ TO CONFIRM** and its four confirmations, cited back to this file's §4. |
| `CONSTANT_LEDGER_receipt.md` | r2380 | **It banked, and it is the receipt behind two register rows.** Its finding is now **p0 §sec:unification way 3** in the paper's own voice — *no free dimensionless constant in the gravitational–cosmological–quantum sector* — and a wisdom-ledger scrap. ⌗ ***And the connection found by retiring it: this is the FIRST MOVE for `L-200` and `L-201`***, p0's own frontier items 1 and 2, registered r2378 when the fold found them carrying no row. Both rows now name it, so a node opening either does not start from nothing. |
| `E1_CITATION_CATALOGUE.md` | r2380 | **E.1 is COMPLETE per `THE_PLAN`, and a gate has superseded the instrument.** `corpus/check_citations.py` (c54.106) does the catalogue's job and does it better on the point this file cared most about: its bar was **LEAN** — *"an entry means go look and decide; it does not assert a citation is owed"* — and **the gate makes that structural**, reporting per-paper FIRST USE so a paper is asked for one attribution per result and never more, and **never failing on content**. *A judgement-shaped job done by a reporting instrument rather than a static list is a list that no longer needs maintaining.* ⌗ The gate paid where the catalogue could not: **NARIAI, used 122 times across five papers with the 1951 reference in none of them.** |
| `PHASE1_seam_crossing_build.md` | r2380 | **Phase 1 of the cosmogenesis-of-matter arc (r810).** Its result — **the three chiral generations are INHERITED across the branch point, leaf-carried, faithfully** — is the corpus's own line: P14 `sec:count`, and the ontology map §1q states it verbatim. It reached it by combining two established facts with no gap between them (A4's transition law + T0.1's leaf-carried modes), *and that combination is the result, not the document.* |
| `PHASE3_baryogenesis_analogue.md` | r2380 | **Phase 3 (r813) — and its verdict was a NEGATIVE, honoured.** It returned **external** on both the baryon asymmetry and the $\rho_r/\rho_m$ IC, **worked to a verdict at source rather than stamped from the pre-constraint**. Both are corpus now: P7 `thm:antimatter-progenitor`'s "NO baryogenesis claim — the standing relational R-conjugation, NOT an asymmetry made at a seam", and `L-150`. *A phase that charted a reach and returned "no" is the shape of the maze.* |
| `A4_matter_seam_crossing_build.md` | r2380 | **A4, the first matter-crossing build — retired partly because it is KNOWN WRONG in a way one banner cannot hold.** ⛔ Its r2206 banner says the build *"repeatedly identifies the crossing surface as the Nariai degenerate horizon at $r=+\alpha/\sqrt3$"* — wrong; **the crossing is the branch point $r=0$**, corrected in the corpus at r2154 and never propagated into this body. *Twenty kilobytes repeatedly stating a wrong locus with one correction at the top:* ***a banner is not a fix, it is a sticky note*** *— which is why `retired/` is a place and not a banner.* **Its live content survives and is load-bearing**: A4's transition law — the reassignment fixes the RATE and does not touch the DENSITY — is ontology map §1e/§1·LEVELS. The crossing question itself closed c54.113. |
| `A5_fermion_sector_build.md` | r2380 | **A5 — the project completed and its OWN TRIP-WIRE fired and was honoured.** It was set up r740 to settle whether a fermion sector inherits the three-fold count; **it was answered in the first sense and the answer is a paper.** A5 wrote its gate condition at r715 — *"if the construction turns positive-constructive … the standalone fermion-sector paper decision is the gate's to make"* — the construction did, and **P14 exists.** ***A document that named the condition under which it would be superseded, and was, is the cleanest retirement available.*** What is still open is not this file's: the descent onto a *propagating* spinor sector is family 6 / `PO-4`. |
| `THE_NEXT_ARC.md` | r2380 | **The r805 "cosmogenesis of matter" arc chart.** All three phases disposed and none orphaned: the family-origin hook is built (P14 `sec:count`/`sec:whichthree`, remainder `PO-4`); **the honest constraint it stated up front — the seam is not R-fixed, so the antimatter parity is NOT a cosmogenesis event — is now the corpus's own line** (P7 `thm:antimatter-progenitor`, ontology map §1e); the baryogenesis-analogue is `L-150`. ***What it uniquely held: it charted a reach knowing it could say no, and said so in its opening.*** |
| `THE_THIRD_ARC.md` | r2380 | **The r815/r819 "correspondence — completing and landing" chart.** Its one open computation (peak heights) is no longer open in that form: resolved qualitatively r822–r826, reopened at a sharper place as **`L-171`** (two internal routes disagreeing, $\ell_1=220$ vs $150$ — **not** a discrepancy with the sky). The landing is `PHASE 9` (`L-199`). Its state read is superseded twice — it reports fifteen papers where the corpus has seventeen. |
| `THE_COLLAPSE_DYNAMICS_ARC.md` | r2380 | **The r828 collapse-dynamics arc, and BOTH its deliverables landed.** ***Its own stated refutation test — the deuterium yield — was run and PASSED***: P16 returns $D/H$ at the observed value and $^4$He from one inherited datum, $-0.5\sigma$/$+0.5\sigma$. Exact heights → `L-171`/`L-147`, fenced. The worldline depth it existed for is disposed twice: **the branch-point crossing closed c54.113** (no worldline crossing to compute) and the residue is `L-150`'s leading-order interior. ***What survives it is the governing discipline*** — validate on ΛCDM before trusting a CR number; read the source before the fourth failed computation; **we do not steer toward the favourable branch** — now in `THE_WISDOM_LEDGER` D and `THE_PLAN`'s per-turn list. |
| `EDITS_r2377.md` | r2380 | **The r2377 edit log.** Its findings are the `### Revision r2377` entry in `CORPUS_MAP`; its unique contribution was the per-edit reversal instruction, kept for that. r2377 is cut, checksummed and handed over. *Later revisions log edits in the changelog directly — **a scaffold that outlives its session becomes a second changelog**, the lesson `CONSOLIDATE` §11 already carried.* |
| `CONSOLIDATE_spent_sections.md` | r2378 | **`CONSOLIDATE` §§3, 4, 4b, 6, 7–11 — the filter, the bin, and the Goal-2 record**, moved whole under `RG-1` after their live content was carried forward. The file's own head banner names where each piece went: the filter's discipline became **`RG-1` itself**; the bin's 202-document table is superseded by the **generated** `DOCUMENT_LEDGER.md`; §6's one live item is `ARC 11`; §7's *DERIVED-or-written-from-a-head* test is `ARC 14`'s design premise; §8's seven operating rules are in `THE_PLAN`'s per-turn list; and **§10's own sentence — "that is grandfathering again; the question is whether each piece still deserves a place" — is `RG-1` stated two years early**, about a kickoff instead of a paper. |
| `KICKOFF_ARTHUR.md` | r1468 | the README (spin-up, the X trilogy, the check-in), `THE_PLAN`'s per-turn list (the operating rules, map-navigation) and the capstone (the seat discipline, which wa |
| `KICKOFF_EXCALIBUR.md` | r1468 | the README's "Bounded work" line. The dispatch model was used once (r931, six parallel readers); bounded verification and focused computation are now done by writi |
| `KICKOFF_GATE.md` | r1468 | `THE_PLAN`'s per-turn list (the two language guards, prior-reading-earns-the-index) and the README's check-in (the four-part synthesis). Its corpus map was stale — |
| `KNOWS_ITSELF_MAP_PLAN.md` | r1509 | the "knows itself" pass — mapping the corpus's ontology. **Landed: it built the ontology map.** Its cards carry *"Pinned r903"* and *"Pinned r907"*, matching this plan's own span. > Kept as  |
| `P15_17_ELEVEN_AVENUE_PLAN.md` | r1451 | the eleven-avenue plan for P15–17. Nothing outstanding — the avenues are `THE_ARSENAL`'s method and the sweep completed r1406. |
| `PHASE_TRANSITION_PLAN_c21.md` | r1509 | an organisational phase/council plan for the c21 arc. **Spent:** the arc closed ~1080 revisions ago. > Kept as record; **do not plan from it.** Its numbering and era predate the current corp |
| `PIVOT_EMBEDDING_FOUNDATION.md` | r1509 | the embedding chart of the slicing picture. **Landed:** P8 `slicing_operator` exists and compiles. > Kept as record; **do not plan from it.** Its numbering and era predate the current corpus |
| `PIVOT_SLICING_PLAN.md` | r1509 | the pivot–slicing geometry. **Landed:** P3 `SdS-slicing-curve_v2` exists and compiles. > Kept as record; **do not plan from it.** Its numbering and era predate the current corpus. *Status: e |
| `PROGRAMME_OVERHAUL_PLAN.md` | r1509 | the Arthur/Excalibur overhaul. **Landed:** `THE_INTERFERENCE_ENGINE` carries *"Note (r928): the roster in §7 is now instantiated as Arthur/Excalibur"* — one revision after this plan. > Kept  |
| `REACH_PLAN_perturbation-projection.md` | r1509 | the low-ℓ scalar-perturbation transfer. **Landed:** P15 carries the shape on a genuine Boltzmann transfer (×6); Lane 1 A1.2 struck DONE r1006. Pre-renumber — it calls the perturbation reach  |
| `RESTRUCTURE_PLAN_c23.md` | r1509 | a post-compaction restructure plan for c23. **Spent:** superseded by every restructure since. > Kept as record; **do not plan from it.** Its numbering and era predate the current corpus. # C |
| `THE_ARC_PLAN.md` | r1509 | the geometry arc — symmetry and symmetry-breaking. **Landed:** the matter-sector descent is built in P14 (three chiral generations, ×4). > Kept as record; **do not plan from it.** Its number |
| `THE_DEPENDENCY_LEDGER.md` | r1451 | the ordering tool. → plan item 4, with its rule *do not work downstream of something still moving* and its list of dependents. |
| `THE_MORPH_QUEUE.md` | r1451 | the campaign's next-actions list. → plan items 4–9 and 15 (P7 stability, propagation, the catalogue, both bakes, the closure sweep, gap tracking). |
| `THE_REACH_LEDGER.md` | r1451 | the reach method. → plan item 7, the bake method carried across. |
| `THE_REFINEMENT_ARC.md` | r1451 | a superseded phase (r1120–~r1201), absorbed by the arsenal phase. → plan items 10–13: the five owed figures, the map's Lane-8 card, the ledger citations, the masthead work. |
| `WP_B_ORDER3_BRIDGE.md` | r1451 | the order-3 bridge work package. Nothing outstanding — resolved r1429–34; the result is in P7 `rem:tworealisations` and the receipt `order3_bridge.py`. |
| `P3_DRAFT_r476.md` | r1521 | a working prose draft of P3 (r476). Superseded — the paper exists and compiles at 40pp, ~1040 revisions on. |
| `PERTURBATION_PAPER_DRAFT.md` | r1521 | a draft of the perturbation paper (r528). **Its work landed in P15** — the low-ℓ shape built on a genuine Boltzmann transfer; Lane 1 A1.2 struck DONE r1006. |
| `CONJUGACY_CONJECTURE_capture_r469.md` | r1513 | the conjugacy/wave-particle capture. Self-declared *"quarantined record"* — grated into the map as **Cluster J** at r551, geometry layer absorbed into P3. Both claims verified. |
| `colour_frontier_dS6.md` | r1514 | the colour-frontier notebook (r281–305). **Its frontier landed as a wall** — su(3)⊄so(5,1), verified across nine papers; chirality built in P14. Its distillation `SILVER_PLATTER` stays at root. |
| `THE_VISION.md` | r1534 | the running account of where the programme stands against the world. Superseded — Lane 2, the map's CURRENT STATE, and P15 carry it. **Source material for the introduction (H.0).** |
| `THE_SYNTHESIS.md` | r1534 | the picture — *the puzzle, and what binds the map*. Superseded by the ontology documents: §1·SHADOW and §1·LEVELS *are* what binds it. **Source material for H.0.** |
| `THE_VISION_JOURNAL.md` | r1534 | the chain the vision grew along; successive nodes' entries. Its r805 hold discharged r1529. |
| `P7_GENERAL_DISCUSSION_grind.md` | r1535 | a grind of P7's general discussion (r559). Landed: P7 carries `sec:general-reach`. |
| `P13_DISCUSSION_epistemic-comparison.md` | r1535 | the epistemic CR-vs-ΛCDM comparison (r553). Landed: the ground is P6, the comparison is in P15. |
| `SEAM_FRONTIER_ORIENTATION.md` | r1535 | a lap-2 orientation on the seam frontier (r500). Landed: the seam is built across P8, P9, P16. |
| `NODE4_TELESCOPE_first-image.md` | r1535 | the first telescope image, tensor/graviton sector (r459). Landed: P11 — ghost-free to all orders, reduced energy positive-definite. |
| `gate_notes_antimatter_proof.md` | r1536 | the working proof that the conjugate branch is **antimatter**, *"honest work, may land."* **Landed:** `thm:antimatter-progenitor` in P7 ×4, identity fixed at the R=γ⁵ level. |
| `DESCENT_STATE.md` | r1536 | the descent banked at r781 as a *"candidate result, not claimed."* **Landed:** P14's three chiral generations, S₃ family, γ⁵ chirality (built r772–793). |
| `CPT_COHERENCE_SWEEP.md` | r1536 | the CPT / charge-conjugation coherence sweep (r1102). **Landed:** P13's `prop:conjugation-closure` ×4 — C = (Q↦−Q)∘(R∘K), the linear and antilinear faces of one Plate. |
| `THE_P13_POSITIVE_CLOSURE_ARC.md` | r1536 | the plan for P13's positive closure (r1107). **Landed:** P7's `sec:two-sided-closure` ×5, built r1280. |
| `gate_notes_hedge_scan.md` | r1536 | the dev-mode hedge scan (r991+), combing for fossil hedges. **Ran:** results recorded per card in the ontology map (*"Hedge sweep r994: swept clean"*). |
| `BEAD_WALK.md` | r1536 | the bead walk, running record (r973→). **Landed:** `thm:bead` is P7's central theorem ×6. |
| `COSMOLOGICAL_THEORY_ROADMAP.md` | r1537 | the roadmap relocating the formal cosmological development into the P10–P12 arc (r514). Landed: all three papers exist and compile. |
| `GROUNDED_THREAD_NOTES.md` | r1537 | the antimatter / *"big bang as a telescope"* thread (r467). Landed: antimatter ×17 across P7 and P16. |
| `FIRST_EDITION_AUDIT.md` | r1543 | the first-edition audit (r935). Landed: its punch-list is in `THE_PLAN`, items 1–3 struck DONE r933/934. |
| `P15_16_CLOSEOUT.md` | r1543 | the P15/P16 closeout (r966). Landed: both papers compile and are receipt-certified. |
| `UNFINISHEDNESS_AND_COHERENCE_r967.md` | r1543 | a corpus pass on unfinishedness (r967). Superseded by `PROGRAMME_UNFINISHEDNESS_CATALOGUE`. |
| `SHADOW_READING_FORMAL_SPINE.md` | r1543 | the formal epistemological core (r390). Landed: **P6 is that core** (×19). |
| `THE_GROUNDED_RECORD.md` | r1543 | an era working record (r468). |
| `CONSOLIDATION_four-node_c21.md` | r1543 | the four-node consolidation (r313). Its subject landed: colour walled in nine papers; the thesis reread recorded in `forks/c20fork/`. |
| `COLD_READS_r343_five-node.md` | r1543 | a five-node cold read (r353) — an era record of the multi-node method, superseded by the Arthur seat. |
| `AVENUE_11_SWEEP_PLAN.md` | r1546 | the execution plan for **avenue 11, the corpus receipt sweep** (r1345). **It ran** — completed r1406 across all seventeen papers; 22 receipt directories. |
| `TRACTABILITY_SWEEP.md` | r1546 | the tractability sweep (r958). Its verdicts fed the plan's lanes and the map's readiness sort — the runway Lane 1 came from. |
| `EMPIRICAL_ALIGNMENT_VERDICT_r552.md` | r1546 | the FLRW-vs-CR alignment verdict (r552). Superseded by the r959–964 empirical cluster resolution. |
| `programme_consolidation_2026-06-13_r220.md` | r1546 | a consolidation from **r220 — before this changelog begins (r231)**. An era record. |
| `CR_uniform-expansion_reductio.md` | r1546 | the isotropy reductio (r339). **Landed:** P4 carries the argument, with Green–Wald weighed at source. |
| `THE_LENS.md` | r1546 | sufficiency, necessity and the engine (r324). Superseded: the necessity criterion is **P6's**, the engine is `THE_INTERFERENCE_ENGINE`. |
| `HOUSEKEEPING_QUEUE.md` | r1550 | the P8-comb debts, *"planned so none is left loose"* (r1284). **Complete — 13 of 13 checked.** Its Task 1 made `depmatrix.py` emit the styled HTML in one step, which is the fix used at r1473. |
| `DELOBOTOMISE_c22_hubble.md` | r1550 | self-marked superseded by `DELOBOTOMISE_c22_proper`; its hold discharged r1527 — the sound horizon is built and P15 carries the resolution. |
| `SPINUP.md` | r1550 | the wake-up index (r928). Superseded by `README.md`. |
| `BEAD_THEOREM.md` | r1550 | closing the bead into a theorem (r974). **Landed:** `thm:bead`, P7's central theorem. |
| `THE_SIFT_AND_SORT_PLAN.md` | r1550 | the r925 sift plan. **It ran** — its product is `RETIRED_PLANNING_THREADS`, and its gate is the rule this consolidation rediscovered: *source-verified, not status-read-trusted*. |
| `C37FORK_INTEGRATION.md` | r1550 | c37-fork integration instructions (r914) — an era procedure superseded by the Arthur seat. |
| `MERGE_NOTES_for_chat_node.md` · `MERGED_r1103_note.md` | r1550 | records of the r1102–03 cowork merge, which completed. |
| `WP_C3_SHEET_ASSIGNMENT.md` · `WP_E_ANTIMATTER_NAMING.md` | r1548 | work packages of the antimatter front. **Both carried the r1280 kill as open** — corrected in place, then retired. Fourth and fifth carriers. |

### Retired r1552 — the era working documents, each verified landed
*Twenty-seven arc plans, findings, handoffs and checklists from r288–r1089, every one traced to what it built.*
| Document | Where its work went |
|---|---|
| `THE_GENERATION_ARC` | P14's three chiral generations |
| `C5_reducible_build` | **P9 `range_paper`** — the GR catalogue as substrate cuts |
| `PLAN_p9map-p10-12-completion` · `CORPUS_ARCH_FLAG_P9-forward-port` | the P9–P12 arc, all four papers compiling |
| `REACH_low-ell-transfer` | **P15's genuine Boltzmann transfer**; A1.2 struck DONE r1006 |
| `P2_P3_OVERHAUL_PLAN` | P2 and P3, both rewritten again at r1491 |
| `BOUNDARY_PAPER_PLAN` | **P13 `boundary_paper`** |
| `F1_DYNAMIC_HALF_closure` | **P11 `dynamics_paper`** |
| `AUT_A2_ALGEBROID_finding` | P5's Aut(A₂)=S₃×ℤ₂≅D₆, and P12 |
| `MOVE13_FORCE_vs_ADMIT_finding` | the forcing register throughout P7 and p0 |
| `W2_empirical_coherence_finding` | the r959–964 crossing, *coherence → empirically favoured* |
| `C37_KNOWS_ITSELF_DIAGNOSTIC` | **the ontology map** (its plan retired r1509) |
| `P6_COHESION_SWEEP` | P6, compiling, discipline ×19 |
| `DELOBOTOMISE_c22_proper` | the sound horizon built; P15 carries the Hubble resolution |
| `THE_ANGULAR_TOUR_3.1` | P3 and p0's power section |
| `COURSE_antimatter_telescope` | `thm:antimatter-progenitor` and P16 |
| `SESSION_FIGURES_CONSOLIDATION` · `THE_RING_LENS` · `THE_BAKEATHON_PLAN` · `LENS_INSERTION_PLAN` · `PROPAGATION_PLAN_r316` | superseded by the figure ledger, the ontology cards, the reach theaters, P6's discipline, and their own completion |
| `RETROSPECTIVE_c21` · `HANDOFF_TO_c22` · `ARTIE_r806_readout` · `GROUNDED_SESSION_NOTE_2026-06-26` · `CLEANUP_CHECKLIST_r551` · `COLD_READ_P13_setup` · `CLUSTER_A_carry-through_status` · `REFERENCES_TO_UPDATE_modern_parallax` · `THE_VISION_THE_LENS_REVEALED_full` · `THIRD_ARC_T01_cmb_heights_lookahead` | era records and spent checklists |

## Groups

- **Superseded phases** — `THE_REFINEMENT_ARC` (the arc superseded by the arsenal phase), `THE_MORPH_QUEUE`.
- **Spin-up kickoffs** — `KICKOFF_ARTHUR`, `KICKOFF_GATE`, `KICKOFF_EXCALIBUR`; replaced by `../README.md`.
  *(`KICKOFF_CODA_REVIEW` was **kept** — a reusable review methodology, indexed in the consolidation plan.)*
- **Spent arc plans, verified landed** — the eight retired at r1509, each naming the thing it built.
- **Spent trackers** — `THE_DEPENDENCY_LEDGER`, `THE_REACH_LEDGER`, `WP_B_ORDER3_BRIDGE`,
  `P15_17_ELEVEN_AVENUE_PLAN`.
