# Retired corpus artefacts — kept as record, not compiled

**`appendix_receipts_p0.tex` — retired r1819 as a superseded duplicate.**
**p0 IS P17** (verified r1804: both appendices carry `P17_power_of_a_point` as their first receipt, and the
directory is `receipts/P17_geometric_core_paper/`). The paper inputs **`appendix_receipts_P17`**; **nothing in
the corpus inputted this one.**

The two differed in exactly three lines, and **the difference is the tell**: this version appends
***"[borrowed from P17]"*** to each description — **the older artefact from when p0 and P17 were treated as
distinct papers and p0 cited P17's receipts as another paper's.**

*Kept because the retirement is the record: it dates the moment the two labels were still two papers, which is
the shape of a naming that has since resolved.* **No receipt is orphaned** — all three (`P17_power_of_a_point`,
`P17_tangent_null`, `P17_su3_ledger`) are in the live appendix and in `receipts/INDEX.md`.
