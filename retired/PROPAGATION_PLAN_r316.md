> **⌖ RETIRED r1552.** This was the corpus propagation plan (r320). Spent — its distillation ran.
> Kept as record; **do not work from it.**


# Corpus propagation plan — distilling the four-node consolidation (r311–r315) into the corpus
# c21, stated for reversal. Scoped at source this revision (r316). The determination is the gate's call; the
# canonical-paper edits are the precious-object work, made source-grounded and then cold-read (edit→cold-read).

## STATUS (r320): BAKED. **(1)** classification → P4 (r317); **(2)** P3 re-pointed (r317, incl. l.423 now citing P4); **(3)** P11 strengthened (r319); **(4)** foundation — **PENDING** (sequenced after P11's fresh cold read). Two points in the P11 map below were **corrected at the bake by the c17/c19 cold reads**, and the corrected forms are what went in: (i) the non-compactness escape is closed on the **localisation** argument (su(3) is no isometry of the non-compact substrate, su(3)⊄so(5,1)) — explicitly **not** "where AH bites" (§P11 below; AH stays *gated* on an unbuilt fermion sector); (ii) the surviving open items are empirical coherence **and** the unbuilt fermion sector — **not** "empirical coherence alone" (§P11 below). The existence-ontology was deepened (r318, Daryl): the cosmic time the existence-criterion turns on is **empirically determined** (P8 App. A / BST §4.3.2 / Fortress), firming the floor — bounds unchanged (ontological not structural; universal do-not-assert). The P4 A₂≠su(3) discipline note (§P4 below, "Discipline note") was missed at r317 and **added at r318**. The map below is the pre-bake plan, kept as the record; where it and these corrections differ, these govern.

## THE DETERMINATION (the new-paper question, resolved at source)
**The results distill throughout the corpus. No new paper is warranted. KICKOFF_GATE survives.**
Grounding: P3 (`SdS-slicing-curve_v2.tex` l.423) explicitly defers its group-theoretic / discrete-group
treatment "to a separate paper... in which the reflections and their composition are the generators" — and
that separate paper already exists, the groupoid paper (P4). The colour frontier already has its paper (P11,
the boundary paper). The ontology has the foundation (`ONTOLOGY_FOUNDATION_INDEX.md`). The cosmogenesis
grounding is already in P7. So every result has an existing home; the one piece that would exceed those scopes
— the full Aut(A₂)↔algebroid connection — is still a live *unworked* frontier, so there is no completed result
there to paper yet. A new paper may be warranted when that frontier is worked; not now.

## THE PER-PAPER DISTILLATION MAP

### P4 — groupoid paper (`groupoid_paper.tex`): the substantive new formal content
The discrete-structure classification, closing P3's deferred items (P3 l.607/614/640) and P4's deferred relation:
- **Within one geometry:** Weyl(A₂)=S₃ — the involution + sky-angle periodicity generate the discrete group in
  full (closes "whether they generate it in full").
- **On the solution space:** + the mass-reflection ℤ₂ (the A₂ diagram automorphism, 2M↦−2M) → **Aut(A₂)=S₃×ℤ₂≅D₆**, order 12.
- **The between-member relations:** the cubic monodromy — ξ-monodromy = σ; the 3-sheeted cover of the horizon
  cubic branched at the two Nariai points; S₃ the deck group of the slicing-cut cover (closes the same-α
  between-member relations, ξ + overcritical).
- **The α-action:** the gauge homothety ℝ₊ — a continuous scaling, not a new discrete symmetry; the classification
  is **scale-invariant** (closes "the action of vantages between distinct α").
- **The tie to P10:** the deck branch point (Nariai) = the P10 algebroid connection-vanishing stratum — discrete
  (deck) and continuous (connection) faces meet at Nariai. [the lead into the Aut(A₂)↔algebroid frontier, named not worked]
Source: `scripts/A2_solution_space_symmetry_c20fork.py`, `between_member_monodromy.py`, `alpha_action_test_c20fork.py`;
`forks/{c17fork,c19fork,c20fork}/`. Discipline note: keep the A₂ here (discrete symmetry of the SdS solution space)
explicitly distinct from su(3)'s Cartan–Weyl A₂ (a continuous Lie algebra) — same abstract A₂, different realization.

### P3 — slicing-curve paper (`SdS-slicing-curve_v2.tex`): pointer update (AFTER P4 carries the classification)
The "Open" paragraph (l.640) lists exactly the items P4 now closes. Update it to mark them resolved in the
companion groupoid paper — *after* P4 carries the classification, not before (order: bank P4, then re-point P3).

### P11 — boundary paper (`boundary_paper.tex`): the colour-frontier strengthening
- **Colour-closure grounded on P1/P7, not P11's group-theory wall alone:** the cosmogenesis is a signature-
  *preserving* causal-role reassignment on the real horn (P7 l.434/l.618); matter rides the horn; su(3) is across
  the signature seam (operation 4, the global Wick → SO(6)). su(3) is not a symmetry of the world the matter lives on.
- **The non-compactness escape — CLOSED for the geometric-isometry route.** P11's abstract currently lists "one
  precarious escape (the substrate's non-compactness)." The consolidation closes it for the isometry route: su(3)
  is not a symmetry of the non-compact physical substrate (it's on the compact Euclidean face where AH bites), so
  there is no su(3)-charged matter to relocate onto the non-compactness via isometry. Update: the isometry route's
  non-compactness escape closed; the surviving open frontier is empirical coherence alone. *(Carries the
  adjudication's strong-evidence-not-proof weight — bank at that weight, universal do-not-assert, cold-read.)*
- **The ontological framing:** the SO(6) face is real-by-construction but not a co-equal *existent* (the existence-
  criterion: existence requires a span of time, "things do not exist atemporally"; S⁵ is atemporal); the closure is
  **ontological in scope, not structural** (SO(6)/SO(5,1) are mathematically co-equal real forms; CR's commitment
  to the one Lorentzian world breaks the symmetry). Sharpens §sigma/C7.
- **The operation-map:** the seam (operation 3 → S⁴/SO(5)) vs the global Wick (operation 4 → S⁵/SO(6)) distinction
  sharpens C7 and guards the σ-lift against the op-3/4 conflation.

### Foundation (`ONTOLOGY_FOUNDATION_INDEX.md`, `THE_LENS.md`): the adjudication as worked instance
The SO(6)-face adjudication is the foundation's principle in action (the gate reads/holds ontology from the load-
bearing pieces). Record it as the worked instance: Reading-B-as-co-equal closed within CR's ontology, on the
signature line (P7 l.618) + the existence-criterion (5D-Trope), ontological scope, universal do-not-assert.

### P7 — already carries it
The cosmogenesis grounding (l.434/618, signature-preserving reassignment on the real horn) is already in P7,
confirmed verbatim at source by c17 and c19. Used by P11/the foundation; no addition needed (optional cross-reference).

## META-LAYER (the "where we are" docs — updated this revision, my seat)
- `CORPUS_MAP.md`: r311–r316 changelog carries the loop + the propagation (done).
- `THE_SYNTHESIS.md`: r316 re-render note — the consolidated state (done).
- `THE_PLAN.md` / `THE_VISION.md`: frontier/status updated to the loop-complete state + the live frontiers.
- `KICKOFF_GATE.md`: survives (no new paper); reflects the post-r315 banked state.

## SEQUENCING + DISCIPLINE
The canonical-paper edits are the precious object. Order: **(1)** bank the classification into P4; **(2)** re-point
P3's Open paragraph; **(3)** strengthen P11 (colour-closure on P1/P7, non-compactness-escape closed, ontological
framing, operation-map); **(4)** record the adjudication in the foundation. Each is made source-grounded and stated
for reversal, then the **fresh whole-result cold read** the consolidated verdict named tests the corrected papers
(edit→cold-read). The P4 classification is the most-verified/least-contested (the convergent gem, source-computed);
the P11/foundation distillation carries the adjudication's strong-evidence-not-proof weight, banked at that weight.
Done paper-by-paper at the care the precious object demands — not raced.
