> **⌖ RETIRED r1543.** This was the P15/P16 closeout (r966) — the outstanding pieces worked one at a time. **Landed:** both papers exist, compile, and are receipt-certified.
> Kept as record; **do not work from it.**


# P15 / P16 closeout — the outstanding pieces, worked one at a time, with the collapse-trap named in the open

**The discipline (r944, Daryl-directed).** P15/16 are the corpus's most ontologically exposed papers:
their standard machinery (Boltzmann codes, r_s/r_D pipelines, BBN networks) is *by construction* a
physics of shadows — it silently commits the synchronous category error (§1·SHADOW) in every routine.
So each item below carries, in the open, **what is to be computed** and **the collapse-trap** — the
shadow-move one is naturally drawn into — so the slip is visible before it is made. The rule for every
item: **the physics runs on the ontological layer (real GR on the evolving 3D leaf, radiation
gravitating normally — the *local dynamics*), and the observed quantity is that physics *projected*
through the reassigned E=1 shadow (the non-synchronous, flat/closed distance slicing — the *observable*).
Never run the shadow's machinery as if the shadow were the existent.** The template already done right
is P15 §largescale (the closed-S³ source projected through the flat transfer — the flat/closed
decoupling carried by the non-synchrony); the template done wrong is my r939–40 Silk computation.

Disposition tags: **[CLOSE]** = tractable, work to done; **[FRONTIER]** = genuine external open problem,
publish-open, confirm it is honestly marked and not a collapsed straw; **[DEFUSED]** = argued
irrelevant/settled, mark clearly as not a blocker.

---

## The seam this closeout actually works — the titles held to the criterion of necessity (r950, Daryl-directed)

**The epistemological heart (P6 §1g, R2 / least-arbitrariness — "the criterion of necessity, the
load-bearing one").** What turns a coherent proposition into a *proof* — what makes CR a science and not
a self-consistent story — is R2: the structure must *require* the phenomenon, not merely *permit* a value
that fits it. This is the bar the papers' titles are staked at, and it is the bar this closeout sorts by.

**The titles are aspirational names, committed to.** P16's title — "the Big Bang as a synthesis of CR's
*deductively forced* consequences, and the primordial light-element abundances *it produces*" — stakes the
paper at R2-necessity for the *whole* pattern. Held honestly and fairly: the **synthesis** half is earned
at necessity (the forced conjunction — P7's theorem chain), and **He-4** is *produced* (computed,
Y_p=0.2506); but "produces" for the *whole* abundance pattern jointly is the computation not yet run. We
read the title as the aspiration it names — the finished Big Bang computation — and **commit to realising
it**, marking the gap in the open (P16 §verdict now does this) rather than letting "produces" stand as
though the whole pattern were already required. P15's title is descriptive and does not over-stake; its
discipline is the same sort applied to its open items. **Nothing buildable is shipped under the rug as if
done.**

**The R2 sort — what each tag *means* against the bar (buildable ⇒ not done):**
- **[CLOSE] = a DEBT OWED, not a frontier.** Buildable now, and load-bearing on a title's necessity claim:
  it *must* be built to claim the title truthfully. The flagship debt is **the Big Bang computation** — the
  full multi-abundance network on CR's collapse thermal history T(τ̃),ρ(τ̃) (P16-A + the joint likelihood
  run), which is what makes P16's "produces" a proof for the whole pattern at R2-necessity. A [CLOSE] item
  left undone is the title running ahead of the proof.
- **[FRONTIER] = a GENUINE UNKNOWN, permitted open.** A real external question whose staying-open costs the
  title nothing — the *derivation* (vs inheritance) of η / ρ_r/ρ_m and the progenitor spectrum is the type
  case: exactly as flat ΛCDM carries its own η as measured boundary data, CR may, at no cost to "produces."
  The title does **not** stake itself on these; marking them open is honesty, not a debt.
- **[DEFUSED] = settled/irrelevant** — argued not to bear on any title claim.

**The commitment.** The end goal is to finish the Big Bang computation so P16's title is earned at
necessity, not merely at coherence — and to keep P15/P16 honest in the interim, every [CLOSE] debt visible
and owed, every [FRONTIER] a true horizon and not a swept-under-the-rug debt in disguise. No rush; but no
pretending a buildable debt is a frontier, and no letting a confident title paper over an unrun computation.

---

## P16 — the cosmogenesis paper

**P16-A [CLOSE] — the exact abundances D/H and Li-7 through a full network.** *To compute:* run a
nuclear network on the cooling-leg thermal history to confirm D/H, Li-7 (He-4 already computed from
first principles, r936, Y_p=0.2506). *Collapse-trap:* the two-rates veer (§1b link 6) — running the
*observable* radiation-free rate in the nuclear window (≈300× too slow, fatal) instead of the *local*
window rate (ordinary GR, radiation gravitating). P16 §rate/§scoping already scopes this correctly, so
the reduction is sound; the trap is only re-entered if one "simplifies" to the observable rate.
*Status:* reduction verified; He-4 computed; D/Li stand on the verified reduction, need a code
(AlterBBN unreachable r936) or a validated reduced network. Bounded, not a frontier.

**P16-B [FRONTIER] — the full multi-abundance likelihood** (one handover fits D, He-4, and the
metallicity floor together). The sharp refutation edge, a data confrontation. *Collapse-trap:* none of
the shadow kind; the trap here is treating a not-yet-run likelihood as if run (the r932 coherence≠
completeness lesson). Publish-open; confirm P16 §verdict marks it not claimed (it does).

**P16-C [FRONTIER] — the derivation (vs inheritance) of η and the progenitor spectrum** (the
baryogenesis-analogue). Matter-sector depth. Like η in ΛCDM, may remain a measured boundary datum at no
cost. Publish-open; P16 §verdict marks it not claimed (it does).

**P16-D [DEFUSED] — the exact regulated peak temperature** ("how deep the compression runs through the
branch point"). P16 §peak argues it downstream-irrelevant (total dissociation erases the peak's memory;
the abundances are fixed in the window on the cooling leg). Not a blocker. Confirm the argument stands
and mark clearly.

---

## P15 — the cosmology paper

> **⚠ RECONCILED (r966, Daryl-directed) — this entry FLIP-FLOPPED on the one assignment the damping
> signature rides, and the record must own it.** As written below (r945), this entry holds the
> acoustic/diffusion physics to be **radiation-INCLUDED** (L2 local dynamics on the closed-S³ layer)
> and calls computing r_D **radiation-free** "the collapse-trap … which manufactures the ~8% θ_D
> tension" — i.e. r945 judged the +8% an ARTIFACT. Yet the same entry then *sizes* the +8% on the
> radiation-free rate (below), and the CURRENT P15 §coherence commits to **radiation-FREE (L1)**
> diffusion via the three-level rule ("the diffuse plasma rides the L1 foliation … one may not take
> the rate radiation-free for the peak spacing and radiation-included for the diffusion"), making the
> +8% a *genuine* CR effect. **Settled reading (r966):** the three-level rule is authoritative — the
> recombination plasma is diffuse content riding the global foliation (L1), NOT a self-gravitating
> local excursion (L2, which is the progenitor collapse where BBN runs), so the diffusion is
> radiation-free and the +8% is real *in the diffusion scale*. **But the flip-flop is exactly why the
> +8% is delicate**: it rides an L1-vs-L2 assignment for the recombination plasma that the corpus
> genuinely contested, and its *observable* consequence remains open pending the high-ℓ transfer
> (r966, gated on CR's perturbation sector). So r945's "trap" instinct was half-right — not that the
> +8% is an artifact, but that reading its observable weight off a fixed-ΛCDM-peak assumption
> (the near-refutation) is the real trap. The r945 text is retained below as the record of the
> earlier reading; this note supersedes its L2/"artifact" verdict.

**P15-1 [CLOSED r945 → RECONCILED r966 — flagship] — the acoustic-spectrum end-to-end transfer** (peak *heights* + the damping
tail). *To compute:* carry the coherent single-phase seam datum through to recombination and confirm the
height pattern, INCLUDING the Silk-damping scale θ_D, **holding the ontology**: the acoustic/diffusion
physics is local dynamics on the closed-S³ layer (radiation-included → the ordinary physical r_s, r_D),
projected to the E=1 sky through the non-synchronous flat/closed distance slicing. *Collapse-trap (the
one I fell into):* compute r_s, r_D on the flat *observable radiation-free* rate and read θ = scale/D_M
as if the shadow FLRW were the existent — which manufactures the ~8% θ_D "tension." *[r966: superseded — see reconciliation note above; the diffusion IS radiation-free (L1) per the three-level rule, the +8% genuine in the scale, its observable weight open.]*

*CLOSED (r945) — computed the paper's way, gated against CAMB. The oscillation is over.* The scales
are the ones P15 defines, not the r939–40 hybrid the paper names as an artifact ("the standard
radiation-governed sound horizon laid on the radiation-free rate … belongs to neither framework").
Receipt: `computations/peak_heights/damping_ratio_clean.py`.
- **Gate:** on the ΛCDM rate with CAMB x_e(z), r_s = 144.0 (CAMB 144.4), π·r_D/r_s = 0.143 (CAMB
  0.154; the ~8% r_D normalization is z-independent and cancels in the ratio). Pipeline reproduces CAMB.
- **Acoustic scale + Hubble resolution:** r_s = ∫_{z_rec}^{z_onset}(c_s/H_free)dz = 145.5 → 100θ_* =
  1.044 (obs 1.041). At **fixed Ω_m**, r_s and D_M both ∝1/H₀ → 100θ_* = 1.0440 *identically* at
  H₀ = 67.4/70/73. H₀-independent, met at the directly-measured value. Clean, not a collapse.
- **Damping tail — the one CR-specific piece, SIZED:** r_D on the radiation-free rate near
  recombination (post-onset; the leaf's own sinh^{2/3} rate, radiation not sourcing it) is **+9%**
  longer (H_free ~15% below H_incl at z_rec). r_s matched in both, so **θ_D/θ_* : ΛCDM 0.0457 → CR
  0.0492 = +7.9%** (100θ_D ~0.161 → ~0.174). Projection-independent.
- **Disposition:** a genuine, modest, testable CR-specific signature — *exactly* the object P15
  §coherence already flags not claimed ("the Silk scale need not coincide with ΛCDM's … the one
  genuinely CR-specific piece … stated without being claimed"). The computation **confirms and sizes** the
  paper's existing honest flag; **no paper revision is forced** — the number is a receipt. Whether
  +7.9% is inside Planck's high-ℓ tolerance is the full-likelihood question → P15-4 frontier.
  > **⊙ UPDATED (r965→r966).** The reabsorption bound (`damping_reabsorption.py`) does add something:
  > the ~8% is in the diffusion **scale** and is **non-reabsorbable** by ω_b (~29% needed vs ~1%
  > allowed). But r965 then over-reached — escalating to "several-σ sharpest edge" and a
  > "near-refutation" via a transfer pass (`full_transfer_verdict.py`) that **assumed CR's high-ℓ
  > peaks equal ΛCDM's**, which is exactly the unbuilt part, not a free input. **Daryl caught the
  > panic (r966); reverted.** Honest status: a computed, non-reabsorbable ~8% diffusion-scale effect
  > whose **observable** high-ℓ consequence is entangled with the unbuilt acoustic transfer —
  > genuinely open, neither tension nor wash (`crossed_wire_hunt.py` shows the +8% itself rides a
  > subtle r_s seam-cut asymmetry). So this record line's "no paper revision forced / modest" was
  > closer to right than r965's escalation; the one real add is the non-reabsorbability.
- **Correcting r941 precisely:** its "artefact" verdict was right about the *method* (the hybrid r_s)
  but I over-generalized to "probably dissolves." Gated: it does not dissolve — done the paper's way
  the effect is a real +7.9%, because the diffusion genuinely runs on the leaf's radiation-free rate
  while r_s is the matched onset-truncated scale, not the swapped one.
- **Heights** DERIVED = ΛCDM by reduction (r939, stands). **This is the piece the whole oscillation
  lived in — now closed with a gated number and consistent with the paper as written.**
- **r946 — the ontology baked so it cannot re-collapse.** The which-rate determination is now stated
  explicitly at every load-bearing spot as the **three-level reading (L1/L2/L3)** with its decision rule
  (ontology map new **§1·LEVELS**; §1·SHADOW's L1-as-shadow conflation corrected; P15 §tensions/§coherence
  own it; P16 §scoping names L3 + the recombination instance; P7 capstone clarified). Recombination is
  L1 (from-seam-outward) → r_D radiation-free → +7.9% **forced, not adjudicated**, locked to the Hubble
  resolution. The map pictured: `computations/reassignment_projection_map.png`. All papers compile clean.

**P15-2 [FRONTIER/CLOSE-adjacent] — the exact large-angle depth** (low-ℓ deficit). Already built
correctly (the closed-S³ source through the flat transfer — the right template); residual is a full
Boltzmann/exact non-synchronous transfer (~10–20%), not expected to move the deficit's existence,
location, or order-of-magnitude. The genuine open is the data comparison (done, cosmic-variance-limited,
octopole the falsification edge). Publish-open as a firm parameter-free prediction. *Collapse-trap:*
projecting the closed source through a *closed* distance slicing instead of the flat one (the
flat/closed decoupling is the physics) — avoided in the existing build.

**P15-3 [FRONTIER] — the progenitor spectrum n_s, A_s** (inherited boundary data, baryogenesis-analogue,
matter sector). The most ambitious reach; a consistency target, not a geometry-only derivation.
Publish-open; honestly marked.

**P15-4 [FRONTIER] — the likelihood-level / full-spectrum model selection.** Low-ℓ likelihood done
(estimator-limited, leans ΛCDM at central octopole — reported straight, not a collapsed win); the
full-spectrum likelihood needs the oscillating-medium transfer (matter sector, downstream). Publish-open.

**P15-5 [FRONTIER] — the detailed matter-crossing dynamics** (worldline/field dynamics of the one
seam-crossing for a concrete matter model). Shown well-posed and structurally governed; the detailed
depth deferred. Publish-open.

---

## Order of work
1. **P15-1 damping tail / spectrum transfer** — ✅ DONE (r945). Gated against CAMB; θ_D/θ_* = +7.9% on
   the radiation-free diffusion, a genuine CR-specific signature, not claimed, consistent with the
   paper as written (§coherence already flags it). Heights = ΛCDM (r939). Acoustic scale + Hubble
   resolution confirmed clean.
2. **P16-A D/Li network** — bounded; get/build a validated network, confirm the anchors. NEXT.
3. **P16-D exact peak** — confirm the downstream-irrelevance argument, mark [DEFUSED].
4. **The FRONTIERS (P15-2..5, P16-B,C)** — confirm each is honestly marked, not a collapsed straw;
   they stay open as the paper's genuine problems, which is what "done" permits.
When 1–3 are worked and 4 is confirmed honest, P15/16 meet the "only genuine problems remain" bar.
