> **⌖ RETIRED r1548.** A work package of the antimatter/conjugation front whose question is closed — see the correction above. Kept as record; **do not work from it.**


> **⚑ THE ITEM THIS DOCUMENT HOLDS OPEN IS DEAD (corrected r1548).** The sheet-to-ruling / antimatter-naming
> question was **KILLED at r1280** — `THE_OPEN_PROBLEMS_LEDGER` family 2, *resolved dead, migrated open→closed*
> — when P13's CPT factorisation `C = (Q↦−Q)_field ∘ (R∘K)_geometric` showed the rulings and the wings are
> **the linear and antilinear faces of one object**, not two candidates for a bijection. **And the naming is
> settled in the corpus:** P7 carries `thm:antimatter-progenitor` — *"The antimatter progenitor: our universe
> from an antimatter black hole"* — with the branch point exchanging the species.
> **Fourth and fifth documents found carrying this kill unpropagated**, after `THE_PLAN` (r1442), the
> entry-point register (r1498) and `ANTIMATTER_FRONT_PLAN` (r1542). **One closure, five documents.**


# WP-E — the antimatter naming: OPEN, do-not-assert (corrected r987)

> **Correction (r987).** The original close below ("the matter sector answers NO") was the
> gospel error — grounding in the corpus's *assertion* (faces r693/r694). The matter sector attaches
> the "antimatter register" to `R=γ⁵` but holds it "a resonance, not an identity" — i.e. it is **not
> worked out**, so it settles nothing. The honest state: geometrically B is the mass-reflected
> (r→−r) dual (the *description*, "conjugate dual"); whether that — or `R=γ⁵`, or any of it — is
> **antimatter** is undischarged on either side, held **do-not-assert**, a genuine drill-site. The
> "rate" frontier was separately spurious (my inflation of an unmotivated musing; the reassignment
> is a causal-character swap, a null congruence is null) and is removed from P7. The reasoning below
> is kept as the record of what the corpus *does* establish (the R/P/T register distinctions), read
> now as description, not as a settlement of the name.

---

# (original) does the conjugate dual earn "antimatter"? The matter sector already answers: NO (r986)

Source: `matter_sector_paper.tex` (esp. §"crossing", the r968 parity footnote), `boundary_paper.tex`
(C-blindness). Status: **settled** — this is not a new construction, it is reading what the corpus
already establishes and applying it to the bead's conjugate branch.

## The question
The front's whole naming question: does B — the bead's conjugate ($r<0$) branch, the red
"conjugate dual" — earn the name **antimatter**? WP-A walked the figure back to "conjugate dual"
pending this; the user chose "Large" scope to pursue WP-E and see if it could be earned.

## The corpus's own answer
Antimatter = CPT conjugate. The matter sector fixes the registers precisely (r968 convention,
`matter_sector_paper.tex:212` footnote):

- **R = γ⁵** — the orientation / **mass-reflection** parity `r₀ ↦ −r₀` (equivalently `2M ↦ −2M`),
  `∈ O(5,1)∖SO₀(5,1)`. This is **"the antimatter register."** It is *spacelike and horn-preserving*.
- **P** — the areal **spatial** parity `r ↦ −r`, Clifford generator `γ¹γ²γ³`, **anticommutes** with γ⁵.
- **T** — the time reflection.

And the paper states, flatly:

> "the chirality/mass parity R=γ⁵ (the antifundamental, the antimatter register) and the time
> reflection T are **distinct** reflections — a resonance, not an identity … R spacelike and
> horn-preserving, T timelike and horn-flipping, so R≠T … **the antimatter register is not the
> time-structure, and matter/antimatter cannot be a cosmogenesis (time) event.**"

## Applying it to the bead's conjugate branch
The bead crosses the seam by the **areal** reflection `r ↦ −r` (the signed areal radius going
negative). By the corpus's own naming that is **P**, the cosmogenesis conjugate — *not* R. The
conjugate branch (`r<0`) is the prior-universe / collapse leg reached through the branch point at
`r=0`; it is a **time / cosmogenesis** structure. The antimatter register R = γ⁵ is the
**mass** reflection `r₀ ↦ −r₀`, a *different*, spacelike, horn-preserving parity, anticommuting
with P, living in the fermion sector — explicitly held "a resonance, not an identity" with the
time-structure.

So calling B "antimatter" would identify the cosmogenesis conjugate (P / the bead's `r<0` branch)
with the antimatter register (R = γ⁵) — exactly the identification the matter sector **rules out**
("matter/antimatter cannot be a cosmogenesis event"). It is the resonance-mistaken-for-identity the
paper warns against.

## Verdict — the front closes
- **B earns "conjugate dual," not "antimatter."** The WP-A walk-back was correct, and WP-E gives the
  *reason* from source: the antimatter register is R = γ⁵ (mass reflection), a distinct register
  from the bead's cosmogenesis conjugate (P, areal `r ↦ −r`).
- This does **not** need the field-level C construction after all: the corpus already separates the
  registers, so the naming is settled without building C. (The C-blindness wall,
  `boundary_paper.tex:74–85`, is the same message from the other side: C is not geometric, so
  "antimatter" is never read off the bead geometry.)
- Landed in P7: the frontiers item and the thm:bead Third-fact now state the sheet assignment
  (photon real; both null bundles ride the level-S³ spaces onto the ±πα/3 wings) and the naming
  caution (conjugate branch = P ≠ antimatter register R=γ⁵, `\cite{JanzenMatter}`). Receipt
  `sheet_assignment.py`.

## What is left open (and it is *only* this)
The **reassigned-congruence rate** — whether the at-rest→null photon congruence runs at a definite
multiple of the null rate. Unmotivated, asserted nowhere, held do-not-assert. That is the single
surviving dangler of the whole front.
