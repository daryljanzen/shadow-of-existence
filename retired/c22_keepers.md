> **⌗ RETIRED r2380 under `RG-1` — the keepers were kept, and its numbering is two eras stale.**
> *A c23-era normalisation of c22's keepers, and its own head is the tell:* ***"NUMBERING NORMALIZED TO MY (c23)
> 13-PAPER MAP. c22 used an 11-paper spine where the SdS-cosmology paper was 'P7'; in my map that paper is
> P9 = CR_flatLCDM_v2."*** *The corpus now has **seventeen** papers and neither mapping survives — so a node
> following its section pointers (§679, §665, §436) lands in a paper that does not exist under that name.*
> ⌗ *Its own audit is marked complete by content, and the r2234 locus repairs this line made at r2377 touched it
> four times — **four stranded `$r=0$` symbols in one file**, which is itself a measure of how much era-scrubbing
> it carries. **A document that needs a translation table at its head to be read is a record.***

---

# ⚠ NUMBERING NORMALIZED TO MY (c23) 13-PAPER MAP. c22 used an 11-paper spine where the SdS-cosmology
# paper was "P7"; in my map that paper is **P9 = CR_flatLCDM_v2** (title: Null-Boundary Correspondence and
# Non-Synchronous Cosmology), and **P7 = modern_parallax**. All cosmology refs below are scrubbed P7→P9.
# c22's §/line numbers (§679, §665, §436, §516, eq:obs-friedmann) are c22-ERA refs to that paper — re-ground
# exact locations at my P9 source when building (verified live: the early-universe/Hubble remark is P9 ~l.679;
# the NBC + reassignment + throat machinery are P9, confirmed 44 hits; modern_parallax has 0). P5=slicing_operator,
# P6=range match across both eras. 'When Black Holes Happen' is a foundation-backbone essay, not a spine number.
# AUDIT (complete, by content): the only spine numbers in this file are P5=slicing_operator, P6=range (both
# BEFORE the insertion point → unshifted → correct), P7=modern_parallax (correct), P9=CR_flatLCDM (cosmology,
# scrubbed from c22's P7). NO references to the shifted-later papers canonical_time/dynamics/algebroid/boundary
# (= my P10-P13, were c22's P8-P11). RULE: any paper number inherited from c22 is suspect → anchor on
# filename/content, verify against my map; a bare digit is never load-bearing on its own.

# C22 HUBBLE-ARC MINING — keepers + quarantine (c23, reading the transcript)
# Source: c22_transcript.txt. Rule: trust source over dramatic numbers; pull good, quarantine bad.

## QUARANTINE (known-bad, do NOT inherit)

## KEEPERS (load-bearing for the two open problems: medium+c_s ; beginning/limits ; projection)

## OPEN QUESTIONS C22 RAISED

### QUARANTINE
- **The +74% sound horizon (r364, reverted).** r_s≈251 Mpc vs ΛCDM 144 Mpc, driving CMB-H0 the WRONG way (67→39). Method: delete radiation term from H(z), keep standard acoustic physics + sound-travel integral to z→∞. CHIMERA — imports the standard plasma + integral-to-infinity CR doesn't have (status doc confirms). "CMB-excluded" is an artifact of this wrong method. DEAD.
- The "no-horizons / GW-echo" branch audit (turns 33-37) — fine but not Hubble-load-bearing; near-degenerate with std BH by construction. Not a keeper for the acoustic problem.

### KEEPERS (turns 31-37)
- **ΛCDM sound-horizon baseline = 144 Mpc** (sanity-check passed). The number any correct CR r_s must be tested against.
- **The acoustic scale θ_* is measured to ~0.03%** — an extremely tight target; r_s/D_M must land on the observed θ_*.
- **The target constraint (c22, turn 37):** a viable CR early universe must yield an acoustic scale matching observation — equivalently, given a robust (flat-ΛCDM) D_M, r_s must come out ≈144 Mpc despite the radiation-free expansion; OR r_s differs and something else compensates. This is THE gating quantity.
- **BBN angle:** CR's "radiation plays no role in the rate" must still reproduce light-element abundances — an independent early-expansion constraint, uncomputed, a place CR could break OR be confirmed.
- World-state (mid-2026): Hubble tension >5σ, hardened by JWST+DESI+Planck; CR sits in the early-universe/sound-horizon class; DESI BAO agrees with ΛCDM sound horizon; sound-horizon reduction ALONE known insufficient. (Context for what a CR resolution must clear.)

### KEEPERS (the recovery, turns 49-53) — THE LOAD-BEARING ONES
- **The strawman's error, diagnosed exactly (turn 51):** P9 §679 requires TWO pieces — (a) model the sound-horizon evolution in the 3-sphere ontology, AND (b) PROJECT it into the observational frame. The naive computation does (a) and skips (b): it computes in a *flat model of the construction* instead of *in the construction*. The skipped projection is the whole error.
- **θ_s = r_s / D_A** — both numerator and denominator must come through the projection, not the flat shortcut.
- **r_s: the bigger value is REAL, not an artifact (turn 53).** Fundamental observer reads (ȧ/a)²=2M/a³+1/α² (matter+Λ, no radiation; P9 eq:obs-friedmann). Early times H∝(1+z)^{3/2} (matter-like) vs ΛCDM (1+z)² (radiation-like) → slower early expansion → r_s=∫c_s/H dz genuinely LARGER than ΛCDM. The strawman's r_s was right *as far as it went*.
- **Open problem #1 — candidate resolution (turn 53):** the plasma IS there — photons and baryons exist (as projected densities), so the sound speed c_s is STANDARD (≈c/√3). The medium instantiates; c_s is ordinary. [matches status-doc line 204-206: acoustics-if-present are ordinary fluid; open piece = whether plasma instantiates → c22 says it does]
- **★ THE ISOLATED QUESTION (two years uncomputed, now clean, turn 53):** the strawman silently used FLAT D_A. But the ontology is a closed S³ of radius α∼Hubble radius, and the comoving distance to recombination is *several times α*, where the closed angular-diameter relation departs sharply from flat. **Does the closed-S³ projection of the distance to last scattering absorb the enlarged sound horizon, or not?** i.e. does D_A move to compensate the larger r_s so θ_s=r_s/D_A stays at the observed value? MUST be computed from the reassignment-geometry null geodesics — NOT reasoned by reflex (that reflex is the strawman).
- **TENSION TO RESOLVE vs the settled status doc:** status doc says "D_M (comoving) robust, flat-ΛCDM, low-z dominated." c22 says "D_A (angular-diameter) departs sharply from flat at recombination." Reconcile: D_M=∫dz/H is low-z dominated (flat-ΛCDM ✓); D_A is the ANGULAR projection of D_M through the closed S³ (the sin-type focusing) — a different object. Which governs θ_s, and does crux-1's reassigned-congruence already fix it? OPEN — carry to Part 2.

### KEEPERS (turns 53-55) — mechanism + verified pieces
- **Open problem #2 (integration limit) — c22's resolution:** the beginning is the branch point at $r=0$ (well-posed because $r_*$ CONVERGES, **not** because curvature is finite -- it diverges there; r2234) (P9 l.665), but in the OBSERVER frame it sits at a→0 (z→∞), and radiation-free H∝(1+z)^{3/2} makes r_s=∫c_s/H dz CONVERGE there. So the seam is ontologically deep but does NOT change the observer's r_s integral or its upper limit → the cosmogenesis side doesn't enter this number; the computation reduces to the D_A projection. [Daryl turn 54: the seam is NOT a reserved wall — read it off the construction like anything else.]
- **Both reflexes are dead; the projection IS the answer (turn 55):** flat reading → exclusion; naive-closed ("α∼Hubble radius so |Ω_k|∼1") → grossly excluded by measured flatness. Yet the observer reads flat. So the projection is precisely *what makes a closed S³ ontology read as flat to the comoving observer*, and θ_s lives in how it carries a recombination scale to an angle.
- **★ THE MECHANISM — the non-synchronous tilt:** SdS fundamental metric ds²=−dτ²+(∂_χr)²dχ²+r²dΩ², r=r(τ+χ) NOT τ alone. r depending on τ+χ is the NON-SYNCHRONOUS tilt, and *that tilt is the entire mechanism by which a closed S³ reads as flat-ΛCDM to the comoving observer* — exactly what the flat shortcut discards. Redshift, D_A, r_s all from the null geodesics of THIS metric. Killing vector ∂_τ−∂_χ (metric depends only on τ+χ).
- **★ VERIFIED redshift law (direct null-geodesic integration = invariant law, all digits):** 1+z = (R'(τ̃_o)−1)/(R'(τ̃_e)−1), NOT the naive areal ratio r_o/r_e. DIVERGES at R'=1 → a FINITE areal radius r*≈1.19 (amplitude units), NOT r=0. Computational confirmation of P9 l.665: the past light cone hits infinite redshift at the finite-curvature seam; the entire z>1090 region is a thin τ̃-shell just outside the seam (nothing like an integral to r=0).
- **QUARANTINE (caught shortcut):** D_A = areal-radius-at-emission is WRONG (fails z→0: D_A must →0 but areal radius →r_o≠0; the comoving observer sits at χ=0 but finite areal radius r_o, not center r=0). No quick compensation claim from it.

### ★★ THE FORK c22 HIT (turns 57-59) — the hinge, source-grounded
c22 computed D_A from the optics of the non-synchronous metric and found:
- The metric is VACUUM: R_μν k^μ k^ν = 0 → NO Ricci focusing → D̈_A=0, an **EMPTY-BEAM (Dyer–Roeder) distance**.
- Flat-ΛCDM's D_A(z) is a **FILLED-BEAM** distance: matter both drives expansion AND focuses light.
- In CR the geometric "matter" term 2M/r³ sits in the EXPANSION (eq:obs-friedmann), NOT in R_μν k^μ k^ν → on the bare vacuum metric it does NOT focus light. **Expansion sees matter; the bare beam doesn't.** That gap = the whole deviation.
- Result on the naive null geodesics: D_A^SdS runs 10% high @z=0.3, 42% @z=1, ~2× @z=2 → would break SNe/BAO at z≲1.
- **The fork (lands on the keystone, not arithmetic):** (a) literal photons-on-vacuum-null-geodesics → empty-beam → low-z SNe deviation (worse than the CMB problem); OR (b) matter / "the bend of the cut" couples into light propagation by a route the bare vacuum metric doesn't carry. An ONTOLOGY question (P5/P6/dagger).
- **Source check (turn 59):** P9 l.516 only ASSERTS flat-ΛCDM redshift–distance from the scale-factor coincidence (eq:LCDM); there is NO derivation of light propagation. For an FRW spacetime the inference is automatic; for this non-synchronous VACUUM spacetime it is NOT. That is the seam c22's computation pried open.

### ★★ THE RESOLUTION (from the corpus I hold + status doc, which postdates this transcript)
c22 computed redshift+D_A on the **NAIVE null geodesics of the frame** — the WRONG photon congruence. The settled corpus result (status doc crux 1; P9 §436): CR's photons follow the **reassigned constant-R congruence** (the OTHER null ruling, spacelike-in-dS, reinterpreted as the null photon congruence under the reassignment), NOT the bare vacuum null geodesics. On the reassigned congruence the redshift is the scale-factor ratio 1+z=r(τ̃_o)/r(τ̃_e) and the redshift–distance is flat-ΛCDM EXACT. So c22's "42% D_A deviation / SNe exclusion" and its (R'−1) redshift law (diverging at the finite seam r*) are the **wrong-congruence artifact** — the same family as the dead low-z blueshift. c22 correctly flagged the fork; branch (b) is right, and it is the reassigned congruence.
- **★ DEEP KEEPER — empty-beam vs filled-beam IS the content of branch (b):** the reassigned constant-R congruence is precisely how CR supplies the **filled-beam** focusing (matter coupling to light) that the bare vacuum geodesics lack. That is WHY D_A comes out flat-ΛCDM on the right congruence. The "matter couples to the beam" question c22 posed = the reassigned congruence answers it.

### ★ CONSEQUENCE FOR THE ACOUSTIC SCALE (the synthesis seed for Part 2)
- On the right congruence, **D_A to last scattering is flat-ΛCDM (robust)**. So θ_* = r_s/D_A with D_A = ΛCDM ⇒ to match the measured θ_*, CR's r_s must come out ≈ 144 Mpc.
- Therefore the compensation can NOT come from D_A (D_A is flat-ΛCDM). It must be that **r_s itself, computed correctly, is ≈ standard** — which throws weight onto the two r_s open problems: the MEDIUM (plasma instantiates, c_s≈c/√3 — c22 keeper) and especially the INTEGRATION LIMITS / the beginning. c22's "r_s genuinely larger" rested on (wrong-congruence high-z structure) + (chimera: standard plasma & integral-to-z→∞ with only radiation removed from H). The live candidate (status doc): the beginning is the branch point at $r=0$ (well-posed because $r_*$ CONVERGES, **not** because curvature is finite -- it diverges there; r2234), possibly = recombination, in which case there is NO long pre-recombination sound-travel integral and r_s is set by / inherited from the seam (collapse side) — which could be ≈ standard.
- **So the Part-2 target sharpens:** redo r_s on the RIGHT congruence and the RIGHT early-universe structure (where does the reassigned-congruence z→∞ / a→0 sit; is there a pre-recombination acoustic era at all; what does the seam inherit), with D_A pinned flat-ΛCDM. The question is no longer "does D_A compensate" but "does the correctly-built r_s land at ~144 Mpc (no tension) — and if so, is the Hubble tension then dissolved because CR's single Λ-fixed H_0 = the late value with a standard acoustic scale."

### ★★★ THE RESOLUTION c22 REACHED (turns 59-61) — the composition
- **"matter = bend of the cut" grounded (P5 §bend/§cosmology, P6):** a straight cut is vacuum, a bent cut carries ρ=m'(r)/4πr². The Friedmann readout (ṙ/r)²=8πρ/3+Λ/3 splits by coth²=1+csch² into the "1" (pure dS, unbent) and the csch² (dust ρ∝a⁻³, the bend). DECISIVE clause (P5): the cut r(τ) is primary; **"ρ is the name of its bend, not its cause… read off afterward."** The cosmological dust is the READING of the constant Nariai mass in the timelike vantage, NOT a distributed Ricci source. So R_μν=Λg_μν holds on the substrate and the beam genuinely does NOT focus there. → KILLS "the bend focuses the beam on the substrate"; the unfocused empty-beam result is FAITHFUL to the substrate.
- **★ Therefore the resolution lives in the SHADOW/PROJECTION, not the substrate (Daryl's framing, made literal):** the observer does NOT measure the bare vacuum substrate (the real thing, empty-beam); they measure the projected flat-ΛCDM shadow, in which the dust is a real density that focuses light the ordinary filled-beam way. **The 42% gap is the gap between projector and projection — the whole subject, not an error to patch.**
- **The two-chairs (double-dagger):** c22 computed D_A as the FIRST chair sees it (off the surface, bare substrate, unfocused) and read the 42% as the world. The cosmological observer is the SECOND chair (on the surface, looking along it) and measures the projection. "The spike is real and shared; the catastrophe is the sitting."
- **★★ THE COMPOSITION (c22 turn 61, from When Black Holes Happen + the dagger backbone):**
  1. **Real thing** = the non-synchronous expanding 3-sphere (eq:SdS-fundamental), the ontology.
  2. **Shadow** = flat-ΛCDM, what the on-surface observer measures: the redshift–distance relation AND **D_A(z) is the standard FOCUSED flat-ΛCDM one** (algebraic identity of a=sinh^{2/3}). [⇒ D_A robust/flat-ΛCDM — matches crux 1 + status doc; resolves the 42% fork: the empty-beam substrate D_A is NOT what's measured.]
  3. **The ONE deviation that survives the projection** = the **sound horizon**. Radiation-free geometric expansion → a different r_s → biases the CMB-inferred H_0. **That is the Hubble tension, the single testable seam.**

## ★ THE PART-2 CRUX (sharp, still open — the one real computation)
With D_A pinned flat-ΛCDM (shadow) and the medium settled (plasma instantiates, c_s≈c/√3), the entire Hubble question reduces to **r_s computed correctly**, and there is a hard DIRECTION problem to settle honestly:
- θ_*=r_s/D_A measured to 0.03%; D_A=flat-ΛCDM. To RESOLVE the tension (raise CMB-inferred H_0 from 67 toward 73) the literature requires a SMALLER r_s (~7%). But c22's "radiation-free → slower early expansion → LARGER r_s" pushes the inferred H_0 the WRONG way (the +74% disaster). So "radiation-free ⇒ bigger r_s" as computed is either wrong or fatal.
- **The escape, and the genuinely open piece — the integration limits / the beginning (open problem #2):** the live candidate (status doc) that **the finite-curvature seam IS (at/near) recombination**, so there is NO long pre-recombination radiation-free sound-travel era to balloon the integral; r_s is then set by / inherited from the seam (the collapse side / cosmogenesis, NBC P9 §665) — could land ≈ standard or smaller, not larger. This is where the cosmogenesis genuinely enters, and it is the crux that decides resolve-vs-worsen.
- So Part 2 = build r_s in the SHADOW projection with the CORRECT early-universe structure: (i) where the reassigned-congruence z→∞/a→0 and the seam sit relative to recombination; (ii) whether a pre-recombination acoustic era exists on this side at all, or the seam=recombination inherits r_s from the collapse side; (iii) the resulting r_s and the sign of the H_0 bias. THE corpus pieces to lean on: the NBC (P9 §665), the reassigned constant-R congruence (P9 §436, crux 1), When Black Holes Happen, the dagger/two-chairs projection backbone, P5 §bend/§cosmology.

### ★★ THE SHARPEST KEEPER (turns 63-65) — the empty-beam 180× and the congruence lever
- **The empty-beam is CMB-excluded by 180× (turn 63, computed):** metric is Einstein (R_μν=Λg_μν, R=4Λ) but Weyl²≠0 (genuinely SdS, de Sitterizing) — so the beam COULD shear, but spherical symmetry (θ,φ tidal equal → no shear) + R_μν k^μk^ν=0 makes the radial cosmological beam genuinely UNFOCUSED, empty-beam (Dyer–Roeder η=0). Empty-beam D_A deviation: 0.08%@z=0.1, 1.9%@z=0.5, 6.8%@z=1, 21%@z=2, 38%@z=3, and **~180× the filled-beam value at z_rec=1089 → θ_s ~180× too small → first peak ℓ~40000 vs observed ℓ≈220.** The empty-beam reading is CMB-excluded by 2 orders of magnitude.
- **★ The forced conclusion:** the observer CANNOT be measuring the bare-substrate optics, or CR is dead. The programme REQUIRES the observable optics to be flat-ΛCDM's FILLED-beam (the shadow's optics). P9's scale-factor+isotropy fixes H(z) and the redshift (1+z=r_o/r_e, the comoving-3-sphere observer, NOT the fixed-χ Killing law which gives an impossible 1.9× error at z=0.1) but says NOTHING about focusing. The empty→filled step is the PROJECTION, and it is UNDERIVED. Quantified: make-or-break, ~180× at the CMB.
- **★★ THE LEVER (turn 65):** the construction is a METRIC reassignment on a FIXED manifold — same topology+foliation, different choice of which congruences are null vs timelike. Comoving worldlines = one bundle of dS null generators reassigned as timelike (fixed-χ); **the observable photons are the COMPLEMENTARY congruence reassigned as the SdS null congruence.** "Which curves are the photons" is precisely what's reassigned — so the projection acts on the causal classification, which is exactly the lever that changes the optics from empty (bare geodesics) to filled (reassigned congruence). → This IS the reassigned constant-R congruence (P9 §436) the status doc later confirms gives flat-ΛCDM (crux 1, the published Janzen 2015 redshift–distance). c22 located the lever; didn't finish the explicit filled-beam derivation.

### NET (Part 1 complete): what the transcript adds beyond the status doc
1. The empty-beam/filled-beam framing + the **180× CMB number** — the sharpest statement of why the naive optics fail and why the projection is load-bearing (status doc only had the low-z blueshift).
2. The photons = COMPLEMENTARY reassigned congruence (the explicit lever for filled-beam).
3. matter=bend, "ρ is the name not the cause" → dust doesn't focus on the substrate; focusing is in the shadow.
4. The two-chairs (catastrophe is the sitting) as the exact diagnosis of the D_A error.
5. The r_s DIRECTION problem made explicit (radiation-free → matter-like high-z H∝(1+z)^{3/2} → bigger r_s → wrong way) — so the resolution can't be "bigger r_s," and must turn on the early-universe construction (the seam/limits, the thermal history, whether a pre-recombination acoustic era exists).

### ★★★ THE LANDING (turns 71-87) — the ontological r_s resolution (the payoff)
- **Observer pinned (turn 71):** the isotropic/CMB-frame observer is ⊥ the {τ̃=const} 3-spheres (NOT ∂_τ, which is tilted to the 3-spheres → sees a DIPOLE). The Hubble diagram we compare to flat-ΛCDM is the isotropic observer's; for it 1+z=r_o/r_e (scale-factor ratio, flat-ΛCDM). [The ∂_τ dipole connects to the CMB dipole / P7 modern parallax — the cosmic rest frame.]
- **The medium question, sharpened (turn 77):** the open piece is *in which frame "radiation plays no role" bites (ontology vs shadow), and whether CR's radiation-free early universe carries a standard photon–baryon acoustic plasma at all, or the acoustic structure itself is REORGANIZED by the projection.* A source question (P9 projection map + slicing papers).
- **The "slightly vs enormously" signal (turn 81):** WBHH predicts the CMB-inferred H_0 is "slightly" wrong, but the grounded comoving-integral computation delivers a ~1.7× sound horizon ("enormously"). That gap is itself the signal — the next real step is CR's early-universe THERMAL HISTORY, not another distance integral.
- **★★★ THE RESOLUTION (turn 87) — r_s is laid down on the FINITE throat 3-sphere, not the shadow's manufactured singularity:** "r_s→251 Mpc was accumulating sound horizon across a FICTITIOUS zero-size early universe that the SHADOW manufactures and the real 3-sphere NEVER passes through." The real ontology (dS₄ background R(T)=α cosh(T/α), spatial S³, **throat minimum X=α, never below — no zero-size singularity**) bottoms out at the throat. So the sound horizon is laid down on the finite throat sphere and the spheres just above it as the generators climb out of the waist — a **bounded, de-Sitter-scale geometry** — i.e. *the sound horizon as an angle on the real 3-sphere near the throat, where the size is finite and the generators are bunched* — NOT a comoving integral of the shadow's manufactured a→0/z→∞ singularity. THE naive +74% balloon was exactly that illegal integral across a singularity the real geometry never reaches. c22 PARKED here (turn 87-88), about to build it on the throat geometry from P9, wanting the throat-sphere + generator-bunching set straight first.

## ===> THE IDEA FOR PART 2 (the complete shape, ready to build)
The Hubble computation, correctly posed, has three settled legs and one object to build:
1. **D_A = flat-ΛCDM, robust** (shadow / reassigned constant-R congruence / isotropic ⊥-τ̃ observer; filled-beam; crux 1, Janzen 2015). The empty-beam 180× and all low-z deviations were wrong-congruence/wrong-frame artifacts. SETTLED.
2. **θ_* = r_s / D_A(z_rec)**, D_A pinned by (1). So the whole question is r_s.
3. **r_s is NOT ∫c_s/H dz to z→∞.** That integral runs across the shadow's manufactured zero-size singularity, which the real closed 3-sphere never passes through (throat minimum X=α). The naive balloon (251 Mpc / 1.7× / +74%) is that illegal integral. QUARANTINED.
4. **BUILD: the sound horizon on the real throat 3-sphere** — the bounded, de-Sitter-scale geometry near the waist (R=α minimum), generators bunched climbing out — then PROJECT it to the observed angle. Inputs to settle from source: (a) does the standard photon–baryon plasma instantiate / is recombination even the same event, or is the acoustic structure reorganized (the thermal-history / medium question, turn 77); (b) the throat-sphere geometry + generator bunching (P9 throat construction, dS₄ background R(T)=α cosh(T/α), NBC P9 §665); (c) the projection of a throat-era physical scale into the observer's angle (the shadow map). If r_s comes out bounded/≈standard (not ballooned), the +74% exclusion is dead and CR's single Λ-fixed H_0 (=local value) stands with a consistent acoustic scale — the tension dissolved; if it lands elsewhere, that's the real falsifiable prediction. Direction is settled honestly only by this build.

## ✅ CORRECTED AT SOURCE (c23, r-reground after Daryl flagged a drift) — c22 turn-87 was RIGHT; my prior 'correction' was an inversion
I had recorded c22's turn-87 (lay r_s on the finite cosh throat, then project) as a 'substrate/cosmology conflation.' That was the drift — a flattened 'cosh-for-sinh' reading of P9 l.657. The authoritative status doc (CMB_ACOUSTIC_FRONTIER_STATUS.md, ONTOLOGY LOCKED r374/r375) settles it at source:
- **r_s is modelled in the dS₄ BACKGROUND** — the noumenal closed 3-sphere R(T)=α cosh(T/α), spatial S³, **throat minimum X=α, never below** (BST §5.4.6 l.1226) — **then projected**; NOT in flat SdS coordinates directly, and NOT a comoving integral to r→0. Its beginning is the **finite-curvature cosmological seam** (the past null boundary B / cosmological-horizon null ruling; P5 §211, P7/P9 §665), not the infinite-curvature r=0 (that is the exterior black-hole-sweep face of the same seam).
- **P9 l.657 is a DIFFERENT statement** — it identifies the comoving OBSERVERS' worldlines as the reassigned rulings (areal radius read along them = sinh^{2/3}), *not* the closed-slicing geodesics. Two LEVELS held apart: the background geometry (cosh closed 3-sphere) vs which congruence threads it as the observers (sinh^{2/3} reading). The status doc warns this exact slip ('calling the background a substrate is the slip that breeds the confusion').
- ⇒ **c22 turn-87's frame is correct and matches the locked status.** r_s lives in the noumenal cosh background, beginning at the branch point at $r=0$ (well-posed because $r_*$ CONVERGES, **not** because curvature is finite -- it diverges there; r2234), then projected to the observed angle. My 'conflation' charge is withdrawn.

## THE HONEST STATUS (from CMB_ACOUSTIC_FRONTIER_STATUS.md at source — authoritative)
- **Crux 1 VERIFIED:** observable redshift–distance is flat-ΛCDM exactly (photons = reassigned constant-R congruence, P9 §436; 1+z=r(τ̃₀)/r(τ̃ₑ), Janzen 2015). D_M robust (low-z dominated). The empty-beam/180×/blueshift/SNe scares are wrong-congruence artifacts, dead. CR NOT in tension at low z.
- **Crux 2 UNCOMPUTED (not a tension, not an exclusion, not a vindication):** θ*=r_s/D_M; D_M settled, r_s uncomputed. The +74%/θ*≈1.8 is the WRONG calculation (standard plasma + integral-to-z→∞ on a radiation-free rate — a chimera), NOT a CR result.
- **The modelling FRAME is locked:** r_s modelled in the noumenal dS₄ cosh background (throat X=α, beginning the branch point at $r=0$ (well-posed because $r_*$ CONVERGES, **not** because curvature is finite -- it diverges there; r2234)), then projected (= c22 turn-87's frame).
- **Two open problems block the number (these are the STARTING POINT for work, not a wall):**
  1. **The early-universe medium and c_s(z):** leaf dynamics is ordinary GR so acoustics-if-present are ordinary fluid (c_s≈c/√3); the open piece is whether the standard photon–baryon plasma *instantiates* as a projected density (Ω_m a clock reading, P7 §541).
  2. **The beginning / integration limits:** where the finite seam sits relative to recombination, whether a pre-recombination sound-travel era exists on this side at all, or 'before' the seam is the collapse side (branch-point crossing, P7/P9 §665) — including the live candidate that the branch point IS recombination (then r_s is inherited from the collapse side, no this-side integral).
- **Framing is the corpus's to settle** from the NBC (§665), the finite-throat dS–SdS arena, and the P5/P7 singularity-species taxonomy, constrained by analysis. Open theory to EXPLORE (the lens), not a calculation to dash at.
