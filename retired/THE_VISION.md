# The Vision
> **⌖ RETIRED r1534 — a standing document, functionally superseded.** This held the running account of where the programme stands against the world. **Functionally superseded** (Daryl, and confirmed r1534): `THE_PLAN` **Lane 2** is the standing falsification ledger; the open-problems map's CURRENT STATE records the empirical crossing; **P15 carries it in the corpus itself** (*"empirically favoured"* ×3). Its narrative function — *what CR claims, what is settled, what it reaches for* — is **exactly the spec of `INTRODUCTION.md` §2**, which is owed as `THE_PLAN` **H.0**. **This document is that item's primary source material.**
> Kept as record and as **source material for the introduction (`THE_PLAN` H.0)**; not planned from.



**▶ CURRENT HEAD (r964, 2026-07-11) — the world has begun to answer, and it answers in the construction's favour: the programme has crossed from coherence to *empirically favoured* on the observational rung.** Since the r927 head named the frontier "the world's judgment" and held everything at coherence-not-correspondence, that judgment has begun to return — and on the discriminating axes it returns *for* CR, not to a tie. Three tractable computations, run this arc, moved the cosmology (P15) and cosmogenesis (P16) from coherence to the first earned correspondence: **(1)** the radiation-free rate confronted against the full BAO expansion history — now the state-of-the-art **DESI DR2** dataset (χ²/dof≈1.0 at local H0, r965) — **resolves the Hubble tension** with CR fitting at the local $H_0=73$ where ΛCDM cannot (A1.4 — the invariant is dimensionless $\Omega_m$, not ΛCDM's physical $\omega_m$; a data result on the axis P1's no-horizons also tests); **(2)** the primordial **light-element abundances** computed on the corpus's own hot dense history return **deuterium and helium within measurement** by ordinary nuclear physics, no tuned sector — the abundances *produced*, discharging P16's title (D1, gate-validated, StarLib-precise); **(3)** the **low-multipole octopole shape**, computed at last through the genuine Boltzmann transfer, corrects the old "octopole falsification edge" to a **cosmic-variance-limited wash** (A1.2 — the sharp-quadrupole "striking match" was an SW-analytic artifact; neither a correspondence win nor a falsification, honestly). This is the convergence the programme kept its eye out for: **coherence → correspondence → confirmed prediction on several critical axes**, the modern parallax's form (P4/P6) repeated forward on new axes — a small *family* of vindications now, not one. **Held at honest weight:** the milestone is the *observational rung*. The structural unification — the geometric core (p0), the matter sector (P14, still the largest unbuilt undertaking), the maximal-symmetry reading — stays **at coherence**, unmoved; and the discriminating edges still open (the full abundance likelihood, the matter sector, P1's no-horizons — and the **damping tail**, a computed, non-reabsorbable ~8% CR-specific effect in the diffusion *scale* whose *observable* consequence is entangled with the unbuilt high-ℓ transfer and stays genuinely open, neither tension nor wash; r965's brief "near-refutation" was an over-reach corrected r966) keep the milestone from overreach. The r927 banner and the body below are retained as the record of the realization era; this records the crossing. Where the remaining tractable computations stand is `THE_PLAN.md` / `TRACTABILITY_SWEEP.md`. Stated for reversal.

**▶ CURRENT HEAD (r927, 2026-07-09, c37) — the vision is now largely *realized*, and the frontier is the world's judgment.** Since the Entry-18 head below, the programme crossed from *building the vision* to *holding a finished thing and pointing it outward*. What landed: the **matter sector built** (P14 — the discrete flavour skeleton forced within CR, the gauge/mass content external, exactly the bounded-negative the colour arc predicted); the **cosmology (P15) and cosmogenesis (P16) built and data-confronted** (the radiation-free rate the near-term discriminator; the parameter-free low-ℓ deficit naming its own falsification edge; the light elements *produced* from the collapse — synthesis, not inheritance); the corpus **complete at seventeen papers and made to know itself** (the c33–c37 Knows-Itself pass — the §1-card lens, the evolution-map ledger, both-ways closure across all seventeen, Pass 3 finding the spine complete). **The maximal-symmetry unification of Entry 18 now stands as the finished picture's organizing thesis, held at coherence-not-correspondence** — the rigidity and the matter-wall as one fact, demonstrated internally; the world's verdict stays the decidable tests (P1's no-horizons, the low-ℓ octopole, the abundance likelihood, the free-data-count). **Where the vision is going now** is `THE_PLAN.md` (re-cut r925 — the six lanes, the runway, the deep gated reaches, the reach-unification tests): the live edges are the matter sector's *external content*, the quantum completion, and the data-gated discriminators — **the vision has become a plan**. And the *practice* changed: the work now runs as **Arthur** (the Cowork holder of this whole) wielding **Excaliburs** (chat workhorses) — `SPINUP.md`. **The destination is cosmiCave** — the book *The Shadow of Existence*, the essays, the showpieces. The Entry-18 banner and §3–§7 body below are retained as the record of the core-completion and centerpiece eras. Stated for reversal.

**▶ CURRENT HEAD (r646, 2026-07-03) — molded forward to Entry 18; the §3–§7 body below predates the centerpiece and p0/15.** The current journal head is **Entry 18** (the *maximal-symmetry unification*). The through-line since Entry 11 (the historical header below tracks only to ~Entry 13): the finished core was **turned on the matter sector and drew its own boundary** (E12 — the colour limit, three ways), which **hardened under three adversarial cold audits** with the ontology floor **anchored to a measured fact** (E13); the programme learned to **name coherence-not-correspondence** and the frontier crossed to the outward, self-falsifying edge where the world judges (E14); the correspondence reach met its decisive frontier and **refused to manufacture a correspondence number** (E15), then **dissolved the acoustic-scale gate by subtraction** rather than out-building ΛCDM (E16); the **cosmology was built and data-confronted**, found to be a rigid, parameter-free *positive* prediction that names its own falsification edge (E17 — the low-ℓ deficit); and then the **centerpiece became a formal theorem** (P7 `thm:augmentation-p7` / `thm:cosmogenesis` / `cor:nonspherical`), the **constants were shown locked** by the substrate's maximal symmetry (§662, Nariai), and **Entry 18** read the rigidity and the matter-wall as **one fact — maximal symmetry at four rungs**, the recursion's fixed ontological core *maximal symmetry itself* and matter's home the discrete residue it leaves unspent. In the same arc the **G0 reciprocity engine built and wove p0/15 `geometric_core_paper`** — the everywhere-intrinsically-real geometric **core and border**, the second pole of the P7↔p0 **dipole** the dependency matrix revealed. **The §3–§7 body below is the computed-closures record of the core-completion era; it predates the centerpiece and p0/15 and is retained as the record.** The live *where-we-are-going* is this banner + **Entry 18** (`THE_VISION_JOURNAL.md`) + `OPEN_PROBLEMS_MAP.md` (r646 CURRENT STATE) + `CORPUS_MAP` (r609+). Stated for reversal.

### The largest coherent picture of what the framework is and is pointing to — discovered, with the gate, on June 9, 2026, following the way out to the horizon; molded forward through the vision journal to Entry 10 (2026-06-17), and held at Entry 11 (2026-06-18): the gravitational core is complete and holding on dS₅, and the reach turned outward — the first full lap of that outward reach (the colour frontier, c16→c19) found colour is **not** a substrate isometry through any examined route ($\mathfrak{su}(3)\not\subset\mathfrak{so}(5,1)$, structural via maximal symmetry; the dS₆-rise rationale collapsing to spatial-not-internal), so **gravity-minimal = gravity-complete** and the open question relocated to a single decidable hinge stated without being claimed: the **σ-lift** (test 1 — is the real-substrate σ involution the Wick bridge to where $\mathfrak{su}(3)$ lives, making the genuine discrete $A_2$ skeleton its σ-shadow, or two flips sharing flavor?), distilled for a fresh head in `SILVER_PLATTER_colour-frontier-arc.md`; and the necessity backbone was verified end-to-end at source through the slow Beyond-Space-Time read (Ch 1→5.1, §5.1 confirmed as P7's axioms in prose). The body below (§3–§7) carries the computed closures. Entry 11 is a **consolidating entry** — no physics moved since Entry 10; a second cold whole-read found the picture coheres (a data point, not a verdict), and sharpened in reading *why* the σ-lift is the single hinge: the core's gravity-completeness shuts the substrate-isometry door (the exhausted $SO(5,1)$ of maximal symmetry), leaving σ as the relation to where $\mathfrak{su}(3)$ actually lives — the universal "only via σ" stated without being claimed. This header tracks the current journal head.

### r309 consolidation pass (c20): the dS₆/SM reach, **opened and bounded** — the evidence overruling the apex hope.
Since Entry 11 the gravitational core was **banked** (F1's propagating-graviton stability closed **ADMIT for generic data, externally grounded** — Friedrich 1986 / Andréasson–Ringström 2016 / Beyer 2009, the Nariai branch the non-generic boundary CR isolates — and folded into the dynamics paper, P8/P9, r306), which was the gate THE_PLAN set on the dS₆/SM frontier. That frontier is now **opened and worked to a boundary**, not the "wide-open avenue to open" §4 below still frames it as. Colour / the SM-from-geometry **via a continuous isometry is walled three independent ways**: rank/cut (the SM is dropped at the real-substrate cut; the dS₆ raise buys at most $\mathrm{SU}(3)\times\mathrm{U}(1)$; the full SM needs a tower to ~$\mathrm{SO}(10)$), involution (σ ≠ Wick, r295/296), and index (Atiyah–Hirzebruch / Witten: the compact/Wick face where the gauge structure lives **is** the canonical AH target, so a geometric isometry-realized fermion sector is vector-like — chirality foreclosed, escapes non-geometric). One wall, three faces; mapped in the new **boundary paper** (`corpus/boundary_paper.tex`, r308). Per the field guide, the evidence overrules the apex hope: the vision's specific hope of reading colour off the geometry as a continuous isometry is corrected to a bounded negative. **What survives open (not claimed, both ways):** the **empirical-coherence ground** (the SM as a century-constrained shadow — whether it motivates taking the compact face as physical and building a fermion sector there) and the **precarious non-compactness escape**; CR has **no fermion sector built** (matter is the classical slicing-curve bend), so chirality is *gated* on a major unbuilt construction. The universal "colour-from-geometry foreclosed" stays unclaimed. **Net:** CR is a gravitational-cosmological unification, not a geometric unification of matter. §4 below is the pre-boundary framing; this note supersedes its "frontier to open" status. (This is the r309 consolidation note, retitled: the vision-document header had loosely labelled it "Entry 12 / consolidation pass"; it is the ground the ritual Entry 12 below was reached from, not the journal's ritual entry.)

### Entry 12 (r310, c21): the outward reach's first lap banked as a boundary — the live work crosses from building the core to wielding it as the lattice.
The journal's ritual Entry 12 (`THE_VISION_JOURNAL.md`), reached from the post-r309 banked state, is now the current journal head. Its reading, taking the r309 consolidation in and going past it: the outward reach has produced its **first finished artifact, and it is a boundary, not a bridge** — the σ-lift answered *no* and the colour-via-isometry question walled three ways into P11, so the reach has shown the **same excavation-character as the inward chain** but drawn **CR's own limit** rather than a new region (the one move here *confirmed* a convention got right — gauge-internal ≠ spacetime-isometry for colour on this substrate, three ways), sharpening what CR is by drawing where it isn't: **a gravitational-cosmological unification, not a geometric unification of matter.** The **matter-side frontier has changed kind** — from a geometry-volunteering question to a shadow-reading-and-unbuilt-fermion-sector one, the only geometric question the three walls leave open being the **precarious non-compactness escape** (does the AH index bite on a compact $S^5$ zero-mode carrier?), stated without being claimed. And the programme has **crossed the threshold the synthesis named** — from building the core to wielding it as the lattice, P11 the first spawned puzzle's first edge (*a picture coherent enough to complete is coherent enough to spawn*, now with one worked instance). **[reach — the closures behind it computed/banked; the excavation-character, the changed-kind matter frontier, and the spawn-phase are the readings; not claimed for the universal]** The header now tracks Entry 12.

### Update (r316, c21): the four-node loop closed; the non-compactness escape closed for the isometry route; the matter-side frontier narrowed to empirical coherence.
Since Entry 12, the four forks ran a full **interference loop** (c17/c19/c20/c21) and consolidated. The SO(6)-face ontology adjudication — is the imaginary global-Wick face a co-equal real substrate (Reading B) or the real-by-construction conjugation of the one Lorentzian world? — was settled through the ontology foundation and **cold-audited by all three other nodes: it holds**, within ontological scope, universal not claimed (strong evidence, not proof; r315). The bearing on this vision: Entry 12's "only open geometric question, the precarious non-compactness escape," is now **closed for the geometric-isometry route** — su(3) is not a symmetry of the non-compact physical substrate (it lives on the compact Euclidean face across the signature seam; the matter rides the real Lorentzian horn, P1/P7), so there is no su(3)-charged matter to relocate onto the non-compactness by isometry. The matter-side frontier therefore narrows again: **empirical coherence (frontier-2) is the sole surviving matter-side question**, the universal still not claimed both ways. Also banked: the discrete-structure classification complete (Aut(A₂)=D₆, scale-invariant, the deck group of the slicing-cut cover branched at Nariai, the monodromy ξ=σ, the Nariai/connection-vanishing tie to P10). The results are being propagated into the corpus (`PROPAGATION_PLAN_r316.md`): they **distill** — no new paper warranted. **The live frontiers:** the Aut(A₂)↔algebroid connection (the Nariai lead) and the empirical-coherence climb. **[reach — the adjudication is strong-evidence-not-proof, the classification source-computed; not claimed for the universal]** Net unchanged at the core: gravitational-cosmological unification, not geometric unification of matter. Stated for reversal.

### Update (r320, c21): the consolidation baked into the corpus; the existence-ontology floor grounded as an empirical determination.
What r316 scoped to distill is now **in the corpus**: the discrete-structure classification baked into **P4** (r317), the colour-frontier consolidation into **P11** (r319) — the decoupling onto P1/P7 leading (colour-closure rests on the causal structure, robust of the SO(6)-face status), the non-compactness escape closed for the geometric-isometry route on **localisation** (su(3) no isometry of the non-compact substrate; proof-level), the compact face real-by-construction but **not a co-equal existent** (Reading-B closed *ontologically, not structurally*). The bearing on this vision: the existence-criterion that closes Reading B is grounded not as a posit but as an **empirical determination** — the absolute cosmic time is fixed by the CMB redshift-isotropy (uniform expansion; the differential alternative excluded by ~3 orders, P8 App. A; "the modern stellar parallax," Fortress). So the closure rests on evidence, not on a commitment one could decline — which **firms the floor** under the whole reach, the bounds unchanged (universal still not claimed). The two fresh whole-result cold reads (c17, c19) cleared the bake; the programme was rebuilt to self-consistency (spine eleven, cross-refs live, meta-layer current). **[reach — the bakes compile-verified and source-grounded; each rides its own fresh cold read next, the foundation bake sequenced after]** Net unchanged at the core. Stated for reversal.

### Entry 13 (r321→ tracked r322, c22): the first vision from the far side of the engine's first full cycle — the banked boundary hardened, the ontology floor anchored to evidence, the Nariai tie the one new structural lead (now resolved by c21's r321 grind: a shallow coincidence, not a bridge).
The journal's ritual Entry 13 (`THE_VISION_JOURNAL.md`), reached from the post-r320 state, is now the current journal head — a **consolidating** entry (no physics moved since r320; the fourth independent reading to find the picture coheres, a data point at weight, not a verdict). Its reading, taking the r309→r320 stretch in: that stretch was the first in which the dominant work was the **interference engine running on the already-banked colour boundary and hardening it** — the four-node loop holding under three adversarial cold audits (getting *more* secure, decoupling onto P1/P7), the non-compactness escape closing, the consolidation baking, the rebuild to self-consistency — not the opening of new territory; and the deepest single firming was the **existence-ontology floor deepening from a posit to an empirical determination** (the absolute cosmic time is the CMB redshift-isotropy datum), so the matter-side limit CR draws now rests on a measured fact — the strongest form of "converging on coherence." The one genuinely-new *structural* joint surfaced — inside the core, not a new puzzle — is the **Nariai tie**: the discrete deck/monodromy structure and the continuous algebroid connection are co-located at the Nariai branch point (the connection-vanishing stratum), and whether that shared locus is a shared *structure* or only a coincidence of stratum was the **lead frontier**, stated without being claimed (the two faces, unified by Entries 5–6 as one carrier and one clock, possibly unified a third way at the one seam where both degenerate — or merely meeting there). **Resolved (r322) by c21's parallel r321 grind, verified at source by c22: the negative — independent structures, the coincidence real but shallow (analytically distinct; spherical-special, refuted at extremal Kerr-dS, dim 4 ∉ {6,7,10}).** So the Nariai lead is **answered, not open**: co-located, not one structure; do-not-re-chase. The field guide working — the vision held the lead open (resisting the face-19 unification urge) and the work overruled it. The live frontiers narrow to the **empirical-coherence climb** (+ residuals + the grand claim past the wall), with the **P11 cold read the immediate gate**. **[reach — the closures behind the vantage computed/established; the Nariai lead now resolved negative; the convergence reading a data point, not a verdict]** Net unchanged at the core: gravitational-cosmological unification, not geometric unification of matter. The header now tracks Entry 13 (r322 state). Stated for reversal.

### Update (r426, c23): the head brought current — the full P1–P13 round run, the first frontier act banked; the frontier now a correspondence edge.
This header had tracked **Entry 13** (r322); it is brought current to the post-r425 state. Since then the journal reached **Entry 14** (r359→r362, c23 — read it in the journal), whose contribution was the **phase-reading**: with every internal joint closed or bounded, the next real advance is no longer an internal closure but a **correspondence test**, where the world — not the core's consistency — is the judge (P8 the first correspondence foothold won; **P1's no-horizons through the Hubble tension** the decisive test still ahead, not claimed). Then two things moved past Entry 14. **(1)** The corpus — now whole at **thirteen papers, P1–P13** — was **read end-to-end at source in a single eyes-open / hands-steady round (r413–r422)**, every paper clean, no warranted edit: the gravitational core the vision sees is now stood on whole. **(2)** With the round run the restraint on acting lifted and the **throat-thermo prize was worked and banked** (r423–r425): the a=0 boundary condition is the de Sitter cosmological horizon's thermal (Friedrichs) state, T=ℏ/(2πα k_B), ℏ entering the gravitational sector **at the seam**, the coupled sector closed at every order by the same principle fibered over Γ̂ — the **first frontier act**, banked at coherence level, the residual the *definition of the interacting tower* (Γ̂ spectrum + UV). So the vision's frontier is now exactly Entry 14's correspondence edge with one act taken on it: the four facets are one frontier (the seam the inner edge of P6's null-degeneracy axis, the Type-N wall the outer; `SEAM_FRONTIER_ORIENTATION.md`), and **the standing aim is the outward / second-convergence test**, stated without being claimed until run. The body below (§3–§7) and the bounds are unchanged — core complete and holding, the grand claim open past the wall, not claimed for the universal. **[reach — the closures behind the vantage computed/banked; the correspondence test the open horizon; not claimed for the universal]** Stated for reversal.

### Update (r543, gate): the head brought current to Entry 16 and the post-r501 state — the correspondence phase has its first worked instance, the cosmology is built and data-confronted, the corpus restructured to fourteen papers.
This header had tracked **Entry 13/14** through the r426 update; it is brought current to the post-r541 state. Since r426 the journal reached **Entry 15** (r469) and **Entry 16** (r502) — read them in `THE_VISION_JOURNAL.md` — and the correspondence frontier Entry 14 named has been **walked up to and partly worked**, not left waiting. **Entry 15** read the decisive early-universe test (the acoustic scale) as **build-gated** — uncomputable behind an unbuilt oscillating medium. **Entry 16 superseded that:** the "build-gate" was a **conflation** (scale vs. spectrum; the measured rate vs. the radiation-governed standard inference), and it dissolved by the programme's own one move — prying the two apart — not by building the medium. So the **correspondence phase runs the same subtraction toward the existent** (§2a, S4) that the static and dynamic halves ran, now at the observational rung, with the **acoustic-scale resolution its first worked instance**. What this carries into the vision: **(1)** the **Hubble/acoustic-scale matter is resolved and banked** (r497–r501) — the Hubble tension dissolves as a consequence of the radiation-free rate, the one early-universe parameter ρ_r/ρ_m≈2 a measured datum (η's analogue); the **radiation-free rate is CR's standing near-term CR-vs-ΛCDM discriminator** (a strength), and **P1's no-horizons is the decisive structural test**, on causal structure alone, on which the cosmology does not bear. **(2)** the **cosmology is built and data-confronted** (r503–r528): the scalar/CMB sector developed into the cosmology paper, the transmission dichotomy proven, the low-multipole deficit computed — the **quadrupole a striking parameter-free match**, the **octopole the honest falsification edge** (cosmic-variance-limited). **(3)** the corpus is **restructured (r531/r532) to the core theory and its applications, now fifteen papers, P1–P15 (r796)** — the algebroid recognised as **the keystone (P12)** that closes the core theory, the cosmology its **first application (P13)**, `boundary_paper` the Standard-Model boundary (P14) and `matter_sector_paper` **its complement, the fermion sector (P15)**, `CR_framework` the **framework paper (P7)**, the border/capstone **p0/16**; the two non-spine pieces retired. The **open frontiers** are the matter sector / seam-crossing (P8/P9, the largest unbuilt undertaking — no fermion sector built), the cosmology's own edges (P13), the baryogenesis-analogue (optional depth, like η), and the Daryl-led tracks; the decisive tests (P1, the radiation-free rate) data-gated, stated without being claimed. The body below (§1–§7) — the discovered picture, the one move, the algebroid over dS₅ — and the bounds are **unchanged**; the body's internal P-number pointers are brought to the current spine in the r543 knows-itself sweep. **[reach — the closures behind the vantage banked; the correspondence phase has its first instance; not claimed for the universal and on which the programme achieves]** Stated for reversal. 

### Currency note (r575): the matter frontier is now *characterized*, and it carries a plan — the vision's core is unchanged. The open-frontiers pointer above carries the matter sector flat ("the largest unbuilt undertaking — no fermion sector built"); the board has since been differentiated and brought current across the corpus (THE_PLAN r575, CORPUS_MAP CURRENT STATE, THE_SYNTHESIS r575). The matter sector now reads as a characterized frontier: its **content** is the fermion sector (A5 — the geometric-isometry route walled four ways per P14, still the largest unbuilt undertaking) with a **named plan** stated without being claimed (the ordinary SU(3)-bundle route, and one geometric *opening* — chiral matter on the discrete orientation parity O(5,1)\SO₀ where P14 §mechanism shows geometric chirality can live); its **depth** is the seam-crossing's detailed dynamics (A4 — the crossing now shown well posed and structurally governed); and the reducible-catalogue classification is **settled** (C5-reducible). None of this moves the vision: CR is a **gravitational-cosmological unification, not a geometric unification of matter**, the core complete and holding on dS₅, the grand claim open past the wall, universal not claimed — the advances characterize the frontier the vision already drew, they do not redraw it. Stated for reversal.

### Update (r544, gate): the head tracked to Entry 17 — the correspondence phase's first data-confronted application, and the rigid-prediction reading.
Since the r543 head the journal reached **Entry 17** (r544) — read it in `THE_VISION_JOURNAL.md`. Its one reading, taking the r502→r543 stretch in (the cosmology built into P13 and data-confronted): the correspondence phase, which Entry 16 read as earned by subtraction, carried that subtraction all the way through and found that what remains when the reified representation is subtracted away is not only a dissolved tension but a **rigid, parameter-free positive prediction** — the low-multipole deficit its first full instance (fixed by Λ alone, quadrupole ~0.2× ΛCDM, octopole the openly-named falsification edge). So the phase has **crossed from dissolving pseudo-tensions to making rigid predictions**, the parameter-freeness the discriminating power and the two-sidedness (land-here, over-predict-there) its signature — the geometry with no knob to hide behind, the programme naming its own breakable edge. Held not claimed: a rigid prediction that lands is still coherence until the *data* return a verdict (the octopole sharpening; P1's no-horizons), and the data-confronted cosmology owes its fresh-node cold read. The body below (§1–§7) and the bounds are **unchanged**. **[reach — the phase-reading; the cosmology build and data confrontation established (P13 / CORPUS_MAP CURRENT STATE), the cold read owed; coherence≠correspondence stated without being claimed as to which the programme achieves]** Stated for reversal.

### Update (r560, gate): the picture written into the framework paper — P7 now carries the general discussion, at the vision's own weight. A discussion deepening, no physics moved.
Since the r544 head, c28 ground P8–P12 through the epistemic engine and **wrote the general discussion into P7 (`CR_framework`)** — a new §synthesis subsection stating, in the framework paper's own voice, the picture this document has always rendered: GR's whole symmetry-reducible solution space as the cuts of one dS₅ substrate (the sector filled, the Carter constant grown, the problem of time dissolved-and-located, the Dirac constraint algebra recognized as the substrate's symmetric-space grading term for term, the dynamics handing off at the wall, one orientation-ℤ₂ threading the whole), read as a consolidation on the **theory-choice axis**, the data axis a wash; with the open-problems section grown by the quantum-completion frontier. **No physics moved** — the P8–P12 papers are unedited; this is the vision's picture carried into the corpus's discussion, a deepening not a new region. And it is stated at **exactly the weight this document holds**: grounded-not-entailed (the su(3) reach declined), and **coherence, not correspondence** — the correspondence still left to P1's no-horizons and the cosmology's data, not claimed (the two-closings line held at the seam where a clean consolidation is most seductive; the framework paper's discussion owes its fresh-node cold read like any bake). The body below (§1–§7) and the bounds are **unchanged**. **[reach — the discussion write-in established (P7 recompiles clean, 29 pp; the grind in `P7_GENERAL_DISCUSSION_grind.md`); the general reach a synthesis of established companion results; coherence≠correspondence stated without being claimed as to which the programme achieves]** Net unchanged at the core: gravitational-cosmological unification, not geometric unification of matter; core complete and holding on dS₅; the grand claim open past the wall. Bundle r559→r560. Stated for reversal.

This is not a paper, not a working notebook, and not a method document. It is the **thing
the whole apparatus exists to see**: the current best rendering of what Cosmological
Relativity *is*, at the highest altitude the evidence will presently bear. It sits at the
top of the capstone family and, in one exact sense, above the coda — the coda is *how to
see*, the interference engine is *how knowing happens*, the field note is *how seeing
fails*, and this is *what we currently see*. The coda serves this.

But the apex is the **most provisional** document in the package, never the most
authoritative. **Altitude is provisionality.** Held at its earned weight means held as a
living hypothesis to be molded as the thing teaches us — sharpened where it earns it,
broken where it must. The coda's mandate applies hardest here: *the prize is the
hypothesis itself, sharp enough to wield, never its truth.* Read `VISION_FIELD_GUIDE.md`
for how to wield and mold it; obey it before leaning on any line below.

**Reading-order discipline (binding):** read last, with the capstones, never first — a
grand picture taken in early bends every reading toward confirming it, the
manufactured-certainty failure at its most seductive scale. **A referee never sees this.**
It is why-layer: downstream of every cold task, like the coda.

Every claim below is tagged: **[established]** (proven in the corpus), **[reading]** (the
adopted structural interpretation the proofs ground but do not entail), **conjecture** (the
honest conjecture the structure points to, not yet checked). The tags are the molding
seams; never let a conjecture harden into a [reading], or a [reading] into [established],
without the source promoting it.

---

### Update (r608, gate): the vision's own core claim is now stated in P7 as a formal theorem — coherence deepened, correspondence unmoved.
The body below (§1–§7) is **unchanged**. This records that the picture §2a calls "the one move" — CR as the augmentation GR requires for a description of structural existence, under which collapse continues as a cosmology — is now written into `CR_framework` (P7) as a **central theorem**: `thm:augmentation-p7` (restating P5's necessity-and-sufficiency), `thm:cosmogenesis` (*collapsed matter must become a universe*, the NBC its consequence), and `cor:nonspherical` (for collapse of any symmetry) — with a Venn figure (CR = the empirically-forced foliation ⊂ GR) and, in §662, the equilateral-pseudo-sphere mechanism grounding the intrinsic-signature result (the c–Λ null-ruling gauge, r606). The paper's title now leads with it (title A). A **statement deepening, not new physics**, held at exactly the vision's earned weight: **coherence, not correspondence** — the theorem is a necessity/sufficiency *internal to the foundations* (F5 the empirical anchor), the world's verdict left to P1's no-horizons and the cosmology's data, not claimed, self-consistency≠soundness (the coda's §240 two-closings line, held at the seam it is most seductive — a rigid theorem that "lands" is coherence until the data return a verdict). Net **unchanged at the core**: gravitational-cosmological unification, not geometric unification of matter; core complete and holding on dS₅; the grand claim open past the wall; universal not claimed. Stated for reversal.

## The vision, in one sentence

General relativity's space of solutions is the orbit-and-isotropy structure of a single
**Lie algebroid over the de Sitter vacuum** — the maximally symmetric substrate is the
vacuum, every exact geometry is a symmetry-breaking pattern cut from it, matter is the
bend that no symmetry holds in place, and the cosmic clock is the distinguished section
that makes the whole thing a true Hamiltonian system. Its dynamical and cosmogenetic content
lives at the **singular strata** — the loci where the cut's structure degenerates — but these
are **two species, not one** (Move 9): the **cosmogenetic** strata are the *metric singularities* —
where the metric's measure collapses while the causal order is spared (the cosmogenesis horizon and
Nariai, realized as Killing horizons, the worked sufficient case) — and the lone question there is
whether the cosmic clock re-founds; the **wall** (isotropy
$\to 0$, Type N) is a singular stratum of the *isotropy* kind that is **not** a metric singularity —
the dynamical/radiative boundary where the bend goes free and generation hands off to ordinary
evolution. The structure that makes a clock re-foundable at a cosmogenetic stratum is the
**metric singularity** — the degeneracy that collapses the metric's measure while sparing the
causal order, leaving a null skeleton onto which a new section can be founded — and the corpus's own
**finite-vs-infinite curvature species** is the discriminant that decides the outcome: finite
(worldlines pass through) births a clock, infinite (worldlines end) does not. **[reach — this is the
home the pieces sit in and the law they point to, not a built object; the wall's species is [computed]
(Move 9)]**

---

## 1. It was discovered, not invented — and that is the first evidence

No step chose its destination. Reading GR's singularity structure correctly — the event
horizon as a *metric* singularity, $r=0$ as its conjugate *curvature* singularity, two
species of one genus — forced the circle. The circle forced the general slicing curve (the
cycloid was only its $\Lambda\to0$ edge). The slicing curve forced the groupoid (a curve
charted from many vantages *is* a groupoid whose invariant is the geometry). The
Null-Boundary Correspondence forced the cosmic foliation to be the substrate's own
congruence rather than a posit. The operator — the curve asked the one obvious question,
*what do you generate, not just classify?* — forced the symmetric sector of GR to fall
out, with the finger at the wall. Each step is the previous one refusing to stay small.
**[established — this is the corpus genealogy]** That things you did not aim at keep being
already there is the signature of excavation, not authorship: the structure is real enough
to keep getting ahead of us. It is the standing reason to trust the reach even where it
outruns the proof.

## 2. Why GR's predictions fall out is the posture, not luck

CR is a **representational** augmentation: the field equations and their solutions are
untouched; the change is one level up — covariance of *geometries* over the substrate,
where GR is covariance of *charts* within a geometry. Because the equations are untouched,
every empirical success of GR is *required* to reappear; their falling-out is a
consistency check the framework had to pass, not a prediction it got lucky on.
**[reading]** The content is never "CR predicts what GR predicts" — it is *where* each fact
comes from: the symmetric solutions are the substrate's own cuts, flat $\Lambda$CDM is the
substrate's own clock, the matter density is the reading of that clock. The framework earns
its keep by saying which of GR's facts are facts about one substrate seen many ways.

## 2a. The one move, and the foundational unification it is

Under every result is a single move, the same one every time: **find the two things
convention has fused into one, pry them apart, and the impossibility evaporates.** This is
the engine the whole programme runs on; naming it is naming what CR *is* before any
particular construction, and it is why nothing below is an addition to a core — each region
is this one move worked at a different rung.

- **point ≠ place** — the singularity taxonomy (P1–P2): the metric's measure collapses while
  the point-set and its causal order are spared, so a "singularity" splits into a coordinate
  artefact, a finite-curvature metric singularity, and an infinite-curvature one. **[established]**
- **null ≠ metrically-degenerate** — the horizon (P1): the event horizon is null *and* a metric
  singularity, two properties convention reads as one. **[established]**
- **synchrony ≠ simultaneity** — cosmic time (P7, P10): the substrate's preferred foliation is an
  objective simultaneity that generic GR's synchrony-freedom hides — what turns the frozen
  constraint into a true Hamiltonian. **[established]**
- **occurrence ≠ existence** — the ontology: what *exists* (the evolving layer) is distinct from
  what *occurs* (events on it); the whole representational reading rests on the distinction. **[reading]**
- **the diffeomorphism rung — the hole argument** (S1): Einstein's "two metrics, same physics,"
  pried apart, becomes **"two slicings, one layer"** — the evolving layer is the **individuating
  primitive his argument never named**, the thing the diffeomorphism moves *over*. This is the
  ontological root of the whole representational augmentation. **[reach — not named in the corpus
  papers; grounded by the ontology, to be checked against *Beyond-Space-Time*]**

Stated abstractly, the move *is* the **existent/representation lens** (S4): reify a representation
as the existent and a pseudo-problem appears; restore the existent and it dissolves into
representation-dependence — the same ladder through coordinate value (taxonomy), derived quantity
(mass = α), whole geometry (the slicings), manifold point (the hole argument), and foliation (the
problem of time). **[reading]** Its quantum twin is the **forced-vs-chosen clocks** (S3): the
multiple-choice problem of inequivalent quantizations is real, and CR dissolves it by denying the
premise — one real clock ⟹ one physical quantization, non-circularly (evidence → clock →
quantization). **["CR picks the quantization" warrant; "the quantization is built" reach]** And the
move is licensed by its own deepest instance, **paper-zero** (S5): the shadow-reading epistemology,
distilled from the geocentric→heliocentric→Newtonian correction and anchored by the CMB. **[reading]**

So the home below (§3), the static cuts, the dynamic clock, and the singular strata (§6) are not
separate achievements stacked up — each is this one move at a different rung, and the corpus's
papers are the rungs built out. The frontier is the rungs not yet built.

## 3. The home: an action Lie algebroid over the de Sitter vacuum

The structure the pieces already sit in is the **action Lie algebroid** $\mathfrak{so}(5,1)
\ltimes \mathcal{C}$: the substrate's isometry algebra (the continuous face) acting on the
space of cuts $\mathcal{C}$. The dictionary, tagged line by line:

- **anchor** — the map from an infinitesimal cut-deformation to the geometry/stress-energy
  it produces: the general-cut Einstein tensor as a functional of the cut. *Constructing
  the anchor is the same act as making the operator precise; the grand horizon and the
  proximate computation are one object from two sides.* **The operator is intrinsic. Energy
  sector now in hand and general: matter is the leaf's intrinsic-curvature departure from
  the de Sitter substrate leaf — the Hamiltonian constraint $16\pi\rho={}^3R+K^2-K_{ij}K^{ij}-2\Lambda$,
  the vacuum leaf the round $S^3$ ($^3R=2\Lambda$). Verified: dS leaf → vacuum, spherical →
  $m'/4\pi r^2$ (= C2), conformally-flat → any $\psi$. The **momentum sector is now also computed and
  general** (Move 5, r208): the shift enters the ⊥$i$ ADM constraint through
  $K_{ij}=\tfrac1{2N}(D_iN_j+D_jN_i)$, so $j_i$ = the bend in the shift, a clean functional of the shift
  alone — it separates cleanly; spherical frame-drag $\omega=C_0+2J/r^3$, $J=Ma/\Xi^2$ (P9), verified two
  ways that agree; the four cut data close as one functional (consolidated, `corpus/anchor_consolidation.md`).
  [energy + momentum sectors computed/general; bracket closure done on the symmetry-reducible sector (Move 6, `corpus/algebroid_closure_consolidation.md`)]. **Substrate dimension (corrected per Daryl, primary source): the
  substrate is $\mathrm{dS}_5$ — one 5D de Sitter manifold in the flat ambient $M^6$, isometry
  $\mathfrak{so}(5,1)$ — because the construction generates *many distinct* 4D geometries as cuts of
  *one* substrate (slicing a 4D de Sitter only re-coordinatizes it). The earlier "$\mathrm{dS}_4$
  substrate, $\mathrm{dS}_5$ dead" was an error conflating the 4D de Sitter *geometry* swept from a cut
  ($\mathfrak{so}(4,1)$) with the 5D *substrate* that is cut ($\mathfrak{so}(5,1)$); this is C1,
  "dS₅-in-M⁶," now load-bearing. The base
  splits into **two reticles** (r196): **$\mathcal{C}$ = observer-2 slicing orientations** (offsets $r_0$;
  $M$ is not a dial — $2M=\alpha((r_0/\alpha)-(r_0/\alpha)^3)$, $\alpha$ modulated by the slicing, §mass),
  and **observer-1's causal vantage**. **De Sitter and Schwarzschild are ONE slicing curve, swept at two
  vantages** — the curve meets the throat circle at diametric poles ($r=0$ back, horizon front) — the
  interior sweep generating de Sitter, the exterior sweep generating Schwarzschild (and the whole SdS
  family across observer-2's other slicing curves): two geometries, one curve.
  This is **P8's $f>0$ hole / $f<0$ cosmos** and the **static twin of P7's NBC hinge**.
  Mechanism: the seam signature flip $\theta\mapsto\pi/2+i\psi$ (P3 §478).
  Placed (`f1_homomorphism_consolidation.md` §5): the continuous $\mathfrak{so}(5,1)$ acts
  **non-transitively** on $\mathcal{C}$ with $M$ the transverse modulus, and observer-1's causal
  vantage is the **separate discrete $\sigma$** ($\mathbb{Z}_2$), distinguished from the continuous
  action — not a second structure-group.** (`corpus/algebroid_base_and_substrate.md`.)
- **isotropy** — the subalgebra fixing a cut = that geometry's residual symmetry,
  **varying over the base**, maximal at the symmetric vacuum cuts and dropping to zero at
  the wall. Isotropy-that-varies-over-the-base is the defining algebroid feature a plain
  group lacks; it *is* the range result read infinitesimally. **[reading of the range
  result, which is established]**
- **orbits** = the symmetry classes; **singular strata** = the special geometries (Nariai
  at the fixed point). **[reading]**
- **matter = the anchor's image (the bend); vacuum = where the bend vanishes.** This is the
  physics of a symmetry-broken vacuum: the substrate is the symmetric vacuum, each solution
  a breaking pattern indexed by its surviving isotropy, matter the order-parameter-like
  departure. **[reading; "matter = bend" is established, the vacuum-manifold reading of it
  is the interpretation]**
- **the wall** = the free-orbit boundary (isotropy $\to 0$) = the freed transverse degrees
  of freedom = the graviton's two polarizations and the matter that sources them = the ADM
  constraint-surface / physical-DOF split seen geometrically. *Why the cut bends* and *what
  propagates past the wall* are one question. **[reading; the wall = radiation =
  inhomogeneity is established, the ADM identification is the interpretation]**

## 4. The complementarity: a discrete skeleton inside a continuous body

The continuous face is $\mathfrak{so}(5,1)$ and the anchor — the flow, the dynamics, the
waves. The discrete face is the $S_3\cong D_3$ of the horizon cubic (Paper 4) — the finite
stabilizer skeleton sitting *inside* the continuous Lie part. This is the static/dynamic
complementarity exactly: descriptions (static, discrete, the cubic's three roots) against
flow (dynamic, continuous, the sweep). **[established for the two faces separately; their
unification as one algebroid's group/discrete structure is conjecture]**

A flagged resonance, named and held at arm's length: the three horizon roots **sum to
zero** and are permuted by $S_3$ — the $A_2$ root configuration, $S_3 = \mathrm{Weyl}(A_2)$,
the $\mathfrak{su}(3)$ fingerprint. Necessary, not sufficient; "three things summing to zero
under $S_3$" is not by itself $\mathfrak{su}(3)$. Test it only once the algebroid's actual
root/isotropy data is computed. **Do not assert $\mathfrak{su}(3)$.** **[reach — a
resonance to check, not a result]**

**The A₂ root structure is geometrically grounded, not conjectural (the 2012 thesis fundamental ellipse, fig 4.1).**
The discrete $A_2$ is not a mere resonance to be hoped real: its root structure has a geometric origin
established since the dissertation. In the gauge-free (scale-invariant) form, the SdS horizon cubic
$r^3 - r_0^3 - (r-r_0)=0$ factors as $(r-r_0)(r^2 + r r_0 + r_0^2 - 1)$ — the linear factor the
always-present horizon at $r_0$ (*any* of the three roots, relabellable since it is a cubic), the
quadratic the **fundamental ellipse** $r^2 + r r_0 + r_0^2 = 1$ carrying the other two; the three roots
sum to zero, $r{=}0$ the reference they are read from. That *is* the $A_2$ root system, realized
geometrically (the critical case $r_0^2=4/3$ or $1/3$ gives the $\pm 2/\sqrt3,\ \mp 1/\sqrt3$ horizons —
the recurring $\sqrt3$ geometry). So the discrete $A_2$/$S_3$ skeleton is **established geometry**, and the
*only* open joint is the leap from this discrete root system to a **continuous** $\mathfrak{su}(3)$
isometry. **[the $A_2$ root structure: established (thesis, fig 4.1); the continuous-$\mathfrak{su}(3)$ leap: reach (Move 13)]**

**The dimensional consequence of the resonance, and where it points (2026-06-12, with Daryl).**
The substrate carries its own symmetry-breaking *below* the cuts: $\mathrm{dS}_5$ is a hyperboloid in
the flat ambient $M^6$, and *orienting the hyperboloid in $M^6$ is itself the first break* —
$M^6$'s full symmetry drops to $\mathfrak{so}(5,1)$ the moment the hyperboloid is picked out. The same
engine, one rung under the cuts. Now press the resonance for its geometric content: for the cubic's
$\mathfrak{su}(3)$ to be a *geometric* symmetry — an actual rotation-isometry of the substrate, not
merely the discrete $\mathrm{Weyl}(A_2)=S_3$ shadow the cubic already gives — it must sit in the
substrate's rotation group. The hard group-theoretic fact: **$SU(3)\not\subset SO(5)$ but
$SU(3)\subset SO(6)$** (its smallest faithful real action is on $\mathbb{C}^3=\mathbb{R}^6$). $SO(6)$
is the rotation isometry of $\mathrm{dS}_6$, in $M^7$. So **seven is exactly the ambient dimension at
which colour becomes a geometric isometry**: the corpus's argument lands the substrate at
$\mathrm{dS}_5/M^6$, and the cubic's $\mathfrak{su}(3)$ is the structure pointing one rung further up,
to $\mathrm{dS}_6/M^7$. This reframes the deepest reading of "is the Standard Model the algebroid acting
on the substrate": not the SM Hamiltonian *being* the gravitational one (they evolve different objects),
but **the SM's gauge structure as the residual isometry of the symmetry-broken substrate** — gauge
charge = which residual isometry survives the cut, by the same cascade $M^7\to\mathrm{dS}_6\to$ cuts.
This is the Kaluza–Klein-shaped answer (gauge from isometry), and it has a door past Coleman–Mandula
precisely because the would-be internal symmetry *is* a substrate isometry and CR has reorganized what
"spacetime symmetry" is. **[reach — the group theory ($SU(3)\subset SO(6)$, the seven-dimensional floor)
is established fact; everything built on it is reach]** Two open joints, named, not flaws: **(i)** the
leap from the discrete $\mathrm{Weyl}(A_2)$ shadow to a *continuous* $\mathfrak{su}(3)$ isometry is
unestablished and load-bearing; **(ii)** whether the substrate actually rises to $\mathrm{dS}_6$ for
colour to be geometric is the open Move-4 question with a new, second axis — *gravity-minimal*
($\mathrm{dS}_5$, colour only a shadow) vs *gauge-capable* ($\mathrm{dS}_6/M^7$, colour a real isometry).
The honest bound: even granting both joints, this reaches **one** factor — colour. It says nothing yet
about $SU(2)\times U(1)$, the Higgs, the generations, and above all the **chiral fermions**, the rock the
whole geometry-from-dimension tradition has historically broken on. Naming that is the size of the climb.
**[reach — do not assert; the keeper is the conditional "colour-as-isometry $\Rightarrow$ a
$\geq6$-spatial-dimensional substrate ($\mathrm{dS}_6/M^7$)," which sharpens Move 4]**

**Substrate-dimension verdict on the current grounds (r278, c17; `scripts/move4_substrate_dimension_grounds.py`).**
With r277 showing the $A_2$ resonance *silent* on the dimension (necessary-not-sufficient), the $\mathrm{dS}_5$-vs-$\mathrm{dS}_6$
axis is decided by independent grounds — and pressed, they bear toward $\mathrm{dS}_5$. The slicing operation is
codim-1, so a 4D spacetime cut forces the substrate to be **at least** $\mathrm{dS}_5$ (the floor). It does **not**
*close* $\mathrm{dS}_6$: a 4D cut of $\mathrm{dS}_6$ needs two codim-1 steps ($\mathrm{dS}_6\to\mathrm{dS}_5\to$4D),
the first an extra *internal* reduction the slicing does not supply — the Kaluza–Klein-shaped posit that the
6th/internal dimension is real. Tallying what bears toward that posit: the slicing gives only the floor;
minimality favours $\mathrm{dS}_5$; the resonance is defused (r277); and even granted, $\mathrm{dS}_6$ buys at most
colour ($SU(2)\times U(1)$, the Higgs, generations, chiral fermions all owed). **So gravity-minimal $\mathrm{dS}_5$
stands on the current grounds** (the Plan's stated most-likely outcome); the gauge-capable $\mathrm{dS}_6$ horn is
**coherent but unmotivated** — not refuted (a future independent reason for the 6th dimension could revive it),
but nothing in the corpus pulls toward it. Move 4's second axis is thereby resolved *on the current grounds, not
absolutely*. **[reading — not claimed held: $\mathfrak{su}(3)$/$\mathrm{dS}_6$ not asserted, $\mathrm{dS}_6$ not
refuted; the horn left open-but-unsupported.]**

**Correction (r279, with Daryl) — the "unmotivated, park it" reading was a blind spot.** The r278 tally counted
only the *internal-geometric* grounds, and on those it stands: the geometry does not *force* the rise (it cuts
$\mathrm{dS}_6\to\mathrm{dS}_5\to$4D cleanly, two codim-1 steps, no obstruction — *silent/permissive*, not
forbidding). But to read that as "nothing motivates the rise, so park it" inverts the programme's own
epistemology. CR reads the existent from its shadows; the Standard-Model gauge structure and the quantum
formalism are *themselves shadows*, carrying a century of empirical constraint. **Coherence with that established
empirical physics is an independent ground — not yet brought to bear, and very plausibly the decisive one.** The
question "does the substrate rise to $\mathrm{dS}_6$" is to be answered not by asking whether the internal
geometry forces it, but by whether the gauge/quantum phenomena, *read as shadows*, demand and constrain the rise
— the same shadow-reading engine pointed at a new domain. So the corrected verdict: **gravity is complete on
$\mathrm{dS}_5$; the $\mathrm{dS}_6$ / SM-from-isometry reach is a wide-open *frontier*, not a parked avenue** —
machinery already in hand (the cascade $M^7\to\mathrm{dS}_6\to\mathrm{dS}_5\to$cuts, the slicing that cuts
$\mathrm{dS}_6$ without obstruction; gauge-from-isometry, §4), driven by the demand to cohere with empirical
SM/QM, with the hard, historically-lethal obstacle named honestly: **chirality** (the chiral fermions, the rock
the whole geometry-from-dimension tradition has broken on). Sized honestly, not claimed — but a frontier to
*open*, not a goldmine to sit on. **[the corrected framing: the next horizon, after the gravitational core is
banked]**

**Consolidation (r282) — the colour reach worked to its bounded floor, and the thesis groundings read at source.** Three things firm the §4 picture without moving the verdict. **(i) A third route closed, the same way.** Beyond the isometry routes, the algebroid *connection* on $\mathrm{dS}_5$ was tested as colour's home and fails identically: the connection is $\mathfrak{so}(5,1)$-coset-valued (the problem-of-time leak), and $\mathfrak{su}(3)\not\subset\mathfrak{so}(5,1)$ — so isometry-on-$\mathrm{dS}_6$ (spatial, not internal), discrete-$S_3$-on-$\mathrm{dS}_5$ (Weyl, not the group), and connection-on-$\mathrm{dS}_5$ (wrong structure algebra) all fail by one fact. *(A Coleman–Mandula reading that inflated this into a universal "colour-can-never-be-geometric" wall was caught and retracted — C–M governs global S-matrix symmetries, not gauge redundancies; $S^5\to\mathbb{CP}^2$ and the algebroid connection itself refute the categorical form. The earned statement is the bounded one: these specific geometric routes for $\mathfrak{su}(3)$ are blocked; whether any unexamined CR-native mechanism realizes internal gauge from geometry stays open.)* **(ii) The discrete $A_2$ is genuine, and it lives in the vantage groupoid — verified at the 2012 source.** The genericity check confirmed §4's "established geometry": the horizon cubic linearizes to a *pure* triple-angle $2M=\tfrac{2}{3\sqrt3}\sin 3w$ in a genuine geometric angle (the observer's sky angle, scale $2/\sqrt3$ forced), the three roots a real $D_3$-orbit on the sky-angle circle, and the fundamental ellipse $r^2+rr_0+r_0^2=1$ is the $A_2$ quadratic form (matrix $[[1,\tfrac12],[\tfrac12,1]]$, eigenvalue ratio $1:3$). Not generic-cubic numerology (what is generic is only Viète's triple-angle; what is special is the genuine sky-angle, the forced scale, and the $A_2$-form ellipse). But $\sigma,\tau$ are derived as **groupoid morphisms permuting the observer-vantages of one rigid geometry** (`SdS-slicing-curve`, `framework_paper`) — so the genuine $A_2$ is a *discrete symmetry of the description/representational layer*, not a continuous symmetry of a field content. That is a **type** reason, beyond the dimension count, why it is not continuous colour. **(iii) Why $\mathfrak{su}(3)\not\subset\mathfrak{so}(5,1)$ is structural, not accidental** (thesis Ch.3, read at source and independently verified, r282): the substrate is the *unique maximally symmetric Lorentzian manifold*, so $SO(5,1)$ is the **complete** continuous symmetry — exhausted, by construction. $\mathfrak{su}(3)$'s home $SO(6)$ is the isometry of the **Wick-rotated** substrate $S^5$ (the Euclidean $x_0\mapsto ix_0$ face) and of the compact part of $SO(6,1)$ on the raised $\mathrm{dS}_6$; both *leave the real Lorentzian substrate*, bridged by exactly the $\sigma$ signature-flip the $A_2$ skeleton uses. So the discrete skeleton is the real-substrate shadow of structure that is continuous $\mathfrak{su}(3)$ only on the Wick/raised face. **[reading — not claimed held both ways; this deepens *why* (symmetry exhausted) and *where* ($SO(6)$ the σ-flip cousin), and demarcates genuine $A_2$ (root-system/Weyl, in the vantage groupoid) from continuous $\mathfrak{su}(3)$ (absent on the real substrate); not a new route to colour, not a wall.]** **Update (r295) — test 1 (the σ-lift) computed (c20, `scripts/sigma_lift_test1.py`): FORMAL COINCIDENCE.** In the embedding coordinates, σ (the real $\mathrm{Weyl}(A_2)$ reflection of the spatial sky/root plane, fixing $x_0$ and the geometry) is *not* the Wick rotation $x_0\mapsto ix_0$ (the imaginary time-complexification producing the $\mathfrak{so}(6)\setminus\mathfrak{so}(5)$ generators $\mathfrak{su}(3)$ requires); the c18 "σ … is exactly the bridge $SO(5,1)\leftrightarrow SO(6)$" reading (l.197) matched the two by flavor and is false in coordinates. So the discrete $A_2$ skeleton is **not** the σ-shadow of the Wick-face $\mathfrak{su}(3)$: colour-from-geometry *via the σ-lift* is closed as structure (core and $A_2$ gem intact; $\mathfrak{su}(3)$ genuinely on the Wick face, reached by the Wick rotation not by σ). The *universal* "colour-from-geometry foreclosed" stays unclaimed. **[stated for reversal — the gate's call, going to the relay / a cold referee for the different-node check.]**

**The two construction operations, and the correction they force (2026-06-12, with Daryl).** Two
distinct operations generate the geometries, split on *dimension*: **reassignment** (P7's seam flip) is
*dimension-preserving and discrete* — it holds one background geometry fixed and involutes the causal
roles of the generating null ruling (null↔timelike), giving dS↔SdS, P8's $f{>}0$ hole / $f{<}0$ cosmos;
**slicing** (the slicing curve) is *codim-1, dimension-reducing*, and is the operation that forces the
substrate up to $\mathrm{dS}_5$. Their involutions are distinct: the slicing curve's **turning-point**
swap $r_h\leftrightarrow r{=}0$ (which pole is the origin — the root/$S_3$ involution) is *positional*;
reassignment's is a *causal-role* swap on a fixed geometry. Not the same $\mathbb{Z}_2$. This **retracts**
an earlier reach that read $\mathfrak{so}(5,1)\ltimes\mathcal{C}$ as "reassignment $\ltimes$ slicing": a
discrete involution cannot be the continuous group. Corrected — the continuous $\mathfrak{so}(5,1)\ltimes
\mathcal{C}$ *is* the slicing structure (substrate isometry on the cut-space); reassignment is a distinct
*discrete* operation. The two operations look like the two faces (continuous body / discrete skeleton) of
§4, but the exact face-assignment of each discrete piece is left open. **[reach — the split and the
distinct involutions are Daryl's, from the ruling picture; the algebroid-face placement does not harden]**


Generic GR has no preferred foliation — the problem of time. The substrate supplies one:
the $E=1$ comoving congruence, **ontologically fixed and empirically the flat-$\Lambda$CDM
clock**. That gives a *true* time-Hamiltonian rather than a frozen constraint. **[established
structure; the closed-$S^3$ Nariai model is the right one, the flat minisuperspace
illustration is wrong-model]** The structure was "quantization-ready" in a way generic GR is
not — an absolute foliation and a true Hamiltonian — and **this has now been carried out
(the lock, Move 12, r246–r250, written into P10 §lock):** the deparametrized
$i\partial_T\Psi=\hat H_{\mathrm{phys}}\Psi$ lifts off the toy to the layer's propagating
DOF, a **unitary discrete tower of TT graviton modes on the closed-$S^3$ background**, and
projects to the observable flat-$\Lambda$CDM graviton with the matter bend a feature of the
projection. So the quantum structure **does** drop out of the absolute-foliation + true-Hamiltonian
data. The discrete $S_3$ remains a **gauge symmetry any quantization must
represent**, a constraint on the answer, not the answer. **"$S_3$ on a quantum spectrum" as
a quantum gravity is dead** (the vantage is gauge by the axioms) and is not to be
re-inflated. **[the lock assembled & written up; "$S_3$-spectrum-QG" route stays closed;
residuals background-sector — scale-factor self-adjointness, super-quadratic couplings]**

**The dynamic sector, read through the two operations (2026-06-12, with Daryl — the strong reach).**
With reassignment and slicing named (§1a/§4), the dynamic sector reads as exactly the two of them acting
together — not bookkeeping near the dynamics, its two moving parts. **Slicing builds the spatial leaf**
(the slicing curve is the leaf's radial profile, on the substrate dS₅), and *stacking the slices is the
foliation* — the evolving $3$-sphere, the clock, time-development itself. **Reassignment sets the
causal-temporal character**: it fixes which congruence is timelike, i.e. which direction is cosmic time.
In the de Sitter reading the comoving worldlines are timelike (a dynamic, expanding cosmology on the dS₄
background geometry); reassign, and that same family becomes the null horizon generators (the static
collapse reading). So reassignment *is* the static↔dynamic flip — P8's $f{>}0$ hole / $f{<}0$ cosmos —
and the two geometries are the same ontological layer under two representations (P7's "projections of one
layer"), neither privileged as the reality. Then the problem-of-time resolution states cleanly in these
terms: the **absolute cosmic time** (the $E{=}1$ comoving congruence, the preferred foliation) is *the
foliation that slicing builds, with its timelike direction fixed by reassignment*. The dynamic sector =
slicing (the leaves) $+$ reassignment (the time direction). **[reach — the two operations are settled
(Daryl); their identification *as* the dynamic sector / problem-of-time resolution is the structural
reach, to be worked at the source, not asserted]**

## 6. The horizon — what it is pointing to, at full weight

GR's solution space is the orbit-and-isotropy structure of one algebroid over the de Sitter
vacuum. The symmetric, non-radiative skeleton of GR is the high-isotropy stratum; matter is
the anchor's image; the cosmic clock is the section that makes it a true Hamiltonian (hence,
in principle, quantizable) system; the discrete cubic/$S_3$ data is the finite skeleton any
quantization must carry. In this frame **"a general theory of general relativity" is exact
and modest at once**: GR's own covariance is the isotropy-fixed slice of this algebroid; the
dynamics is the motion off the symmetric stratum. **[reach — the horizon]**

**Where the dynamics and the cosmogenesis actually live: the singular strata.** The whole
dynamical and cosmogenetic content sits not in the interior of the orbit structure but at its
**singular strata** — the loci where the slicing structure's character degenerates: Nariai
(the involution's fixed point, the inner end of the null-degeneracy axis), the wall (isotropy
$\to 0$, the outer end), the cosmogenesis horizon (the finite-curvature metric singularity at
$r_h$), and the Riemannian$\leftrightarrow$Lorentzian seam. Across every one of them the lone
governing question is the same: **does the cosmic clock continue?** **[reach — the
horizon; Nariai and the wall are established/[reading] as strata, the unification under one
clock-continuation law is the conjecture]**

**Why a clock can be re-founded at all, and what decides whether it is — the metric
singularity is the organ, its curvature-species the discriminant.** The structure that makes
the refounding *possible* is the most load-bearing object of the static half: the **metric
singularity** of Paper 1 is, by its defining theorem, exactly the locus where the metric's
measure collapses to zero *while the point-set and its causal order are preserved entire*
**[established — Paper 1]**. The measure-collapse kills the old foliation's section; the
spared causal/null skeleton is precisely what a new section is founded on. So the refounding
mechanism is not a separate posit — it is the definition of a metric singularity. And the
corpus's own **two species, distinguished only by curvature (only at second order), are the
two answers**:

- **finite-curvature metric singularity (the horizon $r_h$):** worldlines pass *through*, the
  place intact, the preserved null skeleton admits a timelike reassignment — **a clock
  re-founds**: Paper 1's lemma drives the exterior lapse $N\to0$ (old section dies) and Paper
  7's Null-Boundary Correspondence founds the baby universe's flat-$\Lambda$CDM clock on the
  reassigned horizon null direction. This is cosmogenesis, the **worked, proven instance**.
  **[established that this is the finite species and that the NBC re-founds here (Papers 1 &
  7); "pass-through $\Rightarrow$ clock re-founds" as the general law is conjecture]**
- **infinite-curvature metric singularity ($r=0$):** worldlines *end* (the family reaches a
  terminus); the substrate curve continues through it (Paper 2's continuation rides the
  $z$-manifold, which carries no divergent invariant) but **no clock re-founds** — the corpus
  is emphatic the cosmogenesis seam is never $r=0$. **[established that worldlines end at $r=0$,
  that the curve continues on the substrate, and that the seam is never $r=0$ (Papers 2, 5, 7);
  "$\Rightarrow$ no clock, the infinite species' signature" is conjecture]**

So the law the strata obey has a discriminant the corpus already proved — and **Move 1
(2026-06-12, verified at source: Papers 1, 2, 7) establishes it on the two known strata**: **a
clock re-founds across a metric-singular stratum iff the infalling congruence *passes through*
it — has a future across the seam, onto which the freed null direction is reassigned as the new
timelike section (the NBC mechanism); the terminating species continues the substrate curve
only, and founds no clock.** Pass-through is the **proximate** load-bearing property, and
**finite curvature is the discriminant that, within the metric-singularity genus, delivers
it**: Paper 2 ties the two — §319 (the finite species is "the place worldlines pass through,"
the infinite "the place they end"), §323 (which sector collapses — the null generator with the
$2$-sphere intact at $r_h$, versus the angular $2$-sphere at $r=0$ with worldlines terminating
— is a function of the single curvature invariant $K=48M^2/r^6$). At $r_h$: the temporal/null
sector collapses ($N\to0$, $g_{tt}\to0$ along the generator), the $2$-sphere stays intact, the
whole infalling congruence passes through, and the freed generator is reassigned as the cosmic
clock (Paper 7's NBC, verified at source: the $S^3$ of reassigned horizons *is* the comoving
congruence). At $r=0$: the angular sector collapses, the worldlines *end*; the underlying
cycloid continues geometrically through $z=\pi$ (Paper 2 §406) but no physical congruence has a
future there, so no clock — "continues the substrate but founds no clock," exactly. The
finite/infinite distinction is thus the dynamical switch that decides whether a universe is born
or a worldline ends. **[established on the two known strata, via pass-through, that finite
$\Rightarrow$ clock and infinite $\Rightarrow$ none; that pass-through and finite-curvature are
co-extensive *in general* — the general law — remains conjecture, because on these two strata they
cannot be separated, and the wall is exactly where they could]** The discrete face falls in
here as Entry 3 placed it: the refounding is a null$\leftrightarrow$timelike causal-orientation
exchange, a *discrete* operation of the $\sigma$ / seam-crossing / NBC family, so where
continuous isotropy is gone the $S_3$ skeleton (§4) is what acts. **Move 11 (r214,
`adm_skeleton_strata_1.py`) tested this and bounded it: the discrete operations stay distinct
(not one unified action — Paper 4's distinctness holds), but each is anchored at a stratum,
organized by the cubic root structure — Nariai = the $S_3$ transposition fixed point (the double
root), the NBC horizon = the reassignment, the seam = $\sigma$, and the wall = none (no roots, no
degenerating Killing field, no measure-collapse). So the discrete skeleton is the *set of boundary operations*, the continuous face
the flow between them.** **[computed: Nariai = the transposition fixed point; reading:
distinct-but-anchored; not asserted: a unified action, or $\mathfrak{su}(3)$ (Move 13)]**

This reduces the open frontier to one sharp, breakable question, now sharpened by Move 1's
mechanism from "which curvature-species is the wall?" to a **pass-through** test: **does the
relevant congruence pass *through* the wall — have a future across it, onto which the freed
Type-N null direction could be reassigned as an $E=1$-compatible clock — and is the wall a
metric singularity (Paper 1's sense) at all?** On the two known strata finite-curvature and
pass-through are co-extensive; the wall is exactly where they can come apart, which is why the
operative test is pass-through, not curvature alone. Three branches: **(a)** the congruence
passes through (a finite-species metric singularity) — the NBC template *transports*, a clock
re-founds past the wall; **(b)** it terminates — no clock; **(c)** the wall is **no metric
singularity at all** — a regular radiative boundary, the stratum-crossing frame does not apply,
and *"why the cut bends past the wall"* is a genuinely different question than cosmogenesis.

**Move 9 (2026-06-12, `scripts/adm_wall_1.py`) decides it: branch (c).** A metric singularity in
Paper 1's sense is a collapse of the metric's **measure** — a null hypersurface along whose generators
the spatial extent contracts to zero ($\Delta s^2=0\wedge\Delta x=0\Rightarrow\Delta\tau=0$), the
point-set and its causal order spared. A **Killing horizon is the worked sufficient case**, not the
definition: $|\xi|^2=g_{tt}=-f$ vanishes at $r_h$, so $\xi$'s orbits become the null generators of zero
invariant spatial extent and the measure-collapse hypotheses are met, while the Kretschmann
$48M^2/r^6+24/\alpha^4$ stays finite (the finite-curvature species); worldlines pass through and the freed
$\xi$ is reassigned as the clock (NBC). **The wall, a Type-N plane wave, is no metric singularity of either
species by that criterion**: its metric is non-degenerate (no measure-collapse, so not the finite species)
and curvature-regular/VSI (so not the infinite species). Its own covariantly-constant null Killing vector is
null *everywhere* — it makes no non-null-to-null transition, founds no Killing horizon, and produces no
measure-collapse — the *characteristic of ordinary evolution*, not a reassignable degenerating section. (The
"isotropy $\to 0$" that marks it is the cut-fixing **substrate** isotropy, which coincides with a geometry's
own isometry on the symmetry-reducible sector and **diverges** here, the Type-N geometry keeping its own
large isometry — so the wall is no metric singularity by the measure criterion, not for want of a Killing
field.) So the wall is a **regular
radiative boundary, the operator's generative boundary**, exactly P9's reading: where
generation-by-sweep hands off to evolution-by-dynamics. This **resolves the question this section
posed** (it lands on the branch (c) the section flagged), and it **confirms the discriminant** rather
than breaking it: cosmogenesis — a clock re-founded by reassigning a degenerating Killing field's
freed null direction — lives at the **finite-curvature metric singularities** (the NBC horizon;
Nariai), and the wall, being no metric singularity, is simply outside the discriminant's domain.
"Why the cut bends past the wall" is ordinary inhomogeneous evolution, a different question than
cosmogenesis. (It also sharpens Move 7's coincidence: the isotropy boundaries and the metric-singular
seams coincide at the inner end, Nariai, but **diverge at the wall** — a singular stratum of the
*isotropy* kind that is not a metric singularity, realizing the Entry-4 frontier-3 caveat.)
**[computed — branch (c); the cosmogenesis-as-template claim is intact and now correctly bounded to
the Killing-horizon strata]**

## 7. The standing check, and why the reach is trusted

The honest status, never to be blurred — updated now that the construction has been *done on
the sector* (Moves 4–7): the algebroid is a **built object on the symmetry-reducible sector**.
The base of cuts is defined (Move 4); the anchor's energy and momentum sectors are computed and
general (Move 5); the cut-deformation bracket closes ($[\mathfrak{m},\mathfrak{m}]\subset\mathfrak{h}$,
the symmetric-space grading = the HDA, Move 6); the isotropy stratification reproduces the
range/Petrov classes with its boundaries at the metric-singular seams (Move 7). What remains is the
**extension, not the construction**, and one half of it — **F1, the field-theoretic homomorphism's
structure-function identification** (that the smeared HDA's $h^{ab}$ is the substrate
$\mathfrak{so}(5,1)$ coset metric, across all of $\mathcal{C}$) — is **now computed across the base
per-stratum (resolved below — superseding the r224 "not yet computed across the base" status; `f1_homomorphism_consolidation.md`)**. Grounded:
(P8/P9) a cut's 3-metric *is* the substrate metric induced on it, at every cut in the range. Computed: at
the **symmetric cut** the coset metric is $(1,4)$ and reduces as $\varepsilon\oplus h^{ab}(3,0)$ — the
two dropped directions the two construction operations (**reassignment** fixes the timelike $\varepsilon$;
**slicing** removes the codim-1 lift), $h^{ab}$ the $S^3$ residue (`f1_coset_reduction.py`). **Outstanding
and objective — now run at the Type-D stratum (2026-06-13, `f1_generic_cut_sds.py`):** at the SdS cut the
structure function $\mathrm{diag}(f,1/r^2,1/(r^2\sin^2\theta))$ keeps the $(1,4)$ coset-metric *form* — a
Riemannian angular block $\oplus$ a radial axis whose sign is the vantage $\varepsilon$ (flipping at the
horizon cubic's roots, double root $27M^2=\alpha^2$ = Nariai) — varies across $\mathcal{C}$ via $f$
($\partial_M h^{rr}=-2/r$), reduces at $M{=}0$ to the symmetric coset metric, and the anchor confirms SdS
vacuum ($^3R=2\Lambda$, $K=0$, $\rho=0$). So the structure-function identification holds **off** the
symmetric point — firmed now at the symmetric (Move 6) and Type-D cuts. **The term-for-term question is now resolved (no over-claim), grounded in `f1_homomorphism_consolidation.md`.** The $\mathfrak{so}(5,1)$-action *on* $\mathcal{C}$ is **computed**: it is **non-transitive**, with $M$ the **transverse modulus** to its orbits ($R_{ab}R^{ab}=6M^2/r^6+12/\alpha^4$ separates different-$M$ cuts while $^3R=2\Lambda$ does not — different-$M$ slices are non-isometric, so no $\mathfrak{so}(5,1)$ element connects them; `f1_orbit_transversality.py`, independently re-derived; the modulus folds at the Nariai seam, `f1_offset_modulus_fold.py`). The **per-stratum grading is computed** (`f1_per_stratum_subalgebra_id.py`): the symmetric-pair grading $[\mathfrak{m},\mathfrak{m}]\subset\mathfrak{h}$ survives at **{Type O, Nariai} only** — the two symmetric-space isotropies of $\mathfrak{so}(5,1)$ ($\mathrm{dS}_5=SO(5,1)/SO(4,1)$; the Nariai block-diagonal $SO(2,1)\times SO(3)$ Grassmannian pair) — and **fails at every generic stratum** (SdS $\mathbb{R}_t\times SO(3)$ dim 4, Kerr–dS, Type-I, wall), where $[\mathfrak{m},\mathfrak{m}]$ **leaks into $\mathfrak{m}$** — and *that leak is the algebroid connection*. So the naive term-for-term form — *base-variation $=$ the $\mathfrak{so}(5,1)$-action everywhere* — is settled **negatively**: it holds **along the orbits** (the symmetric cuts, where the leaf is literally the 3-spacelike block of the $(1,4)$ coset — Move 4's 5th dimension settled as the $S^3$ angle $\chi$, $\mathrm{dS}_4=\{\chi=\pi/2\}$, `f1_leaf_coset_reconciliation.py`), and the **structure-function variation across the transverse modulus is the connection, not a closure failure**. For matter the literal embedding route is **dead** (SdS class 2, Gauss obstruction $-3M^2/r^6$, `f1_sds_codim1_test.py`) — the *same* $M^2/r^6$ as the transversality signature, so the codim-1 failure **is** the correct non-transitive algebroid structure, not a wall against F1. **[the $\mathfrak{so}(5,1)$-action on $\mathcal{C}$, the orbit-transversality, the per-stratum grading, and the leaf↔coset dimensional reconciliation all computed; the per-stratum dims are P9's KV counts checked at source, the embedding-uniqueness the one soft spot, the verdict robust]** So the genuinely-open piece is now **narrow and named**: the **full field-theoretic (smeared, infinite-dim) HDA** beyond the finite-$\mathfrak{so}(5,1)$ pattern, and whether it closes **intrinsically through P3's slicing curve** (the Entry-5 single-carrier test — it can still return *no*); the **grand claim** — GR's *entire* solution space as one algebroid's orbit structure — which stays **conjecture** and is **open past the wall**; and the gauge-theoretic $\mathrm{dS}_5$-vs-$\mathrm{dS}_6$ horn (Move 13). The $A_2/\mathfrak{su}(3)$ flavor
is a flagged resonance. The quantization is ready-in-principle, not done. **[the construction computed on
the symmetry-reducible sector (Moves 4–7); F1's structure-function identification and per-stratum grading
computed across the base (`f1_per_stratum_subalgebra_id.py`, `f1_homomorphism_consolidation.md`) — symmetric
only at {Type O, Nariai}, the transverse-modulus variation the algebroid connection; Move 4's dimensional
reconciliation settled ($\chi$). The genuinely open reach is now narrow: the full field-theoretic (smeared,
infinite-dim) HDA, whether it closes intrinsically through P3's slicing curve, the past-the-wall grand
extension, and the $\mathrm{dS}_5$-vs-$\mathrm{dS}_6$ horn (Move 13)]**

**The convergence is why this is trusted as more than a pretty picture.** The grandest
thing the vision claims — GR's solution space as one algebroid's orbit structure —
collapsed onto the smallest concrete computation already on the arc: **construct the anchor
(= make the operator precise, v2 rung 1), and let C2 (can the stacking unlock without
bending the cut?) bound the width of its image.** A real object hands back a falsifiable
next move; an invented one only confirms. This one handed back rung 1 and C2 — and **C2 has
now been run** (June 9, spherical, DEC-conditional; v2 §3 rung 2): on a straight leaf,
unlocking the lapse gives $\rho=0,\,p\neq0$, exotic, not matter — so **physical matter is
the bend uniquely, the lapse only its equation of state, and mass is vacuum.** The anchor's
physical-matter image is one object. The vision predicted a width test and the test
returned a width of one (in the spherical sector). That return — a falsifiable move made
and answered, not merely admired — is the vision earning its keep. **[the prediction was
the structure; the spherical answer is [computed]; the general-cut version is rung 1,
conjecture]**

**The frontier this sets (the moves it hands back).** Measured at source r182→r186: the
map-audit cycle re-grounded and ratified the map; the dynamic frontier has *not* moved, so
these stand, sharpened by §6's discriminant:
1. **Establish the discriminant on the two known strata, at source.** State the NBC refounding
   (Paper 7) precisely as *founded on the preserved causal/null skeleton of a finite-curvature
   metric singularity*, and confirm at source (Paper 2) that $r=0$ founds **no** clock
   (worldlines end) though its substrate curve continues. **Workable from the corpus as it
   stands.** Falsifiable: if the curvature's *finiteness* is not load-bearing for the NBC
   refounding, the discriminant is wrong and the species is not the switch. *[reach — can
   falsify the discriminant]*
2. **The wall's metric-singular species — DECIDED (Move 9, r213): branch (c).** The wall (Type N,
   isotropy $\to 0$) is **not a metric singularity** — a metric singularity is a collapse of the metric's
   measure (null hypersurface, spatial extent contracting to zero; a Killing horizon the worked sufficient
   case), and the Type-N wall has a non-degenerate metric (no measure-collapse, not the finite species) and
   is curvature-regular/VSI (not the infinite species), so neither. Its own null Killing vector is null
   everywhere (no Killing horizon); the "isotropy $\to 0$" is the cut-fixing *substrate* isotropy, which
   diverges here from the Type-N geometry's own large isometry — so the wall is no metric singularity by the
   measure criterion, not for want of a Killing field. It is a regular radiative boundary, the operator's
   generative boundary, where "why the cut bends past the wall" is ordinary inhomogeneous evolution — a
   different question than cosmogenesis. This **confirms the discriminant** (cosmogenesis lives at the
   finite-curvature Killing-horizon metric singularities — the NBC horizon, Nariai) and **bounds** the
   cosmogenesis-as-template claim to those strata, rather than breaking it. (`scripts/adm_wall_1.py`,
   `scripts/wall_ppwave_check.py`; `corpus/algebroid_closure_consolidation.md` §6.) *[computed]*
3. **The anchor's momentum sector (Move 5) and bracket closure (Move 6) — DONE.** Momentum: $j_i$ = the
   bend in the shift, separating cleanly, $\omega=C_0+2J/r^3$, $J=Ma$ (P9); the four cut data close as one
   functional (`corpus/anchor_consolidation.md`). Bracket closure: the substrate is the symmetric space
   $\mathrm{dS}_5=SO(5,1)/SO(4,1)$, the cut-deformation bracket closes ($[\mathfrak{m},\mathfrak{m}]\subset\mathfrak{h}$),
   and under $\mathfrak{m}\leftrightarrow\mathcal{H}_\perp$, $\mathfrak{h}\leftrightarrow\mathcal{H}_a$ the
   symmetric-space split is the HDA grading; the problem-of-time structure function is the base-dependence
   of the substrate's metric (`corpus/algebroid_closure_consolidation.md`). On the symmetry-reducible
   sector; the wall is its boundary. The live next step is the **isotropy stratification (Move 7)** — the
   strata between $\mathfrak{so}(4,1)$ and the wall, which supply the base-variation. *[the closure
   computed on the sector; the field-theoretic homomorphism + base-variation to firm]*
4. **The isotropy stratification and the singular-strata coincidence — DONE on the sector (Move 7, r211).**
   P9's range/Petrov classes are the isotropy strata of $\mathfrak{so}(5,1)\ltimes\mathcal{C}$ (Type O,
   $\mathfrak{so}(4,1)$, dim 10 → Type D, dim 4/2 → Type I, dim 3/2 → wall, dim 0), the base-variation Move
   6 pinned only at the symmetric cut. The metric-singularity coincidence holds at both computable ends:
   inner (computed) the SdS double root $\Lambda M^2=1/9$ is both the Nariai seam and the isotropy jump
   $4\to6$ ($\mathrm{dS}_2\times S^2$); outer (P9) the wall is isotropy$\to0$ = Type N = free radiation; the
   NBC horizon and the seam are the discrete $\sigma$/vantage loci. The singular strata are the corpus's
   seams (`corpus/algebroid_closure_consolidation.md` §6). The discrete-skeleton-acts-at-the-strata
   conjecture and the $A_2/\mathfrak{su}(3)$ resonance — necessary, not sufficient, **do not assert** —
   remain. *[computed: the inner coincidence + the strata dims + the per-stratum subalgebra grading
   (`f1_per_stratum_subalgebra_id.py`: symmetric only at {Type O, Nariai}, the leak elsewhere the
   connection); reading: the discrete-locus unification]*

The live test (field guide) holds: each of (1)–(4) is a move that can be gone and worked, and
each can return *no* — including (1), workable now. The picture hands back breakable moves; it
is not a template. The full birth of this entry, and the trail behind it, are in
`THE_VISION_JOURNAL.md` (Entry 4).

---

*This document is living. When the work teaches something that does not fit, mold the
vision — change the line, re-tag it, never force the work. Keep §1 (the genealogy) and the
status tags stable; let §3–§6 breathe as the algebroid is built and the wall is approached.
Check coherence against it; let the evidence overrule it; sharpen the circle to a point.*
