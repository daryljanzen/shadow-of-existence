# The bead walk — running record (r973→, Arthur)
> **⌖ RETIRED r1536 — verified landed.** This was the bead walk, a running record (r973→). **It landed:** `thm:bead` is P7's central theorem (×6) — one closed object on one smooth manifold, the substrate C^∞ across r=0.
> Kept as record; **do not work from it.**



*The corpus-wide working-through of the cosmogenetic bead (`conj:bead`, seated in P7 r973). Resumable trail, stated for reversal. Method: per paper, a read-only scout-Excalibur digs at source and reports where-implicit / small-click / RESISTANCE-or-shift / verdict; Arthur verifies load-bearing findings at source and integrates. Guard held live: the bead is elegant and wanted, so evidence overrules it — a paper that resists is data, not something to smooth. Order (Daryl): P3→P7 done, then the rest of P3–P7 leg, P1–2, P8–12, P15–17, P13–14 last, then back to P7 (abstract/intro), then graph.*

## ▶ DEEP-BAKING CAMPAIGN (r975→) — the real job, not the light pass
*After `thm:bead` was closed (r975), the task is to bake the ontology **deeply into every paper in its own voice**, each telling its own tale with the bead to hand, **every paper citing P7 `thm:bead`.** This is NOT the "restraint/already-there" light scan of the earlier walk — it is substantive weaving + the theorem naming. Tracker (per paper: BAKED = deep passage naming `thm:bead` in the paper's own voice done; TODO = pending):*
- **P1 `BH_causality`** — BAKED (r975): §sec:5 the collapse leg named as the bead's collapse turning point (the horizon it proves is where the curve runs tangent to the throat), forward-pointer to `thm:bead`. 15pp.
- **P15 `CR_cosmology`** — BAKED (r975): the scale factor's real branch named as the bead's expansion leg, `\cite{JanzenCRframework}` at `eq:scalefac`. 23pp.
**ALL 17 PAPERS BAKED (r975→r976), every one citing P7 `thm:bead` in its own voice, all compile clean:**
- **P2** — the framework's central theorem named as the closure of *this cycloid* (Schwarzschild-limit seed); P7 bibitem added. 20pp.
- **P3** — the closed curve here named the bead's geometric skeleton at §sec:lap. 32pp.
- **P4 modern_parallax** — the measured cosmic time named as the bead's real expansion-leg time (phase leaves the real axis only on the unobservable lap). 10pp.
- **P5 groupoid** — σ/R/ξ as the bead's stations, "establishes" (theorem). 22pp.
- **P6 shadow** — the bead theorem named as held at P6's coherence altitude (a theorem *of the construction*, correspondence the data's to judge). 11pp.
- **P8 slicing_operator** — the E=1 geodesic named the bead's expansion leg, vacuum cut the curve it bends off (§sec:synchronous). 11pp.
- **P9 range** — collapse-forward-as-expansion named the two readings of the bead, its seam the Nariai member this range places. 12pp.
- **P10 canonical_time** — the deparametrized cosmic clock named the bead's very parameter (real on the leg, bounded contour on the lap). 12pp.
- **P11 dynamics** — the strata named the ordered turning points of the one closed slicing curve (earlier). 11pp.
- **P12 algebroid** — the continuous-slicing face named the closed bead (slicing axis, doctrine preserved). 9pp.
- **P13 boundary** — the real Lorentzian horn the matter rides named the bead's expansion leg. 14pp.
- **P14 matter_sector** — the signed-r one-closed-object named the bead, on which the chiral wall is built. 8pp.
- **P15 CR_cosmology** — the scale factor's real branch named the bead's expansion leg. 23pp.
- **P16 cosmogenesis** — the fossil lap named the single closed bead. 12pp.
- **p0/17 geometric_core** — the lap named a concrete instance of this paper's reached-through-the-imaginary principle (cosmic time off the real axis, radius real). 20pp.
- **Every paper cites P7** (verified: 1–13 cites each). P7's dependency column is filled. **Remaining: dependency-matrix regen; then the graph.**

## Headline pattern (through leg 1 — the earlier light walk, superseded by the campaign above)
The "distinct crossings / different destinations" fragmentation was **corpus-wide**, not P7-local — it had propagated into P2 (`janzen_circle`) and P5 (`groupoid`) as well. So the real walk-work is **de-fragmentation toward the closed bead**, preserving the correct ledger distinctions (α ≠ α/√3; the loci genuinely distinct *by radius*) and restraint everywhere the bead is already right. **No genuine physics shift found yet** — the bead is already load-bearing across the foundational papers; the work is making it explicit and aligning stale framing.

## Two constraints on the conjecture's open core (captured — fold into `conj:bead` at the P7-return leg)
1. **(from P4 `modern_parallax`) τ̃-phase quarantine.** The complex excursion of τ̃ must stay confined to the **lap** (the seam between prior universe and ours — unobservable, outside the decoupling-to-present interval P4 measures). P4's measured cosmic time is the real **legs**; if any intra-universe observable stretch were given a complex τ̃ it would collide with P4's *measured absolute simultaneity* (`thm:augmentation`). A real physics constraint on the contour, not just wording.
2. **(from P6) derive, don't fit.** The lap↔physical-seeding map must be **derived**, or the bead forfeits R2 (requires≻permits) and becomes a save-the-appearances patch (R4 / `prin:reclass`). The conjecture is correctly stated without being claimed at P6's altitude (`thm:modal`, `sec:boundary`, `sec:place` — the geometric-core-at-coherence template). Optional backward-pointer P7 `conj:bead` → P6 (held at P6's altitude).
3. **The τ̃-contour, COMPUTED (r974) — and the β=2πα claim RETRACTED as noise.** *Correction to a prior over-reach: I had hyped P10's Hartle–Hawking period β=2πα as "the contour datum." It is NOT — computed, it is a factor of 6 off the actual imaginary scale, i.e. noise for this. Retracted.* The real result (`python`, flat-ΛCDM leg law r=sinh^{2/3}((3/2α)τ̃) continued to the real signed lap): as r runs real from +α/√3 through 0 to negative (the geometric slicing curve, r real per P3 §lap / fig (f)), **cosmic time τ̃ traces a bounded L-shaped contour** — real on the expansion leg; straight down the imaginary axis as r: 0→−α (Im τ̃: 0→−πα/3); then along the line Im τ̃ = −πα/3 on the prior-collapse leg. The imaginary part is **bounded and locks at −πα/3**, and this "60°" (π/3) is a property of the 2/3 exponent ((2/3)(π/2)=π/3) — the finite imaginary displacement of cosmic time, **not** a phase of r (r stays real) and **not** an obstruction (the previous node's "60° phase problem" was a misread). A-independent. So the bead **closes**, r real & signed, τ̃ complex on a bounded contour — exactly what `conj:bead` says; the open-core framing is right, and the contour is concrete, not mystical.
   - *What P10 legitimately gives (unretracted):* unitarity `i∂_τΨ=Ĥ_physΨ` holds iff τ real, so the complex excursion must stay off the observable legs — a second, independent reason (with P4) for the lap-quarantine. That stands; the β=2πα-as-contour claim does not.
   - *Remaining precise step (not yet done):* this is the pure-dS/flat-ΛCDM **leg law**; the full Nariai cut with the actual roots +α/√3, 0, −2α/√3 needs the E=1 master equation `dr/dl=√|f|`, f=1−2M/r−r²/α², integrated. Qualitative picture + the β-noise verdict are settled; the exact full-cut contour is the real work that would promote `conj:bead` toward theorem.

## Leg 1 — rest of P3–P7 leg + P1–2

- **P3 `SdS-slicing-curve_v2.tex`** — DONE at r973 (holds the lap at source, §sec:lap: "one closed object on one manifold… a previous universe's collapsed matter continued through the seam"). The settled source of truth. No change.
- **P1 `BH_causality_v2.tex`** — **restraint (no edit).** Owns the collapse leg + singularity taxonomy the bead leans on (`thm:metric-singularity`, `cor:threefold`, `prop:causal-alignment`, §sec:5). Masthead **forbids forward dependencies** ("P1 owes the arc nothing"); §sec:5 already forward-points to P7/P3/P16 with the collapse→seam→expansion reading. P7 `conj:bead` already points *back* to P1 for the open r=0 spacetime-extension question — the edge exists both ways. **Two cross-corpus flags (not P1 defects):** (a) P1's "finite-curvature seam" = the *physical-seeding / finite-curvature-horizon* level, categorically **not** the bead's geometric *equatorial seam at X=α* — do not let a reviser conflate the two "seam" words; (b) P1's "endpoint rather than a crossing" at r=0 is the *spacetime/worldline* level the bead correctly leaves open — do not let a reviser "fix" endpoint→crossing.
- **P2 `janzen_circle_v3.tex`** — **EDITED (de-fragmentation).** §sec:ring line 467 was "the bead in all but name" but carried the stale "distinct turning points kept apart… three crossings of the one curve, not one… onto the cosmological/conjugate branch." Recast to "distinct turning points of *one continuous lap* — a single closed bead… not three separately-destined crossings," preserving α≠α/√3 and the homogeneous-circle clause; cites `JanzenSlicing` (§lap, present). The clean **analytic seed** of the bead's contour (`sec:continuation`: z↦π+iρ', r real, phase in the parameter) and the **two-level r=0** reading (spacetime-extension explicitly left open, divergence never flattened) are already impeccable — untouched. Compiles 20pp. *(Deferred to the column-build leg: an explicit P2→P7 `\cite{JanzenCRframework}` matrix cite — P2 doesn't yet cite P7.)*
- **P4 `modern_parallax.tex`** — **restraint (no edit).** Not load-bearing on the geometry; grounds the *real expansion leg* (measured cosmic time, `thm:augmentation`). Gave constraint #1 above. Optional cross-ref only; omission is the disciplined choice.
- **P5 `groupoid_paper.tex`** — **EDITED (de-fragmentation + wire).** Supplies the bead's group-theoretic skeleton (σ Nariai-fixed/no-seam, R the r=0 back-seam, ξ the X=α throat seam; `D_6=⟨σ,R⟩`; `prop:nariai-fixed` gives two of the three lap turning-point radii +α/√3, −2α/√3). PASSES the distinct-vs-fused guard emphatically. `rem:P-dS-Schw` had the fragmenting "different turning points with different destinations" — recast to "distinct but **consecutive** crossings… on the single closed slicing curve — the cosmogenetic bead the framework conjectures (`\cite{JanzenCRframework}`)… ξ the entry to the throat lap, R the turn at its back… not separate crossings with unrelated destinations." Held the paper's **R** symbol (it reserves "P" for spatial parity; do not import the ledger's "P"). Compiles 22pp.
- **P6 `shadow_of_existence.tex`** — **restraint (no edit).** No geometry; **sanctions the conjecture's epistemic status** (`thm:modal` not claimed; R2; `sec:boundary` coherence≠correspondence; `sec:place` the geometric-core-at-coherence template). Gave constraint #2 above. Optional P7→P6 backward-pointer.

## Leg 2 — the core theory (P8–P12)

- **P8 `slicing_operator.tex`** — **small-clicks-only (deferred to column-build).** Owns the bead's **expansion leg** (`prop:cosmo`/`eq:scalefactor`, the sinh^{2/3} law) and the collapse↔expansion continuity (§sec:synchronous: "the one finite-curvature layer carries both causal readings… the collapse's event-of-events… is the big bang"), **already de-fragmented** (one congruence, already cites `JanzenCRframework`). Ledger clean. Does NOT constrain the τ̃-contour (its finite-proper-time result is physical-level, distinct from the substrate τ̃). One optional naming click at l.273 → column-build.
- **P9 `range_paper.tex`** — **restraint (no edit).** Strong *independent* corroboration: "seam = Nariai Killing horizon ≠ wall (Type-N)," derived from Petrov/isometry reasoning (§sec:homog "Nariai pins to the seam, not the wall"; §sec:rotation; §sec:wall). No stale framing, no r=0-as-separate-destination. Fully consistent with cosmogenesis bounded to the Killing-horizon strata.
- **P10 `canonical_time.tex`** — **major yield (verified at source); one P7-return item, no P10 body edit this turn.** Owns τ̃, the bead's parameter. §sec:lock independently pins the seam continuation to **β=2πα** (Hartle–Hawking) and forbids complex τ on the legs (unitarity) — see open-core constraint #3 above. No correction needed; P10 is bead-consistent and ledger-clean (a=0 the finite-curvature horizon, not r=0; κ at r=α; α/√3≠α). Optional P10→P7 forward-pointer (flagging the β=2πα↔contour lead, not claimed) deferred to the column-build.
- **P11 `dynamics_paper.tex`** — **EDITED (soft de-frag).** Wall≠cosmogenesis machinery is correct and bead-consistent — left alone (manufactured-relevance trap avoided). §sec:discrete l.275 "distinct operations, not one; they relate only through the strata they mark" under-claimed; recast to "…but the strata they mark are the ordered turning points of a single closed slicing curve — the operations distinct, the object one~`\cite{JanzenSlicing,JanzenCRframework}`," holding both axes. Compiles 11pp.
- **P12 `algebroid_paper.tex`** — **restraint on body; P7-side fix made.** Its §sec:discrete "different involutions, not one" is a *deliberate, correct* two-operations doctrine (continuous slicing vs discrete markers) on the **discrete axis** — NOT stale fragmentation; editing it would conflate the two axes it purposely separates. But P7's `rem:perspectival-singularity` had over-attributed "closing into one object" to `\cite{JanzenSlicing,JanzenAlgebroid}` — **the closure is P3's alone**, so `JanzenAlgebroid` dropped from that cite (P7-side accuracy fix, made + recompiled 38pp). Optional P12 l.240 forward-nod declined (would over-identify P12's comoving/synchronous rulings as the collapse/expansion legs, which P12 doesn't assert).

## Leg 3 — cosmology / cosmogenesis / geometric core (P15, P16, p0/17) — RESTRAINT (dug myself, no scouts)
All three already hold the bead correctly and are de-fragmented; **no edits warranted.** Verified my τ̃-contour computation against what they already say — it matched, added nothing new.
- **P15 `CR_cosmology.tex`** — already the bead, both levels: `eq:scalefac` "a single analytic function of τ̃… the cosmological reading of the slicing curve's closed lap, on which r=0 is a branch point and not a barrier~\cite{JanzenSlicing}" (geometric); `fig:scalefac3d` shows the conjugate branch at phase 2π/3 with the real-r projection a "manufactured cusp" (the τ̃-parametrization face — consistent with my contour: real-r goes with complex-τ̃, so real-r-vs-real-τ̃ is a spurious projection); the register close "r=0 lies off the continuation **and on no finite cosmic-time slice**" (physical level, correctly disambiguated — NOT the retired fragmentation; left alone, phantom-fault avoided). Three-levels L1/L2/L3 machinery intact. Already cites `JanzenCRframework`.
- **P16 `cosmogenesis_paper.tex`** — §sec:lap states the bead verbatim in spirit: the lap through the throat "onto the far side of r=0… signed coordinate… continues around the throat through r=0 and out onto the negative branch, closing on the backward-radial root… r=0 the branch point, not a barrier, the substrate one smooth C∞ de Sitter manifold across it," and "reached by an **imaginary** continuation of the slicing curve — yet real at every point it lands on (p0/17)" — exactly my computed contour. The fossil reading is its whole point. No fragmenting language.
- **p0/17 `geometric_core_paper.tex`** — grounds the "imaginary yet real" (the X0/imaginary instrument, §sec:imaginary) the contour rides on; the lap "reached through the imaginary yet real, read forward as a previous universe's collapsed matter." Consistent; no fragmenting language.
- *Optional (deferred to column-build):* naming `conj:bead` as P7's flagship at the lap-statements in P15/P16 — but the whole-paper `\cite{JanzenCRframework}` edges already exist; restraint preferred.

## Leg 4 — the matter boundary (P13 boundary, P14 matter_sector) — RESTRAINT (dug myself)
Held last per Daryl (matter wields P3 differently, via least-arbitrariness); both already bead-consistent, no fragmenting language, **no edits.**
- **P13 `boundary_paper.tex`** — the SM colour-wall. Its "three operations co-localised at the seam, NEVER FUSE THEM" (real Weyl σ / seam continuation θ→π/2+iψ on a *single* slicing curve → S⁴/SO(5) / global Wick → S⁵/SO(6)⊃su(3)) is the correct colour-boundary argument, bead-consistent (the seam continuation flips a *single* slicing curve). P/R/T discrete-residue canon intact. Restraint.
- **P14 `matter_sector_paper.tex`** — **wields the bead heavily and correctly.** "r a signed coordinate: the curve runs forward through the seam and backward through r=0 as one closed object, r=0 a branch point the field crosses smoothly, not a barrier~\cite{JanzenSlicing,JanzenCircle}" (§, line 161) is load-bearing: the r=0 **branch point** (not a loop crossing) is exactly what makes the even-crossing constraint fail → ker₊=3, ker₋=0, the three-generation γ⁵-graded index. The seam real→imaginary continuation of the wall-mode (W=λ√f/r real between horizons, turns imaginary past them → propagating fermion on the sinh^{2/3} leg) is the fermion riding the bead's contour — consistent with my computation. The settled bead reinforces this foundation. Restraint.
- *Hygiene noted (E.1, not fixed here — out of bead scope):* P7 is cited under **two bibkey aliases** — `JanzenCRframework` (most papers) and `JanzenFramework` (matter_sector line 227, geometric_core line 1175). A `BIBKEY_ALIAS_MAP` reconciliation item, like the `JanzenShadow`/`JanzenShadowExistence` one.

## Walk status: all 17 papers dug. The bead was **already load-bearing and largely correct corpus-wide** — Daryl's hypothesis confirmed. Fragmentation was localized (P7 rem + P2 + P5, all fixed; one soft spot P11, fixed); everything else restraint. Remaining: return to P7 (abstract/intro billing + fold the verified contour into `conj:bead`'s open core + column fill), then the coherence pass + graph.

## ⚠ Knows-itself flag (surfaced during leg 1, r973)
`CORPUS_MAP.md`'s numbered causal-order list ("The papers" section) still uses the **pre-r620 numbering** (groupoid #4, modern_parallax #5) — the r531 assignment, *before* the r620 physics-first **P4↔P5 swap**. The authoritative current numbering is the papers' own headers and `BIBKEY_ALIAS_MAP`: **P4 = `modern_parallax`, P5 = `groupoid`, P6 = `shadow`.** This stale list misled Arthur into swapping the P4/P5 labels in an earlier draft of this log (caught by Daryl, fixed r973). *Fix target for the knows-itself pass: reconcile the CORPUS_MAP causal-list item numbers with the r620 spine numbering, or annotate the list as old-numbering.* Weight to the source (the paper headers), not the map's list.

## Legs remaining
- **Leg 2 — core theory:** slicing_operator, range, canonical_time (τ̃/the lock — highest relevance), dynamics, algebroid (the C⁰ slicing P7 cites).
- **Leg 3 — cosmology/cosmogenesis/core:** CR_cosmology (the sinh^{2/3} expansion leg), cosmogenesis (fossil-of-collapse), p0/17 geometric_core (X0/the imaginary instrument).
- **Leg 4 — matter boundary last:** boundary, matter_sector.
- **Return to P7:** abstract/intro billing of `conj:bead`; fold in the two open-core constraints; P7→P6/P5 pointers; fill P7's dependency-matrix column.
- **Then:** coherence pass + graph.

## r977 — the graph (the work being done, Daryl's "nothing graphed until the work is done" satisfied)
The theorem is closed, the ontology deep-baked across all 17, the matrices regenerated — so the figure was built.
`corpus/synthesis_figure.py` rebuilt as the six-panel plate that holds the bead **as one object**: (A) the bead
as one curve on the hyperboloid (null-ruling-in → equatorial boing → null-ruling-out, r real & signed), (B) the
observer chart with the lap edge-on at the seam, (C) the honest bounded τ̃-contour (r real, |Im τ̃|≤πα/3, the lock
at the turnaround) **replacing** the old fake-cusp panel, (D) Nariai, (E) handoff, (F) lap flattened. P7 caption
rewritten to A–F; thm:bead pointer (f)→(F); P7 clean at 38pp. Figure now agrees with the closed text.

## r980 — three frames, the photon crossing, and the lap-as-cusp-smoothing
The figure campaign turned into physics. Findings (full record: SESSION_FIGURES_CONSOLIDATION.md): the two
bundles read correctly (blue A out / red B in), the τ̃→τ̃* conjugation with the 2πα/3 Wick period (e^{2πi/3},
the groupoid triple), the photons crossing the seam into the collapse side (signed r, verified), the r-τ̃
collapse (r globally a function of τ̃ → all bundles coincide, separate only off the real axis), and the lap as
the analytic rounding of the photon's real seam cusp. Two receipts pass (bead_conjugate.py, photon_cross_test.py).
Open lead: the sheet-assignment (which bundle rides which cube-root branch) + the double-speed question. Next:
place the new figure, re-bake fig:dS_SdS, fold conjugation + photon-crossing into P7 text.
