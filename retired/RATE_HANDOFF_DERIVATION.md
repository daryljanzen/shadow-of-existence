> **⌗ RETIRED r2380 under `RG-1` — IT ANSWERED ITS QUESTION AND THE ANSWER IS A PAPER SECTION'S TITLE.**
> *It was opened r899 on P16 §network's r898 open item:* ***"the nucleosynthesis window runs at the standard,
> radiation-sourced Friedmann rate, while the observable universe runs at the radiation-free Λ-set rate. Why
> does radiation 'stop sourcing' the rate at the branch point? Asserted scoping, or derivable?"***
>
> ***DERIVABLE — and P16's section is now titled `The scoping of the two rates, DERIVED` (`sec:scoping`).***
> *Its answer survives in the corpus's own voice and is sharper there:* **radiation never "stops sourcing" the
> rate, because on CR's ontology it never sourced any rate** — *what changes at the branch point is whose
> stacking the matter's history is strung on. Assembled from four already-proven results (P8's leaf/stacking
> separation, P10's lapse as the metrical rate of advance, P15's leftward reading), and that assembly is the
> general L1↔L2 rule at `ONTOLOGY_FOUNDATION_INDEX` §1·LEVELS, with the fatal-both-ways scoping stated there.*
>
> **⌗ WHY THIS RETIREMENT IS CLEAN:** *the document asked whether a scoping was **asserted or derived**, and the
> corpus now says **derived** in a section heading. **A question answered into a title is a question that no
> longer needs its working file.***

---

> **[†ONT-COSMO] Cosmological grounding — read before computing.** Whenever this doc's work touches the expansion rate, the early universe, or the seam, the forced model to plug in — **not** an FLRW default — is held whole in `ONTOLOGY_FOUNDATION_INDEX.md` §1b: the *observable* rate is radiation-free sinh^{2/3} (Nariai proper frame, fixed by Λ, **read leftward** — radiation/matter inherited content, never a rate source); the *progenitor/local* dynamics is ordinary GR at the standard Friedmann rate (where BBN runs, below the ~1.6 eV onset); the two regimes are distinct and meet at the reassignment seam; the beginning is the branch point at $r=0$ (well-posed because $r_*$ CONVERGES, **not** because curvature is finite -- it diverges there; r2234), not the seam. Conflating the two regimes is the veer that thrashes.

# The rate handoff at the branch point — derived from the structural transition law (r899, 2026-07-08)

**The question** (P16 §network's r898 open item): the nucleosynthesis window runs at the standard, radiation-sourced Friedmann rate, while the observable universe runs at the radiation-free Λ-set rate. Why does radiation "stop sourcing" the rate at the branch point? Asserted scoping, or derivable?

**The answer: it is derived — by assembly of four results already proven in the corpus.** Radiation never "stops sourcing" the rate, because on CR's ontology it never sourced any rate. What changes at the branch point is *whose stacking the matter's history is strung on*. Geometry first, no imposed dynamics:

## The assembly

**1. The rate is a property of the stacking, not of the leaf content.**
P8 (prop. *Leaf and stacking*, proven): the slicing operator's data separate — the **leaf** carries the density (ρ = m′/4πr², leaf-carried and **lapse-independent**), the **stacking** (the lapse) carries the pressure and the time-spacing of the leaves. P10: on the forced cosmic foliation the lapse is "the metrical rate of the existing layer's advance," and the deparametrized constraint yields the **true Hamiltonian that generates the advance** — the rate *is* the canonical structure of the stacking. P15 (§properframe, line 139, verbatim): "radiation, like matter, is inherited content read off that clock, not a term that sources the rate… The objection that a perturbing radiation fluid 'must gravitate and so alter the rate' is a category error against a construction that reads [the Friedmann equation] leftward: the rate is set, and the fluid's energy is among the contents the set rate carries."

**2. The deep leg's rate is the progenitor-side local dynamics — and standard where it matters.**
The heat–turnaround–cool excursion is a collapse inside the previous universe: the regime where GR's rightward reading is the valid layered phenomenology (a local process embedded in an ambient foliation — the progenitor's, not ours). r837 (banked, receipts): the collapse leg at density ρ is the FLRW time-reverse at equal density, so |ℋ(ρ)| is the standard Friedmann value with the actual content — radiation included — hence exactly the standard rate through the nuclear window (P16 §rate). No cosmological rate of *ours* exists yet for this matter to obey.

**3. The reassignment re-founds the stacking, and only the stacking.**
P7 (line 838, verbatim, grounded in P8's proposition): "the reassignment preserves the cosmic foliation and the density is leaf-carried and lapse-independent, so **the reassignment acts on the time-stacking, fixing the Λ-set rate, and not on the leaf**." This is not a stipulation: because the density is *proven* lapse-independent, the stacking is the only datum the reassignment *can* act on while preserving the foliation — and the Null-Boundary Correspondence fixes the new stacking's rate to the Λ-set sinh²ᐟ³ law (P8: amplitude and rate both Λ-set at the Nariai tangency). The leaf data — composition, spectrum, the entropy datum ρ_r/ρ_m — cross faithfully (κ=0 transmission, P15 prop:transmission: the degenerate horizon imprints no scale). The rate does not cross at all, because it was never leaf data.

**4. Consistency: what must match, and what may jump.**
The only objects existing on both sides of the seam are leaf data — exactly what κ=0 transmits. The rate is one-sided by construction. Quantified: at the onset datum (ρ_r/ρ_m ≈ 2, z ≈ 6850, Λ negligible), the rightward local readout would be √(1 + ρ_r/ρ_m) ≈ **√3 ≈ 1.73** times the geometric rate — a finite, order-unity re-founding, absorbed entirely by the reassignment. **Audit — no observable straddles the jump:** the abundances are fixed in the window (deep side, standard rate; BBN complete by ~30 keV ≫ 1.6 eV); the neutrino-background composition is deep-side leaf content; the acoustic spectrum is a transmitted datum plus observable-side evolution (the deep-side driving handled by the banked time-reversal equality, r831); the sound-horizon and distance integrals begin at the onset (observable side, geometric rate); and the ρ_r/ρ_m datum itself is the handover measurement. Nothing computes across the jump; a rate re-founding no observable straddles is not a discontinuity in any prediction — it is the seam doing what the transition law says it does.

## What this closes and what remains

**Closed:** the scoping is a theorem-level consequence of the transition law, not an assertion — the switch is located at the branch point because that is where the stacking changes hands (P7/NBC); the deep rate is standard because the excursion is progenitor-side local dynamics (r837); the observable rate is radiation-free because the new stacking is geometric (P8/P15); the consistency requirement is leaf-data continuity only, already established (κ=0).

**Remains (already named, unchanged):** the full worldline-and-field *dynamics* of the crossing — the frontier P7 itself names (this derivation is structural/kinematic); the digit-level full-network integration; the derivation-vs-measurement of the inherited datum.

**Sources, verified at source this session:** P7 CR_framework.tex ~line 838 (transition law); P8 slicing_operator.tex prop:lapse + abstract (leaf/stacking separation; Λ-set rate at Nariai tangency); P10 canonical_time.tex abstract + §deparam (true Hamiltonian as the advance's generator); P15 CR_cosmology.tex line ~139 (leftward reading, category error) + prop:transmission; P16 cosmogenesis_paper.tex §rate (r837); the r898 scoping passage this derives.
